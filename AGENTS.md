# AGENTS.md — Loom

供 AI coding agents（Claude Code / Codex / Cursor / Copilot 等）在本仓库工作时自动读取。

## 项目概览

Loom：私域内容生产白名单平台（SaaS 后台）。核心是一条 **13 段链（CHAIN_13）** 内容生产链——产品录入 → 冷启动建模 → 字段池规划 → 字段下原子 → PWC 条件包 → PWS 冻结 → 平台适配 → PCP → 策略/结构/表达三包 → 合规清洗 → `final_id` 组装 → 内容生成 → 反馈回流。AI 只产候选、人工 Gate 裁决、`final_id` 唯一出口（E1.1 publishFCW）、反馈回流闭环反哺知识。

**当前阶段**：设计/文档已定稿；代码已进入实现阶段——V1 主链 段1→6→10→11 闭合，V1 任务包 M0–M12 各切片、V2 P4 段12 首片（Q116）、Q118 一致性收口（4 个漏网 GET 补闸 / publish_slots.gate V1 人工直编口径 / 迁移 0027 两 FK / CI 七 checker）与 Q119 段12 多语言 Q58（content_languages 语言清单配置化 / 发布位市场∩产品目标语言交集 / 每语言独立成品唯一约束，迁移 0028）、Q120 段12 AI 质量分 Q57 + 重生成上限 Q56（模型网关第 8 场景 ARTICLE-QC 内嵌生成、纯 advisory 不阻断发证、阈值 content.ai_quality_threshold 与上限 content.regen_limit 配置化，迁移 0029/0030）、Q121 复检第②项语义级检测（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK 内嵌、纯 advisory 不抬 block_required/不阻断 approve、落 review_hits.semantic，迁移 0031 纯种子）、Q122 客户「内容生产与发布」功能页 + Q56-a 客户人工改稿（无闸列表/详情/PATCH body 三端点、manual_resubmit revising→review、前端内容列表/详情/审阅/改稿岛与第八 checker check-content，零迁移；三接缝交互超时按推荐甲落地待负责人追认，02 C1.66）、Q123 段1 客户目标语言录入控件（客户 PATCH /api/intakes/{id}/target-languages + 无闸 GET /api/content/languages、ProductSpaceView 回显，零迁移，02 C1.67）、Q124 难产骨架作废回池 Q56-b（第七态 discarded 终态、discard 端点 reason 必填、needs-attention 队列、partial unique index WHERE status<>'discarded' 回池，迁移 0032，02 C1.68）、Q125 运营发布回填 Q60c（publish-info PUT + ready-to-publish 队列、发布三列、不新增 published 态，迁移 0033，管理端 /admin/content 内容运营台，02 C1.69）、Q126 段13 反馈回流入站第一片 effect-callback（`POST /api/effect-callback` 复用 Q88 Agent Key Bearer 为**首个受 Agent Key 保护的业务端点**、新建 `app/core/effects` 与 `effect_records` 时序表按 status=matched/orphan 分流、仅精确匹配非 discarded 成品、metrics 缺席记 NULL 绝不补 0、(content_id,captured_at) 幂等覆盖/异点追加、运营只读孤儿队列与成品时序两口带 query actor 闸、整批 all-or-nothing + effect.batch_received 审计，迁移 0034 新建表，02 C1.70）、Q127 Q60a 孤儿人工认领（`POST /api/admin/effects/claims` operations 写口硬闸、`effect_claims` 持久映射 external_content_id→content_id + effect_records 加 claimed_by/claimed_at 两溯源列不新增第三状态、认领后同 ID 推送经映射兜底不回落孤儿、支持改绑重指、effect.claimed 审计，迁移 0035，02 C1.71）、Q128 customer-backfill 客户专用通道（`POST /api/effects/backfill` **无 Agent Key**、body {tenant_id,records,actor} 同 Q122 客户写口、source 服务端固定、只命中本租户非 discarded 成品否则整批 422、**绝不产生孤儿**、effect.customer_backfilled 审计 tenant=客户租户，零迁移，02 C1.72）、Q129 孤儿批量认领 + 取消认领/解绑（claims/batch 整批 all-or-nothing、claims/unclaim 删映射并把旧映射目标行回滚 orphan、effect.claim_revoked 审计，零迁移，02 C1.73）、Q130 管理端效果回流运营台（新建 /admin/effects 为 sidebar 第 8 项：孤儿队列单条/批量认领岛 + 成品时序查询/解绑岛，check-admin 锁恰 8 项，02 C1.74）、Q131 客户内容详情页效果回填岛（消费 Q128 通道、非 discarded 成品挂载、V1 单条手工、批量/CSV 随 V2、客户 nav 不增项，check-content 扩守卫，02 C1.75）已落地，五接缝甲案**已经负责人 2026-09-20 追认**（02 C1.73–C1.75 追认销账）；**Q132** 中台导出 JSON 形态 + 异步导出任务（GET fcw.json envelope 与 CSV 同口径、POST /api/exports/jobs V1 同步落 completed + 状态口/下载口、export_jobs 不存 payload 下载按参数重渲染、审计 export.job_created，02 C1.76）；**Q133/Q134/Q135** 多副本 V2 基建三件（Q133 fencing token：`leader_lease` 取 `INCR loom:fence` 跨易主单调令牌、看门狗易主/续约故障置 lost、INCR 失败 fail-closed，02 C1.77；Q134 Redis Streams 消费组细粒度认领原语新包 `app/core/queue`〔XADD/XREADGROUP `>`/XCLAIM 崩溃接管/ACK/死信〕，只落原语不接 worker、不落 env，02 C1.78；Q135 配置缓存多副本 pub/sub 失效广播 + ConfigBroadcastSubscriber 收消息全量 reload、env `LOOM_CONFIG_CACHE_BROADCAST_ENABLED` 默认关、发布 best-effort，02 C1.79；三件零迁移）；**Q136** 客户效果批量 CSV 回填（内容详情页非 discarded 成品在单条岛下新增批量岛 backfill-batch-island.tsx：纯前端 CSV 解析〔固定 9 列表头/双引号转义〕+行级校验同单条+预览前 10 行+文件/粘贴双入口、前端软上限 500 行、captured_at 转 UTC、空指标缺席，整批走 Q128 records[] 客户通道 all-or-nothing，甲案不建后端端点/无迁移/无新依赖，check-content 扩守卫，02 C1.80）；**Q137/Q138/Q139/Q140 多副本/异步生产接线四件（2026-09-20，四件零迁移、env 默认关、不改 V1 单副本默认行为）**：Q137 中台导出真后台 worker（exports 扩 worker.py `ExportWorker`：queued/running 异步态经 Redis Streams 消费组水平并行〔只读、下载按参数重渲染的幂等作业〕、read_new delivery=1 + reclaim_pending 崩溃接管、成功 ACK/未超限留 PEL/超限 move_to_dead 置 failed/终态重复幂等 ACK；service 加 create_queued/enqueue/process/fail/list_jobs 保留同步 create_export_job；router POST 门控分同步/异步 + 新增 `GET /jobs` 列表口、download 对 queued/running 409；env `LOOM_EXPORT_WORKER_ENABLED` 默认关 + `LOOM_EXPORT_STREAM_BLOCK_SECONDS`=5.0；FakeStreamsRedis 抽共享 tests/integration/stream_fakes.py，02 C1.81）；Q138 restock requested 信号 XADD **只接生产侧**（新 restock/notify.py，maybe_request_restock after_commit〔event.listen once=True〕best-effort XADD、DB requested 行是唯一事实源/DB 轮询兜底/XADD 失败只告警；**worker 主认领不切消费组**——restock 自动花真 token 而 Q87/Q89 已裁决 leader 单实例防双花，消费组水平并行语义冲突，留 V2 重评花钱隔离；env `LOOM_RESTOCK_STREAM_ENABLED` 默认关，02 C1.82）；Q139 两循环 leader_lease 协作中止（sla/scheduler._tick、restock/worker._tick、sla/router run_sweep、restock/router run_restock_once 四持锁点 leader_lock→leader_lease；run_jobs/run_restock 加零参 checkpoint 回调传 lease.raise_if_lost〔run_jobs 在每作业独立会话之外调用防被作业内 except 吞、run_restock 在每条信号处理前〕，锁中途易主抛 LockLost；两 _tick except LockLost 静默返回不拖垮循环、两手工 /run LockLost→409〔LockUnavailable 409/LockBackendError 503 不变〕；门控关 lease 恒 held 行为同 Q89、leader_lock 保留；复用 LOOM_DISTRIBUTED_LOCK_ENABLED 无新 env；下游 PG 行级 fence 条件更新仍 V2，02 C1.83）；Q140 配置缓存单 key 增量失效 + 订阅断线退避（cache.py 加 reload_keys〔in_ 一次查回、DB 已删 key 从快照 pop、原子合并〕；broadcast.poll_once 解析 payload，`*`/缺失全量 reload、具体 key 走 reload_keys〔兼容 bytes/str〕，consecutive_failures 指数退避 backoff_base 1s·2^(n-1)·封顶 30s、wait_for(stop) 可立即唤醒、成功重置，健康路径每轮 sleep(0) 零拍让出防替身/立即返回路径忙转饿死 asyncio 定时器；复用 LOOM_CONFIG_CACHE_BROADCAST_ENABLED 无新 env，02 C1.84）；**Q141** 配置缓存版本号/TTL 防陈旧（cache 快照携 per-key ConfigItem.version 版本向量、reload_keys 单调门控拒旧快照回灌，订阅器无消息超 env `LOOM_CONFIG_CACHE_TTL_SECONDS` 默认 300 回源全量 reload，零迁移，02 C1.85）、**Q142** 中台导出分页与行数硬上限（fcw.csv/fcw.json 加 limit/offset、env `LOOM_EXPORT_MAX_ROWS` 默认 10 万超限 422，JSON envelope 加 total/limit/offset/has_more、CSV 走 X-Export-* 响应头，同步/异步 worker/下载同一上限、job 不加分页列，零迁移，02 C1.86）、**Q143** 下游 PG 行级 fence 接 restock 花钱路径（新表 restock_claims 记每 requested 信号当前获准花钱最大 fence、新模块 restock/fencing.py 花钱前 claim_request 独立短事务认领/成功提交前 fence_current 条件 UPDATE 两段式，锁在模型调用期间易主则旧 leader skipped_lost 不调模型、迟到成功写被拒回滚，fence=None 行为同 Q87/Q139，迁移 0037；SLA sweep 行级 fence 仍 V2，02 C1.87）；迁移头 0037_restock_claims（物理表 59，Q128–Q131、Q133–Q135、Q137–Q142 零迁移、Q143 迁移 0037 restock_claims +1 表；0032–0037 经 pg16 容器往返实测）、后端 699 测试（678→699：Q141 +6/Q142 +8〔单测 2+集成 6〕/Q143 +7〔fencing 原语 4+restock worker 3〕，全 FakeRedis/FakeStreams/FakeBroadcast 内存替身）、eval 101/101、前端 8 个 checker（CI 八 checker 全接线，Q122 起含 check-content，Q130 扩 check-admin 至 sidebar 8 项、Q131/Q136 扩 check-content，无新 checker 文件；token 仍 86，Q132/Q133–Q135/Q137–Q143 前端零改动）。private beta 上线准备六片已落地：Q144 上线形态裁决（受控 private beta、公网自助 NO-GO、P0-① 身份层压 V2 第一项，02 C1.88）、Q145 最小部署制品（前后端 Dockerfile + entrypoint `alembic upgrade head` 先行 + compose backend 仅 expose/frontend 唯一发布口 3000，02 C1.89）、Q146 最小备份监控（db-backup 每日 pg_dump+7 天保留、日志 10m×3 轮转、watchdog 探活+webhook、frontend healthcheck，02 C1.90）、Q147 staging 彩排（docker-compose.staging.yml overlay、`-p loom-staging` 6 服务 healthy、真实 Chrome SSR 零 console 错误、段1 冒烟，02 C1.91）、Q148 真实 LLM 接线（agnes OpenAI 兼容网关 `https://apihub.agnes-ai.com/v1`/agnes-2.5-flash/USD/日预算 50 USD/8 chat 场景真模型/出站 Key Fernet，真实 CAT-RECOG 635/231 tokens；ATOM-AFFINITY 仍 synthetic、单价待供应商，02 C1.92）、Q149 beta 阈值确认（SLA 72h/黄 24h/红 48h、QC 0.85、cluster_line 0.9、G2 12 字段基线、付费档额度划掉）+ docs/19 主数据录入模板（02 C1.93）；**Q150 loom 作为 Zeus 联邦第二封臣 A2A 第一阶段 plan 模式任务层**（新横切包 `app/core/a2a/` 五件：card 三公开发现端点 `/api/a2a/agent-card`·`/.well-known/agent-card.json`·`/.well-known/agent.json`（Cache-Control 300s）、skills 三件 generate-content/compliance-check/effect-backfill 纯 plan 执行器、rpc JSON-RPC tasks/send·sendSubscribe(SSE)·get·cancel 进程内存存储 TTL 30min/上限 500、router 的 `POST /api/a2a/tasks` 复用 Q88 Agent Key Bearer 401、每调用审计 a2a.task tenant=`_platform`（input-required 也留痕）；plan 模式不触链/不花 token cost.llmTokens=0/不越 Gate；零迁移零新 env，02 C1.94；单测 12+集成 10）；**Q151 SLA sweep 写路径 PG 行级 fence**（新表 sweep_tick_claims 迁移 0038 物理表 59→60、新模块 sla/fencing.py 两段式 claim_tick（tick 前独立短事务 acquired/held/lost）/fence_current（作业同事务条件 UPDATE 门）、runner.run_jobs 加 commit_guard 门闭抛 LockLost 中止本轮、scheduler._tick 与手工 /api/admin/sla/run 先认领（lost 静默/409）再透传、fence=None 不写表行为同 V1；Q143 restock fence 的 SLA 同型收口，02 C1.95；pg16 容器 55432 往返实测，+8 集成）；**Q152 导出 worker 单进程多 consumer 并发度/批量配置**（exports/worker.py 纯工厂 build_export_workers：N 个同消费组 ExportWorker、N>1 姊妹 consumer 共享前缀带 1..N 序号、参数全透传、concurrency/count<1 启动 ValueError；env LOOM_EXPORT_WORKER_CONCURRENCY 默认 1/LOOM_EXPORT_STREAM_COUNT 默认 20 保 V1 单 worker、不入 .env.example；main 单例改 _export_workers 列表逐个 start/反序 stop；零迁移，02 C1.96；+6 集成）；**Q153 docs/10 第二轮 DBA 全量列复核**（把 0037 restock_claims/0038 sweep_tick_claims 两 fence 表纳入，一次性 pg16 容器 upgrade head 到 0038 后导出 information_schema/pg_indexes/pg_constraint 并与全量 ORM metadata 逐表逐列比对，实测 60 表/637 列/212 索引/PK60·FK32·UQ20、列漂移 0，纯文档实测、零迁移零测试新增，02 C1.97）；**Q154 真 Redis/真 PG 多副本锁易主→中止/拒写集成验证**（新增 `tests/integration/test_real_infra.py` 8 例、函数级 skipif、无 env 全 skip 不进 CI，真容器 pg16 宿主 55433＋redis7 宿主 6380 带 env 8/8 全过 52.6s，覆盖跨连接认领/升级与条件 UPDATE 拒旧 fence/真并发行锁串行化/lease 短 TTL 易主置 lost＋raise LockLost/Streams 两 consumer 分片·XACK·XCLAIM 接管·死信/pub-sub 失效广播；首跑抓到 request_id varchar(36) 被 sqlite 替身掩盖的 StringDataRightTruncation、统一 32 字符 uuid.hex，零生产代码改动零迁移，02 C1.98）；**Q155 台内白名单卡片 6 层原料包全量 JSON**（新模块 final/final_whitelist/material.py 反解析产品 PwsSnapshot/平台 PcpWeightTable+PublishSlot/策略结构表达三包 csp·cstp·cep/合规 CcrReport+LawReview 六路，只读端点 `GET /api/fcw/{final_id}/material.json`、附件下载、引用缺失回 available:false+warning 不 500；**台内卡片口径非中台 final_id-only 面**〔Q100/Q132/Q142 定论〕，前端随 D3.5 菜单不做，零迁移，02 C1.99，+3 集成）；**Q156 客户批量回填服务端化**（新 core/effects/csv_io.py 标准库 csv 解析 + `POST /api/effects/backfill/upload` 无 Agent Key、JSON body 携 CSV〔零新依赖〕、整份挂单一 content_id、复用 Q128 绝不孤儿、逐行坏行一次回 422、captured_at 强制 tz-aware、行数硬上限 env LOOM_BACKFILL_UPLOAD_MAX_ROWS 默认 10000、回执带 filename+rows[] 逐行；~~Excel/异步/前端岛切换仍 V2、三接缝甲案待追认~~ 已由 Q159–Q161 全部落地并经负责人 2026-09-22 追认，零迁移，02 C1.100，+7 集成）；**Q157 真多副本多 worker 持续负载端到端部署演练**（alembic/env.py 加 PG 咨询锁 0x4C4F4F4D 串行多副本并发迁移、新增 infra/docker-compose.ha.yml〔backend 2 副本〕+ ha-rehearsal.sh 四阶段 16 断言、uvicorn-log.json+entrypoint --log-config 修容器日志；演练抓到并修复 redis-py 8.1.0 XREADGROUP BLOCK 到期抛 redis.exceptions.TimeoutError〔RedisError 子类非内置 TimeoutError 子类〕被误判后端故障致 worker 空转不分片，block 模式两类 TimeoutError 归一返回 []、非 block fail-closed；全新库 16/16：迁移 38/0 串行、24 job 两 consumer 分片 12/12、SLA/restock 抢锁 200/409、配置广播对端 reload 并还原，synthetic 不花钱；演练本地手动不进 CI、restock 消费组并行仍 V2，零迁移，02 C1.101，test_redis_streams 8→12 +4）；**Q158 异机备份恢复端到端演练**（新增 infra/restore-rehearsal.sh：两独立一次性 pg16 容器源 55440/异机 55441 不触 atlas-pg，源库全链 alembic upgrade head 到 0038＋造 tenants/export_jobs/effect_records 覆盖 JSONB/timestamptz/可空 metrics，用 Q146 同款 pg_dump --format=custom --compress=9 离源入异机、异机 drop/create 空库后 pg_restore --no-owner；bash 层 12/12 PASS：两库版本均 0038_sweep_tick_claims、业务表 60/总 61 集合一致、全表逐表行数零差异〔非空 19 表〕、结构 indexes213/PK61/FK32/UQ20/vector 列 2/pgvector 扩展一致、关键表整行逐字段一致、config_items61/QC0.85、import app.main ORM 只读通、恢复库再 upgrade head 幂等 no-op；零迁移/零生产代码/后端测试数仍 761/前端零改动，演练本地手动不进 CI，RPO·RTO 量化达标/WAL 增量/跨区冗余/指标监控仍 V2，02 C1.102）；**Q159/Q160/Q161 客户批量回填收口三片（2026-09-22，Q156 原挂 V2 三件全销，02 C1.103–105）**：Q159 批量回填岛切服务端 CSV（纯前端：server action 提交 CSV 原文到 Q156 `/upload`、422 逐行错误结构化〔line＝含表头物理行号〕、check-content 扩守卫、解除前端 500 行软上限，零迁移）；Q160 Excel .xlsx 上传（新端点 `POST /api/effects/backfill/upload-excel` 与 CSV 同契约同回执、新模块 effects/excel_io.py 用 openpyxl `read_only` 读活动 sheet、单元格归一化后复用 csv_io 同一校验、旧 .xls/损坏文件加载期 422、依赖 openpyxl>=3.1.5、行数上限复用 env 默认 1 万、审计复用 effect.customer_backfilled、+8 集成，零迁移）；Q161 异步导入任务（新包 `app/core/imports/`、`POST/GET /api/effects/backfill/jobs`＋`GET /jobs/{job_id}`、迁移 **0039 import_jobs** 物理表 60→61〔payload Text 落 CSV 原文/xlsx base64 可重放、queued/running/completed/failed〕、ImportWorker 经 Redis Streams 消费组〔确定性校验失败 failed+ACK 不重试不进死信、基础设施异常留 PEL/XCLAIM/死信〕、门控 `LOOM_IMPORT_WORKER_ENABLED` 默认关〔关＝请求内同步跑到终态保 V1 行为、开＝异步、入流失败 fail-closed 503〕、另三旋钮 BLOCK_SECONDS=5.0/CONCURRENCY=1/STREAM_COUNT=20 不入 .env.example、审计 import.job_created/completed/failed、+17 集成＝API 8+worker 9，前端零改动）；Q156 三接缝（强制 tz-aware/行数硬上限/JSON body 零 multipart）连同三片同类工程默认（xlsx base64、payload 落库可重放、worker 门控默认关）已经负责人 2026-09-22「A和B吧，尽量搞一下」追认；迁移头 **0039_import_jobs（down=0038，物理表 61，Q159/Q160 零迁移、Q161 迁移 0039）**、后端常规 **803 passed＋8 real-infra 默认 skip**（带 LOOM_TEST_REAL_PG_DSN/LOOM_TEST_REAL_REDIS_URL 两 env 时另计 real-infra 8 例；747→750 Q155 +3、→757 Q156 +7、→761 Q157 +4、→769 Q160 +8、→786 Q161 +17〔API 8+worker 9，Q159 纯前端 0〕）、eval 101/101（golden 21）、ruff 净、前端 tsc+八 checker（Q159/Q160 扩 check-content 服务端 CSV/xlsx 守卫，无新 checker，token 仍 86）。**Q166–Q177（2026-09-22～23，02 C1.110–C1.121）**：Q166 客户效果 analytics 只读页（翻转 Q97/Q114 提前入 V1）、Q167 Agent Key 治理页、Q168 导出 job 管理页（管理端 sidebar 至 10 项）、Q169 全链 golden-path e2e、Q170 运营 SOP（docs/21）、Q171 第三轮 DBA 全量列复核（61 表/654 列/215 索引零漂移）、Q172 真 agnes 全链演练（LLM HTTP 超时配置化）、Q173 持续高压负载演练（9/9＋jobs 列表 offset 补全）、Q174 批量导入岛切异步任务轮询、Q175 RPO·RTO 量化达标（RPO 1.919s/RTO 70.111s）；**Q176** ATOM-AFFINITY KNN 索引经 grounding 判定应用层零 DB 层 KNN 查询（向量距离算子全仓零命中、affinity 按 Q86 内存确定性重算）、裸建为无消费方死索引，列甲乙丙三档，**已经负责人 2026-09-24「那你一口气搞了」按丙案（暂不做/YAGNI）裁决：不建索引、不写迁移 0040、不改 Q86，甲乙两档归档备查**（C1.120）；**Q177** D3.5 白名单组装引擎运营只读首片——管理端 /admin/fcw 跨租户分页列表＋行内六层原料包卡片（后端 `GET /api/admin/fcw` 与 `GET /api/admin/fcw/{final_id}/material` 两口、读闸 operations|platform_admin、复用 Q155 build_material_pack 但内嵌面无 attachment 头，前端 sidebar **第 11 项**、零迁移，C1.121，三接缝甲案已经负责人 2026-09-24 追认；组装工作台/6 层可视化编辑/合理性校验/审核台/冻结管理/客户卡片视图/复制 ID 等 D3.5 余项随菜单点工）；**Q178** 身份层 P0-① 甲案第一切片内部运营人员令牌 PAT（负责人经 interaction.ask 拍板甲案，02 C1.122）：新横切包 `app/core/staff_auth/` 与 staff_api_keys 表（迁移 **0040**、业务物理表 61→62、pg16 宿主 55442 往返实测），明文 `loom_staff_`+token_urlsafe(30) 与 Q88 机器 `loom_` agent_api_keys 分表分前缀不可互换、无 tenant_id 审计恒 `_platform`、可签发限五内部角色（不含 whitelist_owner）；端点 POST/GET `/api/admin/staff-keys`、revoke（platform_admin 红线）、`GET /api/auth/me`，agent-keys GET 补 internal_gate；**认证（app 级全局依赖 staff_auth_context，门控 `LOOM_STAFF_AUTH_ENABLED` 默认关，仅验 loom_staff_ Bearer 写 contextvar、坏/吊销 401、机器 loom_/客户/公开口放行）与授权（rbac.require_any_role 唯一收口，新增 NotAuthenticated 401/PermissionDenied 403，通过即就地用令牌身份覆盖自报 actor 防提权）分离、HTTP 端点签名零改动**；前端 lib/staff-auth.ts httpOnly cookie、lib/api 注入 Bearer＋管理口 401 跳 /admin/login，新建 /admin/login 与 /admin/staff-keys（sidebar **第 12 项**，login 不进 sidebar），check-admin 扩守卫段；引导＝门控关自报签发首个 platform_admin 令牌→/admin/login 录入→开门控重启，回滚＝关 env；test_staff_auth_api.py 25 例（StaticPool 共享认证/业务两 session、先关闸签发再 monkeypatch 开闸），后端常规 **842 passed＋9 skip**、eval 101/101、ruff 净、前端 tsc＋八 checker＋next build 全绿、token 86/客户 nav 8；纯加法门控默认关 V1 自报行为不变，**工程接缝（旋钮不入 .env.example、零端点签名改动、客户侧后置）待负责人追认，客户侧真实认证/actor↔tenant 绑定（Q88 口径待补）/账号密码会话仍为 V2 第一项**。效果反哺校准算法（口径【待补】）、restock 消费组水平并行（花钱隔离，Q157 只验单 leader 不双花未改造）、~~真 Redis/真 PG 多副本易主→中止/拒写集成验证~~ 已随 Q154 真容器验证（02 C1.98）、~~真多副本多 worker 持续负载端到端部署演练~~ 已随 Q157 落地（ha-rehearsal 16/16、本地手动不进 CI，02 C1.101；持续高压压测/RPO·RTO 量化达标仍 V2）、~~异机备份恢复演练~~ 已随 Q158 落地（restore-rehearsal 12/12 逐表零差异、应用可读/迁移幂等、本地手动不进 CI，02 C1.102）、~~6 层原料包全量 JSON~~ 后端台内卡片 JSON 已随 Q155 落地（台内卡片前端 UI 随 D3.5 菜单，02 C1.99）、~~批量导入服务端上传端点/逐行回执/Excel/异步导入/前端岛切换~~ 已随 Q156＋Q159–Q161 全部落地（02 C1.100、C1.103–105，接缝经负责人 2026-09-22 追认）、A2A 第二阶段（任务持久化跨实例/真 LLM 与链路执行/Zeus↔loom 真机联调，Zeus 侧 C2 待定）随 V2，须点工；private beta 验收唯一剩余阻塞＝业务方按 docs/19 模板回填首批主数据（publish_slots/PCP/三包，禁工程臆造）。当前基线、待办与逐片记录一律以 [handoff.md](handoff.md)「当前状态/待办」与 `docs/02` C1 日志为准，本文件不复述数字。

## 文档体系与治理规则（必须先读）

- **入口**：先读 [handoff.md](handoff.md)（当前状态/待办），再读 [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md)（场景导航 + 治理规则）。
- **handoff 归档惯例（2026-09-18）**：`handoff.md` 只保留项目当前状态 + 活跃待办 + 最近 5 条进度；更早的进度条目与已销账待办详细过程整块原文归档至 `docs/handoff-archive-YYYY-MM-DD.md`（可作历史检索，逐条一字未改），权威逐条台账仍是 docs/02（C1 日志）/ 08 / 16。
- **唯一事实源** = `docs/` 全部文档；原 `v3.md` 已删除，一切规格以 docs 为准。
- **修改流程**：内容变更先改 docs 对应文档 → 新决策追加 Q 编号（02 文档 C1 日志）→ 涉及展示层再同步渲染（09）。
- **两套口径**：13 段链（权威，01 PRD）与 03-A~E 展示口径（09 全景）并存不混用，冲突以 01 为准；路线图口径合并属裁决事项，不得擅自合并。
- **行号溯源（Q115 收口）**：`line NNNN` 引用指向基准文件 `Docs/Loom_后台_V6.0-需求说明（不是原型).html`；该文件经负责人 2026-09-17 确认**已永久丢失、无法找回**，故行号引用**降级为历史痕迹**，不再作为可核对基准，改动时无需（也无法）同步核对。

## 事实纪律

- 每个数字/状态/结论必须来自 docs 文档原文（可溯到 Q 编号或 line 行号）；**禁止编造业务事实**。
- 文档空缺处一律标【原文未给出，待补】，不得猜测填充。
- 状态词口径：✅ 权威 / 🟡 待补 / ⬜ 待拍板——沿用现有标注，不静默升级状态。

## 待裁决事项（不擅自裁决）

**截至 2026-09-18 无待裁决事项**：

- ~~两套路线图口径合并~~ 已于 2026-09-13 裁决（Q73，08 §1.3：A7 为骨 + D9 厚度，段 12 后置 V2，V1=段 1→6→10→11）。
- ~~2026-09-17 Q117 审计挂账的 4 项实现差异~~ 已于 2026-09-18 由负责人「一口气开搞」均按推荐（甲）拍板并落地，记 **Q118（02 C1.62）**：① 4 个漏网管理面 GET 补 query actor 闸；② `publish_slots.gate` 回填 V1 仅 Q42 人工直编口径；③ `product_spaces` 两软引用补 DB 级 FK（迁移 0027）；④ CI 接入第 7 个 checker check-settings；审计外另修 Alembic 版本表列长缺陷。详见 [handoff.md](handoff.md) 待办 6（已销账）与 02 C1.62。

> M0（S 级红旗 4 条 + 业务方素材采纳）已于 2026-09-13 完成，见 02 中 Q2/Q22/Q22a/Q22b/Q60 转正注记与 C1.16 素材确认；04 §3 另有 4 个实现时挂账子项，非待裁决事项。
> 技术选型 6 项已于 2026-09-13 拍板定稿（14 为正式实现依据）；15/17 等下游文档已同步刷新。
> 今后新出现的挂起事项仍必须由用户/负责人拍板，禁止静默替换或使用默认值，并在 02 追加 Q 记录。

## 常用入口

| 任务 | 读 |
|---|---|
| 了解全貌 | README.md → handoff.md → docs/README 文档地图 |
| 落数据模型/API/状态机代码 | docs/04 · 05 · 06（契约层） |
| 写测试 | docs/16（分层测试策略） |
| 排迭代 | docs/08（V1/V2/V3 任务包，V1=M1–M8+M10/M10-Q/M11/M12，验收标准引用 Guard/Q 编号） |
| 查技术选型 | docs/14（✅ 2026-09-13 已拍板定稿，可作实现依据；15/17 已同步） |

## 不要做的事

- 不要改动 docs 中标注"待拍板/待补"的文档状态，除非对应裁决已发生并在 02 追加 Q 记录。
- 不要拿摘要/二手口径替代 docs 原文。
- 不要删除或移动 docs 文档（唯一事实源，先改文档再谈其他）。
- 不要提交 `.env`、密钥或任何凭证；仓库**已 git init**（分支 `dev`/`main`，origin 为 GitHub），提交规范见 [git-commit-message.md](git-commit-message.md)。
