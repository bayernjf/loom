# AGENTS.md — Loom

供 AI coding agents（Claude Code / Codex / Cursor / Copilot 等）在本仓库工作时自动读取。

## 项目概览

Loom：私域内容生产白名单平台（SaaS 后台）。核心是一条 **13 段链（CHAIN_13）** 内容生产链——产品录入 → 冷启动建模 → 字段池规划 → 字段下原子 → PWC 条件包 → PWS 冻结 → 平台适配 → PCP → 策略/结构/表达三包 → 合规清洗 → `final_id` 组装 → 内容生成 → 反馈回流。AI 只产候选、人工 Gate 裁决、`final_id` 唯一出口（E1.1 publishFCW）、反馈回流闭环反哺知识。

**当前阶段**：设计/文档已定稿；代码已进入实现阶段——V1 主链 段1→6→10→11 闭合，V1 任务包 M0–M12 各切片、V2 P4 段12 首片（Q116）、Q118 一致性收口（4 个漏网 GET 补闸 / publish_slots.gate V1 人工直编口径 / 迁移 0027 两 FK / CI 七 checker）与 Q119 段12 多语言 Q58（content_languages 语言清单配置化 / 发布位市场∩产品目标语言交集 / 每语言独立成品唯一约束，迁移 0028）、Q120 段12 AI 质量分 Q57 + 重生成上限 Q56（模型网关第 8 场景 ARTICLE-QC 内嵌生成、纯 advisory 不阻断发证、阈值 content.ai_quality_threshold 与上限 content.regen_limit 配置化，迁移 0029/0030）、Q121 复检第②项语义级检测（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK 内嵌、纯 advisory 不抬 block_required/不阻断 approve、落 review_hits.semantic，迁移 0031 纯种子）、Q122 客户「内容生产与发布」功能页 + Q56-a 客户人工改稿（无闸列表/详情/PATCH body 三端点、manual_resubmit revising→review、前端内容列表/详情/审阅/改稿岛与第八 checker check-content，零迁移；三接缝按推荐甲落地并**已经负责人 2026-09-19 追认销账**，02 C1.66【追认销账】条）、Q123 段1 客户目标语言录入控件（客户 PATCH /api/intakes/{id}/target-languages + 无闸 GET /api/content/languages、ProductSpaceView 回显，零迁移，02 C1.67）、Q124 难产骨架作废回池 Q56-b（第七态 discarded 终态、discard 端点 reason 必填、needs-attention 队列、partial unique index WHERE status<>'discarded' 回池，迁移 0032，02 C1.68）、Q125 运营发布回填 Q60c（publish-info PUT + ready-to-publish 队列、发布三列、不新增 published 态，迁移 0033，管理端 /admin/content 内容运营台，02 C1.69）、Q126 段13 反馈回流入站第一片 effect-callback（`POST /api/effect-callback` 复用 Q88 Agent Key Bearer 为**首个受 Agent Key 保护的业务端点**、新建 `app/core/effects` 与 `effect_records` 时序表按 status=matched/orphan 分流、仅精确匹配非 discarded 成品、metrics 缺席记 NULL 绝不补 0、(content_id,captured_at) 幂等覆盖/异点追加、运营只读孤儿队列与成品时序两口带 query actor 闸、整批 all-or-nothing + effect.batch_received 审计，迁移 0034 新建表，02 C1.70）、Q127 Q60a 孤儿人工认领（`POST /api/admin/effects/claims` operations 写口硬闸、`effect_claims` 持久映射 external_content_id→content_id + effect_records 加 claimed_by/claimed_at 两溯源列不新增第三状态、认领后同 ID 推送经映射兜底不回落孤儿、支持改绑重指、effect.claimed 审计，迁移 0035，02 C1.71）、Q128 customer-backfill 客户专用通道（`POST /api/effects/backfill` **无 Agent Key**、body {tenant_id,records,actor} 同 Q122 客户写口、source 服务端固定、只命中本租户非 discarded 成品否则整批 422、**绝不产生孤儿**、effect.customer_backfilled 审计 tenant=客户租户，零迁移，02 C1.72）、Q129 孤儿批量认领 + 取消认领/解绑（claims/batch 整批 all-or-nothing、claims/unclaim 删映射并把旧映射目标行回滚 orphan、effect.claim_revoked 审计，零迁移，02 C1.73）、Q130 管理端效果回流运营台（新建 /admin/effects 为 sidebar 第 8 项：孤儿队列单条/批量认领岛 + 成品时序查询/解绑岛，check-admin 锁恰 8 项，02 C1.74）、Q131 客户内容详情页效果回填岛（消费 Q128 通道、非 discarded 成品挂载、V1 单条手工、批量/CSV 随 V2、客户 nav 不增项，check-content 扩守卫，02 C1.75）已落地，五接缝甲案**已经负责人 2026-09-20 追认**（02 C1.73–C1.75 追认销账）；**Q132** 中台导出 JSON 形态 + 异步导出任务（GET fcw.json envelope 与 CSV 同口径、POST /api/exports/jobs V1 同步落 completed + 状态口/下载口、export_jobs 不存 payload 下载按参数重渲染、审计 export.job_created，02 C1.76）；**Q133/Q134/Q135** 多副本 V2 基建三件（Q133 fencing token：`leader_lease` 取 `INCR loom:fence` 跨易主单调令牌、看门狗易主/续约故障置 lost、INCR 失败 fail-closed，02 C1.77；Q134 Redis Streams 消费组细粒度认领原语新包 `app/core/queue`〔XADD/XREADGROUP `>`/XCLAIM 崩溃接管/ACK/死信〕，只落原语不接 worker、不落 env，02 C1.78；Q135 配置缓存多副本 pub/sub 失效广播 + ConfigBroadcastSubscriber 收消息全量 reload、env `LOOM_CONFIG_CACHE_BROADCAST_ENABLED` 默认关、发布 best-effort，02 C1.79；三件零迁移）；**Q136** 客户效果批量 CSV 回填（内容详情页非 discarded 成品在单条岛下新增批量岛 backfill-batch-island.tsx：纯前端 CSV 解析〔固定 9 列表头/双引号转义〕+行级校验同单条+预览前 10 行+文件/粘贴双入口、前端软上限 500 行、captured_at 转 UTC、空指标缺席，整批走 Q128 records[] 客户通道 all-or-nothing，甲案不建后端端点/无迁移/无新依赖，check-content 扩守卫，02 C1.80）；**Q137/Q138/Q139/Q140 多副本/异步生产接线四件（2026-09-20，四件零迁移、env 默认关、不改 V1 单副本默认行为）**：Q137 中台导出真后台 worker（exports 扩 worker.py `ExportWorker`：queued/running 异步态经 Redis Streams 消费组水平并行〔只读、下载按参数重渲染的幂等作业〕、read_new delivery=1 + reclaim_pending 崩溃接管、成功 ACK/未超限留 PEL/超限 move_to_dead 置 failed/终态重复幂等 ACK；service 加 create_queued/enqueue/process/fail/list_jobs 保留同步 create_export_job；router POST 门控分同步/异步 + 新增 `GET /jobs` 列表口、download 对 queued/running 409；env `LOOM_EXPORT_WORKER_ENABLED` 默认关 + `LOOM_EXPORT_STREAM_BLOCK_SECONDS`=5.0；FakeStreamsRedis 抽共享 tests/integration/stream_fakes.py，02 C1.81）；Q138 restock requested 信号 XADD **只接生产侧**（新 restock/notify.py，maybe_request_restock after_commit〔event.listen once=True〕best-effort XADD、DB requested 行是唯一事实源/DB 轮询兜底/XADD 失败只告警；**worker 主认领不切消费组**——restock 自动花真 token 而 Q87/Q89 已裁决 leader 单实例防双花，消费组水平并行语义冲突，留 V2 重评花钱隔离；env `LOOM_RESTOCK_STREAM_ENABLED` 默认关，02 C1.82）；Q139 两循环 leader_lease 协作中止（sla/scheduler._tick、restock/worker._tick、sla/router run_sweep、restock/router run_restock_once 四持锁点 leader_lock→leader_lease；run_jobs/run_restock 加零参 checkpoint 回调传 lease.raise_if_lost〔run_jobs 在每作业独立会话之外调用防被作业内 except 吞、run_restock 在每条信号处理前〕，锁中途易主抛 LockLost；两 _tick except LockLost 静默返回不拖垮循环、两手工 /run LockLost→409〔LockUnavailable 409/LockBackendError 503 不变〕；门控关 lease 恒 held 行为同 Q89、leader_lock 保留；复用 LOOM_DISTRIBUTED_LOCK_ENABLED 无新 env；下游 PG 行级 fence 条件更新仍 V2，02 C1.83）；Q140 配置缓存单 key 增量失效 + 订阅断线退避（cache.py 加 reload_keys〔in_ 一次查回、DB 已删 key 从快照 pop、原子合并〕；broadcast.poll_once 解析 payload，`*`/缺失全量 reload、具体 key 走 reload_keys〔兼容 bytes/str〕，consecutive_failures 指数退避 backoff_base 1s·2^(n-1)·封顶 30s、wait_for(stop) 可立即唤醒、成功重置，健康路径每轮 sleep(0) 零拍让出防替身/立即返回路径忙转饿死 asyncio 定时器；复用 LOOM_CONFIG_CACHE_BROADCAST_ENABLED 无新 env，02 C1.84）；**Q141** 配置缓存版本号/TTL 防陈旧（cache 快照携 per-key ConfigItem.version 版本向量、reload_keys 单调门控拒旧快照回灌，订阅器无消息超 env `LOOM_CONFIG_CACHE_TTL_SECONDS` 默认 300 回源全量 reload，零迁移，02 C1.85）、**Q142** 中台导出分页与行数硬上限（fcw.csv/fcw.json 加 limit/offset、env `LOOM_EXPORT_MAX_ROWS` 默认 10 万超限 422，JSON envelope 加 total/limit/offset/has_more、CSV 走 X-Export-* 响应头，同步/异步 worker/下载同一上限、job 不加分页列，零迁移，02 C1.86）、**Q143** 下游 PG 行级 fence 接 restock 花钱路径（新表 restock_claims 记每 requested 信号当前获准花钱最大 fence、新模块 restock/fencing.py 花钱前 claim_request 独立短事务认领/成功提交前 fence_current 条件 UPDATE 两段式，锁在模型调用期间易主则旧 leader skipped_lost 不调模型、迟到成功写被拒回滚，fence=None 行为同 Q87/Q139，迁移 0037；SLA sweep 行级 fence 仍 V2，02 C1.87）；迁移头 0037_restock_claims（物理表 59，Q128–Q131、Q133–Q135、Q137–Q142 零迁移、Q143 迁移 0037 restock_claims +1 表；0032–0037 经 pg16 容器往返实测）、后端 699 测试（678→699：Q141 +6/Q142 +8〔单测 2+集成 6〕/Q143 +7〔fencing 原语 4+restock worker 3〕，全 FakeRedis/FakeStreams/FakeBroadcast 内存替身）、eval 101/101、前端 8 个 checker（CI 八 checker 全接线，Q122 起含 check-content，Q130 扩 check-admin 至 sidebar 8 项、Q131/Q136 扩 check-content，无新 checker 文件；token 仍 86，Q132/Q133–Q135/Q137–Q143 前端零改动）。private beta 上线准备六片已落地：Q144 上线形态裁决（受控 private beta、公网自助 NO-GO、P0-① 身份层压 V2 第一项，02 C1.88）、Q145 最小部署制品（前后端 Dockerfile + entrypoint `alembic upgrade head` 先行 + compose backend 仅 expose/frontend 唯一发布口 3000，02 C1.89）、Q146 最小备份监控（db-backup 每日 pg_dump+7 天保留、日志 10m×3 轮转、watchdog 探活+webhook、frontend healthcheck，02 C1.90）、Q147 staging 彩排（docker-compose.staging.yml overlay、`-p loom-staging` 6 服务 healthy、真实 Chrome SSR 零 console 错误、段1 冒烟，02 C1.91）、Q148 真实 LLM 接线（agnes OpenAI 兼容网关 `https://apihub.agnes-ai.com/v1`/agnes-2.5-flash/USD/日预算 50 USD/8 chat 场景真模型/出站 Key Fernet，真实 CAT-RECOG 635/231 tokens；ATOM-AFFINITY 仍 synthetic、单价待供应商，02 C1.92）、Q149 beta 阈值确认（SLA 72h/黄 24h/红 48h、QC 0.85、cluster_line 0.9、G2 12 字段基线、付费档额度划掉）+ docs/19 主数据录入模板（02 C1.93）；**Q150 loom 作为 Zeus 联邦第二封臣 A2A 第一阶段 plan 模式任务层**（新横切包 `app/core/a2a/` 五件：card 三公开发现端点 `/api/a2a/agent-card`·`/.well-known/agent-card.json`·`/.well-known/agent.json`（Cache-Control 300s）、skills 三件 generate-content/compliance-check/effect-backfill 纯 plan 执行器、rpc JSON-RPC tasks/send·sendSubscribe(SSE)·get·cancel 进程内存存储 TTL 30min/上限 500、router 的 `POST /api/a2a/tasks` 复用 Q88 Agent Key Bearer 401、每调用审计 a2a.task tenant=`_platform`（input-required 也留痕）；plan 模式不触链/不花 token cost.llmTokens=0/不越 Gate；零迁移零新 env，02 C1.94；单测 12+集成 10）；**Q151 SLA sweep 写路径 PG 行级 fence**（新表 sweep_tick_claims 迁移 0038 物理表 59→60、新模块 sla/fencing.py 两段式 claim_tick（tick 前独立短事务 acquired/held/lost）/fence_current（作业同事务条件 UPDATE 门）、runner.run_jobs 加 commit_guard 门闭抛 LockLost 中止本轮、scheduler._tick 与手工 /api/admin/sla/run 先认领（lost 静默/409）再透传、fence=None 不写表行为同 V1；Q143 restock fence 的 SLA 同型收口，02 C1.95；pg16 容器 55432 往返实测，+8 集成）；**Q152 导出 worker 单进程多 consumer 并发度/批量配置**（exports/worker.py 纯工厂 build_export_workers：N 个同消费组 ExportWorker、N>1 姊妹 consumer 共享前缀带 1..N 序号、参数全透传、concurrency/count<1 启动 ValueError；env LOOM_EXPORT_WORKER_CONCURRENCY 默认 1/LOOM_EXPORT_STREAM_COUNT 默认 20 保 V1 单 worker、不入 .env.example；main 单例改 _export_workers 列表逐个 start/反序 stop；零迁移，02 C1.96；+6 集成）；**Q153 docs/10 第二轮 DBA 全量列复核**（把 0037 restock_claims/0038 sweep_tick_claims 两 fence 表纳入，一次性 pg16 容器 upgrade head 到 0038 后导出 information_schema/pg_indexes/pg_constraint 并与全量 ORM metadata 逐表逐列比对，实测 60 表/637 列/212 索引/PK60·FK32·UQ20、列漂移 0，纯文档实测、零迁移零测试新增，02 C1.97）；**Q154 真 Redis/真 PG 多副本锁易主→中止/拒写集成验证**（新增 `tests/integration/test_real_infra.py` 8 例、函数级 skipif、无 env 全 skip 不进 CI，真容器 pg16 宿主 55433＋redis7 宿主 6380 带 env 8/8 全过 52.6s，覆盖跨连接认领/升级与条件 UPDATE 拒旧 fence/真并发行锁串行化/lease 短 TTL 易主置 lost＋raise LockLost/Streams 两 consumer 分片·XACK·XCLAIM 接管·死信/pub-sub 失效广播；首跑抓到 request_id varchar(36) 被 sqlite 替身掩盖的 StringDataRightTruncation、统一 32 字符 uuid.hex，零生产代码改动零迁移，02 C1.98）；**Q155 台内白名单卡片 6 层原料包全量 JSON**（新模块 final/final_whitelist/material.py 反解析产品 PwsSnapshot/平台 PcpWeightTable+PublishSlot/策略结构表达三包 csp·cstp·cep/合规 CcrReport+LawReview 六路，只读端点 `GET /api/fcw/{final_id}/material.json`、附件下载、引用缺失回 available:false+warning 不 500；**台内卡片口径非中台 final_id-only 面**〔Q100/Q132/Q142 定论〕，前端随 D3.5 菜单不做，零迁移，02 C1.99，+3 集成）；**Q156 客户批量回填服务端化**（新 core/effects/csv_io.py 标准库 csv 解析 + `POST /api/effects/backfill/upload` 无 Agent Key、JSON body 携 CSV〔零新依赖〕、整份挂单一 content_id、复用 Q128 绝不孤儿、逐行坏行一次回 422、captured_at 强制 tz-aware、行数硬上限 env LOOM_BACKFILL_UPLOAD_MAX_ROWS 默认 10000、回执带 filename+rows[] 逐行；~~Excel/异步/前端岛切换仍 V2、三接缝甲案待追认~~ 已由 Q159–Q161 全部落地并经负责人 2026-09-22 追认，零迁移，02 C1.100，+7 集成）；**Q157 真多副本多 worker 持续负载端到端部署演练**（alembic/env.py 加 PG 咨询锁 0x4C4F4F4D 串行多副本并发迁移、新增 infra/docker-compose.ha.yml〔backend 2 副本〕+ ha-rehearsal.sh 四阶段 16 断言、uvicorn-log.json+entrypoint --log-config 修容器日志；演练抓到并修复 redis-py 8.1.0 XREADGROUP BLOCK 到期抛 redis.exceptions.TimeoutError〔RedisError 子类非内置 TimeoutError 子类〕被误判后端故障致 worker 空转不分片，block 模式两类 TimeoutError 归一返回 []、非 block fail-closed；全新库 16/16：迁移 38/0 串行、24 job 两 consumer 分片 12/12、SLA/restock 抢锁 200/409、配置广播对端 reload 并还原，synthetic 不花钱；演练本地手动不进 CI、restock 消费组并行仍 V2，零迁移，02 C1.101，test_redis_streams 8→12 +4）；**Q158 异机备份恢复端到端演练**（新增 infra/restore-rehearsal.sh：两独立一次性 pg16 容器源 55440/异机 55441 不触 atlas-pg，源库全链 alembic upgrade head 到 0038＋造 tenants/export_jobs/effect_records 覆盖 JSONB/timestamptz/可空 metrics，用 Q146 同款 pg_dump --format=custom --compress=9 离源入异机、异机 drop/create 空库后 pg_restore --no-owner；bash 层 12/12 PASS：两库版本均 0038_sweep_tick_claims、业务表 60/总 61 集合一致、全表逐表行数零差异〔非空 19 表〕、结构 indexes213/PK61/FK32/UQ20/vector 列 2/pgvector 扩展一致、关键表整行逐字段一致、config_items61/QC0.85、import app.main ORM 只读通、恢复库再 upgrade head 幂等 no-op；零迁移/零生产代码/后端测试数仍 761/前端零改动，演练本地手动不进 CI，RPO·RTO 量化达标/WAL 增量/跨区冗余/指标监控仍 V2，02 C1.102）；**Q159/Q160/Q161 客户批量回填收口三片（2026-09-22，Q156 原挂 V2 三件全销，02 C1.103–105）**：Q159 批量回填岛切服务端 CSV（纯前端：server action 提交 CSV 原文到 Q156 `/upload`、422 逐行错误结构化〔line＝含表头物理行号〕、check-content 扩守卫、解除前端 500 行软上限，零迁移）；Q160 Excel .xlsx 上传（新端点 `POST /api/effects/backfill/upload-excel` 与 CSV 同契约同回执、新模块 effects/excel_io.py 用 openpyxl `read_only` 读活动 sheet、单元格归一化后复用 csv_io 同一校验、旧 .xls/损坏文件加载期 422、依赖 openpyxl>=3.1.5、行数上限复用 env 默认 1 万、审计复用 effect.customer_backfilled、+8 集成，零迁移）；Q161 异步导入任务（新包 `app/core/imports/`、`POST/GET /api/effects/backfill/jobs`＋`GET /jobs/{job_id}`、迁移 **0039 import_jobs** 物理表 60→61〔payload Text 落 CSV 原文/xlsx base64 可重放、queued/running/completed/failed〕、ImportWorker 经 Redis Streams 消费组〔确定性校验失败 failed+ACK 不重试不进死信、基础设施异常留 PEL/XCLAIM/死信〕、门控 `LOOM_IMPORT_WORKER_ENABLED` 默认关〔关＝请求内同步跑到终态保 V1 行为、开＝异步、入流失败 fail-closed 503〕、另三旋钮 BLOCK_SECONDS=5.0/CONCURRENCY=1/STREAM_COUNT=20 不入 .env.example、审计 import.job_created/completed/failed、+17 集成＝API 8+worker 9，前端零改动）；Q156 三接缝（强制 tz-aware/行数硬上限/JSON body 零 multipart）连同三片同类工程默认（xlsx base64、payload 落库可重放、worker 门控默认关）已经负责人 2026-09-22「A和B吧，尽量搞一下」追认；迁移头 **0039_import_jobs（down=0038，物理表 61，Q159/Q160 零迁移、Q161 迁移 0039）**、后端常规 **803 passed＋8 real-infra 默认 skip**（带 LOOM_TEST_REAL_PG_DSN/LOOM_TEST_REAL_REDIS_URL 两 env 时另计 real-infra 8 例；747→750 Q155 +3、→757 Q156 +7、→761 Q157 +4、→769 Q160 +8、→786 Q161 +17〔API 8+worker 9，Q159 纯前端 0〕）、eval 101/101（golden 21）、ruff 净、前端 tsc+八 checker（Q159/Q160 扩 check-content 服务端 CSV/xlsx 守卫，无新 checker，token 仍 86）。**Q166–Q177（2026-09-22～23，02 C1.110–C1.121）**：Q166 客户效果 analytics 只读页（翻转 Q97/Q114 提前入 V1）、Q167 Agent Key 治理页、Q168 导出 job 管理页（管理端 sidebar 至 10 项）、Q169 全链 golden-path e2e、Q170 运营 SOP（docs/21）、Q171 第三轮 DBA 全量列复核（61 表/654 列/215 索引零漂移）、Q172 真 agnes 全链演练（LLM HTTP 超时配置化）、Q173 持续高压负载演练（9/9＋jobs 列表 offset 补全）、Q174 批量导入岛切异步任务轮询、Q175 RPO·RTO 量化达标（RPO 1.919s/RTO 70.111s）；**Q176** ATOM-AFFINITY KNN 索引经 grounding 判定应用层零 DB 层 KNN 查询（向量距离算子全仓零命中、affinity 按 Q86 内存确定性重算）、裸建为无消费方死索引，列甲乙丙三档，**已经负责人 2026-09-24「那你一口气搞了」按丙案（暂不做/YAGNI）裁决：不建索引、不写迁移 0040、不改 Q86，甲乙两档归档备查**（C1.120）；**Q177** D3.5 白名单组装引擎运营只读首片——管理端 /admin/fcw 跨租户分页列表＋行内六层原料包卡片（后端 `GET /api/admin/fcw` 与 `GET /api/admin/fcw/{final_id}/material` 两口、读闸 operations|platform_admin、复用 Q155 build_material_pack 但内嵌面无 attachment 头，前端 sidebar **第 11 项**、零迁移，C1.121，三接缝甲案已经负责人 2026-09-24 追认；组装工作台/6 层可视化编辑/合理性校验/审核台/冻结管理/客户卡片视图/复制 ID 等 D3.5 余项随菜单点工）；**Q178** 身份层 P0-① 甲案第一切片内部运营人员令牌 PAT（负责人经 interaction.ask 拍板甲案，02 C1.122）：新横切包 `app/core/staff_auth/` 与 staff_api_keys 表（迁移 **0040**、业务物理表 61→62、pg16 宿主 55442 往返实测），明文 `loom_staff_`+token_urlsafe(30) 与 Q88 机器 `loom_` agent_api_keys 分表分前缀不可互换、无 tenant_id 审计恒 `_platform`、可签发限五内部角色（不含 whitelist_owner）；端点 POST/GET `/api/admin/staff-keys`、revoke（platform_admin 红线）、`GET /api/auth/me`，agent-keys GET 补 internal_gate；**认证（app 级全局依赖 staff_auth_context，门控 `LOOM_STAFF_AUTH_ENABLED` 默认关，仅验 loom_staff_ Bearer 写 contextvar、坏/吊销 401、机器 loom_/客户/公开口放行）与授权（rbac.require_any_role 唯一收口，新增 NotAuthenticated 401/PermissionDenied 403，通过即就地用令牌身份覆盖自报 actor 防提权）分离、HTTP 端点签名零改动**；前端 lib/staff-auth.ts httpOnly cookie、lib/api 注入 Bearer＋管理口 401 跳 /admin/login，新建 /admin/login 与 /admin/staff-keys（sidebar **第 12 项**，login 不进 sidebar），check-admin 扩守卫段；引导＝门控关自报签发首个 platform_admin 令牌→/admin/login 录入→开门控重启，回滚＝关 env；test_staff_auth_api.py 25 例（StaticPool 共享认证/业务两 session、先关闸签发再 monkeypatch 开闸），后端常规 **842 passed＋9 skip**、eval 101/101、ruff 净、前端 tsc＋八 checker＋next build 全绿、token 86/客户 nav 8；纯加法门控默认关 V1 自报行为不变，工程接缝（旋钮不入 .env.example、零端点签名改动、客户侧后置）**已经 Q179 追认（02 C1.123）**，客户侧真实认证/actor↔tenant 绑定（Q88 口径待补）/账号密码会话仍为 V2 第一项）；其后 **Q179 第四轮 DBA 全量列复核**（一次性 pg16 容器 loom-dba4/宿主 55448 upgrade head 到 0040，information_schema/pg_indexes/pg_constraint 对 ORM metadata 逐表逐列：62 业务表/666 列/219 业务索引/PK62·FK32·UQ21、模型⇄迁移列名零漂移，纯只读复核零迁移零测试，02 C1.123）、**Q180 D3.5 客户白名单卡片视图**（后端客户集合口 `GET /api/fcw?tenant_id=` 无闸强制单租户、复用 list_fcw_admin，六层沿用 Q155 material，test_fcw_admin +3；前端 content 子页 `/content/cards` 客户 nav 不增项仍 8、RSC 卡片列表＋CardMaterialIsland 按需展开，check-content 加守卫，02 C1.124）、**Q181 零依赖指标级监控**（新包 `app/core/metrics`：Counter/Gauge/Histogram 与 Prometheus 文本、标签转义＋ASGI MetricsMiddleware〔path 取路由模板低基数〕＋`GET /metrics` 不鉴权同 healthz；可选 prometheus 走 compose profile `monitoring` 默认不启、镜像用 quay.io；test_metrics_registry 8＋test_metrics_api 2 共 +10，02 C1.125）、**Q182 postgres WAL 归档＋基础备份＋对象存储 PITR**（postgres 开归档 walarchive 卷＋db-basebackup 周期 pg_basebackup＋wal-archiver〔mc 显式 alias〕推 MinIO 成功后清理＋init-replica-hba 补 host replication/chown 卷；`infra/wal/pitr-rehearsal.sh` 端到端实测 before=1/after=0，RPO 日级→分钟级并具备时点恢复；坑：官方镜像 pg_hba 不含 replication、命名卷默认 root 须 chown 999/1000、mc 须 --entrypoint sh 且 MC_HOST env 不加载、Docker Hub 用户命名空间镜像被拒改 quay.io；跨 region 冗余仍 V2，02 C1.126）；三片零迁移、Alembic 头仍 **0040_staff_api_keys**/业务物理表 **62**，后端常规 **855 passed＋9 skip**（842→855＝Q180 +3、Q181 +10，Q179/Q182 零增量；总收集 864）、eval 101/101、ruff 净、前端 tsc＋八 checker＋next build 全绿/token 86/客户 nav 8/admin sidebar 12。效果反哺校准算法（口径【待补】）、restock 消费组水平并行（花钱隔离，Q157 只验单 leader 不双花未改造）、~~真 Redis/真 PG 多副本易主→中止/拒写集成验证~~ 已随 Q154 真容器验证（02 C1.98）、~~真多副本多 worker 持续负载端到端部署演练~~ 已随 Q157 落地（ha-rehearsal 16/16、本地手动不进 CI，02 C1.101；持续高压压测/RPO·RTO 量化达标仍 V2）、~~异机备份恢复演练~~ 已随 Q158 落地（restore-rehearsal 12/12 逐表零差异、应用可读/迁移幂等、本地手动不进 CI，02 C1.102）、~~6 层原料包全量 JSON~~ 后端台内卡片 JSON 已随 Q155 落地（台内卡片前端 UI 随 D3.5 菜单，02 C1.99）、~~批量导入服务端上传端点/逐行回执/Excel/异步导入/前端岛切换~~ 已随 Q156＋Q159–Q161 全部落地（02 C1.100、C1.103–105，接缝经负责人 2026-09-22 追认）、A2A 第二阶段（任务持久化跨实例/真 LLM 与链路执行/Zeus↔loom 真机联调，Zeus 侧 C2 待定）随 V2，须点工；private beta 验收唯一剩余阻塞＝业务方按 docs/19 模板回填首批主数据（publish_slots/PCP/三包，禁工程臆造）。**Q184–Q187（2026-09-25，02 C1.128–131，负责人「执行选项 B」一口气四片）**：Q184 文档对账收口（订正过期「未 push」标注＝实测 dev 与 origin/dev 已同步；docs/20 §6.5 按 post-Q183 现状整体重写，原 A1–A4/C1–C3 候选全部落地转历史注记、C4 收口、C6 关闭，重扫后同类唯一余项＝R1 业务级指标打点＋对应告警）；Q185 指标消费侧（`infra/monitoring/alert_rules.yml` 三条 RED 规则＋Grafana 文件式看板/数据源＋监控栈拆独立 opt-in overlay `docker-compose.monitoring.yml`；**改判撤 Alertmanager**——实测 0.34.1 `webhook_configs.url` 不展开 `$(VAR)`、不支持 `env://`，改由 watchdog 轮询 `/api/v1/alerts` 转发进既有 `LOOM_ALERT_WEBHOOK`，firing-only＋按 activeAt 去重；**事实纪律要点**：全仓真实注册指标只有 `up`/`http_requests_total`/`http_request_duration_seconds` 三族、零业务打点，故队列积压/job failed/LockLost/LLM 预算四类规则属臆造指标名一律不写，并有契约测试硬守「规则与面板不得引用未注册指标」；**顺带修掉 Q182 两个致 compose 整体不可用的遗留缺陷**——全新卷 postgres 必挂改由一次性 root `wal-archive-init` 预备归档卷属主、wal-archiver 的 minio 依赖补 `required:false` 且随 minio 移出 staging overlay；`infra/alerting-rehearsal.sh` 真栈 24/24 PASS）；Q186 D3.5 余项最小件（`/admin/fcw` final_id 单条＋本页多选复制、表格下沉 client 岛承载选中态、六层原料包 JSON 存盘，`/content/cards` 同步补齐；纯前端零迁移零后端，客户 nav 仍 8、sidebar 仍 12）；Q187 C4 收口（discarded 成品保留期清理登记为**第五个 SLA sweep 作业**，甲案：配置中心 `content.discard_retention_days`＝180 天＝迁移 0041 纯种子无 schema 变更、物理删＋逐行 `content.discard_purged` 审计、不动 partial index、**有下游引用者留档不删**（effect_records/effect_claims 硬 FK ＋ import_jobs 软引用）、env `LOOM_DISCARD_PURGE_ENABLED` 默认关、复用既有 `/api/admin/sla/run` 不新增端点页面）。三片工程接缝（Q185 撤 Alertmanager／Q186 表格下沉岛／Q187 六项）按推荐甲落地，**已经负责人 2026-09-25 概括追认（02 C1.134）**。**Q188（2026-09-25，02 C1.132，负责人「按你建议来」）**收口 Q184 重扫后那条唯一余项 R1：四类业务信号（作业失败 / 队列积压 / 锁易主 / LLM 日预算与上游延迟）在 `backend/app/core/metrics/business.py` 落七族指标并全部挂在**真实产出点**（五个 DB `failed` 写点／`move_to_dead` 死信漏斗／`LockLease._mark_lost` 租约漏斗／新只读原语 `stream_depth` 在三处 worker tick 取样／网关预算硬停与驱动往返），`alert_rules.yml` 相应新增 `loom-operations` 七条规则与第二张看板 `loom-operations.json`；契约守卫由单向改**双向**（不得引用未注册名＋注册了无人消费即判红）、指标名正则扩至认裸比较、看板守卫改为遍历 provisioning 目录全部 JSON。**grounding 推翻草案四处**：有第三个 worker（Q165 FCW）、入流 fail-closed 因走同一 `fail_*_job` 被自动覆盖、**restock 流刻意不采深度**（Q138 只接生产侧无消费者，采它＝造一条永远正确的假积压告警）、`reclaim_pending` 的 `XPENDING RANGE` 带 idle 过滤且被 count 截断故不代表积压总量。零迁移零新 env 零前端改动；**七条规则的设计接缝按推荐甲落地、已经负责人 2026-09-25 概括追认（02 C1.134）**；但阈值**数值**（100 未 ACK／5000 流长／30s 上游 P99）原文未给出，空栈演练只能证明「表达式可求值」，其合理性仍待首批主数据回填后校准——**追认结设计、不结校准**。「零外部依赖＋纯技术＋不破坏基线」的池子自此抽干，下一批须重新 grounding。**Q189（2026-09-25，02 C1.133，纯文档零代码）**为该收口后的第一批对账：docs/08 §2.2「落地状态总览」实测停在 Q136／699 测试／迁移头 0037（差 45 片），已新增权威当前行（旧行就地标注为历史快照、不删），并把 handoff 超「最近 5 条」上限的六条**经内容级核验后逐字滚档**（核验必要性＝其正文在本机归档里零命中，直接删会丢信息）；顺带复原一处被前缀式 Edit 误吞的 Q184–Q187 条目标题头。**纪律补条**：docs/08 对 Q160 之后不再逐片复述完成情况，逐片权威记录只在 docs/02 C1；对「一条逻辑记录＝数千字符长行」的台账禁止用相邻条目前缀做 Edit 锚点。**Q190（2026-09-25，02 C1.134）**把 Q185／Q186／Q187／Q188 四批按甲落地的工程接缝**全部追认销账**（零代码逻辑改动，仅一处配置播种文案 `content.discard_retention_days` 的 `source_ref` 由「待追认」改标「已追认」，存量库那行仍显示旧文案、本仓无种子回写机制故不为此补迁移）；**追认只结设计选择，不结数值校准**——180 天保留窗口与三个告警阈值的合理性仍待主数据落地后重看。另在扫描中自我更正一次：AGENTS 旧句把 Q122 三接缝写成「待追认」，回查 02 C1.66 实为 **2026-09-19 已追认销账**，属入口文档措辞过期而非决策挂起，已就地订正。**Q191（2026-09-25，02 C1.135，零代码）**diff「已具备能力 ⇄ 运营可达面」查出：10 条告警规则已立，而唯一给值班人看的 docs/21 里「告警」0 次——平台能喊出事却没人被告诉先查哪里、要不要停线，故 docs/21 新增 §5（三条承重前提：默认部署一条都不发／一个故障只通知一次故静默≠恢复／worker 默认关时队列规则永不触发是事实非漏报；10 条规则→首查位置→是否停线表，只引用回源码点过名的对象；数值来源纪律：**追认不豁免校准**）。同批自查纠正一处：§5 初稿写了不存在的 `LOOM_LLM_HTTP_TIMEOUT_*`，实为 `LOOM_LLM_HTTP_TIMEOUT_SECONDS`（默认 60s）。

**Q192（2026-09-25，02 C1.136，负责人拍板「开 Grafana 回环端口」）**补上告警消费面最后一公里：监控 overlay 给 Grafana 发布 `127.0.0.1:3001`（**只绑回环**——口令是它唯一一道门；**刻意避开 3000**——base compose 里那已是 frontend），绑错网卡／撞端口／多开口由 `test_grafana_is_loopback_only` 判红（该守卫按四种坏配置做过证伪自检），宿主可达由 `alerting-rehearsal.sh` 两条**宿主侧**断言守着（全量 34/34）。docs/21 §5 的看板访问段已从「未实测不给步骤」换成实测事实。**Q193（2026-09-25，02 C1.137，负责人「你搞」）**把 Q188 那七条业务规则从「表达式可求值」推到「真出事前会发生」，第一次实打就查出**系统性盲点**：counter 懒创建，标签组合的首个样本即非零 ⇒ Prometheus 看不见 0→1 跳变 ⇒ **进程内第一次发生的该类事件永不可告警**（修前实测：`/metrics` 已是 1、Prometheus 也抓到 1，`increase(…[15m])` 恒 0、alerts 空、转发 0 次）。修法＝启动时预置 0 序列，且**由拥有该词表的模块各自登记**（作业三类／两把循环锁／死信随被消费的流／预算按 `seeds.all_scene_codes()`）；**不换 `min_over_time`**——对累计计数器会事件后永久常亮，是另一种坏。另修掉演练自身三处自伤：阶段 4 一条**永远绿的空断言**、`docker logs` **跨轮次累积**造成的假失败、以及「我宣布已清空重跑其实没清空」（手敲 `down -v` 被 Grafana `:?` 口令在解析期插值挡掉，留 10 容器 7 卷，于是同一轮同时出现假失败与假通过）；脚本现起栈前自检残留、非 0 即 ABORT。**新增纪律两条**：「clean slate」是需要被证明的断言，不能凭命令退出码宣布；演练判据一律用轮询内「曾观测到」采样，不用事后单次快照（pending→firing 窗口必撞）。最终干净栈 39/39 PASS、后端 898 passed＋10 默认 skip。**Q194（2026-09-25，02 C1.138，纯文档零代码零迁移零测试变化）**是对本仓「一条逻辑记录＝数千字符长行」这一顽固缺陷的第六次清扫：回答「迭代计划还剩什么」时逐字读 docs/08 §2.2/2.3/2.4、docs/20 §6.6 全三十行与 handoff 待办，查出**六处**「决策早已落地、状态词仍写待办」的陈旧文本（docs/20 边界模糊项 C1/C2/C3、§6.6 #29 analytics 与 #30 discarded、「不可一口气推进（摘要）」第三条、§6.7 第 1/3/4 条，加 **docs/02 文末续编名单断在 C1.131 Q187＝权威台账自己的索引过期**）；订正一律原文加删除线保留＋补 ✅ 注记引 Q/C1 编号，§6.6 其余 18 行未落地项与全部【待补】口径**一字未动**——判「已落地」的唯一依据是 02 里存在对应 Q 条目。本批同时自我订正两处计数：口头汇报说「三处」实测六处；首次跑门禁因把 eval 路径写成 `backend/eval` 造出一个**由命令本身造成的**假 error，照 CI 原文重跑即净（**门禁命令照 CI 抄，不凭记忆构造**）。**Q196（2026-09-25，02 C1.140，负责人按 Q195 推荐拍板 A甲＋B甲＋C甲，零迁移零新 env）**把「已验真凭证 > 请求自报」收在**全仓唯一审计口** `append_audit`——122 个调用点一行未改即全量生效，刻意不逐端点补闸（那正是 Q117/Q118 抓过的漏网来源）；新增 `app/core/identity.py` 请求级凭证视图、`staff_auth_context` 每请求先清零，审计 `detail._actor_via` 标来源、被推翻的自报值降级为 `declared_actor` 留证。同片补齐 docs/20 #31 两处实测缺口（导出任务两条读口 `tenant_id` 必填＋按任务行归属收口、跨租户与不存在同回 404；`job_id` uuid1→uuid4），并**明写未闭合面**：客户侧读面仍以声明 tenant 过滤（C甲＝beta 不做登录，属已接受风险）、92 个自带 `actor` 的请求体模型保留（B乙未选）。**纪律同 Q193 再验一次**：本批九种植入缺陷全部判红，而第一轮「状态口退回不校验租户」植入后 24 条用例全绿——我新写的用例只钉了下载口分支，状态口那条断言压根不存在；**新增守卫不当场植缺陷，就只是看起来在守**。**Q198（2026-09-25，02 C1.142，纯文档零代码）**是本轮纪律的一次实弹检验：负责人「更新项目文档」后，13 份文档里就地订正 **24 处**「实现已走远、文字还停在原地」并补登 4 张从未登记的物理表——包括 docs/05 导出任务口仍写「V2 真异步后供轮询」且无 `tenant_id`、docs/10 权威标题停在迁移 0001–0040、docs/15 `app/core/` 注释停在 6 项（实测 24 包）、docs/11 两个 §2.4 抢号、BENCHMARK 仍称「文档阶段无代码可测」、MIGRATION_CONVENTION 教人用 `--autogenerate`（41 个迁移零个自动生成）。**做法：只改活状态，逐片历史记录一字不动；【待补】/【待拍板】一处都不因「实现做了」而静默转正**；调研给的每条结论都回源码复核，因此拦下四处错（主键名写错、重号未消、补登少一列、批量插入落错位置）。**「工程侧无可推进项」至此已被自己的扫描推翻五次**（Q189／Q191／Q194／Q195→Q196／Q198）——被推翻的原因始终是同一件事：文档与代码的漂移本身就是活。**Q199（2026-09-25，02 C1.143，纯评审）**按负责人要求重判 MVP，做法是**不引用自家旧结论、逐条回源码复测**，结论分三层不可合并成一句「达到 MVP」：**功能覆盖达标**（13 段全有可运行路径、段1→6→10→11 真链路 e2e）；**「产品核心完全可用」未达标**——除主数据这一外部阻塞，查出三条此前任何文档都没登记的工程缺陷并只登记不自动修（§6.6 #32 **多租户隔离可被单个客户穿透**：段12 裁决口与段1 语言口只按全局唯一 id 定位对象、无角色无归属校验；#33 默认 compose 把 5432/6379/9000 发布到宿主、口令写死、`LOOM_MASTER_KEY` 默认空使供应商 Key 重启即不可解；#34 治理不变式默认不被强制：门控关时自报 `operations` 即可 mint `final_id`，「唯一出口」仅注释约定）；**可上线未达标**。可承诺最小集＝这三条＋主数据回填。**Q200（2026-09-25，02 C1.144，负责人「那你开搞」点工三条，零迁移）**把三条 P0 按 §7.4 判据修到先红后绿：**#32 多租户隔离收口**（五个带 id 客户口＋intake target-languages 全加 tenant_id 必填、归属不符统一 404、运营口不动，隔离测试 7 绿＋存量 110 处/23 文件适配）；**#33 默认发布形态收口**（5432/6379/9000-9001 全绑 127.0.0.1、口令 env 化、三 datastore 补 restart、空 LOOM_MASTER_KEY entrypoint 启动 fail-fast〔不落 compose 解析期插值避 Q185 坑〕、三彩排注入测试密钥，契约测试 5 绿）；**#34 治理不变式强制**（fcw 两写口补 require_any_role(INTERNAL_ROLES)，门控开无令牌 401、门控关维持 V1 自报，_require_ops 留 worker 兜底，判红 2 绿）。基线 912→**926 passed＋10 skipped**（总收集 936，＋14）、eval 101/101、ruff 净、前端 tsc＋八 checker＋next build 全绿；Q199 判定 ②③ 工程项销账，**唯一剩余阻塞＝主数据回填（docs/19，禁工程臆造）**。**方法论留痕**：本轮同时抓出我自己上一批写进 docs/20 的更新块序号与位置错误（插在「第七次」之前、编号跳到「第九次」），以及本轮 §6.7 结论一度插错位置——**位置、序号、列数、交叉引用都要独立复核，批量脚本打印成功不算证据**。**Q202（2026-09-26，02 C1.146，**纯评审零代码零迁移零测试变化**）**负责人再问「是否达到产品核心完全可用的 MVP」，本轮先与 git 对账（HEAD 已从 `8e3804a` 前移到 `174604f`、并行会话落了 Q200 三条修复）再逐条**对当前代码复测**、不复述其自我声明：抽出 docs/20 §7.2 探针再跑（读口 200→404、写口 422），**并补第二刀探针**——422 只证明 `tenant_id` 变必填、不证明归属被强制，于是让攻击者带上自己合法的 `tenant_id`：五条越权 **404**、discard **403**，而 A 侧正路 200 且状态推到 `ready_for_publish` ⇒ **#32 判闭合（执行级）**；#33 对产物逐行核过判**闭合**；**#34 半闭**（角色强制只在门控开时成立，而 `core/config.py:112 staff_auth_enabled=False` 是默认 ⇒ opt-in；「`final_id` 唯一出口」仍无运行期守卫，4 个测试文件照旧直插 ORM，§7.4 第 3 项判据未达成）。**复判＝① 功能覆盖达标／②「核心完全可用」仍未达标／③ 可上线仍未达标**，卡点收敛为**主数据零行 ＋ #34 另一半**，另登记四条残留（弱默认口令、门控默认关、唯一出口无守卫、演练与真 PG 迁移不在 CI 且**本次 CI 实际结果从未被读过**）。**两处方法教训**：判据必须打在攻击者能满足的最有利形式上，否则会把「契约变了」当成「归属被强制」而高估修复；以及复核自己刚写下的转述——本批初稿把 #34 的闸转述成上一批台账里的 `require_any_role(INTERNAL_ROLES)`，回源码实为 `require_any_role(body.actor, OPERATIONS)`（逐片历史记录按「不回改」保留原样，订正见 docs/20 §8.5／02 C1.146 勘误条）。报告＝docs/20 §8。**Q203（2026-09-26，02 C1.147，**代码＋测试＋文档，零迁移零新表零新 env**）**负责人「按你推荐的来」点工 Q202 报告的三条推荐，把 #34 的两半与弱默认口令一次收口：**① `final_id` 唯一出口从注释变成运行期事实**——新 `app/final/final_whitelist/exit_guard.py`（`issue_scope()` 协程作用域 ＋ `FinalContentWhitelist` 的 mapper `before_insert` 事件），装载点在 `models.py` 类定义之后（只 import 模型也带着守卫），生产端唯一入口仍是 `service.assemble_one`；作用域外 INSERT 抛 `OutsidePublishFCW`，当场植缺陷（把装载行标注释）观测到 3 例判红；**覆盖边界只到 ORM flush**，Core `insert()`/裸 SQL 不经 mapper 事件，由 `test_core_insert_bypasses_the_mapper_guard` 钉成实测事实（全仓今日对该表无 Core 写入）；并改正 Q202 的低估——「四处直插」实为 **9 文件 16 写入点**，统一改走显式夹具 `tests/integration/fcw_rows.py::add_fcw`。**② 发证两写口只认已验真令牌**——新 `staff_auth/deps.py::require_internal_actor(*roles)`，与 `internal_gate` 相反：门控关闭时**自行验真**，缺/坏/吊销 Bearer → 401、令牌无 operations → 403、已验真身份覆盖 body 自报 `actor`（Q196 存证口径不变），`get_auth_session` 改为「门控关但带 Bearer 也给连接」；**这取消了 Q200「门控关维持 V1 自报」承诺在这两个口的适用性，是本批唯一破坏性契约变更**（docs/05 §1.3/§1.4、docs/19 验收第③步、docs/21 §2 同步加注：外部自动化须先按 Q178 引导签发令牌）。**③ datastore 口令无仓内默认**——`infra/docker-compose.yml` 六处 `:-loom`/`:-minioadmin` 回退删除，未设即由 postgres/minio 容器自己启动失败；刻意不用 `${VAR:?}`（compose 解析期全局求值会打断没设变量的 overlay/彩排，Q185 同型坑）；ha/load/alerting 三套彩排注入演练专用值；compose 契约测试 5→7 例。「摘掉安全依赖」式证伪被本仓权限层拦下（一次性 worktree 亦拦），凭证侧改用**永久接线自检**（`test_write_routes_stay_wired_to_the_credential_dependency`，含只读 GET 反向对照）替代。基线 **938 passed＋10 skipped**（总收集 948；926→938＝出口守卫 6＋发证口凭证净 4＋compose 契约 2）、ruff 净、eval 101/101、前端 tsc＋八 checker＋next build 全绿（前端零改动）。**复判保持分层：① 功能覆盖达标／②「核心完全可用」仍未达标但工程侧卡点清零（唯一理由＝`publish_slots`/`pcp_weight_tables`/`packages`/`platform_rules` 四表零行播种，须业务方按 docs/19 回填）／③ 可上线仍未达标**。另自我更正一条 Q202 登记的**假缺口**：「docs/06／docs/09 三处内容缺口」经 grep 复核不成立（二手结论未回原文，本仓「转述未复核」第三次发作），真缺口在运营令端口径与验收步骤、本批已补——**登记「缺什么」之前先 grep 它是否已经在那儿**。**Q204（2026-09-26，02 C1.148，**测试＋文档，零生产代码零迁移**）**负责人「按你的来」＝按我给的推荐执行三件事：push（`origin/dev` 174604f→`ae8b2c2` 快进）后**第一次把 CI 结果读到 step 级**——run 36219634868 对 `ae8b2c2` 两个 job 全 success（后端 `Ruff lint`／`Pytest`／`Eval regression` 三步、前端 `npm ci`／`typecheck`／八 checker 三步），更早 `174604f` 亦 success，于是「本次推送的 CI 实际结果从未被读过」这句挂了六批的残留**自本批结清**（**但前半句未结**：五套演练、真 PG 迁移往返、real-infra 8 例仍不在 CI，"仓库绿"≠"部署形态被验证过"）；compose 契约扫描提为纯函数 `credential_interpolations()`，覆盖面从"只看 base"扩到 `infra/docker-compose*.yml` **全部五份**（base＋staging/ha/monitoring/alerting-rehearsal 四个 overlay），并新增坏样本自检 `test_the_credential_scan_can_fire`（`:-`／`:?`／干净三种），因为**只在真文件上跑绿的检查等于没有检查**；契约测试 7→9 例。docs/19 §0.1 补**令端口径分岔**：录入类写口仍按 Q178（门控关接受自报），发证两写口自 Q203 无条件要 `loom_staff_` Bearer，故验收第③步前必须先引导签发，否则门控关着也撞 401。**三处自我更正**：① §9.3「已核过三个 overlay」实为**四个**（漏 alerting-rehearsal）；② 我自己提的推荐「给业务方起草首批最小集说明」经翻原文发现**早已存在于 docs/19 §0**——与上一条假缺口同一毛病第二次发作，**列"接下来做什么"和列"缺什么"适用同一规则：先查它在不在**；③ **决定不做** DB 层挡 Core 裸写（签发作用是进程内 contextvar，PG 触发器看不见，等效强度要 `SET LOCAL` GUC＋迁移 0042＋pg16 往返＋sqlite 替身无法覆盖，而该表 Core 写入实测为零），边界改由 `test_core_insert_bypasses_the_mapper_guard` 保持**可见**而非补一层伪守卫。基线 **940 passed＋10 skipped**（总收集 950）、ruff 净、eval 101/101、前端全绿；**复判不变**：① 达标／② 仍未达标（唯一卡点＝主数据零行）／③ 仍未达标。**Q205（2026-09-26，02 C1.149，**纯文档零代码零迁移零测试变化**）**负责人问「你能推吗」，本仓「工程侧已推到零」这句话已被自己的扫描推翻过六次，所以答案是当场重扫而不是重申——扫出**两处我自己前两批造成的漂移**：docs/08 §2.2 权威当前行仍停在 Q200／926 passed（Q203/Q204 同步了七份文档偏偏漏了它），docs/11「已定稿接口规格」**压根没有 E1.1 两个发证口**（于是 Q203 的鉴权变更在 API 规范文档里查不到）。处置：新权威行加在上方（Q204／**940 passed＋10 skipped**／总收集 950，并写明 Q203/Q204 零迁移、前端零改动、CI 首次读到 step 级 success），旧两行标注为历史快照、正文一字不动；新增 **docs/11 §2.8** 写全两口与 `require_internal_actor` 的鉴权契约、错误码、审计、唯一出口守卫及其 ORM flush 边界，**引用的六个代码位置逐个 sed 复核**；顺带修 docs/08 三条「当前基线见下一行」的错向指针，并抓掉一处自己刚引入的渲染缺陷（正文紧贴 `---` 会被解析成 setext 标题）。**诚实的另一半**：想把 docs 里「未设口令即容器起不来」从推断升级为实测（真起一个空口令 postgres 容器）被本仓权限层拦下，故 compose 渲染那半已实测、容器拒绝那半**仍标注为推断**未冒充已验。本批不造活：剩余三项分别需要外部输入（主数据）／负责人裁决（网关 TLS 归属）／已裁决不做（DB 挡 Core 裸写）。**Q206（2026-09-26，02 C1.150，**纯文档零代码零迁移**）**与负责人讨论唯一硬阻塞「首批主数据」时，发现「主数据零行」这句话掩盖两件事、而 docs/19 的索取方式本身在拖慢它：新增 **§0.2「最小可发证清单」**，判定**逐条回代码取**（`_resolve_slot:132-141`／`_resolve_pcp:164-178`／g3／`gate_view:603-624`）——发证只要求「1 个 active 发布位（**不看 `gate` 档**）＋该 PS×平台一条 active PCP（**PCP 只是选一个模板码**，4 套已播种）＋三包各一条 active」；**四维分/`risk`/`source_url`/fit 权重/`slot_type_defaults` 都不卡**（Q54 评分仅排序不做门槛）⇒ 把最贵的人工打分列进首批清单＝主要假阻塞。另一半：Guard①②⑦ 要的 PWS 冻结与合规清洗报告**不来自填表，而来自有人跑一遍段1→6→10 并在 Gate 裁决** ⇒ 这项阻塞真正要的是**两个人**（录入的＋跑链的），不是两张表。另标注 `cp_law_sensitive_domains` 零行种子 ⇒ **Guard⑥ 默认放行属可配置项、非永久豁免**，首批选产品应避开拟启用敏感类目的行业；并建议「首次打通用 synthetic、真 agnes 全链另立点工」。§0 完整模板一字未动，只加一行指针；草稿两处引用臆造（凭空 Q 编号、三包定键误写 §4）在落地前被复核拦下。**Q207（2026-09-26，02 C1.151，**代码（仅 alembic 配置）＋脚本＋测试＋CI，零迁移零新表零新 env**）**负责人对「需要放行真容器与改 CI 两件事」说「那你搞吧」，于是把四轮手工 DBA 复核变成每次 push 的门：新 `backend/scripts/dba_schema_check.py` 在真 `pgvector/pgvector:pg16` 上比对 表／列可空性与粗类型类／PK／FK 列对／唯一约束／索引，CI 第三个 job `Migration gate` 跑 `upgrade head` → 漂移检查 → `downgrade -1` → 再 `upgrade head`（四步先在本地按 CI 的 service 定义重建容器原样跑通，每步 exit 0）。**三条从现场学到的设计**：比对用**多重集**不用集合——按集合比时"同列重复索引"隐形，我第一次种 canary 就漏了；按**列集**不按名字——本仓无 metadata 命名约定且迁移手写，这也正是**不能用 `alembic check` 当门**的原因（实测在 HEAD 上报 19 条纯命名差异，永久红）；CI **不钉表数**，否则每次加表都假红。**检查器自己的 25 例单测抓出它两处 bug**：用通用方言渲染类型（`DateTime` 而非 TIMESTAMP）、`"time zone" in s` 同时匹配 `without time zone` ⇒ 两侧都把无时区列判成 timestamptz、**悄悄丢掉时区漂移检测能力**；含「验证验证器」用例（种多出的列与重复索引必须判红、撤掉回绿，真跑过 exit 1→0）。**顺带修掉一个更硬的隐患**：`alembic/env.py` 只 import 24 个 `app.**.models` 中的 14 个 ⇒ `target_metadata` 只见 **44／62** 张表；库里不缺表（迁移全手写），但**一次 `--autogenerate` 就会提议 DROP 那 18 张**——本仓迁移规范禁止 autogenerate，却没说清它防的是这个。已补齐并加单测钉住。基线 **966 passed＋10 skipped**（总收集 976；＋26＝漂移单测 25＋CI 接线契约 1）、ruff 净、eval 101/101。残留措辞随之更新：「真 PG 迁移不在 CI」**已结**，未结的是**五套全栈演练与 real-infra 8 例**；本批**不检查**列长度精度/索引存储方法/默认值/CHECK，Q154 那类长度回归仍归 real-infra 用例，不读成"DBA 复核从此全自动化"。**复判不变**：① 达标／② 仍未达标（唯一卡点＝主数据零行）／③ 仍未达标。**Q208（2026-09-26，02 C1.152，纯文档零代码零迁移零测试变化）**负责人「更新项目文档」＝又一次全仓漂移清扫：查出**九处**入口/契约文档停在旧时点并就地订正（CONTRIBUTING 一句里三处过期：两 job→三 job、七 checker→八、指向 docs/17 不存在的小节；BENCHMARK 两个早已可答的 TODO 按其自订「本文件不复制数字」口径改成场景→制品→权威小节指针表，并写明**那八条业务吞吐维度至今无一条有专属 harness**；**文档地图自己的逐文档状态列有五行过期**，包括把 Q118 早已拍板的发布位 gate 仍标「待拍板」；docs/15 backend 树补 `scripts/`；MIGRATION_CONVENTION 的 autogenerate 禁令补上本仓真正原因；docs/10 写明 CI 门与四轮人工复核是两把尺、219＝157＋62 条主键索引）。**本批把 AGENTS 的「截至 2026-09-18 无待裁决事项」降级为时点快照，并在其上登记四项等负责人/业务方**：网关与 TLS 归属／首批主数据（真正要两个人）／V2 第一项客户侧真实认证／docs/08 排期三列——**四表主数据仍是唯一硬阻塞，四项一律不得代裁**。**两处自我更正**：只读子代理的命中里**至少八处**指向根本不存在的文本（四处当场抓、四处提交后复测才抓，含「docs/18 tokens 总数 85」，真数 86 由 checker 自证）（含「docs/18 tokens 总数 85」，真数 86 由 checker 自证），凡落地前必须回原文；本批差点为 docs/11 补写 Q161 三口规格，读 §2.1 才发现早已写全——**列「缺什么」与列「接下来做什么」适用同一条规矩：先 grep 它在不在**（本仓第四次因此拦下自己）。当前基线、待办与逐片记录一律以 [handoff.md](handoff.md)「当前状态/待办」与 `docs/02` C1 日志为准，本文件不复述数字。

## 文档体系与治理规则（必须先读）

- **入口**：先读 [handoff.md](handoff.md)（当前状态/待办），再读 [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md)（场景导航 + 治理规则）。
- **handoff 归档惯例（2026-09-18）**：`handoff.md` 只保留项目当前状态 + 活跃待办 + 最近 5 条进度；更早的进度条目与已销账待办详细过程整块原文归档至 `docs/handoff-archive-YYYY-MM-DD.md`（可作历史检索，逐条一字未改），权威逐条台账仍是 docs/02（C1 日志）/ 08 / 16。
- **唯一事实源** = `docs/` 全部文档；原 `v3.md` 已删除，一切规格以 docs 为准。
- **修改流程**：内容变更先改 docs 对应文档 → 新决策追加 Q 编号（02 文档 C1 日志）→ 涉及展示层再同步渲染（09）。
- **两套口径**：13 段链（权威，01 PRD）与 03-A~E 展示口径（09 全景）并存不混用，冲突以 01 为准；路线图口径合并属裁决事项，不得擅自合并。
- **行号溯源（Q115 收口）**：`line NNNN` 引用指向基准文件 `Docs/Loom_后台_V6.0-需求说明（不是原型).html`；该文件经负责人 2026-09-17 确认**已永久丢失、无法找回**，故行号引用**降级为历史痕迹**，不再作为可核对基准，改动时无需（也无法）同步核对。

## 事实纪律

- 每个数字/状态/结论必须来自 docs 文档原文（可溯到 Q 编号或 line 行号）；**禁止编造业务事实**。
- 文档空缺处一律标【原文未给出，待补】，不得猜测填充。
- 状态词口径：✅ 权威 / 🟡 待补 / ⬜ 待拍板——沿用现有标注，不静默升级状态。

## 待裁决事项（不擅自裁决）

**2026-09-26 复扫：四项要由负责人或业务方给东西，工程侧不得代裁**——① **网关与 TLS 归属**（private beta 的前提「后端在网关后」在仓内至今无产物，是「可上线未达标」的部署侧原因，docs/20 §7 第 6 条与 §9、02 C1.149）；② **首批主数据**（`publish_slots`/`pcp_weight_tables`/`packages`/`platform_rules` 四表零行 ⇒ 全新库发不出证，业务方按 docs/19 §0.2「最小可发证清单」回填；该项真正要的是**两个人**——录入的＋把段1→6→10 跑一遍并在 Gate 裁决的）；③ **V2 第一项＝客户侧真实认证与 actor↔tenant 绑定**（Q144 压后，Q178 只做了内部令牌，客户口仍按声明 tenant 过滤属已接受风险，02 C1.122）；④ **docs/08 §2.2 排期三列（工期／人员／起止）原文未给**，须等负责人提供资源信息，**禁止倒推编造**。

下方 2026-09-18 行是该时点快照，按「逐片记录不回改」保留：

- ~~两套路线图口径合并~~ 已于 2026-09-13 裁决（Q73，08 §1.3：A7 为骨 + D9 厚度，段 12 后置 V2，V1=段 1→6→10→11）。
- ~~2026-09-17 Q117 审计挂账的 4 项实现差异~~ 已于 2026-09-18 由负责人「一口气开搞」均按推荐（甲）拍板并落地，记 **Q118（02 C1.62）**：① 4 个漏网管理面 GET 补 query actor 闸；② `publish_slots.gate` 回填 V1 仅 Q42 人工直编口径；③ `product_spaces` 两软引用补 DB 级 FK（迁移 0027）；④ CI 接入第 7 个 checker check-settings；审计外另修 Alembic 版本表列长缺陷。详见 [handoff.md](handoff.md) 待办 6（已销账）与 02 C1.62。

> M0（S 级红旗 4 条 + 业务方素材采纳）已于 2026-09-13 完成，见 02 中 Q2/Q22/Q22a/Q22b/Q60 转正注记与 C1.16 素材确认；04 §3 另有 4 个实现时挂账子项，非待裁决事项。
> 技术选型 6 项已于 2026-09-13 拍板定稿（14 为正式实现依据）；15/17 等下游文档已同步刷新。
> 今后新出现的挂起事项仍必须由用户/负责人拍板，禁止静默替换或使用默认值，并在 02 追加 Q 记录。

## 常用入口

| 任务 | 读 |
|---|---|
| 了解全貌 | README.md → handoff.md → docs/README 文档地图 |
| 落数据模型/API/状态机代码 | docs/04 · 05 · 06（契约层） |
| 写测试 | docs/16（分层测试策略） |
| 排迭代 | docs/08（V1/V2/V3 任务包，V1=M1–M8+M10/M10-Q/M11/M12，验收标准引用 Guard/Q 编号） |
| 查技术选型 | docs/14（✅ 2026-09-13 已拍板定稿，可作实现依据；15/17 已同步） |

## 不要做的事

- 不要改动 docs 中标注"待拍板/待补"的文档状态，除非对应裁决已发生并在 02 追加 Q 记录。
- 不要拿摘要/二手口径替代 docs 原文。
- 不要删除或移动 docs 文档（唯一事实源，先改文档再谈其他）。
- 不要提交 `.env`、密钥或任何凭证；仓库**已 git init**（分支 `dev`/`main`，origin 为 GitHub），提交规范见 [git-commit-message.md](git-commit-message.md)。
