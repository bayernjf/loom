# Changelog

All notable changes are documented here. The format is based on [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/).

## [Unreleased]
- **Q201 文档对账更新批（2026-09-25，**纯文档零代码零迁移零测试变化**；基线沿用 **926 passed＋10 skipped**（总收集 936）；02 C1.145；负责人「更新项目文档」）** — README「当前状态」停在 Q188/912 已补至 Q200（概述 Q189/Q190/Q191/Q192/Q193/Q194/Q196/Q198/Q199/Q200，基线 912→926、两处 Q1–Q189→Q1–Q200、阻塞口径更新为「主数据回填＋网关/TLS 属部署侧决策」）；docs/05 段12 内容口六行（Q200 五个客户口＋intake target-languages）补「`tenant_id` 必填 query、跨租户与不存在同回 404」契约注记；docs/21 §3 错误码表 404/422 行补内容口/intake 语言口口径。只改活状态，评审当日实测基线（842/912）与逐片历史一字不动、【待补】一处未转正。
- **Q200 三条 P0 修复落地（2026-09-25，**零迁移**；后端 912→**926 passed**＋10 默认 skip（总收集 936；＋14＝隔离 7＋fcw 判红 2＋契约 5）；02 C1.144；负责人「那你开搞」点工 Q199 §7.4 最小集前三项）** — 三条全部按判据修到先红后绿，Q199 判定 ②③ 工程项销账，唯一剩余阻塞＝主数据回填（docs/19，禁工程臆造）。**#32 多租户隔离收口**：五个带 id 客户口（详情＋approve/reject/revise＋PATCH body）与 intake target-languages 全加 `tenant_id` 必填，service 归属不符统一 404（与 #31 同口径），运营口不动；新建 `test_content_tenant_isolation.py` 7 例全绿，存量 110 处/23 文件适配；前端 `lib/api.ts` 附 `CURRENT_TENANT_ID`。**#33 默认发布形态收口**：5432/6379/9000-9001 全绑 `127.0.0.1`、口令 env 化、三 datastore 补 `restart: unless-stopped`、空 `LOOM_MASTER_KEY` entrypoint 启动 fail-fast（不落 compose `${VAR:?}` 避 Q185 解析期坑）、三彩排注入测试密钥；新建 `test_default_compose_hardening_contract.py` 5 例全绿。**#34 治理不变式强制**：fcw 两写口补 `require_any_role(INTERNAL_ROLES)`（门控开无令牌 401／无内部角色 403，门控关维持 V1 自报），`_require_ops` 留 worker 兜底，判红 2 例全绿。门禁：926 passed＋10 skipped、ruff 净、eval 101/101（沿用前测）、前端 tsc＋八 checker＋next build 全绿；零迁移、物理表仍 62、Alembic 头仍 0041。
- **Q199 项目级评审（2026-09-25，**纯评审零代码零迁移零测试变化**；基线实测 **912 passed＋10 skipped**；02 C1.143；负责人要求重判 MVP）** — 三层判定拆开：功能覆盖达标／「产品核心完全可用」未达标／可上线未达标；新登记 §6.6 #32 跨租户归属缺失、#33 默认发布形态不安全、#34 治理不变式默认不生效，三条只登记不自动修。
- **Q198 文档一致性扫描批（2026-09-25，**纯文档零代码零迁移零测试变化**；基线仍 **912 passed＋10 skipped**；02 C1.142；负责人「更新项目文档」）** — 13 份文档就地订正 24 处过期陈述，补登 4 张缺失物理表／4 行横切组件／1 节选型补记；只改活状态、【待补】口径一处未转正。
- **Q197 Q196 三接缝追认销账（2026-09-25，**纯文档零代码零迁移零测试变化**；基线仍 **912 passed＋10 skipped**；02 C1.141；负责人「追认」）** — 结掉 ①②（审计 provenance 住 detail 保留键、不建列不建迁移）与 ③（导出任务口用租户归属交叉校验而非内部角色闸）；**追认只结设计选择**，Q196 列出的未闭合面原样继续挂着。
- **Q196 身份层第二切片落地＝A甲＋B甲＋C甲（2026-09-25，**零迁移零新 env 零新表**；后端 898→**912 passed**＋10 默认 skip；02 C1.140；负责人「好的，按你推荐来」按 Q195 推荐组合拍板）** — 见 handoff 同条与 02 全文。要点：凭证优先于自报收在唯一审计口 `append_audit`（122 调用点零改动），新增 `app/core/identity.py` 与每请求清零，审计 `detail._actor_via`/`declared_actor` 留痕；客户回填审计租户取自成品行；导出任务状态口/下载口补 `tenant_id` 归属并统一 404，任务 ID 改 uuid4；九种植入缺陷全部判红（第一轮抓出我自己写的一处假守卫）。
- **Q195 身份层第二切片的前置口径决策请求（2026-09-25，**⬜ 待负责人拍板**；纯文档零代码零迁移零测试变化；新增 docs/design-identity-layer-v2.md；02 C1.139）** — 见上方 handoff 同条与 02 全文：三问各列甲乙丙（推荐 A甲＋B甲＋C甲），现状十条全部实测取证。
- **Q194 对账批：docs/20 五处「决策已落地、状态仍写待办」＋ docs/02 续编名单断在 Q187（2026-09-25，**纯文档、零代码零迁移零测试变化**；02 C1.138；负责人「照这个顺序来，先清理文档错误」）** — 回答「迭代计划还剩什么」时逐字读 docs/08 §2.2/2.3/2.4、docs/20 §6.6 全三十行与 handoff 待办 1–9，对出**同文自相矛盾**：§6.5 的「重写缘由（Q184 对账）」注记（现 L179）与「2026-09-22 第二次更新」注记早已写明「A1–A4 与边界模糊项 C1/C2/C3 已随 Q166–Q168 落地」，而边界模糊项条目本身、§6.6 #29（analytics）、#30（discarded 清理）、「不可一口气推进（摘要）」第三条、§6.7 第 1/3/4 条仍是落地前的措辞——与 Q184／Q190 同型根因（一条逻辑记录＝数千字符长行，改一处漏一处）。订正口径＝**原文加删除线保留 ＋ 补 ✅ 收口注记并引 Q/C1 编号**：C1＝Q166/C1.110（并写明「原句自陈的前置条件『要做需改裁决』确已发生——负责人该批拍板把 analytics 提前入 V1，**翻转** Q97/Q114 徽标裁决」）、C2＝Q167/C1.111 `/admin/agent-keys`、C3＝Q168/C1.112 `/admin/exports`、#30＝Q187/C1.131 六项并经 C1.134 追认，且把解锁条件改写成「**追认只结设计选择**，180 天数值仍待主数据落地后校准」；§6.7 第 3 条「文档滞后已全部闭合」注明它**不是一条永久性质——本批即是对它的一次证伪**。**顺带查出第六处**：docs/02 文末「信息保全」续编名单断在 C1.131 Q187，缺 C1.132–C1.137，即**权威台账自己的索引过期**，已续至 C1.138。**边界严守事实纪律**：§6.6 其余 18 行未落地项与全部【待补】口径（DIM-SOURCE／video-studio／复检余两项／效果反哺校准／单价／embedding／图像／五型 SLA／G2 余 6 字段／slotType 与平台目录／触发方／语义四 code／Router fallback／draft 通道／G1／PWC 复核／Q70 二期／多币种）**一字未动**；判「已落地」的唯一依据是 02 存在对应 Q 条目，找不到即不改。**自我订正两处计数**：授权前口头汇报说「三处」，动手核对实测**六处**；第一次跑门禁把 eval 路径写成 `backend/eval`（实际在仓库根 `eval/`），ruff 因此报出一个**由我的命令造成的**假 error——照 CI 原文（`ruff check .` ＋ `python -m pytest -q` ＋ `python eval/runner.py`）重跑即净，教训是**门禁命令照 CI 抄、不凭记忆构造**。复跑实测：后端 **898 passed＋10 skipped**（70.33s，退出码从日志内读、不经管道）、ruff 净、eval **101/101**（golden 21/21）；`git status` 佐证本批只改两份 docs，未触碰前端与 infra 源码故未复跑前端门禁。
- **Q193 业务告警实触发：查出并修掉「首次事件系统性不可告警」（2026-09-25，零迁移；后端 893→**898 passed**＋10 默认 skip；`alerting-rehearsal.sh` 34→**39/39**；02 C1.137；负责人「你搞」）** — 把 Q188 七条业务规则从「能求值」推到「真会响」：演练叠加层开 `LOOM_EXPORT_WORKER_ENABLED`，停 redis 让 `POST /api/exports/jobs` 走 Q137 真实入流 fail-closed。**修前实测**：`/metrics` 已为 1、Prometheus 抓到 1，但 `increase(…[15m])` 恒 **0**、alerts 空、转发 0 次。根因是 counter 懒创建——该标签组合**首个样本就是 1**，Prometheus 看不见 0→1 跳变 ⇒ 进程内第一次事件系统性不可告警（低流量的 beta 要报的正是这一次）；单测验注册表增量、假 Redis 验语义形状，都盖不住。修法＝**启动时预置 0 序列，且由拥有词表的模块各自登记**（作业三类／两把锁／死信随被消费的流／预算按 `seeds.all_scene_codes()` 九场景）；**不换 `min_over_time`**——对累计计数器会永久常亮，是另一种坏。新增四条**顺序无关**守卫＋两条漂移守卫（`record_job_failed("…")` 字面量必须都在已预置集合内；`all_scene_codes()` 必须等于 `SCENE_*` 常量集）。**顺带修掉演练自身三处自伤**：阶段 4「恢复后可抓取」原是**永远绿的空断言**（规则名恒在列表），换真判据后又暴露固定 sleep 45 不够（entrypoint 先跑 alembic），改轮询收敛；`docker logs` **跨轮次累积**使去重断言假失败，改 `--since 本阶段起点`；以及**我宣布的「已清空重跑」其实没清空**——手敲 `down -v` 因 Grafana `:?` 口令在解析期插值而整条失败（Q185 记过的行为），留 10 容器 7 卷，于是同一轮同时出现假失败（去重数到 2）与假通过（15s 就 firing，是上一轮事件还挂在 15 分钟窗口里）。脚本现起栈前自检残留、非 0 即 ABORT。干净栈最终 **39/39 PASS**：firing 75s（正对 `for:1m`）、转发 105s 恰 1 次、`kind=export` 在负载内。固有局限注明：直方图不能预置空序列 ⇒ 上游 P99 规则需窗口内 ≥2 次调用，单次慢调用不触发。**教训：「clean slate」是一条需要被证明的断言，不能凭命令退出码宣布。**
- **Q192 Grafana 发布宿主回环端口 `127.0.0.1:3001`（2026-09-25，零迁移零后端应用代码；compose＋契约测试＋演练断言；负责人拍板「开 Grafana 回环端口」；02 C1.136）** — Q185 依 Q145「唯一发布口」刻意不给 Grafana 开宿主口，结果两张看板置备了却没人能从宿主打开（Q191 写值班手册时暴露：只能写「需临时转发、本仓未实测故不给步骤」，告警消费面最后一公里没通）。现开，但两条约束刻意写死：**只绑 127.0.0.1**（Grafana 唯一那道门是管理员口令，绑 0.0.0.0＝裸露全网）与**绝不能是 3000**（base compose 里宿主 3000 已是 frontend）。契约测试 `test_monitoring_services_publish_no_new_host_ports` 拆两条，新增 `test_grafana_is_loopback_only`（恰一个映射／绑 IP 字面量 127.0.0.1／容器口 3000／宿主口≠3000／与 frontend 宿主口求交为空）；**并按「守卫须能被证伪」自检**：内存构造四种坏配置（缺 IP／撞 3000／开两口／显式 0.0.0.0）全部判红、真配置判绿——首次证伪误用标量替换使 `ports` 变成字符串（"19 ports" 其实是 19 个字符），无效，已重做。`alerting-rehearsal.sh` 阶段 3 加两条**宿主侧**断言（从宿主机 curl `127.0.0.1:3001/api/health` 200、`docker port` 显示只绑回环）——网格内可达在没发布端口时同样成立，用它证明等于没验。全量重跑 **34/34 PASS**（Q188 时 32/32，+2），演练后 `down` 无残留。基线 **893 passed＋10 默认 skip**（原 892；Q192 把监控契约测试一条拆两条，**passed 净增 1**，非新增被测行为）。docs/21 §5 可达性段换成实测事实（含「远程看盘需自起隧道，该做法未实测故不作标准步骤」）；Q185 原「不新增发布口」的逐片记录不回改，只在 docs/17 续写变更。未做：隧道/反代方案、匿名或只读放行（现仍只有 admin 一道门）。
- **Q191 补值班面：docs/21 新增 §5「告警与值班处置」（2026-09-25，零代码零迁移零测试变化；02 C1.135）** — diff「已具备的能力 ⇄ 运营可达面」查出的缺口：Q185＋Q188 共立 **10 条告警规则**，而唯一给值班人看的 `docs/21` 里**「告警」0 次、「Grafana」0 次**——平台第一次能自己报出事，没人知道收到后先查哪里、要不要停线。新增 §5：① 三条承重前提（**默认部署一条都不发**／**一个故障只通知一次**（firing-only＋按 activeAt 去重、无 grouping/silencing/inhibition，故静默≠恢复）／**worker 默认关时两条队列规则永不触发是事实不是漏报**）；② 10 条规则→首查位置→是否停线表（每格只引用已核实的端点/审计 action/死信流字段/门控 env）；③ 数值来源纪律（100/5000/30s 与 180 天原文未给出，甲案虽已追认，**追认不豁免校准**）；④ `LOOM_GRAFANA_ADMIN_PASSWORD` 无默认值的运维事实。**刻意不硬编的一件事**：Grafana 按 Q145 未发布宿主端口，看板可达需临时端口转发——该配方本仓未实测，故不写进手册当标准步骤；是否开 `127.0.0.1` 回环端口属发布面收敛口径，**待负责人拍板**。**自查纠正**：初稿把超时开关写成不存在的 `LOOM_LLM_HTTP_TIMEOUT_*`，回查 `drivers.py:36` 实为 `LOOM_LLM_HTTP_TIMEOUT_SECONDS`（默认 60s），已订正。
- **Q188 补记·真 Redis 复核队列深度语义（2026-09-25，零生产代码改动；`test_real_infra.py` 8→9 例；默认基线 892 passed＋**10** 默认 skip（总收集 902，passed 不变）**） — 自查发现 `stream_depth` 的 ``XPENDING`` 汇总与 ``XLEN`` 语义**只在自建假件上验过**，而假件按实现者假设写成、**无法证伪该假设**（与 Q154 起真容器销「全内存替身」挂账同一理由）。起一次性 redis:7 容器实测三件事成立后固化为第 9 例：① `xpending` 汇总返回 `{'pending': int, min, max, consumers}`；② pending **只数已投递未 ACK**（建组后入流 3 条＝(0,3)、投递 2 条未 ACK＝(2,3)、XACK 后回落 (0,3) 而 XLEN 不变，两轴正交）；③ 组不存在时真 Redis 抛 `NOGROUP`→`ResponseError`（`RedisError` 子类，不同于 Q157 逃逸的 TimeoutError 情形）→原语归一 `StreamBackendError`→取样层吞掉并保持旧值。③ 是关键：这条链若断，一个纯观测读会把 worker tick 打成「失败→退避→再失败」的**无限空转**。带 env 实测 5 passed／4 skip（PG 组无 DSN 自然 skip），无 env 9 例全 skip 不进 CI。docs/16/17 同步。
- **Q190 四批甲案接缝追认销账（2026-09-25，零代码逻辑改动、零迁移、零测试变化，仅一处配置播种文案；02 C1.134；负责人回复选项「2」＝全追认）** — Q185（撤 Alertmanager 改 watchdog 轮询转发＋「只写真实注册指标名」口径）／Q186（`/admin/fcw` 表格下沉 client 岛等三接缝）／Q187（保留窗口落配置中心因而多出纯种子 0041、物理删＋逐行审计、取 `updated_at` 计时、有下游引用者留档不删、门控默认关、180 天·200 行）／Q188（severity 分档、作业失败仅 `kind` 标签、restock 流排除采样、不建看数页、不加门控、延迟只在网关层测）四批按甲落地的工程接缝**全部追认**，各条目内「标待负责人追认」就地订正为「已经负责人 2026-09-25 追认（C1.134）」，CHANGELOG 历史条目按先例不回改。**追认只结设计选择、不结数值校准**：180 天窗口与 100/5000/30s 三告警阈值原文未给出，合理性仍待主数据回填后重看。唯一非文档改动＝`content.discard_retention_days` 播种 `source_ref` 改标已追认（**存量库那行仍显示旧文案，本仓无种子回写机制、不为此补迁移**，注释留痕；全仓无测试断言该串）。**另记一次自我更正**：初稿把 AGENTS 里 Q122 的「待追认」措辞当成未决事项，回查 02 C1.66 实为 2026-09-19 已追认销账——缺陷是入口文档措辞过期，不是决策挂起，已订正。
- **Q189 文档对账批：docs/08 落地状态总览刷新 ＋ handoff 滚档收口 ＋ 复原一处上轮自伤（2026-09-25，**纯文档、零代码零迁移零测试变化**；02 C1.133；负责人「好，按你建议来」）** — 开工时逐项核对基线发现两处漂移：① **docs/08 §2.2 落地状态总览最后一行停在「截至 2026-09-20／最新 Q136／后端 699 测试／迁移头 0037／物理表 55→59」，与真实差 45 片**——这是任务包与排期文档，过期总览会直接误导接手者；② handoff「最近进度」11 条超「最近 5 条」上限。动作：**新增权威当前行**（Q188／0041／62 表／892 passed＋9 skip／eval 101/101／八 checker＋tsc＋next build／token 86／客户 nav 8／sidebar 12／五套演练数字＋七项未闭合及其归属），旧行不删就地标注为历史快照；§2.2 时间参考行续到 Q188；**超窗六条经内容级核验**（正文切片在本机全部归档里零命中，即并非已滚出的重复残留，删=丢信息），故**逐字搬入** `docs/handoff-archive-2026-09-25.md` 再删并补滚出说明；**另修一处自伤**：Q188 批以前缀式 Edit 插条目时误吞了 Q184–Q187 的标题头、使其正文沦为无归属续行，本批按原文复原。**刻意不补写 Q160 之后 29 条「切片完成情况」长段**——docs/02 是唯一逐片事实源，双写只会再造一次漂移。四批甲案接缝仍待负责人追认，本批不代裁。
- **Q188 R1 收口：四类业务信号真实打点 ＋ `loom-operations` 七条告警 ＋ 第二张看板 ＋ 契约守卫改双向（2026-09-25，零迁移零新 env 零前端改动；02 C1.132；负责人「按你建议来」授权）** — 销 docs/20 §6.5 唯一余项 R1 与 docs/17 §7.6 仍挂①。新模块 `backend/app/core/metrics/business.py` 落七族（`loom_job_failed_total{kind}` / `loom_stream_dead_letters_total{stream}` / `loom_stream_pending` / `loom_stream_length` / `loom_lock_lost_total{lock}` / `loom_llm_budget_blocked_total{scene}` / `loom_llm_upstream_duration_seconds{scene,provider,outcome}`），全部挂在**真实产出点**：作业失败取五个 DB 写点（含 router 入流 fail-closed 的 503 路径，因它调的就是同一个 `fail_*_job`）、死信取 `move_to_dead` 唯一漏斗（写死信＋ACK 双成功后才计，防重试双计）、锁易主取 `LockLease._mark_lost`（看门狗两分支共同漏斗，不在四个 catch 各计一次）、队列深度新增只读原语 `stream_depth`（`XPENDING` 汇总＋`XLEN`；`reclaim_pending` 那条带 idle 过滤且被 count 截断的 RANGE 不代表积压总量）在三处 worker `_tick` 末尾取样、LLM 取网关 `invoke`/`embed` 的预算硬停与驱动往返。不打 tenant/model_id 标签；LLM 直方图显式 1..120s 桶；**restock 流刻意不采**（Q138 只接生产侧，采它＝造一条永远正确的假积压告警）；`sample_stream_depth` 吞后端故障只记 debug（观测读不得干扰消费循环）。契约守卫从单向改**双向**（注册了却无人消费的族判红，同 Q176 死索引教训），指标名正则扩至认裸比较（原式漏过 `loom_stream_pending > 100` 这类 Gauge 引用），看板守卫改为遍历 provisioning/dashboards 全部 JSON。测试 879→**892**＋9 skip（新单测 11、契约 19→21、既有 worker/网关测试内联扩断言证真实接线；REGISTRY 进程全局故一律取前后增量）。**七条规则阈值（100/5000/30s）为工程默认值、原文未给出，连同六项接缝按甲案落地标待负责人追认**。
- **Q187 C4 收口：段12 discarded 成品保留期物理清理＝第五个 SLA sweep 作业（2026-09-25，**迁移 0041 纯配置种子、无 schema 变更**、业务物理表仍 62；02 C1.131；负责人「执行选项 B」按甲案）** — 销 docs/20 §6.5 边界模糊项 C4（Q124 挂账"随 V2"但归档三问未定义）。甲案：① 保留窗口＝配置中心 `content.discard_retention_days`（默认 **180 天**，`source_ref` 明标"原文未给出，待追认"，走 Q9 热更因属数据治理口径）；② 归档目标＝**物理删除＋逐行 `content.discard_purged` 审计**（不建归档表、不加新状态）；③ 索引＝**不动** Q124 迁移 0032 partial unique index。**承重安全边界**：只清理**无任何下游引用**的行——`effect_records.matched_content_id` 与 `effect_claims.content_id` 是硬 FK（Q126/Q127）、`import_jobs.content_id` 是 payload 可重放的作业历史（Q161），三条相关子查询 `~exists` 排除并永久留档，既免 IntegrityError 也不切断段13 时序；孤儿行 `matched_content_id` 为空不构成引用、不挡清理。计时基准取 discard 时写入的 `updated_at`（表无 `discarded_at` 列且 discarded 为终态）。**破坏性动作 env 门控 `LOOM_DISCARD_PURGE_ENABLED` 默认 false**（同 Q87/Q137/Q161 先例；关＝空转返回 0、V1 sweep 行为一字不变），批量上限 `LOOM_DISCARD_PURGE_BATCH=200`，二者按 Q135 先例不入 .env.example。实现在 `content/service.purge_expired_discarded`（不自行提交），`sla/jobs.py` 薄包装登记为 JOBS 第五作业 → 自动继承定时 sweep、Q139 checkpoint 协作中止、Q151 commit_guard 行级 fence 与既有手工 `POST /api/admin/sla/run`（**不新增端点、不新增管理页**）。新建 `tests/integration/test_discard_purge.py` **11 例**。迁移 0041 经 pg16 容器（宿主 55449）全链 0001→0041 up/downgrade-1/up 往返实测：表 62、列 666、`config_items` 61→62 行、downgrade 两表该键行归 0；**须记录的编排事实**——0010 系运行时 `import CONFIG_SEEDS` 全量播种，故全新库该键由 0010 插入（change_note `C2 seed`）、0041 幂等空转，**0041 的真实作用对象是已停在 0040 的存量库**，与 0021/0022 同型、两路终态收敛。六项接缝按甲落地**标待追认**。
- **Q186 D3.5 余项最小件：final_id 单条/多选复制 ＋ 六层原料包 JSON 一键存盘（2026-09-25，零迁移零后端改动；纯前端；02 C1.130；负责人「执行选项 B」授权）** — docs/20 §6.6 #10 的 D3.5 余项中**只有这两件不需新后端**（Q177 `/admin/fcw` 与 Q180 `/content/cards` 两口已够用），其余五项（组装工作台/6 层可视化编辑/合理性校验/审核台/冻结管理）经重扫判定均需新写口或新交互语义、仍随菜单点工。运营台新建 `admin/fcw/fcw-table.tsx`（client）：表格从 RSC 下沉为岛，**唯一理由是"本页选中态"为跨行共享 React state、RSC 无法持有**（刻意弃用初版的 `document.querySelectorAll` 命令式采集）；列首全选＋逐行 checkbox、工具条复制所选（换行分隔）＋实时已选计数、行内单条复制；`page.tsx` 仍 force-dynamic RSC 负责取数/筛选/分页并以 props 传 items。`material-island.tsx` 加 JSON 存盘（同 Q168 范式 `Blob`+`createObjectURL`+`revokeObjectURL`、不经裸 URL、不 `router.refresh`）。客户侧新建 `content/cards/copy-id-island.tsx` 并在 `card-material-island.tsx` 补同款存盘。剪贴板**沿用 Q167/Q178 密钥岛范式**（`navigator.clipboard.writeText` + try/catch + 同按钮换文案，不引 toast、不做 execCommand 兜底），样式全复用既有 token 类、零硬编码色值。check-admin（admin.fcw 必检键 +6、`fcwPage` 必含 token 由 MaterialIsland 改 FcwTable、新增岛守卫含**禁运行时 `@/lib/api` 但允许 `import type`**）与 check-content（copy-id-island 守卫段 + 三键）扩守卫；sidebar 计数不动（仍 12）、客户 nav 仍 8、token 仍 86。三接缝按甲落地**标待追认**。
- **Q185 指标消费侧：Prometheus 告警规则（仅真实注册指标）＋ Grafana 文件式看板 ＋ watchdog 告警转发（2026-09-25，零迁移零后端应用改动；纯 infra；02 C1.129；负责人「执行选项 B」授权）** — 补 Q181 只落了采集面的**消费半条**（docs/20 §6.6 #14）。**事实纪律修正（重要）**：原拟「队列积压 / 导出导入 job failed / 锁易主 LockLost / LLM 预算与延迟」四类规则经 grounding **全部作废**——全仓 `app/core/metrics` 真实注册指标只有 `up`/`http_requests_total`/`http_request_duration_seconds` 三族、`middleware.py` 之外**零业务打点**，写那四类等于引用不存在的指标名（臆造）。故只落 RED 三条基本盘：`LoomHigh5xxRatio`（>5%，带零流量守卫防 0/0=NaN）、`LoomHighP99Latency`（>1s）、`LoomBackendUnreachable`（`up{job="loom-backend"}==0`），并加**契约测试硬守**「规则与看板面板不得引用未注册指标」防回流。监控栈拆入独立 opt-in overlay `infra/docker-compose.monitoring.yml`（prometheus 挂 alert_rules ＋ grafana 文件式置备数据源 `loom-prom`/看板 `loom-http` ＋ 给 watchdog 注入 `PROMETHEUS_ALERTS_URL`）；**除既有 9090 外不新增宿主发布端口**。**改判撤 Alertmanager**：实测 0.34.1 `webhook_configs.url` 既不展开 `$(VAR)`（容器崩溃重启循环 `invalid URL: unsupported scheme ""`）也不支持 `env://`（`amtool check-config` FAILED），仅剩 `url_file` 需运维在 `LOOM_ALERT_WEBHOOK` 之外另挂 URL 文件、配置面翻倍 → 改为 **watchdog 轮询 `/api/v1/alerts` 转发进同一 `LOOM_ALERT_WEBHOOK`**（复用 Q146 已被单测证明的投递路径，零新容器），语义「只转发 `state=firing` ＋ 按 `alertname|instance|activeAt` 去重」，代价是放弃 grouping/silencing/inhibition。**两条 compose 硬事实**：`${VAR:?}` 系**解析期全局求值、与 profile 无关**，故 Grafana 口令 `LOOM_GRAFANA_ADMIN_PASSWORD`（无默认值、缺失即响亮报错、拒绝内置弱口令）的护栏只能与 opt-in overlay 同处一域；**wal-archiver 的 minio 依赖须 `required:false`** 否则 staging/ha overlay 下整个 project 判 `invalid compose project`。**顺带修掉 Q182 两个致 compose 整体不可用的遗留缺陷**：全新卷 `docker compose up` 必挂（官方镜像以 postgres uid999 跑 initdb.d 脚本，对 root:root 空命名卷 chown/chmod 双双 EPERM → `set -e` 打死 postgres），改由一次性 root 服务 `wal-archive-init` 预备卷属主。`tests/unit/test_backup_monitoring_contract.py` 6→**19 例（+13）**。新建 `infra/alerting-rehearsal.sh` 真栈四阶段 **24/24 PASS、exit 0**（本地手动不进 CI，docs/17 §7.6），含停 backend→up 转 0→firing→watchdog 转发且 180s/~6 轮**去重命中恰 1 次**、规则 `health` 全 ok（真 TSDB 上可求值）、Grafana 数据源代理查回真值；演练另抓出并修掉本片脚本自身四处断言缺陷（`$?` 串联误判／rules API 须 `type=alert`／Grafana 代理须 GET query-string 而非 POST JSON body／告警名含数字致 `[A-Za-z]*` 正则截断计数）。仍挂业务级打点（＝§6.5 R1）。
- **Q184 文档对账收口：过期 push 标注订正 ＋ docs/20 §6.5 候选清单按 post-Q183 现状重写（2026-09-25，零代码零迁移；纯文档；02 C1.128；负责人「执行选项 B」授权）** — ① 订正 handoff L3/L78/L80 与 docs/20 §6.5 第六次更新中过期的「Q180–Q183 已原子提交、未 push」（实测 `dev == origin/dev == de08de2` 早已全部推送；先例 Q176 第③项同类订正），历史条目中描述当时订正动作的「未 push」引文按原样保留不动；② **§6.5 整体重写**——原「可一口气推进（4 个候选）A1–A4」早已全部落地（A1=Q162、A2=Q164、A3=Q163、A4=Q165）、边界模糊项 C1/C2/C3 随 Q166–Q168 落地、C5 随 Q176 丙案关闭、C6 随 Q165 关闭，该清单长期展示"全为空号"的候选表会误导接手者；重写后新增「本批四件（Q184–Q187）」表、C4/C6 销账、并给出重扫后**唯一同类余项 R1**（业务级指标打点＋对应告警，需改后端应用代码故建议单独点工）。**结论：零依赖·纯技术·不破坏基线的池子已基本抽干**；private beta 唯一硬阻塞仍是业务方按 docs/19 回填首批主数据。本条只做对账与结构重写，不新增业务事实、不擅自升级任何状态词。

- **Q183 文档收口：运营 SOP 第二版＋README/AGENTS 同步 Q179–Q182＋全量门禁复跑（2026-09-24，零代码零迁移；02 C1.127；负责人「一口气搞了」）** — Q178–Q182 落地后的文档同步与验证批次，不新增业务事实。docs/21 运营 SOP：① 订正「V1 无真实认证层、身份层压 V2 第一项」过期口径（已被 Q178 staff PAT 推翻），新增 **§0.1 运营登录与身份**（人员 `loom_staff_` 与机器 `loom_` 分表分前缀不可互换、限五内部角色、门控默认关、鸡生蛋引导三步、/admin/staff-keys 与 /admin/login、回滚＝关 env）；② §0 角色表管理台 10→12 页、platform_admin 入口补 staff-keys/exports/fcw；③ 段 11 补 Q177/Q180 白名单卡片查看（运营 /admin/fcw 跨租户、客户 /content/cards 单租户）；④ §3 401 行补 staff 令牌场景；⑤ §4 补 GET /metrics（Q181，monitoring profile 默认不启）与备份恢复双轨（逻辑 pg_dump＋restore-rehearsal／物理 WAL＋`infra/wal/pitr-rehearsal.sh`）及 ha/load 演练。README「当前状态」补 Q179–Q182 四片与 855/0040/62 表基线、订正 Q177 追认状态；AGENTS「当前阶段」补 Q179–Q182 长条、Q178 接缝订正为已经 Q179 追认（C1.123）。本批实跑验证：后端全量 **855 passed + 9 skipped**（156.75s）、eval **101/101**（golden 21）、前端 tsc 零错＋八 checker 全 OK＋next build 通过；docs/16（Q179–Q182 与 855 台账）、docs/17（§1/§7.5）核对完整无需补。同日补遗：docs/20 追加第六次更新追记 Q179–Q182（原评审追记止于第五次 Q178；§6 待办指标监控/WAL·PITR/RPO·RTO 销线），handoff L3「待原子提交」订正为已提交未 push、交接 Q 范围刷至 Q1–Q183。
- **Q182 WAL 增量归档＋对象存储基础备份＋PITR 端到端演练（2026-09-24，零迁移；postgres 归档 + db-basebackup/wal-archiver 两服务 + `infra/wal/` 脚本，02 C1.126；负责人「一口气搞了」）** — 销 docs/20 §6.5 A4「WAL 增量＋对象存储跨区冗余」。postgres 开归档（archive_mode=on/wal_level=replica/archive_timeout=300，归档 walarchive 卷）；`wal/base-backup.sh` 周期 `pg_basebackup -Ft -z -X stream`；`wal/archive-wal.sh`（mc 镜像，**显式 `mc alias set`**）把 WAL 段与 .done 基础备份推 MinIO（WAL 成功后删本地、base 推 base/ 前缀）；`wal/init-replica-hba.sh`（首次 init）补 `host replication` pg_hba 并 chown 归档卷。`wal/pitr-rehearsal.sh` 自包含端到端演练（临时网络＋全命名卷，trap 清理）：**先**空库基础备份（必须早于 T）→造 before/取 T/after→推 MinIO→拉回→解 base/pg_wal＋recovery.signal/postgresql.auto.conf（restore_command/recovery_target_time=T/promote）→恢复库断言 **before=1、after=0**（实测通过「rolled forward to T exactly」）。工程坑实录：① 官方 pg 镜像 pg_hba 只追加 `host all all all`、不含 replication 条目，pg_basebackup 跨容器 FATAL；② 命名卷默认 root，归档卷 chown 999、MinIO 卷 chown 1000；③ mc entrypoint=mc 跑 shell 须 `--entrypoint sh`、MC_HOST env 在 sh 场景不加载（写入不落 MinIO）；④ Docker Hub 用户命名空间镜像（minio/mc、prometheus）拉取被拒，统一改 **quay.io** 源。零迁移（仅 postgres 参数＋sidecar＋脚本），RPO 由日级缩短到分钟级并具备 PITR；跨 region 冗余仍 V2。
- **Q181 指标级监控：零依赖进程内指标库＋HTTP 中间件＋GET /metrics（2026-09-24，零迁移零新依赖；`app/core/metrics/` 全包 + prometheus profile，02 C1.125；负责人「一口气搞了」）** — 销 docs/20 §6.5 A3 与任务板 #41。不引 prometheus-client，自写 Counter（只增/标签分区）、Gauge（set/inc/dec）、Histogram（默认桶 .005…10s＋Inf，记 count/sum/各 le 桶）与 Registry（render `# HELP`/`# TYPE`＋样本，标签值转义）；纯 ASGI `MetricsMiddleware` 记 `http_requests_total{method,path,status}` 与 `http_request_duration_seconds`，**path 取路由模板 scope["route"].path**（如 /api/fcw/{final_id}，避免原始 id 高基数，无匹配记 unmatched）；`GET /metrics`（Prometheus 文本，默认不鉴权同 /healthz）。可选 prometheus（quay.io/prometheus/prometheus，`monitoring/prometheus.yml`，**compose profile `monitoring` 默认不启**）。测试 tests/unit/test_metrics_registry.py 8 例 + tests/integration/test_metrics_api.py 2 例（路由模板标签、原始 id 不入标签）。
- **Q180 D3.5 客户白名单卡片视图：content 子页 /content/cards（2026-09-24，零迁移；后端客户集合口＋前端卡片页，02 C1.124；负责人「一口气搞了」）** — Q177 管理端卡片台（/admin/fcw）的客户侧对应。后端新增客户集合口 `GET /api/fcw?tenant_id=`（无闸、强制单租户，复用 `list_fcw_admin(tenant_id=,limit=100000)` 逐条 fcw_view；区别于 Q177 管理口读闸可跨租户），单条六层包沿用 Q155 material.json；tests/integration/test_fcw_admin_api.py +3（缺 tenant 422/t1·t2 隔离/空租户 []）。前端新增 content 子页 `content/cards/`（客户 nav **不增项**仍 8，经 content 页 subnav）：page.tsx（force-dynamic RSC，listMyFcw 卡片列表＋publish_status chip＋元信息 dl，内嵌 CardMaterialIsland，含 unconfigured/empty 态）、card-material-island.tsx（client，点击经 getFcwMaterialAction 拉六层 pre 展示，不直连/不 refresh）、cards.module.css；lib/api 加 FcwCardView/listMyFcw/getFcwMaterial（六层类型**复用 Q177 FcwMaterialPack** 不重复定义）；messages 加 content.cards.*；check-content 加 cards 守卫。D3.5 其余（组装工作台/六层编辑/审核台/复制 ID）随菜单点工。
- **Q179 第四轮 DBA 全量列复核（2026-09-24，零迁移；一次性 pg16 容器实测，02 C1.123；负责人「一口气搞了」）** — Q178 迁移 0040 staff_api_keys 在第三轮复核（61 表/654 列/215 索引）之后落地，按 docs/10 挂账纳入本轮。容器 loom-dba4（宿主 55448）upgrade head 后导出 information_schema/pg_indexes/pg_constraint 并与 ORM `Base.metadata` 逐表比对：业务表 db=62/orm=62 无单边表，总列数 **666**（654＋staff_api_keys 12 列对账闭合）、业务索引 **219**（215＋四索引：staff_api_keys_pkey/ix_staff_id/ix_key_hash/uq_key_hash）、约束 PK62/FK32/**UQ21**（较第三轮 +uq_staff_api_key_hash），**62 表模型⇄迁移列名零漂移**。纯只读复核、零 pytest 不进 CI。同期 Q178 三接缝（旋钮按 Q135 先例不入 .env.example、认证授权分离零端点签名改动、客户侧后置 V2）已经负责人 2026-09-24「一口气搞了」追认。
- **Q178 身份层 P0-① 甲案第一切片：内部运营人员令牌 PAT（2026-09-24，一迁移 0040 staff_api_keys、业务物理表 61→62；后端 817→842〔+25 集成〕＋9 默认 skip，前端 sidebar 11→12；02 C1.122；负责人经 interaction.ask 拍板甲案）** — docs/20 §6.6「需重裁」第一项身份层启动，纯加法、门控默认关、不破坏 V1 actor 自报、可回滚；第一切片仅内部运营管理端，客户侧真实认证与 actor↔tenant 绑定仍 V2。后端新包 `app/core/staff_auth/`（models/context/service/schemas/deps/router）：staff_api_keys 表（staff_id 非唯一索引支持一人多 token 轮换、roles JSONB、key_hash SHA-256 unique、明文 `loom_staff_`+token_urlsafe(30) 仅签发回一次、与 Q88 机器 agent_api_keys 的 `loom_` 前缀分表分域不可互换、无 tenant_id 审计恒 `_platform`）；可签发角色限 operations/platform_admin/product_reviewer/dictionary_admin/internal_compliance 五内部角色（不含 whitelist_owner，空/未知/客户角色 422）；端点 POST/GET `/api/admin/staff-keys`、POST `/api/admin/staff-keys/{key_id}/revoke`（platform_admin 红线）、GET `/api/auth/me`（门控关 400/开无令牌 401），agent-keys GET 补 `internal_gate(platform_admin)`。**认证与授权分离、HTTP 端点签名零改动**：认证＝app 级全局依赖 `staff_auth_context`（门控 `LOOM_STAFF_AUTH_ENABLED` 默认关；开则仅验 `loom_staff_` Bearer、独立短事务验真并把人员 Actor 写入请求级 contextvar；无令牌/机器 `loom_`/客户口/公开口直接放行，坏或吊销 staff 令牌 401）；授权＝唯一收口 `rbac.require_any_role`（新增 NotAuthenticated 401/PermissionDenied 403，门控开内部口无认证人 401、角色不足 403、通过即就地用令牌身份覆盖自报 actor.id/roles——拼 `?actor_id&roles=platform_admin` 提权失效、审计落真实 staff_id；门控关或纯客户 whitelist_owner 维持 V1 自报）。前端 lib/staff-auth.ts（httpOnly/sameSite lax cookie）、lib/api 自动注入 Bearer 且管理口 401 跳 /admin/login；新建 /admin/login（令牌录入/登出）与 /admin/staff-keys（签发/列表/吊销）、sidebar 第 12 项（login 不进 sidebar）；check-admin 扩 staff-keys/login 专属守卫段＋后端路径断言。引导（鸡生蛋）：门控仍关时由自报 platform_admin 签发首个令牌→/admin/login 录入→置 LOOM_STAFF_AUTH_ENABLED=true 重启；回滚＝关 env。测试 tests/integration/test_staff_auth_api.py 25 例（StaticPool 共享认证/业务两 session、override get_session 与 get_auth_session；先门控关 _bootstrap 签发再 monkeypatch 开闸防 settings 单例跨测泄漏；覆盖防 query/body 自报提权、机器 loom_ 与客户口不受影响、internal_gate、auth.me）。迁移 0040 经 pg16 容器宿主 55442 up/downgrade-1/up 实测；ruff 净、eval 101/101、前端 tsc＋八 checker＋next build 全绿、token 仍 86/客户 nav 仍 8。工程接缝（旋钮按 Q135 先例不入 .env.example、认证授权分离零端点签名改动、客户侧后置 V2）标待负责人追认。
- **Q176 丙案裁决 ＋ Q177 接缝追认 ＋ push 标注订正（2026-09-24，纯文档、零代码零迁移；02 C1.120–121 追认注记）** — 负责人「那你一口气搞了」授权收口：① **Q176 ATOM-AFFINITY KNN 索引按工程推荐丙案（暂不做 / YAGNI）拍板销账**——不写迁移 0040、不建 HNSW/IVFFlat、不改 Q86 应用层内存余弦计算路径；甲（只建预备 HNSW）/乙（建索引＋KNN 下推 DB、改 Q86、须 eval 回归）两档归档备查，待真出现 DB 层 KNN 查询消费方或 D3.7 RAG 检索落地再重评；② **Q177 D3.5 白名单卡片台三接缝甲案追认销账**（入口落管理端运营台 /admin/fcw 第 11 项、新增跨租户只读列表口、六层内嵌面与 Q155 导出口共用 build_material_pack 但分两口）；③ 订正 handoff 4 处过期「未 push」标注（Q169–Q172/Q173/Q174–Q175/Q177 各笔经核实均已 push，dev 与 origin/dev 同步、origin/dev 头 1cce400）。基线不变：后端 817 passed＋9 默认 skip、eval 101/101、Alembic 头 0039/物理表 61、前端 tsc＋八 checker＋next build 全绿、管理端 sidebar 11 项、token 86。
- **Q177 D3.5 白名单组装引擎运营只读首片：管理端白名单卡片台（2026-09-23，零迁移；后端两口＋7 集成测试、前端 admin/fcw，02 C1.121；负责人「一口气搞了」，三接缝甲案已经负责人 2026-09-24 追认）** — 销 docs/20 §6 边界项 #10「Q155 台内卡片 UI」的运营只读腿。后端 `final/final_whitelist`：①`GET /api/admin/fcw` 跨租户分页列表（query `actor_id` 必填＋`roles` 可重复，闸 operations|platform_admin，缺 actor 422/越权 403；`tenant_id` 可选过滤、`limit` 1..200 默认 50、`offset≥0`；envelope `{count,total,limit,offset,has_more,items:[fcw_view 元信息]}`，列表不含六层大包；`service.list_fcw_admin` 按 created_at/final_id 排序）；②`GET /api/admin/fcw/{final_id}/material` 六层包**内嵌 JSON**（同款读闸、**无** Content-Disposition 头、复用 Q155 `material.build_material_pack`、未知 id 404、纯只读不触发 Guard 不写审计）；Q155 原导出口 material.json（attachment、无闸）保留不动。`tests/integration/test_fcw_admin_api.py` 7 例（422/403〔OWNER·NOBODY〕/跨租户见 2 条＋tenant_id 过滤＋platform_admin 可读/limit·offset 不重不漏＋limit=201 判 422/内嵌六层齐全且无下载头/越权 403/缺 actor 422/未知 404）。前端新目录 `app/[locale]/admin/fcw/`：`page.tsx`（force-dynamic 纯只读 RSC，sidebar 第 11 项；tenant_id 留空＝全部租户、分页、枚举 platform/goal/publish_status 原样直出）、`actions.ts`（Server Action getFcwMaterialAction，本地角色闸 operations/platform_admin，映射 403/404/409/422/unconfigured/missing_role）、`material-island.tsx`（client 岛，点击才经 action 拉六层，details/pre 分层＋warnings/guards，不直连 API、不刷新列表）；lib/api 加 listAdminFcw/getAdminFcwMaterial＋三类型；messages 加 admin.fcwNav 与 admin.fcw 全套；check-admin 加 Q177 守卫段、sidebar 计数 10→11。三接缝甲案（入口落管理端运营台／新增跨租户只读列表口／内嵌面与导出口共用 build_material_pack 但分两口）已经负责人 2026-09-24「那你一口气搞了」追认；D3.5 其余（组装工作台/6 层可视化编辑/合理性校验/审核台/冻结管理/客户卡片视图/复制 ID）随菜单点工。后端 810→817（+7）、skip 仍 9；ruff 净、eval 101/101；前端 tsc＋八 checker＋next build 全绿、token 仍 86、客户 nav 仍 8。
- **Q176 ATOM-AFFINITY KNN 索引评估：丙案（暂不做/YAGNI）裁决销账（2026-09-23 评估、2026-09-24 负责人「那你一口气搞了」按丙案拍板，零代码/零迁移；02 C1.120）** — docs/20 §6 C5 原列「KNN 索引/算子下推」为纯性能优化，grounding 全仓 grep 对 `<=>`/cosine_distance/l2_distance/max_inner_product **零命中**（app 与 tests 除 real-infra 外），affinity/cluster_id 按 Q86 在应用层内存确定性重算（atom_expand.py 同维 basis＋批内一次 embed、max_affinity 本地余弦＋同维并查集、模型自报值剥离）。故裸建 HNSW/IVFFlat 是**无查询消费方的死索引**（YAGNI），下推 DB KNN 则改 Q86 已定计算路径属设计变更。列三档待裁（甲只建预备 HNSW 迁移 0040／乙建索引＋下推改 Q86 需 eval 回归／丙暂不做，工程推荐丙）；本轮不写迁移 0040、不建索引、不改 Q86。**2026-09-24 负责人「那你一口气搞了」按丙案拍板：暂不做（YAGNI），不写迁移 0040、不建索引、不改 Q86；甲/乙两档归档备查，待真有 DB KNN 查询消费方或 D3.7 RAG 落地再重评。**
- **Q175 RPO·RTO 量化达标认证（2026-09-23，零迁移；infra/rpo-rto-rehearsal.sh + docs/17 §7.4，02 C1.119；负责人「一口气搞了」）** — 故障注入＋计时恢复演练 12/12 PASS：源库造 tenants 2 行→Q146 同款 pg_dump（T_DUMP）→备份后再写 1 行→`docker rm -f` 源库（T_FAIL）→新机 drop/create＋pg_restore＋应用 ORM 只读冒烟（全程计时）。**RPO 观测 1.919s**（生产每日备份频率保证 ≤24h，备份后新增行确认丢失、恢复 tenants=2）、**RTO 观测 70.111s**（≤4h；认证工程恢复动作，不含人员发现/决策）。纯 infra shell＋文档、零 pytest 不进 CI（同 Q158 先例）。
- **Q174 前端批量回填/Excel 导入岛切异步任务轮询（2026-09-23，零迁移/零后端；纯前端消费 Q161 imports jobs，02 C1.118；负责人「一口气搞了」）** — docs/11 唯一 V2 尾项闭合。lib/api 加 BackfillImportJobView 类型与 submitBackfillImportJob/getBackfillImportJob；content/actions 加 submit/poll 两 Server Action（re-export 视图类型供岛用）、删除不再使用的同步 batchBackfill* 两 action；批量岛提交 `/jobs` 后 useEffect 每 2s 经 poll action 轮询（封顶 90 次＝180s；404 置失败、瞬时错误重试），completed 显 matched＋router.refresh、failed 渲染 job.errors 逐行或 error；messages 加 backfillBatchProcessing/backfillBatchPollTimeout；check-content 守卫锁定 Q174。门控关提交即终态、门控开 queued 轮询。tsc＋八 checker＋next build 全绿。
- **Q173 持续高压负载演练 + jobs 列表 offset（2026-09-23，零迁移；02 C1.117；负责人「那你推进」）** — 2 副本 HA + 真 PG/Redis（synthetic 不花钱）5 分钟持续混合负载 9/9 PASS：562 export job 提交==completed、零 failed/5xx，SLA/restock `/run` 200=414/409=1842，副本零重启/OOM/Traceback。演练抓到 `GET /api/exports/jobs` 页大小硬顶 200 且无 offset（Q142 漏网），修 service/router 透传 offset。新增 `infra/load-rehearsal.sh`（隔离 `--rm` driver，不 exec 进 app 容器）。后端 809→810。
- **Q172 真 LLM 全链演练 + 驱动超时配置（2026-09-22，零迁移；02 C1.116；负责人 B4 授权）** — agnes CAT-RECOG/ARTICLE-GEN 真模型沿 e2e 全链贯通（gated 默认 skip，46.3s 两测全过，日预算内）；远程驱动新增 env `LOOM_LLM_HTTP_TIMEOUT_SECONDS`（默认 60＝原硬编码，演练取 180）。
- **Q171 DBA 第三轮全量列复核（2026-09-22，零迁移/零生产代码；docs/10 §4，02 C1.115）** — head=0039 实测 61 表/654 列/215 业务索引/PK61·FK32·UQ20，ORM 逐表逐列零漂移。
- **Q170 运营 SOP 全链操作手册（2026-09-22，纯文档；docs/21，02 C1.114）** — 段1→13 逐步操作与 4xx/5xx 处置，步骤经 Q169 e2e 实测。
- **Q169 全链 golden path e2e（2026-09-22，零迁移；tests/e2e + infra/fullchain-rehearsal.sh，02 C1.113）** — 一个产品沿真实 API 走段1→13，断言 final_id/content_id/matched=1；sqlite 入 CI，真 PG 一次性容器本地手动。后端 808→809（+1，真 LLM 变体 skip 不计常规）。

- **Q168 中台导出 job 管理前端页（2026-09-22，零迁移/零后端；纯前端消费 Q132/Q137，02 C1.112）** — 平台代运营补导出任务管理与下载 UI。新目录 `app/[locale]/admin/exports/` 四件：`page.tsx`（force-dynamic 只读 RSC，顶部纯 GET 租户过滤表单 name=tenant_id、无租户提示先查询、有租户列 jobs——job_id 短码/format·status 枚举原样 chip/row_count/file_name/created_at/completed_at/error，data-testid=exports-admin·exports-tenant-filter）、`create-island.tsx`（client 选 csv|json＋选填 product_space_id，createExportJobAction 成功 router.refresh；门控关同步 completed、开 queued）、`download-island.tsx`（client 仅 status=completed 可点，经 Server Action 取回文本后 Blob＋URL.createObjectURL＋a.download 触发浏览器落盘并 revokeObjectURL，queued/running/failed 禁用、409 提示 notReady）、`actions.ts`（"use server"，未配 CURRENT_ADMIN_ACTOR_ID 本地 unconfigured，映射 403/404/409/422）；lib/api 加 ExportJobView/ExportJobsPage/ExportDownload 类型与 listExportJobs/createExportJob/downloadExportJob，并新增**文本下载通道 requestText**（csv/json 均文本、解析 Content-Disposition 的 filename 与 content-type，不经浏览器裸 URL、不走 NEXT_PUBLIC）；中台面无 RBAC 闸、actor 仅留痕（同 Q100/Q132）；admin-sidebar 加第 10 项 /admin/exports；messages 加 admin.exportsNav 与 admin.exports（25 键，status/format 枚举码原样不翻译）；check-admin 计数 9→10 加 Q168 守卫（四件齐备/page 只读禁 POST/两岛 "use client" 禁直连 lib/api·fetch/下载必须走 Blob+createObjectURL/后端 exports prefix 与 /jobs、/jobs/{id}、/jobs/{id}/download 齐备）。tsc＋八 checker＋next build 全绿（新路由与 client 岛均编译）。
- **Q167 入站 Agent Key 治理前端管理页（2026-09-22，零迁移/零后端；纯前端消费 Q88 三端点，02 C1.111）** — 平台代运营补 Agent Key 签发/吊销 UI。新目录 `app/[locale]/admin/agent-keys/` 四件：`page.tsx`（force-dynamic 只读 RSC，列表 name/key_prefix/status(active|revoked 枚举原样 chip)/created_at/last_used_at，?include_revoked=true 切换，data-testid=agent-keys-admin；列表 GET 免闸不返 secret）、`actions.ts`（"use server"，issue/revoke 两 Server Action，本地 guardPlatform 校验 CURRENT_ADMIN_ACTOR_ID 存在且 ADMIN_ROLE_LIST 含 platform_admin，缺身份 unconfigured/缺角色 missing_role 零请求，映射 403/404/409/422/unknown）、`issue-island.tsx`（client name 输入签发，secret 明文仅成功时在 data-testid="issued-secret" 一次性驻留展示＋navigator.clipboard 复制＋醒目警告，成功 router.refresh）、`revoke-island.tsx`（client window.confirm 终态确认、已吊销禁用、成功 router.refresh）；lib/api 加 AgentKeyView/AgentKeyIssued 与 listAgentKeys/issueAgentKey/revokeAgentKey（写口 body actor={id:CURRENT_ADMIN_ACTOR_ID,roles:ADMIN_ROLE_LIST}）；admin-sidebar 加第 9 项 /admin/agent-keys；messages 加 admin.agentKeysNav 与 admin.agentKeys（25 键，revokeConfirm 带 {name}）；check-admin 计数 8→9 加 Q167 守卫（四件齐备/page 只读/两岛禁直连/secret 一次性区/window.confirm/后端三路由与 service PLATFORM_ADMIN 闸在位）。后端签发/吊销 service 层硬闸 platform_admin（Q88），secret 只在签发响应回一次、任何读路径不回显。tsc＋八 checker＋next build 全绿。
- **Q166 客户效果 analytics 数据分析页（2026-09-22，零迁移；后端 service/router＋5 测试、前端真页，02 C1.110；负责人本批拍板翻转 Q97/Q114 裁决提前入 V1 private beta）** — 段13 客户只读聚合。后端 effects 域新增无闸只读 `GET /api/effects/analytics`（tenant_id 必填 query、date_from/date_to 可选 YYYY-MM-DD、from>to 422、date_to 含当天＝排他上界次日 00:00 UTC）；`service.customer_analytics` 口径＝只统计 status=matched 且 tenant_id 命中（orphan 运营面不向客户暴露、他租户不串、未知租户 200 空、不写审计），六计数 plays/likes/comments/shares/inquiries/conversions 按存在键稀疏累加（缺席不补 0），read_rate 比率不可求和、仅对含键记录平均（round 4，分母 read_rate_samples），by_content 按 plays 降序 top 50（ANALYTICS_TOP_CONTENT=50），扫描上限 ANALYTICS_SCAN_LIMIT=10000 超限置 truncated。前端 `(shell)/analytics` 由 MenuPlaceholder 改真 RSC（force-dynamic，searchParams Promise 需 await；nav.ts 该页 phase v2→v1，客户 nav 仍恰 8 项）：内联纯服务端 GET 日期表单、KPI 卡＋六计数卡＋by_content 表（content_id 链详情），data-testid=analytics-overview/filter/kpis/by-content；新增 analytics.module.css（全语义 token）；lib/api 加 EffectAnalytics/getEffectAnalytics；messages 加 analytics 命名空间；check-content 加 Q166 守卫。test_effect_analytics_api.py 5 例，后端 803→808。效果反哺 KUP-PROPOSE 校准仍随 V2。
- **Q165 FCW 组装异步 worker（2026-09-22，零迁移；后端 4c4902e＋测试 7d8f4dc，02 C1.109）** — 销 docs/08 §2.2 M8「Q55 批量组装异步 worker 随 M10」挂账。新模块 `app/final/fcw_worker/`（FcwWorker 镜像 ExportWorker/ImportWorker，流 `loom:fcw-assembly`/组 `fcw-assembly-group`/死信 `loom:fcw-assembly:dead`，`build_fcw_workers` 多 consumer 工厂）；`final_whitelist/service.py` 重构 `create_task` 拆为 `create_task_record`（验参＋建 task 行，status 由门控决定）＋`process_task`（循环 assemble_one→收集 issued/failures→写 results/status=completed/completed_at/fcw.assembly_task 审计），`create_task`（同步保留）= record(running)＋process 同 session（V1 行为不变）；router 门控分支：关＝同步（原行为），开＝queued＋commit＋XADD，入流失败 `session.delete(task)`＋commit＋503 fail-closed 无 task 残留；4 个 env 旋钮默认全关保 V1 单副本行为：`LOOM_FCW_WORKER_ENABLED`=false、`LOOM_FCW_STREAM_BLOCK_SECONDS`=5.0、`LOOM_FCW_WORKER_CONCURRENCY`=1、`LOOM_FCW_STREAM_COUNT`=20（后三不入 .env.example）；main.py lifespan 门控启停；状态/结果查询复用已有 `GET /api/fcw/assembly-tasks/{task_id}`，前端零改动（V1 不做任务管理 UI）。PT-FCW-ASM 七 Guard 原子性不弱化（assemble_one 无独立 commit，worker 路径 process_task 末尾统一 commit，测试实证 fcw.assembly_blocked 审计完整）。`tests/integration/test_fcw_worker.py` 11 例（门控关同步回归/门控开 queued 不组装/worker 消费到 completed+ACK/状态流转/终态幂等 ACK/per-slot 失败隔离/幽灵 task ACK/入流失败 503/基础设施异常留 PEL/超限死信+failed/worker 路径审计完整），FakeStreams 内存替身。零迁移（`fcw_assembly_tasks.status String(16)` 已表达 queued/running/completed/failed）。接缝待追认：actor 重建（task.created_by+roles=["operations"]）、入流失败策略（delete+503 无残留）、幽灵 task ACK、concurrency 预留、失败无 error 列（results.error 截断存因）。
- **Q164 管理端驾驶舱日期筛选器 UI（2026-09-22，零迁移/零后端；前端 9ac6245，02 C1.108）** — 销 Q104–Q108 ⑤挂账。纯前端：新建共享组件 `admin/_components/DateRangeFilter.tsx`（纯服务端组件无 "use client"，`<form method="get">` 两个 `<input type="date">` name=date_from/date_to＋应用按钮＋i18n Link 重置，导出 defaultFromIso/defaultToIso/isIsoDate/asSearchParam 工具）；`admin/token-cost/page.tsx` 与 `admin/review-workload/page.tsx` 读 `searchParams.date_from/date_to` 传给后端 API（Q92 后端已支持 date_from/date_to 查询参数，默认近 30 天）、渲染筛选器（from>to 行内提示＋透传后端 422）；`lib/api.ts` 两驾驶舱函数加可选 `DashboardWindowOpts {dateFrom, dateTo}`；messages 加 admin.dateFilter(7) 键；check-admin.mjs 扩守卫。tsc exit 0、八 checker 全绿、token 仍 86。
- **Q163 客户 Onboarding 前端引导页（2026-09-22，零迁移；后端 24d9b0a＋测试 4d310ea＋前端 eefbb2d，02 C1.107）** — 销 Q95 ④/Q105 ⑤挂账。后端（接缝甲案）：`CustomerTenantView` 追加 `onboarding: dict` 字段，customer_router 的 `get_current_tenant` 调用既有 `service.onboarding_progress(session, tenant_id)` 填入返回（复用 Q95 服务函数，不新建端点）。前端：工作台 `(shell)/workbench/page.tsx` 内嵌引导岛（客户 nav 不增项，同 Q131 先例）——`deriveSteps(onboarding)` 四步状态（注册开通恒完成/第一产品建模=intakes>0/AI拆解=product_spaces>0/内容上线=product_spaces>0 近似 active，精确 FCW 成品判断随 V2），`intakes===0` 渲染完整引导卡（data-testid=onboarding-guide＋`/products/new` CTA），`intakes>0` 渲染紧凑进度条；messages 加 workbench.onboarding(10) 键；check-content.mjs 扩守卫。`tests/integration/test_tenants_api.py` 新增 `test_customer_tenant_onboarding_reflects_intakes`（原 test_customer_tenant_read 改写断言 onboarding 存在），+1 净增。接缝待追认：CustomerTenantView 追加 onboarding 派生字段（客户读口追加非新建端点）、内容上线步用 product_spaces>0 近似。
- **Q162 客户合规风控页扩展三件套（2026-09-22，零迁移；后端 cf47e9e＋测试 1d77471＋前端 eefbb2d，02 C1.106）** — 销 Q104–Q108 ⑤连续挂账。后端新增 4 个无闸客户侧只读 GET（同 Q101 口径：tenant_id 必填 query、未知租户 200 空、不 writeAudit）：①`GET /api/compliance/ccr-history`（全租户 CCR 历史 pws×country 时间倒序，可选 pws_id/country 过滤，limit 默认 50/上限 200/offset 分页甲案）；②`GET /api/compliance/ccr/{ccr_id}`（按 tenant 收窄单条含 hits 完整 JSON，越权/不存在 404）；③`GET /api/compliance/law-reviews`（服务端派生 `sla_remaining_seconds`＋`sla_state`(normal/overdue/resolved)，复用 `ccr_rules.law_review_due_hours()`=48h，倒计时服务端派生不做客户端 tick）；④`GET /api/compliance/wordlist`（只读 active 词条，可选 level/layer 过滤，`list_entries` 加 layer 可选参向后兼容）。前端 `(shell)/compliance/page.tsx` 扩三子岛（data-testid=ccr-history/law-sla/compliance-wordlist）：CCR 历史列表取近 20 条逐行补 hits、法审卡片倒计时小时级、词库表格按 level 筛选；messages 加 compliance.history(13)/lawSla(9)/wordlist(13) 共 35 键；check-content.mjs 扩守卫。`tests/integration/test_compliance_history_api.py` 5 例（空租户/分页过滤/详情租户收窄 404/SLA 派生/词库 active+level+layer）。接缝待追认：CCR 历史 limit/offset 分页甲案、法审 SLA 倒计时服务端派生不做客户端 tick、客户侧词库只读视图（写口仍归 internal_compliance 管理端）。
- **Q161 客户批量回填异步导入任务（Redis Streams 消费组 worker；2026-09-22，迁移 0039 import_jobs 新表、业务物理表 60→61；后端 761→778〔API 8＋worker 9 共 +17〕，前端零改动；02 C1.105）** — 销 Q156 挂账「异步导入任务随 V2」。新横切包 `app/core/imports/`（models/router/schemas/service/worker），端点挂效果回填命名空间：`POST /api/effects/backfill/jobs`（201）、`GET /jobs`（租户+content 限定）、`GET /jobs/{job_id}`（轮询）；载体 csv/xlsx 与 Q156/Q160 同契约、载体不匹配 422。迁移 **0039_import_jobs**（down=0038）：payload Text 落 CSV 原文/xlsx base64 **可重放**（与 Q132 export_jobs 不存 payload 相反，导入是写）、状态 queued/running/completed/failed、errors JSON。ImportWorker 经 Redis Streams 消费组（IMPORT_STREAM/GROUP/DEAD）水平并行，纪律镜像 Q137/Q152（read_new delivery=1、reclaim_pending 接管、成功 ACK、未超限留 PEL、超限 move_to_dead 置 failed、终态幂等 ACK）；**确定性校验失败直接 failed+ACK 不重试不进死信**，仅基础设施异常留 PEL/死信。门控 `LOOM_IMPORT_WORKER_ENABLED` 默认 false 保 V1 单副本行为（关＝请求内同步跑到终态 201；开＝queued 入流异步、入流失败 fail-closed 503），另三旋钮 `LOOM_IMPORT_STREAM_BLOCK_SECONDS`=5.0/`LOOM_IMPORT_WORKER_CONCURRENCY`=1/`LOOM_IMPORT_STREAM_COUNT`=20（不入 .env.example/C2）；main lifespan 门控启停。审计 import.job_created/completed/failed。
- **Q160 客户批量回填 Excel（.xlsx）服务端上传（2026-09-22，零迁移；后端 778→786〔+8 集成〕；02 C1.104）** — 销 Q156 挂账「Excel（openpyxl）随 V2」。新端点 `POST /api/effects/backfill/upload-excel`（无 Agent Key 客户通道、与 CSV `/upload` 同契约同回执，JSON body 携 .xlsx 标准 base64、零 multipart）；新模块 `core/effects/excel_io.py` 用 openpyxl `read_only` 流式读活动工作表（V1 只认第一个 sheet），单元格归一化为字符串后复用 `csv_io.parse_backfill_table` 同一套严格校验（空单元格＝缺席绝不补 0、整数值浮点 100.0→整数、日期单元格 isoformat 不带时区被 tz-aware 铁律逐行拒、尾部空列补齐到 9、第 10 列后非空才报多列）；旧 .xls/损坏文件加载期转 CsvValidationError(field="file") 回 422；行数上限复用 `LOOM_BACKFILL_UPLOAD_MAX_ROWS` 默认 10000；审计复用 effect.customer_backfilled。依赖 pyproject 加 `openpyxl>=3.1.5`（同笔加阿里云 uv index 镜像）。前端批量岛增 .xlsx 入口、check-content 扩守卫（55d5f47）。test_effect_backfill_excel_api.py 8 例。
- **Q159 客户批量回填岛切服务端 CSV 上传（2026-09-22，纯前端、零迁移/零后端测试；02 C1.103）** — 销 Q156 挂账「前端岛切换到新端点随 V2」。Q136 批量岛原在浏览器解析 CSV 后发 records[]（500 行软上限），本片改为提交 CSV 原文到 Q156 `POST /api/effects/backfill/upload`（8440cdc）：`content/actions.ts` 新增 server action uploadBackfillCsv 与 BackfillServerError/parseUploadFailure（422 逐行错误 line＝含表头物理行号、文件级 line=1），backfill-batch-island.tsx 渲染逐行错误，lib/api.ts 加客户端，zh-CN 文案，check-content.mjs 扩守卫；解析权威收归服务端、解除前端 500 行软上限（受服务端 env 上限约束）。
- **Q156 接缝追认（2026-09-22，02 C1.100 追认注记）**：Q156 三接缝（服务端强制 captured_at tz-aware 不臆造时区、行数硬上限 env 默认 1 万、JSON body 携文本零 multipart）连同 Q159/Q160/Q161 同类工程默认（xlsx base64 同原则、导入 payload 落库可重放、worker 门控默认关保 V1 行为）已经负责人「A和B吧，尽量搞一下」追认，销账。
- **工程修复·check-admin 跨平台 CRLF 兼容 + Windows 全量门禁首跑（2026-09-21，零迁移/后端 761 测试数不变；fix 仅前端第八 checker）** — Windows 本机（git core.autocrlf=true、仓库无 .gitattributes）checkout 后后端源码为 CRLF，frontend/scripts/check-admin.mjs 对 backend/app/content/router.py 的四条跨行路由断言用字面 LF 字符串（'@router.get(\n    "/api/admin/content/ready-to-publish"' 等）做 includes 匹配，在 CRLF 文本上必然失配、误报 4 problem；CI 在 Linux/LF 从不触发，后端 pytest 761 全绿亦证明 Q124/Q125 路由真实存在。修复为读取该 router 后 .replace(/\r\n/g, "\n") 归一再匹配（LF 检出为空操作、断言强度不变）；其余 7 checker 用单行 token/正则本就 CRLF 健壮、不动。Windows 全量 CI 等价门禁首跑全绿：uv 建 CPython 3.12.13 venv，后端（须在 backend/ 目录）pytest 761 passed＋8 real-infra skip、ruff All checks passed、eval 101/101（dataset 80＋golden 21），前端 npm ci＋tsc＋八 checker（修复后 check-admin exit 0）。next build 非 CI 门槛、Linux 容器已由 Q145/Q147 验证，本次未在 Windows 重跑。
- **Q158 异机备份恢复端到端演练（2026-09-21，零迁移/零生产代码；后端常规 761 测试＋8 real-infra skip 不变〔纯 infra shell 不新增 pytest〕，前端零改动；02 C1.102）** — 销 Q146 挂账「异机备份/恢复演练」的恢复腿（Q146 只落每日 pg_dump 备份腿，恢复从未在全新异机端到端演练）。新增可复跑 `infra/restore-rehearsal.sh`（up/down、KEEP=1，两独立一次性 pg16 容器源 55440/异机 55441 不触 atlas-pg，--rm 自删）：源库全链 `alembic upgrade head` 到 0038（38 条）+ 造 tenants/export_jobs/effect_records 覆盖 JSONB/timestamptz/可空 metrics（叠加种子共 19 张非空表），用 Q146 同款 `pg_dump --format=custom --compress=9`（custom 二进制必须 pg_restore）离源入异机，异机 drop/create 空库后 `pg_restore --no-owner`。实测 **bash 层 12/12 PASS**：dump 155110 字节/TOC 61、pg_restore rc=0 无 error、两库版本均 0038_sweep_tick_claims、业务表 60/总 61 集合一致、全表逐表行数零差异、结构 indexes=213/PK=61/FK=32/UQ=20/vector 列=2 一致、关键表整行逐字段一致、config_items 61/QC 0.85 一致、import app.main ORM 只读成功、恢复库再 upgrade head 幂等 no-op。脚本本地手动不进 CI（同 Q147/Q154/Q157）。**仍挂 V2**：RPO/RTO 量化达标认证（docs/17 §5 仍【建议】不升级状态）、WAL 增量/对象存储跨区冗余、指标级监控、持续高压压测。
- **Q155 台内白名单卡片 6 层原料包全量 JSON（2026-09-21，零迁移；后端 747→750 测试〔+3 集成〕，前端零改动；02 C1.99）** — 负责人「3件一起推」点工销 V2 挂账「6 层原料包全量 JSON」的**台内卡片口径**（非中台导出面）。新增 `final/final_whitelist/material.py`（schema 常量 `loom.fcw.material-pack.v1`、`build_material_pack` 反解析六路：product=PwsSnapshot、platform=PcpWeightTable+PublishSlot、strategy/structure/expression=三包 csp/cstp/cep、compliance=CcrReport+LawReview，引用行物理缺失回 `{_ref,available:false}` 并收 warning 不 500）与只读端点 `GET /api/fcw/{final_id}/material.json`（JSONResponse + Content-Disposition 附件、只读/无 RBAC 闸/不触发 Guard/不写审计/未知 404，同 FCW 读纪律）。**面的分界**：中台 /api/exports 经 Q100/Q132/Q142 定论严格 final_id-only，6 层包属台内 FCW 卡片详情/复制口径（09:87、01 line 14 final_id=6 层快照），故落 final FCW 域不塞 /api/exports；本片只销后端 JSON，台内白名单列表/卡片/复制 UI 属 D3.5 菜单整片未建、不做。test_fcw_material_api.py 3 例（六层齐/含已批准法审/未知 404）。
- **Q156 客户批量回填服务端化·CSV 上传端点 + 逐行回执（2026-09-21，零迁移/零新依赖；后端 750→757 测试〔+7 集成〕，前端零改动；02 C1.100）** — 销 Q136 纯前端批量岛挂账的「服务端上传端点/逐行回执」后端能力（解除前端 500 行软上限）。新增 `core/effects/csv_io.py`（标准库 csv 解析、固定 9 列表头按名定位、CsvValidationError 聚合全部坏行、dataclass ParsedUpload）与客户通道端点 `POST /api/effects/backfill/upload`（**无 Agent Key**、JSON body 携 CSV 文本而非 multipart〔零新依赖 python-multipart 未装〕、整份挂单一 content_id、复用 Q128 客户通道绝不孤儿、成功回执加 filename+rows[] 逐行、CsvValidationError/EffectValidationError 均 422）。规则：去 BOM、跳纯空行、captured_at `datetime.fromisoformat` 且**强制 tz-aware**（naive/纯日期逐行报错，守 Q128 铁律）、计数 `^\d+$` 拒小数负数、read_rate 0..1、空指标缺席绝不补 0、逐行收集一次回 422 不首错即停；行数硬上限 env `LOOM_BACKFILL_UPLOAD_MAX_ROWS` 默认 10000（运维旋钮非 Q9 阈值、不入 .env.example/C2）。test_effect_backfill_upload_api.py 7 例。**仍挂 V2**：Excel(openpyxl)/异步导入任务/前端批量岛切到新端点；三接缝（强制时区/上限工程值/JSON body）按甲落地待负责人追认。
- **Q157 真多副本多 worker 持续负载端到端部署演练 + XREAD BLOCK 超时缺陷修复（2026-09-21，零迁移；后端 757→761 测试〔test_redis_streams 8→12，+4〕，前端零改动；02 C1.101）** — 销 Q154 挂账「真多副本多 worker 进程持续负载端到端部署演练」。**设施（chore）**：`alembic/env.py` 加 PG 咨询锁（常量 0x4C4F4F4D「LOOM」、独立 NullPool 长连接 pg_advisory_lock 串行化多副本并发启动迁移，仅 postgresql 方言）；新增 `infra/docker-compose.ha.yml`（backend deploy.replicas:2，开分布式锁/restock 生产侧/导出 worker〔两副本各一 consumer 同组〕/配置广播，LLM base_url 置空走 synthetic 绝不花钱）与可复跑 `infra/ha-rehearsal.sh`（up/run/logs/down、四阶段 16 断言）；新增 `backend/uvicorn-log.json`（root INFO dictConfig）+ entrypoint uvicorn `--log-config` 修容器内 app.* 日志不可见。**缺陷修复（fix）**：演练抓到 FakeStreams 测不到的真实问题——redis-py 8.1.0 把 `XREADGROUP BLOCK` 空轮询到期抛 `redis.exceptions.TimeoutError`（文本 "Timeout reading from redis"，是 RedisError 子类但**非内置 TimeoutError 子类**），`streams.read_new` 原只 except RedisError 将其误判后端故障，worker 每 block 周期退避、空转不分片且削弱 PEL 接管；修复为在 RedisError 分支**前** `except (redis.exceptions.TimeoutError, TimeoutError)`，block 模式归一返回 []（协议语义＝无消息）、非 block 仍 fail-closed 抛 StreamBackendError，真连接故障 ConnectionError 不被吞。全新库实测 **16/16 PASS**：迁移咨询锁串行（一副本跑 38 条全量、另一阻塞后 no-op 0 条、无 already exists）、24 导出 job 两副本 consumer 分片 12/12 且空轮询 tick skipped=0/failed=0、SLA/restock 手工 /run 抢 leader 锁 200/409 无 5xx、PUT 配置后对端日志命中 invalidation broadcast reload 并自动还原（0.85→0.82→0.85）。演练本地手动、不进 CI（同 Q147/Q154）；restock 消费组水平并行仍 V2（本片只验单 leader 不双花、不改造）。
- **Q154 真 Redis/真 PG 多副本锁易主→中止/拒写集成验证（2026-09-21，零迁移/零生产代码；test 73f0e77；Alembic 头仍 0038/60 表，后端默认 747 passed＋8 skipped〔总收集 755，带 LOOM_TEST_REAL_PG_DSN/LOOM_TEST_REAL_REDIS_URL 两 env 时 755 全过〕，前端零改动）** — 销 Q143/Q151/Q152 共同挂账的 FakeRedis/FakeStreams/FakeBroadcast 内存替身信任缺口（02 C1.98）。新增 `backend/tests/integration/test_real_infra.py` **8 例、函数级 skipif、无 env 全 skip 不进 CI**（`LOOM_TEST_REAL_PG_DSN` 门 4 例、`LOOM_TEST_REAL_REDIS_URL` 门 4 例，key/流名带 uuid 收尾删除可反复跑）；一次性真容器 pgvector/pgvector:pg16（宿主 55433）+ redis:7（宿主 6380），带 env **8/8 全过 52.6s**：真 PG 两独立连接池跨连接认领/升级与条件 UPDATE 拒旧 fence（restock/sweep 参数化）、真并发行锁串行化（持行锁未提交时同行 UPDATE 阻塞、rollback 后才 acquired）；真 Redis leader_lease SET NX 互斥/INCR 严格 +1 单调/短 TTL(0.6s) 易主看门狗置 lost＋raise LockLost＋安全释放不误删新主锁、Streams 两 consumer `XREADGROUP '>'` 对 6 条各分 3 条不重叠/XACK=3/`XCLAIM(idle=0)` 接管投递计数升 2/move_to_dead 死信附 _dead_reason/_dead_origin_id、pub/sub 跨连接 `PUBLISH '*'` 触发 reload。**真容器首跑即抓到 sqlite/Fake 替身盲区**：restock.request_id 为 varchar(36)，初版测试 key 带前缀达 49 字符撞真 PG `StringDataRightTruncation`（sqlite 不强制 varchar 长度故替身从不报错），统一改 32 字符 `uuid.uuid4().hex`（同时满足 lock_name varchar64）后全过；唯一失败是测试数据问题、零生产代码改动。复跑见 docs/17 §7、台账见 docs/16。**仍挂 V2**：真多副本多 worker 进程持续负载端到端部署演练（本片在单进程内用两独立连接复现全部原语语义）、restock 消费组水平并行（花钱隔离需重裁 Q87/Q89）。
- **Q153 docs/10 第二轮 DBA 全量列复核（2026-09-21，纯文档/实测、零迁移零测试新增；无代码提交；Alembic 头仍 0038/60 表，后端 747 测试不变，前端零改动）** — 把首轮（head 0036/58 表）复核之后新增的 0037 restock_claims、0038 sweep_tick_claims 两 fence 表纳入全量列复核（02 C1.97）。一次性 pgvector/pgvector:pg16 容器（宿主 55432 避本机 atlas-pg 占 5432）`alembic upgrade head` 到 0038 后导出 information_schema/pg_indexes/pg_constraint，并 `import app.main` 注册全量 ORM 后 Base.metadata 逐表逐列比对：业务表 **db=60/orm=60**（无单边表）、总列数 **637**（首轮 head 0036 为 627，0037/0038 各 5 列，627+5+5 闭合）、索引 **212**（首轮 210＋两新表各 1 主键索引）、约束 **PK60/FK32/UQ20**（FK+UQ=52 与首轮持平，两新表各仅 1 主键、无 FK/UQ）、**列漂移表数=0**；两新表五列/pkey 与 docs/10 §2.8 逐字一致（restock_claims：request_id varchar36 PK/fence int NOT NULL/claimed_by varchar64 可空/claimed_at、updated_at timestamptz 默认 now；sweep_tick_claims：lock_name varchar64 PK，余四列同型）。docs/10 五处回填（头部第二轮结论、§2 表头、§4 迁移登记项、第二轮 [x] checklist）。
- **Q152 导出 worker 单进程多 consumer 并发度/批量配置（2026-09-21，零迁移；feat a41a174、test 977e193；Alembic 头仍 0038/60 表，后端 741→747 测试〔+6〕，前端零改动）** — 销 Q137/Q142 挂账「多 ExportWorker 并发度/批量配置」。新增 env `LOOM_EXPORT_WORKER_CONCURRENCY`（默认 1，保持 V1 单 worker）与 `LOOM_EXPORT_STREAM_COUNT`（默认 20，对齐 queue.DEFAULT_COUNT），均运维参数非 Q9 旋钮、不入 .env.example；`exports/worker.py` 加纯工厂 `build_export_workers`（N 个同消费组 ExportWorker，N>1 姊妹 consumer 共享随机前缀带 1..N 序号、批量/阻塞/接管参数全透传，concurrency/count<1 启动期 ValueError），main lifespan 单例改 `_export_workers` 列表逐个 start、关闭反序 stop；消费组 XREADGROUP `>` 原生分片、PEL 接管按 consumer 独立。测试 +6（默认 V1 形态、3 consumer 唯一名与参数透传、非法值参数化 3 例、两 consumer 对 6 任务各 3 无重复）。**仍挂 V2**：真 Redis 多 consumer/多副本分片与接管集成验证（FakeStreams 内存替身）。
- **Q151 SLA sweep 写路径 PG 行级 fence（2026-09-21，一迁移；feat b06f902、test 3f36b9b；Alembic 头 0037→0038_sweep_tick_claims/业务物理表 59→60〔pg16 往返实测〕，后端 733→741 测试〔新文件 +8、既有适配 3 处〕，前端零改动）** — Q143 restock 花钱路径 fence 的 SLA 同型收口（02 C1.95），挡住锁在**单个作业执行期间**易主后旧 leader 的迟到提交（Q139 checkpoint 只管作业开始前）。新表 `sweep_tick_claims`（lock_name PK/fence/claimed_by/claimed_at/updated_at，每循环锁一行）+ `sla/fencing.py` 两段式：tick 开始短事务 `claim_tick`（acquired/held/lost，先提交）、作业同事务提交前 `fence_current`（`UPDATE ... WHERE lock_name AND fence=:token`，rowcount=0 即回滚）；`run_jobs` 加 `commit_guard` 回调，门闭抛 LockLost 中止本轮；scheduler 定时 tick 与手工 `/api/admin/sla/run` 均先认领再透传门（lost 分别静默/409）；fence=None（V1 默认锁关）不写表、行为同 Q89/Q139。测试新 `test_sweep_fencing.py` 8 例（原语 4/runner 门 2/scheduler 接线 2），迁移经 pgvector/pgvector:pg16（55432 避 atlas-pg）up→down-1→up 对称实测。**仍挂 V2**：真 PG+真 Redis 多副本易主→中止/拒写集成验证（sqlite 单连接以顺序预置更大 fence 复现，边界同 Q143）。
- **Q150 loom 作为 Zeus 联邦第二封臣·A2A 第一阶段 plan 模式任务层（2026-09-21，零迁移/零新 env；feat 0ba84d3〔含单测 12+集成 10〕、docs 6404b99；Alembic 头仍 0037 当时/60 表口径见 Q151，后端 711→733 测试〔单测 +12、集成 +10〕，前端零改动）** — 负责人裁决四项全按 `docs/design-a2a-vassal.md` 草案（接入且不阻塞 #6 beta；skills＝generate-content/compliance-check/effect-backfill 三件均 plan 模式；认证复用 Q88 Agent Key 体系发 Zeus 专用 Key；卡片与审计租户维度 `_platform`）。新模块 `backend/app/core/a2a/`：`card.py`（Agent Card + x-zeus-fealty：swornTo=zeus/domain=content-production/dataRealms=[enterprise]/dataPolicy=read-task-scope/escalationPolicy=auto/ack 10s，url 取 settings.public_base_url）、`skills.py`（三 plan skill 纯函数执行器，缺参 input-required，无 DB/LLM/链路副作用）、`rpc.py`（JSON-RPC tasks/send|sendSubscribe|get|cancel 生命周期，进程内存存储 TTL 30min/上限 500，状态机 submitted→working→completed|input-required|failed|canceled，错误码 -32700/-32600/-32601/-32602/-32603/-32001/-32002）、`router.py`（公开只读 `GET /api/a2a/agent-card`、`/.well-known/agent-card.json`、`/.well-known/agent.json`，Cache-Control 300s；`POST /api/a2a/tasks` Q88 Bearer Key 保护 401，sendSubscribe 走 SSE；每个入站 Key 调用落审计 action=`a2a.task`/tenant=`_platform`，input-required 也留痕）。plan 模式承诺：不触链、不花 token（cost.llmTokens=0）、不越人工 Gate。**边界（挂 V2/第二阶段）**：任务进程内存存储跨实例不保证、真 LLM 与链路执行不做、未与 Zeus 真机联调（Zeus 侧 registry/dispatcher 就绪，待部署环境），持久化随 Zeus 联邦统一裁决（Zeus C2 待定）。
- **Q149 Checklist #3 阈值确认 + G2 基线（2026-09-21，零迁移/零代码；纯文档 89b0666 等；后端 711 测试不变；裁决 C 第一部分销账）** — 负责人逐项拍板：SLA 72h/黄 24h/红 48h、QC 0.85、cluster_line 0.9 沿用现有占位值为 beta 口径（配置中心热更、beta 协议显式认账，业务方给真数后热更）；G2 提交闸接受现有 12 个可溯 common 字段为基线（余 6 维持【原文未给出，待补】）；付费档额度因 beta＝平台代运营/无计费场景划掉（随 V2）。新增 `docs/19_private_beta主数据录入模板.md`（publish_slots/PCP/三包字段与调用示例，最小集合＝1 个 final_id）。**#6 验收现唯一阻塞＝#2 主数据业务回填**。
- **Q148 Checklist #1 真 LLM 接线（2026-09-21，零迁移；ops 1279e21、fix bafc529、test ef975cb/c117755；Alembic 头仍 0037/59 表，后端 711 测试不变，裁决 B 完成）** — 负责人提供 agnes 网关凭证并裁决 USD、50 USD/天、全部 9 场景启用。staging 栈实测接线：注册 `agnes-2.5-flash`（provider=openai 走 OpenAI 兼容驱动，currency=USD、daily_budget=50）、Fernet 加密 outbound Key（指纹 SxLd，密钥绝不落 env/文件/提交）、路由 8 个 chat 场景（CAT-RECOG/PWC-BUILDER/TYPE-MATCH/DIM-MERGE/CONFLICT-PRECHECK/ARTICLE-GEN/ARTICLE-QC/ARTICLE-SEMANTIC-CHECK）；端到端真实调用 CAT-RECOG 成功（run_id e9f8d382，635 input/231 output tokens，候选待人工 Gate）。compose 仅透传 base_url（`LOOM_LLM_BASE_URL_OPENAI`，staging overlay 固定 `https://apihub.agnes-ai.com/v1`；负责人给的 `…/v1` 原文有笔误，已实测订正）；修复后端镜像漏 COPY 仓库根 `runtime/` 技能注册表（RegistryError 500），契约测试守 `COPY runtime/ /runtime/` 与密钥不入 compose。**挂账（如实）**：agnes 无 embedding 端点（ATOM-AFFINITY 维持 synthetic）、无报价接口（单价待供应商，成本台记 token/次数、金额 0.0 不臆造）、图像/视频模型注册与接线随 V2。
- **Q147 Checklist #6 staging 彩排（2026-09-21，零迁移；feat 4e01912、test ef25c74；Alembic 头仍 0037/59 表，后端 710→711 测试〔overlay 契约 +1〕；#6 验收本身不销账）** — 新增 `infra/docker-compose.staging.yml` overlay：`!reset []` 清空 postgres/redis/minio 宿主端口（与本机 atlas 容器并存）、minio profile 排除（V1 无 S3 消费方），仅 frontend 发布 3000。`-p loom-staging` 真实起栈：6 服务全 healthy、entrypoint 迁移至 0037、backend 对宿主不可达、db-backup 产出备份、watchdog 无告警、真实 Chrome 验证 `/zh-CN/workbench` 与 `/zh-CN/products` SSR 零 console 错误、段1 API 冒烟（建租户→intake→列表/详情/onboarding）通过，`down -v` 全量清理未触 atlas-pg。**阻塞确认**：彩排库 publish_slots=0、packages=0、仅合成模型，真实 final_id 全链仍待裁决 B（LLM 供应商/出站密钥/日预算/币种）与 C（平台主数据 + 阈值）。
- **Q146 private beta Checklist #5 最小备份与监控（2026-09-21，零迁移；feat 0874500、test 6fb00fe；Alembic 头仍 0037/59 表，后端 704→710 测试〔+6〕）** — ①db-backup sidecar：`infra/backup/backup.sh` 每日 pg_dump custom gzip、7 天保留（`pgbackups` 卷），已在一次性 PG16（55432）实跑、pg_restore 验证归档；②全 7 服务 json-file 日志 10m×3 轮转，frontend 补 node http healthcheck；③watchdog：`infra/monitoring/watchdog.py` 每 30s 探 backend/frontend，失败 ALERT + `LOOM_ALERT_WEBHOOK` best-effort POST（pytest 本地 HTTP 实测）。异机备份/恢复演练/指标级监控随 V2。
- **Q145 private beta Checklist #4 最小部署制品（2026-09-21，零迁移；feat dc5eca7、test 8f2d23b；Alembic 头仍 0037/59 表，后端 699→704 测试〔+5 静态契约〕，frontend next build 通过）** — 受控 private beta 的最小运行制品：①`backend/Dockerfile`（python:3.12-slim，`pip install .`，`/healthz` HEALTHCHECK）+ `backend/docker-entrypoint.sh`（`set -e` → `alembic upgrade head` → `exec uvicorn app.main:app`，迁移先于服务、失败即退出）；②`frontend/Dockerfile` 三阶段（deps npm ci / build / runner 仅 Next standalone+static），`next.config.mjs` 加 `output: "standalone"`；③`infra/docker-compose.yml` 增 backend/frontend：backend **不发布宿主端口**（DSN 指向内网 postgres/redis，`LOOM_MASTER_KEY` 透传），frontend 是唯一发布口 3000（SSR 经内网 `http://backend:8000`），service_healthy 依赖序；根级 `.dockerignore`。不含独立 TLS 网关容器（由部署环境在 frontend 口外终结）与备份监控（Checklist #5）。
- **Q144 项目级 MVP / 可上线性评审与首发形态裁决（2026-09-21，纯文档零迁移；docs f26523c；Alembic 头仍 0037/59 表，后端 699 测试、eval 101、前端八 checker 全不变）** — 项目级评审（docs 口径 + 门禁/迁移独立实测），三判准：docs-V1 内核（段1→6→10→11 到 final_id）✅ 达标且超额；公网多租户自助 SaaS ⛔ NO-GO；受控试点 ✅ 有条件 GO。负责人裁决首发＝**受控 private beta**：后端内网/网关后、平台代运营，P0-① 身份层压到 V2 第一项。上线前 Checklist 与工时（约 6–8 人日，关键路径卡 B/C）见 docs/08 §2.2 Q144：①真 LLM 接线（待 B：供应商/币种裁决）；②首批平台主数据与阈值（待 C：业务输入，publish_slots 开箱零种子）；③最小部署制品（Dockerfile/compose app/自动迁移/网关/备份监控）。
- **Q141 配置缓存版本号门控 + TTL 兜底防陈旧（2026-09-20，纯代码零迁移；feat 692b435、test 63d9b4c；Alembic 头仍 0036/物理表 58，后端 678→684 测试〔+6 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「一口气」授权延续每片接缝按推荐甲（02 C1.85），销 C1.84 挂账「配置版本号/TTL 防陈旧兜底」：①**per-key 版本向量门控**——`config_center/cache.py` 快照随带每个 key 的 `ConfigItem.version`（发布即 +1 单调整数），`reload_keys` 对 DB 行做单调门控：DB 行版本 < 本进程持有版本即陈旧跳过（skipped）、显式删除仍移除（removed）、新版本接受（accepted），返回 `{accepted,skipped,removed}`；全量 `reload` 仍以 DB 为事实源整体替换不受门控；`apply(updates, versions=None)`、新增 `version_of`/`loaded_at`（time.monotonic）、`invalidate` 重置版本向量。②after_commit 本进程 apply 携带权威发布版本（service `_schedule_cache_apply(..., item.version)`，`_publish` 传 item.version），乱序/迟到旧增量快照无法回灌新值。③**订阅器 TTL 兜底**——`broadcast.ConfigBroadcastSubscriber` 加 `ttl_seconds`（env `LOOM_CONFIG_CACHE_TTL_SECONDS` 默认 300.0）：无失效消息时若距上次成功装载超过 TTL 则回源全量 reload，给广播 best-effort 丢消息设最坏陈旧上限；TTL 仅在曾成功装载（`_last_reload_at` 非 None）且订阅门控开启时触发，首次装载前不触发、缺 factory 不报错；单副本 after_commit 即时 apply 不依赖 TTL。④运维防护旋钮、非 Q9 业务阈值，走 settings 不入配置种子、零迁移；按 Q135 先例不入 backend/.env.example。测试 test_config_broadcast.py 11→17 例（旧版本被拒新版本接受、apply 无版本兼容、TTL 到期无消息回源、TTL 未到期不触发、首次装载前不触发且缺 factory 不报错、默认 300）。**仍挂 V2**：真 Redis/多副本订阅断线重连与 TTL 的集成验证（当前 FakeBroadcast/FakePubSub 内存替身）。
- **Q142 中台导出分页 + 行数硬上限（2026-09-20，纯代码零迁移；feat 992b7e8、test 8a0f820；Alembic 头仍 0036/物理表 58，后端 684→692 测试〔单测 +2、集成 +6〕/eval 101·golden 21，ruff 净，前端零改动）** — 接缝按推荐甲（02 C1.86），销 02 挂账「导出大结果集分页/行数硬上限」：①硬上限走 env 运维防护参数 `LOOM_EXPORT_MAX_ROWS`（settings `export_max_rows` 默认 100000），非 Q9 业务阈值、零迁移、不入配置种子。②`GET /api/exports/fcw.csv` 与 `/fcw.json` 加 `limit`（1..cap）/`offset`（≥0）查询参数，limit 超硬上限 422、offset 负 422，不传 limit 回落到硬上限（全量受截断保护）。③service 抽 `_published_stmt`、新增 `count_exported_final_ids` 与统一 `fetch_export_page`（返回 final_ids/total/limit/offset/has_more/truncated；total 为匹配过滤条件总数不受分页影响；truncated＝未显式分页 offset=0 却仍有更多行即被硬上限截断）；JSON envelope 在 Q132 `{tenant_id,product_space_id,count,final_ids}` 上追加 total/limit/offset/has_more（count 仍本页行数，向后兼容加字段）；CSV 正文守 Q100 严格单列不动，分页/截断信息走 `X-Export-Total/Limit/Offset/Has-More/Truncated` 响应头。④同步 job、Q137 异步 worker 渲染、job 下载全部经 fetch_export_page 受同一上限约束；export_jobs 不新增分页列（零迁移），export.job_created/job_completed 审计 detail 追加 total/limit/truncated；render_job 由二元组改返回三元组 (media_type, body, page)，下载仍按参数重渲染幂等。⑤**范围排除**：6 层原料包全量 JSON 不属中台导出（Q100/Q132 中台只 final_id，6 层包属台内卡片口径 docs/09:87），不在本切片。测试 test_export_render.py 5→7（envelope 分页字段/page 透传）、test_exports_api.py +6（JSON limit/offset、CSV X-Export-* 头、超限 422、不传 limit 截断、非法参数 422、同步 job 截断审计）。**仍挂 V2**：6 层原料包全量 JSON 体量治理、多 ExportWorker 副本并发度/批量配置、真 Redis/多副本验证。
- **Q143 下游 PG 行级 fence 条件更新接 restock 花钱路径（2026-09-20，后端加法+一迁移；chore fe2a62a、feat ab672b8、test 78ac7d4；Alembic 头 0036→0037_restock_claims/业务物理表 58→59〔pg16 往返实测〕，后端 692→699 测试〔原语 4+worker 端到端 3〕/eval 101·golden 21，ruff 净，前端零改动）** — 接缝按推荐甲（02 C1.87），销 C1.77/C1.83 挂账「受保护游标行携带 fence 做 WHERE fence=:token 条件更新拒旧持有者迟到写」中最关键的 restock 花钱路径：①**新表 restock_claims（迁移 0037，down=0036）**：每个 requested 补货信号至多一行，request_id String36 PK/fence Integer NOT NULL（当前获准花钱的最大 Q133 令牌）/claimed_by String64 可空（owner_token 溯源）/claimed_at、updated_at timestamptz 默认 now。②**新模块 app/core/restock/fencing.py**：`claim_request(session, request_id, fence, owner)` 在花钱**前**用独立短事务认领（无行插入/更小 fence 单调升到自己＝acquired，同 fence 重入＝held，已存在更大 fence＝lost）并先提交让易主后新 leader 可见；`fence_current(session, request_id, fence)` 在成功结果的**同一业务事务**内 `UPDATE restock_claims SET updated_at WHERE request_id=:id AND fence=:token`，rowcount=1 才放行提交（行锁把与新 leader 认领升级串行化）；fence=None 全程 no-op 恒 held/门恒开、不写表。③worker `_process_one`：fence 非 None 先短事务认领，lost 直接 skipped_lost **不再调模型**；成功生成后业务 commit 前过 fence_current，命中 0 行回滚整轮（succeeded 子 run/候选/审计一律不落库）；run_restock 加 fence/owner 形参（lease.fence/owner_token 透传）、report 加 skipped_lost 计数；定时 _tick 与手工 /api/admin/restock/run 均透传 lease 值。④**边界（如实）**：外部模型调用已花费用不可撤销，fence 只保下游 DB 不出现旧 leader 重复结果；deferred/failed 不产候选且幂等不接门；fence=None（锁关闭，V1 单副本默认）行为同 Q87/Q139。⑤**测试替身边界**：sqlite 内存库单连接无法在业务事务打开时由另一会话并发提交接管，worker「花钱中途易主」端到端用 monkeypatch 关闭 fence_current 确定性验证回滚；跨连接接管的真实条件 UPDATE 语义由新 test_restock_fencing.py 4 个顺序事务原语测试覆盖（首次 acquired/同 fence held、大 fence 接管小 fence lost、None no-op 不落行、接管后旧门闭新门开），test_restock_worker_api.py +3 端到端（带 fence 成功落认领行、被更新 fence 预占时花钱前 skipped_lost 不产子 run、门关时回滚已生成结果），test_lease_cooperative_abort fake_process 补 **_kwargs。迁移 0037 经一次性 pgvector/pgvector:pg16 容器（55432 避 atlas-pg）upgrade head→downgrade-1（to_regclass 空/drop 表）→upgrade 重建实测对称、五列/pkey 正确，总 public 表 60（59 业务+alembic_version）。**仍挂 V2**：SLA sweep 写路径 PG 行级 fence、真 PG 多副本+真 Redis 易主→中止/拒写集成验证、restock worker 主认领切消费组水平并行（花钱隔离，Q87/Q89 leader 单实例防双花）。
- **Q140 配置缓存单 key 增量失效 + 广播订阅断线指数退避（2026-09-20，纯代码零迁移；feat 310789a、test 743748b；Alembic 头仍 0036/物理表 58，后端 674→678 测试〔+4 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「开搞 A 到 E」批次 D 项（02 C1.84），把 Q135 广播原语接到生产并补 Q135 挂账「单 key 增量失效、断线重连退避」：①`config_center/cache.py` 新增 `reload_keys(session, keys)`——`in_` 一次查回给定 key、DB 仍在的覆盖快照、**DB 已删/缺失 key 从快照 pop**、未涉及 key 保持、空集合直接返回、整次原子合并（区别于 Q135 订阅侧仅会的全量 reload）；②`broadcast.poll_once` 解析消息 payload：`*`/缺失走全量 reload、具体 key 走 reload_keys([key])（bytes/str 兼容），成功（含连接健康但无消息）清零 consecutive_failures、故障累加且不抛（懒重连不自毁）；③`_loop` 退避＝backoff_base_seconds（默认 1.0）×2^(n−1)、封顶 backoff_cap_seconds（30.0），用 `wait_for(stop.wait(), timeout=delay)` 等待使 stop 可立即唤醒、一次成功重置；④**实测交互 bug**：首版删掉健康路径全部 sleep 后，测试替身 get_message 不真正阻塞导致纯忙转，连 asyncio.wait_for 的 5s 超时定时器都被饿死（最小复现脚本 hang）；修复为健康路径每轮保留一次 `await asyncio.sleep(0)` 零拍让出（真 Redis 的 get_message(timeout=) 自身阻塞一个 poll_timeout，仅替身/立即返回路径需要让出）。无新 env（复用 Q135 `LOOM_CONFIG_CACHE_BROADCAST_ENABLED` 默认关）、无 PG 迁移、无新配置键。测试 test_config_broadcast.py 7→11 例。**仍挂 V2**：配置版本号/TTL 防陈旧兜底、真 Redis 多副本验证。
- **Q139 SLA/restock 两循环 leader_lease 协作中止（2026-09-20，纯代码零迁移；feat 1ea9f6d、test 45fe6f5；头仍 0036/物理表 58，后端 669→674 测试〔+5 集成〕/eval 101·golden 21，ruff 净，前端零改动）** — 批次 C 项（02 C1.83），把 Q133 fencing leader_lease 原语接到四个持锁点（销 Q133 挂账「两循环 tick 改 leader_lease 协作中止」）：①`sla/runner.run_jobs` 与 `restock/worker.run_restock` 各加**零参 checkpoint 回调**——run_jobs 在每作业独立会话**之外**调用（避免被作业内 BLE001 宽 except 吞掉）、run_restock 在每条 requested 信号处理前调用；②四个持锁点（sla/scheduler._tick、restock/worker._tick、sla/router.py run_sweep、restock/router.py run_restock_once）由 Q89 布尔 leader_lock 换成 leader_lease，传 `lease.raise_if_lost`：锁一轮中途易主（看门狗续约返回 0 或续约期 Redis 故障置 lost）抛 LockLost，当前短事务/当前信号走完即协作中止、本轮后续不再执行；③两 _tick `except LockLost` warning「aborted mid-tick」静默返回不拖垮循环（下轮重新抢锁），两个手工 /run 的 LockLost→HTTP 409（LockUnavailable 仍 409、LockBackendError 仍 503）；④门控关时 lease 为 fence=None 恒 held 空租约、不触 Redis，行为与 Q89 完全一致，leader_lock 保留。无新 env（复用 `LOOM_DISTRIBUTED_LOCK_ENABLED` 默认关）、零迁移。测试新建 test_lease_cooperative_abort.py 5 例（复用 test_fencing_lease 的 FencingRedis）；全量回归暴露两处旧测试假函数签名不含 checkpoint（test_distributed_locks.py 三处假 run_jobs/fake_run_restock、test_scheduler.py 假函数），补 `**_kwargs` 适配（test_scheduler 那处 amend 入本片 test 提交，曾致 677 passed+1 failed，修后 674 全绿）。**仍挂 V2**：下游 PG 行级 fence 条件更新（业务表带 fence 列、WHERE fence<token 拒旧持有者迟到写）、真 Redis 多副本验证。
- **Q138 restock requested 信号 XADD 生产接线·仅生产侧入流（2026-09-20，纯代码零迁移；feat 820b876、test 3d26722；头仍 0036/物理表 58，后端 666→669 测试〔+3 集成〕/eval 101·golden 21，ruff 净，前端零改动）** — 批次 B 项（02 C1.82），把 Q134 Streams 原语接到 restock 生产路径。**范围收敛（设计期裁决）**：深入设计发现 restock 自动补货会花真 token，Q87/Q89 已裁决 leader 单实例防双花，而 Streams 消费组是多副本水平并行语义，二者冲突；故本片**只落生产侧入流、不把 worker 主认领切消费组**（与 Q137 导出只读幂等可完整接消费组形成对照），水平并行消费留 V2 且需重新评估花钱隔离（如按产品空间分区串行、预算 fencing）。①新建 `app/core/restock/notify.py`：publish_restock_requested（幂等 ensure_group 后 XADD，载荷 request_id）、_publish_best_effort（Redis/流故障只告警不抛）、spawn_restock_notification（模块级 _bg_tasks 保强引用防 GC，纪律同 Q135 广播发布侧）；②skill7/service.py `maybe_request_restock` 审计之后用 `event.listen(session.sync_session, "after_commit", fn, once=True)` 挂 spawn（局部 import 防循环依赖）——DB requested 行是唯一事实源、既有 DB 轮询 worker 兜底，XADD 仅为后续多副本细粒度认领的信号、失败不影响落库；③config 加 `LOOM_RESTOCK_STREAM_ENABLED` 默认关，门控关不触 Redis。零迁移。测试新建 test_restock_stream_notify.py 3 例（内存 SQLite + FakeStreamsRedis：开门 commit 后建组+XADD、关门不触 Redis 但 requested 行照落、XADD 故障 best-effort 被吞且行仍为事实源），既有 test_restock_worker_api.py 15 例零回归。**仍挂 V2**：restock worker 主认领切消费组（花钱隔离/单实例防双花的调和）、真 Redis 多副本验证。
- **Q137 中台导出真后台 worker·Redis Streams 消费组异步态 + 任务列表口（2026-09-20，纯代码零迁移；feat 96d3d58、test f8e1f28；头仍 0036/物理表 58，后端 656→666 测试〔+10 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 批次 A 项（02 C1.81），销 Q132 挂账「queued/running 真后台 worker、任务列表口随 V2 Redis Streams」。关键判断＝导出是**只读、下载按任务参数重渲染的幂等作业**，可安全用 Streams 消费组水平并行、无需 leader 锁（与 Q138 restock 花钱作业对照）。①models 加 JOB_QUEUED/JOB_RUNNING/JOB_TERMINAL_STATES（String(16) 普通字符串常量、**无 PG enum**，故零迁移）；②service 加导出流常量 EXPORT_STREAM=`loom:stream:exports`/EXPORT_GROUP=`export-workers`/EXPORT_DEAD_STREAM=`loom:stream:exports:dead`、WORKER_ACTOR=`system:export-worker` 与 create_queued/enqueue/process_export_job/fail_job/is_terminal/list_jobs，**保留 Q132 同步 create_export_job**；③新建 `exports/worker.py` `ExportWorker`（read_new 取新消息 delivery=1 + reclaim_pending 崩溃空闲接管：成功 XACK、未超 max_deliveries 的异常留 PEL 下轮重试、超限 move_to_dead 并置 failed、终态作业的重复投递幂等 ACK 不重复渲染、job 行不可见时留 PEL 不误置失败；默认 consumer export-{uuid4hex8}、block_ms=5000、count=20、min_idle_ms=60000、max_deliveries=5）；④router POST /jobs 按门控分同步（Q132 行为）/异步（queued+commit+enqueue，StreamBackendError→fail+503），新增 `GET /api/exports/jobs?tenant_id=&limit=` 列表口（envelope {tenant_id,count,jobs[]}，limit 默认 50、ge1 le200），download 对 queued/running 409 not ready、failed 409 带 error；⑤config 加 `LOOM_EXPORT_WORKER_ENABLED` 默认关 + `LOOM_EXPORT_STREAM_BLOCK_SECONDS`=5.0，main lifespan 启停（启动序 scheduler→restock→export→config subscriber、关闭反序）；⑥把 test_redis_streams.py 的 FakeStreamsRedis 抽到共享 `tests/integration/stream_fakes.py`。测试新建 test_export_worker.py 7 例 + test_exports_api.py +3 例（656→666）；测试期发现 min_idle_ms=0 会让同 tick 刚留 PEL 行被立即 reclaim（生产默认 60s 不会），「持续失败→死信」用例改用两个 worker 规避该测试假象。**仍挂 V2**：大结果集分页/行数硬上限/6 层原料包全量 JSON、多 ExportWorker 并发度配置、真 Redis 多副本验证。
- **E docs/10 建表要素首轮 DBA 复核（2026-09-20，纯文档零代码/零迁移；docs 单笔；Alembic 头仍 0036_export_jobs/物理表 58，后端 656 测试/eval 101·golden 21、前端基线全不变）** — 负责人「好的，开搞A到E」批次 E 项，销 handoff「docs/10 建表要素待 DBA 复核」挂账。方法＝一次性 PG16（pgvector/pgvector:pg16，端口 5433 避 atlas-pg）容器 `alembic upgrade head` 后导出 information_schema.columns（627 列）/ pg_indexes（210 行，含主键与唯一索引）/ pg_constraint（FK+UQ 52 条）+ numeric 精度/vector 维度，逐表核对 docs/10 全部「实现补登」段；另以 ORM `Base.metadata` 在内存 SQLite `create_all` 比对模型⇄迁移表列。结论：**58 张业务表表名/列名模型⇄迁移零漂移、零单边表**；各迁移新增表数对账闭合（0001=4/0002=8/0003=3/0004=5/0005=6/0006=3/0007=3/0008=7/0009=2/0010=2/0012=2/0014=5/0019·0020·0023·0025·0028·0034·0035·0036 各 1＝58，0011/0013/0021/0022/0024/0026/0027/0029–0033 不新增表）；新三表 effect_records（0034 建表 12 列、0035 加认领两列后现 14 列，4 业务索引 + FK fk_effect_records_content）/ effect_claims（5 列、仅 PK、FK fk_effect_claims_content）/ export_jobs（11 列、仅 PK+ix_tenant、status 默认 completed、row_count 默认 0）与登记一致；content_products 24 列、partial unique index `uq_content_final_language_kind ... WHERE status<>'discarded'` 就位；numeric 精度 ai_models 单价/日预算 (12,4)、skill_runs 成本 (12,6)；vector(1536) 两列（atom_candidates/product_atom_instances.embedding）+ pgvector 0.8.6；FK/UQ 约束名（fk_product_spaces_category_node_id/active_pws_id、uq_fcw_same_issue、uq_pcp_ps_platform、uq_pwc_combo_atom、uq_effect_content_captured 等）全部一致。**仅订正两处旧稿笔误**：content_products.status String24→**String32**（迁移 0025 建表与 ORM 模型本就 String(32)）、tenants.detail JSON→**JSONB（PG 端 JSONB、SQLite 变体 JSON，sa.JSON().with_variant）**；复核方法与逐项结论写入 docs/10 头部状态与 §4 核对清单。§3 标注【建议】而未落地的复合索引仍按性能实测后置，本次不判为缺陷。
- **Q136 客户效果批量 CSV 回填（2026-09-20，纯前端加法零后端改动/零迁移；feat 2915bdd；Alembic 头仍 0036/物理表 58，后端 656 测试/eval 101·golden 21 不变，tsc 净、八 checker 全过、next build 成功，token 仍 86/CSS 零新增）** — 负责人「好的，开搞A到E」点工 D 项（02 C1.80，接缝按推荐甲），销 Q131 挂账「批量表格/CSV 随 V2」：**甲案=纯前端形态，不建后端上传端点、无 PG 迁移、无新 npm 依赖**——浏览器内完成 CSV 解析+预览+行级校验，一次调 Q128 `POST /api/effects/backfill` 的 `records[]` 整批提交（后端 CustomerEffectBatchIn 早已 min_length=1·无上限·整批 all-or-nothing·422 detail 带 index·source 固定 customer-backfill·绝不产生孤儿），与单条岛共用同一客户通道/审计/租户隔离/422 回显。①新增 `(shell)/content/backfill-batch-island.tsx`（"use client"）：固定 9 列表头 `platform_post_id,captured_at,plays,likes,comments,shares,inquiries,conversions,read_rate` 按列名映射（缺列即表头错误不解析），内置最小 CSV 行解析（双引号包裹/`""` 转义，不支持字段内换行）；行级校验同单条岛（post 必填、captured_at 支持 ISO/`YYYY-MM-DD HH:MM`/datetime-local、纯日期按本地 00:00、统一 `toISOString()` 带 Z、六计数非负整数、read_rate 0..1、空指标即缺席不补 0），错误按物理行号（含表头）逐条列出、任一行错禁用提交；前端软上限 `MAX_ROWS=500`（后端无上限）超出提示拆分、预览前 10 行（缺席显「—」）、文件选择 FileReader/readAsText 载入 .csv + textarea 粘贴双入口；成功清空文本并 router.refresh、显整批 matched 数，失败复用单条岛 failureText（unconfigured/unknown/422 detail/403·404·409）。②content/actions.ts 加 `batchBackfillEffectsAction` 与 `BatchBackfillRowInput`（无 CURRENT_TENANT_ID→unconfigured、空 rows→422，rows.map 拼 content_id 后整批走 customerBackfillEffects，catch 复用 ApiError/KNOWN_STATUSES/errorDetail）。③详情页非 discarded 片段内单条岛后挂批量岛；zh-CN content 命名空间 +13 个 backfillBatch* 键（格式说明含 {max}、解析 {count}、错误 {count}、行错误 {line}/{message}、超限 {max}/{count}、成功 {count}、预览余 {count}）。④check-content.mjs 扩守卫：13 个消息键与占位符断言、必需文件 backfill-batch-island.tsx、详情页挂 BatchBackfillIsland、actions 含 batchBackfillEffectsAction、岛纪律循环纳入批量岛（"use client"/禁 @/lib/api/禁裸 fetch·URL/router.refresh）、批量岛必含 FileReader/readAsText/toISOString/all-or-nothing token 且禁直连。**挂 V2**：服务端文件上传端点（multipart/大文件流式/大小闸）、Excel(.xlsx) 解析、列名映射/多模板/编码探测、服务端行数硬上限与异步导入任务（同 Q132 queued/running 形态）、完整 RFC4180 字段内换行、导入结果逐行回执（当前整批 all-or-nothing）。
- **Q135 配置中心多副本缓存失效广播·Redis pub/sub（2026-09-20，纯代码零迁移；feat 032bab6、test 038b376；Alembic 头仍 0036/物理表 58，后端 649→656 测试〔+7 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「好的，开搞A到E」点工 C-3（02 C1.79，接缝按推荐甲），销 M10 切片 a/c 挂账「多副本配置变更广播」：①新模块 `app/core/config_center/broadcast.py`，频道 `loom:config:invalidate`；发布侧配置事务 after_commit 在本进程 `config_cache.apply` 后 `spawn_invalidation(key)`（同步钩子取 running loop 起 fire-and-forget PUBLISH，模块级 `_bg_tasks` 保强引用防 GC，payload=key/全量 `*`，门控关直接 return 不触 Redis）；②广播 **best-effort**（区别于 Q89 锁 fail-closed）——事务已提交，PUBLISH 失败只 error 不上抛污染 after_commit；③`ConfigBroadcastSubscriber` 后台任务（start/stop/running 同 SweepScheduler）pubsub 订阅、get_message 轮询 1s，收消息独立会话 `config_cache.reload()` 全量重载（单 key 增量随 V2），Redis 故障丢 pubsub 下轮懒重连、reload 异常只告警任务不自毁、CancelledError 透传保证 stop；④env `LOOM_CONFIG_CACHE_BROADCAST_ENABLED` 默认 false，main lifespan 开启时启停（启动失败 warning 不阻断应用）；无 PG 迁移、无新配置键。测试 test_config_broadcast.py 7 例（内存 FakeBroadcastRedis/FakePubSub：关闭不触 Redis/发布 key 与 */失败 best-effort/after_commit 全链〔in-memory sqlite〕/订阅 reload/轮询故障 poll_once 不抛可复用/订阅者数），既有 config 测试零回归。**挂 V2**：单 key 增量失效、断线重连退避与配置版本号/TTL 防陈旧、真多副本集成验证。
- **Q134 Redis Streams 每信号行细粒度认领原语（2026-09-20，纯代码零迁移；feat f255856、test 80da0c2；头仍 0036/物理表 58，后端 641→649 测试〔+8 集成〕/eval 101·golden 21，ruff 净，前端零改动）** — C-2（02 C1.78，甲案），销 Q89–Q97 反复挂账「Redis Streams/细粒度认领」的底座部分，**只落原语、不接 worker、不落 env、不建占位端点**（同 Q88/Q91 底座先行）：新横切包 `app/core/queue/`（streams.py），常量 `loom:stream:restock`/消费组 `restock-workers`/死信流 `:dead`、保留字段 `_dead_reason`/`_dead_origin_id`、默认 COUNT=20/MIN_IDLE_MS=60000/MAX_DELIVERIES=5/MAXLEN≈10000；`add_event`(XADD 近似 MAXLEN)/`ensure_group`(XGROUP CREATE mkstream，BUSYGROUP 幂等)/`read_new`(XREADGROUP `>` 读即入 PEL)/`ack_event`(XACK)/`reclaim_pending`(XPENDING_RANGE idle 过滤+XCLAIM 崩溃接管，delivery_count=times_delivered+1)/`move_to_dead`(超限进死信附因后 ACK)/纯函数 `delivery_exhausted(count,max)=count>max`；`StreamBackendError` fail-closed、override_stream_client 测试注入、懒连 decode_responses=True；消费组 PEL 隔离。测试 test_redis_streams.py 8 例（内存 FakeStreamsRedis 六命令）。**挂 V2**：restock requested 信号 XADD 生产接线、消费组 worker 循环（水平并行/并发度）、与 Q90 退避游标/Q133 fencing 协同、Q132 queued/running 真后台 worker、真 Redis 多副本验证。
- **Q133 多副本 leader 锁补 fencing token（2026-09-20，纯代码零迁移；feat c48a003、test 08951f4；头仍 0036/物理表 58，后端 633→641 测试〔+8 集成〕/eval 101·golden 21，ruff 净，前端零改动）** — C-1（02 C1.77，甲案），销 Q89 挂账「fencing token」：locking/service.py 新增 `leader_lease()` 返回 `LockLease`（fence 整数单调令牌/held/owner_token，`raise_if_lost()` 抛 LockLost、`wait_lost()`），抢锁 SET NX PX 成功后 `INCR loom:fence:{name}` 取跨易主全局单调递增令牌、序列键不设 TTL 防回绕；INCR 失败释放刚取得的锁并 LockBackendError fail-closed；看门狗续约返回 0（易主）或续约期 Redis 故障 → `_mark_lost()`+error，持锁方关键步骤 `raise_if_lost()` 协作中止；`leader_lock` 重构为薄封装保持 Q89 布尔语义（关闭 yield 字面 False、开启 yield held、中途易主仍只 log 不打断），两循环两端点零改动；下游 PG 行级 fence 条件更新随 V2 写者接入。测试 test_fencing_lease.py 8 例（内存 FakeRedis 补 set/incr/eval），test_distributed_locks.py 局部 FakeRedis 补 incr 后原 13 例零回归。**挂 V2**：下游 DB fence 条件更新、两循环 tick 改 leader_lease 协作中止随多副本启用切换。
- **Q132 M12 中台导出 JSON 形态 + 异步导出任务（2026-09-20，后端加法+一迁移；feat 469e74e、test 355af08、chore ee6b14c；Alembic 头 0035→0036/业务物理表 57→58，后端 618→633 测试〔+5 单测 +10 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「好的，开搞A到E」点工 B 项（02 C1.76，接缝均按推荐甲），销 Q100/docs-11 §2.3 挂账「JSON 形态、异步导出/导出任务记录」：①**JSON 同步口 `GET /api/exports/fcw.json`** 与 fcw.csv 同 query/同口径（tenant_id 必填、product_space_id 可选、只导 published、created_at DESC、未知租户空 envelope 200、不触发 Q95 准入门），body=`{tenant_id,product_space_id,count,final_ids[]}`，不拼 6 层原料包；②**导出任务 `POST /api/exports/jobs`** body `{tenant_id,product_space_id?,format:csv|json=csv,actor}`（中台面同 Q100 无 RBAC 闸、actor 留痕），V1 请求内同步执行置 completed（同 Q55 同步先例），201 回 ExportJobView 含 download_url，审计 `export.job_created`（tenant=客户租户、actor_id=requested_by、roles=[]、detail format/row_count/product_space_id）；③**新表 export_jobs（迁移 0036）** job_id PK(uuid1)/tenant 索引/product_space_id 可空/format String8/status server_default completed/row_count/file_name/requested_by/error/created_at/completed_at，**不存文件 payload**；④**状态口 `GET /api/exports/jobs/{id}`**（未知 404）与**下载口 `GET /api/exports/jobs/{id}/download`**（未知 404、failed 409，按 format 重渲染、幂等反映当前 published 集合，row_count 创建时留痕），V1 不做任务列表口；⑤测试 tests/unit/test_export_render.py 5 例 + test_exports_api.py 3→13 例；迁移 0036 经 pg16 容器（5433 避 atlas-pg）up（11 列/pkey/ix，57→58）/downgrade-1（回 57）/up（58）实测往返对称。**挂 V2**：queued/running 真后台 worker（Redis Streams，C 项基建）、任务列表口、大结果集分页/上限、6 层原料包全量 JSON。
- **Q129 P3 段13 孤儿批量认领 + 取消认领/解绑（2026-09-20，后端加法零迁移；feat bb7c8c2、test 2078bf3；Alembic 头仍 0035/业务物理表 57，后端 607→618 测试〔+11 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「按你推荐的一起搞吧」点工段13 操作面收口三件之一（02 C1.73，接缝按推荐甲落地待追认）：①**批量认领 `POST /api/admin/effects/claims/batch`**，body `{items:[{record_id,content_id}](≥1), actor}`，逐条复用单条认领校验（未知记录 404/非 orphan 409/未知目标 404/discarded 目标 409），批内 record_id 重复 422 detail={index,field:"record_id",message}、空 items pydantic 422，**整批 all-or-nothing**（任一失败事务回滚零落库），成功回 {claimed, updated_rows}，逐条 `effect.claimed` 审计随事务；②**取消认领/解绑 `POST /api/admin/effects/claims/unclaim`**，body {external_content_id, actor}（operations 硬闸，客户/platform_admin 403），映射不存在 404（新异常 ClaimMappingNotFound）：删 effect_claims 映射行，并把该 external_content_id 下 **matched_content_id 等于旧映射目标 content_id** 的全部行回滚 orphan（清 matched_content_id/tenant_id/claimed_by/claimed_at 四列；实现中发现按 claimed_by 非空筛会漏掉认领后经兜底 matched 的新推送行，故按旧目标 content_id 筛；external_id 自匹配的真自动行天然不受影响），回 {external_content_id, reverted_rows}，审计 `effect.claim_revoked`（tenant=_platform、entity_type=effect_claim、detail 旧 content_id+reverted_rows）；③service 抽 `_claim_one(session,*,record_id,content_id,actor)`（无角色闸），claim_orphan/batch/unclaim 各自先 require_any_role(OPERATIONS)。测试 test_effect_claims_api.py 8→19 例（unclaim 5 + batch 6）。
- **Q130 管理端效果回流运营台 /admin/effects（2026-09-20，纯前端加法零迁移；feat d668da2；后端 618 测试/eval 101 不变，tsc 净、八 checker 全过、next build 成功，token 仍 86）** — 同批三件之二（02 C1.74，甲案待追认）：新建 `app/[locale]/admin/effects/` 五文件——page.tsx（RSC force-dynamic，getEffectOrphans({limit:100}) 首屏孤儿队列 + 成品时序两区，缺管理员身份显 unconfigured、缺 operations 写口本地拦 missing_role）、actions.ts（"use server"：claimEffectAction/batchClaimEffectsAction/unclaimEffectAction/queryEffectSeriesAction，errorDetail 兼容批量 422 object detail）、orphan-table-island.tsx（单条认领 + 勾选批量认领，整批成功或整批拒绝，成功 router.refresh）、series-island.tsx（content_id 查询成品时序，人工认领行带 claimed_by 与「取消认领」按钮，window.confirm 后解绑并按 queriedId 重查）、effect-fields.ts（METRIC_FIELDS 七键顺序与 metricText/capturedAtText helper，指标缺席显 "—" 绝不显 0）；admin-sidebar 加第 8 项 /admin/effects（labelKey admin.effectsNav，恰 8 项）；lib/api.ts 加 effects 段（EffectRecordView 等类型、私有 adminQuery 拼 query actor、getEffectOrphans/getEffectSeries/claimEffect/batchClaimEffects/unclaim 用管理员 actor）；messages 加 effectsNav 与完整 admin.effects.* 段（status.matched/orphan 枚举码原样不翻译）；check-admin.mjs 扩守卫（sidebar 恰 8 且含 /admin/effects、effects 五文件、admin.effects.* 键、页 force-dynamic 只读、两岛 "use client"+禁 @/lib/api 裸 fetch+router.refresh、api.ts 六函数、后端 effects router 六路由齐备）。**V2 D3.6 五个分析二级页（归因/自学习/KUP 等）不提前实现**，本页只是运营操作台。
- **Q131 客户内容详情页效果数据回填岛（2026-09-20，纯前端加法零迁移；feat d668da2；后端 618 测试/eval 101 不变，tsc 净、八 checker 全过、next build 成功，token 仍 86）** — 同批三件之三（02 C1.75，甲案待追认）：新建 `(shell)/content/backfill-island.tsx`（"use client"，仅非 discarded 成品详情页挂载；platform_post_id 必填、captured_at datetime-local 经 `new Date(value).toISOString()` 转带 Z 的 UTC、metrics 七键 number 输入留空=缺席绝不补 0，COUNT_METRICS 六键挡非负整数、read_rate 挡 0..1；V1 仅单条手工录入，成功 router.refresh 清空表单显 matched 数，批量表格/CSV 随 V2）；content/actions.ts 加 backfillEffectAction 与 BackfillActionResult 判别联合（无 CURRENT_TENANT_ID→unconfigured，403/404/409/422 映射，errorDetail 解析 JSON.stringify 过的 object detail 取 field/message），消费 Q128 `POST /api/effects/backfill`（lib/api.ts customerBackfillEffects 用客户 actor、roles 恒空）；详情页在 discardedReason 块后条件挂载 BackfillIsland（discarded 不挂）；messages 加 content.backfill* 全套（含七指标键与 backfillSuccess{count}）；**客户 nav 不增项仍恰 8 项**（第 5 项数据分析仍 V2，本岛是录入口不是分析页）；check-content.mjs 扩守卫（api.ts customerBackfillEffects 与 /api/effects/backfill、详情页挂 BackfillIsland、actions backfillEffectAction/CURRENT_TENANT_ID、岛纪律循环加 backfill-island、岛必含 datetime-local/toISOString、后端 backfill 路由串）。
- **Q128 P3 段13 customer-backfill 客户专用效果回填通道（2026-09-19，后端加法零迁移；feat 5e60ef3、test 9447e33；Alembic 头仍 0035/业务物理表 57，后端 599→607 测试〔Q127+Q128 合计 +16 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「一口气搞了」点工段13 续片（02 C1.72，与 Q127 同批）：①**新端点 `POST /api/effects/backfill`（客户通道，无 Agent Key）**，请求体 `{tenant_id, records[], actor}`，与 Q122 客户写口同构（actor 在体、客户 roles 恒空、无运营闸；tenant_id 口径同 `GET /api/content?tenant_id=`）；source 服务端固定 `customer-backfill`（不接受客户端传入，对应 Q60 降级口径「客户手工回填转化」）。②**客户通道绝不产生孤儿**：每条 content_id 必须命中本租户非 discarded 成品，否则该条 422 detail={index,field:"content_id",message}，整批 all-or-nothing；不查 effect_claims 认领映射兜底（认领是运营动作，客户通道不旁路）。③captured_at 带时区（naive 422）归一 UTC、(content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏纪律（缺席/显式 null 不补 0）与 Agent 通道完全一致——复用 Q126 抽出的同一归一/持久化路径（_normalize_records/_resolve_contents(tenant_id=)/_persist_records）；received_by=客户 actor.id（区别 Agent 通道 key_id）；审计 `effect.customer_backfilled`（**tenant=客户租户**，区别 Agent 通道 _platform）；回执复用 {received,matched,orphan,upserted}，orphan 恒 0。④测试 tests/integration/test_effect_customer_backfill_api.py 8 例（无 Key happy 落 source/received_by/tenant、幂等覆盖与时序追加、他租户 content 422 不落库、未知/discarded 双 422 且零孤儿、一条坏整批拒回带 index、metrics/captured_at 五坏例 all-or-nothing、空 tenant/空 records 422、批内重复点 422）。⑤**明确不落**：客户回填前端 UI、效果反哺 KUP/校准算法（口径【待补】禁臆造）、Agent 抓取（Q62 不抓取）。
- **Q127 P3 段13 Q60a 孤儿效果记录人工认领/绑定（2026-09-19，后端加法+一迁移；feat 5e60ef3、test 9447e33、chore cadddc3；Alembic 头 0034→0035/业务物理表 56→57，后端 591→599 测试〔+8 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「一口气搞了」点工段13 续片（02 C1.71），销 Q126 挂账「Q60a 孤儿人工认领动作」：①**持久映射表 `effect_claims`**（迁移 0035_effect_claims，down=0034）：external_content_id String36 PK（推送方自报 ID）/content_id String36 NOT NULL FK `fk_effect_claims_content`→content_products.content_id/claimed_by String64 NOT NULL/claimed_at timestamptz NOT NULL default now/updated_at 可空；一个推送 ID 一行映射，重复认领=upsert 改绑。effect_records 同步加 nullable claimed_by String64/claimed_at timestamptz 两行级溯源列；**不新增第三状态**——认领后行 status 置 matched 并回填 matched_content_id/tenant_id，claimed_by 非空即人工认领。②**写口 `POST /api/admin/effects/claims`**，body {record_id,content_id,actor}（管理面写口 actor 在体，服务层 require_any_role(OPERATIONS) 硬闸，客户/platform_admin 403，同 Q125 publish-info 口径）：入口记录须存在且当前 orphan（未知 404、非 orphan 409），目标成品须存在且非 discarded（未知 404、discarded 409），失败回滚不落半成品；动作=upsert 映射＋批量回填该 external_content_id 下全部 orphan 行与历史 claimed 行（条件 status=orphan OR claimed_by IS NOT NULL，支持旧目标 discarded 后改绑重指；自动 matched 行不动），响应 {external_content_id,content_id,claimed_by,claimed_at,updated_rows}，审计 `effect.claimed`（tenant _platform、entity_type=effect_claim、entity_id=external_content_id）。③**认领兜底（解决 Q126 挂账「认领后不被同 ID 后续推送冲回 orphan」）**：Agent 通道 ingest 自动匹配未命中的 ID 再查 effect_claims，映射目标存在且非 discarded 即按映射 matched——幂等覆盖行与新 captured_at 采集点都不再回落孤儿；目标缺失/discarded 则回落 orphan、映射保留待重新认领。④测试 tests/integration/test_effect_claims_api.py 8 例（认领绑定清孤儿队列/成品时序可见/claimed 溯源/映射落表、认领后新采集点与同点幂等覆盖均 matched、旧目标 discarded 后改绑新目标 updated_rows=2、未知 record 404、非 orphan 409、未知目标 404 不落半成品、discarded 目标 409、客户与 platform_admin 403）。迁移 0035 经 pgvector/pgvector:pg16 一次性容器实测 up（effect_claims 5 列/PK/FK、两 nullable 列，56→57）/downgrade -1 回 0034（表与两列全清回 56）/再 up 恢复（57）往返对称。⑤**明确不落**：取消认领/解绑、批量认领、认领前端操作页（随续片/V2）。
- **Q126 P3 段13 反馈回流入站第一片·effect-callback（2026-09-19，后端加法+一迁移；feat 63a84c8、test 2b72768、chore 83d7ef6；Alembic 头 0033→0034/业务物理表 55→56，后端 554→591 测试〔+24 单测+13 集成〕/eval 101·golden 21，ruff 净，前端零改动、checker 仍八、token 86）** — 负责人「一口气再推进一些任务」，选 13 段链最后一段「段13 反馈回流」入站第一片（Q60 契约完整、Q88 入站 Key 鉴权半套与 Q125 运营发布回填已铺路、纯本地 sqlite/PG 可闭环），接缝按推荐（甲）拍板记 Q126（02 C1.70）：①**新横切包 `app/core/effects/`（models/schemas/service/router 四件）+ 单表 `effect_records`**，用 `status=matched|orphan` 一列分流命中/孤儿（不分两表），迁移 0034_effect_records（down=0033_content_publish）：record_id uuid1 PK/source String64/external_content_id String36（推送方上报的 content_id）/matched_content_id String36 nullable FK→content_products.content_id（约束名 fk_effect_records_content）/tenant_id String36 nullable（Key 不绑租户，由命中成品 content.tenant_id 反解，孤儿留空）/platform_post_id Text/captured_at timestamptz/metrics jsonb nullable/status String16 server_default 'orphan'/received_by String36/审计时间戳；4 业务索引（uq_effect_content_captured(external_content_id,captured_at) 幂等唯一、ix_status、ix(matched_content_id,captured_at) 时序、tenant_id）。②**入站端点 `POST /api/effect-callback`** 复用 Q88 `require_agent_key` Bearer 依赖（缺失/错误/吊销统一 401 detail "invalid or revoked agent API key"，**全系统首个受 Agent Key 保护的业务端点**，命中同事务盖 agent_api_keys.last_used_at），请求体 {source:str, records:[{content_id, platform_post_id, captured_at, metrics?}]}；匹配=仅按推送 content_id 精确匹配**非 discarded** 成品——命中 status=matched 回填 matched_content_id+tenant_id，查无或命中终态 discarded→status=orphan（matched_content_id/tenant_id 留空、platform_post_id 照常留存）；platform_post_id 只留存、**不做** content_id↔platform_post_id 自动绑定（人工认领/绑定留续片 Q60a）。③**metrics 稀疏纪律（不补 0）**：六计数 plays/likes/comments/shares/inquiries/conversions 须非负整数（bool 显式拒、负数/小数拒）、read_rate 须 0..1 数值（bool 拒）、未知键拒绝、键缺席或显式 null 一律省略不落（**绝不写 0、不估算**，界面缺席显「—」，metrics 整组可空为 NULL）。④**时间与幂等**：captured_at 须带时区偏移（naive 422）、服务端归一 UTC；幂等键 (external_content_id,captured_at)——同键重推=upsert 覆盖该行 metrics/status/匹配并刷 updated_at（不新增行），不同 captured_at=追加为时间序列点。⑤**整批 all-or-nothing（同 Q93）**：source 必填非空白、records≥1，逐条校验失败 422 detail={index,field,message}、批内重复幂等键 422、任一坏记录整批不落；成功 200 回执 {received,matched,orphan,upserted}。⑥**运营只读两口** `GET /api/admin/effects/orphans`（跨租户孤儿、captured_at 升序）与 `GET /api/admin/effects?content_id=`（成品时序），operations|platform_admin 的 query actor 闸（复用 content router `_query_actor`：缺 actor 422、越权 403），limit 1..100 默认 20、offset 0..500 默认 0。⑦**审计**每批一条 `effect.batch_received`（tenant=_platform、actor_id=key_id、actor_roles=[]、entity_type=effect_batch、entity_id=uuid4、detail 含 source 与四计数）。⑧测试：tests/unit/test_effect_records.py 24 例纯函数（metrics 归一/六计数坏值参数化/read_rate 越界/bool 与未知键/稀疏 null、validate_record 各坏例、_as_utc）+ tests/integration/test_effect_callback_api.py 13 例（无/坏/吊销 Key 三态 401、matched 回填 tenant、orphan 查无、命中 discarded 转 orphan、稀疏不补 0 且整组可空、customer-backfill 透传、同键幂等覆盖不新增行+异点追加、批内重复键 422、五坏例整批不落、空批/空 source 422、两口缺 actor 422/越权 403/双角色 200）；fixture 造 active/revoked 两 Key 与 ready/discarded 两成品。迁移 0034 经 pgvector/pgvector:pg16 一次性容器全新库 up（12 列/jsonb/timestamptz/4 业务索引+主键/FK，物理表 55→56）/ downgrade -1 回 0033（表·索引·FK 全清回 55）/ 再 up 恢复实测对称；main.py 挂 effects_router（content_router 后、exports_router 前）。⑨**明确不落（随段13 续片/V2）**：Q60a 孤儿**人工认领/绑定**动作（本片未建 claimed 标记，续片须保证认领后不被同 content_id 后续推送冲回 orphan）、customer-backfill **客户专用通道**（本片仅接受透传该 source 字符串、不另建客户端点）、content_id↔platform_post_id 自助映射、**效果反哺 KUP-PROPOSE 与 Q54/Q57/Q61/Q65 评分阈值校准算法——口径原文未给【待补】，禁止臆造公式**、Agent 抓取与发布自动化（Q62 本系统不抓取，发布由 Q125 运营人工回填）。eval 不新增（薄校验为纯函数+HTTP 集成，同 Q88 口径，16 已登记）。
- **Q124 V2 P4 段12 难产骨架作废回池 Q56-b（2026-09-19，后端加法+一迁移+前端管理台；feat ccc23e6/ef6074b/4d6f0b9、test 370e94c/a45b635/2bb3abd、chore 36c8133、fix a3ebf96；Alembic 头 0031→0032，后端约 534→545 测试/eval 101·golden 21，ruff 净，前端 tsc+八 checker、next build 成功，token 仍 86）** — 负责人对挂账点留言「给我推荐方案」后回复「好的，按你推荐的来」拍板（02 C1.68）：①内容状态机六态→**七态**，新增终态 **discarded（只读）**，与 rejected 严格分工（rejected=客户驳回、原因回流段13、可继续 revise/regenerate；discarded=运营判定骨架不要、释放白名单键生成机会）；事件 `discard`（operations 硬闸、reason 必填 1–500 字、空白 422）仅 review/revising/rejected → discarded，draft/generating/ready_for_publish 不可作废（409），终态 allowed_events 为空、客户 403、未知 404；②新端点 `POST /api/content/{content_id}/discard`（落 discard_reason Text + 审计 content.discarded）与跨租户读口 `GET /api/admin/content/needs-attention`（review/revising/rejected、created_at 升序、行不含 body，operations | platform_admin 双角色读口；router `_query_actor` 改 *roles 可变参，a3ebf96 修正双角色读口）；③回池机制=迁移 0032_content_discard 把普通唯一约束 uq_content_final_language_kind(final_id,language,kind) 换成 **partial unique index `WHERE status <> 'discarded'`**（模型 postgresql_where + sqlite_where 双方言），作废后同键可重新生成新成品、旧行只读保留，service generate 查重另加 status != discarded 双保险；不做 FCW 状态回退、不自动作废（达上限仍只 422）。测试：test_content_statemachine.py 扩 discarded（终态空事件、三态可作废/四态不可，约 +2 单测）；新建 test_content_discard_api.py 9 集成（happy+审计+列表回带、客户 403、未知 404、revising/rejected 可作废、ready/draft 409、空白 reason 422、回池后同键重新生成两行独立且未作废重复仍 409、终态动作全拒、needs-attention 队列过滤排序与角色闸）。迁移 0032（down=0031：drop discard_reason、partial index 还原普通唯一索引；downgrade 不处理同键冲突，V1 无生产数据）经 pgvector/pgvector:pg16 一次性容器实测：全新库 up、discarded+live 同键共存而第三条 live 同键 duplicate key 409、downgrade -1 回 0031、再 up 恢复，往返对称。前端管理端 /admin/content 内容运营台见 Q125 条（两切片共用一页）。挂账：达上限自动转人工工单、discarded 行物理清理/归档（V2）。
- **Q125 V2 P4 段12 运营发布回填与待发布队列 Q60c（2026-09-19，后端加法+一迁移+前端管理台；feat d55e4d5/4d6f0b9、test 7a9af70/2bb3abd、chore 30dba15、fix fe82255；Alembic 头 0032→0033，Q124/Q125 合计后端 534→554 测试/eval 101·golden 21，ruff 净，前端 tsc+八 checker、next build 成功，token 仍 86）** — 同轮「按你推荐的来」拍板（02 C1.69）：①**不新增 published 态**，ready_for_publish 仍终态，`published_at` 非空即已发布信号；迁移 0033_content_publish 加 nullable published_url Text / platform_post_id String(128) / published_at timestamptz 三列（down=0032 drop 三列）；②写口 `PUT /api/admin/content/{content_id}/publish-info`（operations 硬闸：仅 ready_for_publish 否则 409、url 必填非空白 422、未知 404、客户 403；可重复修正 url 但 published_at 仅首次落、post_id 缺省不动/空串清空；审计 content.publish_info_set）；③读口 `GET /api/admin/content/ready-to-publish`（operations | platform_admin，跨租户 ready 行、created_at 升序、行不含 body；fe82255 定稿口径=**返回全部 ready 行含已回填**，前端分待回填/已回填两分区，回填后不离队以便运营核对/修正链接）；两个静态 GET 注册在参数 PUT/POST 之前（checker 顺序断言）。④前端新建管理端 `/admin/content` 内容运营台（与 Q124 共用一页四文件：page.tsx force-dynamic RSC 双队列 Promise.all 双取、actions.ts 两 Server Action 含 unconfigured/missing_role/403/404/409/422 映射、publish-info-island.tsx、discard-island.tsx 含 window.confirm；sidebar 6→7 项 labelKey admin.contentOpsNav；messages 加 admin.contentOps.* 全套 + content.status.discarded + publishedLink*/discardedReasonTitle；客户列表 STATUS_TONE 加 discarded、content.module.css .toneDiscarded 灰+删除线终态弱化、客户详情页 ready 态显已回填外链否则「待平台统一发布」、discarded 态显作废原因；lib/api.ts 加四函数与 ListItem 四字段；check-admin/check-content 扩守卫，**无新 checker 文件、八 checker 总数不变**，CI 契约测试无需改）。测试：新建 test_content_publish_api.py 9 集成（happy+审计、客户 403、未知 404、非 ready 409、空白 url 422、重复回填覆盖 url 但 published_at 不重置/空串清空 post_id、队列跨租户排序排除非 ready 且回填后仍在队列带链接、角色闸、platform_admin 读 200 写 403）。迁移 0033 经 pg16 容器实测 up/downgrade-1（三列消失、discard_reason 保留）/up 对称，业务物理表仍 55。**明确不落（随段13/P3/V2）**：Agent 抓取、effect-callback 端点与 effect_records 时序、孤儿发布队列、Q60a 客户自助发布、发布自动化。
- **Q123（B3）段1 客户目标语言录入控件 Q58 产品侧客户自助（2026-09-19，跨栈加法零迁移；feat a9193a1/c3af8bf、test 50fa594/76fef22；Alembic 头仍 0031/物理表 55，后端 528→534 测试/eval 101·golden 21，ruff 净，前端 tsc+八 checker、next build 成功，token 仍 86）** — 接缝按推荐（甲）拍板（02 C1.67，与 Q122 同轮授权）：①新开客户侧写口 `PATCH /api/intakes/{intake_id}/target-languages`（按 intake 定位产品空间，客户 actor roles 恒空、无运营闸；语言码 strip/去重且必须在 active content_languages 内，未知/归档/重复 422，产品空间未生成 404，空数组=未声明写 NULL，刷新 updated_at，审计 product_space.target_languages.set 带 channel=customer_intake；product_intake service 内部 commit 惯例）；②新开客户侧无闸只读 `GET /api/content/languages`（仅 active、按 code 排序、不含归档；管理面含归档清单仍走 /api/admin/content-languages + dictionary_admin；**注册必须先于 GET /api/content/{content_id}** 否则 "languages" 被路径参数吞，check-content 加顺序断言）；③Q119 operations 代设入口 PUT /api/product-spaces/{id}/target-languages 与 OPERATIONS 闸原样保留（回归测试锁 roles=[]→403、operations→200），ProductSpaceView 回显 target_languages（GET product-space 同步）。前端：lib/api.ts 加 ContentLanguageView/getContentLanguages/setIntakeTargetLanguages；products/actions.ts 加 setTargetLanguagesAction；新建 client 岛 target-languages-form.tsx（active 语言 checkbox 多选、回显、空选=不限制、useTransition+router.refresh、禁直连服务端访问层）；录入详情产品空间段挂岛（空间未生成不渲染，RSC 取清单）；messages products.targetLanguages 四键；check-products 扩键/文件/api token/岛纪律/后端守卫，check-content 加语言端点与注册顺序守卫。测试 +6（tests/integration/test_target_languages_customer_api.py，内存 sqlite、zh-CN/en-US active + fr-FR archived）。挂账：zh-CN 外语言清单待业务方提供后由 dictionary_admin 配置（不预置）。
- **Q122 客户「内容生产与发布」功能页 + Q56-a 客户人工改稿（2026-09-19，跨栈加法零迁移；feat e346198/e41bad3、test bbbd4ae/ecdfec5；Alembic 头仍 0031，后端 523→528 测试/eval 101·golden 21，ruff 净，前端 tsc+八 checker、next build 成功，token 仍 86）** — 三接缝在负责人未在线时沿用「按你的来/狠狠搞完」授权按推荐（甲）落地、待追认（02 C1.66）：①客户页能力=只读+审阅+人工改稿——列表/详情/approve/reject/revise/Q56-a 人工编辑进客户页，generate/regenerate 维持 operations 闸不放客户页（前端访问层不得出现两端点字面量，check-content 守卫，Q116 定稿不改）；②Q56-a=客户自助人工编辑——六态机新增事件 `manual_resubmit`（revising→review，六态不变），新端点 `PATCH /api/content/{id}/body`（客户无闸，仅 revising 否则 409、空白 422、404；换正文后重跑词库+语义复检+ARTICLE-QC 回 review，不调 ARTICLE-GEN、不增 regenerate_count，审计 content.body_edited 带 actor_roles）；③目标语言客户录入另开客户侧端点（见 Q123）。另加无闸 `GET /api/content?tenant_id=`（created_at DESC，列表项 ContentProductListItem 去 body，未知租户空，缺 tenant_id 422）与 `GET /api/content/{id}`（详情含 body，404）。前端新建 (shell)/content/ 六文件：列表页（每语言成品并列、六态 chip、AI 分 0–100、复检旗标 ban 红/降级与语义黄/无异常灰）、详情页（正文 pre、质量卡「仅供参考」、复检卡 bans/downgrades/语义 code 原样不翻译、checked=false 显暂不可用、驳回原因、ready 显待平台统一发布、404 notFound）、DecisionIsland（review 态，block_required 禁通过，reject 原因必填）、BodyEditIsland（revising 态）、actions.ts（"use server"）、content.module.css；lib/api.ts content 段（客户 actor roles 恒空）；messages content 命名空间 +61 行（含六态中文）；nav content v2→v1；**新增第八 checker scripts/check-content.mjs**（消息键/文件/岛纪律/nav v1/后端防漂移/生成端点不外放），ci.yml seven→eight 接线，test_ci_workflow_contract 枚举同步。测试 +5（tests/integration/test_content_customer_api.py：列表租户隔离/列表项无 body/未知租户空、详情 404、人工编辑 happy path SkillRun 集合 GEN+QC×2+SEMANTIC×2 且 regenerate_count 恒 0、状态/空白/404 闸、编辑后哨兵仍回 review）。挂账：Q56-b 作废 final_id 回池记难产原因（六态机无 discarded 态、段11 FCW 状态机无回池原文口径）、ready 后统一发布动作不在 V1 客户页、语义四 code 与 QC 阈值待业务方/法审校准。
- **Q121 V2 P4 段12 复检第②项·语义级检测 Q59（2026-09-19，后端加法+一纯种子迁移；feat f30c747、test 2f1c585、chore 6e8bd1c；Alembic 头 0030→0031，后端 511→523 测试/eval 101·golden 21，ruff 净，前端零改动）** — 负责人点工 B 组后接缝按推荐（甲）拍板（02 C1.65）：①模型网关新增第 9 场景 ARTICLE-SEMANTIC-CHECK（与 ARTICLE-QC 同构只读检测、不改写正文，variables [body,language]，机读输出 {findings:[{code,message,excerpt}]}，模板四检测维度 unsubstantiated_claim/absolute_guarantee/off_material_exaggeration/misleading_ambiguity 标注 v0.1 工程口径待业务方/法审校准；迁移 0031 纯种子：scene route→synthetic + SkillPrompt/PromptVersion v0.1）；②正文 ARTICLE-GEN 写 body、词库扫描之后、ARTICLE-QC 之前由新建 `app/content/semantic.py` 内嵌调用一次（generate/regenerate 同路径，改稿强制重过复检），结果聚合进既有 `review_hits["semantic"]={checked,findings,?error}`，**零新 HTTP 端点/零新列/零新表**，ContentProductView 不增字段；③定位**纯 advisory（甲）**——不抬 block_required、不阻断 approve、不新增状态（词库 ban 的确定性硬阻断原样保留；与 Q57 质量分降级、Q66 AI 不持审批/否决角色一致），段12 Gate 仍客户审阅 Q59；④**检测不可用/坏输出不阻断生成**——场景未配置 ModelConfigError、模型不可用/预算耗尽 ModelUnavailable(BudgetExhausted)、GenerationUpstreamError → {checked:false,findings:[],error:类型}、审计 content.semantic_unavailable、不落 SkillRun；网关成功但输出非法 JSON/非对象/findings 非数组（parse_semantic_output 归一 invalid_json/not_object/findings_not_array）仍落 SkillRun（output 带 parse_error）+审计 content.semantic_checked；findings 逐项容错（项非对象/缺非空 code 跳过，message/excerpt 非字符串丢弃，code 去空白）。synthetic build_article_semantic_check：正文含哨兵 [SEMANTIC_RISK]→1 条 unsubstantiated_claim、其余（含空正文，空正文归质量分）空列表。测试 +12（解析纯函数单测 8 test_content_semantic.py：合法/非法 JSON/四类坏形态参数化/逐项丢弃/非字符串字段丢弃；语义复检集成 4 test_content_semantic_api.py：happy 三场景 SkillRun 集合 {GEN,QC,SEMANTIC}、哨兵命中 findings 非空但 block_required 不抬且客户可 approve、网关 ModelConfigError 时 checked=false/error/无 SEMANTIC SkillRun 仍可 approve、regenerate 重跑共 2 条；三个内容 fixture 补 SEMANTIC 三件套，test_content_api happy path SkillRun 集合断言 2→3）。迁移 0031_article_semantic_seed（down=0030，三件套 bulk_insert/downgrade 三 DELETE，不建表）经 pgvector/pgvector:pg16 一次性容器全新库 up（三件套就位）/ downgrade -1 回 0030（语义三件套全清 0/0/0、ARTICLE-QC 三件套保留 1/1/1）/ 再 up（恢复 1/1/1）实测对称，业务物理表仍 55。挂账：复检余两项（施工指令核对、国家规则核对）【原文未给出，待补】、四 code 口径业务方/法审校准与段13 命中率评估、语义发现界面提示（随 Q122）、视频语义复检（video-studio）。
- **Q120 V2 P4 段12 AI 质量分真打分 Q57 + 重生成上限配置化 Q56（2026-09-18，后端加法+两迁移；feat 3969705、test eb32031、chore a13a7cb；Alembic 头 0028→0030，后端 494→511 测试/eval 101·golden 21，ruff 净，前端零改动）** — 负责人「一口气搞了」接缝按推荐（甲）拍板（02 C1.64）：①模型网关新增第 8 场景 ARTICLE-QC（只读打分、不改写正文，variables [body,language]，机读输出 {score,issues}，模板规则注明辅助参考非门槛；迁移 0030 纯种子：scene route→synthetic + SkillPrompt/PromptVersion v0.1）；②正文 ARTICLE-GEN 生成 + 词库复检之后、状态 COMPLETE 之前由新建 `app/content/quality.py` 内嵌调用一次，**零新 HTTP 端点**；`content_products` 加 `quality_score`(Float 0..1 可空)/`quality_issues`(JSON 可空)（迁移 0029 两 nullable 列），`ContentProductView` +4 字段 quality_score/issues/threshold/advisory；③定位**纯 advisory**（Q57 降级裁决）——分数不自动发证/驳回、不新增 needs_manual 状态、不阻断 approve（段12 Gate 仍客户审阅 Q59，六态机 Q116 不动），低于 `content.ai_quality_threshold`（默认 0.85，种子早已预置、本片接通消费）仅置 quality_advisory=true 作界面提示，阈值随视图回带；④**QC 故障/坏输出不阻断生成**——场景未配置 ModelConfigError、模型不可用/预算耗尽 ModelUnavailable(BudgetExhausted)、GenerationUpstreamError，或输出非法 JSON/非对象/score 缺失越界/bool（parse_qc_output 严格归一 invalid_json/not_object/score_not_number/score_out_of_range，issues 非数组归一 []、score round 4）时 score 留 NULL、issues 记 {"qc_error":类型}、成品照常进 review；失败写审计 content.quality_unavailable 不落 SkillRun（无成功调用产出），成功落 SkillRun（source=llm_auto、wf=WF-10、output {score,issues}、tokens/cost）+ 审计 content.quality_scored；⑤**Q56 止损配置化**——statemachine.revise_allowed 增 limit 形参（MAX_REGENERATE=3 兜底常量），service 读 `content.regen_limit`（默认 3、运营可改）作改稿重生成上限，超限 revise 返回 422（detail 回带 {count}/{limit}）只能 reject；Q56 的 a 清洗区人工编辑 / b 作废 final_id 骨架回池记难产原因均保留为人工动作，本期不自动作废/回池。synthetic build_article_qc 确定性：正文含哨兵 [LOW_QC]→0.62、空正文→0.55、否则 0.92（≥0.85）。测试 +17（QC 解析/阈值/上限纯函数单测 13 test_content_quality.py 含边界 0/1·bool·七类坏输出参数化；质检集成 4 test_content_quality_api.py：真打分落 SkillRun / 低分 0.62 仍可客户 approve→ready_for_publish 且 advisory=true / QC 抛 ModelConfigError 不阻断且 score 空 / regen_limit=1 后二次 revise 422 含 1/1，autouse fixture teardown 还原进程级 config_cache；两内容 fixture 补 ARTICLE-QC 三件套，test_content_api happy path 一次生成 SkillRun 断言 1→2）。迁移 0029_content_quality（down=0028 加两列）+ 0030_article_qc_seed（down=0029 三件套 bulk_insert/downgrade 三 DELETE，不建表）经 pgvector/pgvector:pg16 一次性容器全新库 up（两列+QC 种子就位）/ downgrade 0030→0029（种子全清列保留）/ 0029→0028（两列全删）/ 再 up 恢复实测对称，业务物理表仍 55。挂账：界面「AI 评估分，仅供参考」标注与每语言成品并列（随 Q122）、段13 回流后相关性校准（Q57 后置）、Q56 a/b 人工路径前端化、视频质量分（video-studio）、复检语义级（Q121）。
- **Q119 V2 P4 段12 多语言 Q58（2026-09-18，后端加法+一迁移；feat bb6acd6、test 5e1340d、chore b16da9b；Alembic 头 0027→0028，后端 483→494 测试/eval 101·golden 21，ruff 净，前端零改动 token 86）** — 负责人「一口气搞了」接缝按推荐（甲）拍板（02 C1.63）：①新表 `content_languages` 语言清单配置化（code=BCP-47 PK/name/markets=适用国家码 JSON〔空数组=全市场含 country=NULL〕/status active|archived，dictionary_admin CRUD 仿 content_goals、读口 query actor 闸、upsert 复活归档行；迁移种子仅 zh-CN、markets=[] 保证单语言向后兼容，其余语言业务方未给清单不预置）；②`ProductSpace` 加 nullable `target_languages` JSON（NULL/空=未声明不收窄；operations `PUT /api/product-spaces/{id}/target-languages` 写入口，段1 录入表单控件挂账）；③交集纯函数 `covers_market`/`eligible_codes`（active 语言 markets 覆盖 `fcw.country`，受限语言不覆盖 country=NULL，再被 PS 目标语言非空收窄、保清单序），配 `GET /api/content/eligible-languages?final_id=` 预检（operations query actor）；④`content_products` 唯一约束 `uq_content_final_language_kind(final_id,language,kind)`，generate 语言不交集→422（detail 回带 eligible）、同 final+lang+kind 已存在→409（走 revise/regenerate 不重建），每语言一条成品天然独立复检/独立客户审（Q59 按 content_id 不分叉）；ARTICLE-GEN 模板加 `$language` 与硬性规则 4（须用目标语言撰写），synthetic 非 zh-CN 产出英文确定性占位，SkillRun.input_payload 记 language。端点共 5（admin/content-languages 的 GET/PUT/POST `{code}/archive` + content/eligible-languages + product-spaces target-languages）。迁移 0028_content_languages（down=0027：建表+zh-CN 种子、PS 加 target_languages jsonb、唯一约束；downgrade 对称），pgvector/pgvector:pg16 一次性容器 up head / downgrade-1（表列约束全清回 0027）/ 再 up（恢复）实测对称。测试 +11（交集纯函数单测 5、多语言集成 6；原 test_content_api fixture 自插 zh-CN 种子，否则 create_all 不跑迁移种子致交集为空 happy path 422）。挂账：zh-CN 外语言清单运营配置、段1 目标语言录入控件、每语言成品前端并列呈现（随 Q122）、视频生成。
- **Q118 Q117 挂账四项收口（2026-09-18，后端加固+一迁移+CI；feat 5713b93/12673b7、fix f663c48、chore 16f99a1、test 55c6e07、docs 2f34149；Alembic 头 0026→0027，后端 472→483 测试/101 eval，前端 tsc+七 checker 全绿，token 仍 86）** — Q117 挂账 4 项均按推荐（甲）拍板（02 C1.62）：①4 个漏网管理面 GET 补 query actor 闸（同 Q109/Q113，缺 actor 422/越权 403/命中 200）：content-goals=dictionary_admin、cp-law-domains=internal_compliance、publish-slots 与 slot-type-defaults=operations（`require_fit_weights_view` 重构为通用 `require_operations_view` 三读口共用），写口 body actor 服务层闸不动、前端零消费；②`publish_slots.gate` 回填 V1 仅 Q42 人工直编口径（默认 approved，无 slot_items 表/无 pending_gate 产生路径/无 Gate 翻转端点，`pa_rules.GATE_PENDING` 为 V2 预留零引用，AI 拓展 Gate 流整体随段7 完整版/V2）；③迁移 0027_product_space_fks 为 product_spaces.category_node_id→g1_categories、active_pws_id→pws_snapshots 补两道 nullable FK（先 NOT EXISTS 幂等置空孤儿，PG16 全链 up/downgrade-1/up + 孤儿 INSERT/UPDATE 被拒、NULL 合法实测）；④CI 六→七 checker（接入 check-settings，job/步骤名同步，防漂移单测新增「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除漏接）。审计外修复 Alembic 版本表 version_num 默认 VARCHAR(32) 容不下 0021 起 33 字符 revision id 的全新 PG 全链截断缺陷（env.py 迁移前独立事务幂等加宽至 VARCHAR(128)，不动已发布迁移）。
- **Q117 文档⇄代码一致性审计与纯事实回填（2026-09-17，纯文档无代码/迁移/新业务口径；11 文件；docs 475622d；Alembic 头 0026、后端 472 测试/101 eval、token 86）** — 先取实测基线（472 passed/ruff 净/eval 101/head=0026_article_gen_seed/前端 7 checker/tokens.css 86 变量）再逐份订正：状态类文档（handoff、08 §2.2、docs/README、README.md、AGENTS.md）刷新至 Q116 基线；docs/10 一次性登记迁移 0021–0026+head=0026，并修正 `product_spaces.lifecycle`（旧稿 lifecycle_priority）与 `pwc_combo_items (pwc_id, atom_id)`（旧稿含 fp_id）；契约回填 docs/05（Q114/Q116 补登+§2.12 content_products 状态机）、13（§1.13+§1.8 段7 适配 V2 标注+§1.9 实现差异标注）、11（出站 Key 录入路径与驾驶舱登记）、04（核销 content_products/writeAudit/api_keys 待补）；4 项实现差异只挂账（4 个无闸 GET、publish_slots.gate 口径、两外键无 DB 约束、check-settings 未接 CI，后由 Q118 全部收口）；核销 approveAtomGuard「9 vs 10 项」非冲突（10 项定义/9 项机检+⑩审计服务保证）。
- **Q116 V2 P4 段12 内容生成第一片·文章单语言最小闭环（2026-09-17，跨栈加法+两迁移；feat 363113d/bcdc75a/3f11afc、test 9a94412/f76e33c/101fd26；Alembic 头 0024→0026，后端 457→472 测试/101 eval，token 86）** — V2 首个主链片（02 C1.60）：范围=文章单语言最小闭环，视频（video-studio 四模块）、多语言（Q58）、AI 质量分真打分（Q57 待回流校准）缓做；AI 通道=模型网关第 7 场景 ARTICLE-GEN，**不走 skill7 运营 Gate**（段12 的 Gate 是客户审阅 Q59），成本经 SkillRun 手动落（Q67）+writeAudit 留痕，产 `{"body"}` 直写 content_products.body（PT-ART-GEN-V1.5 只读消费不重决策）；迁移 0025_content_products 建表（content_id/tenant/product_space/final_id 只读软关联/goal/platform/slot/country/kind/language/body/review_hits/status/reject_reason/regenerate_count/审计列）+0026_article_gen_seed 纯种子（ARTICLE-GEN 场景路由+Prompt v0.1）；新模块 `app/content/`（models/statemachine/schemas/generation/service/router）挂 `/api/content/*` 五端点（generate/approve/reject/revise/regenerate），六态机 draft→generating→review→ready_for_publish/rejected/revising，revising→generating；复检 4 项只做词库扫描（复用 Q48 词库+ccr_rules 三层裁决），语义级检测/施工指令核对/国家规则核对 3 项占位【待补】。P4 不闭合：客户前端内容页、视频、多语言、质量分、复检余 3 项缓做。
- **Q115 基准 HTML 永久丢失后的溯源口径收口 + G2 fid 工程定稿（2026-09-17，纯种子迁移；feat 15fd29c；Alembic 头 0023→0024，后端 457 测试/101 eval，token 86）** — 负责人确认 V6.0 需求说明 HTML 彻底丢失、无法找回（02 C1.59），销 handoff 待办 4 四类挂账：①`line NNNN` 溯源降级为历史痕迹（引用保留、不再作为可重新核对的基准）；②docs/12「7 定稿+35 推断候选」定为最终名单（35 候选保留【推断】标注），不再挂"待 HTML 核对"；③触发方事件永久标【原文未给出，待补】，实现口径以代码角色闸为准（Q106 客户 4 码/Q107 运营 6 码/系统事件）；④G2 fid 改判**工程定稿**（真正业务事实是 field_name 中文 canonical），迁移 0024_g2_common_fields 落 12 个 cat='common' 字段（fid 英文 slug+中文 canonical+status=active，ON CONFLICT DO NOTHING 幂等），其余 6 个【原文未给出，待补】不落；docs/04 §2.2/§2.5、docs/12 §1.2 同步收口。
- **Q114 M12 收尾·客户 templates/settings 两页 + 五型 SLA 数值占位拍板（2026-09-17，跨栈加法无迁移；feat 29b9f68/e61efbc、test 339e207；Alembic 头仍 0023，后端 455→457 测试/101 eval，token 86，前端 checker 增至 7 个）** — 负责人「都按推荐」拍板（02 C1.58）：①settings=V1 只读账户面板——后端 tenants 新增 customer_router `GET /api/tenants/{tenant_id}`（无闸只读，CustomerTenantView 不含 detail/onboarding）+前端 RSC 账户面板（账户信息+语言展示，切换随 V3；平台授权/通知/帮助/团队/账单五子项 V2 徽标）；②templates=仅占位 V2 徽标（类型模板库随 V2 类目，后端无客户读端点）；③五型 SLA=V1 统一 72h+黄色窗口「到期前 sla.yellow_hours（种子 24）」配置化占位：`core/sla/policies.py` 黄色判定由「created_at+hours」改为「due_at−sla.yellow_hours」，YELLOW_BY_TYPE 改 YELLOW_TYPES 并把五型 review_* 并入（法审 due=48h 数值不变、既有测试零漂移），业务方给数后经配置中心热更、不写死；④新增 `scripts/check-settings.mjs`（npm run check-settings）。M12 不因此闭合：付费档第 5 档额度、五型 SLA/黄色窗口真实数值仍待业务方输入。
- **Q113 三族管理面读口补闸收口（2026-09-17，纯后端加固无迁移；feat 0144672/test 36fb564/docs 1f36dbb；Alembic 头仍 0023，后端 445→455 测试/101 eval，前端零改动 token 86）** — 负责人"按你的来，狠狠的搞完"授权收口 Q109 挂【待补】三族 7 个读口（02 C1.57），角色溯自已落地矩阵/写口同组、不新增业务口径：config 三 GET=platform_admin（写口 PUT/rollback 同角色，配置含 SLA 时限/各域阈值平台级敏感，同 ai-models 先例）、g2-candidates=dictionary_admin（05 表行 GET/promote 同行 Q13/Q68，与 /categories 同型）、skill-prompts 三 GET=platform_admin（发布写口同角色，单版本含 template+variables 全文，同 ai-models 平台级敏感口径）；3 路由各加本地 query actor 依赖（require_config_view/require_g2_candidates_view/require_prompts_view），处理器末位 `_: Actor = Depends(...)`、函数体零改动，写口 body actor 服务层闸一字不动；agent-keys 与 ai-models/{id}/keys 矩阵有意开放仍免闸、测试继续锁定；三族前端全仓 grep 零消费。
- **Q112 契约文档回填·13 状态机迁移表/11 API 规范据已落地实现补登（2026-09-17，纯文档无代码/迁移/测试；docs 06c8152/2bc616c；Alembic 头仍 0023、后端 445 测试/101 eval、token 86）** — 销 handoff 待办④"11/13 待 M0 后补齐"挂账（02 C1.56）：13 号 productIntake15 七处【待补】迁移行据 statemachine/service/router 补事件码/角色闸/已实现副作用（每次迁移写审计 intake.{event}、submit 动态完整度闸缺字段 422、start_modeling 建 ProductSpace 并整体复制 Q74 profile_snapshot），G1 审核/合并/废弃/归档流随 V3、PWC 阻断→复核/待入库 M5 未实现，未实现行保留【待补】不虚构，标注 Q106/Q107 外放口径（客户白名单 4 码/运营 6 码，wf01_*与触发方【待补】事件后端可调前端不外放）；11 号总览表回填白名单消费（POST .../pwc/consume，业务方参考路径 /api/whitelist/consume 不采用）、池查询（GET .../pwcs）、取用即写 usage_record 并回带 usage_record_id、Key 治理（Q88 入站 agent-keys 三端点+Q82 出站 ai-model-keys）、CSV（Q100 GET /api/exports/fcw.csv，final_id 单列同步流式/只导 published/未知租户仅表头 200），新增 §2.2 消费端点规格/§2.3 CSV 规格/§2.4 Key 治理规格（入站 SHA-256 单向哈希/明文仅签发一次/append-only 吊销，出站 Fernet 可逆加密刻意分域）；effect-callback 本体随段13 P3/V2、中台 API/SDK、DB 浏览/AI 调试台/配额、JSON/异步导出保留【待补】。
- **Q111 CI workflow 落地·GitHub Actions（2026-09-16，纯工程基建无迁移；feat 44659b1/test 5f747bf）** — 销 Q77 与 docs/17 CI/CD 节挂账"CI workflow 本体随 M12/部署落地（仓库托管平台确认后）"：origin 实为 github.com:bayernjf/loom，平台事实成立，负责人"你推进搞"点工定 GitHub Actions。新增仓库首个 workflow `.github/workflows/ci.yml`（51 行），触发 = push 到 main/dev + pull_request，两 job 均零 secret、零外部服务：backend（setup-python 3.12 + pip 缓存，`pip install -e "./backend[dev]"`，working-directory backend 跑 `ruff check .` 与 `python -m pytest -q`——pytest 全用 sqlite，无需 Postgres/Redis 服务容器；仓库根跑 `python eval/runner.py` Q77 Evaluation 回归 101 案）；frontend（setup-node 20 + npm 缓存锁 package-lock.json，`npm ci` → `npm run typecheck` → check-tokens/nav/products/workbench/compliance/admin 六 checker 串行）。**明确不纳入**：ruff format --check（从非项目门槛，对 116 个历史文件报 would be reformatted，不借 CI 静默升级）、next build 与 next lint（非既定闸门）、CD/部署（仍随部署阶段）。防漂移：新增 tests/unit/test_ci_workflow_contract.py 静态解析 workflow YAML，断言触发分支、backend 三命令与 frontend typecheck+六 checker 全部在位，删闸即挂（删 check-nav 行失败探针实测 1 failed 后还原）。干净 env（env -i 仅 HOME/PATH）复跑全闸门：backend 445 passed（444+1）、ruff All checks passed、eval 101/101（21/21 文件）、tsc 零输出、六 checker 全绿（token 仍 86）；无迁移（头仍 0023）、前端代码零改动。**注意**：本仓不 push，workflow 尚未在 GitHub 实际触发过，首次远端运行待负责人 push dev/main 或开 PR。
- **Q110 热修·审核工作量页空态缺键 admin.workload.empty（2026-09-16，纯前端；fix 0b10ed5/test d4059a9）** — Q109 浏览器 E2E（9222 Chrome、/tmp 一次性 sqlite 空库回归六管理页）发现 `/zh-CN/admin/review-workload` 四处空表（待审候选/审核待办/已裁候选/已决待办）原样显示消息键 `admin.workload.empty`：Q102 建页时四个空态分支均调 `admin.workload` 命名空间 `t("empty")`，但 zh-CN.json workload 块漏收该键、check-admin 必检键清单同步漏列；逐片 E2E 均带种子数据、空态分支从未被走到。顶层 `admin.empty`（"当前窗口暂无数据。"）属另一命名空间，next-intl 不向下回退、不能复用。修复：workload 块补 `"empty": "暂无数据。"`（两快照表+两窗口产出表共用，不带"窗口"字样以对快照表准确）；全仓其余 10 处 `t("empty")`（opsIntakes/slaTodos/tenants/reviewQueue/workbench/products/compliance）逐一核键均存在；check-admin.mjs workload 必检清单补 empty，删键失败探针实测 exit 1 `missing message key: admin.workload.empty` 后还原。六 checker+tsc 全绿、token 仍 86；后端零改动（444 测试/101 eval 不变、头仍 0023）；Q109 E2E 同环境复验空库四空态显中文、无原始键泄漏。
- **Q109 管理端第七片·管理面读端点鉴权审计 + 同型补闸落地（2026-09-16，纯后端加固无迁移；feat 9269315/test 885574b）** — 审计起因：Q108 发现 GET /api/admin/sla/todos 自 M10b 起裸奔，逐行核对 Q75 补闸矩阵（docs/05）后穷举全部 `/api/admin` GET，确认另有 8 个矩阵把 GET 与写操作同组标注角色、而代码自 Q75 红线收口起仅在写口挂闸的裸读口。本片统一按 Q92/Q108 query actor 同型补闸（actor_id 必填缺参 422、重复 roles、require_any_role 不符 403）：GET /api/admin/c1/signal-weights 与 GET /api/admin/c1/industries=operations（Q2/Q7）、GET /api/categories=dictionary_admin（Q68，注意无 /admin 前缀）、GET /api/admin/fp-source-routes 与 GET /api/admin/fit-weights=operations（Q8/Q34）、GET /api/admin/compliance-wordlist=operations|internal_compliance（Q48，platform_admin 不在内）、GET /api/admin/ai-models=platform_admin（Q82）、GET /api/admin/ai-scene-routes=operations|platform_admin（Q82）；5 个路由文件各加本地 query 依赖（不抽共享工厂），处理器仅末位挂 `_: Actor = Depends(...)`、函数体零改动，全部写口服务层闸一字不动。**不补闸的两类严格区分**：矩阵未定读角色的 config 三读口（列表/单项/history）、g2-candidates、skill-prompts 三读口保持免闸，读角色标【原文未给出，待补】等拍板；矩阵明文"GET 无 actor 体（同 outbound Key 列表口径）"的 agent-keys 与 ai-models/{id}/keys（只回 key_id/fingerprint/status/时间元数据）有意开放，测试锁定；fcw.csv 的隔离是 Q100 tenant_id 口径不在本片。对 Q75 矩阵方法枚举只收写口与表行 GET 角色的张力，解释为红线收口当片写口优先（05 显式"有意开放"清单不含这 8 口），按 Q108 先例以后续加固片补闸，不回溯改 Q75 原文。CCR/建模读词表、配置 knobs、场景路由等系统内部调用走服务层不经 HTTP 不受影响；前端全仓 grep 确认 8 端点零浏览器/RSC 消费方，前端零改动、无页面变化。测试：新建 test_admin_read_rbac_api.py 表驱动 23 条（8 端点无参 422+customer 403、矩阵角色逐个 200、词表 platform_admin/product_reviewer 403 而 internal_compliance 200、categories 对 operations 403、config/g2-candidates/skill-prompts/agent-keys 无参 200 锁定待补/有意开放结论、未知 model keys 不返 403/422），既有 14 处裸 GET 调用补 query（c7_llm_resolve 的 _category_id 改 dictionary_admin 影响 10 用例、model_gateway ai-models 两处 platform_admin 与 scene-routes operations、platform fit-weights operations）；全套 backend 444 passed（421→444）、ruff check 净（ruff format --check 从非门槛，对 116 个历史文件同样报告，不批量重排）；六 checker+tsc 回归全绿、token 仍 86；无迁移（头仍 0023）、无新端点/无新写口，纯后端切片无 UI 变化未做浏览器 E2E（422/403/200/开放锁定均由集成测试矩阵覆盖）。M12 不闭合：config/g2-candidates/skill-prompts 读角色待拍板、客户侧余五菜单、触发方【待补】事件、五型 review SLA 72h【待补】、其他待办黄色口径【待补】、产品模板/资料库、付费档额度/"第5档"【待补】/计费订阅/团队账号（V2）、G2 common fid 基线、CSV JSON/异步导出、日期筛选器与其余驾驶舱（不进 V1）、CCR 历史/法审倒计时/规则库界面，下一点工前不自动开工。
- **Q108 管理端第六片·待办 SLA 看板落地（2026-09-16，跨栈加法无迁移；feat 15387a9/test 1975946）** — 安全加固：M10 切片 b 落地的 `GET /api/admin/sla/todos` 自始为**无鉴权裸端点**（Q75 补闸矩阵只收 POST /sla/run），本切片补 require_sla_view query 依赖——actor_id 必填（缺参 422）+重复 roles，require_any_role 仅放行 platform_admin，operations/internal_compliance/customer 全 403；POST /sla/run 的 body 内闸与 leader_lock 409/503 口径不动。status 由原 open|all 扩为白名单恰 4 值 {open,escalated,resolved,all}，非法 422 `unknown todo status: {status}`，默认 open；继续不分页、due_at ASC、返回字段集不变，sla_state 派生仍走 core/sla/policies.py（resolved 终态优先；escalated/过 due_at→red；黄仅 law_review 且 created_at 超 sla.yellow_hours=24h 窗口；其余类型到期前 green，黄色口径【原文未给出，待补】）。前端新增管理端第六页 `app/[locale]/admin/sla-todos/page.tsx`（约 166 行纯只读 RSC，无 actions/无 client 岛）：force-dynamic、searchParams Promise、GET 表单四筛选（URL 非法 status 前端归一 open 不透传）、9 列（待办/租户/类型/负责角色/关联实体/状态/SLA 态/截止时间/升级时间），时间空值显 —、长 id 截前 8 且 title 留全值；无任何写控件，POST /sla/run 运维口不上页面（checker 守禁含 /sla/run、无 "use server"、无 POST/PATCH/DELETE）；身份沿用 server-only LOOM_ADMIN_ACTOR_ID/LOOM_ADMIN_ROLES（默认 platform_admin 即可看），未配显 unconfigured 不发请求，403/422 映射中文 error 键；todo_type/status/assignee_role/entity_type/sla_state 英文码全部原样不译，只 UI 框架中文入消息表；sla_state 四色 chip 复用 riskChip（red=riskCritical/yellow=riskMedium/green=riskLow/resolved=stateArchived）；yellowNote 如实声明黄色仅法审（到期前 24h）、其他类型到期前绿、超时或升级红，不造口径；重置链接走 @/i18n/navigation（首版硬编码 /zh-CN 路径提交前自查修正）。lib/api.ts 增 SlaTodoView/getSlaTodos；admin-sidebar 增第六项（恰 6）；消息增 admin.slaTodosNav + admin.slaTodos.* 21 键；admin.module.css 零新增（token 仍 86）；.env.example 无需改；无迁移（头仍 0023）。测试：test_sla_api.py +3 集成（RBAC 无参 422/三角色 403/platform_admin 200 默认 open 空体；非法 status 422 detail 原文；5 种子 pws_ready green/law_review yellow/review_pwc_combo overdue red/ops_assist_category escalated red/wordlist_rescan resolved——all 恰 5 行 due_at 升序且 states 字典精确、open 恰 3、escalated/resolved 各恰 1），全套 backend 421 passed、ruff check 净（ruff format --check 对全仓 116 个历史文件同样报告，format 从非项目门槛，不批量重排）；check-admin.mjs 扩展（slaTodosNav+21 键/必备文件/侧栏第六项恰 6/Q108 守卫块：force-dynamic、禁 "use server"、剥 // 注释行后禁 NEXT_PUBLIC·http(s)·/zh-CN/ 字面路径·写词·/sla/run，必含 getSlaTodos/四状态值/yellowNote/shortId/四色类/sla_state，api 三探针，后端 router require_sla_view/PLATFORM_ADMIN/Depends 挂载/GET /todos/TODO_STATUS_FILTERS/422 原文）；两失败探针（Depends 改名、页面加 /sla/run 常量）实测 exit 1 后 Edit 还原，六 checker+tsc 全绿。9222 Chrome 端到端（/tmp 一次性 SQLite loom-q108.db，不碰 Docker PG；uvicorn 8018 LOOM_SCHEDULER_ENABLED=false+next 3018；6 种子跨 t1/t2/t3 覆盖五 todo_type）全过：默认 open 4 行 due_at 升序四态正确、四色 chip computed background/color 各异且英文码原样；?status=resolved 恰 1 行下拉回显"已解决"resolved chip=stateArchived；?status=all 恰 6 行下拉"全部状态"；?status=bogus 归一 open 下拉"待处理"不透传 422；open 行临时刷 resolved 后空态"当前筛选下暂无待办。"；侧栏第六项"待办 SLA 看板"存在；yellowNote 与定稿一致；重启 LOOM_ADMIN_ROLES=operations→主区"没有操作权限"无表格；去 LOOM_ADMIN_ACTOR_ID→unconfigured 中文通知；浏览器侧 XHR/fetch 零条（后端仅 RSC 服务端访问）、console 零 error/warn；验收后进程与 /tmp 全清。M12 不闭合：客户侧余五菜单、触发方【待补】事件管理端化、五型 review SLA 72h【待补】、其他待办类型黄色口径【待补】、产品模板/资料库、付费档额度/"第5档"【待补】/计费订阅/团队账号（V2）、G2 common fid 基线、CSV JSON/异步导出、日期筛选器与其余驾驶舱（不进 V1）、CCR 历史/法审倒计时/规则库界面，下一点工前不自动开工。
- **Q107 管理端第五片·录入单运营队列与跨租户运营操作落地（2026-09-16，跨栈加法无迁移；feat 6b11897/test 475c2b7）** — 后端段1 新增**唯一**只读端点 `GET /api/intakes/ops-queue`（require_ops_view：query actor_id 必填+重复 roles，require_any_role(actor, OPERATIONS, PLATFORM_ADMIN)，缺 actor 422/customer 403；跨租户 select+count 子查询、created_at DESC、可选 status（不在 15 态 STATE_LABELS→422 `unknown intake status: {x}`）、limit 1-100 默认 20/offset；OpsIntakeView 在租户内 IntakeView 上加 created_at；路由注册先于 `/{intake_id}`）；service.list_ops_intakes、schemas OpsIntakeView/OpsIntakeList；**无新写口、无迁移（头仍 0023）**，运营操作复用既有 POST transitions（403/409/422 口径沿用）。前端新增管理端第五页 `app/[locale]/admin/intakes/`：ops-intake-codes.ts（**运营事件白名单恰 6 码** OPS_INTAKE_EVENTS=[ops_confirm,to_cold_start,reject,b2_approved,b2_parent_fallback,b2_rejected] as const+isOpsIntakeEvent；INTAKE_STATUS_FILTERS 15 态+守卫；注释列排除理由：wf01_*/auto_confirm 系统、客户四码在 Q106 客户页、send_review/review_*/start_modeling/model_*/failed_* 触发方【待补】不外放）、actions.ts（"use server" opsTransitionIntakeAction；OpsIntakeActionResult 判别联合 ok | 403/404/409/422+detail | unconfigured/missing_role/unknown；无 CURRENT_ADMIN_ACTOR_ID→unconfigured、空/非白名单事件→本地 422、**ADMIN_ROLE_LIST 不含 operations→missing_role 零请求**；KNOWN_STATUSES+errorDetail JSON.parse）、ops-intake-actions.tsx（唯一 client 岛，allowedEvents∩六码白名单按白名单序出按钮，空交集显"当前状态暂无可执行的运营操作"，useTransition+router.refresh，禁 @/lib/api 连 type import/禁 fetch·URL）、page.tsx（RSC force-dynamic，15 态 URL GET 筛选 select（option 文案复用 intake.status.*）+白名单归一非法 status、PAGE_LIMIT=50 仿 review-queue pager、四列 intake_id 链接/tenant_id/中文状态 chip/created_at，unconfigured/403/404/422/unknown 分支）、[intakeId]/page.tsx（RSC force-dynamic，getIntake+getAllowedEvents Promise.all，404→notFound()，getProductSpace 404→null；基本信息 dl+候选类目 ID、profile 原样键值 dl、OpsIntakeActions 岛、产品空间只读区 id/lifecycle 原样码+snapshot details/pre，CSS 两级 `../../admin.module.css`）。lib/api.ts 原 ADMIN_ROLES 改名导出 ADMIN_ROLE_LIST（8 处引用同步），增 OpsIntakeView/OpsIntakeList/getOpsIntakeQueue（actor_id+重复 roles）/adminTransitionIntake（管理员 actor{id,roles} 复用 transitions）；admin-sidebar 增第五项 /admin/intakes（恰 5 项）；消息增 admin.opsIntakesNav + admin.opsIntakes.* 41 键（eventLabels 六事件动词语译：运营确认/转冷启动类目创建/驳回录入/B2 类目通过/挂最近父类目继续/B2 打回补充资料；**15 态状态词零新增**复用 intake.status.*）；.env.example 补 LOOM_ADMIN_ACTOR_ID/LOOM_ADMIN_ROLES 并注明 platform_admin 默认只读、6 ops 事件硬要求 operations、不含则只读零请求；admin.module.css 零新增（全复用 table/filters/filterField/pager/planChip/detailList/payloadPre/decisionCell/decideActions/notice/msgOk/msgErr 等，token 仍 86）。测试：test_intake_api.py 9→12（RBAC 422/403/operations·platform_admin 双角色 200 空体、跨租户 t1 三单+t2 一单 total=4+created_at 存在+draft=4/submitted=0/非法 status 422/limit=2 两页不重不漏、完整链 submit+wf01_confirm 后 pending_confirm 命中、ops_confirm→submitted 后该队列清空/submitted 命中），backend 全套 418 passed/ruff 过；check-admin.mjs 扩展（41 键/五新文件/侧栏恰 5/eventLabels 恰 6 键/六码数组 JSON.stringify 精确相等+INTAKE_STATUS_FILTERS 恰 15/禁词扫描剥 // 注释行（白名单设计依据注释合法，首版 7 处误报后修）/两页 force-dynamic·禁 NEXT_PUBLIC·http(s)·写词/actions "use server"+四状态码+unconfigured/missing_role/CURRENT_ADMIN_ACTOR_ID/ADMIN_ROLE_LIST/isOpsIntakeEvent/岛 "use client"·禁 @/lib/api·URL+router.refresh/api getOpsIntakeQueue/adminTransitionIntake//api/intakes/ops-queue/ADMIN_ROLE_LIST/后端 router 探针 @router.get("/ops-queue")·require_ops_view·require_any_role(actor, OPERATIONS, PLATFORM_ADMIN)·STATE_LABELS+422·/ops-queue 注册 indexOf 先于 /{intake_id}/service list_ops_intakes；三失败探针：codes 注 auto_confirm、岛注 @/lib/api、后端角色改单 PLATFORM_ADMIN 均实测 exit 1，git checkout 还原全绿；check-products/tokens(86)/nav(8)/workbench/compliance 不回归）。9222 Chrome 端到端（/tmp 一次性 SQLite loom-q107.db，不碰 Docker PG；uvicorn 8017 LOOM_SCHEDULER_ENABLED=false+next 3017；async 种子 t1/t2 两租户 8 单覆盖五态+stored 带产品空间）全过：队列跨租户 5 行含 t2、15 态下拉全中文、?status=pending_confirm 过滤 1 行且 select 回显、bogus_state 前端归一显全部；详情岛六事件全部真实点击——ops_confirm 待确认→已提交、to_cold_start→类目创建中（RSC 刷新换 b2 三键）、b2_approved/b2_parent_fallback 类目创建中→待补充参数（仅留驳回）、b2_rejected→需补充资料（交集空显"当前状态暂无可执行的运营操作"）、reject 待补充参数→驳回终态零按钮；stored 显产品空间只读区 id/lifecycle=modeling 原样码+快照折叠、无空间单不渲染；未知 id→Next 404；重启 next 改 LOOM_ADMIN_ROLES=platform_admin→点驳回显 missing_role 中文红文、状态不变、API 日志零 transitions POST（零请求守卫成立）；去 LOOM_ADMIN_ACTOR_ID→队列 unconfigured 通知+岛 unconfigured 提示零请求；网络全同源 :3017（后端仅 RSC/Server Action 服务端访问）、console 零 error/warn；验收后进程与 /tmp 全清。E2E 教训：bash for 循环种子脚本事件载荷变量名笔误会被 422/403 静默吞掉致单停留 draft、product-space 404——多事件种子必须用显式 fire() helper 逐环放并断言状态推进。M12 不闭合：触发方【待补】事件管理端化须等 docs/13 补裁决、五型 review SLA 72h 待补、客户侧余五菜单、产品模板/资料库、付费档额度/"第5档"【待补】、额度拦截/计费订阅/团队账号（V2）、G2 cat='common' fid 基线回填、CSV JSON/异步导出、管理端日期筛选器与其余四驾驶舱（不进 V1）、CCR 历史/法审倒计时/规则库界面，下一点工前不自动开工。
- **Q106 录入单 15 态客户操作 + draft 资料编辑落地（2026-09-16，纯前端无迁移；feat 64fe6f4/test 3333e39）** — 纯前端消费段1 既有四端点（allowed-events/transitions/PATCH profile/product-space），后端零改动（头仍 0023，415 测试/101 eval 不变）：产品详情页 `[intakeId]/page.tsx` 重写为 force-dynamic（getIntake+getAllowedEvents 并发，404→notFound，再取 productSpace），新增操作岛 intake-actions.tsx——按 `GET allowed-events` 与**客户事件白名单** `{submit, params_completed, quota_confirmed, resubmit}`（intake-codes.ts as const + isCustomerIntakeEvent 守卫，白名单序）的交集渲染按钮；运营/系统事件（ops_confirm/to_cold_start/reject/b2_*、wf01_*/auto_confirm）不外放，send_review/review_*/start_modeling/model_*/failed_* 触发方 docs/13 标【待补】不外放；终态/空交集显"当前状态暂无可执行操作"。draft 态新增资料编辑岛 draft-profile-form.tsx（仅产品名，fid 沿用 Q98 product_name 挂账），PATCH profile 成功 router.refresh，非 draft 409 ProfileNotEditable 走中文错误。身份新增 server-only `LOOM_ACTOR_ID`（roles 恒 []，V2 改会话派生；.env.example 同步，不带 NEXT_PUBLIC），未配→unconfigured 不发任何写请求。actions.ts 两 Server Action（transitionIntakeAction/updateDraftProfileAction，判别联合 unconfigured/403/404/409/422/unknown；空 id 或非白名单事件→422；空名/超 100 前端拦截）：transitions 422 体实测为 FastAPI 包装的 `{"detail":{"missing_fids":[...]}}`（首版读 body.missing_fids 未中，curl 核体后改解析 detail），fid 原样码以"、"拼接中文句式回显"资料不完整，缺失字段：{fids}"，枚举 fid 不入消息表。产品空间存在时只读展示 id/lifecycle（码原样 mono）/profile_snapshot（`<details><pre>` 原样 JSON，404→null 不造）；category_creating 显 docs/13:31 原文中性话术"资料分析中"。两 client 岛禁 @/lib/api（连 type import 也禁）与 fetch/URL，仅同源 Server Action；CSS import 须同目录 `./products.module.css`（首版误用 `../` 致 next 构建期才暴露，tsc 不查 CSS 模块存在性——教训：新接岛必须真实跑页面不能只过 tsc）。lib/api.ts 增 AllowedEvents/ProductSpaceView 类型与 getAllowedEvents/transitionIntake/patchIntakeProfile/getProductSpace（404→null），消息 products.* +18 键（四事件操作动词语译 eventLabels；状态仍走 intake.status.* 15 中文键，事件码不入值），CSS 追加 5 类只消费既有 86 token。check-products.mjs 整体重写：18 键/三新文件/env 单行 NEXT_PUBLIC 赋值守卫（首版跨行正则误报已修）/api 五函数四路径/actions missing_fids·unconfigured/两岛 "use client"·禁 @/lib/api·fetch·URL·必含 router.refresh/白名单数组精确比对且禁含 ops_confirm·to_cold_start·reject·send_review·auto_confirm·b2_/详情页 getAllowedEvents·getProductSpace·两岛·CURRENT_ACTOR_ID·category_creating·force-dynamic/后端防漂移探针读 router.py 四路由与 statemachine.py 四客户事件；两失败探针（岛注 @/lib/api、白名单注 ops_confirm）实测 exit 1 后 Edit 还原；check-tokens 仍 86、check-nav 恰 8。9222 Chrome 端到端（/tmp 一次性 SQLite loom-q106/app.db，不碰 Docker PG；uvicorn 8766+next 3211；async 种子十态）全过：draft PATCH 改名保存+RSC 刷新、draft_incomplete submit→422"资料不完整，缺失字段：brand_name"、ai_recognizing 交集空无岛无编辑、pending_params/pending_quota/need_more_info 三客户按钮→submitted（curl 复核状态）、stored/rejected 零按钮、category_creating"资料分析中"零按钮、modeling 产品空间 id/lifecycle=modeling 原样+快照、未知 id→Next notFound；curl 补验终态非法事件 409、非 draft PATCH 409、空 roles 触发 b2_approved→403；网络 11 条全同源 :3211 零直连 :8766、console 零 error/warn；重启 next 去 LOOM_ACTOR_ID→两处 unconfigured、零按钮、只读正常。验收后进程与 /tmp 文件全清。M12 不闭合：录入单运营侧操作页（ops/review/modeling 事件管理端化，触发 actor 待补）、客户侧余五菜单、产品模板/资料库、付费档额度/"第5档"【待补】、额度拦截/计费订阅/团队账号（V2）、五型 SLA 72h 待补、G2 cat='common' fid 基线回填（客户补参表单随之）、CSV JSON 形态/异步导出等，下一点工前不自动开工。
- **Q105 管理端租户管理 + 客户入驻 Onboarding 前端页落地（2026-09-16，纯前端无迁移；feat 9ffb7cf/test 058c8bb）** — 新建 `/[locale]/admin/tenants/` 消费 Q95 既有六路由（后端零改动，头仍 0023，415 测试/101 eval 不变）：page.tsx（RSC force-dynamic，listTenants 全量七列表：租户 ID 链接/名称/套餐/状态/月度 token 额度/创建时间/操作；空态 colSpan 7）、provision-form.tsx（唯一开通 client 岛：tenantId 必填、名称可选、plan 四档下拉默认 trial，成功清空表单+router.refresh）、tenant-row-actions.tsx（每行 client 岛：plan 下拉+改套餐按钮同档禁用，暂停/恢复按 status 二选一）、`[tenantId]/page.tsx`（RSC force-dynamic，params Promise await；注册表六字段只读 dl + Onboarding 派生 intakes/product_spaces 计数/first_modeling_started 是·否 dl，onboardingNote 注明实时派生无独立引导状态机）、actions.ts（"use server"：provisionTenantAction/changePlanAction/pauseTenantAction/resumeTenantAction，TenantActionResult 判别联合 403/404/409/422+detail/unconfigured/unknown，errorDetail 复用 Q104 JSON.parse 法，空 id provision→422 其余→404，空名 trim→null）、tenant-codes.ts（TENANT_PLANS 四码/TENANT_STATUSES 三码 as const 单一事实源）。写操作全走同源 Server Action（provision/change-plan/pause/resume），无删除（后端无此口不造）；两 client 岛禁 @/lib/api（连 type import 也禁）与 URL。plan（trial/basic/pro/enterprise）/status（trial/active/paused）枚举码原样不翻译不入消息表；三态 chip 套 riskChip 基类 statusTrial=info/statusActive=success/statusPaused=warning；付费档 monthly_token_quota=null 显 —，仅 trial 显 500000，付费档各档额度与"第5档"【原文未给出，待补】不造数。身份沿用 Q104 server env 自报（无 LOOM_ADMIN_ACTOR_ID→unconfigured 零请求），422 FastAPI detail 英文原样红文（本切片仅 provision InvalidPlan），403/404/409 走中文 error 键。lib/api.ts 增 TenantView/TenantOnboarding/TenantDetailView 与 listTenants/getTenant/provisionTenant/changeTenantPlan/pauseTenant/resumeTenant（adminPath 编码，写口 body actor{id,roles}），admin-sidebar 增第四项，消息增 tenantsNav+admin.tenants.* 29 键，CSS 追加 8 类只消费既有 86 token。check-admin 扩展守卫（29 键/六文件/六枚举禁译/侧栏第四项/api 六函数+prefix+/change-plan·/pause`·/resume`/Q105 块——两页面 force-dynamic·禁 NEXT_PUBLIC·URL·写词、详情派生三字段、actions 四导出名+四码+CURRENT_ADMIN_ACTOR_ID、两岛 "use client"/禁 @/lib/api·URL/router.refresh、四 action 至少一岛引用、codes 四 plan、三态 statusXxx、后端 router prefix+require_admin_view+六路由探针；失败探针向未跟踪岛追加 @/lib/api 实测 exit 1 后 Edit 还原）。9222 Chrome 端到端（/tmp 一次性 SQLite loom-q105.db，不碰 Docker PG；async 种子三租户 t1 pro/active/null 带 1 intake stored+1 space、t2 无名 trial/trial/500000、t3 basic/paused/null）八项全过：三行+侧栏第四项+三态 chip computed 色（active rgb(246,255,237)/rgb(56,158,13)、trial rgb(230,244,255)/rgb(9,88,217)、paused rgb(255,251,230)/rgb(212,136,6)）+额度 —/500000/—、t1 详情派生 1/1/是、nope 404 中文、开通 t4 enterprise→active 成功+表单重置+同档改套餐禁用、重复 t1 409、t2 改套餐 trial→pro 状态仍 trial（change-plan 只动 plan）额度→—、t4 暂停→paused+恢复按钮→恢复回 active、auditor 403、去 actor unconfigured 零行、网络仅同源 :3210（Server Action POST+_rsc GET）零直连 :8765 console 零消息。
- **Q104 管理端统一审核工作台逐条裁决落地（2026-09-16，跨栈加法无迁移；feat 13d29f9/test 5fa0086）** — 每个 pending_review 候选行新增唯一 client 岛 `decision-cell.tsx` 三按钮：通过（confirmed；critical 档 window.confirm 中文二次确认）、修改后通过（modified；内联 JSON textarea 预填美化 payload，前端 JSON.parse 且必须为非数组对象，非法显 invalidJson 不出请求）、拒绝（rejected；理由可选，modified 面板同样带可选理由）；全部经 Server Action `decideCandidateAction`（actor 取 server env、判别联合 403/404/409/422+detail/unconfigured/unknown、useTransition+router.refresh），client 岛禁 @/lib/api（连 type import 也禁）与硬编码 URL。复用既有 `POST /api/skill-candidates/{id}/decision`（无新写口/端点/迁移，头仍 0023）：rejected→archived+review_note、modified→替换 payload+human_modified=True 再过真实适配器、confirmed→直接适配器，403 按 WF review_role 逐候选；422 适配器业务错误 FastAPI detail 英文原样红文回显不翻译。裁决列三态都渲染：pending 出岛，applied/archived 出语义 chip+裁决时间+已人工修改+裁决备注（空值不渲染），岛仅 pending_review。后端唯一加法=workbench `_candidate_view` 补 review_note（排序用例补 None 断言 + 新增 rejected 后 archived 视图回显备注集成；全套 415 passed，eval 101 不变）；lib/api.ts 加 ReviewQueueCandidate.review_note 与 decideCandidate（body decision/payload??null/reason??null/actor），消息新增 13 键，CSS 追加 7 类只消费既有 86 token；check-admin 扩展守卫（existsSync 缺失文件探针、actions split 限定函数体查四状态码、/decision 子路径正则、岛仅 pending）。9222 Chrome 端到端（/tmp 一次性 SQLite，不碰 Docker PG；种子走真实 HTTP 含租户注册与真实 CAT-RECOG medical critical 投递）：critical 取消阻断/接受过真实 modeling 适配器、modified 非法 JSON 零请求+适配器 422 英文 detail 原样+合法替换内容真正落 atom-candidates、rejected 备注落库、applied/archived tab 三态展示无岛、reviewer 裁 WF-01 403、去 actor unconfigured 零行、网络仅同源 :3210 console 零消息。
- **Q103 管理端统一审核工作台前端页落地（2026-09-16，跨栈无迁移；feat 8c7fe47/test 8e4f496）** — 新建 `/[locale]/admin/review-queue/` 三文件消费 Q93 API：page.tsx（RSC force-dynamic、searchParams Promise await；target_type 五型多选 chip、risk_level 下拉、state 默认 pending_review 可切 applied/archived、wf_id 文本全部 URL 驱动并白名单归一；limit=50 offset 上一页/下一页简分页；五型混排按后端 risk_rank DESC, created_at ASC）、actions.ts（`"use server"` batchApproveAction，actor 取 server env，判别联合 unconfigured/403/404/409/422/unknown，all-or-nothing 透传 POST /batch-approve）、batch-bar.tsx（唯一 client 岛，组件内 useTranslations——函数 t 闭包作 props 传 Client Component 曾致 RSC 序列化 500；useTransition+router.refresh，禁 import @/lib/api；data-batch-candidate 勾选）。风险四档 critical/high/medium/low 用 danger/warning/info/success 语义色 chip + 英文码原样不翻译，risk_reason 机读来源码原样副显，confidence 显百分比并回显后端 batch_pass_confidence=0.85 门槛说明；非 batch_eligible 勾选禁用、applied/archived 全禁用、BatchBar 仅 pending_review；payload 原样 JSON `<details><pre>` 折叠（不为五型造字段中文名）。后端唯一加法=workbench `_candidate_view` 增 `payload` 字段（无迁移，测试一条 industry=medical；414 测试、101 eval 不变，迁移头仍 0023）。admin-sidebar 增第三项（避让 /admin/review-workload），token 仍 86，lib/api.ts 增 ReviewQueue 类型/getReviewQueue/batchApprove，五型码/四风险码/三态码不入消息表；check-admin.mjs 扩展守卫（两失败探针实测 exit 1）。9222 Chrome 端到端（/tmp 一次性 SQLite，不碰 Docker PG）：默认 8 行排序、四档 chip computed rgb、payload 折叠、?target_type=atom_batch 4 行、WF-01 候选 reviewer 403 红文、真实两候选批量通过 pending 8→6、?state=applied 3 行全禁用、网络仅同源 :3210、console 零错、去 actor unconfigured 与 auditor 403 三态；逐条 confirmed/rejected/modified 裁决（含 payload 替换编辑器）留下一切片。
- **Q102 管理端独立布局 + 两驾驶舱只读页落地（2026-09-16，纯前端无迁移；feat 9ba1b5b/test 2bc8765）** — 新建与客户 `(shell)` 平级的路由组 `app/[locale]/admin/`（独立 frame/侧栏/「Loom 管理端」顶栏）：Token 成本、人工审核工作量两 RSC 只读页（force-dynamic）消费既有 Q92 API——窗口回显默认近 30 天（无日期筛选器）、合计卡、按天×模型/Skill/失败三表与候选/待办积压+窗口产出四表，等待秒数机械换算天/小时；客户 shell 八菜单不放入口（checker 守 nav 恰 8 项），`/[locale]/admin` 307 到 token-cost，侧栏带返回客户前台。管理员身份 server-only env 自报 `LOOM_ADMIN_ACTOR_ID`/`LOOM_ADMIN_ROLES`（默认 platform_admin，actor_id+重复 roles query，未配置显 unconfigured 不请求；V2 改会话派生）。枚举码（model_id/skill_id/currency_code/五型 target_type/todo_type/state）原样不翻译；admin.module.css 只用既有语义 token（仍 86）。新增 check-admin.mjs（消息键/枚举码禁入消息表/force-dynamic/禁 NEXT_PUBLIC 与硬编码色/nav 隔离/后端路由守卫，失败路径实测）；后端零改动（414 测试、101 eval 不变），迁移头仍 0023。9222 Chrome 端到端：两页各桶数据与后端 JSON 一致、307 重定向、auditor 403 文案、去 actor unconfigured、客户侧栏 8 链接无 admin、浏览器零直连后端、零业务 console 错；curl 422/403 验收通过。
- **Q101 合规风控只读功能页落地（2026-09-16，跨栈无迁移；feat da644e9/test df2c3b8）** — 后端段10 新增租户只读聚合 `GET /api/compliance/overview`：仅 active frozen PWS 一行一产品，各市场 append-only CCR 报告取最新一行后跨市场从严（blocked>downgrade_pending>approved>clean）、block_required OR，附单条 LawReview（domain/status/conclusion/decided_at）；无报告/无法审分别 null（不伪造 clean），产品名走 profile.product_name 临时键否则回退产品空间 ID 前 8 位；tenant_id 必填非空（422），未知租户 200 空 items，读路径不触发 Q95 准入门、不 writeAudit。前端 `/compliance` 占位升级为 RSC（force-dynamic）：总览行 + `<details>` 展开逐市场命中词/`{word} → {target}` 降级建议/法审 dl，CCR 四态与法审三态语义 chip（compliance.module.css 四 tone，token 仍 86）；清洗触发、降级批准、法审裁决、CP-LAW 词库 CRUD 一律不进客户页（internal_compliance 台内）。新增 check-compliance.mjs（消息键/force-dynamic/禁写台面/Query 守卫，失败路径实测）；后端 +4 集成（全套 414），9222 Chrome 端到端（含 superseded/他租户干扰、ghost 空态、chip 计算色、零直连）验收通过；迁移头仍 0023，eval 101/101。
- **Q100 中台 FCW CSV 导出落地（2026-09-16，后端纯只读无迁移；feat 12884b7/test e67c604）** — 新横切包 `app/core/exports/` 挂 `GET /api/exports/fcw.csv`：同步流式 CSV，**仅 final_id 单列**（含表头），tenant_id 必填（空 422）、product_space_id 可选收窄，只导 published（draft 排除；Q32 revoked 落地后同过滤自然生效）、created_at DESC；读路径不触发 Q95 准入门、不 writeAudit，未知租户 200 仅表头空文件；`text/csv; charset=utf-8` + attachment filename=fcw-<tenant>.csv。D4 行【待补】就 CSV 收口，6 层原料包属 docs/09:87 台内详情/复制口径（09 已注分界），CSV JSON 形态/异步导出挂后续。后端 +3 集成（全套 410），curl 一次性 SQLite 端到端验收通过；迁移头仍 0023，eval 101/101。
- **Q99 工作台功能页落地（2026-09-16，跨栈无迁移；feat 43f4fcb/659d959，test 1f05c27/a0a6ca3）** — 后端段1 新增 `GET /api/intakes/overview` 租户状态聚合只读端点（IntakeOverview；读路径不触发 Q95 准入门，未知/暂停租户零值；注册序先于 /{intake_id}）；前端 `/workbench` 占位升级为 RSC 总览（force-dynamic）：录入单总数 + 15 态分组（intake.status.* 十五键对齐 docs/13，仅渲染计数>0）+ 快捷入口；D5 原文五指标（今日生成/发布/互动/趋势/健康度，数据源段12/13）渲染禁用态 V2 卡、不显任何数字（含 0）；products 列表/详情状态 chip 同一切片切中文状态词。新增 check-workbench.mjs（含跨栈路由顺序守卫）、check-products.mjs 增裸状态码守卫；后端 +3 集成（全套 407），9222 Chrome 端到端验收通过（真实分组/五卡/零直连/零控制台错误/ghost 空态）；迁移头仍 0023，eval 101/101。
- **Q98 产品中心功能页落地（2026-09-16，跨栈无迁移；feat 6693c18/test a9f4c4a）** — 后端段1 新增 `GET /api/intakes` 租户分页只读端点（IntakeList；读路径不触发 Q95 准入门，未知/暂停租户空列表 200；参数 422）；前端 `/products` RSC 列表（force-dynamic）+ `/products/new` Server Action 建 draft + `/products/[intakeId]` 只读详情 + 产品模板/资料库占位；`lib/api.ts` 唯一服务端访问层（server-only env LOOM_API_BASE_URL/LOOM_TENANT_ID，浏览器零直连、后端不加 CORS）；产品名走工程临时键 `product_name`（G2 fid 基线挂账）；新增 `scripts/check-products.mjs` 零依赖校验。后端 +3 集成（全套 404），9222 Chrome 端到端验收通过（真实数据/CJK 新建闭环/404/零直连/控制台零错）；迁移头仍 0023，eval 101/101。
- **Q97 应用外壳+D5 八菜单导航骨架落地（2026-09-16，feat 345e15e/test 09deafd）** — `(shell)` 路由组：240px 侧栏+顶栏+1280 内容列，nav.ts 注册表驱动 8 菜单（next-intl Link/usePathname active）；段8/12/13 三项（content/analytics/social-accounts）V2 徽标占位，余五项 V1 占位；`/[locale]` 307 到 workbench，欢迎页删除；新增 `--color-bg-hover`（86 token）、`scripts/check-nav.mjs` 三方漂移校验、`i18n/navigation.ts`；管理端 `/[locale]/admin` 独立布局不预埋。typecheck/build、两级 307/8×200、9222 Chrome 验收通过；无后端/迁移变化（头仍 0023，401 测试、101 eval 不变）。
- **Q96 前端 i18n+tokens.css 脚手架落地（2026-09-16，feat 946a774/test 8b614b6）** — next-intl v4（routing/request/middleware，仅 zh-CN，无前缀 307 实测→/zh-CN）+ `app/[locale]` 段（旧根页面删除）+ messages/zh-CN.json（common/home/error）+ tokens.css 三层 85 变量唯一事实源与 tokens.ts 镜像，`scripts/check-tokens.mjs` 零依赖逐值校验（npm run check-tokens）；Semantic 补 `--color-brand-bg` 并补登 docs/18 §5。typecheck/build 通过，curl 307/200 与 9222 Chrome 可视验收通过；无后端/迁移变化（头仍 0023，401 测试、101 eval 不变）。M12 菜单页面逐片点工。

### Added

- **Q96 前端 i18n+设计 Token 方案拍板定稿（2026-09-16，纯文档；18 号文档 🟡→✅）** — 四接缝全甲：next-intl + `[locale]`（V1 仅 zh-CN，V3 首外语默认 en-US）；CSS Modules + tokens.css 三层 Token（不引 Tailwind/AntD 组件库）；运营数据字典多语言延 V3、V1 不预埋翻译列（Q58 分治）；色值借 AntD5 默认调色板作 primitive 工程初值（只借值不装依赖）。docs/18 补 §2.4 落码结构与 §3.6 全量 CSS 初值，可直接照做；02 C1.40 记录、docs/README 升 ✅。无代码/迁移/测试变化；前端页面仍按 M12 逐片点工。

- **Q95 租户管理+客户 Onboarding 落地（2026-09-16，09 D3.11①③；迁移 0023 建 tenants 表+存量回填）** — 四接缝经负责人拍板追加 Q95（02 C1.39，“四项全按推荐（甲）”）：①租户注册表+platform_admin 生命周期端点（开通/列表/详情/改套餐/暂停/恢复，全程审计；GET query actor 422·403，写 actor 在体 403·404·409·422），段1 `POST /api/intakes` 未知租户 404、暂停租户 409（应用层闸，无硬外键，链内仍走 intake/PS 派生）；②plan 四档 trial/basic/pro/enterprise、status 三态 trial/active/paused，试用档月 token 额度 50 万（line 1466），付费档额度【待补】、价格仅展示不计费不拦截，改套餐不动 status；③Onboarding 不建状态机，详情派生 intakes/product_spaces 计数；④0023 回填存量 DISTINCT tenant_id 为 basic/active+backfilled 标记。勘误：租户实体此前误链 Q33（Q33 实为同租户指纹查重），以 Q95 为准。新包 `app/core/tenants/`；backend **401 测试全绿**（+10 集成 test_tenants_api.py，7 个既有 HTTP 建单测试补 t1 租户种子），ruff 通过；eval 不新增、仍 101/101。M12 余量：CSV 导出、客户前端与页面（18 🟡 门控）、额度/计费/团队账号（V2）。详见 08 §2.2 Q95 完成情况。

- **Q94 候选审核 SLA 待办自动挂接落地（2026-09-16，Q70②；迁移 0022 五条纯配置种子）** — 四接缝经负责人拍板追加 Q94（02 C1.38，“四项全按推荐（甲）”）：①五型时限各一键 `review.sla_hours.*` 种子统一 72h（原文无数值，source_ref 标待补，热更不改码）；②投递（人工/llm_auto 同事务）一候选一 ops_todo，assignee 取 WF 注册表 review_role，裁决（含工作台批量）即 resolve（applied/archived），escalated 不阻断裁决，挂接前存量不回填；③五型五个 todo_type（review_pwc_combo 等），Q92 驾驶舱积压自动分型，Q49 升级审计统一 `skill7.review_sla_escalated`，黄色窗口原文未给挂账。新模块 `app/core/skill7/review_sla.py`，Q49 sweep/调度器零改动；backend **391 测试全绿**（+7 集成 test_skill7_review_sla_api.py；WF-01 两例改按类型计数），ruff 通过；eval 不新增、仍 101/101。Q70 一期三件（统一列表/批量/SLA）已齐；M12 余量：五型时限待业务定值、租户 Onboarding/CSV/客户前端与页面。详见 08 §2.2 Q94 完成情况。

- **Q93 M12 统一审核工作台候选池落地（2026-09-15，Q70 审核运营机制一期①③；迁移 0021 纯配置种子）** — 四接缝经负责人拍板追加 Q93（02 C1.37，“四项全按推荐（甲）”）：①范围=统一列表+置信批量通过，候选自动挂 SLA 待办（Q70②）留下一片，派单/抽检/6 场景细分维持二期；②风险四档派生（atom_batch 取批内 ai_risk 最高、c1 敏感行业归 critical、无信号型 low 不编造），队列风险降档+同档最老优先，回带 risk_level/reason/confidence/batch_eligible；③可见角色=注册表 WF skill7 Gate 角色并集（operations+product_reviewer 数据驱动，platform_admin 不放行），裁决逐条按 WF Gate 角色，混角色批整批 403；④新配置键 `review.batch_pass_confidence=0.85`（Q70/Q9，不挪用 fieldpool 键），仅 pending 且置信严格过线且非 high/critical 可批量，单事务 all-or-nothing 复用 skill7 适配器（modified/rejected 仍逐条）。新包 `app/core/workbench/`，`GET /api/review-workbench/candidates` + `POST /api/review-workbench/batch-approve`；backend **384 测试全绿**（+6 集成 test_review_workbench_api.py），ruff 通过；eval 不新增、仍 101/101。M12 未闭合：候选 SLA 挂接/租户 Onboarding/CSV/客户前端与页面待续。详见 08 §2.2 Q93 完成情况。

- **Q92 M12 两个驾驶舱只读聚合 API 落地（2026-09-15，Q73 收敛的 2 个 V1 驾驶舱首块；纯只读无迁移）** — 四接缝经负责人拍板追加 Q92（02 C1.36，“四项全按推荐（甲）”）：①**M12 首片只做两驾驶舱只读 API**，工作台/租户 Onboarding/前端/CSV 留后续切片（18 号文档 🟡 门控不变）；②**Token 成本**：默认近 30 天可选 from/to（半开 UTC，非法 422），succeeded 且 model_id 非空按天×模型×币种聚合（embedding 计入、价 0 计调用数），failed 不计费按 skill 单列，多币种分组不换算（币种【待补】，单价归 Q67 不重算）；③**人工审核工作量**=积压快照（pending_review 候选按 target_type+最老等待秒数，待办 open/overdue/escalated 按 todo_type）+窗口产出（reviewed_at/resolved_at 计数），Q70 派单/10% 抽检/6 场景细分二期不做；④**platform_admin 只读+实时 PG 聚合**：`GET /api/admin/dashboards/token-cost|review-workload`，管理面 GET 首次 query 带 actor（422/403），不建表/不物化/无调度器/无租户过滤参数（租户实体尚不存在）。新包 `app/core/dashboards/`，append-only 三表只读；backend **378 测试全绿**（+7 集成 test_dashboards_api.py），ruff 通过；eval 不新增、仍 101/101。M12 未闭合：审核工作台/租户+Onboarding/CSV/客户前端与驾驶舱页面待续。详见 08 §2.2 Q92 完成情况。

- **Q91 注册表驱动编排并行 DAG 原语落地（2026-09-15，14 §2.2“复杂 DAG 二期”原语部分提前销账；纯代码无迁移）** — 四接缝经负责人拍板追加 Q91（02 C1.35，“四项全按推荐（甲）”）：①**只落原语不接真实流**：新包 `app/core/orchestration/`（planner+engine），YAML 步骤可选 `needs`、无 needs 退化为顺序链（WF-01..04 零改动），Kahn 拓扑分层+五类加载期校验（未注册引用/未知依赖/环/重复/gate 锚点）；五站点显式端点与 Q84 不自动串链口径不变，DIM-SOURCE 七路 fan-out 随 V2 连接器声明 needs 接入；②**同层 fail-fast、层间屏障、引擎不重试**——兄弟任务取消、NodeExecutionError 带失败节点与已完成快照，重试归调用侧（Q90 游标）；③env 信号量 `LOOM_ORCH_MAX_CONCURRENCY=4`（运维参数非 Q9 旋钮），日预算全局硬停不绕；④**skill7 Gate 不跨、不建新表**——只规划第一道 gate 的 after 步骤+前驱闭包（WF-01 止于 CAT-RECOG、WF-04 仅 PWC-BUILDER），引擎不调模型/不投递/不写 skill_runs，处理器注入，orch_run_id(uuid)+wf_id 经 NodeContext 关联。backend **371 测试全绿**（+20 单测 test_orchestration_dag.py），ruff 通过；eval 不新增、仍 101/101。挂账：Redis Streams/细粒度认领、配置缓存多副本广播、fencing token、DIM-SOURCE 七路连接器（V2）；LangGraph 迁移口径不变。详见 08 §2.2 Q91 完成情况。

- **Q90 restock 瞬态失败重试退避落地（2026-09-15，Q87/Q89 V2 挂账“瞬态失败重试退避”提前销账；迁移 0020）** — 四接缝经负责人拍板追加 Q90（02 C1.34，“四项全按推荐（甲）”）：①**重试状态=新表 upsert 游标** `restock_retry_state`（request_id PK/attempts/next_attempt_at 索引/last_reason/updated_at；迁移 0020_restock_retry_state，PG16 up/downgrade-1/up 实测）——Q87 requested 信号行 append-only 纪律与“瞬态不留子 run”一字不改，信号成功或转终态删游标，游标是执行态非业务日志；②**指数退避 base×2^(attempts−1) 封顶 30min**（60/120/…/1800s），定时轮过滤窗口内信号；**BudgetExhausted 永不转终态**（UTC 日界预算必然恢复，转终态反永久漏补），**GenerationUpstreamError 连续 10 次（约 17h）转终态**——failed/llm_auto 子 run + restock_failed(terminal, attempts=10) 转人工、反连接排除；③**env 运维参数非 Q9 旋钮**：`LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`_MAX_SECONDS=1800`/`_MAX_ATTEMPTS=10`（与 Q87 interval/batch、Q89 锁开关同档）；④**手工 /run 绕过退避窗口**（honor_backoff=False，platform_admin“立即试一次”），瞬态结果仍 attempts+1 重排窗口、成功清游标。backend **351 测试全绿**（Q87 预算用例更新 +6：退避序列封顶、窗口到期重认领、预算 11 连试不转终态、上游错第 10 次转终态/删游标、成功清游标、终态清游标），ruff 通过；eval 不新增、仍 101/101。挂账：Redis Streams/每信号行细粒度认领、配置中心多副本缓存广播、fencing token、编排并行/DAG（均 V2）。详见 08 §2.2 Q90 完成情况。

- **Q89 多副本单实例锁落地（2026-09-15，Q87 挂账“多副本单实例调度+补货锁”提前销账；env 默认关，不改 V1 单副本形态；无迁移）** — 四接缝经负责人拍板追加 Q89（02 C1.33，“四项全按推荐（甲）”）：①**机制=Redis SET NX PX 分布式锁**——新横切包 `app/core/locking/`（service.py 单模块），uuid4 owner token，释放/续约走 Lua 比对 token（不删别人的锁），持锁期间看门狗每 TTL/3=20s 续约、进程崩溃 TTL 60s 自动放锁；②**粒度=循环级两把命名锁** `loom:lock:sla-sweep`/`loom:lock:restock-worker`，每轮 tick 仅持锁副本执行、抢不到跳过；不做每信号行细粒度认领（Q87 requested 行永不 mutate 一字不改，水平并行消费留 V2）；③**`LOOM_DISTRIBUTED_LOCK_ENABLED` 默认 false + fail-closed**——单副本/本地零 Redis 依赖行为不变，开启后 Redis 故障跳过本轮+error 日志，绝不无锁双跑（restock 双跑会在并发窗口重复花真 token）；④两个手工触发端点 `POST /api/admin/sla/run`、`POST /api/admin/restock/run` 抢同一把锁，抢不到 **409**（already running，RBAC 403 判定在前）、Redis 故障 **503**。实现：两循环各抽 `_tick()` 包锁；看门狗发现锁易主/续约失败仅记 error 不打断进行中一轮（fencing token 强制退场挂账 V2）。backend **345 测试全绿**（+13 集成，内存 FakeRedis 实现 set/eval 两原语：门控关闭放行/NX 互斥与重入/异 token 不删/看门狗续约/锁丢失静默/获取故障 fail-closed；两循环 tick 锁空·被占·Redis 宕三态；两端点 200→409 与 503），ruff 通过；eval 不新增、仍 101/101。挂账：瞬态失败重试退避、Redis Streams/每信号行细粒度认领、配置中心多副本缓存广播、fencing token、编排并行/DAG（均 V2）。详见 08 §2.2 Q89 完成情况。

- **Q88 入站“一 Agent 一 Key”凭证半套落地（2026-09-15，Q60b effect-callback 鉴权前置；段13 回调本体仍 V2/P3）** — 四接缝经负责人拍板追加 Q88（02 C1.32，“四项全按推荐（甲）”）：①**范围=只做入站 Key 半套**——新表 + platform_admin 治理端点（签发/列表/吊销）+ Bearer 验签函数/依赖 + 审计；`POST /api/effect-callback`、effect_records 时序（content_id+captured_at 幂等、缺席指标记“—”不当 0）、孤儿队列人工认领、Q60a content_id↔platform_post_id 运营回填、customer-backfill 全部随段13/P3（V2），本切片不提前落；V1 无受 Key 保护的业务消费端点是显式记录的已知挂账（同 Q82 注册表/配置中心“底座先行”），不建占位回调端点；②**SHA-256 单向哈希存储**——明文 Key（`loom_`+secrets.token_urlsafe(32)）仅在签发响应中返回一次，后台不可再读，DB 存 sha256 hex（unique + 索引）与明文前 12 字符展示前缀；与 outbound 的 Fernet 可逆加密刻意区分（出站须还原明文给供应商、入站只需比对，库泄露不暴露可用 Key）；吊销=active/revoked 状态位 + revoked_by/revoked_at，历史行 append-only 不物理删除；③**Key 不绑定租户**——平台级凭证由 platform_admin 签发，未来回调租户归属由 content_id 解析 FCW.tenant_id（对不上进孤儿队列）；“Key 是否限单租户”原文未给口径【待补】，不臆造 tenant 列；④**新横切包 `app/core/api_keys/`**（models/service/schemas/router，与 model_registry outbound 域并列不合并），治理路径 `POST/GET /api/admin/agent-keys`、`POST /api/admin/agent-keys/{key_id}/revoke`（越权 403、空名 422、未知 Key 404；列表默认仅 active 且永不含 secret）；`parse_bearer` 严格解析、`verify_key` 对未知/空串/已吊销统一返回 None（不区分原因，防凭证探测侧信道）、命中盖 last_used_at（调用方提交），FastAPI 依赖 `require_agent_key` 失败 401 `invalid or revoked agent API key` 留待段13 端点复用；writeAudit `agent_api_key.issue`/`agent_api_key.revoke`（tenant_id=`_platform`）。交付：迁移 **0019_agent_api_keys**（down_revision=0018；pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触本地 atlas-pg；downgrade drop index + drop table）、`app/core/api_keys/` 四模块 + main.py 挂载。backend **332 测试全绿**（+17 集成：Key 形态与 20 枚唯一、Bearer 解析 6 形、只存哈希不存明文、签发 RBAC/空名、验签 active·未知·空·吊销、吊销 append-only 与审计动作集合、吊销 RBAC+404、列表默认隐去吊销、HTTP secret 一次性+列表掩码+二次签发不同、403×2/422、HTTP 吊销 403→200/404/列表旗标），ruff 通过；eval 不新增、仍 101/101（无确定性纯业务函数，16 §4）。挂账：effect-callback 端点与段13 回流整切片（P3/V2，鉴权半套已就绪）、Key 单租户绑定口径【原文未给出，待补】、编排并行/DAG V2、多副本广播/单实例调度+补货锁（含瞬态重试退避）、M12 平台厚度、DIM-SOURCE 第七路/source_route L4/成本币种枚举【待补】、真 embedding 模型维度演进、两个基线 HTML 缺块。详见 08 §2.2 Q88 完成情况。

- **Q87 M8 restock_auto worker 落地（2026-09-15，Q71 补货闭环；无迁移纯代码切片）** — 四接缝经负责人拍板追加 Q87（02 C1.31，均选推荐项）：①**形态=进程内第二个 asyncio 循环** `RestockWorker`（与 SweepScheduler 同构同生命周期，task 名 loom-restock-worker，V1 单进程；docs/07 §7.5 队列选型就此裁决 V1=进程内轮询，Redis Streams/多副本锁后置 V2）；`LOOM_RESTOCK_WORKER_ENABLED` **默认 false、部署显式 opt-in**（自动花真 token）、`LOOM_RESTOCK_INTERVAL_SECONDS`=60、`LOOM_RESTOCK_BATCH_SIZE`=20（env 实现值，非 Q9 业务旋钮）；另提供管理端 `POST /api/admin/restock/run`（platform_admin，形态同 Q75 sla/run）；②**纯追加认领、append-only 不放松**——requested/restock_auto 信号行永不 mutate：成功追加 succeeded/llm_auto 子 SkillRun（`input.restock_request_id` 回链，PWC 候选挂子 run），终态失败追加 failed/llm_auto 子 SkillRun（Q71 预留的 failed 状态首次启用），认领=反连接已有子 run，单进程 tick 串行 + 最早优先；瞬态失败不留子 run、下轮重试（`?status=requested` 持续含已消费信号行，处理状态以 join 子 run 判断）；③**范围仅 PWC-BUILDER**（唯一 requested 写者），每轮 ≤20、一次信号一次调用（≤Q21 cap 50），只产 pending_review 候选、product_reviewer 人工 Gate 一字不改；新增内部入口 `invoke_pwc_build_for_restock`（system:llm-gateway roles=[]，OPERATIONS 角色闸只保留在 HTTP `/pwc/llm-build` 包装层），共享 `_run_pwc_build` 使 Q83 手动路径行为零变化；全量预闸/变量白名单/输出校验同 Q83；不自动串 ATOM-EXPAND（原子跨维 <2 判终态失败转人工 llm-expand）；④**失败/预算分类**——终态（PS 消失/池未 approved/PwcBuildNotReady/PwcBuildOutputInvalid 结构脏/ModelConfigError 缺路由·Prompt·Key·能力不符/停用无 fallback）追加 failed 子 run + audit `skill7.restock_failed(terminal=true)` 不重试；瞬态（日预算硬停：新增 `BudgetExhausted(ModelUnavailable)` 子类与"停用无 fallback"区分；上游 GenerationUpstreamError）只记 `skill7.restock_deferred(terminal=false)`、下轮重试（预算次日自然恢复；HTTP 侧仍由 ModelUnavailable→409 映射不变）；成功记 `skill7.restock_delivered`；不静默切供应商、fallback 仅注册表已注册；V1 不设重试次数/退避（挂账 V2）；未预期异常 log+rollback 不拖垮整轮；每信号独立会话/独立提交（SLA runner 同款隔离）。交付：新包 `app/core/restock/`（worker.py=`run_restock` 一轮函数 + `RestockWorker`，router.py 管理端点）、pwc_build 拆 HTTP 包装/内部入口/共享实现、gateway 新增 BudgetExhausted、config 三项 env、main lifespan 启停；**无 Alembic 迁移（failed 状态 Q71 已预留、回链搭乘既有 input JSON，头仍 0018）**。backend **315 测试全绿**（+9 HTTP/集成：成功交付且信号行原样不动+子 run 回链+候选全 pending_review+第二轮反连接 claimed=0、非 restock_auto/他 skill 信号忽略、not-ready/池未批/停用无 fallback/脏输出四类终态 failed 子 run+终态审计、预算硬停瞬态无子 run+deferred 审计+下轮仍可认领、admin 端点 customer/ops 403、platform_admin 200 触发、worker start/stop 生命周期），ruff 通过；eval 不新增、仍 101/101（worker 为调度/装配逻辑无确定性纯业务函数，按 16 §4 由集成测试覆盖）。挂账：入站一 Agent 一 Key + Q60 效果回调（建议下一棒）、编排并行/DAG V2、多副本 Redis 广播/单实例调度+补货锁（含瞬态重试退避）、M12 平台厚度、DIM-SOURCE 第七路/source_route L4/成本币种枚举【原文未给出，待补】、真 embedding 模型维度演进、两个基线 HTML 缺块。详见 08 §2.2 Q87 完成情况。

- **Q86 WF-03 原子批量补池真 LLM 第五站/末站落地（2026-09-15，Q82–Q85 模式复用；五站全通，Q67 站点 backlog 闭合）** — 四接缝经负责人拍板追加 Q86（02 C1.30，均选推荐项）：①仅显式业务端点、operations 角色、花 token 前全量预闸——`POST /api/product-spaces/{id}/atom-batches/llm-expand`（触发体 `{batch_size?, actor}`，缺省 Q14 敏感 20/非敏感 50），PS 404/字段池 404（与手动批次同口径）/gate≠approved 409/已通过+冻结正式原子 ≥ target_atom_max 409 TargetReached（Q15 停拓仅约束 AI 批次，手动追加端点不变），不自动串链（restock_auto 留 M8 worker）；②**两次模型调用、一个 atom_batch 候选**——CONFLICT-PRECHECK chat（WF-03 唯一 `candidate_target: atom_batch, single_candidate: true` 步骤，Prompt 挂此 skill）产整批，ATOM-AFFINITY 为首个 capability=embedding 场景（不挂 Prompt、不产候选，网关新 `embed(scene, texts)` 通道、路由能力不符 422）向量化 item 文本与同维 approved/frozen 有向量基；只落 1 条 SkillRun（model_id=chat，两次 input tokens/cost 求和、output 仅 chat，embedding 模型身份记 run.input 可审计）；整批复用 Q80 apply_atom_batch→submit_batch（强制 source=ai，Q14/Q15/批内 normalized 去重/维度必选/line 11189 全局事实唯一/Q17 词表双轨/line 840/Q18 缺证降级一项不绕，有证 pending_review/无证 pending_evidence），1536 维向量以机读 `_embeddings` 键随候选 payload 带外传递（skill7 校验与适配器双侧 pop，不进 BatchSubmitRequest/run.output），approve 复制向量到 product_atom_instances（Q86 前历史 NULL 行不 backfill、不入基），逐条 approveAtomGuard/Q70/Q18/Q19/Q20 Gate 原样；③**Q16 相似度本体 V1 口径**——迁移 **0018 为首个带 schema 变更的 LLM 切片**：PG `CREATE EXTENSION vector`、`ai_models.capability`(chat|embedding)、atom_candidates/product_atom_instances 两列 nullable `vector(1536)`（SQLite 测试端 float32 LargeBinary，portable TypeDecorator 双方言）；V1 相似度=进程内纯 Python 余弦，无 KNN 索引/`<=>` 下推（千万级切换按 14 §2.3 后置），affinity=同维有向量 approved/frozen 原子 max（6 位小数；无基→None 不伪造 0），cluster_id=批内同维并查集（余弦 ≥ 成簇线才连边，仅 ≥2 成员成分发 uuid4，跨维不成簇）；**成簇线 `atom.cluster_line=0.9` 系本次拍板借 Q10 维度同义线（原文未给【待补】，进配置中心可后台调）**，Q16 low_affinity_line=0.5 仍只标不淘汰；④输出契约——白名单 `{batch_size 必须原样回传, items:[{content, dimension_id, ai_risk, evidence?, fact_type?}]}`，模型自报 affinity/cluster_id/score/status 一律剥离本地重算；**ai_risk 与前四站"模型分全剥"不同，属 PT-ATOM-EXP/Q17 契约内 AI 字段，枚举（critical/high/medium/low）校验通过即透传作兜底档，词表命中仍由 submit_batch 强制只升不降**；结构脏数据（非 JSON/回显漂移/items 空或超批/空白 content/批内 normalized 重复/维度非本池 selected/fact_type·ai_risk 越界）与 embedding 行数不符/维度≠1536 在投递前 502、不留 run/候选；业务违规（line 11189 冲突/事实重复）不 502，照由 submit_batch 既有 409/422 处理、适配器失败回滚留 pending_review；预算/失败沿用 Q82–Q85（日预算按模型全局硬停 409 含 chat 成功后 embedding 触顶、disabled 仅走已注册 fallback、非合成缺 active Key 422、上游坏 502、系统 Actor `system:llm-gateway` roles=[] Q66、source=llm_auto、成本四列照记）。交付迁移 **0018_atom_affinity_embedding**（pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触本地 atlas-pg；cluster_line 种子 on_conflict_do_nothing 兼容 0010 CONFIG_SEEDS 快照全新库；downgrade 删种子+丢两列+DROP EXTENSION）、`app/core/model_registry/atom_expand.py`（第十三模块）、gateway/drivers `embed` 通道（per-1M input-only 成本/行数维度校验，OpenAI 兼容打 /embeddings）、第二个合成模型 synthetic-embedding（blake2b 哈希字袋 1536 维 L2 单位向量，价 0）与 CONFLICT-PRECHECK 每维一对共享长前缀近似原子的结构夹具、seeds CONFLICT-PRECHECK Prompt v0.1（6 模板变量 + 2 合成机读键；ATOM-AFFINITY 无 Prompt）、atom_rules cosine_similarity/max_affinity/assign_clusters 纯函数、portable Vector TypeDecorator、atom router 新端点（201；403/404/409/422/502 齐）。backend 306 测试全绿（+4 相似度纯函数单测：余弦基本/钳制/零向量/无基 None/成簇线与跨维标签隔离；+3 网关单测：CONFLICT-PRECHECK 构造器形态与回显/批帽、embedding 确定性 1536 维与成对相似度；+10 HTTP 集成：向量落库与本地字段/approve 复制向量后第二批算亲和/Q86 前 NULL 向量原子 None 亲和/模型自报 affinity·cluster_id·score 剥离/ai_risk 透传与词表强制高档/RBAC 与四道预闸/八类结构脏 502 零 run 零候选/chat disabled fallback 与远程无 Key/embedding 预算 409·能力不符 422·缺 Key 422/向量行数不符 502），ruff 通过；eval 不新增、仍 101/101（synthetic chat/embedding 构造器均为测试夹具而非确定性业务函数，16 §4）。**M10 五个真 LLM 站点全部接通（Q82 CAT-RECOG/Q83 PWC-BUILDER/Q84 TYPE-MATCH/Q85 DIM-MERGE/Q86 CONFLICT-PRECHECK+ATOM-AFFINITY）**。挂账：restock_auto 自动触发（M8 worker）、入站一 Agent 一 Key（Q60 效果回调）、编排并行/DAG（V2）、多副本 Redis 广播/单实例调度+补货锁、DIM-SOURCE 第七路/source_route L4/成本币种枚举【原文未给出，待补】、真 embedding 模型接入时维度按模型实际演进（TypeDecorator 单点承载）。详见 08 §2.2 Q86 完成情况。

- **Q85 WF-02 DIM-MERGE 字段池方案真 LLM 第四站落地（2026-09-15，Q82–Q84 模式复用）** — 四接缝经负责人拍板追加 Q85（02 C1.29，均选推荐项）：①仅显式业务端点、operations 角色——`POST /api/product-spaces/{id}/field-pools/llm-plan`（触发体 `{target_atom_min?, target_atom_max?, actor}`，缺省 Q15 旋钮 15/30），不自动串链（链式/DAG 后置 V2）；前置闸=PS 存在 404、无池或现有池 gate=rejected（否则 GateNotAllowed 409，与 submit_plan 同口径）、target min≤max（409）；②场景键=DIM-MERGE（WF-02 唯一声明 `produces_candidates: true, candidate_target: field_plan, single_candidate: true` 的步骤；FIELDPOOL-PLAN 明确不产候选，DIM-SOURCE 七路外部连接器属 V2），整方案单候选（payload=去 actor 的 PlanSubmitRequest）原样复用 Q78 apply_field_plan→submit_plan，target_atom_min/max 必须原样回传（漂移 502）；<3 维/缺 product_attribute/敏感缺 risk_control 属业务违规，照落非合规 pending_gate 池（violations 保留），WF-02 Gate approve 被机械拦截 409；③Prompt 机读变量有界白名单 7 个（product_profile/sensitive_industry/industry_tag/enabled_routes/active_g2_fields/dim_range/target_atom_range + `_` 前缀机读变量），synthetic 驱动确定性造合规整方案（active G2 fid 复用维+资料键补维+敏感补 risk_control、无真实 ref 不伪造占位、confidence 固定 0.9 不模拟分布）；结构白名单：role ∈ planning 角色、source_route ∈ 启用路由、fid 必须 ∈ active G2（Q68 "-"/幻觉 fid 502）、批内字段名去重、非空且 ≤dim_max、未知键剥离；证据红线 line 14081 在生成边界强制执行（source_ref 空白 502，只许引用有界输入 g2:/product_profile:/industry_tag:）；**confidence 口径与 Q83/Q84 不同——它是 PT-FP-PLAN 契约内 AI 字段（Q9 细看线/Q12 Top8 依赖），0..1 数值校验后透传，缺失/越界 502**，similarity/related_fid/definition 可选透传（Q10 仅标记）；④预算/失败语义沿用前三站（日预算硬停 409 不静默切供应商、非 synthetic 缺 active Key 422、坏输出/回显漂移/结构脏 502、disabled 无注册 fallback 409），系统 Actor `system:llm-gateway` roles=[]（Q66）、source=llm_auto、成本四列照记。交付迁移 **0017_dim_merge_seed**（纯数据 3 行：ai_scene_routes DIM-MERGE→synthetic、skill_prompts/skill_prompt_versions v0.1 含 7 变量，无 schema 变更，downgrade 三表 DELETE，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/field_plan.py`（model_registry 第十二模块：角色/PS/池态/target 前置闸→有界变量→gateway.invoke→严格 JSON/回显/逐维结构与证据校验/confidence 透传/未知键剥离→deliver_generated_run 单候选，SkillRun.input 只存有界摘要）、schemas 增 FieldPlanInvokeRequest、seeds 增 DIM-MERGE Prompt v0.1、synthetic 增合规整方案夹具、fieldpool router 增 llm-plan 端点（201；403/404/409/422/502 齐）。backend 289 测试全绿（+2 纯函数单测：合规整方案 fid 复用/资料键补维/风险维/回显/固定 0.9/确定性、非敏感无 risk_control 且 10 活跃字段封顶 8 维；+11 HTTP 集成：happy path→pending_review field_plan→product_reviewer 裁决→落 pending_gate 合规池→Gate approve、敏感补 risk_control、target 12/20 回显、RBAC reviewer/客户 403、PS 404、现有非 rejected 池 409、rejected 池重新触发 201、disabled 409→注册 fallback 201→预算 0 路由 409、远程模型无 Key 422、坏驱动输出 502、target 漂移 502、脏方案十形 502（坏 role/禁用路由/幽灵 fid/"-"fid/confidence 1.5 与 "high"/空白 source_ref/重名/9 维/空批）、confidence 0.5 透传→裁决后 Q9 needs_detail=true、未知键剥离、2 维 below_min 非合规池 Gate approve 409），ruff 通过；eval 不新增、仍 101/101（synthetic 构造器是测试夹具而非确定性业务函数，16 §4）。挂账：余一站真 LLM（ATOM-EXPAND·ATOM-AFFINITY 含 Q16 pgvector 语义相似度本体）、DIM-SOURCE 第七路枚举【原文未给出，待补】、restock_auto 自动触发（M8 worker）、入站 Key（Q60）、编排并行/DAG（V2）、多副本广播/锁、source_route L4 枚举【原文未给出，待补】、成本币种【原文未给出，待补】。详见 08 §2.2 Q85 完成情况。

- **Q84 C7 Layer4 TYPE-MATCH 真 LLM 第三站落地（2026-09-15，Q82/Q83 模式复用）** — 四接缝经负责人拍板追加 Q84（02 C1.28，均选推荐项）：①仅显式业务端点、operations 角色——`POST /api/intakes/{id}/c7/llm-resolve`（触发体 `{category_id, required_fids, actor}`），不与 CAT-RECOG llm-invoke 自动串联（链式/DAG 后置 V2）；前置闸=intake 存在 404、状态 ai_recognizing 409（TYPE-MATCH 属 WF-01 识别内步骤，与 CAT-RECOG llm-invoke 同状态闸，机械补登非新业务规则）、类目存在 404 且 status=active（draft 409）、required_fids 过 c7.validate_fids（Q68，调模型前 422，非法输入不花 token）；②整结果单候选复用 Q81 形态——WF-01 TYPE-MATCH 已声明 `single_candidate: true, candidate_target: c7_layer4`，payload=去 actor 的 C7ResolveRequest `{category_id, required_fids, l4_proposals[]}`，经既有 apply_c7_layer4→modeling.resolve_c7 原样复用（L1/L2/L3 提案自然 no-op，仅 L4 创建 g2_field_candidates），模型必须原样回显 category_id 与 required_fids 集合（漂移 502），空 l4_proposals 合法（裁决后自然落 L3）；③Prompt 机读变量有界白名单 6 个（category/required_fids/own_template/sibling_summary/coverage_floor/g2_coverage：目标类目/必填 fid/本类目 approved L1 模板/approved 兄弟计数/L3 G2 active 覆盖清单+现状覆盖率+0.6 floor），synthetic 驱动只按覆盖率缺口确定性造"待补字段·{fid}"结构提案、不打置信分，模型回带 confidence/score/weight/fid/status/source_route 编排层一律剥离（仅 field_name+非空 definition 透传，G2FieldCandidate.confidence 落 NULL；source_route 枚举【原文未给出，待补】，v0.1 不接模型值），L4 转正仍走 Q13 dictionary_admin 人工 Gate；④预算/失败语义沿用 Q82/Q83（日预算硬停 409 不静默切供应商、非 synthetic 缺 active Key 422、坏输出/回显漂移/脏提案 502、disabled 无注册 fallback 409），系统 Actor `system:llm-gateway` roles=[]（Q66）、source=llm_auto、成本四列照记。交付迁移 **0016_type_match_seed**（纯数据 3 行：ai_scene_routes TYPE-MATCH→synthetic、skill_prompts/skill_prompt_versions v0.1，无 schema 变更，downgrade 三表 DELETE，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/c7_resolve.py`（model_registry 第十一模块：角色/intake 状态/类目/fid 前置闸→有界变量→gateway.invoke→严格 JSON/回显/脏提案校验/剥离→deliver_generated_run 单候选，SkillRun.input 只存有界摘要）、schemas 增 C7ResolveInvokeRequest、seeds 增 TYPE-MATCH Prompt v0.1、synthetic 增覆盖率缺口夹具、model_registry router 增 llm-resolve 端点（201；403/404×2/409×3/422×2/502 齐）。backend 276 测试全绿（+2 纯函数单测：缺口提案只含未覆盖 fid 且无 confidence/score/fid 且确定性、全覆盖/空输入空提案；+11 HTTP 集成：happy path 5 缺口→pending_review c7_layer4→operations 裁决→C7Run layer=4+五条 g2_field_candidates pending_gate、全覆盖空提案裁决后落 L3 零候选、reviewer/客户 403、intake/类目 404、draft intake/draft 类目 409、fid "-" 422、disabled 无 fallback 409→注册 backup 成功、预算 0 路由 409、远程模型无 Key 422、坏驱动输出/类目回显不符/required_fids 漂移 502、脏提案 502、score/route 剥离专测），ruff 通过；eval 不新增、仍 101/101（synthetic 构造器是测试夹具而非确定性业务函数，16 §4）。挂账：余两站点真 LLM（FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY 含 Q16 pgvector 语义相似度本体）、restock_auto 自动触发（M8 worker）、入站 Key（Q60）、编排并行/DAG（V2）、多副本广播/锁、source_route L4 枚举【原文未给出，待补】、成本币种【原文未给出，待补】。详见 08 §2.2 Q84 完成情况。

- **Q83 PWC-BUILDER 真 LLM 第二站落地（2026-09-14，Q82 模式复用）** — 四接缝经负责人拍板追加 Q83（02 C1.27，均选推荐项）：①仅显式业务端点、operations 角色——`POST /api/product-spaces/{id}/pwc/llm-build`，Q71 `restock_auto` 的 SkillRun(requested) 本切片不自动消费（自动触发随 M8 worker）；②一次 LLM 调用产整批、多条候选——WF-04 PWC-BUILDER 步骤未声明 `single_candidate`，每条组合各成一条 `target_type=pwc_combo` 候选（payload `{"combos":[{atom_ids,goals}]}`，既有 apply_pwc_combo 适配器循环 payload["combos"]），输出超 Q21 `pwc.funnel_batch_limit`=50 拒，per-candidate 漏斗不共享 run 级去重故同原子集重复由编排层机械去重（保序），Q22/Q23 重合仍由漏斗算；③Prompt 机读变量有界白名单（approved 原子清单/库容/ready 数/active contentGoals/目标平台），前置闸=PS 存在 404、字段池 gate=approved 409、approved 原子跨 ≥2 维 409（不足不调模型），synthetic 驱动只产结构配对（锚定首维、至多 5 条）不造分，模型回带 logic_score/fit_score/weight 编排层始终剥离，Q22b 子分缺失→score_incomplete→人工 Gate；④预算/失败语义原样沿用 Q82（日预算硬停 409 不静默切供应商、非 synthetic 缺 active Key 422、坏输出 502、disabled 无注册 fallback 409），系统 Actor `system:llm-gateway` roles=[]（Q66）、source=llm_auto、成本四列照记。交付迁移 **0015_pwc_builder_seed**（纯数据 3 行：ai_scene_routes PWC-BUILDER→synthetic、skill_prompts/skill_prompt_versions v0.1，无 schema 变更，downgrade 三表 DELETE，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/pwc_build.py`（model_registry 第十模块：角色/PS/池/原子维数前置闸→gateway.invoke→JSON 与逐组合校验/分剥离/保序去重→deliver_generated_run 多候选，SkillRun.input 只存有界摘要）、seeds 增 PWC-BUILDER Prompt v0.1、synthetic 增结构夹具、condition router 增 llm-build 端点（201；403/404/409/422/502 齐）。backend 263 测试全绿（+2 纯函数单测：结构夹具只配对不造分/封顶 5/单维与空输入空批；+8 HTTP 集成：客户 403、PS 404、池未 approved 409、单维原子 409 不调模型、预算 0 路由切换 409、远程模型无 Key 422、驱动坏输出 502、幽灵原子 502、模型回带分剥离专测、happy path 多候选→product_reviewer 逐条裁决→漏斗落 pending_gate/score_incomplete→PWC Gate 到 ready），ruff 通过；eval 不新增、仍 101/101（synthetic 构造器是测试夹具而非确定性业务函数，16 §4）。挂账：余三站点真 LLM（FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY 含 Q16 语义相似度本体 / C7 L4 新字段生成）、restock_auto 自动触发（M8 worker）、入站 Key（Q60）、编排并行/DAG（V2）、多副本广播/锁、成本币种【原文未给出，待补】。详见 08 §2.2 Q83 完成情况。

- **Q67/Q82 模型网关 + CAT-RECOG 真 LLM 试点落地（2026-09-14，Q67 首个真 LLM 切片）** — 四接缝经负责人拍板追加 Q82（02 C1.26，均选推荐项）：①范围=模型底座+CAT-RECOG 单站试点，其余四生成站点（PWC-BUILDER / FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY / C7 L4）后续切片复用；②进程内同步调用、复用 skill7 通道——业务端点同步调模型网关，系统机器 Actor（`system:llm-gateway`，roles=[]，Q66）经既有投递逻辑落 skill_runs/skill_candidates(pending_review)，operations 人工 Gate 一字不改，异步 worker 留 M8；③outbound Key DB 对称加密（Fernet，env `LOOM_MASTER_KEY` 主密钥仅 env 不入库不入仓，注册表页创建/轮换、旧 Key 自动吊销、明文密文不回显只回指纹；入站一 Agent 一 Key 随 Q60 效果回调）；④Prompt 模板 DB 版本化（skill_prompts/skill_prompt_versions，v0.1 起步、append-only、platform_admin+writeAudit；仓库只携迁移种子；不用 15 §1 的 runtime `prompt/*.md`）。机械补登：场景路由键=skill_id；日预算按模型全局硬停（用尽 409，不静默切供应商）；fallback V1 仅注册态+人工（disabled 才走，无自动故障转移）；成本 per-1M decimal(12,6)+currency_code（种子币种【原文未给出，待补】）；仓内无真凭证，synthetic 确定性替身驱动测试/本地。交付迁移 **0014_model_gateway**（ai_models/ai_model_keys/ai_scene_routes/skill_prompts/skill_prompt_versions 五表 + skill_runs 加 model_id/input_cost/output_cost/currency_code + synthetic 模型/CAT-RECOG 路由/CAT-RECOG v0.1 Prompt 种子，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/` 九模块（models/crypto/gateway/drivers/synthetic/extraction/schemas/seeds/router）、skill7 拆 `deliver_generated_run`（系统 Actor、source=llm_auto、无角色门；顺手修 SkillRun.source 原硬编码 delivery 的潜在缺陷）、8 治理端点（模型/Key/路由/Prompt，platform_admin 红线、场景路由 operations|platform_admin、全 writeAudit）+ `POST /api/intakes/{id}/c1-recognition/llm-invoke`（operations；403/404/409/422/502 齐：状态闸 ai_recognizing、信号权重/类目配置、预算硬停、缺 Key、驱动坏输出）。backend 253 测试全绿（+5 纯函数单测：Key 加解密/指纹/错钥、per-1M 成本 ROUND_HALF_UP、synthetic 确定性；+10 HTTP 集成：治理 RBAC/校验、Key 轮换不回显、Prompt 版本化、llm-invoke 全链路落 pending_review 后 operations 裁决照走 Q1 分支、disabled→fallback、预算 0 硬停、远程模型无 Key 422、坏 JSON 502、服务层配置错误），ruff 通过；eval 不新增、仍 101/101（无确定性纯业务函数，试点按 16 §4 由后端集成测试覆盖）。挂账：其余四站点真 LLM 切片、入站 Key（Q60）、M8 worker、编排并行/DAG（V2）、多副本广播/锁、成本币种【原文未给出，待补】。详见 08 §2.2 Q82 完成情况。

- **浏览器人工验收通道接入（2026-09-14，本地开发约定）** — chrome-devtools MCP 以 local scope 注册到 loom 项目（存于 `~/.claude.json`，不入仓库），挂载本机 `127.0.0.1:9222` 独立 profile Chrome（`~/.chrome-claude`）；用法先起 Chrome 再驱动，仅登验收环境、用完关窗。约定登记于 15 §4、16 §1。

- **WF-01 C7 Layer4 占位替换切片落地（2026-09-14，Q81，Q79-1 销账）** — M10 余量第四个 WF 替换切片，同一 WF 第二个候选产出步骤；四接缝经负责人拍板追加 Q81（02 C1.25）：①产出步骤=TYPE-MATCH、Gate 在其后（同 WF 两个产出者 CAT-RECOG→c1_recognition / TYPE-MATCH→c7_layer4，MISSING-INFO 仍非产出）；②候选=去 actor 的整 C7ResolveRequest（category_id/required_fids/l4_proposals[]）单候选 `target_type=c7_layer4`，新增 `adapters.apply_c7_layer4` 薄包装复用 `modeling.resolve_c7`——L1 缓存/L2 approved 兄弟/Q6 覆盖率 0.6 地板/Q68 fid:'-' 一项不绕，落 L1/2/3 提案不生效，落 L4 才写 g2_field_candidates(pending_gate) 仍走段3 Q13 dictionary_admin，applied_refs=run_id；③准入 Gate 角色 operations，与 Q13 转正两道人工 Gate 不合并（Q66）；④eval 只打 c7 确定性纯函数，Q68/错误分支留集成测试，真 LLM 新字段生成挂 Q67。intake 锚点集合扩 {c1_recognition,c7_layer4}，router 补 IntakeNotFound·CategoryNotFound 404 / IllegalFid·InvalidDecision 422，适配器失败回滚留 pending_review；顺手修 resolve_c7 L4 盲插违反 uq(source_layer,field_name) 的潜在缺陷，改同名全局候选复用（跨单复用有集成测试）。无新迁移（仍 0001–0013）；backend 238 测试全绿（+8 HTTP 集成、注册表双产出断言），ruff 通过；eval 新增 TYPE-MATCH 17 案（回归 14+Golden 3，三 target c7_coverage/c7_pick_sibling/c7_layer3_floor），合计 101/101（80+21）。挂账：Q67 真 LLM（五处生成侧通道均已通）、编排并行/DAG、M8 worker、多副本锁。详见 08 §2.2 C7 Layer4 完成情况。

- **WF-03 字段下原子占位替换切片落地（2026-09-14，Q80，M10 WF 替换切片 3/3）** — 四接缝经负责人拍板追加 Q80（02 C1.24）：①整批一条候选 `target_type=atom_batch`、`single_candidate: true`（payload=去 actor 的 BatchSubmitRequest 形态），`adapters.apply_atom_batch` 复用 `atom.submit_batch` 并强制 source=ai（防伪装 manual 绕过 Q15），Q14 20/50、Q15 达标停拓、同批去重、维度属本池、line 11189 事实原子唯一、Q17 词表双轨、line 840 三类冲突、空证据降级一项不绕，入库后 approveAtomGuard/Q70/Q18/Q19/Q20 全保留，applied_refs=batch_id；②Gate 角色 product_reviewer（06 line 172，投递仍 operations）；③CONFLICT-PRECHECK 为末步候选产出者，投递即四步全部完成后的成型批次；④PS 锚点、无新迁移。router 补 404/409/InvalidBatch 422，Q15 达标 409 时批次未入库候选留 pending_review。backend 230 测试全绿（+8 HTTP 集成+1 注册表），ruff 通过；eval 四 Skill 31 案（回归 24+Golden 7，7 target），合计 84/84。详见 08 §2.2 WF-03 完成情况。

- **WF-01 冷启动识别占位替换切片落地（2026-09-14，Q79；范围仅 C1，C7 Layer4 当时挂账）** — 四接缝经负责人拍板追加 Q79（02 C1.23）：CAT-RECOG 为产出者、识别整结果单候选 `target_type=c1_recognition`（去 actor 的 C1RecognitionRequest）；准入统一过一次 operations 人工 confirm，准入后 Q1 三分支机械逻辑一字不改；锚点：迁移 0013 为 skill_runs/skill_candidates 加 nullable intake_id（product_space_id 改 nullable），PS/intake 二选一并按 WF 归属校验。单候选约束改由 WF 步骤 `single_candidate` 声明驱动（registry 新增 producer_step_for，WF-02 同步去硬编码）；`adapters.apply_c1_recognition` 复用 `modeling.submit_recognition`，router 补 IntakeNotFound 404/RecognitionNotAllowed 409/ConfigError·InvalidDecision 422。交付 WF-01.yaml（六顺序 Skill+CAT-RECOG 后单个 skill7 Gate）与六 Skill YAML。backend 221 测试全绿（+7 HTTP+1 注册表），迁移 0013 PG16 up/downgrade-1/up 实测；eval CAT-RECOG 14 案（回归 11+Golden 3，weighted_conf/decide_branch 两 target），合计 53/53。详见 08 §2.2 WF-01 完成情况。

- **WF-02 字段池占位替换切片落地（2026-09-14，Q78，M10 余量首个 WF 替换切片）** — 接缝经负责人拍板追加 Q78（02 C1.22）：DIM-MERGE 为候选产出者，外部完成 7 路采集/合并后投递整方案单候选 `target_type=field_plan`（去 actor 的 PlanSubmitRequest：dimensions+target_atom_min/max），FIELDPOOL-PLAN/DIM-SOURCE 为普通步骤。投递校验从硬编码 pwc_combo 泛化为 registry `candidate_target_for` 按 WF 步骤分派（非产出步骤带候选/target 不符/非单条均 422）；`adapters.apply_field_plan` 复用 `fieldpool.submit_plan`（Q8 路由/PT-FP-PLAN/Q9/Q10/Q12/G2 候选不绕，落 pending_gate 再过既有 WF-02 Gate），router 补 404/409/422，适配器失败回滚留 pending_review。交付 WF-02.yaml（双 Gate 插槽）与三 Skill YAML。backend 213 测试全绿（+6 HTTP+1 注册表），ruff 通过，无迁移；eval DIM-MERGE 15 案（回归 12+Golden 3，target=evaluate_plan，覆盖 Q8/Q9/Q10/Q11/Q12/line 14081/fid），合计 39/39。详见 08 §2.2 WF-02 完成情况。

- **M10-Q 质量两件套轻量版落地（2026-09-14，Q77）——任务包闭合** — 14 §2.6 Evaluation Dataset+Golden Cases 首次实体化；三接缝经负责人拍板追加 Q77（02 C1.21）：首批只打 WF-04 有确定性替身的两 Skill（PWC-SCORING→Q22/Q22a/Q22b、COMBO-VALIDATE→Q48 match_words；PWC-BUILDER 外部生成不造案）；独立 `eval/runner.py`+YAML（不进 pytest，对应 16 §1 回归行；dict 子集断言+float 容差、失败退出码 1）；本次只落命令与文档、不建 CI（CI workflow 随 M12）。交付 Evaluation Dataset 19 案+Golden 5 案，每案 source.refs 溯 Q/line、词例全合成；staging 闸门命令入 17；backend 新增 3 条 runner 闸门单测，共 206 测试全绿、ruff 通过、无迁移。详见 08 §2.2 M10-Q 完成情况。

- **M10 切片 e（skill7 AI 候选通道·WF-04 试点）落地（2026-09-14，Q76）** — 05 §2.3 skill7 状态机（line 11314）与 06 §4 注册表/编排器首次落代码；五接缝经负责人拍板追加 Q76（02 C1.20）。范围=通道核心+WF-04 试点：`app/core/skill7/`（registry/models/schemas/service/router/adapters）+ WF-04.yaml 与三 Skill YAML（lru_cache 加载器、LOOM_RUNTIME_DIR 可覆盖、未注册引用即拒；06"11 字段"按 10 项实现不擅补）；无模型注册表/LLM 客户端/API Key（Q67 随真接 LLM 切片）。通用 skill_candidates：投递即 pending_review，confirmed/modified 经 target_type 适配器写既有流程（pwc_combo 适配器复用 pwc/funnel source=ai，预筛/Q48/Q22/限量 50/PWC Gate 全保留，applied_refs 回写），rejected→archived，适配器失败回滚留 pending_review，run 无更新/删除路径（PT-COMPLIANCE 不可 mutate）。Q71 补货：consume 致 ready<critical 按 5min 防抖落 skill_runs(requested,restock_auto) 只记 run 不造候选（M5/切片 b 挂账销账）。投递 operations、裁决 product_reviewer（Gate 插槽角色驱动）。迁移 0012（PG16 up/downgrade-1/up 实测），203 测试全绿。详见 08 §2.2 M10 完成情况（切片 e）。

- **M10 切片 d（RBAC 红线收口）落地（2026-09-14，Q75）** — 端点级角色映射缺口经负责人拍板追加 Q75（02 C1.19）：手工 SLA/sweep 触发归 platform_admin；G1 类目创建/模板更新归 dictionary_admin（Q69，CategoryCreate 请求体加 actor）；CCR run 归 internal_compliance（闸先于 PWS 存在性）；pws/evaluate 与 atom revive 经裁决有意开放。新建 `app/core/rbac`（6 角色常量+require_any_role+统一 403，闸在服务层，既有各模块 RoleNotAllowed 不合并）。无 schema 变更/无迁移；194 测试全绿（新增 6 HTTP 403 矩阵，15 条既有测试补正角色）。详见 08 §2.2 M10 完成情况（切片 d）。

- **M10 切片 c（缓存引导+常量迁移）落地（2026-09-14）** — 新增消费侧统一入口 `app/core/config_center/knobs.py`：`knob(key)` 读进程内 config_cache，未引导时回落 SEED_BY_KEY 种子拍板值，默认值单一事实源。M1–M8 全部硬编码数值常量迁为零参访问器（c1 0.6/0.1/72h、c7 0.6、fieldpool 0.85/0.9/3–8 维/15–30、atom 20·50/0.5/7 天、pwc 50/100·70·50/5min/评分权重/冷却 14 天、pws 3·1/7 天、ccr 48h、sla 24h、fcw 0.4·0.3·0.3 等）；状态字符串/原因码不动，显式传值仍可覆盖。lifespan 启动以独立会话 reload 缓存，失败仅告警回落种子、不阻断启动。无 schema 变更/无迁移；188 测试全绿。详见 08 §2.2 M10 完成情况（切片 c）。

- **M10 切片 b（通用 SLA 引擎+定时调度）落地（2026-09-14）** — `app/core/sla/` 五件：通用到期升级引擎（open ops_todo 过 due→escalated，审计按 todo_type 分流，M2 旧函数变薄包装）、四作业登记（待办升级/Q18 证据超时驳回/Q24 冷却回可用/Q51 未来生效词激活补扫）、每作业独立会话异常隔离、lifespan asyncio 调度循环（默认 300s，环境变量可控）、管理端点（POST /api/admin/sla/run、GET .../todos 派生 sla_state，yellow 仅法审有口径，其余【待补】）。Q51 未来生效词条闭环：词表 API 开放 effective_from/until（窗口倒置 422），新列 activated_at（迁移 0011，存量回填 now），到点置位复用 rescan_for_entry。183 测试全绿。详见 08 §2.2 M10 完成情况（切片 b）。

- **M10 切片 a（配置中心底座）落地（2026-09-14）** — 迁移 0010 两表（config_items：key PK/category/typed JSONB value/validation/source_ref/version；config_item_versions：uq(key,version)，种子 v1 用确定性 uuid5），PG16 up/downgrade-1/up 实测。54 项种子覆盖 02 §C2 全部纯标量旋钮（含后置阶段 Q56/Q57/Q61/Q63/Q65 与 line2119 agent 限额——配置页先行）；已有专用 CRUD 表的 C2 项不入种子，避免双事实源。发布语义：强类型 coerce+边界/枚举校验→version+1→版本留痕→writeAudit→after_commit 进程内 config_cache 原子切换；回滚=以新版本号重发历史值，历史永不改写。HTTP /api/admin/config 五端点仅 platform_admin，403/404/422 齐。176 测试全绿。详见 08 §2.2 M10 完成情况（切片 a）。

- **M8（段11 FCW 组装/final_id 发证闭环）落地（2026-09-14）——V1 主链 段1→6→10→11 全通** — 迁移 0009 两表（final_content_whitelists/fcw_assembly_tasks，PG16 实测）。PT-FCW-ASM 七 Guard 纯逻辑 fcw_rules.py：g1 PWS frozen、g2 block_required=false 且 cleaning_passed=true（无 CCR 报告默认 False 不伪装）、g3 PCP+三包 active/approved、g4 六路 product_space_id 一致（Q52）、g5 tenant 一致、g6 法审仅被触发时要求 approved（Q49）、g7 PWS is_active（Q53）；不短路，任一败不 mint final_id，409 回带逐项 guards。E1.1 publishFCW 唯一出口（line 11036），阻断尝试留 fcw.assembly_blocked 审计。Q54 评分 pwc×100×0.4+fit×0.3+三包 conf 均值×100×0.3，缺失 null+incomplete 仅排序。Q55 批量组装端点（count≤200，slot 长度须=count 且不重复，单项隔离逐条留痕）；骨架 PWC 从冻结快照按 goals 交集+score 降序选取【实现补】；Q51 rescan 扩展命中关联 draft FCW。165 测试全绿。未含：WF-09（V2）、draft 生命周期、段12、FCW 消费侧 API（契约【待补】）、异步 worker、Q54 段13 校准。详见 08 §2.2 M8 完成情况。

- **M11（段7/8 静态底表+段9 三包静态实例）落地（2026-09-14，实现序先于 M8）** — 迁移 0008 七表（publish_slots/goal_fit_weights/platform_rules/slot_type_defaults/pcp_templates/pcp_weight_tables/packages，PG16 实测，种子集中 seeds.py 供迁移与测试共用）。Q35 发布位 operations CRUD（无平台种子——line 1098-1221 基准 HTML 不在仓库，标【原文未给出，待补】不虚构）；Q34 fit 权重矩阵（ENGAGEMENT/CONVERSION 两种子，4 维 Σ=1 硬校验，fit_score 派生不落库，缺权重 incomplete 不凑分）；Q36 规则 4 层 selector（同级同条件异结论 409、overwrite 归档旧行、高优先级覆盖+同级从严+country 横切）；Q39 四模板种子（17 键 Σ=1.0 期初草稿），PCP 实例走 Q40 校验器、uq_pcp_ps_platform 落实平台不共享、PUT 为 Q42 人工直编；Q45 三包 V1 静态切片（payload 键 04 §2.17 定死，产品×平台×目的×包型 active 唯一，带 tenant_id/product_space_id 供 M8 Guard④⑤）。147 测试全绿。未含：Q37/Q41/Q42 动态信号、fit 学习、PLATFORM-ADAPTER Skill（均 V2）、跨层 R-X01~X05、3 个目的 fit 权重（待补）。详见 08 §2.2 M11 完成情况。

- **M7（段10 合规清洗）落地（2026-09-14）** — CCR 复用 M4 同源 Q48 词表（零复制），迁移 0007 增 layer 列承载 Q50 三层优先序（国家>平台>底座不可配置，同级 Q36 从严 ban>downgrade、critical>high）；POST /api/pws/{id}/ccr/run 仅 active frozen 可跑，分市场独立判，ban→blocked/block_required=true 一票否决、downgrade→downgrade_pending 只出建议、无命中→clean，报告 append-only（PT-COMPLIANCE-V2.0）；降级人工批归 internal_compliance（仅 downgrade_pending 可批，批后 cleaning_passed）；Q49 敏感领域（CP-LAW 六码种子小表）自动触发 law_reviews（pws_id 唯一幂等）+48h 法审待办，decision 端点录线下律师结论（approved 放行 Guard⑥/rejected 维持否决/已决再判 409）；GET .../ccr/gate 机械视图供段11 消费；Q51 词表保存生效同事务扫全部 active frozen 快照，命中幂等出 wordlist_rescan 待办，重冻仍须 BO-07 人工红线。迁移 0007（layer 列+3 表，PG16 up/down/up）；134 测试全绿。未含：WF-08（V1 纯机械）、SLA 引擎、Q51 对 draft FCW 扫描、未来生效词定时扫描、拍板值配置化（均 M10）。详见 08 §2.2 M7 完成情况。

- **M6（段6 PWS 冻结）后端切片落地（2026-09-14）** — pwsReadiness 5 门机械求值（approved 字段池 / ≥3 已批准原子 / ≥1 active PWC（approved+ready/used【实现补】） / 0 未解决 blocked 冲突 / 0 pending_gate 池【实现补】，回带计数明细）；Q28 `evaluate` 全绿只出 pws_ready OpsTodo（whitelist_owner、7 天 due、幂等、复用 M2 sweep 升级），无全自动冻结；BO-07 红线：冻结/重冻/急停仅 `whitelist_owner`（英文码【实现补】），非属主 403、不全绿 409 回带 5 门明细；Q31 版本 v1.0 起 vN.0 主版本递增（v9→v10）、`(product_space_id, version)` 唯一、同刻仅 1 is_active，frozen+is_active 表达当前版（active 不另设状态值），版本列表主版本号倒序；不可变快照：原子/PWC 物化为有序 `pws_snapshot_items`（payload JSON，PG 侧 JSONB），源头废弃旧版不动，superseded/revoked 只读；Q29 重冻三档（forced: atom_compliance_suspend/atom_deprecated/wordlist_hit/product_fact_change，suggested: asset_increment，none: unrelated_change→409；有 active 版时原因码缺失/未知 → 422），重冻即建新版、旧版 superseded+superseded_by、supersede/refreeze 双日志；Q30 dispositions 结构先行（superseded_version/draft_fcws_voided 空列表/unpublished_pending_recheck 仅 forced/true/published_marked_outdated=0；FCW 与内容实体随 M8/段12 回填，不溯及既往）；Q32 revoke 仅对 active frozen（否则 409），is_active=false+status=revoked+原因/人/时间留痕，立即断消费，作废后重冻新版仍过 5 门；Q33 sha256(排序 atom_ids|pwc_ids) 指纹仅在同租户其他 PS 范围比对返回 dup_hints，跨租户不比对、提示永不阻断。迁移 0006（pws_snapshots/pws_snapshot_items/pws_freeze_logs 3 表，PG16 up/down/up 通过）；118 测试全绿（5 纯逻辑 + 9 HTTP 集成新增）。未含：WF-05 Skill AI 通道（12 #15 占位，段6 纯机械门）、Q51 词表生效即扫触发强制重冻（随 M7/M10）、pws_ready 升级定时调度（sweep 随 M2 就绪，调度随 M10）、Q30 实体处置与 Q53 FCW 引用 active 版（随 M8/段12）、拍板值配置化（均 M10）。契约文档同步以"实现补登"落 08 §2.2（M6 完成情况）、05 §1.4（M6 端点）+§2.5（状态实现口径）、04 §2.11（3 物理表/active 双维/指纹）、10 §2.3（0006）、13 §1.6（重冻/急停实现口径）、15 §1（whitelist_center 包，118 测试）。

- **M5（段5 PWC 条件包）后端切片落地（2026-09-14）** — Q25 contentGoals 字典（`content_goals` 5 码种子 ENGAGEMENT/CONVERSION/EDUCATION/TRUST/RETENTION，dictionary_admin 维护颜色/配比上下限，软归档后不可选用）；Q27 每 PS 库容配置（默认 100、capacity=null 无上限、target_platforms/high_reuse_n）；WF-04 漏斗 `pwc/funnel`：预筛（本 PS 已 Gate 通过原子、同租户、组合跨 ≥2 维度【实现补】、同批重复/跨 PS 原子/停用 goal → 422，字段池非 approved → 409）→检测（复用 Q48 词表按行业/生效期子串匹配，ban→blocked 不评分，downgrade 落明细不阻断；Q26 手拼不豁免）→Q22/Q22a/Q22b 评分（(合理性×0.6+多样性×0.4)×品类乘数；logic/fit 各 0.5；多样性=1−与待用池最大重合，空池=1.0；乘数 V1 恒 1.0；AI 子分缺失 → score=null/"—"/人工 Gate 不凑分；score 仅排序；重合度=原子集合 Jaccard【实现补】）→单批限量 50；Q23 重合 ≥0.8 标疑重（低分 is_backup+dup_of，人工 Gate 可改判）；HumanGate（product_reviewer：approve→ready 受库容 409，reject→archived，blocked 不可批）；Q71 消费（score 降序 null 末位，同 平台+账号+发布位 去重 409，跨平台可复用，goals 交集过滤，返回 pool_ready_count/pool_health target100·low70·critical50/restock_hint）；Q24 per-platform 冷却（7 天 ≥3 次→14 天，挂 `pwc_platform_states` 不影响跨平台，sweep 服务函数就绪、调度随 M10），目标平台全用过→used（清单来源【原文未给出，待补】），high_reuse_n 高复用旗标；Q61 爆款 V1 仅 operations 手工标；全部写操作 writeAudit，取用流水带 tenant 隔离。迁移 0005（6 表+5 码种子，PG16 up/down/up 通过）；104 测试全绿（13 纯逻辑 + 17 HTTP 集成新增）。未含：WF-04 三 Skill AI 通道、冷却 sweep/自动补货（5 分钟防抖）定时调度、Q61 中位数×5 自动检测、Q23 语义向量、逻辑/搭配检测插件、target_platforms 来源、拍板值配置化（均 M10 或待补）。契约文档同步以"实现补登"落 08 §2.2（M5 完成情况）、05 §1.4（M5 端点）、04 §2.10（6 物理表/双状态/冷却口径）、10 §2.3（0005）、13 §1.7（gate×生命周期双维 + per-platform 冷却）、15 §1（condition 包，104 测试）。

- **M4（段4 字段下原子）后端切片落地（2026-09-13）** — Q48 统一合规词库横切包先行（`compliance_wordlist`：word/level/action/downgrade_target/country/industry/生效期，operations/internal_compliance 维护，软归档+审计，段4 已按行业/生效期消费，段5/10 同表待 M5/M7）；拓展批次仅产候选（PT-ATOM-EXP-V1.3，不直接写正式实例）：Q14 批次 20/50 提交时可覆盖、Q15 AI 达 target_atom_max 停拓（manual 放行）、同批 normalized 去重、维度须属本池 selected；Q17 risk 双轨（词表命中强制定级、人工改判对词表候选 409；AI 判级可复核并重建冲突集；medium/low 为实现补等级）；AtomConflict 三类（disabled_expression=critical+ban blocked+驳回合规审计 / high_risk_single_review / evidence_required）；approveAtomGuard 9 项机检 + ⑩审计先于正式化，Q70 high/critical 单条审、批量整批 409；Q18 pending_evidence + 7 天 due，补证据解冲突，sweep 系统自动驳回 evidence_timeout、仅超时驳回可复活（复活重检事实原子唯一性）；Q19 同义簇 keeper 人工终裁，merged 不删并入 atom.aliases；line 11189 事实原子五类 (fact_type, normalized) 全局唯一、跨产品/跨租户拦截、reference_count=1；Q20 atom8 生命周期（operations 冻结/解冻免重审/废弃/归档，internal_compliance 暂停/恢复复核，product_reviewer 驳回，废弃复活=新原子重审）。Actor 上移 `app/core`；迁移 0004（5 表，PG16 up/down/up 通过）；74 测试全绿（16 纯逻辑 + 13 集成新增）。未含：WF-03 四 Skill（ATOM-EXPAND/CANON/AFFINITY/CONFLICT-PRECHECK）AI 通道、sweep 定时调度、Q51 生效即扫、Q16 相似度本体、拍板值配置化（均 M10）。契约文档同步以"实现补登"落 05 §1.4（M4 端点）、04 §2.8/2.9/2.19（物理表/候选侧状态映射/词表）、10 §2.2/2.5（0004）、13 §1.3（atom8 候选/正式两段 + Q17/Q18/Q20 实现口径）、15 §1（atom 包 + core 词库/Actor，74 测试）。

- **新增 docs/18 前端 i18n 与设计 Token 方案（2026-09-13，🟡 工程建议）** — 界面 i18n 与内容多语言划界（Q58 内容侧不重定义；运营数据中文名 V1 canonical、V3 再扩 name_i18n）；建议 next-intl + `[locale]` 路由，V1 仅 zh-CN、文案全量走消息表、后端只返枚举码、UTC 存储/上海展示，V3 随路线图启用外语；设计 Token 建议 primitive→semantic→component 三层 CSS 变量（状态色对齐 docs/13 状态词）、V1 CSS Modules + tokens.css 不引 Tailwind、深色/租户白牌包留 V3；5 项选型列入待拍板（确认后追加 Q75 升 ✅）；已登记文档地图与 handoff。

- **契约层文档同步 M1–M3 实现（2026-09-13）** — 以"实现补登"形式（不改设计原文、不升级文档状态）把后端切片登记回契约层：05 §1.4 新增已落地 REST 端点清单（M1–M3 全量路由 + 404/409/403/422 错误口径）；04 §2.4/2.6/2.7 补物理表名与字段（M2 七表、候选唯一约束与转正回填、M3 三表与 violations 取值）；10 §2.1/2.2 补 Alembic 0001–0003 物理表与设计名映射；13 §1.12 新增字段池 Gate 流转（标注 Gate 非状态机）；15 §1 补实际包结构（product_intake/modeling/fieldpool）与规划名对应。

- **M3（段3 字段池规划）后端切片落地（2026-09-13）** — Q8 维度来源路由表后台 CRUD（6 路种子：用户输入/通用灵感库/产品专属灵感库/类目模板/G2 高频/合规风险面，被引用禁删，改动 writeAudit）；字段池方案提交（一品一池，每维度强制角色+启用路由+依据标注 line 14081 红线，fid 必须为 active G2 否则违规，新字段进 g2_field_candidates 同名全局复用）；PT-FP-PLAN-V2.0 校验（product_attribute 恒 ≥1；risk_control 仅敏感行业强制 Q11；>8 按 conf 留 Top8、余者入备选档可人工捞回 Q12；<3 不达标转人工不自动补）；conf<0.85 标"需细看"不自动通过（Q9，第一期全量过 Gate）、相似度 ≥0.9 仅机器标记人工终裁（Q10）、目标原子数 15-30（Q15）；WF-02 HumanGate（product_reviewer 角色，非合规方案 409 拒批，approved 锁定，rejected 可重提）；Q13 新字段转正端点（dictionary_admin 角色，fid 冲突/重复转正 409，转正后回填池维度，G2 cat 完整取值待基准 HTML，默认 extension 不污染 common）。迁移 0003（fp_source_routes/field_pools/fp_dimensions + 6 路种子，PG16 up/down/up 通过）；45 测试全绿。未含：WF-02 三 Skill AI 通道（M10）、灵感库两层实体、转正保护期加权（第二期）、拍板值配置中心迁移（M10）。

- **M2（段2 C1 识别）后端切片落地（2026-09-13）** — 信号权重配置表（启用行 Σ 必须精确等于 1 否则拒绝保存，文本三信号 0.50/0.33/0.17 种子，图像后置，Q2）；行业阈值后台 CRUD（general 默认档不可删 + writeAudit，medical 0.90/electronics 0.80/general 0.85 种子，Q7）；conf 单指标三分支（Q1/Q3：高置信 wf01_confirm+系统 auto_confirm 直送已提交；中置信出 ops_assist 运营待办、72h 未处理 sweep 升级、全否转 cold_start；低置信带 B2 候选单进类目创建中；Top1-Top2 差<0.1 矛盾规则）；C7 四层兜底（L1 模板缓存/L2 兄弟 product_count 最大继承/L3 G2 覆盖率 60% 降级线/L4 新字段进 g2_field_candidates，Q6；模板写入与 L4 提案两入口禁止 fid:'-'，Q68）。迁移 0002（8 表 + 权重/阈值种子，PG16 up/down/up 通过）；AuditLog 上移 core 横切包；26 测试全绿。未含：WF-01 六 Skill AI 通道、定时调度（M10）、G1 完整类目管理（V3）、拍板值配置中心迁移（M10）。

- **M1（段1 产品录入）后端切片落地（2026-09-13）** — productIntake15 状态机（15 态全流转；类目创建中态仅受 B2 三结果，Q5；类目确认 operations 角色闸，Q3；三终态拒事件）；录入 REST `/api/intakes`（创建/查询/allowed-events/profile 补资料/事件迁移/ProductSpace 查询），提交时按 `g2_fields` active 的 cat='common' 行动态校验缺字段阻断（D7.1 闸门1，名单不硬编码），非法迁移 409/越权 403/缺字段 422/审核后资料不可改 409；writeAudit append-only 审计；Alembic 0001（g2_fields/product_intake_applications/product_spaces/audit_logs，PostgreSQL 16 实测 up/down/up 通过）；11 条单元 + 集成测试全绿。配套 Q74 双落库裁决（见下）。未含：WF-01 6 Skill 候选通道（随 M10）、72h SLA（M10）、18 字段真实 fid 种子（待基准 HTML 找回）。

- **决策 Q74（2026-09-13）** — 18 个 G2 cat='common' 字段值两处落库、职责不同：申请单 `profile`（fid 键控，提交后不可变）+ ProductSpace `profile_snapshot`（start_modeling 时整体复制的不可变快照，同 PWS 快照哲学 Q31）；02 新增 C1.18/Q74，04 §2.2、10 §2.1 同步；M0 挂账子项"18 通用字段值归属"销账。

- **建立 docs 唯一事实源体系（2026-09-13）** — 原唯一事实源《Loom_核心业务主链梳理_v3.md》经段落级核验（231 段、业务信息零丢失）后归档删除，内容 100% 迁入 `docs/`（01–09 为 v3 忠实切分，10–17 为工程骨架文档）；配套文档地图 README 承载治理规则与信息保全映射表。详见 [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md)。

- **根目录项目文档** — README（项目介绍与 13 段链一图流）/ handoff（当前状态、待办与文档清单单一事实源）/ AGENTS + CLAUDE（AI 工作规范）/ git-commit-message（提交规范）/ CONTRIBUTING（贡献指南）/ MIGRATION_CONVENTION（迁移规范）/ BENCHMARK（基准登记表，代码启动后启用）。

- **仓库基建** — git init（`main` + `dev` 工作分支），推送至 GitHub private 仓库（bayernjf/loom）。

- **代码脚手架落地（2026-09-13）** — 按 15/17 定稿建最小骨架：backend（Python 3.12 + FastAPI 模块化单体，13 模块包 + core 9 横切包 + Alembic + tests 四层 + `.env.example`）、runtime/eval/middleplatform 数据驱动目录、frontend（Next.js + TS 最小页）、infra docker-compose（pgvector/pg16、redis:7、MinIO）；MIGRATION_CONVENTION 补 Alembic 现行口径。仅骨架，业务代码自 V1 M1 起写。

- **路线图口径合并裁决 Q73（2026-09-13）** — A7 三阶段与 D9 V1/V2/V3 两套并存路线图由负责人裁决合并：**A7 为骨 + D9 厚度**；V1（0–3 月/5–10 客户）主链到段 11 `final_id` 发证闭环，**段 12 内容生成后置 V2**，段 7/8 V1 仅交付 FCW 必需的静态底表基础版，驾驶舱收敛为 Token 成本 + 人工审核 2 个，Evaluation/Golden 两件套维持 V1；08 §1.3 改为裁决记录 + 合并后权威路线图表，§2 任务包重排（V1=M1–M8+M10/M10-Q+新增 M11/M12；V2=P1–P5）；02 新增 C1.17/Q73；01/03/06/09/12/16/README/handoff/AGENTS 同步；原文挂起待裁决事项清零。

- **技术选型 6 项定稿（2026-09-13）** — 模块化单体；自研注册表驱动编排器最小集（YAML 声明式，留 LangGraph 逃生口）；PostgreSQL 16 + pgvector + Redis 7（Streams）+ S3 兼容对象存储；DB 配置中心 + 变更广播热更新；进程内独立合规模块；MVP 仅建 Evaluation Dataset + Golden Cases。技术栈 = Python 3.12 + FastAPI / React + Next.js + TS。14 升 ✅ 定稿，15/17/08/10/06 已同步。

- **M0 规格仲裁完成（2026-09-13）** — Q2/Q22/Q60(a/b/c) 临时采纳转定稿；新增 Q22a/Q22b（PWC 分项评分：合规前置硬条件、合理性加权、多样性=1−最大重合占比、AI 失败转人工不凑分、score 仅排序不做门槛），同步 01 段5/06 §1.1/02 §C2；业务方 3 份素材采纳经负责人确认（02 C1.16）；段 1-6 缺口实体 ProductSpace/g2FieldCandidates 补字段（04/10）；03 S1–S4 销账注记。

## 待办（未入本日志）

- V1 任务包排期（工期/人员/优先级待负责人提供资源信息）、两个基准 HTML 找回（阻塞 12 占位名单核对与 line 溯源）、10 建表要素 DBA 复核——见 [handoff.md](handoff.md) 待办区。
