# Loom · 契约层 · 数据模型（实体与字段）

> **本文档来源**：`../Loom_核心业务主链梳理_v3.md` **Part A / Part C / Part D 结构化提取**（非原文直排，是按实体整理的重组视图）。
> **文档定位**：AI 落代码的硬前提之一。列出全系统实体、原文已明确给出的字段及其来源；**原文未给出字段定义处一律标注【待补：原文未给出】，不猜测**。
> **字段完整度说明**：v3 对部分核心实体（PWC、FCW、catFeedback、KUP）给出了完整字段，对多数实体只给了名称与语义。字段级补全是开发前置项（见 01 §7 第 0 步①）。
> **配套文档**：状态机与 API 见 05；WF/Skill 协议见 06。

---

## 1. 实体总览（按 13 段链 + 横切层）

| # | 实体 | 所属段/层 | 原文字段完整度 | 关键来源 |
|---|---|---|---|---|
| 1 | ProductIntakeApplication（录入申请单） | 段1 | 部分（状态机明确） | line 1454 / Q5 |
| 2 | ProductSpace（PS，一品一空间） | 段1 | 名称级 | line 1762 等 |
| 3 | c1Records（C1 识别记录） | 段2 | 部分（signals 明确） | line 1834 / Q2 |
| 4 | G1 类目树（含 template） | 段2 | 部分（状态机 + field_list 明确） | line 708 / Q6 / Q68 |
| 5 | g2Fields（G2 通用字段池） | 段2/3 | 部分（syn[]、usage、cat 明确） | line 682-698 / Q68 |
| 6 | g2FieldCandidates（字段候选） | 段3 | 名称级（dup 语义明确） | Q13 |
| 7 | FieldPool（字段池） | 段3 | 部分（gate、目标原子数明确） | Q15 |
| 8 | ProductAtomInstance（原子实例） | 段4 | 部分（状态机 + Guard 明确） | line 2013 / Q17-Q20 |
| 9 | AtomConflict（原子冲突） | 段4 | 完整（类型/状态） | line 840 |
| 10 | conditionPackages（PWC/条件包） | 段5 | **完整** | line 1780 |
| 11 | PWS（产品白名单冻结快照） | 段6 | 部分（版本/状态明确） | Q28-Q33 |
| 12 | publishSlots（发布位） | 段7 | 部分（格式约束/四维分明确） | line 1098-1221 / Q35 |
| 13 | platformRules（平台规则） | 段7 | 部分（selector/effect 明确） | line 1383 / Q36 |
| 14 | slotRuleByType（slotType 默认值） | 段7 | 部分（约 40 列） | line 1428 |
| 15 | strategyFields17（PCP 权重池） | 段8 | 完整（17 字段名） | line 1294 / Q39-Q43 |
| 16 | layerSpaces（通用底座 4 层） | 段9/10 | 完整（维度名） | line 1261 / 段10 |
| 17 | packages（CSP/CSTP/CEP） | 段9 | 完整（字段名） | line 863 / Q45 |
| 18 | countries（国家事实库） | 段10 | 名称级 | line 1417 / Q50 |
| 19 | 合规词库（Q48 合并后） | 段4/5/10 | **完整（属性明确）** | Q48 |
| 20 | CP-LAW 敏感领域清单 | 段10 | 完整（领域非词） | Q48 |
| 21 | FCW（final_content_whitelist） | 段11 | **完整** | line 870 / Q52-Q55 |
| 22 | 内容成品（文章/视频/多语言） | 段12 | 名称级 | Q58 / line 5196 |
| 23 | catFeedback（类目效果回流） | 段13 | **完整** | line 746 / Q60 |
| 24 | KUP 提案 | 段13 | **完整** | line 1006 / Q63-Q64 |
| 25 | SkillRunLog（Skill 运行日志） | 横切 | 名称级（不可 mutate） | line 2029 / D3.10 |
| 26 | 审核任务/待办（SLA 引擎） | 横切 | 部分（时限/升级明确） | Q49 / Q70 |
| 27 | writeAudit（审计日志） | 横切 | 名称级 | 多处 |
| 28 | 统一模型注册表 | 横切 | **完整（字段明确）** | Q67 |
| 29 | 场景→默认模型路由 | 横切 | 完整（字段 Q82 补齐，场景键=skill_id；底座+CAT-RECOG 已实现，Q83 增 PWC-BUILDER、Q84 增 TYPE-MATCH、Q85 增 DIM-MERGE、Q86 增 CONFLICT-PRECHECK+ATOM-AFFINITY 种子路由；后者 capability=embedding 不挂 Prompt） | Q67 / Q82 / Q83 / Q84 / Q85 / Q86 |
| 30 | Agent 注册表 | 横切 | 部分（3 个 Agent + 边界） | A4 / line 2115 |
| 31 | 配置中心（系统配置） | 横切 | 见 02 §C2 全表 | C2 |
| 32 | API Key | 横切 | outbound 已实现（字段 Q82 补齐：密文/指纹/轮换；入站一 Agent 一 Key 可吊销待后续切片） | Q60b / Q67 / Q82 |
| 33 | 字典管理（目的/动作/选项/底座） | 段8/9 | 部分（Q25/Q38/Q43/Q46） | Q25/Q38/Q43/Q46 |
| 34 | A1 内容目的库 / A2 动作库 / A3 格式库 | 展示口径 | 部分（数量明确） | 09 §3.2 |
| 35 | Memory 6 层（M1–M6） | 展示口径 | 名称级 | 09 §3.13 |

---

## 2. 核心实体字段明细（原文已明确者）

### 2.1 ProductIntakeApplication（段1）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| intakes | 录入申请集合（实体容器名） | line 1454 |
| 输入字段集 | 产品名/品牌/简介/卖点/SEO/主图/包装图/目标市场/语言/渠道/受众/内容用途（G2 `cat:'common'` 18 个通用字段） | line 682-698 |
| 状态 | productIntake15 状态机（15 态，见 05 文档 §2.1） | line 1454 / Q5 |
| → ProductSpace | 通过后生成 PS（一品一空间） | A3 段1 |
| 类目确认动作 | 由运营执行（Q3 变更） | Q3 |

### 2.2 ProductSpace（段1，PS）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| product_space_id | PK，一品一空间 | A3 段1 / A6 |
| tenant_id | 必填；跨租户同款产品各开独立空间、互不感知 | Q33 |
| intake_id | 来源录入申请单（申请单"已通过"后生成 PS） | 01 段1 |
| lifecycle | 产品生命周期判定优先级 `frozen > stale > cold > modeling > active`（原文为判定优先级，状态机迁移触发条件【原文未给出，待补】） | line 1655 |
| category_node_id | C1 识别落定的 G1 叶子类目；冷启动时关联 B2 新类目候选单 | 01 段2 / Q5 |
| industry_tag | 行业标签，由类目路径映射；运营可在产品详情手动修改 | Q7 |
| sensitive_industry | 是否敏感行业（决定 risk_control 强制、阈值档 0.90 等） | Q7 / Q11 |
| active_pws_id | 当前 active PWS 版本引用（同刻仅 1 个 active） | Q31 |
| business_owner | 业务方（=产品记录字段，非登录角色） | 09 D8 |
| 私有资产 | 字段池 + 原子 + PWC + PWS（产品私域下级，禁止跨产品/跨客户复用） | line 14813 / Q24 |
| 产品资料字段 | 18 通用字段值（产品名/品牌/简介/卖点等）**两处落**（Q74，2026-09-13 定稿）：申请单存提交原始值（`profile`，按 G2 fid 键控，不可变）；生成 PS 时整体复制不可变快照 `profile_snapshot`，下游只读消费快照。fid 由 Q115 工程定稿（12 字段英文 slug + 中文 canonical，迁移 0024），余 6 字段【原文未给出，待补】 | line 682-698 / Q74 / Q115 |
| 审计 | 创建/变更走 writeAudit（横切） | A4 |

### 2.3 c1Records（段2，C1 识别）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| signals | 五信号加权：name≈0.33 / brief≈0.22 / 卖点≈0.12 / 主图≈0.13 / 包装图≈0.06-0.10（Q2 后改为配置表，Σ=1 强校验） | line 1834 / Q2 |
| conf | Σ 加权置信（唯一判定指标，Q1 废弃 hit_rate） | Q1 |
| 三分支结果 | direct_approve / ops_assist / cold_start（Q3 更名） | Q1/Q3 |
| 行业阈值映射 | c1_thresholds：medical 0.90 / electronics 0.80 / general 0.85（Q7 后为 CRUD 表） | line 1834 / Q7 |
| 矛盾判定 | Top1-Top2 差 < 0.1 视为信号矛盾 | Q3 |

### 2.4 G1 类目树（段2）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 状态机 | active/draft/review/deprecated/archived/merged | A3 段2 |
| merged_into | merged 状态永久重定向 | A3 段2 |
| template.field_list | 每叶子挂模板，字段引用 G2 fid（**禁止 fid:'-'**） | line 708 / Q68 |
| 跨产品共享 | 类目树跨产品共享（第二个同类产品 Layer1 缓存 0 token） | line 595 / Q24 |

> **实现补登（2026-09-13，M2 后端切片）**：实际落 7 表——`c1_signal_weights`（Q2 配置表）、`c1_industry_thresholds`（Q7 CRUD，is_default 不可删）、`c1_records`（含 branch/top_candidates/top_gap/selected_category_id）、`ops_todos`（Q3/Q4 待办，assignee_role=operations，due 72h，sweep 置 escalated；M10 并入通用 SLA 引擎）、`g1_categories` + `g1_category_templates`（G1 树与模板拆两表；M2 仅最小切片，完整类目管理在 V3）、`c7_runs`（四层留痕）。文档其余处出现的 c1_thresholds / g1_category_tree 为设计名，物理表以上述为准。

### 2.5 g2Fields（G2 通用字段池，段2/3）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 字段集 | 43+ 字段（含 cat:'common' 18 个通用录入字段） | line 682-698 |
| syn[] | 同义词数组 | A3 段3 |
| usage | 使用计数 | A3 段3 |
| status | 含 deprecated（被历史 template 引用会悬空） | A6 |
| fid | 字段身份证号（**黑户 fid:'-' 禁止落库**） | Q68 |

> **实现补登（2026-09-17，Q115/迁移 0024 纯种子）**：g2_fields 首次落 cat='common' 种子 12 字段——fid 为工程定稿英文 slug（f_name/f_brand/f_intro/f_selling_points/f_seo/f_main_image/f_packaging_image/f_target_market/f_language/f_channel/f_audience/f_content_usage），field_name 为中文 canonical（docs/04:58 枚举），status=active。原文权威 fid 随 V6.0 HTML 永久丢失（Q115 改判工程定稿）；18 个 common 字段余 6 个【原文未给出，待补】不落。syn[]/usage 为逻辑口径（A3 段3），未落物理列。

### 2.6 g2FieldCandidates（段3，G2 通用字段候选）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| candidate_id | PK【建表命名建议】 | — |
| field_name / 定义 | 候选字段名与释义（Layer4 大模型新生成字段进此实体） | 01 段2 C7 / 段3 |
| source_layer | 产生来源：C7 Layer4 新生成 / WF-02 DIM-SOURCE 等（维度依据红线：须标来源，禁止凭空造字段） | line 14133/14081 |
| source_route | 来源路由（用户输入/通用灵感库/产品专属灵感库/类目模板/G2 高频/合规风险面） | Q8 |
| confidence | AI 产出置信度（<0.85  Gate 必审/细看线） | Q9 / PT-FP-PLAN |
| dup | 同义词去重标记，联动 g2Fields.syn[]；相似度 ≥0.9 标疑似重复，人工终裁 | 01 段3 / Q10 |
| related_fid | 疑似重复/同义指向的既有 g2Fields.fid | Q10 |
| status | 候选状态：pending_gate / approved / rejected（→转正不直接写 g2Fields，走人工 Gate）【枚举名建议，原文未给完整状态集，待补】 | Q13 / 10【建议】 |
| 转正审批 | 转正需"字典管理员"级权限（G2 为全局资产，高于单产品审核） | Q13 |
| 来源租户可见性 | 候选为全局字典资产还是租户隔离【原文未给出，待补，多租户设计时定】 | — |
| 审计 | 提出/审批留痕走 writeAudit（横切） | A4 |

> **实现补登（2026-09-13，M2/M3 后端切片）**：表 `g2_field_candidates`；实际 `source_layer` 已用两值——C7 Layer4 = `c7_layer4`（M2）、WF-02 维度 = `wf02_dim_source`（M3，`source_route` 取 Q8 路由）；唯一约束 `(source_layer, field_name)`——同层同名候选全局复用不重复建（G2 为全局字典资产，多租户可见性仍按上行挂账）。Q13 转正端点已落地：dictionary_admin 角色 + 分配正式 fid（冲突 409），转正后自动回填引用该候选的 fp_dimensions.fid（不允许池长期挂候选，Q68）。

### 2.7 FieldPool（字段池，段3）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| gate | approved / pending_gate | A3 段3 |
| 目标原子数 | 默认 15-30，达标停拓（Q15 新增） | Q15 |
| 维度数约束 | 3-8（越界处置见 Q12） | PT-FP-PLAN |
| 角色维度 | 必须 ≥1 个 product_attribute 角色维度 | PT-FP-PLAN-V2.0 |
| risk_control | 敏感行业强制 ≥1 个合规预筛候选 | Q11 |

> **实现补登（2026-09-13，M3 后端切片）**：落 3 表——①`field_pools`（一品一池唯一约束；`gate` 实为三态 pending_gate/approved/**rejected**，rejected 可重提、approved 锁定，rejected 非原文态是实现补规格；`compliant`+`violations` 快照结构校验结果；target_atom_min/max 默认 15/30）；②`fp_dimensions`（池内维度行：role/source_route/source_ref 依据必填/field_name/fid/candidate_id/confidence/needs_detail/dup/related_fid/status=selected|backup/sort_order）；③`fp_source_routes`（Q8 路由表，种子 6 路，被维度引用禁删）。违规明细取值：missing_product_attribute/missing_risk_control/below_min/missing_evidence/unknown_route/unknown_role/illegal_fid。**不达标方案也允许提交**（violations 留痕、Gate 拒批），与 Q12"转人工"一致；AI 不自动补维度。

### 2.8 ProductAtomInstance（原子实例，段4）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 状态机 | atom8：草稿→待审核→已通过→已冻结→已废弃→已驳回→合规暂停→归档 | line 1454 |
| evidence | 必填（空则降级 pending_evidence，7 天超时自动驳回可复活） | Q18 |
| risk 等级 | critical/high/…；双轨判定（词表强制 + AI 兜底，只升不降） | Q17 |
| 引用数 | 产品事实原子（容量/成分/浓度/疣类型/品牌）引用数恒=1，严禁跨产品复用 | line 11178/11189 |
| approveAtomGuard | 10 项（见 01 §3 段4） | line 2633 |
| alias | 同义词簇被合并词条记为别名不删 | Q19 |

### 2.9 AtomConflict（段4，**字段完整**）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 类型 | disabled_expression（critical→驳回+合规审计）/ high_risk_single_review（high→单条 HumanGate）/ evidence_required（high→补证据或驳回） | line 840 |
| 状态 | blocked / pending_gate | line 840 |

> **实现补登（2026-09-13，M4 后端切片）**：段4 物理落 4 表——①`atom_batches`（tenant/product_space/pool 归属；batch_size；source=ai/manual；sensitive_snapshot）；②`atom_candidates`（候选主体：fid+dimension_id 挂选中池维度、content/normalized、fact_type、risk_level/risk_source=wordlist|ai|manual、matched_words、affinity/low_affinity、evidence/evidence_due_at、cluster_id/aliases/alias_of、status、reject_reason、approved_atom_id；唯一约束 `(batch_id, normalized)` 同批去重）；③`atom_conflicts`（type/status/detail + resolved_at，Guard 只数未解决冲突）；④`product_atom_instances`（Gate 通过才派生；唯一约束 `(fact_type, normalized)`——NULL fact_type 在 PG/SQLite 互不相等，故只约束事实原子，落实 line 11189 引用数=1；reference_count 默认 1）。候选侧状态实现映射：atom8 的草稿/待审核两态不建正式实例——候选状态 pending_evidence/pending_review/approved/rejected/merged；正式实例状态 approved/frozen/deprecated/rejected/compliance_suspended/archived（atom8 后六态）。risk 等级 critical/high 之外 medium/low 为【实现补】（原文只给 critical/high）。Q14 20/50、Q15 15-30（读池 target_atom_min/max）、Q16 0.5、Q18 7 天均为代码常量（02 §C2 已登记 M10 配置化）。Q48 词表物理实现见 §2.19 实现补登。

### 2.10 conditionPackages（PWC/条件包，段5，**字段完整**）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| combo[{fp,atom}] | 跨字段原子组合 | line 1780 |
| weight | 权重 | line 1780 |
| goals[] | 内容目的标签（contentGoals 枚举，Q25） | line 1780 / Q25 |
| strategy[] / structure[] / expression[] | 策略/结构/表达包引用 | line 1780 |
| compliance{ban,downgrade,gate} | 合规状态 | line 1780 |
| score | 评分（Q22 公式：(合理性0.6+多样性0.4)×品类乘数） | line 1780 / Q22 |
| source | ai / manual / hybrid | line 1780 |
| usage | 使用计数 | line 1780 |
| gate_status | pending / blocked / approved | line 1780 |
| 状态枚举 | 待用/已用/爆款/冷却/待Gate/待入库/候选/阻断/归档 | line 1780-1806 |
| 复用边界 | 严禁跨产品/跨客户复用；共享只在知识层 | Q24 |
| 库容 | 待用池默认 100（可配无上限） | Q27 |

> **实现补登（2026-09-14，M5 后端切片）**：段5 物理落 6 表（迁移 0005，PG16 up/down/up 实测）——①`content_goals`（Q25 字典，code 主键 + 5 码种子，color/ratio_min/ratio_max 可空，active/archived）；②`pwc_pool_configs`（一品一配置：capacity 可空=无上限、target_platforms、high_reuse_n）；③`condition_packages`（PWC 主体：goals/三包 refs/compliance_result/score+score_detail/score_incomplete/source/dup_of 自 FK/is_backup/gate_status/status/usage_count/high_reuse/is_hot+hot_at/审核人与时间戳，score 与双状态建索引）；④`pwc_combo_items`（组合原子行，唯一约束 `(pwc_id, atom_id)`，冗余 dimension_id/fid 便于溯源）；⑤`pwc_usage_records`（Q24/Q71 取用流水，唯一约束 `(pwc_id, platform, account, slot)`，带 tenant_id 索引）；⑥`pwc_platform_states`（每组合每平台 available/cooldown + cooldown_until，唯一 `(pwc_id, platform)`）。实现口径：冷却挂 per-platform state 而非组合全局状态（Q24 跨平台可复用）；line 1780 九态中"冷却"由此表表达，"待入库"原文【待补】未实现，"爆款"V1 仅手工（is_hot，Q61 自动检测后置 M10）；组合生命周期实现为 pending_gate/ready/used/blocked/archived（gate_status 另维 pending/blocked/approved）；重合度用原子集合 Jaccard【实现补，Q22b/Q23 共用】；target_platforms 全部用过后才置 used，平台清单来源【原文未给出，待补】，默认不配不产生 used；50/100/70/50/0.8/0.6/0.4/7 天/3 次/14 天为代码常量（02 §C2 已登记 M10 配置化）。

### 2.11 PWS（段6）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 全名 | 产品白名单冻结快照（PS 全部已批准资产打包对象） | Q28 词义澄清 |
| 版本 | v1.0 起大版本递增，历史全保留，同刻仅 1 个 active | Q31 |
| 状态 | frozen（可消费）/ superseded（旧版只读）/ revoked（急停） | Q30/Q32 |
| 就绪门 | pwsReadiness 5 项（见 01 §3 段6） | line 2634 |
| 所有者 | BO-07 角色 + 人工 Gate（红线操作） | line 7674 |
| 换版处置 | 四行（Q30）：旧版只读 / 草稿作废 / 未发布待复查 / 已发布不追溯 | Q30 |

> **实现补登（2026-09-14，M6 后端切片）**：段6 物理落 3 表（迁移 0006，PG16 up/down/up 实测，JSON 列 PG 侧为 JSONB）——①`pws_snapshots`（pws_id PK、tenant_id/product_space_id 索引、version(16)、status(16)=frozen/superseded/revoked、is_active 布尔、pool_id 冗余、fingerprint(64)=sha256(排序 atom_ids|pwc_ids)、snapshot/readiness JSON、refreeze_tier/reason_code/superseded_by、created_by/at、revoked_by/at/reason；唯一约束 `(product_space_id, version)`，is_active/status 建索引；同刻仅 1 个 active 由服务在重冻翻旧版保证，非 DB 部分索引）；②`pws_snapshot_items`（不可变物化成分行：kind=atom/pwc、ref_id、冗余 dimension_id、seq 有序、payload JSON；唯一 `(pws_id, kind, ref_id)` 防同版重复物化）；③`pws_freeze_logs`（freeze/refreeze/supersede/revoke 事件流水 + tier/reason_code/detail JSON/actor_id）。实现口径：active 不是独立状态行——frozen 与 is_active=true 组合表达"当前版本"（05 §2.5 的 active 行），superseded/revoked 均 is_active=false；就绪门③ active PWC=gate_status=approved 且 status∈ready/used【实现补】，门⑤ pending_gate 字段按本 PS gate=pending_gate 的字段池计数【实现补】；Q33 指纹仅同租户内其他 PS 比对且只提示；BO-07 角色码 `whitelist_owner`【实现补】；Q30 四行中草稿/未发布/已发布内容实体随 M8/段12 才有处置对象，M6 dispositions 结构先行（空列表/0/forced 布尔占位）。

### 2.12 publishSlots（发布位，段7）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 规模 | 14 平台 100+ 发布位 | line 1098-1221 |
| slotType | 发布位类型（13 类） | line 1428 |
| 推荐档 / 格式约束 | chars/dur（如 X-01 280 字符、TT-01 15-180s） | line 1098-1221 |
| 四维静态分 | traffic/safe/conv/load（0-100）——AI 拓展候选四维全 0 + pending_gate | line 1098-1221 |
| risk / gate | 风险等级 / 审核状态 | line 1098-1221 |
| fit_score | 四维加权聚合（Q34，权重按内容目的配置） | Q34 |
| 档案维护 | 后台 CRUD，客观字段附来源、四维分明示人工评估 | Q35 |

### 2.13 platformRules（平台规则，段7）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| selector 优先级 | 低→高：slotType > platform > platform+slotType > slotId；country 为横切 modifier | line 1383 |
| effect | 仅 blocked / partial（native 默认不存） | line 1383 |
| 规模目标 | <300 条覆盖 ~10 万理论格子 | line 1382 |
| 跨层规则 | R-X01~X05（其他层已选原子也作 selector，做内部矛盾检测） | line 1383 |
| 冲突消解 | 同级取最严（blocked > partial > 无）+ 保存时静态查重 | Q36 |

### 2.14 slotRuleByType（段7）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| slotType 默认值 | 13 类，约 40 列 | line 1428 |
| 单账号日发布限量 | 主发布位 2-3 / 互动位 3-5 / 短视频位 1-3 | line 1428 |
| 优先级 | 最低，可被逐位覆盖 | line 1428 |

### 2.15 strategyFields17（PCP 权重池，段8，**17 字段名完整**）
| 字段 | 说明 | 来源 |
|---|---|---|
| 17 个 *_pool | goal/action/struct/intensity/rhythm/tone/emotion/style/perspective/stage/title/hook/opening/mid1/mid2/mid3/ending | line 1294 |
| 权重约束 | 17 字段权重总和 ≤1.0（统一校验器强制） | PT-PCP-V1.5 / Q40 |
| 平台隔离 | 平台间不共享权重池 | PT-PCP-V1.5 |
| 模板初值 | 4 套平台类型模板（短视频型/社区讨论型/图文种草型/电商型） | Q39 |
| 重算 | 动态信号每周触发；AI 出候选+对照单，HumanGate 批准；单项 ±0.05 | Q41/Q42 |

> **实现补登（2026-09-14，M11 后端切片）**：段7/8 落 6 表于 `app/platform/platform_adaptation/`（迁移 0008，PG16 up/down/up 实测；JSON 列 PG 侧 JSONB，与 §2.5/§2.11 同口径）：
> - `publish_slots`（slot_id PK；platform/code unique/name/slot_type；chars_max/dur_min/dur_max；traffic/safe/conv/load Float 0–100 默认 0；score_source 默认 manual_eval【Q35 四维分明示人工评估】；risk/gate 默认 approved；source_url；status active/archived 软归档）。**14 平台 100+ 发布位目录原文在基准 HTML line 1098-1221，基准文件不在仓库，种子【原文未给出，待补】——M11 不虚构任何发布位，由后台 CRUD 录入**。
> - `goal_fit_weights`（goal PK，逻辑引用 content_goals.code；weights JSON=四维权重；Q34 Σ=1 硬校验，缺维/越界/和不为 1 → 422，不做归一化）。种子 2 行 ENGAGEMENT(0.4/0.2/0.2/0.2)、CONVERSION(0.2/0.3/0.4/0.1)：原文种草/转化两组，种草→ENGAGEMENT 为【实现补】目的码映射；EDUCATION/TRUST/RETENTION【原文未给出，待补】。fit_score 为派生值不落库；目的未配权重返回 fit_score=null + incomplete=true，不凑分。
> - `platform_rules`（rule_id；selector_level=slot_type/platform/platform_slot_type/slot；platform/slot_type/slot_id/country 可空=通配；effect 仅 blocked/partial；status active/archived）。层级必填字段校验（slot 级必须有 slot_id 等）；保存时同层级同条件不同结论静态查重 → 409 返回冲突行，overwrite=true 重发即归档旧规则（Q36"覆盖旧规则/放弃保存"的 API 化）；match 裁决=高优先级层级覆盖低层级、同级取最严 blocked>partial、country 横切；native 默认不存、无命中=无限制。跨层规则 R-X01~X05【M11 未实现，待后续任务包】；Q37 动态信号 V2。
> - `slot_type_defaults`（slot_type PK；daily_limit_min/max；defaults JSON 承载约 40 列）。约 40 列明细原文 line 1428 依赖基准 HTML【原文未给出，待补】，M11 仅落单账号日限量两列 + 自由 JSON，不种子（主发布位 2-3/互动位 3-5/短视频位 1-3 由后台按需录入）；优先级最低、可被逐位覆盖属消费侧口径，随段11/段12 接入。
> - `pcp_templates`（Q39 4 套模板种子，id=pcpt-{code}，short_video/community/photo_text/ecommerce，weights 17 键 Σ=1.0；初值为【实现补：Q39 明确"模板初值起草放实现阶段"的初值草稿，回流后校准】）。
> - `pcp_weight_tables`（pcp_id/tenant_id/product_space_id/platform；template_code 可空——人工直编后清空；weights 17 键 Σ≤1.0 走 Q40 统一校验器；唯一约束 uq_pcp_ps_platform(product_space_id, platform) 落实 PT-PCP-V1.5 平台间不共享、同 PS×平台同刻仅一条 active（软归档旧行后可再建）；Q52 实例带 product_space_id/tenant_id 供段11 Guard④⑤）。
>
> 维护角色统一 operations（Q35 后台 CRUD），所有写操作 append_audit（slot.create/update/archive、fit_weights.put、rule.create/archive、slot_type_default.put、pcp.create/update）。Q41/Q42 每周动态信号重算 + AI 对照单 + 单项 ±0.05、fit_score 学习闭环、PLATFORM-ADAPTER AI Skill 均 V2；M11 仅保留 Q42 人工直接编辑通道（无幅度限制、有 before/after 审计）。

### 2.16 layerSpaces（通用底座，段9/10，**维度名完整**）
| 层 | 维度 | 来源 |
|---|---|---|
| strategy（6 维） | 认知阶段/目的/强度/角度/情绪/CTA | line 1261 |
| structure（6 维） | 标题/钩子/开头/中段/结尾/脚本 | line 1261 |
| expression（6 维） | 语气/本地化/直白度/软化映射/视觉/符号 | line 1261 |
| compliance（4 维） | CP-BAN 禁用 / CP-DOWN 降级 / CP-LAW 法审触发 / CP-CLEAN 清洗替换 | 段10 |
| 共享属性 | 跨产品共享；改动收权平台级管理员 + 影响面提示 + 审计 | Q46 |

### 2.17 packages（CSP/CSTP/CEP，段9，**字段名完整**）
| 包 | 字段 | 来源 |
|---|---|---|
| CSP | goal/stage/angle/intensity/cta/emotion | line 863 |
| CSTP | struct 段式 | line 863 |
| CEP | tone/perspective/explicit/soften | line 863 |
| 通用属性 | 各带 conf 与 gate | line 863 |
| 复用 | 按（产品×平台×目的）三元组配一份，满 20 次或 PCP 更新触发重配 | Q45 |

> **实现补登（2026-09-14，M11 后端切片，段9 V1 静态最小切片）**：物理表 `packages` 落于 `app/decision/layer_strategy/`（迁移 0008）：package_id PK、kind=csp/cstp/cep、tenant_id/product_space_id/platform/goal、payload（PG 侧 JSONB）、conf Float 可空、gate 默认 approved、status active/archived、usage_count 默认 0、created_by/at、updated_at。payload 键按本表定死：CSP=goal/stage/angle/intensity/cta/emotion，CSTP=struct，CEP=tone/perspective/explicit/soften；缺键/多键 → 422。Q45 V1 口径：（产品×平台×目的）三元组每包型仅一条 active（重复 409，软归档后可重建），operations 后台配置，行带 product_space_id/tenant_id（Q52，段11 Guard④⑤ 消费）；goal 须为活跃 content_goals 行，product_space 须存在（否则 404）。**WF-07 AI 选包与"满 20 次/PCP 更新触发重配"V2 不做**；usage_count 列先行，M11 不增计数。写操作 append_audit（package.create/update/archive）。

### 2.18 countries（段10）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 国家事实库 | 承载国家级规则：R-030（EU/GDPR 禁留邮箱手机）、R-031（CN/NMPA）、R-032（US/FDA OTC 禁 cure/treat） | line 1417 |
| 优先序 | 国家法规 > 平台规则 > 底座默认（不可配置） | Q50 |

### 2.19 合规词库（Q48 合并后，**字段完整**）
| 字段 | 说明 | 来源 |
|---|---|---|
| 词 | 禁用/敏感词 | Q48 |
| 等级 | critical / high | Q48 |
| 处置 | 禁用 / 降级 | Q48 |
| 降级映射目标 | 如 治愈→感受改善 | Q48 |
| 适用国家 | 按市场取词条 | Q48 |
| 适用行业 | 行业维度 | Q48 |
| 生效期 | 生效时间 | Q48 |
| 消费关卡 | 段4 定原子风险 / 段5 相撞合规检测 / 段10 清洗（同一张表） | Q48 |

> **实现补登（2026-09-13，M4 后端切片；2026-09-14 M7 补 layer 列）**：物理表名 `compliance_wordlist`（横切包 `app/core/compliance_wordlist/`，迁移 0004；M7 迁移 0007 增 `layer` 列 country/platform/base，存量行回填 base）；字段 entry_id/word/level/action/downgrade_target/country/industry/layer/effective_from/effective_until/status(active|archived)/created_by+时间戳。维护角色 operations\|internal_compliance，action=downgrade 缺 downgrade_target 拒绝保存（422），DELETE 为软归档，写操作 writeAudit。三关卡消费：段4 原子提交时按 PS.industry_tag + 生效期做大小写不敏感子串匹配、命中按词表等级强制定级（Q17 词表轨，人工不可改）；段5 漏斗同法匹配（ban→blocked 不评分，Q26 手拼不豁免）；段10 M7 起按行业+市场（country 为空=全市场）取词，同词命中多条按 **Q50 层级（国家>平台>底座）+ Q36 同级从严（ban 严于 downgrade、critical 严于 high）** 裁决取一条。Q51 生效即扫已随 M7 落（保存生效事务内扫 active frozen 快照，命中出 wordlist_rescan 待办）；未来生效词条的定时扫描随 M10。

### 2.20 CP-LAW 敏感领域清单（段10）
| 字段 | 说明 | 来源 |
|---|---|---|
| 领域 | 医疗健康/儿童/减肥/美白/医疗器械/金融（领域非词） | Q48 |
| 触发 | 产品属敏感领域即自动触发法审待办 | Q49 |

> **实现补登（2026-09-14，M7 后端切片）**：段10 三表落于 `app/decision/compliance_center/`，迁移 0007（PG16 up/down/up 实测）：
> - `cp_law_sensitive_domains`（code/name/status，internal_compliance 全 CRUD + 审计；迁移种子 medical/children/weight_loss/whitening/medical_device/finance 六码）。
> - `law_reviews`（Q49：(pws_id) 唯一、一冻结版一条；status pending/approved/rejected + conclusion + decided_by/at；CCR 清洗运行时按 PS.industry_tag 命中活跃领域码（或 sensitive_industry 旗标兜底，领域码取行业标/sensitive_other）幂等自动触发，生成 internal_compliance 法审待办 48h due；线下律师结论经 `POST /api/law-reviews/{id}/decision` 录入，通过→段11 Guard⑥ 放行，不通过→维持否决，已决不可改）。
> - `ccr_reports`（CCR 清洗结果：pws_id+country 分市场独立判，country 为空=不按市场细分；status clean/downgrade_pending/approved/blocked；block_required 布尔；hits=Q50 裁决后的 bans/downgrades；wordlist_context 记录词条 id 集合与判定时间；append-only——重跑产新行、历史不可 mutate（PT-COMPLIANCE-V2.0），段11 消费每市场最新行；降级只出建议、须 internal_compliance 人工 approval）。
> - Guard 消费视图 `GET /api/pws/{pws_id}/ccr/gate`：block_required/cleaning_passed（Guard②）、law_review_passed（Guard⑥），M8 段11 组装接入。
> - Q51 扫描只"触发"不自动建新版：重冻仍须 BO-07 whitelist_owner 人工执行（红线 line 7674，与 M6 无全自动冻结一致）；draft FCW 扫描已随 M8 接入（命中快照关联的 draft FCW 出 operations 待办），成品库未发布内容随段12。

### 2.21 FCW（段11，**字段完整**）
| 字段 | 说明 | 来源 |
|---|---|---|
| final_id | final_content_whitelist_id（唯一出口 E1.1 publishFCW 生成） | line 870 / line 11036 |
| pws_id / pcp / csp / cstp / cep / ccr | 6 层快照输入 | line 870 |
| platform / slot / goal | 平台/发布位/目的 | line 870 |
| score | FCW-SCORE（0.4/0.3/0.3，仅排序不做门槛） | Q54 |
| publish_status | draft / published | line 870 |
| E1_owner | publishFCW | line 870 |
| Guard | 7 项（5 项原 + ⑥law_review 已通过 + ⑦PWS active 版本） | Q53 |

> **实现补登（2026-09-14，M8 后端切片，迁移 0009，PG16 up/down/up 实测）**：物理表 `final_content_whitelists` 落于 `app/final/final_whitelist/`——final_id String36 PK（uuid1 时间有序，**全系统仅本包 E1.1 publishFCW 可 mint**，line 11036 红线）/ task_id（Q55 任务发证留痕，手动单条为空）/ tenant_id/product_space_id 索引 / pws_id/pwc_id（骨架 PWC 取自冻结快照 items 的 ref_id）/ pcp_id/csp_package_id/cstp_package_id/cep_package_id（M11 配置实例 id 引用，供 Guard④⑤ 归属核验）/ ccr_report_id（分市场最新报告；law_review_id 列预留）/ platform/slot_id/goal 索引 / country String8 可空（分市场维度，空=不按市场细分）/ score Float 可空 + score_detail JSONB + score_incomplete Bool / guards JSONB（7 项逐项 code/passed/detail）/ guards_passed Bool / publish_status String16（server_default published；draft 通道 V1 未开【实现补：Q55 全自动发证直接 published】）/ issued_by/created_at/published_at。唯一约束 `uq_fcw_same_issue(pws_id, pwc_id, platform, slot_id)`【实现补：同冻结版×同骨架×同平台×同发布位不重复发证，Q24/Q71 去重精神落到组装口；country 为可空维度不进 DB 约束，同键含 country 的查重在服务层】。Q55 任务表 `fcw_assembly_tasks`：task_id PK / tenant/PS/pws 索引 / platform/goal/country / requested_count / slot_ids JSONB / status（V1 同步执行，建单 running→completed）/ results JSONB（requested/resolved_slots/issued[{slot_id,final_id}]/failures[{slot_id,detail}]，单项失败不阻断其余）/ created_by/created_at/completed_at；异步队列随 M10。**Q54 临时评分实现口径**：尺度统一到 0-100（PWC 分与三包 conf 为 0-1 ×100；slot fit_score 本为 0-100），score = pwc×100×0.4 + fit×0.3 + mean(conf)×100×0.3，权重为代码常量（M10 配置中心迁移，02 §C2）；任一来源缺失（PWC 未评分/fit 矩阵缺/任一包 conf 空）→ score=null + incomplete=true，不凑分（对齐 Q22b），分永不参与 Guard。**Guard② 实现口径**：除 block_required=false 外同时要求 cleaning_passed=true（分市场报告 status∈clean/approved）——无报告时 gate_view 的 block_required 默认 False，不使"未清洗"伪装放行【实现补】；Guard⑥ 仅在 law_review_required 时要求 approved，未触发不阻断（Q49/Q53）；Guard③ 含 PCP active + 三包 active 且 gate=approved。骨架 PWC 选择【实现补】：冻结快照 items kind=pwc 中取 payload.goals 与请求目的有交集者，按 payload.score 降序（null 末位）取一；无交集→材料缺失 409，不生成 final_id。**未含（事实纪律挂账）**：WF-09 两 Skill（FCW-ASSEMBLY/FCW-SCORE）AI 通道随 V2（V1 纯机械解析+评分）；draft FCW 的生命周期/列表与人工处置端点（Q30 换版作废 draft 的实体联动目前仅结构上可挂，publish_status=draft 无创建入口）；段12 内容成品实体；FCW 消费/交付中台 API（05 §1.2 待补契约）。

### 2.22 catFeedback（段13，**字段完整**）
| 字段 | 说明 | 来源 |
|---|---|---|
| content_count | 内容数 | line 746 |
| avg_ctr / avg_read / avg_conv | 平均点击率/阅读/转化 | line 746 |
| hot | 热度标记 | line 746 |
| field_impact:[{fid, fill, lift, impact}] | 字段级：填充率/转化增益/影响 | line 746 |
| 采集纪律 | 缺席字段=未采集记"—"，绝不当 0/不许估算 | Q60 |

### 2.23 KUP 提案（段13，**字段完整**）
| 字段 | 说明 | 来源 |
|---|---|---|
| type | add / promote / demote | line 1006 |
| target | 目标（G2 字段/原子 weight/类目模板） | line 1006 |
| reason / evidence / conf | 理由/证据/置信 | line 1006 |
| status | pending（PM 人工 Gate 后回写） | line 1006 |
| 证据三关 | 样本≥30 条 + 增益差≥0.5% + 连续 2 周期方向一致 | Q63 |
| 审批 | 单产品=运营；共享知识层=平台管理员（"PM"废除） | Q64 |
| 回滚 | 预期对账未兑现可一键回滚（每次回写留回滚点） | Q63 |

### 2.24 统一模型注册表（横切，**字段完整**；Q82 已实现底座 + CAT-RECOG 试点）
| 字段 | 说明 | 来源 |
|---|---|---|
| 模型/供应商/输入价/输出价/日预算/状态/fallback | 统一 per-1M 口径 | Q67 |
| 场景路由 | 场景→默认模型配置页（类目识别/原子拓展/内容生成/合规复检等）；场景键 = `skill_id` | Q67 / Q82 |

物理实现补登（字段级定义原文未给，Q82 接缝裁决 + 实现补登；详细口径见 10 §2.8）：

| 表 | 关键字段 | 说明 |
|---|---|---|
| ai_models | model_id(PK)/model_code 唯一/provider/**capability(chat|embedding)（Q86/0018 增，server_default chat）**/input_price_per_1m/output_price_per_1m Numeric(12,4)/currency_code String3 可空/daily_budget 可空/status(active/disabled)/fallback_model_id 自引用 FK/审计与时间戳 | per-1M 入出价；embedding 模型仅计 input 侧；币种原文未给【待补】，不猜币种；daily_budget 为 None=不限，超限硬停不静默换商；fallback 仅注册+人工挂接，不做失败自动转移（V2）；场景路由按 capability 解析，能力不符 422（Q86） |
| ai_model_keys | key_id(PK)/model_id FK/ciphertext LargeBinary/fingerprint String8/status(active/revoked)/created_by/created_at/revoked_by/revoked_at | outbound 供应商密钥；Fernet 对称加密、env 主密钥（Q82-3）；明文永不回显，仅末 4 位指纹；一模型至多一 active 行，轮换=旧行置 revoked + 插新行保留历史（outbound 域，入站半套见下 agent_api_keys） |
| ai_scene_routes | scene(PK, = skill_id)/model_id FK/updated_by/updated_at | 运营可改不动代码（operations\|platform_admin） |
| skill_prompts | skill_id(PK)/current_version/updated_by/updated_at | 当前生效版本指针 |
| skill_prompt_versions | version_id(PK)/skill_id/version/template Text/change_note/variables JSONB/created_by/created_at；uq(skill_id,version) | Prompt DB 版本化（v0.1 起步，Q82-4）；append-only，platform_admin 发布 + writeAudit；仓库只携初始种子，不用 15 §1 规划的 runtime/skills/&lt;id&gt;/prompt/*.md |
| **agent_api_keys** | key_id(PK)/name String128/**key_hash String64（uq_agent_api_key_hash + 索引）**/key_prefix String24/status(active/revoked)/created_by/created_at/revoked_by/revoked_at/last_used_at；**无 tenant_id 列** | **入站**一 Agent 一 Key（**Q88/迁移 0019**，`app/core/api_keys/`）：明文 `loom_`+token_urlsafe(32) **仅签发响应返回一次**，DB 只存 SHA-256 hex + 前 12 字符展示前缀（刻意区别于 outbound Fernet 可逆加密——入站只需比对，库泄露不暴露可用 Key）；platform_admin 签发/列表/吊销，越权 403、空名 422、未知 Key 404；吊销=状态位 append-only 不物理删除；未知/空串/已吊销验签统一返回 None（防探测侧信道），命中盖 last_used_at；审计 agent_api_key.issue/revoke（tenant `_platform`）。Key 为平台级凭证不绑租户（单租户绑定口径原文未给【待补】，未来租户归属由 content_id→FCW.tenant_id 解析）；**V1 无受保护业务消费端点**——`POST /api/effect-callback`/effect_records/孤儿队列/Q60a 回填/customer-backfill 全部随段13/P3（V2），本切片不提前落 |

要点（Q82 实现补登）：真 LLM 调用形态=业务端点进程内同步调用网关，产出以系统机器 Actor（`roles=[]`，Q66）经 skill7 同一条投递通道落 `skill_runs`/`skill_candidates(pending_review)`，运营 HumanGate 不变；`skill_runs` 增列 model_id/input_cost/output_cost/currency_code（decimal + 币种），成本按 per-1M 价格 × tokens/1e6 折算（ROUND_HALF_UP，6 位小数）；仓库不放真实供应商凭证，测试/本地用确定性合成客户端（provider=synthetic），真实 Key 仅在部署环境经 env 主密钥 + 注册页录入。其余四个生成站点中 **PWC-BUILDER 已随 Q83 第二站接通、C7 Layer4 TYPE-MATCH 已随 Q84 第三站接通、WF-02 字段池方案 DIM-MERGE 已随 Q85 第四站接通、WF-03 CONFLICT-PRECHECK + ATOM-AFFINITY 已随 Q86 第五站（末站）接通——五个真 LLM 站点全部接通，Q67 站点 backlog 闭合**（迁移 0015/0016/0017 仅加场景路由/Prompt 种子无 schema 变更；**迁移 0018 是首个带 schema 变更的 LLM 切片**：PG 端 `CREATE EXTENSION vector`、`ai_models.capability`(chat|embedding)、`atom_candidates`/`product_atom_instances` 两列 nullable `vector(1536)`、配置项 `atom.cluster_line=0.9`（借 Q10 同义线，原文未给）与第二个合成模型 synthetic-embedding；V1 相似度=进程内纯 Python 余弦不建 KNN 索引，千万级切换按 14 §2.3 后置；详见 02 C1.30 与 10 §2.8）。**restock_auto 自动触发已随 Q87 M8 worker 闭环（2026-09-15，无迁移）**：requested/restock_auto 信号行不可 mutate，worker 纯追加 succeeded/failed 的 llm_auto 子行（`input.restock_request_id` 回链）表达认领与终态，详见 02 C1.31、05 §2.3 Q87 补登。**入站一 Agent 一 Key 凭证券半套已随 Q88 落地（2026-09-15，迁移 0019，新表 agent_api_keys，SHA-256 单向哈希）**：与 outbound Fernet 分域（新横切包 `app/core/api_keys/`，不与 model_registry 合并），治理端点 `/api/admin/agent-keys`；effect-callback 本体/effect 时序/孤儿队列/Q60a 仍随段13 V2，详见 02 C1.32、05 §1.3/§2.3 Q88 补登、10 §2.8。

### 2.25 审核任务/待办（横切）
| 字段/属性 | 说明 | 来源 |
|---|---|---|
| 类型 | 类目确认/法审/权重表审批/原子证据等（全链几十处 Gate） | Q70 |
| SLA 时限 | 法审 48h / 类目确认 72h 等（各配时限） | Q49 |
| 升级 | 通用 SLA 引擎：24h 黄/48h 升级上级 | Q49 |
| 批量 | 置信 >0.85 支持批量通过，低于线逐条审 | Q70 |

### 2.26 系统配置中心（横切；M10 切片 a 已实现）

对应 §2 表 #31 / 02 §C2 全表 / 14 §2.4 定稿。物理实现补登（字段级定义原文未给，属本节补齐范围；详细口径见 10 §2.8 实现补登）：

| 表 | 关键字段 | 说明 |
|---|---|---|
| config_items | key(PK)/category/value(JSONB)/value_type/validation(JSONB)/source_ref/version/updated_by/时间戳 | 54 项 C2 标量种子；新键只随代码+迁移进入，运营改值不造键；类型 int/float/bool/string/json + min/max/choices 校验 |
| config_item_versions | version_id(PK)/key/version/value/change_note/changed_by/created_at；uq(key,version) | 发布即新版本行，回滚=新版本重发历史值，永不改写 |

要点：仅 `platform_admin`（平台级管理员 Q46/Q64 角色英文码【实现补】）可改；发布=校验→版本+1→版本留痕→writeAudit(`config.update`/`config.rollback`)→提交后进程内快照原子切换；已有专用 CRUD 表的 C2 项（行业阈值/信号权重/词库/字典/发布位/模型注册表等）不进配置中心，无双事实源。消费侧（M10 切片 c，2026-09-14）：`knob(key)` 统一访问器读缓存、未引导回落种子默认；M1–M8 拍板值均已迁移；lifespan 启动引导 reload（失败告警不阻断）。挂账：多副本广播。

---

## 3. 原文未给出字段定义的实体（待补清单，开发前置）

以下实体在 v3 中仅有名称/语义，字段级定义**原文未给出**，属开发第 0 步①（见 01 §7）的补齐范围：

| 实体 | 已知语义 | 待补内容 |
|---|---|---|
| 内容成品（文章/视频/多语言） | 独立成品/复检/入成品库 | 全字段定义（载体见 01 段12） |
| SkillRunLog | 输入/输出/成本/置信度/失败/人工修改记录，不可 mutate | 全字段定义（**切片 e 已补实现**：物理表 `skill_runs` + 通用候选 `skill_candidates`，字段补登见 10 §2.8，Q76；成本金额列 model_id/input_cost/output_cost/currency_code 已随 Q82 落地，币种【原文未给出，待补】）；**Q87 补登**：`status=failed`（Q71 预留）首次启用——restock worker 对终态失败追加 failed/llm_auto 子行；requested 信号行永不更新，认领靠子行 restock_request_id 反连接 |
| writeAudit | 所有人工操作 + AI 调用全记录 | 全字段定义 |
| API Key | 唯一入口、可吊销 | outbound 已补字段并实现（§2.24，Q82：密文/指纹/轮换/吊销）；入站"一 Agent 一 Key"全字段定义仍待补（Q60 效果回调切片） |
| Memory 6 层（M1-M6） | SQL + 向量库 + 对象存储 + 缓存 | 全字段定义 |
| 动态信号事件（Q37） | 平台/发布位/事件类型/严重度/生效期 | 全字段定义 |
| 爆款判定记录（Q61） | 中位数 5 倍 + 绝对门槛 | 全字段定义 |
| 校准报表（Q65） | FCW score / AI 质量分 vs 实际互动 | 全字段定义 |
| 待办 SLA 引擎 | 时限+升级动作 | 全字段定义（含各待办类型时限表） |

> **2026-09-13 M0 补写**：ProductSpace 与 g2FieldCandidates 已补入 §2.2 / §2.6（字段逐项带来源；原文真空处标【待补】）。残留待补子项：①PS.lifecycle 五档的迁移触发条件；②18 通用字段值挂 intake 还是 PS 快照（M1 实现前定）；③g2FieldCandidates 的全局/租户可见性；④候选完整状态枚举。其余 9 个实体属段 7/12/13 或横切，不在 M0 段1-6 范围，随对应阶段补。

---

> **信息保全**：本表全部字段来自 v3 Part A（A1/A3/A4/A6）、Part C（Q1–Q72）、Part D（D3 菜单）原文，逐项带来源；原文未给出字段处已显式标注【待补】，未新增任何虚构字段。展示口径实体（A1/A2/A3 库、Memory、辅助系统等）明细见 09 文档。
