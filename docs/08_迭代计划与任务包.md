# Loom · 迭代计划与任务包

> **本文档来源**：`../Loom_核心业务主链梳理_v3.md` **Part A7（权威三阶段）+ Part D9/D9.5（展示路线图）** + 【任务包拆解】（基于原文的派生工程结构）。
> **文档定位**：迭代执行视图。**两套路线图口径已于 2026-09-13 由负责人裁决合并（见 §1.3）：A7 定主链纵向顺序，D9 定每层交付厚度 + 时间盒/客户目标；合并后的权威三版为 V1/V2/V3（§1.3 末）。** §1.1/1.2 原文按信息保全保留。
> **⚠ 阅读注意**：§1.1/1.2 为原文路线图（历史口径）；§1.3 末"合并后权威路线图"为现行依据；§2 任务包已按合并结论重排（V1 = 段 1→6→10→11，段 12 移 V2）。工期/人员/排期原文未给，**留待排期**，不编造。

---

## 1. 路线图（两套口径；§1.3 已裁决合并）

### 1.1 权威口径 · 实现范围建议（A7，以 HTML 13 段链为准）

**第 0 步 — 补规格仲裁（写代码前）**：裁决 §B1 S 级 4 条——①段 1-6 数据模型补齐（实体字段以 §A3 各实体清单为底）②平台适配与 KUP 两个 pending Skill 的协议定稿 ③回流指标降级为平台可得数据（公开互动数 + 客户手工回填转化）④评分公式定稿（四维聚合、C1 信号提取、PWC 品类乘数）。§B2 三处规格缺口决定是否采纳业务方素材（需明确告知并映射）。

**第一阶段 — 核心主链 MVP（段 1→6→10→11→12）**：
产品录入（14 态可裁剪为 8 态左右）→ C1 识别（先文本信号，图像后置）→ 字段池规划 + Gate → 原子拓展 + approveAtomGuard → PWC 构建/校验（评分先用可解释的简化公式）→ PWS 冻结（pwsReadiness 5 项）→ 合规清洗（CP-BAN/CP-DOWN 规则库驱动）→ FCW 组装（5 项 Guard，通用包一致性校验问题先按「通用包免校验 PS 归属」处理并记录）→ 内容生成（只读消费 + 强制复检）。
横切最小集：skill7 候选-审核通道、writeAudit、final_id 单一出口、RBAC 红线操作。

**第二阶段**：段 7/8 平台层（发布位静态规则 + 简化 fit_score）、段 9 D0 三包、段 13 回流（先发布记录 + 可得互动数据 + 人工 KUP，AI 提案后置）。

**第三阶段 — 周边**：§D 清单按需展开（计费/账号管理/视频工作台/Agent/成本治理等）。

### 1.2 展示口径 · V1/V2/V3 路线图（D9，完整保留）

**V1 · MVP（0-3 个月）目标：5-10 客户验证**
产品空间（字段池+原子管理）/ 平台层 16 张底表（基础版）/ 策略层 13 张底表（基础版）/ 白名单组装引擎（核心）/ 候选池审核工作台 / AI Skill 注册表+编排器 / 6 个治理驾驶舱 / RBAC+租户管理 / 客户入驻 Onboarding / CSV 导出对接中台 / 客户前端 8 大菜单（基础版）

**V2 · 成熟版（3-6 个月）目标：30-50 客户**
完整 7 大活水机制 / 6 种 AI Skill 全部上线 / 反馈归因系统 / 跨表亲和度自学习 / 知识库系统（公共+私有+产品）/ 类目管理（通用+非通用+冷启动）/ Memory 6 层完整 / API 对接中台 / 版本快照管理 / Multi-model Router / 客户自助门户

**V3 · 平台（6-12 个月）目标：100-200 客户 · 估值 $50M-200M**
10 个辅助系统全部上线 / Knowledge Graph 关系图 / Simulation Sandbox 模拟 / Golden Cases 标杆库 / Drift Detection 漂移检测 / SDK 嵌入式集成 / 完整 BI 仪表盘 / Webhook 推送体系 / 移动端 / 国际化（多语言界面）/ 白牌定制方案

### 1.3 口径合并裁决（2026-09-13 负责人拍板，本项原"待裁决"销账）

> D9 原文标注：⚠ 与 §A7（以 13 段链为准的三阶段实现建议）**并存但口径不同**；两版路线图在项目推进时需由负责人裁决合并口径。
> 差异要点：A7 以 13 段链纵向切阶段（MVP 只做段1→6→10→11→12）；D9 以全景体系横向切版本（V1 即含 16/13 张底表+组装引擎+前端）。两套的 V1/MVP 范围**并不等价**。

**裁决结论（2026-09-13，负责人三项逐拍）**：

1. **合并框架 = A7 为骨 + D9 厚度**：纵向交付顺序/主链范围以 A7 13 段链为准（不可被展示口径稀释）；每一版的交付厚度（平台/租户/前端/驾驶舱/辅助系统）吸收 D9 横向清单。时间盒与客户目标采用 D9：V1 0–3 月 / 5–10 客户，V2 3–6 月 / 30–50，V3 6–12 月 / 100–200。
2. **段 12（内容生成）后置 V2**：A7 原 MVP 含段 12，裁决将其移出 V1——V1 到段 11 `final_id` 发证即闭环；段 12 随段 13 反馈在 V2 落地（理由：5–10 客户验证阶段中台可先消费 FCW，成品生成非白名单价值闭环的必要段）。
3. **V1 驾驶舱只留 2 个**：D9 V1 原列"6 个治理驾驶舱"，裁决收敛为 ① 单位经济/Token 成本驾驶舱（Q67 口径）② 人工审核工作量驾驶舱（Q70 SLA/批量审核）；其余 V2/V3 再扩。
4. **质量两件套前置覆盖 D9 V3**：Evaluation Dataset + Golden Cases 已在 14 §2.6 定稿纳入 V1（M10-Q），覆盖 D9 原列 V3 的 Golden Cases；Simulation Sandbox / Drift Detection 等仍随 V2/V3。

**合并后权威路线图（现行依据，§2 任务包据此重排）**：

| 版 | 时间盒/客户 | 主链范围（A7 骨） | 厚度（D9 吸收） |
|---|---|---|---|
| **V1** | 0–3 月 / 5–10 | 段 1→6→10→**11**（到 `final_id` 发证闭环；**不含段 12**）；段 7/8 **仅做 FCW 6 路输入所必需的静态底表基础版**（发布位/平台规则/17 权重池录入与静态值，无动态信号、无 fit_score 学习、无 PLATFORM-ADAPTER AI Skill） | skill7 候选-审核通道、writeAudit、RBAC、配置中心（DB+广播热更新）、SLA 引擎、**审核工作台**、租户管理 + 客户入驻 Onboarding、客户前端 8 菜单基础版、CSV 导出对接中台、Skill 注册表+自研编排器最小集、**M10-Q 质量两件套**、**2 个驾驶舱**（Token 成本 + 人工审核） |
| **V2** | 3–6 月 / 30–50 | 段 7/8 完整（动态信号 + fit_score 学习 + PLATFORM-ADAPTER 首次落地）、段 9 三包（CSP/CSTP/CEP）、**段 12 内容生成（由 V1 移入）**、段 13 回流 + KUP | 知识库（公共/私有/产品）、Memory 6 层、Multi-model Router、客户自助门户、反馈归因、跨表亲和度自学习、版本快照管理、API 对接中台、类目管理 |
| **V3** | 6–12 月 / 100–200 | 平台化扩展 | 10 辅助系统剩余 8 个（Knowledge Graph / Evidence Center / Simulation Sandbox / Rollback Center / Drift Detection / Human Feedback Loop / Context Pack Builder 等）、完整 BI、Webhook、SDK 嵌入式、移动端、国际化、白牌定制 |

> 段 7/8 的 V1 最小切片仅为满足 FCW Guard③"所有 Package state=active"的静态输入；Q52 的实例归属校验在静态底表上同样生效（非"通用包免校验"，A6/Q52 已裁决）。

### 1.4 周边功能清单（D9.5，主链之外，完整保留）

> 来源：旧 md v2 §7，为主链之外的模块清单（与 D3 的 13 一级菜单展示口径不同，按原文保留）。
>
> 工作台（总览/通知中心）· 智库中心（灵感库/类目管理/运作总图 SOP/智能生成工作台/内容动作中枢 8 类矩阵/通用底座规则集/条件包工作台 merge-engine/D0 运行态包/活水库/决策证据链）· 内容加工台（共用原料台/调度大脑/文章生成中心/视频生成中心 12 子模块）· 社媒账号管理（账号 10 阶段/健康分/环境设备/IP/账号治理）· AI 管理（端到端真跑/Skill 编排/Prompt 模板库 49 协议/模型+Key 注册表/Agent 注册表/单位经济仪表盘/成本治理/原子定义库/活水知识 Memory·RAG/治理回滚中心）· 商业化（计费钱包/套餐 u1Plans）· 用户管理 U1（员工+AI agent/租户总览）· 权限 U2（RBAC 双命名空间/全局策略）· 系统后台 API 端（白名单查询/使用上报/效果回流/DB 浏览/AI 调试台/调用日志/配额/Key 管理）· 用户前端（产品/内容/充值）。

---

## 2. 任务包拆解（【任务包·派生】基于合并后路线图 + 各段规格 + Q 编号）

> 拆解规则：每个任务包 = 范围（引用 01 文档对应段）+ 关键输入 + 验收标准（引用原文 Guard/约束/Q 编号）+ 依赖。**工期/人员/优先级原文未给，留待排期**。
> 阶段划分以 §1.3 合并裁决为准：**V1 = 段 1→6→10→11**（M1–M8），段 12 移至 V2（P4）。

### 2.1 第 0 步 · 规格仲裁（前置，非编码）

| 包 | 内容 | 验收标准 | 依赖 |
|---|---|---|---|
| M0 | S 级 4 条收尾（据 02/03 现状，非全部待裁）：①段1-6 数据模型缺口补写——以 01 §A3 实体清单为底、补 04 §3 待补清单（规格写作，非裁决）②PLATFORM-ADAPTER 协议已定稿（06 §2.3，12 标 ✅）；KUP-PROPOSE 占位属第二阶段（段13），不阻塞 MVP，随段13 首次落地时补 ③Q60 已临时采纳——确认回流指标降级口径（平台公开互动数指标集 + 客户手工回填转化，source=customer-backfill）后转定稿 ④Q34 已定稿；Q2/Q22 仍为临时采纳——C1 信号权重/图像信号提取、PWC 品类乘数公式待拍板转定稿。业务方 3 份素材已采纳并销账（D5→Q21、F3→Q70+Q49、E1→Q71+Q24），本包仅需向负责人明确告知并留确认记录 | Q60/Q2/Q22 转定稿并在 02 留记录；04 §3 待补实体补齐（同步 10）；素材采纳确认记录；更新 04/06/12 对应状态 | 无（全部任务的输入） |

> **口径说明（2026-09-13 核对 02/03）**：02 §C1 收官注已载"S 级 4 条经 Q34/Q60 等解决、B18-B20 已裁决销账"；故 M0 的实际性质是**临时采纳转定稿 + 规格缺口补写 + 既成采纳告知**，而非从零裁决。01 §7 第 0 步为 v3 原文表述，按治理规则保留原文不改。
> **M0 完成情况（2026-09-13）**：✅ Q2/Q22/Q60(a/b/c) 转定稿；新增 Q22a/Q22b 评分分项规格（同步 01 段5、06 §1.1、§C2）；✅ 业务方素材采纳经负责人确认（02 C1.16 注记）；✅ 段1-6 缺口实体 ProductSpace / g2FieldCandidates 字段补齐（04 §2.2/§2.6，同步 10）。挂账子项（不阻塞 M1）：PS.lifecycle 迁移触发条件、~~18 通用字段值归属（M1 前定）~~ ✅ 已于 2026-09-13 由 Q74 裁决（两处落库：申请单 profile + PS.profile_snapshot）、候选实体全局/租户可见性（多租户设计时）、候选状态完整枚举。KUP-PROPOSE 随第二阶段段13 补。

### 2.2 V1 · 0–3 月 / 5–10 客户（主链段 1→6→10→11 + 静态底表 + 平台厚度）

> **V1 落地状态总览 / 排期骨架（2026-09-17 据 handoff 与各包完成情况注记整理，不含新口径）**
> 落地状态口径：✅ 已落地（后端/前端切片均有完成情况注记）｜🟡 部分落地（剩余见末列挂账）｜⬜ 待启动/待拍板。
> **工期/人员/计划起止三列原文未给，一律留【待补】，由负责人提供资源信息后填写；不推测工时。** 实际落地时间参考：M0–M8、M11 后端切片为 2026-09-13；M10/M12 各切片（Q76–Q111）为 2026-09-14 ~ 09-16；Q112 契约文档回填、Q113 三族读口补闸、Q114 客户 templates/settings 收尾、Q115 基准 HTML 收口、Q116 段12 P4 首片为 2026-09-17，Q117 文档审计/Q118 挂账四项收口/Q119 段12 多语言/Q120 段12 AI 质量分为 2026-09-18，Q121 段12 复检语义级检测为 2026-09-19（以 git 历史与 02 C1 日志为准）。
> 截至 2026-09-19（最新 **Q123（B3）** 段1 客户目标语言录入控件落地（客户 PATCH /api/intakes/{id}/target-languages + 无闸 GET /api/content/languages、ProductSpaceView 回显，零迁移，528→534 passed）；**Q122** 客户「内容生产与发布」功能页 + Q56-a 客户人工改稿落地（无闸列表/详情/PATCH body 三端点、新事件 manual_resubmit revising→review、前端内容列表/详情/审阅/改稿岛与第八个 checker check-content，零迁移，523→528 passed）；**Q121** 段12 复检第②项语义级检测落地（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK 内嵌生成、纯 advisory 不阻断、零端点/零列/零表，迁移 0031 纯种子，511→523 passed）；**Q120** 段12 AI 质量分 Q57 真打分 + Q56 上限配置化落地（模型网关第 8 场景 ARTICLE-QC 内嵌生成、纯 advisory 不阻断、阈值/重生成上限配置化，迁移 0029/0030，494→511 passed）；**Q119** 段12 多语言 Q58 落地：content_languages 语言清单配置化、发布位市场∩产品目标语言交集、content_products 每语言独立成品唯一约束〔迁移 0028〕，483→494 passed；此前 **Q118** Q117 挂账四项收口：4 个管理面 GET 补 query actor 闸、publish_slots.gate V1 人工直编口径回填、product_spaces 两道 DB 级 FK〔迁移 0027〕、check-settings 接入 CI〔六→七 checker〕，472→483 passed；另修复 Alembic 版本表 VARCHAR(32) 容不下 0021 起 revision id 致全新 PG 全链截断的缺陷；Q117 文档一致性审计为纯文档回填、数字不变；前序 Q116 段12 P4 首片，迁移 0025+0026，457→472；Q115 迁移 0024；Q114 跨栈加法无迁移；Q113 +10 测试；Q112 纯文档无测试变化）：后端 **534 测试全绿** / eval 101 案（golden 21） / 前端 **8 个 checker**（check-tokens/nav/content/products/workbench/compliance/admin/settings）+ tsc（86 token 计数，Q122/Q123 前端零新 token）；**Alembic 迁移头 0031**（Q122/Q123 零迁移、业务物理表 55）；CI（GitHub Actions，Q111；**Q118 起 frontend 七 checker、Q122 起八 checker 全接入**）已在 GitHub 实际触发，PR #38/#39 合并均全绿（backend pytest+ruff+eval、frontend tsc+checker）；Q119–Q123 提交本地 dev 未 push，尚无新 run。

| 包 | 范围 | 落地状态 | 关键 Q / 迁移 | 剩余挂账（不改变 V1 范围者注明去向） | 工期 | 人员 | 计划起止 |
|---|---|---|---|---|---|---|---|
| M0 | 规格仲裁（前置） | ✅ 完成（2026-09-13） | Q2/Q22/Q60 转正、Q22a/Q22b、C1.16 素材确认、Q74 | PS.lifecycle 迁移触发条件、候选实体多租户可见性、候选状态完整枚举（多租户设计时销）；KUP-PROPOSE 随段 13 | 【待补】 | 【待补】 | 【待补】 |
| M1 | 段1 产品录入 | ✅ 后端 + 客户/管理前端 | 迁移 0001；Q74；前端 Q98/Q106/Q107 | G2 common 字段基线（Q115 工程定稿：迁移 0024 已落 12 个；余 6 个【原文未给出，待补】） | 【待补】 | 【待补】 | 【待补】 |
| M2 | 段2 C1 识别 | ✅ 后端 + AI 通道 | 迁移 0002；Q79/Q81 占位替换、Q82/Q84 真 LLM（0014/0016） | source_route L4 枚举【原文未给出，待补】，v0.1 不接模型值 | 【待补】 | 【待补】 | 【待补】 |
| M3 | 段3 字段池规划 | ✅ 后端 + AI 通道 | 迁移 0003；Q78/Q85（0017） | DIM-SOURCE 七路真实 fan-out 连接器随 V2（Q91 原语已备） | 【待补】 | 【待补】 | 【待补】 |
| M4 | 段4 字段下原子 | ✅ 后端 + AI 通道 | 迁移 0004；Q80/Q86（0018，pgvector/embedding） | atom.cluster_line=0.9 借 Q10 同义线【原文未给出，待补】；真 embedding 模型接入时维度按模型实际演进 | 【待补】 | 【待补】 | 【待补】 |
| M5 | 段5 PWC 构建 | ✅ 后端 + AI 通道 + 补货闭环 | 迁移 0005；Q76（0012）、Q83（0015）、Q87 worker、Q90 退避（0020） | Q54 评分待段 13 回流校准（临时 0.4/0.3/0.3，仅排序不做门槛） | 【待补】 | 【待补】 | 【待补】 |
| M6 | 段6 PWS 冻结 | ✅ 后端 | 迁移 0006；Q28–Q33 | Q30 换版处置结构实体随 M8/段12 | 【待补】 | 【待补】 | 【待补】 |
| M7 | 段10 合规清洗 | ✅ 后端 | 迁移 0007；Q48–Q51 | —（法审 48h 已落；五型候选审核 SLA 时限见 M10 挂账） | 【待补】 | 【待补】 | 【待补】 |
| M8 | 段11 FCW 组装 / final_id | ✅ 后端（V1 主链终点） | 迁移 0009；七 Guard、E1.1 publishFCW、Q52–Q55 | FCW 消费侧契约【原文未给出，待补】；draft 通道 V1 未开（直接 published）；段 12 随 V2 P4 | 【待补】 | 【待补】 | 【待补】 |
| M10 | 横切最小集 | 🟡 主体落地 | 切片 a 配置中心 0010、b SLA/调度 0011、c 缓存引导（无迁移）、d RBAC Q75、e skill7 Q76（0012）；Q88 入站 Key 0019、Q89 单实例锁（无迁移）、Q91 DAG 原语（无迁移） | config/g2-candidates/skill-prompts 三族读角色 ✅ Q113 已补闸（config/prompts=platform_admin、g2=dictionary_admin）；五型候选审核 SLA 具体时限（72h 等）【待业务方补】；配置缓存多副本广播、fencing token、Streams 细粒度认领略 V2；effect-callback 本体随段 13 P3 | 【待补】 | 【待补】 | 【待补】 |
| M10-Q | 质量两件套（Evaluation + Golden） | ✅ 落地 | Q77；eval 101 案 + Golden Cases，闸门 `backend/.venv/bin/python eval/runner.py` | Simulation Sandbox 后置 V2；新 Skill 随切片补案 | 【待补】 | 【待补】 | 【待补】 |
| M11 | 段7/8 静态底表 + 段9 三包静态实例 | ✅ 后端 | 迁移 0008；Q34–Q43、Q45/Q52 | 14 平台目录原文已随基准 HTML 永久丢失（Q115 已收口，不再等找回）；slotType 默认值约 40 列明细【待补】 | 【待补】 | 【待补】 | 【待补】 |
| M12 | V1 平台厚度 | 🟡 主体落地 | Q92 双驾驶舱、Q93/Q103/Q104 统一审核台、Q94 SLA 挂接（0021/0022）、Q95/Q105 租户 Onboarding（0023）、Q96–Q102 前端脚手架/页面、Q100 CSV、Q106–Q111 运营页/鉴权/CI、Q112 契约文档回填、Q113 三族读口补闸 | templates/settings 两客户页已按 Q114 拍板（settings=V1 只读账户面板+语言展示+五子项 V2 徽标，templates=V2 徽标占位；content/analytics/social-accounts 已裁带 V2 徽标，V1 不做）；付费档"第 5 档"额度【待补】；CSV JSON 形态/异步导出、日期筛选器与其余驾驶舱不进 V1 | 【待补】 | 【待补】 | 【待补】 |

> 排期填写规则：① 只填负责人提供的工期/人员/日期，禁止倒推编造；② 新切片落地后在本表更新状态与 Q/迁移，并在 02 追加 C1/Q 记录；③ V2（P1–P5，见 §2.3）与 V3（§2.4）未启动，排期随 V1 收尾另行排定。

| 包 | 范围（01 文档） | 关键输入 | 验收标准（原文依据） | 依赖 |
|---|---|---|---|---|
| M1 | 段1 产品录入 | G2 18 通用字段、WF-01 6 Skill、productIntake15 状态机 | 申请单可走 15 态全流转；类目创建中态（Q5）；录入缺字段阻断（D7.1 闸门1） | M0 |
| M2 | 段2 C1 识别 | 文本三信号（0.50/0.33/0.17，Σ=1 强校验）、行业阈值表 CRUD、三分支、C7 四层兜底 | conf 单指标判定（Q1）；运营 ops_assist 待办 72h 升级（Q3/Q4）；C7 Layer3 禁止发明新字段（Q68 无 fid:'-'） | M1 |

> **M2 完成情况（2026-09-13，后端切片）**：✅ 验收标准全部达成——①信号权重配置表（c1_signal_weights，启用行权重 Σ 必须精确等于 1 否则拒绝保存，种子 name 0.50/brief 0.33/sellpoint 0.17，图像信号后置，Q2）；②行业阈值后台 CRUD（c1_industry_thresholds，general 默认档 is_default 不可删 409、每次改动写 writeAudit，种子 medical 0.90 敏感/electronics 0.80/general 0.85，Q7）；③三分支（c1_records 留痕：conf≥阈值 wf01_confirm+系统 auto_confirm 直送已提交；[0.6,阈值)或 Top1-Top2 差<0.1 出 ops 待办（72h due，sweep 升级 escalated）→ops_confirm/全否 to_cold_start；conf<0.6 必须带 B2 候选单进类目创建中；行业匹配不到兜底 general，Q1/Q3/Q4/Q5）；④C7 四层兜底（g1_categories/templates 最小切片：L1 approved 模板缓存→L2 同父 product_count 最大兄弟继承→L3 G2 active 覆盖率（<60% 降级，Q6）→L4 提案进 g2_field_candidates pending_gate；Q68 fid:'-' 在模板写入与 L4 提案两个入口均拦截）。交付：迁移 0002（8 表 + 权重/阈值种子，PG16 up/down/up 实测）、26 条测试全绿（纯逻辑单测 + HTTP 集成）。**未含**：~~WF-01 六 Skill（PARSE/UNDERSTAND/CAT-RECOG/TYPE-MATCH/MISSING-INFO/RISK-TAG）AI 通道（M2 以结构化入参占位，随 M10 skill7 候选通道接入）~~ **CAT-RECOG→c1_recognition 已于 Q79 接入、TYPE-MATCH→c7_layer4（整 C7 解析请求）已于 2026-09-14 经 Q81 接入**（见下文 WF-01 两段完成情况；真 LLM 信号提取与 Layer4 新字段生成本体仍挂 Q67）；待办定时调度与看板高亮（sweep 逻辑已就绪，调度随 M10 通用 SLA 引擎 Q49）；G1 完整状态机 CRUD/类目管理（09 在 V3）；0.6/0.1/72h/60% 等拍板值暂为代码常量，M10 配置中心迁移（02 §C2 已登记）。
| M3 | 段3 字段池规划 | WF-02、PT-FP-PLAN-V2.0、维度来源路由表、Gate | ≥1 product_attribute 维度 + 敏感行业 ≥1 risk_control（Q11）；维度 3-8 越界处置（Q12）；<0.85 细看线（Q9） | M2 |

> **M3 完成情况（2026-09-13，后端切片）**：✅ 验收标准全部达成——①Q8 维度来源路由表（fp_source_routes 后台 CRUD + writeAudit，种子 6 路：用户输入/通用灵感库/产品专属灵感库/类目模板/G2 高频/合规风险面，被维度引用的路由禁删 409，运营可继续增删改）；②方案提交（field_pools/fp_dimensions，一品一池唯一约束）：每维度强制角色 + 启用路由 + 非空依据 source_ref（line 14081/14133 红线），持既有 fid 必须为 active G2 字段否则 illegal_fid（Q68 延续），无 fid 的新字段落 g2_field_candidates（source_layer=wf02_dim_source，同名候选全局复用不重复建）；③PT-FP-PLAN 结构校验：≥1 product_attribute 恒强制，risk_control 仅敏感行业按 PS.sensitive_industry 强制（Q11）；Q12 越界——>8 按 confidence 留 Top8、余者入 backup 备选档（不删，可人工捞回，满 8 时挤回最低置信），<3 记 below_min 标不达标转人工、不自动补；Q9 全量走 Gate、conf<0.85 仅标 needs_detail"需细看"；Q10 相似度≥0.9 仅标 dup 疑似重复，机器只标记、Gate 终裁；Q15 目标原子数 15-30 随池落库；④WF-02 HumanGate：pending_gate→approved/rejected，approve 需 product_reviewer 角色（06 §4 产品审核员）且方案 compliant，approved 后锁定不可改/不可重提，rejected 可重提（旧维度行清除、审计保留）；⑤Q13 转正端点：dictionary_admin 角色把候选提升为 G2 正式 fid（fid 冲突 409、重复转正 409），转正后自动回填引用该候选的池维度。交付：迁移 0003（3 表 + 6 路种子，PG16 up/down/up 实测）、45 条测试全绿（7 纯逻辑单测 + 13 HTTP 集成）。**未含**：WF-02 三 Skill（FIELDPOOL-PLAN/DIM-SOURCE/DIM-MERGE）AI 产维度通道（M3 以结构化入参占位，随 M10 skill7 接入）；灵感库两层实体（通用/产品专属，M3 仅把它们作为路由来源登记，库本身随知识层建设）；G2 cat 完整取值原文未给（转正默认 cat=extension 占位，非 common，待基准 HTML 找回后核对，不污染段1 完整度闸门）；候选转正保护期加权（第二期）；0.85/0.9/3-8/15-30 拍板值暂为代码常量，M10 配置中心迁移（02 §C2 已登记）。
| M4 | 段4 原子拓展 | WF-03、approveAtomGuard 10 项、AtomConflict、evidence、risk 双轨 | 10 项 Guard 全过才正式化；critical 单条审（Q70）；evidence 7 天超时自动驳回可复活（Q18）；产品事实原子引用数=1 | M3 |

> **M4 完成情况（2026-09-13，后端切片）**：✅ 验收标准全部达成——①Q48 统一合规词库先行落地（横切表 `compliance_wordlist` + `/api/admin/compliance-wordlist` CRUD：word/level(critical\|high)/action(ban\|downgrade)/downgrade_target/country/industry/生效期/status，operations\|internal_compliance 角色，downgrade 缺 downgrade_target → 422，DELETE=软归档，全部 writeAudit；M4 匹配为确定性子串匹配，按行业/生效期过滤）；②拓展批次落 4 表（`atom_batches`/`atom_candidates`/`atom_conflicts`/`product_atom_instances`）：PT-ATOM-EXP-V1.3 仅产候选、不直接写正式实例；Q14 批次上限敏感 20/非敏感 50（提交时可显式覆盖），Q15 已通过+冻结正式原子达 `target_atom_max` 时 AI 批次 409 停拓、manual 批次不受限，同批 normalized 去重，维度必须属本池 selected；③Q17 risk 双轨：命中词表按词表等级强制定级（risk_source=wordlist，人工改判端点对词表候选 409），未命中走 AI 判级，人工可复核改判并重建冲突集；critical/high 之外的 medium/low 为【实现补】等级（原文只给 critical/high）；④AtomConflict 三类齐：disabled_expression（critical+ban→blocked，驳回记 atom.reject_disabled_expression 合规审计）/ high_risk_single_review（high→pending_gate）/ evidence_required（high 无证据→blocked）；⑤approveAtomGuard 落 9 项可机检（属当前 PS / 属选中池 / 池 gate=approved / 状态非驳回 / 态可审 / 无 blocked 冲突 / approved_id 空 / 证据存在；⑩writeAudit 由服务在正式化前保证），⑨Q70 high/critical 禁批量——批量端点任一命中整批 409、单条可过，违规以 409 detail 码数组返回；⑥Q18：无证据候选 pending_evidence + 7 天 due，补证据转 pending_review 并解 evidence_required，sweep（系统动作）自动驳回 reason=evidence_timeout（定时调度随 M10），仅超时驳回可复活、复活时重检事实原子全局唯一；⑦Q19：同义簇人工终裁 keeper，非 keeper 置 merged 作 alias 不删，keeper 正式化时并入 atom.aliases；⑧line 11189：产品事实原子（capacity/ingredient/concentration/wart_type/brand）(fact_type, normalized) 全局唯一，跨产品/跨租户在提交与复活两处拦截，通用表达原子不受限，正式实例 reference_count=1；⑨Q20 生命周期：freeze/unfreeze=operations（解冻不重审）、compliance-suspend/resume=internal_compliance（恢复须合规复核）、deprecate/archive=operations（仅 deprecated/rejected 可归档；废弃恢复=视同新原子重走审核，不做原地复活端点）、正式原子 reject=product_reviewer。交付：迁移 0004（含词表共 5 表，PG16 up/down/up 实测）、74 条测试全绿（16 纯逻辑单测 + 13 HTTP 集成）。**未含**：~~WF-03 四 Skill（ATOM-EXPAND/CANON/AFFINITY/CONFLICT-PRECHECK）AI 通道（M4 以结构化入参占位，随 M10 skill7 接入）~~ **已于 2026-09-14 经 Q80 切片接入**（M10 WF 替换切片 3/3，见下文 WF-03 完成情况；真 LLM 拓展生成本体仍挂 Q67）；evidence 超时 sweep 的定时调度（逻辑就绪，随 M10 SLA/调度）；Q51 词表生效期自动扫描与段 5/10 联动（随 M5/M7）；~~Q16 亲和度语义相似度计算（<0.5 标低亲和、无硬淘汰已落，相似度本体随 AFFINITY Skill）~~ 通道侧 AFFINITY Skill 已登记（Q80），语义相似度计算本体仍挂 Q67；20/50、0.5、7 天、15-30 拍板值暂为代码常量，M10 配置中心迁移（02 §C2 已登记）。
| M5 | 段5 PWC 构建 | WF-04、漏斗（预筛→检测→评分→限量）、contentGoals 字典 | 评分=(合理性0.6+多样性0.4)×品类乘数（Q22/Q22a/Q22b）；重合≥0.8 标重（Q23）；状态流转（Q24）；库容 100（Q27） | M4 |

> **M5 完成情况（2026-09-14，后端切片）**：✅ 验收标准全部达成——①Q21 漏斗：`POST .../pwc/funnel` 单批限量 50（可显式覆盖上限校验），预筛（原子须为本 PS 已 Gate 通过的正式原子、同租户、组合跨 ≥2 维度【实现补：最小宽度原文未给值】、同批重复组合集 422、跨 PS/跨租户原子 422、goals 须为在用字典码）→检测（合规复用 Q48 词表，按 PS 行业/生效期对子串匹配，ban 命中→blocked 不参与评分，downgrade 命中落档 compliance_result 不阻断；逻辑/搭配检测通道留插拔位）→评分→限量；前置要求字段池 gate=approved 否则 409。②Q22/Q22a/Q22b 评分：score=(合理性×0.6+多样性×0.4)×品类乘数；合理性=logic×0.5+fit×0.5（w1=w2=0.5），多样性=1−与待用池最大重合度（空池=1.0），品类乘数 V1 恒 1.0；logic/fit 任一缺失→score=null、明细记"—"、score_incomplete=true，只进人工 Gate 不凑默认分；score 仅排序，无自动通过门槛；组合重合度实现为原子集合 Jaccard【实现补，Q22b/Q23 共用，无向量相似度】。③Q23：与待用组合重合 ≥0.8 标疑重，保留高分者、低分者 is_backup 备选并记 dup_of，人工 Gate 可改判放行；同完全组合的重复提交按结构校验 422 拦截。④Q24 状态流转与取用：`POST .../pwc/consume` 按 score 降序（null 末位）消费，写 usage_record；同 平台+账号+发布位 三元组不重复（同键自动跳过取次条，用尽 409），跨平台可复用；同平台 7 天内 ≥3 次→该（组合,平台）置 cooldown 14 天（冷却状态挂 per-platform state 表，不影响别的平台），到期 sweep 回 available（服务函数就绪、调度随 M10）；配置 target_platforms 后全部目标平台均用过→组合终态 used（平台清单来源【原文未给出，待补】，默认不配则不产生 used）；高复用次数超 high_reuse_n 标高复用旗标；爆款 V1 仅手工标（operations），Q61 中位数×5/90 天自动检测后置 M10；PWC 严禁跨产品/跨租户复用，消费去重查询带 tenant 过滤。⑤Q25 contentGoals 字典：`content_goals` 表种子 5 码 ENGAGEMENT/CONVERSION/EDUCATION/TRUST/RETENTION（line 1090），dictionary_admin 角色维护（颜色/配比上下限，ratio_min≤ratio_max 否则 422，软归档后漏斗与消费不可再选），录入与 PWC 共用同一字典、消费时按 goals 交集过滤；⑥Q26：source=manual 手拼组合跳过预筛但合规检测/评分/人工 Gate 不豁免，ban 命中一样 blocked；⑦Q27 库容：每 PS `pwc_pool_configs` 默认容量 100，Gate 通过入库时超限 409，capacity=null 配为无上限；⑧Q71 消费契约：响应带 pool_ready_count、pool_health（target100/low70/critical50，line 1451）、restock_hint（待用 <50 提示补货；critical→target 自动补货与 5 分钟防抖后置 M10）；⑨Gate：product_reviewer 裁决 pending→approved(ready)/rejected(archived)，blocked 不可批（409），全部写 writeAudit（funnel_run/approve/reject/consume/hot_mark/pool_config/goal 维护）。交付：迁移 0005（6 表 + 5 码种子，PG16 up/down/up 实测）、104 条测试全绿（13 纯逻辑单测 + 17 HTTP 集成）。**未含**：WF-04 三 Skill（COMBO-GEN/SCORE/DUP-CHECK）AI 通道（M5 以结构化入参 logic_score/fit_score 占位，随 M10 skill7 接入）；冷却到期 sweep 与 critical 自动补货（5 分钟防抖）的定时调度（逻辑就绪，随 M10 SLA/调度）；Q61 爆款自动检测；Q23 语义向量相似度（V1 用 Jaccard 集合重合）；逻辑/搭配检测插件本体；target_platforms 平台清单来源待补；50/100/70/50/0.8/0.6/0.4/7 天/3 次/14 天等拍板值暂为代码常量，M10 配置中心迁移（02 §C2 已登记）。
| M6 | 段6 PWS 冻结 | pwsReadiness 5 项、BO-07 红线、版本管理 | 5 项就绪门全绿出待办（Q28）；frozen 后下游只读；重冻三档（Q29）；换版四行（Q30）；revoked 急停（Q32） | M5 |

> **M6 完成情况（2026-09-14，后端切片）**：✅ 验收标准全部达成——①pwsReadiness 5 项（line 2634）：`GET .../pws/readiness` 机械求值 ①存在 gate=approved 字段池 ②已批准正式原子 ≥3 ③active PWC ≥1（gate_status=approved 且 status∈ready/used【实现补：active 口径】）④未解决 blocked 冲突 =0（AtomConflict→候选属本 PS、status=blocked、resolved_at 空）⑤pending_gate 字段 =0（本 PS gate=pending_gate 的字段池计数【实现补：字段侧挂起口径】），返回每项布尔 + 计数明细。②Q28：`POST .../pws/evaluate` 全绿时系统只**出待办**（OpsTodo todo_type=pws_ready，assignee_role=whitelist_owner，due=now+7 天，开放/已升级待办幂等不重开），复用 M2 sweep 自动升级；冻结只能 BO-07 人工执行，无全自动冻结；冻结成功后开放待办置 resolved(resolution=frozen)。③BO-07 红线：冻结/重冻/急停仅 whitelist_owner 角色（英文码【实现补】，line 7674 仅给 BO-07/白名单负责人），非属主 403；冻结前再过一遍就绪门，不全绿 409 且 detail 回带 5 项明细；全程 writeAudit（pws.readiness_todo/freeze/refreeze/revoke）。④Q31 版本：首版 v1.0，主版本号 vN.0 递增（v9→v10），(product_space_id, version) 唯一，同刻仅 1 个 is_active 冻结版；`GET .../pws` 版本列表（主版本号倒序）、`GET /api/pws/{id}` 明细。⑤不可变快照：冻结时物化 3 原子类 + PWC 类明细行（pws_snapshot_items 有序 seq + payload JSON），源头原子废弃/池变更后旧版内容不变；旧版 superseded/revoked 只读。⑥Q29 三档：已存在 active 冻结版时重冻必须给 reason_code（缺失 422、未知码 422）；unrelated_change→none 档 409 RefreezeNotNeeded；asset_increment→suggested；atom_compliance_suspend/atom_deprecated/wordlist_hit/product_fact_change→forced；重冻即建新版（不在旧版上改），旧版置 superseded + superseded_by 并双写 freeze_log（supersede/refreeze）。⑦Q30 换版处置结构返回：superseded_version、draft_fcws_voided（段11 未实现，空列表占位）、unpublished_pending_recheck（仅 forced=true，强制档重生成）、published_marked_outdated（已发布内容对象段12 未实现，计数 0 占位，不溯及既往）。⑧Q32 急停：`POST /api/pws/{id}/revoke` 仅 active frozen 可作废（否则 409），is_active=False/status=revoked/reason/by/at 落库 + freeze_log；作废后可重冻新版（Q32"作废后重冻新版"），仍须 5 门全绿。⑨Q33：指纹=sha256(排序 atom_ids + pwc_ids)，冻结时仅在**同租户其他 PS** 范围内比对返回 dup_hints，跨租户永不比对、提示永不阻断。交付：迁移 0006（pws_snapshots/pws_snapshot_items/pws_freeze_logs 3 表，PG16 up/down/up 实测，JSON/JSONB 变体）、118 条测试全绿（5 纯逻辑单测 + 9 HTTP 集成）。**未含**：WF-05 Skill（12 #15 占位，Skill 名原文未列；段6 为纯机械门，V1 无 AI 通道）；Q51 词表生效即扫触发 wordlist_hit 强制重冻（扫描随 M7/M10）；pws_ready 待办 7 天升级的定时触发（sweep 逻辑随 M2 已就绪，调度随 M10 SLA）；Q30 ②③④的 FCW/未发布/已发布内容实体处置（随 M8 段11/段12 回填）；Q53 FCW 组装须引用 active 版本（随 M8 落 Guard）；7 天/3 个/1 个等拍板值暂为代码常量，M10 配置中心迁移（02 §C2 已登记）。
| M7 | 段10 合规清洗 | WF-08、Q48 合规词库、block_required、法审 | 三关卡同源词库；block_required 一票否决阻断段11；法审待办卡 Guard⑥（Q49）；国家>平台>底座优先序（Q50）；词表生效即扫（Q51） | M6 |

> **M7 完成情况（2026-09-14，后端切片）**：✅ 验收标准全部达成——①三关卡同源 Q48：段10 CCR 直接复用 M4 `compliance_wordlist` 同一张表（横切 `app/core/compliance_wordlist`，零复制），按行业+市场取词（country 为空=全市场），迁移 0007 增 `layer` 列（country/platform/base，存量回填 base）承载 Q50。②block_required 一票否决：ban 命中即 `status=blocked/block_required=true`，`GET /api/pws/{id}/ccr/gate` 机械视图返回 block_required/cleaning_passed 供段11 Guard② 消费（M8 接入即阻断组装）。③Q49 法审：CCR 运行时按 PS.industry_tag 命中活跃 CP-LAW 领域码（sensitive_industry 旗标兜底 sensitive_other）幂等自动触发 `law_reviews`（一冻结版一条，pws_id 唯一）+ internal_compliance 法审待办（48h due，sweep 可升级）；线下律师结论经 `POST /api/law-reviews/{id}/decision` 录入（已决再判 409），approved 才放行 Guard⑥、rejected 维持否决，待办同步 resolved。④Q50/Q36：同词命中多条按 layer 国家>平台>底座裁决（不可配置），同级从严（ban 严于 downgrade、critical 严于 high）；分市场独立判（country 维度报告行，空=不按市场细分基线）。⑤Q51 生效即扫：词表 POST/PUT 保存生效同事务扫全部 active frozen 快照（行业过滤+大小写不敏感子串），命中幂等产出 `wordlist_rescan` 待办（whitelist_owner，detail.reason_code=wordlist_hit），响应附 `q51_impacted`；扫描只触发不自动重冻——重冻仍须 BO-07 人工（红线 line 7674，与 M6 一致）。其余落点：降级只出建议须 internal_compliance approval（`POST /api/ccr/{id}/approve-downgrades`，仅 downgrade_pending 可批否则 409，批后 cleaning_passed）；报告 append-only（重跑产新行，PT-COMPLIANCE-V2.0 历史不可 mutate，取最新按 created_at+uuid1 兜底同秒）；CP-LAW 领域小表全 CRUD（internal_compliance，code 唯一 409，软归档，迁移种子 6 码）。交付：迁移 0007（layer 列 + cp_law_sensitive_domains/law_reviews/ccr_reports 3 表，PG16 up/down/up 实测）、134 条测试全绿（8 纯逻辑单测 + 8 HTTP 集成）。**未含**：WF-08 三 Skill（COMPLIANCE/CLAIM-DOWNGRADE/LAW-REVIEW）AI 通道（V1 为确定性机械判定，随 M10 skill7 接入）；法审 24h 黄/48h 升级统一 SLA 引擎与定时调度（逻辑可 sweep，随 M10）；Q51 对 draft FCW/未发布成品的扫描（实体随 M8 段11/段12 接入后回填）；未来生效词条的定时扫描（随 M10）；48h/7 天等拍板值暂为代码常量，M10 配置中心迁移（02 §C2 已登记）。
| M8 | 段11 FCW 组装 | WF-09、7 项 Guard、唯一出口 E1.1 | 7 项 Guard 全过才生成 final_id（Q52/Q53）；全自动发证 + 审计（Q55）；score 仅排序不做门槛（Q54）；越权写 final_id=违反协议 | M6+M7（与 M5 串行）；PCP 静态包见 M11 |
| M10 | 横切最小集 | skill7 通道、writeAudit、RBAC 红线、配置中心（DB+广播热更新）、SLA 引擎 | AI 仅产候选人工 Gate；红线操作角色+Gate；40+ 配置项后台可配；待办 SLA 统一引擎（Q49/Q70） | 贯穿 M1-M8 |
| M10-Q | 质量基建两件套（14 §2.6 定稿纳入 V1） | Evaluation Dataset（每 Skill 测试集/回归）+ Golden Cases；落 15 `eval/`，与 16 测试策略、17 staging 回归闸门联动 | 关键 Skill 有 Golden Cases；staging 部署跑 Evaluation 回归防 S2"Skill 从未运行"；Simulation Sandbox 后置 V2 | 随各 Skill 落地，进 staging 闸门 |
| M11 | 段7/8 静态底表（FCW 6 路输入最小切片） | publish_slots / platform_rules / slot_rule_by_type（16 张平台底表基础版）+ strategy_fields_17（13 张策略底表基础版，静态权重） | 后台 CRUD + 客观字段来源链接（Q35）；规则 4 层选择器/冲突静态查重（Q36）；17 权重 Σ≤1.0 统一校验器（Q40）；平台间不共享（PT-PCP-V1.5）；配置实例带 product_space_id/tenant_id 过 Guard④⑤（Q52）。**不做**：动态信号、每周重算、fit_score 学习、PLATFORM-ADAPTER AI Skill（均移 V2） | M6（PWS 冻结后即可挂静态 PCP）；M8 消费 |
| M12 | V1 平台厚度（D9 横向吸收） | 审核工作台（候选池）、租户管理 + 客户入驻 Onboarding、客户前端 8 菜单基础版、CSV 导出对接中台、Skill 注册表 + 自研编排器最小集（06 §4.2） | 候选-审核全链在台内闭环（Q70 批量/单条）；多租户隔离（Q33）；中台 CSV 只消费 final_id（Q100 已落：`GET /api/exports/fcw.csv` final_id 单列同步导出，D4 CSV 契约收口）；WF/Skill/PT YAML 数据驱动可注册；**2 个驾驶舱**：Token 成本（Q67 单位经济）+ 人工审核工作量（SLA/积压）。客户前端页面遵循 18 号文档（✅ Q96 定稿；Q96 i18n/tokens 脚手架、Q97 应用外壳/D5 八菜单导航、Q98 产品中心（列表/新建 draft/只读详情）、Q99 工作台（录入单总览+状态聚合）已落，Q100 中台 CSV 导出已落，Q101 合规风控只读页已落，Q102 管理端独立布局+两驾驶舱只读页已落，Q103 统一审核台前端页（队列+筛选+批量通过）已落，Q104 逐条裁决（confirmed/rejected/modified + payload 替换编辑器）已落，Q105 Q95 租户管理 + Onboarding 前端页（列表/开通/改套餐/暂停恢复 + 详情派生进度）已落，Q106 录入单 15 态客户操作（提交/补参/确认额度/补资料重提）+ draft 产品名编辑已落，Q107 管理端跨租户录入单运营队列（GET /api/intakes/ops-queue + /admin/intakes 列表/详情，6 个 ops 事件复用 transitions）已落，Q108 管理端待办 SLA 看板（GET /api/admin/sla/todos 补 platform_admin 闸 + status 四值白名单 + /admin/sla-todos 纯只读页）已落，Q109 管理面读端点鉴权审计+8 个裸 GET 同型补闸已落，Q110 审核工作量页空态缺键热修已落，见 Q96–Q110 完成情况；Q111 CI workflow（GitHub Actions：pytest+ruff+eval/tsc+前端 checker；Q118 起七 checker 全接入并加「磁盘 check-*.mjs 集合==CI 接线集合」一致性断言，详见 17 CI/CD）已落，Q112 契约回填、Q113 三族读口补闸收口、Q114 客户 templates/settings 与五型 SLA 占位、Q115 基准 HTML 收口+G2 工程 fid（迁移 0024）、Q116 段12 P4 首片（迁移 0025/0026）、Q117 文档一致性审计回填、Q118 审计挂账四项收口（迁移 0027）已落，见下 Q111–Q116 完成情况、§2.2 基线行与 17 CI/CD）：无硬编码业务文案/状态词、色值走语义 token | 贯穿 M1-M8 |

> **M11 完成情况（2026-09-14，后端切片；实现序先于 M8——段11 七 Guard 必须有静态 PCP/三包实例可消费，属 Q73 路线图内的实现顺序调整，非业务裁决）**：✅ 验收标准全部达成——①Q35 后台 CRUD：`publish_slots` 发布位档案（operations 角色，code 唯一 409、软归档、source_url 客观来源、四维分 score_source=manual_eval 明示人工评估）。②Q36 4 层选择器：slot_type/platform/platform_slot_type/slot 低→高 + country 横切；层级必填字段校验（422）；同级同条件不同结论保存时静态查重（409 回带冲突行，`overwrite=true` 重发归档旧行=原文"覆盖旧规则/放弃保存"选择）；match 裁决高优先级覆盖、同级 blocked>partial、native 默认不存。③Q40 统一校验器：PCP 17 键 Σ≤1.0、fit 目的权重 4 维 Σ=1（硬拒绝不归一化），保存即校验。④PT-PCP-V1.5 平台不共享：唯一约束 uq_pcp_ps_platform(product_space_id, platform)，同 PS×平台仅一条；Q39 四模板（short_video/community/photo_text/ecommerce）种子派生，17 键 Σ=1.0（实现期初值草稿，Q39 明确起草放实现阶段）。⑤Q52：PCP 与三包实例均带 product_space_id/tenant_id，供 M8 Guard④⑤。⑥段9 三包静态最小切片：`packages`（csp/cstp/cep）按产品×平台×目的×包型一条 active（409 防重），payload 键按 04 §2.17 定死（CSP 六键/CSTP struct/CEP 四键，缺多键 422），各带 conf/gate——Q45 V1 人工配置复用口径；这是把 V2 P2 的实体最小化提前到 V1（仅静态实例，不做 AI）。⑦Q34 fit_score 派生不落库，目的缺权重矩阵返回 null+incomplete=true 不凑分。交付：迁移 0008（publish_slots/goal_fit_weights/platform_rules/slot_type_defaults/pcp_templates/pcp_weight_tables/packages 共 7 表，种子单一事实源 `seeds.py`，PG16 up/down/up 实测 JSONB bulk_insert 正常）；147 条测试全绿（M1–M7 134 + 7 纯逻辑单测 + 6 HTTP 集成）。**未含（事实纪律挂账）**：14 平台 100+ 发布位目录与 slotType 13 类约 40 列明细——原文在基准 HTML line 1098-1221/1428，基准文件不在仓库，全部标【原文未给出，待补】，不虚构种子；fit 权重仅种子 ENGAGEMENT/CONVERSION（种草→ENGAGEMENT 为【实现补】目的码映射），EDUCATION/TRUST/RETENTION 待补；跨层规则 R-X01~X05 未实现；Q37 动态信号、Q41/Q42 每周重算+AI 对照单、fit_score 学习、PLATFORM-ADAPTER AI Skill、WF-07 AI 选包与 20 次重配、usage_count 递增均按任务包约定移 V2（M11 仅留 Q42 人工直编通道，无幅度限制有审计）。

> **M10 完成情况（切片 a：配置中心底座，2026-09-14，后端切片；M10 整体未闭合）**：✅ 本分片验收——"40+ 配置项后台可配"+"版本/回滚/审计"先于消费方落地：①统一配置表 `config_items`（key PK / typed JSONB 值 / validation min·max·choices / source_ref 出处 / version）+ 版本子表 `config_item_versions`（uq(key,version)，种子即 v1，确定性 uuid5 version_id）；迁移 0010 播种 **54 项** 02 §C2 标量旋钮（c1/fieldpool/atom/pwc/pws/sla/fcw/content/feedback/platform/agent 各类，含 Q56/Q57/Q61/Q63/Q65 等后置阶段值——配置页面先行；已有专用 CRUD 表的阈值/权重/词库/字典/发布位/模型注册表等不入种子，无双事实源），PG16 up/downgrade-1/up 实测 54+54 行 JSONB 核对通过。②发布语义严格按 14 §2.4：coerce 类型/边界校验（bool 必须真布尔、int 拒浮点字符串）→ version+1 → 版本留痕 → writeAudit（`config.update`/`config.rollback`，entity_type=config_item）→ **after_commit** 后进程内快照原子切换；回滚=以新版本号重发历史值，历史 append-only；失败/回滚事务不动缓存。③`/api/admin/config` 五端点（列表+category 过滤/单项/历史/改值/回滚），仅 `platform_admin`（Q46/Q64 平台级管理员英文码【实现补】），403/404/422 口径齐；176 条测试全绿（+6 纯逻辑单测 +5 HTTP 集成）。**M10 剩余切片（挂账）**：①应用启动缓存引导 + M1–M8 代码常量（0.6/0.1/72h/20·50/0.8/7d3 次/14d/100·70·50/3·1/7d/48h/0.4·0.3·0.3/8000 等）逐项迁移读缓存；②通用 SLA 引擎 + 定时调度接通已就绪 sweep（M2 72h 升级、M4 evidence 超时、M5 冷却到期+critical 自动补货 5 分钟防抖、M6 pws_ready 7 天、M7 法审 48h 与未来生效词扫描、M8 任务异步化）；③skill7 AI 候选-审核通道（各 WF Skill 占位入参替换）；④RBAC 红线收口复核；⑤多副本配置变更广播（Redis pub/sub，V1 单进程仅进程内热更新）。

> **M10 完成情况（切片 b：通用 SLA 引擎 + 定时调度，2026-09-14，后端切片；M10 整体未闭合）**：✅ 本分片验收——Q49/Q70"待办 SLA 统一引擎"与各包挂账的 sweep 调度接通：①`app/core/sla/` 落引擎/作业登记/runner/调度器/路由五件：通用到期升级（ops_todos open 且 due_at 到期→escalated，审计动作按 todo_type 分流，M2 `escalate_due_todos` 改为薄包装保留既有端点/契约）、Q18 证据超时驳回、Q24 冷却到期回 available、Q51 未来生效词到点激活+补扫四个作业统一登记，顺序执行、每作业独立会话与提交、异常隔离回滚不阻断其余（测试覆盖坏作业不污染好作业）。②`POST /api/admin/sla/run` 手工触发回带每作业 changed/error；`GET /api/admin/sla/todos` 看板派生 sla_state（yellow 仅法审 created_at+`sla.yellow_hours` 24h，热读配置中心、未引导回退默认；其余类型黄色口径【原文未给出，待补】，到期前恒 green）。③Q51 未来生效词条全链路：Q48 API 开放 effective_from/effective_until（窗口倒置 422），新增 `activated_at`（迁移 0011，存量 active 回填 now()，PG16 up/downgrade-1/up 实测），未来生效创建即待补扫、到点作业置位并复用 rescan_for_entry。④lifespan asyncio 调度器默认 300s 一轮（LOOM_SWEEP_INTERVAL_SECONDS/LOOM_SCHEDULER_ENABLED 可控），轮内错误记日志不退出循环。183 条测试全绿（+3 纯逻辑/调度器单测 +4 HTTP 集成）。**仍挂账**：Q71 critical→target 自动补货（5 分钟防抖）——V1 无 skill7 AI 漏斗可触发，挂 skill7 切片，不构造虚拟候选；M8 任务异步 worker（V1 仍同步）；缓存启动引导与业务常量消费迁移；skill7 候选-审核通道；RBAC 红线收口（`/sla/run` 与既有 sweep 端点暂无角色闸）；多副本单实例调度锁/配置广播。

> **M10 完成情况（切片 c：缓存引导 + 常量迁移，2026-09-14，后端切片；M10 整体未闭合）**：✅ 本分片验收——切片 a 的配置中心从"可配"接通到"实际生效"：①消费侧统一入口 `app/core/config_center/knobs.py` 的 `knob(key)`：读进程内 `config_cache`，缓存未引导（测试/启动前）或键缺失时回落 `SEED_BY_KEY` 种子拍板值，默认值单一事实源仍是 seeds，不二次抄写。②M1–M8 硬编码拍板值全部迁移为零参访问器函数（模块状态字符串/原因码映射不动）：c1 冷启动 0.6/TopGap 0.1/ops 72h、c7 L3 覆盖 0.6、fieldpool 细看 0.85/疑重 0.9/3–8 维/目标原子 15–30（Pydantic 默认改 `default_factory`，按请求取值）、atom 20·50/0.5/7 天、pwc 50/100·70·50/5min/100/0.6·0.4/0.5·0.5/1.0/0.8/7 天 3 次/14 天、pws 3·1/7 天、ccr 法审 48h、sla 黄 24h、fcw 0.4·0.3·0.3；纯函数权重入参改 `None` 缺省并在函数内解析（显式传值仍可覆盖，供测试/未来分行业配置）。③启动引导：FastAPI lifespan 在调度器启动前以独立会话 `config_cache.reload()`，失败仅告警不阻断启动（knob 回落种子，healthz 与既有测试均不依赖缓存引导）。无 schema 变更（无新迁移）。188 条测试全绿（+3 knob 单测：种子回落/热更翻转冷却判定与池档位；+2 lifespan 集成：DB 引导取到覆盖值、引导失败进程仍起且回落种子；既有 1 条常量溯源测试改访问器口径）。**仍挂账**：skill7 AI 候选-审核通道（各 WF Skill 占位入参替换；Q71 critical→target 自动补货 5 分钟防抖随该切片，不构造虚拟候选）；M8 任务异步 worker（V1 仍同步）；RBAC 红线收口（`/sla/run` 与既有 sweep 端点暂无角色闸）；多副本配置广播/单实例调度锁（V1 单进程仅进程内热更新）。

> **M10 完成情况（切片 d：RBAC 红线收口，2026-09-14，后端切片；M10 整体未闭合）**：✅ 本分片验收——05 §1.4 切片 b 挂账"RBAC 收口在 M10 后续切片"销账（Q75）：①统一入口 `app/core/rbac`：6 个角色常量 + `require_any_role(actor, *roles)` + 单一 `PermissionDenied`（路由统一 403）；闸放服务层，定时调度等系统内部调用不经 HTTP 不受闸。②新裁决收口：`POST /api/admin/sla/run` 与既有 `POST /api/admin/ops-todos/sweep` 手工触发归 **platform_admin**（两端点请求体新增/补齐 `actor`，缺省 422）；G1 `POST /categories`、`PUT /categories/{id}/template` 归 **dictionary_admin**（CategoryCreate 请求体新增 `actor`）；`POST /pws/{id}/ccr/run` 归 **internal_compliance**（角色闸先于 PWS 存在性，错角色 403）。③既有裁决补闸（非新决策）：信号权重 PUT=operations（Q2）、行业阈值 CRUD=operations（Q7）、来源路由 PUT/DELETE=operations（Q8）。④有意开放并在 05 实现补登：`pws/evaluate`（Q28 系统提请的机械求值）与 atom `revive`（前置状态即闸）不加角色闸。无 schema 变更（无迁移；仅请求契约加 actor）。194 条测试全绿（+6 HTTP 集成 403 矩阵，覆盖闸先于存在性/校验、缺 actor 422、正角色放行；既有 15 条 sweep/ccr/category 测试补正角色）。**仍挂账**：skill7 AI 候选-审核通道（各 WF Skill 占位入参替换；Q71 critical→target 自动补货 5 分钟防抖随该切片）；M8 任务异步 worker（V1 仍同步）；多副本配置广播/单实例调度锁（V1 单进程仅进程内热更新）。既有各模块服务内 `RoleNotAllowed` 未合并，全库异常类统一非本切片目标。

> **M10 完成情况（切片 e：skill7 AI 候选通道·WF-04 试点，2026-09-14，后端切片；M10 整体未闭合）**：✅ 本分片验收——05 §2.3 skill7 状态机（line 11314）与 06 §4 注册表/编排器首次落代码，接缝五项经负责人拍板记 **Q76**：①**范围=通道核心+WF-04 试点**：`app/core/skill7/`（registry/models/schemas/service/router/adapters）+ `runtime/workflows/WF-04.yaml`、`runtime/skills/{PWC-BUILDER,COMBO-VALIDATE,PWC-SCORING}/skill.yaml`（注册表加载器，未注册引用即拒，`LOOM_RUNTIME_DIR` 可覆盖；原文"11 字段"逐项列为 10 项，按 10 项实现）；②**外部投递边界**：不建模型注册表/LLM 客户端/API Key（Q67 表与机器通道契约仍【待补】，随真接 LLM 切片）；③**通用候选表 `skill_candidates`**：投递即 pending_review（ai_suggested 生产者侧态不持久化），confirmed/modified 经 target_type 适配器写既有流程——`pwc_combo` 适配器复用 `pwc/funnel`(source=ai)，预筛/合规/评分/限量/Gate 一项不绕，applied_refs 回写 pwc id；rejected→archived；④**Q71 补货**：consume 致 ready<critical 按 `pwc.restock_cooldown_minutes` 防抖落 `skill_runs(status=requested,source=restock_auto)`，只记 run 不造候选，consume 响应回带 `restock_run_id`（M5/切片 b 挂账销账）；⑤**投递 operations、裁决 product_reviewer**（WF Gate 插槽角色驱动）。端点：`POST/GET /api/skill-runs`、`GET /api/skill-runs/{id}`、`GET /api/skill-candidates`、`POST /api/skill-candidates/{id}/decision`（403/404/409/422 口径齐；run 无更新/删除路径，PT-COMPLIANCE 不可 mutate）。交付：迁移 0012_m10_skill7（skill_runs/skill_candidates，PG16 up/downgrade-1/up 实测）、203 条测试全绿（+3 注册表单测 +6 HTTP 集成：投递闸/未注册 422/confirmed 适配落库/rejected 不落 PWC/modified 422+human_modified/适配器失败候选留 pending/补货 requested+防抖）。**仍挂账**：WF-01/02/03（M2 C1/C7、M3 维度、M4 原子批次）占位入参按同模式逐一切片替换；Q67 模型注册表/真 LLM/API Key 鉴权；编排器并行与复杂 DAG（二期，可迁 LangGraph）；M8 任务异步 worker；多副本补货防抖锁/配置广播/单实例调度锁。

> **M10-Q 完成情况（2026-09-14，质量两件套轻量版；任务包闭合）**：✅ 验收标准达成——①Evaluation Dataset 与 Golden Cases 均落 15 `eval/`，接缝三问经负责人拍板记 **Q77**：首批案例只打 WF-04 有确定性替身的两个 Skill（PWC-SCORING→Q22/Q22a/Q22b 评分函数、COMBO-VALIDATE→Q48 词表匹配；PWC-BUILDER 外部生成无实现，不构造案例挂账）；独立 runner + YAML 数据集（不进 pytest 体系，对应 16 §1"回归测试"行）；本次只落可调用命令+文档不建 CI（托管平台未确认，CI 属 M12）。②交付物：`eval/runner.py`（扫 datasets/ 与 golden_cases/ 两套 YAML，target 注册于 `eval/targets.py`，dict 子集断言+float 容差+全量错误汇报，失败退出码 1）、`eval/README.md`（schema/运行/边界）、Evaluation Dataset 19 案（PWC-SCORING 13：完整公式/空池/全重合/双缺失分项/乘数/显式权重/疑重边界/Jaccard；COMBO-VALIDATE 6：ban/downgrade/无命中/大小写/多条/子串）、Golden Cases 5 案（公式标杆/Q22b 不凑分/疑重/医疗绝对化一票否决/降级只建议）；每案 source.refs 溯 Q/line，词例全合成无生产数据（16 §4）。③staging 闸门命令（17 CI/CD 已同步）：`backend/.venv/bin/python eval/runner.py`，24/24 通过退出码 0；另加 3 条 runner 单测进 backend（已提交套件绿/失败/无案例退出码），共 206 测试全绿。**未含（挂账）**：PWC-BUILDER 案例随真 LLM 切片（Q67 模型注册表）；COMBO-VALIDATE 结构预筛内联于 DB 漏斗、未抽纯 target（现由集成测试覆盖）；WF-01/02/03 数据集随占位替换切片；Simulation Sandbox/Drift Detection 仍 V2/V3（14 §2.6）；CI workflow 本体随 M12/部署。

> **WF-02 占位替换切片完成情况（2026-09-14，M10 WF 替换切片 1/3；M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝经负责人拍板记 **Q78**：DIM-MERGE 为候选产出者，**整方案单候选**（外部侧完成 7 路采集/合并后投递），`target_type=field_plan`，payload=去 actor 的 PlanSubmitRequest 形态（dimensions + target_atom_min/max）；confirmed/modified 经新适配器复用既有 `fieldpool.submit_plan` 落 pending_gate，再过既有 WF-02 HumanGate（product_reviewer），Q8 启用路由/PT-FP-PLAN/Q9 细看/Q10 疑重/Q12 Top8+备选/G2 候选转正一项不绕；rejected→archived 不落池；FIELDPOOL-PLAN、DIM-SOURCE 登记但不带 candidate_target。通道泛化：投递校验从硬编码 pwc_combo 改为 registry `candidate_target_for(wf,skill)` 按 WF 步骤声明分派（非产出步骤带候选 422、target 不符 422、field_plan 限恰好 1 条、payload 按 ComboItem / PlanSubmitRequest 契约预校验），裁决 modified 改单按 target_type 分派同一校验；router 补 fieldpool 异常口径（ProductSpaceNotFound 404 / GateNotAllowed 409 / InvalidPlan 422），适配器失败回滚候选留 pending_review 可重裁。交付：`runtime/workflows/WF-02.yaml`（skill7+fieldpool 双 Gate 插槽；failure_policy=Q9 mark_needs_detail/Q10 flag_only）+ `runtime/skills/{FIELDPOOL-PLAN,DIM-SOURCE,DIM-MERGE}/skill.yaml`（均 10 字段、model_tier 空、cost_limit null）；**每切片自带 eval/ 回归（Q77 模式）**：`evaluate_plan` target + DIM-MERGE 回归矩阵 12 案（合规/Q9 细看/Q10 疑重/below_min/缺 product_attribute/Q11 敏感与非敏感/unknown_route/unknown_role/缺证据/illegal_fid/Top8 备选）+ Golden 3 案，合成数据带 source.refs，eval 合计 39/39；backend +6 HTTP 集成（投递闸六连/落 pending_candidate/confirmed 适配落池+Gate 可裁/rejected 无池/modified 422+human_modified/适配器 409 候选留 pending）+1 注册表单测（WF-02 三 Skill/candidate_target_for/双 WF review_role），**共 213 测试全绿、ruff 通过、无新迁移（skill_candidates 通用表 Q76-3 承载）**。**仍挂账**：WF-01（M2 C1/C7）、WF-03（M4 原子批次）占位替换（同模式，各带 eval 案例）；Q67 模型注册表/真 LLM/API Key；编排并行/DAG（V2）；M8 异步 worker；多副本广播/锁。

> **WF-01 占位替换切片完成情况（2026-09-14，M10 WF 替换切片 2/3；M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝经负责人拍板记 **Q79**（02 C1.23，四项）：**(1) 范围仅 C1 识别**，CAT-RECOG 为候选产出者，识别整结果单候选 `target_type=c1_recognition`（payload=去 actor 的 C1RecognitionRequest），C7 Layer4 新字段提案通道留挂账随后续切片；**(2) Q1 接缝**：统一过一次人工 confirm（skill7 Gate=AI 产出准入，平台铁律 AI 只产候选），Q1 三分支机械逻辑一字不改（direct_approve→auto_confirm submitted / ops_assist→pending_confirm+72h OpsTodo / cold_start→需 category_pending_id）；**(3) Gate 角色 operations**；**(4) 锚点**：skill_runs/skill_candidates 加 nullable intake_id、skill_candidates.product_space_id 改 nullable（迁移 0013，PG16 up/downgrade-1/up 实测），投递 PS/intake 锚点二选一按 WF 归属校验。通道泛化：单候选约束由 WF 步骤 `single_candidate` 声明驱动（Q78 硬编码 field_plan 一并泛化，WF-02 YAML 同步加声明），registry 新增 `producer_step_for`；适配器 `c1_recognition` 复用既有 `modeling.submit_recognition`（IntakeNotFound 404 / RecognitionNotAllowed 409 / ConfigError·InvalidDecision 422，适配器失败回滚候选留 pending_review）；GET 列表与视图加 intake_id。交付：`runtime/workflows/WF-01.yaml` + `runtime/skills/{PARSE,UNDERSTAND,CAT-RECOG,TYPE-MATCH,MISSING-INFO,RISK-TAG}/skill.yaml`（均 10 字段、model_tier 空、cost_limit null）；**每切片自带 eval/ 回归（Q77 模式）**：`weighted_conf`/`decide_branch` 两 target + CAT-RECOG 回归矩阵 11 案（Q2 加权 4 + Q1/Q3 分支边界 7）+ Golden 3 案（高置信直批 / Q3 矛盾转 ops / 0.6 地板冷启动），合成数据带 source.refs，eval 合计 53/53；backend +7 HTTP 集成（投递闸：角色/404/422/锚点/单候选、落 pending 无 C1Record、高置信 auto_confirm、中置信 OpsTodo、低置信缺 pending id 422 留 pending→modified 补 B2 单成功、rejected 无识别、错误 intake 态 409 留 pending）+1 注册表单测（WF-01 六 Skill/producer_step/role=operations；WF-02 single_candidate 并入既有 gate 槽单测断言），**共 221 测试全绿、ruff 通过、迁移 0013 PG16 实测**。**仍挂账**：WF-03（M4 原子批次）占位替换（同模式带 eval 案例，~~已由 Q80 完成~~）；~~**C7 Layer4 新字段提案通道（Q79-1）**（已由 Q81 完成，见下）~~；Q67 模型注册表/真 LLM/API Key；编排并行/DAG（V2）；M8 异步 worker；多副本广播/锁。

> **WF-03 占位替换切片完成情况（2026-09-14，M10 WF 替换切片 3/3；M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝经负责人拍板记 **Q80**（02 C1.24，四项）：**(1) 整批一条候选** `target_type=atom_batch`、`single_candidate: true`，payload=去 actor 的 BatchSubmitRequest 形态（items[{content,dimension_id,ai_risk,affinity?,evidence?,fact_type?,cluster_id?}] + 可选 batch_size），confirmed/modified 经新适配器复用既有 `atom.submit_batch`（强制 source=ai，Q14 20/50、Q15 AI 达标停拓、同批 normalized 去重、维度属本池 selected、line 11189 事实原子全局唯一、Q17 词表双轨、line 840 三类冲突、PT-ATOM-EXP 空证据降级一项不绕），applied_refs 回写 batch_id；入库后每条 AtomCandidate 的 approveAtomGuard/Q70/Q18/Q19/Q20 原样保留，skill7 Gate 仅 AI 产出准入不替代逐条 Gate；**(2) Gate 角色 product_reviewer**（06 line 172 原子审核=产品审核员；投递仍归 operations）；**(3) CONFLICT-PRECHECK 为末步候选产出者**，投递即四步 AI 处理全部完成后的最终成型批次，ATOM-EXPAND/CANON/AFFINITY 登记普通步骤不挂 candidate_target；**(4) 锚点 product_space_id**，复用 Q79-4 PS 分支，无新迁移。通道泛化：投递/裁决 payload 预校验按 BatchSubmitRequest（source/actor 剥离后强制 source=ai 构造），router 补 atom 异常口径（ProductSpaceNotFound·PoolNotFound 404 / PoolNotApproved·TargetReached·FactAtomConflict 409 / InvalidBatch 422），适配器失败回滚候选留 pending_review 可重裁（Q15 达标 409 时批次未入库、候选留待后续重决）。交付：`runtime/workflows/WF-03.yaml`（ATOM-EXPAND→ATOM-CANON→ATOM-AFFINITY→CONFLICT-PRECHECK 顺序四步，仅末步挂 produces_candidates/candidate_target=atom_batch/single_candidate；一个 skill7 Gate 插槽 after=CONFLICT-PRECHECK role=product_reviewer；failure_policy=Q15 stop_expand / 空证据 pending_evidence）+ `runtime/skills/{ATOM-EXPAND,ATOM-CANON,ATOM-AFFINITY,CONFLICT-PRECHECK}/skill.yaml`（均 10 字段、model_tier 空、cost_limit null）；**每切片自带 eval/ 回归（Q77 模式）**：7 个 target（default_batch_size/target_reached/normalize/is_low_affinity/pick_keeper/grade_risk/conflicts_for）+ 四 Skill 回归矩阵 24 案（EXPAND 5：Q14 20/50、Q15 0/30、30/30 边界、31/30；CANON 4：trim+casefold/内空白/幂等/非 ASCII；AFFINITY 7：Q16 0.5 线四档+Q19 keeper 最高/None 殿后/平局保序；CONFLICT 8：Q17 词表强制双轨四案+line 840 冲突四案）+ Golden 7 案，合成数据带 source.refs，eval 合计 **84/84**（66 数据集 + 18 标杆）；backend +8 HTTP 集成（投递闸：403/404/非产出步骤 422/空 items 422/target 不符 422/双候选 422/intake 锚点 422；落 pending 无批次；OPS 403、reviewer confirmed 整批入库后逐条 approve 可过；rejected 无批次；modified 坏维度 422 后替换 payload+human_modified；池未批 409 留 pending；Q15 达标 409 留 pending；无证据条目 pending_evidence+due）+1 注册表单测（WF-03 四 Skill/10 字段/producer_step=CONFLICT-PRECHECK/single_candidate/review_role=product_reviewer），**共 230 测试全绿、ruff 通过、无新迁移（迁移仍 0001–0013，atom_batch 复用 skill_candidates 通用表与 PS 锚点）**。M10 WF 替换切片 3/3 全部完成（WF-04 试点 Q76 → WF-02 Q78 → WF-01 Q79 → WF-03 Q80）。**仍挂账**：**C7 Layer4 新字段提案通道（Q79-1）**（~~已于 2026-09-14 经 Q81 切片接入，见下~~）；Q67 模型注册表/真 LLM/API Key 鉴权（四 WF 的真生成侧：PWC-BUILDER/字段与维度生成/CAT-RECOG 信号提取/原子拓展生成本体均挂此项）；编排器并行与复杂 DAG（V2）；M8 异步 worker；多副本补货防抖锁/配置广播/单实例调度锁。

> **WF-01 C7 Layer4 占位替换切片完成情况（2026-09-14，Q79-1 挂账销账；M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝经负责人拍板记 **Q81**（02 C1.25，四项，均选推荐项）：**(1) 产出步骤 = TYPE-MATCH，Gate 在其后**：TYPE-MATCH/skill.yaml 本自陈 failure_policy=fallback_to_c7；同一 WF 允许两个候选产出步骤（CAT-RECOG→c1_recognition、TYPE-MATCH→c7_layer4），registry 按"WF+步骤"逐步骤校验投递；MISSING-INFO 仍非产出者（缺字段清单驱动 pending_params）；**(2) 候选形态 = 整 C7 解析请求单候选** `target_type=c7_layer4`、`single_candidate: true`，payload=去 actor 的 C7ResolveRequest（category_id/required_fids/l4_proposals[]），confirmed/modified 经新适配器复用既有 `modeling.resolve_c7`，L1 缓存/L2 approved 兄弟继承/Q6 覆盖率 0.6 地板/Q68 fid:'-' 闸一项不绕——实际落 L1/2/3 时 l4_proposals 自然不生效（与 Q15 target_reached 同口径），落 L4 才写 g2_field_candidates(pending_gate)，applied_refs=[C7Run.run_id]；**(3) Gate 角色 operations**（与 Q79-3 同档），与段3 Q13 dictionary_admin 深度审核/转正**不合并**——准入裁"AI 产出能否进解析"、转正裁"候选字段能否进 G2 字典"，两道人工 Gate 先后独立（Q66 AI 无角色）；**(4) eval 只覆盖 c7 确定性纯函数**，Q68/错误分支留集成测试，真 LLM 新字段生成体挂 Q67。通道泛化：投递 payload 按 C7ResolveRequest 契约预校验（actor 剥离后占位重造），intake 锚点集合扩为 {c1_recognition, c7_layer4}（复用 Q79-4 intake 分支，无新迁移），router 补 modeling 异常口径（IntakeNotFound·CategoryNotFound 404 / IllegalFid·InvalidDecision 422），适配器失败回滚候选留 pending_review。顺手修一潜在缺陷：resolve_c7 Layer4 原盲插违反 uq(source_layer,field_name)，改同名 c7_layer4 全局候选复用（与 wf02_dim_source 同口径）。交付：`runtime/workflows/WF-01.yaml`（TYPE-MATCH 挂 produces_candidates/candidate_target=c7_layer4/single_candidate；第二道 skill7 Gate after=TYPE-MATCH role=operations）+ TYPE-MATCH/skill.yaml 输出/review_rule 同步（均仍 10 字段、model_tier 空、cost_limit null）；**每切片自带 eval/ 回归（Q77 模式）**：3 个 target（c7_coverage/c7_pick_sibling/c7_layer3_floor）+ TYPE-MATCH 回归矩阵 14 案（地板 1 + 覆盖率 8：空必填 1.0/全满足/1-of-6/0.6 边界/round 4/0.3333/0.0/0.6667 + 兄弟 5：approved 最大/draft 不可继承/None/平局保序/缺计数按 0）+ Golden 3 案（0.6 地板/<地板必降 L4/兄弟只认 approved），合成数据带 source.refs（Q6/line 708/Q68），eval 合计 **101/101**（80 数据集 + 21 标杆）；backend +8 HTTP 集成（投递闸：403/404/MISSING-INFO 非产出 422/target 不符 422/缺 category_id 422/双候选 422/PS 与双锚点 422；落 pending 无 C7Run 无候选；reviewer 403、OPS confirmed 落 layer4+pending_gate 候选；落 layer1 提案不生效零候选；rejected 无 C7Run；modified fid:'-' 422 留 pending 后替换成功+human_modified；category 不存在 404 留 pending；跨单同名提案复用全局候选）+ 既有注册表单测扩断言（WF-01 两产出步骤/双 single_candidate），**共 238 测试全绿、ruff 通过、无新迁移（迁移仍 0001–0013，c7_layer4 复用 skill_candidates 通用表与 intake 锚点）**。**仍挂账**：Q67 模型注册表/真 LLM/API Key 鉴权（五处真生成侧：PWC-BUILDER/字段与维度生成/CAT-RECOG 信号提取/原子拓展生成/**C7 L4 新字段生成本体**，通道均已通）；编排器并行与复杂 DAG（V2）；M8 异步 worker；多副本补货防抖锁/配置广播/单实例调度锁。

> **Q67/Q82 模型网关与 CAT-RECOG 真 LLM 试点切片完成情况（2026-09-14；M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝四项经负责人拍板记 **Q82**（02 C1.26，均选推荐项）：**(1) 范围=模型底座+CAT-RECOG 单站试点**，其余四生成站点（PWC-BUILDER / FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY / C7 L4）后续切片复用本模式；**(2) 进程内同步调用、复用 skill7 通道**：业务端点进程内同步调模型网关，以系统机器 Actor（`system:llm-gateway`，roles=[]，Q66）经既有投递逻辑落 skill_runs/skill_candidates(pending_review)，operations 人工 Gate 一字不改，异步 worker 留 M8；**(3) outbound Key：DB 对称加密（Fernet 级）+ env 主密钥**（`LOOM_MASTER_KEY` 仅 env、不入库不入仓；模型注册表页创建/轮换；明文不回显；入站"一 Agent 一 Key 可吊销"随 Q60 效果回调切片）；**(4) Prompt 模板 DB 版本化**（v0.1 起步、ChangeProposal 式 append-only、platform_admin + writeAudit；仓库只携迁移初始种子；不走 15 §1 规划的 runtime/skills/&lt;id&gt;/prompt/*.md）。机械补登：场景路由键=skill_id；日预算按模型全局硬停、用尽 409 不静默切换供应商；fallback V1=仅注册态+人工（disabled 才走 fallback，无自动故障转移）；skill_runs 成本列 decimal + currency_code，种子币种【原文未给出，待补】；仓内不放真凭证，测试/本地用确定性 synthetic 替身驱动。交付：迁移 **0014_model_gateway**（ai_models/ai_model_keys/ai_scene_routes/skill_prompts/skill_prompt_versions 五表 + skill_runs 加 model_id/input_cost/output_cost/currency_code 四列 + synthetic 模型/CAT-RECOG 场景路由/CAT-RECOG v0.1 Prompt 种子，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/` 九模块（models/crypto/gateway/drivers/synthetic/extraction/schemas/seeds/router）、skill7 拆 `deliver_generated_run`（系统 Actor、source=llm_auto、无角色门；修复 SkillRun.source 原硬编码 delivery 的潜在缺陷）、端点 8 治理 + `POST /api/intakes/{id}/c1-recognition/llm-invoke`（403/404/409/422/502 口径齐，05 §1.4 切片 f）；测试 **253 全绿**（+5 纯函数单测：Key 加解密/指纹/错钥、per-1M 成本 ROUND_HALF_UP、synthetic 确定性；+10 HTTP 集成：模型 CRUD RBAC/校验、Key 轮换生命周期不回显、场景路由、Prompt 版本化、llm-invoke 全链路落 pending_review 后 operations 裁决照走 Q1 分支、disabled→fallback/预算硬停、远程模型无 Key 422、坏输出 502、服务层配置错误），ruff 通过；**eval 不新增（仍 101/101）**——本切片无确定性纯业务函数，试点行为按 16 §4 由后端集成测试（synthetic 替身）覆盖。**仍挂账**：其余四生成站点真 LLM 切片；入站一 Agent 一 Key（随 Q60 效果回调）；M8 异步 worker；编排并行/DAG（V2）；多副本广播/锁/单实例调度锁；成本币种【原文未给出，待补】。

> **Q83 PWC-BUILDER 真 LLM 组合生成切片完成情况（2026-09-14；Q82 模式第二站，M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝四项经负责人拍板记 **Q83**（02 C1.27，均选推荐项）：**(1) 仅显式业务端点 operations 触发**（`POST /api/product-spaces/{id}/pwc/llm-build`）；Q71 restock_auto 的 SkillRun(requested) 本切片不自动消费，自动触发等 M8 worker；**(2) 一次调用产整批、投多条 candidates[]**：WF-04 PWC-BUILDER 步骤不声明 single_candidate，每条组合各成一条 `target_type=pwc_combo` 候选（payload=`{"combos":[{atom_ids,goals}]}`），适配器逐 candidate 原样跑 M5 漏斗；输出超 Q21 `pwc.funnel_batch_limit`（50）502，同原子集重复组合编排层机械去重（保序，补跨候选去重缺口），跨候选语义重合仍归漏斗 Q22/Q23；**(3) Prompt 机读变量有界**（approved 原子清单/库容/ready 数/active goals/目标平台），前置闸=PS 404、字段池非 approved 409、approved 原子不足 2 维 409；synthetic 只按维度确定性配对不出分，真模型回带的 logic/fit/weight 一律丢弃不透传，Q22b 子分缺失 → score incomplete → 人工 Gate；**(4) 预算/失败语义沿用 Q82**（预算硬停 409、缺 Key 422、坏输出 502、disabled 无 fallback 409、系统 Actor roles=[]、source=llm_auto、成本四列照记）。交付：迁移 **0015_pwc_builder_seed**（纯数据：PWC-BUILDER→synthetic 场景路由 + skill_prompts/versions v0.1 三行，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/pwc_build.py`（编排，输入只记有界摘要）、synthetic.build_pwc_builder 注册、seeds PWC 常量/Prompt、condition router 新端点（403/404/409/422/502 齐）；测试 **263 全绿**（+2 纯函数单测：synthetic 结构配对/封顶 5/不足两维、+8 HTTP 集成：全链路两候选→product_reviewer 裁决→漏斗 pending_gate→PWC Gate approve→ready、RBAC/404/池未批 409/单维原子 409、预算 409、远程无 Key 422、坏 JSON 502、未知原子 502、模型回带评分被剥离），ruff 通过；**eval 不新增（仍 101/101）**——synthetic 构造器为测试夹具非业务函数，按 16 §4 由集成测试覆盖。**仍挂账**：其余三生成站点（FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY 含 Q16 本体 / C7 L4）；restock_auto 自动触发（M8 worker）；入站一 Agent 一 Key（Q60）；编排并行/DAG；多副本锁；成本币种【原文未给出，待补】。

> **Q84 C7 Layer4（TYPE-MATCH）真 LLM 新字段生成切片完成情况（2026-09-15；Q82 模式第三站，M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝四项经负责人拍板记 **Q84**（02 C1.28，均选推荐项）：**(1) 仅显式业务端点 operations 触发、不与 CAT-RECOG 串联**（`POST /api/intakes/{id}/c7/llm-resolve`，body=`{category_id, required_fids, actor}`；链式/DAG 二期）；**(2) 整 C7 解析请求单候选原样复用 Q81 契约**（WF-01 TYPE-MATCH 已声明 single_candidate/c7_layer4/intake 锚点；适配器 resolve_c7 不改；模型回显 category_id/required_fids 必须与触发输入一致，集合漂移 502；空 l4_proposals 合法，裁决后自然落 L3）；**(3) Prompt 机读变量有界**（目标类目/必填 fid/L1 approved 模板/L2 approved 兄弟计数/L3 G2 active 清单+覆盖率+0.6 floor），前置闸=intake 404、状态非 ai_recognizing 409、类目 404/非 active 409、required_fids 含空串/'-' 按 Q68 调模型前 422；synthetic 只按覆盖率缺口造结构提案（field_name=`待补字段·<fid>`+固定 definition）不打置信分，真模型回带的 confidence/score/fid/status/source_route 一律剥离（只透传 field_name/非空 definition，G2FieldCandidate.confidence 落 NULL；source_route 枚举原文未给 v0.1 落 NULL），L4 候选仍 status=pending_gate 走 Q13 dictionary_admin 转正；**(4) 预算/失败语义沿用 Q82/Q83**（预算硬停 409、缺 Key 422、坏输出 502、disabled 无 fallback 409、系统 Actor roles=[]、source=llm_auto、成本四列照记）。交付：迁移 **0016_type_match_seed**（纯数据：TYPE-MATCH→synthetic 场景路由 + skill_prompts/versions v0.1 三行，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/c7_resolve.py`（第十一模块，输入只记有界摘要）、synthetic.build_type_match 注册、seeds TYPE-MATCH 常量/Prompt、model_registry router 新端点（403/404/409×2/422/502 齐）；测试 **276 全绿**（+2 纯函数单测：缺口结构提案/不打分/确定性、全覆盖空提案；+11 HTTP 集成：happy path→operations 裁决→resolve_c7 L4 建 5 候选 pending_gate、全覆盖落 L3、RBAC/404×2/状态 409/draft 类目 409/Q68 422、预算 409+fallback、远程无 Key 422、坏 JSON 502、类目/必填回显漂移 502、脏提案 502、模型回带分与 source_route 剥离专测），ruff 通过；**eval 不新增（仍 101/101）**——synthetic 构造器为测试夹具非业务函数，按 16 §4 由集成测试覆盖。**仍挂账**：其余两生成站点（FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY 含 Q16 语义相似度本体）；restock_auto 自动触发（M8 worker）；入站一 Agent 一 Key（Q60）；编排并行/DAG；多副本锁；source_route L4 取值枚举【原文未给出，待补】；成本币种【原文未给出，待补】。

> **Q85 WF-02 字段池方案（DIM-MERGE）真 LLM 生成切片完成情况（2026-09-15；Q82 模式第四站，M10 余量推进，不闭合 M10）**：✅ 切片验收——接缝四项经负责人拍板记 **Q85**（02 C1.29，均选推荐项）：**(1) 场景键=DIM-MERGE、仅显式业务端点 operations 触发、不自动串链**（DIM-MERGE 是 WF-02 唯一声明 field_plan 产出步骤；`POST /api/product-spaces/{id}/field-pools/llm-plan`，body=`{target_atom_min?, target_atom_max?, actor}`，缺省 Q15 旋钮 15/30；FIELDPOOL-PLAN/DIM-SOURCE 七路外部采集连接器与链式/DAG 二期；第 7 路枚举原文未列全【待补】）；**(2) 整方案单候选原样复用 Q78 契约**（single_candidate/field_plan/PS 锚点；适配器 apply_field_plan→submit_plan 不改；target_atom_min/max 必须原样回传，漂移 502；<3 维/缺 product_attribute/敏感缺 risk_control 属业务违规，照常落非合规 pending_gate 池、Gate 不可 approve，不做 502）；**(3) Prompt 机读变量有界**（profile_snapshot 资料行/敏感行业标+行业标签/启用来源路由/active G2 清单/dim 3-8/目标原子 15-30），前置闸=PS 404、无池或 gate=rejected（否则 409，与 submit_plan 同口径）、min>max 409；结构白名单=role∈{product_attribute,risk_control}、source_route 必须启用（unknown_route_reject）、fid 给值必须 active（Q68 '-' 与幻觉 fid 502；新字段省略 fid 走 wf02_dim_source→Q13 dictionary_admin 转正）、批内去重、dimensions 非空且 ≤8；**证据红线 line 14081 在生成边界强制执行**（source_ref 空白即 502，只许引用有界输入 g2:/product_profile:/industry_tag:）；**confidence 与 Q83/Q84 剥离口径不同——PT-FP-PLAN 契约内 AI 字段（Q9 细看/Q12 Top8 依赖）0..1 校验后透传**，缺失/越界 502，synthetic 固定 0.9 不模拟分布，similarity/related_fid 可选透传（Q10 仅标记），未知键剥离；**(4) 预算/失败语义沿用前三站**（预算硬停 409、缺 Key 422、坏输出 502、disabled 无 fallback 409、系统 Actor roles=[]、source=llm_auto、成本四列照记）。交付：迁移 **0017_dim_merge_seed**（纯数据：DIM-MERGE→synthetic 场景路由 + skill_prompts/versions v0.1 三行，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/field_plan.py`（第十二模块，输入只记有界摘要）、synthetic.build_dim_merge 注册、seeds DIM-MERGE 常量/Prompt、fieldpool router 新端点（403/404/409/422/502 齐）；测试 **289 全绿**（+2 纯函数单测：synthetic 合规整方案/敏感补 risk_control/固定 0.9/证据引用/确定性、非敏感 dim_max 截断；+11 HTTP 集成：happy path→product_reviewer 裁决→pending_gate→WF-02 Gate approve、敏感行业风控维、target 区间回显覆盖、RBAC/404/在场 409/rejected 重提 201、预算 409+fallback、远程无 Key 422、坏 JSON 502、十类脏维度与回显漂移 502、低 confidence 透传落 needs_detail、below_min 非合规池 Gate 409），ruff 通过；**eval 不新增（仍 101/101）**——synthetic 构造器为测试夹具非业务函数，按 16 §4 由集成测试覆盖。**仍挂账**：余一生成站点（ATOM-EXPAND·ATOM-AFFINITY 含 Q16 pgvector 语义相似度本体）；restock_auto 自动触发（M8 worker）；入站一 Agent 一 Key（Q60）；编排并行/DAG；多副本锁；DIM-SOURCE 第 7 路枚举/source_route L4 取值枚举【原文未给出，待补】；成本币种【原文未给出，待补】。


> **Q86 WF-03 原子批量补池（CONFLICT-PRECHECK chat + ATOM-AFFINITY embedding）真 LLM 生成切片完成情况（2026-09-15；Q82 模式第五站/末站，M10 真 LLM 五站 backlog 闭合，不闭合 M10）**：✅ 切片验收——接缝四项经负责人拍板记 **Q86**（02 C1.30，均选推荐项）：**(1) operations 显式端点 + 花 token 前全量预闸、不自动串链**（`POST /api/product-spaces/{id}/atom-batches/llm-expand`，body=`{batch_size?, actor}` 缺省 Q14 敏感 20/非敏感 50；PS 404/字段池 404/gate≠approved 409/已通过+冻结原子 ≥ target_atom_max 409 TargetReached 且 Q15 停拓仅约束 AI 批次；restock_auto 留 M8 worker）；**(2) 两次模型调用、一个 atom_batch 候选**：CONFLICT-PRECHECK chat（WF-03 唯一 atom_batch/single_candidate 产出步骤，Prompt 挂此 skill）+ ATOM-AFFINITY embedding（首个 capability=embedding 场景，不挂 Prompt、不产候选，gateway.embed 能力不符 422）；1 条 SkillRun（model_id=chat，两次 input tokens/cost 求和、output 仅 chat，embedding 模型身份入 run.input）；整批候选复用 Q80 apply_atom_batch→submit_batch（强制 source=ai，Q14/Q15/批内去重/维度必选/line 11189/Q17 双轨/line 840/Q18 一项不绕），1536 维向量以 `_embeddings` 机读键带外双侧 pop，approve 复制到 product_atom_instances；**(3) Q16 相似度本体 V1 口径**：迁移 0018 首个带 schema 的 LLM 切片（PG `CREATE EXTENSION vector`、ai_models.capability chat|embedding、两表 nullable vector(1536)、SQLite float32 LargeBinary TypeDecorator），进程内纯 Python 余弦（无 KNN 索引，千万级切换按 14 §2.3 后置），同维 approved/frozen 有向量原子 max（无基 None，历史 NULL 不入基不 backfill），批内同维并查集成簇，**成簇线 atom.cluster_line=0.9 系本次拍板借 Q10 同义线（原文未给【待补】，进配置中心）**，跨维不成簇，Q16 0.5 低亲和只标不淘汰；**(4) 输出契约**：白名单 `{batch_size 回传, items:[{content,dimension_id,ai_risk,evidence?,fact_type?}]}`，模型自报 affinity/cluster_id/score 一律剥离本地重算，ai_risk 为 PT-ATOM-EXP/Q17 契约内字段枚举校验后透传（词表只升不降仍在 submit_batch），结构脏数据与 embedding 行数/维度不符投递前 502 不留痕，业务违规不 502；预算/失败沿用 Q82–Q85（日预算按模型全局硬停 409 含 embedding 触顶、disabled 仅已注册 fallback、缺 Key 422、上游坏 502、系统 Actor system:llm-gateway roles=[]、source=llm_auto）。交付：迁移 **0018_atom_affinity_embedding**（pg16 pgvector 镜像 up/downgrade-1/up 实测，不触本地 atlas-pg）、`app/core/model_registry/atom_expand.py`（第十三模块）、gateway/drivers embed 通道 + 第二个合成模型 synthetic-embedding（确定性哈希字袋 1536 维单位向量，价 0）、atom_rules cosine_similarity/max_affinity/assign_clusters 纯函数、atom router 新端点（403/404/409/422/502 齐）；测试 **306 全绿**（+4 atom_rules 纯函数单测：余弦/零向量/无基 None/成簇线与标签隔离；+3 gateway 单测：CONFLICT-PRECHECK 构造器/批帽/embedding 确定性与成对相似度；+10 HTTP 集成：向量落库与本地字段、approve 复制向量后第二批算亲和、Q86 前 NULL 向量原子 None 亲和、模型自报字段剥离、ai_risk 透传与词表强升、RBAC/预闸、八类结构脏 502 零残留、chat disabled fallback/远程无 Key、embedding 预算/能力不符/缺 Key、向量行数不符 502），ruff 通过；**eval 不新增（仍 101/101）**——synthetic chat/embedding 构造器均为测试夹具，按 16 §4 由集成测试覆盖。**M10 五个真 LLM 生成站点全部接通（Q82 CAT-RECOG/Q83 PWC-BUILDER/Q84 TYPE-MATCH/Q85 DIM-MERGE/Q86 CONFLICT-PRECHECK+ATOM-AFFINITY），站点 backlog 闭合**。**仍挂账**：restock_auto 自动触发（M8 worker）；入站一 Agent 一 Key（Q60）；编排并行/DAG V2；多副本 Redis 广播/单实例调度+补货锁；M12 平台厚度；DIM-SOURCE 第 7 路枚举/source_route L4 取值枚举【原文未给出，待补】；成本币种【原文未给出，待补】；真 embedding 模型接入时维度按模型实际演进（TypeDecorator 单点承载）；两个基线 HTML 缺块。
> **Q87 M8 restock_auto worker 切片完成情况（2026-09-15；无迁移纯代码切片，Q71 补货闭环——信号→自动消费→候选→人工 Gate；不闭合 M10 横切集）**：✅ 切片验收——接缝四项经负责人拍板记 **Q87**（02 C1.31，均选推荐项）：**(1) 形态=进程内第二个 asyncio 循环** `RestockWorker`（与 SweepScheduler 同构同生命周期，V1 单进程；**docs/07 §7.5 队列选型就此裁决 V1=进程内轮询**，Redis Streams/多副本锁后置 V2）；`LOOM_RESTOCK_WORKER_ENABLED` 默认 false 部署 opt-in（自动花真 token）、`LOOM_RESTOCK_INTERVAL_SECONDS`=60、`LOOM_RESTOCK_BATCH_SIZE`=20（env 实现值，非 Q9 旋钮）；管理端 `POST /api/admin/restock/run`（platform_admin，同 Q75 sla/run）；**(2) 纯追加认领，append-only 不放松**：requested/restock_auto 信号行永不 mutate，成功追加 succeeded/llm_auto 子 run（`input.restock_request_id` 回链，候选挂子 run），终态失败追加 failed/llm_auto 子 run（Q71 预留 failed 首次启用），认领=反连接子 run，单进程 tick 串行，多副本锁随 V2；`?status=requested` 持续含已消费信号行（日志本色，处理状态 join 子 run）；**(3) 范围仅 PWC-BUILDER**（唯一 requested 写者），最早优先每轮 ≤20，只产 pending_review 候选、product_reviewer Gate 一字不改；内部入口 invoke_pwc_build_for_restock（system:llm-gateway roles=[]，OPERATIONS 角色闸只在 HTTP /pwc/llm-build），全量预闸/白名单/校验同 Q83，不自动串 ATOM-EXPAND（原子跨维 <2 → 终态失败转人工 llm-expand），一次信号一次调用（≤Q21 cap 50）；**(4) 失败/预算分类**：终态（PS 消失/池未批/PwcBuildNotReady/PwcBuildOutputInvalid 结构脏/ModelConfigError 缺路由·Prompt·Key·能力不符/停用无 fallback）→ failed 子 run + audit restock_failed(terminal=true) 不重试；瞬态（新增 BudgetExhausted(ModelUnavailable) 子类区分日预算硬停、上游 GenerationUpstreamError）→ 不留子 run、audit restock_deferred(terminal=false)、下轮重试（预算次日自然恢复；HTTP 侧 409 映射不变）；不静默切供应商，fallback 仅注册表已注册；V1 不设重试次数/退避（挂账 V2）；成功 audit restock_delivered；未预期异常 log+rollback 不拖垮整轮；每信号独立会话/提交。交付：`app/core/restock/`（worker.py run_restock+RestockWorker、router.py）、pwc_build 拆 HTTP 包装/内部入口/共享 _run_pwc_build（Q83 手动路径零变化）、gateway BudgetExhausted 子类、config 三项 env、main lifespan 启停；**无 Alembic 迁移（头仍 0018）**；测试 **315 全绿**（+9 HTTP/集成：成功交付+信号行不动+反连接幂等、非 restock/他 skill 信号忽略、not-ready/池未批/停用无 fallback/脏输出四类终态 failed 子 run+审计、预算硬停瞬态无子 run 且下轮仍可认领、admin 端点 RBAC+手工触发、worker start/stop 生命周期），ruff 通过；**eval 不新增（仍 101/101）**。**仍挂账**：入站一 Agent 一 Key（Q60 效果回调）；编排并行/DAG V2；多副本 Redis 广播/单实例调度+补货锁（含瞬态重试退避）；M12 平台厚度；DIM-SOURCE 第 7 路枚举/source_route L4 取值枚举/成本币种【原文未给出，待补】；真 embedding 模型维度演进（TypeDecorator 单点）；两个基线 HTML 缺块。

> **Q88 入站“一 Agent 一 Key”凭证半套切片完成情况（2026-09-15；迁移 0019；Q60b effect-callback 鉴权前置，段13 本体仍 V2/P3）**：✅ 接缝四项经负责人拍板记 **Q88**（02 C1.32，“四项全按推荐（甲）”）：**(1) 范围=只做 Key 半套**——新表 agent_api_keys + platform_admin 治理端点（签发/列表/吊销）+ Bearer 验签函数/依赖 + 审计；POST /api/effect-callback、effect_records 时序（content_id+captured_at 幂等、缺席指标“—”不当 0）、孤儿队列/人工认领、Q60a 运营回填、customer-backfill 全部随段13/P3 V2 不提前落；V1 无受保护业务消费端点为显式挂账（同 Q82 注册表“底座先行”），不建占位端点；**(2) SHA-256 单向哈希**——明文 loom_+token_urlsafe(32) 仅签发响应返回一次，DB 存 sha256 hex（unique+索引）+ 前 12 字符展示前缀，刻意区别 outbound Fernet（入站只需比对，库泄露不暴露可用 Key）；吊销=active/revoked 状态位+revoked_by/revoked_at，append-only 不物理删除；**(3) Key 不绑租户**——平台级凭证，未来租户归属由 content_id→FCW.tenant_id 解析，单租户绑定原文未给【待补】不臆造 tenant 列；**(4) 新横切包 `app/core/api_keys/`**（models/service/schemas/router）与 model_registry outbound 域并列，路径 `/api/admin/agent-keys`（201 签发 secret 仅一次/列表默认隐去 revoked 且无 secret/吊销幂等 404/越权 403/空名 422），验签未知·空·吊销统一 None 防探测、命中盖 last_used_at，require_agent_key 依赖 401 已备；审计 agent_api_key.issue/revoke（tenant `_platform`）。交付：迁移 **0019_agent_api_keys**（down=0018；pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触本地 atlas-pg）、`app/core/api_keys/` 四模块 + main 挂载；测试 **332 全绿**（+17 集成：Key 形态/20 枚唯一、Bearer 解析 6 形、只存哈希、RBAC、空名、验签 active/未知/空/吊销、吊销 append-only+审计集合、404、列表默认隐吊销、HTTP secret 一次性+掩码+第二枚不同、403×2/422/404），ruff 通过；**eval 不新增（仍 101/101）**。**仍挂账（M10 余量）**：effect-callback 端点与段13 回流整切片（P3/V2，鉴权半套已就绪）；编排并行/DAG V2；多副本 Redis 广播/单实例调度+补货锁（含瞬态重试退避）；M12 平台厚度；DIM-SOURCE 第 7 路枚举/source_route L4 取值枚举/成本币种【原文未给出，待补】；Key 单租户绑定口径【待补】；真 embedding 模型维度演进；> **Q89 多副本单实例锁切片完成情况（2026-09-15；无迁移纯代码；Q87 挂账“多副本单实例调度+补货锁”提前销账，env 默认关不改 V1 单副本形态）**：✅ 接缝四项经负责人拍板记 **Q89**（02 C1.33，“四项全按推荐（甲）”）：**(1) 机制=Redis SET NX PX 分布式锁**——新横切包 `app/core/locking/`（service.py 单模块），uuid4 owner token、Lua 比对释放/续约（不删别人的锁）、看门狗 TTL/3 周期续约、崩溃靠 TTL 60s 自动释放；**(2) 粒度=循环级两把命名锁** `loom:lock:sla-sweep`/`loom:lock:restock-worker`，持锁副本才跑该轮、抢不到跳过，不做每信号行细粒度认领（Q87 requested 行永不 mutate 不冲击，水平并行消费留 V2）；**(3) `LOOM_DISTRIBUTED_LOCK_ENABLED` 默认 false + fail-closed**——单副本/本地无 Redis 依赖行为不变，开启后 Redis 故障跳轮+error 日志绝不无锁双跑；**(4) 两个手工触发端点同锁**——`POST /api/admin/sla/run`、`POST /api/admin/restock/run` 抢锁失败 409（already running）、Redis 故障 503，RBAC 403 在前。实现：两循环各抽 `_tick()` 包锁，两 router 包锁映射 409/503；看门狗发现锁易主/续约失败仅 error 不打断在跑一轮（fencing token 退场挂账 V2）；无 Alembic 迁移（头仍 0019）；测试 **345 全绿**（+13 集成：内存 FakeRedis——门控关闭放行/NX 互斥重入/异 token 不删/看门狗续约/锁丢失静默/获取故障 fail-closed；两循环三态；两端点 200→409 与 503），ruff 通过；**eval 不新增（仍 101/101，基础设施无业务纯函数）**。**仍挂账（M10 余量/V2）**：~~瞬态失败重试退避（Q87 restock_deferred 仍自然重试）~~ **已随 Q90 落地（见下条）**、Redis Streams/每信号行细粒度认领、配置中心多副本缓存失效广播、fencing token、~~编排并行/DAG~~ **原语已随 Q91 落地（见 Q91 完成情况）**；M12 平台厚度；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 维度演进；两个基线 HTML 缺块。
> **Q90 restock 瞬态失败重试退避切片完成情况（2026-09-15；迁移 0020_restock_retry_state；Q87 瞬态重试挂账销账）**：✅ 接缝四项经负责人拍板记 **Q90**（02 C1.34，“四项全按推荐（甲）”）：**(1) 状态=新表 upsert 游标** `restock_retry_state`（request_id PK/attempts/next_attempt_at 索引/last_reason/updated_at）——Q87 requested 信号行 append-only 纪律不改、瞬态不留子 run 口径不改，可变重试态只落侧表，成功/转终态删游标；**(2) 指数退避 base×2^(attempts−1) 封顶 1800s**（60/120/240/480/960/1800…），定时轮过滤窗口内信号；BudgetExhausted（UTC 日界预算必然恢复）**永不转终态**无限续试，GenerationUpstreamError 连续 **10 次（约 17h）转终态** failed/llm_auto 子 run+restock_failed(terminal, attempts=10) 人工介入并被反连接排除；**(3) env 运维参数非 Q9 旋钮**——`LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`_MAX_SECONDS=1800`/`_MAX_ATTEMPTS=10`（同 Q87 interval/batch、Q89 锁开关一档）；**(4) 手工 /run honor_backoff=False 绕过窗口**（platform_admin 立即试一次），瞬态结果仍 attempts+1 重排窗口、成功清游标。交付：迁移 **0020**（down=0019；pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触 atlas-pg）、`app/core/restock/models.py` + worker 退避/升级分流/游标清理 + router honor_backoff=False；测试 **351 全绿**（Q87 预算用例按新行为更新 +6：退避序列封顶纯函数、窗口到期重认领、预算 11 连试不转终态、上游错第 10 次转终态且删游标/反连接排除、成功清游标、终态清游标），ruff 通过；**eval 不新增（仍 101/101）**。**仍挂账（M10 余量/V2）**：Redis Streams/每信号行细粒度认领、配置中心多副本缓存失效广播、fencing token、~~编排并行/DAG~~ **原语已随 Q91 落地（见下条），真实 fan-out 随 DIM-SOURCE 七路 V2**；M12 平台厚度；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 维度演进；两个基线 HTML 缺块。
> **Q91 注册表驱动编排并行 DAG 原语切片完成情况（2026-09-15；纯代码无迁移，Alembic 头仍 0020；14 §2.2“复杂 DAG 二期”原语部分提前销账）**：✅ 接缝四项经负责人拍板记 **Q91**（02 C1.35，“四项全按推荐（甲）”）：**(1) 只落原语不接真实流**——新包 `app/core/orchestration/`（planner+engine），YAML 步骤可选 `needs`、无 needs 退化为顺序链（WF-01..04 零改动），Kahn 分层+五类加载期校验（未注册引用/未知依赖/环/重复/gate 锚点）；五站点显式端点与 Q84 不自动串链口径不变，DIM-SOURCE 七路 fan-out 连接器 V2 来时声明 needs 接入；**(2) 同层 fail-fast/层间屏障/不自动重试**——任一节点失败取消同层兄弟、整轮 NodeExecutionError（带失败节点+已完成快照）、后层不执行，重试归调用侧（Q90 游标）不双层烧 token；**(3) env 信号量** `LOOM_ORCH_MAX_CONCURRENCY=4`（运维参数非 Q9 旋钮），日预算全局硬停不绕；**(4) skill7 Gate 不跨、不建新表**——只规划第一道 skill7 gate 的 after 步骤+前驱闭包（WF-01 止于 CAT-RECOG、WF-04 仅 PWC-BUILDER），引擎不调模型/不投递/不写 skill_runs，处理器注入，orch_run_id(uuid)+wf_id 经 NodeContext 传递、真实接入时进 run.input。测试 **371 全绿**（+20 单测 tests/unit/test_orchestration_dag.py：规划 12 例含四既有 WF 快照、执行 8 例含并发上限实测/屏障时序/兄弟取消），ruff 通过；**eval 不新增（仍 101/101）**。**仍挂账（M10 余量/V2）**：Redis Streams/每信号行细粒度认领、配置中心多副本缓存失效广播、fencing token、DIM-SOURCE 七路采集连接器（首个真实 fan-out 接入方）；LangGraph 迁移回滚口径不变；M12 平台厚度；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 维度演进；两个基线 HTML 缺块。
> **Q92 M12 首片·两个驾驶舱只读聚合 API 切片完成情况（2026-09-15；纯只读无迁移，Alembic 头仍 0020；Q73“V1 驾驶舱收敛为 2 个”首个落地块，不闭合 M12）**：✅ 接缝四项经负责人拍板记 **Q92**（02 C1.36，“四项全按推荐（甲）”）：**(1) M12 首片=两驾驶舱只读 API**——统一审核工作台（Q70 统一审核列表跨表形态待提案）、租户管理+Onboarding（无租户实体表，Q33 隔离待裁决）、客户前端 8 菜单（18 号文档 🟡 门控）、CSV 中台导出（契约“中台手动用【待补】”待补规格）均留后续切片；**(2) Token 成本口径**——默认近 30 天（today−29…today）可选 `date_from`/`date_to`（半开 UTC，from>to/坏日期 422），计费行=succeeded 且 model_id 非空（embedding 运行 Q86 起计入、价 0 计调用数与 token），按天×model_id×currency_code + skill_id×币种聚合 runs/tokens/cost，failed 不计费按 skill 单列，requested 无 model 排除，多币种分组**不换算**（币种原文【待补】沿用 Q82，per-1M 价格归 Q67 本视图只消费实际成本）；**(3) 人工审核工作量=积压快照+窗口产出**——pending_review 候选按 target_type 计数+最老等待秒数、ops_todos 按 todo_type 拆 open(未到期)/overdue(open 且 due_at 过)/escalated + 四类 totals，窗内产出=候选 reviewed_at 按 state、待办 resolved_at 按类型计数，snapshot_at UTC；Q70 二期项智能派单/10% 抽检/6 场景细分明确不做；**(4) platform_admin 只读+实时 PG 聚合**——`GET /api/admin/dashboards/token-cost|review-workload`，管理面 GET 首次带 actor（query actor_id 必填 422/roles 重复，依赖判 403；真认证 V2，V1 自报同写端点），不建表/不物化/无调度器/无租户过滤参数。交付：新包 `app/core/dashboards/`（service+router，main 挂载），append-only 三表只读；测试 **378 全绿**（+7 集成 tests/integration/test_dashboards_api.py：RBAC 422/403、默认窗聚合与 requested/failed/窗外排除、零成本 embedding、多币种不换算、坏窗 422、积压快照+窗口产出、空库零值），ruff 通过；**eval 不新增（仍 101/101，纯只读聚合无业务纯函数，同 Q89–Q91 口径）**。**M12 未闭合**：审核工作台候选池、租户管理+Onboarding、CSV 导出、客户前端 8 菜单+驾驶舱页面（18 🟡 门控）待后续切片；Skill 注册表+自研编排器最小集已分别随 Q76/Q91 落地。**仍挂账（V2）**：Redis Streams/细粒度认领、配置缓存多副本广播、fencing token、DIM-SOURCE 七路采集连接器（首个真实 fan-out 接入方）；LangGraph 迁移回滚口径不变；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 维度演进；两个基线 HTML 缺块。
> **Q93 M12 第二片·统一审核工作台候选池切片完成情况（2026-09-15；迁移 0021 纯配置种子无业务表变更；Q70 审核运营机制一期①③落地，不闭合 M12）**：✅ 接缝四项经负责人拍板记 **Q93**（02 C1.37，“四项全按推荐（甲）”）：**(1) 范围=统一列表+置信批量通过**——新包 `app/core/workbench/`（risk/service/router/schemas，main 挂 `/api/review-workbench`）；Q70②候选投递自动挂 Q49 SLA 待办（建 todo/裁决 resolve/各型时限）留下一切片，派单/10% 抽检/6 场景细分维持二期；**(2) 跨型风险四档归一**——critical/high/medium/low 沿用 atom RISK_LEVELS：atom_batch 取批内 ai_risk 最高、c1_recognition 行业 sensitive 命中归 critical（Q70“敏感行业/critical”）否则 low、pwc_combo/field_plan/c7_layer4 无信号归 low（risk_reason=no_risk_signal 不编造）；队列 risk_rank DESC+created_at ASC（同档最老先审），筛 state/target_type 可重复/wf_id/risk_level + limit(50,≤200)/offset；**(3) 可见角色=WF skill7 Gate 角色并集（数据驱动）**——注册表动态取（当前 operations+product_reviewer，YAML 加角色自动生效），platform_admin 非裁决角色不放行；GET query actor 同 Q92（422/403），批量再逐候选按所属 WF Gate 角色校验，混角色批整批 403；**(4) 批量通过=新配置键+整批成败一致**——`review.batch_pass_confidence=0.85`（Q70/Q9，不挪用 fieldpool 键，迁移 0021 幂等种子）；仅 pending 且 run.confidence 严格 >线且 risk<high（high/critical 含敏感行业禁批量，承接 Q80）入批；预检 404→409→403→422 全过后单事务复用 skill7 decide_candidate(confirmed)，适配器规则一项不绕、任一失败整批回滚，modified/rejected 仍逐条；补 workbench.batch_approved 汇总审计。测试 **384 全绿**（+6 集成 tests/integration/test_review_workbench_api.py：RBAC 含 platform_admin 403、风险排序与 batch_eligible 旗标、筛选分页、预检 422 零写入、混角色 403/404/409、真实两 atom_batch 批量通过落 4 条 AtomCandidate），ruff 通过；**eval 不新增（仍 101/101，风险归一机械映射经 DB join，同 Q89–Q92 口径走集成）**。**M12 未闭合**：候选 SLA 待办自动挂接（下一片）、租户管理+Onboarding、CSV 导出、客户前端 8 菜单+工作台/驾驶舱页面（18 🟡 门控）；Skill 注册表+编排器最小集（Q76/Q91）与两驾驶舱只读 API（Q92）已落地。**仍挂账（V2）**：Redis Streams/细粒度认领、配置缓存多副本广播、fencing token、DIM-SOURCE 七路采集连接器（首个真实 fan-out 接入方）；LangGraph 迁移回滚口径不变；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 模型维度演进；两个基线 HTML 缺块。
> **Q94 M12 第三片·候选审核 SLA 待办自动挂接切片完成情况（2026-09-16；迁移 0022 五条纯配置种子无业务表变更；Q70②落地，不闭合 M12）**：✅ 接缝四项经负责人拍板记 **Q94**（02 C1.38，“四项全按推荐（甲）”）：**(1) 五型时限各一键**——`review.sla_hours.{pwc_combo,field_plan,c1_recognition,atom_batch,c7_layer4}` 种子统一 72h 且 source_ref 标 Q70②/待补（原文无数值，不编造，热更调值不改码）；**(2) 一候选一 todo、裁决即 resolve**——投递（人工 delivery 与 llm_auto 共用 `_persist_delivery`）flush 后同事务建 OpsTodo（entity_type=skill_candidate、assignee_role=注册表该 WF review_role、detail 带 run/candidate_index），confirmed/modified→resolution=applied、rejected→archived；escalated 不阻断裁决；工作台批量复用 decide_candidate 自动收口；**(3) 五型五个 todo_type**（review_pwc_combo/…/review_c7_layer4），Q92 驾驶舱积压自动分型，Q49 升级审计统一 `skill7.review_sla_escalated`；黄色窗口原文未给→到期前 green 挂账；**(4) 不回填**挂接前存量（无真实数据，resolve 查无静默）。新模块 `app/core/skill7/review_sla.py`（create/resolve 两函数）+ service 投递/裁决接线 + sla.engine 动作映射；sweep/runner/调度器零改动。测试 **391 全绿**（384→391，+7 集成 test_skill7_review_sla_api.py：建待办粒度/角色/72h/链接、多候选、reject/apply 收口、升级审计+升级后裁决、批量收口、驾驶舱 overdue 分组；WF-01 两例总数断言改按 todo_type 计数），ruff 通过；**eval 不新增（仍 101/101，写路径挂接+配置时限由集成覆盖，同 Q89–Q93 口径）**。**M12 未闭合**：五型时限具体数值与黄色窗口待业务方补、租户管理+Onboarding、CSV 导出、客户前端 8 菜单+工作台/驾驶舱页面（18 🟡 门控）；Q70 一期（统一列表/批量/SLA）三件已全部落地，二期派单/抽检/6 场景细分随 V2。**仍挂账（V2）**：Redis Streams/细粒度认领、配置缓存多副本广播、fencing token、DIM-SOURCE 七路采集连接器；LangGraph 迁移回滚口径不变；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 模型维度演进；两个基线 HTML 缺块。

> **Q95 M12 第四片·租户管理+Onboarding 切片完成情况（2026-09-16；迁移 0023 建 tenants 表+DISTINCT 回填；D3.11①③ 部分落地，不闭合 M12）**：✅ 接缝四项经负责人拍板记 **Q95**（02 C1.39，“四项全按推荐（甲）”）：**(1) 租户注册表+生命周期端点+段1 准入门，不含团队账号/前端**——`app/core/tenants/` 挂 `/api/admin/tenants`（开通/列表/详情派生 Onboarding 进度/改套餐/暂停/恢复，全程 writeAudit，GET query actor 422·403、写 actor 在体 403·404·409·422）；`POST /api/intakes` 未知租户 404、暂停租户 409，应用层闸无硬外键，链内端点仍从 intake/PS 锚点派生租户；**(2) plan 四档（trial/basic/pro/enterprise）、status 三态（trial/active/paused）**——开通 trial→trial+月额度 50 万（line 1466 唯一有值档位），付费档开通→active/额度 null，改套餐不动 status，暂停→paused/恢复→active；价格仅展示、不计费不拦截，付费档额度与 §C2“5 档”疑义【待补】；**(3) Onboarding=派生进度无状态机**——注册/套餐由开通覆盖，建模→拆解→上线复用既有链，详情回带 intakes/product_spaces 计数，团队设置随 V2 账号；**(4) 0023 回填**存量 DISTINCT tenant_id 为 basic/active+detail.backfilled，存量数据/测试零阻断。新包 tenants（models/schemas/service/router）+ 段1 create_intake 接线；迁移 0023（建表+回填，Alembic 头 0022→0023）。测试 **401 全绿**（391→401，+10 集成 test_tenants_api.py；7 个 HTTP 建单既有测试夹具补播 t1），ruff 通过；**eval 不新增（仍 101/101，管理/装配逻辑由集成覆盖，同 Q87–Q94 口径）**。**M12 未闭合**：付费档额度与额度拦截、计费订阅、团队账号（均 V2/待补）、CSV 中台导出（契约【待补】）、客户前端 8 菜单+工作台/驾驶舱/租户/引导页面（18 🟡 门控）；五型审核 SLA 时限具体数值待业务方补；Q70 二期派单/10% 抽检/6 场景细分随 V2。**仍挂账（V2）**：Redis Streams/细粒度认领、配置缓存多副本广播、fencing token、DIM-SOURCE 七路连接器；LangGraph 迁移回滚口径不变；DIM-SOURCE 第 7 路/source_route L4/成本币种/Key 单租户绑定【原文未给出，待补】；真 embedding 模型维度演进；两个基线 HTML 缺块。

> **Q96 前端 i18n+tokens.css 脚手架切片完成情况（2026-09-16；纯前端无迁移，Alembic 头仍 0023；客户前端前置切片，不闭合 M12 8 菜单）**：✅ 按 docs/18 §2.3/§3.6 落地（feat 946a774 / test 8b614b6）：**(1) next-intl v4 三件套**——`i18n/routing.ts`（defineRouting 仅注册 zh-CN）、`i18n/request.ts`（hasLocale 兜底+动态 import messages）、`middleware.ts`（matcher 排除 api/_next/_vercel/带点文件；无前缀请求 **307**→/zh-CN 为 next-intl 默认实测，docs/18 原写 308 已更正）；next.config.mjs 接 createNextIntlPlugin；页面全部迁入 `app/[locale]/`，旧根 app/layout.tsx、app/page.tsx 删除。**(2) 消息表** messages/zh-CN.json 暂仅 common/home/error 命名空间，M12 页面随片扩域；枚举码/fid/审计码不翻译。**(3) Token** tokens.css 唯一事实源 Primitive+Semantic 85 变量 + tokens.ts 镜像；Semantic 较 §3.6 增 `--color-brand-bg`（消 Primitive 直引，补登 docs/18 §5）。**(4) 一致性测试** scripts/check-tokens.mjs 零依赖逐值校验（npm run check-tokens），失败 exit 1。验证：typecheck/build 通过（/zh-CN SSG），curl `/`→307、`/zh-CN`→200，9222 Chrome 可视验收通过（html lang/消息文案/语义变量计算值）；无后端变化，401 测试与 101 eval 不变。**M12 未闭合**：客户前端 8 菜单+工作台/驾驶舱/租户/引导页面逐片点工；CSV 中台导出（契约【待补】）；付费档额度/计费订阅/团队账号（V2/待补）。**仍挂账（V3/V2）**：外语 en-US/深色/白牌/stylelint/W3C token 链/RTL/TMS；Redis Streams/配置广播/fencing token/DIM-SOURCE 七路连接器等 V2 挂账同前片。

> **Q97 M12 客户前端切片 1·应用外壳与 D5 八菜单导航骨架完成情况（2026-09-16；纯前端无迁移，Alembic 头仍 0023，401 测试/101 eval 不变；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q97**（02 C1.41，“四项全按推荐（甲）”）：**(1) 八菜单全显+V2 占位**——路由组 `app/[locale]/(shell)/` 落 workbench/products/content/templates/analytics/social-accounts/compliance/settings 八页（共享 menu-placeholder），挂段8/12/13 的 content/analytics/social-accounts 三项带“V2”徽标与 V2 文案，其余 V1 占位；**(2) 左侧 240px 边栏+64px 顶栏+1280 内容列**——sidebar 为 client 组件（next-intl Link/usePathname，active aria-current），样式只消费 Semantic/刻度；新增语义别名 `--color-bg-hover`（86 token）；**(3) `/[locale]` 307→`/[locale]/workbench`**，Q96 欢迎页删除，消息命名空间改 common/nav/shell/error（home 删除）；**(4) 管理端未来 `/[locale]/admin/*` 独立布局**，本切片不预埋。新增 `i18n/navigation.ts`（createNavigation）与注册表 nav.ts；测试原子 `scripts/check-nav.mjs`（npm run check-nav：注册表↔消息 key↔页面文件三方一致，ghost 项失败路径实测三报全中）。typecheck/build（8 页 SSG）、curl 两级 307/8 路由 200、9222 Chrome 可视验收（240px/语义边框色/客户端导航 active/V2 徽标/控制台零错）均通过。feat 345e15e/test 09deafd。**M12 未闭合**：八菜单真实功能（产品中心段1→6 页面、工作台数据等）、管理端布局与 Q92/Q93/Q95 页面、前端 API client/actor 约定、CSV 中台导出（契约【待补】）、付费档额度/计费/团队账号（V2/待补）。**仍挂账（V2/V3）**：外语/深色/白牌等同 Q96；段12/13 客户功能、Redis Streams/配置广播/fencing token/DIM-SOURCE 七路连接器等 V2 挂账同前片。


> **Q98 M12 客户前端切片 2·产品中心功能页完成情况（2026-09-16；跨栈无迁移，Alembic 头仍 0023，后端 404 测试/101 eval；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q98**（02 C1.42，"四项全按推荐（甲）"）：**(1) 跨栈最小闭环**——后端补 `GET /api/intakes?tenant_id=&limit=&offset=`（IntakeList 分页，读路径不触发 Q95 准入门，未知/暂停租户 200 空列表），前端落"我的产品"列表、"新建产品"（Server Action 建 draft）、只读详情（404→notFound）；产品模板/资料库保留 V1 占位；不含 15 态迁移操作。**(2) env 固定租户**——server-only `LOOM_TENANT_ID`，V2 认证后改会话派生，不做切换器。**(3) Next 服务端代理**——RSC/Server Action 调 FastAPI（server-only `LOOM_API_BASE_URL`，lib/api.ts 唯一访问层），浏览器不直连、后端不加 CORS；列表页 force-dynamic。**(4) 仅产品名**——profile 工程临时键 `product_name` 显式挂账（G2 fid 基线未回填），缺失回退 id 前 8 位。测试原子：后端 +3 集成（空列表/分页隔离/422），前端 check-products.mjs（消息键/文件/禁 NEXT_PUBLIC/指令边界，失败路径实测）。9222 Chrome 端到端验收：真实数据列表、CJK 新建→详情闭环、未知 id 404、占位页、零直连 :8000、控制台零错。feat 6693c18/test a9f4c4a。**M12 未闭合**：其余七菜单功能、15 态迁移操作页、产品模板/资料库、管理端布局与 Q92/Q93/Q95 页面、CSV 中台导出（契约【待补】）、付费档额度/计费/团队账号（V2/待补）、fid 基线回填与 product_name 对齐。
> **Q99 M12 客户前端切片 3·工作台功能页完成情况（2026-09-16；跨栈无迁移，Alembic 头仍 0023，后端 407 测试/101 eval；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q99**（02 C1.43，“四项全按推荐（甲）”）：**(1) 跨栈最小总览**——后端补 `GET /api/intakes/overview`（IntakeOverview{total,by_status}，读路径不触发 Q95 准入门，未知/暂停租户 200 零值；注册序先于 /{intake_id}），前端工作台落录入单总数 + 15 态分组 + 快捷入口（新建产品/查看全部）。**(2) D5 五指标 V2 禁用态**——今日生成/发布/互动/趋势/健康度（docs/09 D5 表，数据源全在段 12/13）照原文渲染五案但置灰 + V2 徽标 + 挂账文案，不显任何数字（0 也是业务断言）。**(3) intake.status.* 15 态落地**——消息键逐一对齐 docs/13 §1.1 权威中文名（枚举码不翻译），按 docs/13 顺序仅渲染计数 > 0；Q98 产品列表/详情状态 chip 一切片切到该命名空间，docs/18 §7 临时英文码口径收口。**(4) 状态聚合独立端点**——单次 group by，total=分组和，不扩 IntakeList 契约。测试原子：后端 +3 集成（零值不被 /{intake_id} 吞/跨态分组+租户隔离/422），全套 407=404+3；check-workbench.mjs（消息键/force-dynamic/五卡/跨栈路由顺序守卫）、check-products.mjs 增裸状态码守卫，失败路径均实测。9222 Chrome 端到端验收：t1 四单（draft2/submitted/stored）总数与分组真实渲染、五卡无数字、快捷跳转、active、零直连 :8000、控制台零错；ghost 租户空态实测。feat 43f4fcb / 659d959，test 1f05c27 / a0a6ca3。**M12 未闭合**：五指标真实数据随段 12/13（V2）、其余六菜单功能页、15 态迁移操作页、产品模板/资料库、管理端布局与 Q92/Q93/Q95 页面、CSV 中台导出（契约【待补】）、付费档额度/计费/团队账号（V2/待补）、fid 基线回填与 product_name 对齐。

> **Q100 M12 中台导出·FCW final_id 单列 CSV 切片完成情况（2026-09-16；后端纯只读无迁移，Alembic 头仍 0023，后端 410 测试/101 eval；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q100**（02 C1.44，“四项全按推荐（甲）”）：**(1) 排期**——CSV 导出独立切片先做，客户前端八菜单各自点工（下一切片预告合规风控，本切片不开工）。**(2) CSV 仅 final_id 单列**（含表头）——守 M12「中台只消费 final_id」，不拼 6 层原料包；docs/09:87 的 6 层包属台内卡片详情/复制口径，09 已就地加注分界；docs/05 D4 行【待补】就 CSV 部分收口（JSON 形态仍挂后续）。**(3) 同步 GET 流式**——`GET /api/exports/fcw.csv`（tenant_id 必填空则 422、product_space_id 可选收窄；text/csv; charset=utf-8 + attachment filename=fcw-<tenant>.csv），只导 published（draft 排除；Q32 revoked 落地后同过滤自然生效）、created_at DESC；不分页不异步（异步任务/对象存储挂 V2）。**(4) 读路径纪律**同 Q98/Q99：不触发 Q95 准入门、不 writeAudit，未知租户 200 仅表头空文件。新横切只读包 `app/core/exports/`（service/router），main 挂 `/api/exports`。测试原子 +3 集成（ghost 仅表头+头断言、5 行播种验 published 过滤/严格单列无逗号/租户隔离/空间收窄、422），全套 410=407+3，ruff 干净，eval 不新增仍 101/101。curl 端到端验收（/tmp 一次性 SQLite，不碰 Docker PG）：t1 表头+3 条、ps-other 收窄、t2 隔离、ghost 仅表头、422、content-type/disposition 全符，验收后进程与临时文件全清。feat 12884b7 / test e67c604。**M12 未闭合**：客户前端六菜单功能页（下一预告合规风控）、15 态迁移操作页、产品模板/资料库、管理端 `/[locale]/admin` 布局与 Q92/Q93/Q95 页面、CSV JSON 形态与异步导出、付费档额度/计费/团队账号（V2/待补）、fid 基线回填与 product_name 对齐。

> **Q101 M12 客户前端切片 4·合规风控只读功能页完成情况（2026-09-16；跨栈只读无迁移，Alembic 头仍 0023，后端 414 测试/101 eval；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q101**（02 C1.45，“四项全按推荐（甲）”）：**(1) 只读总览+行展开明细**——active frozen PWS 一行一产品，行内 CCR 从严结论 chip+block_required 阻断标记+法审 chip，展开逐市场最新报告（ban 词/降级建议）与法审 dl；清洗触发/降级批准/法审裁决/CP-LAW 词库 CRUD 一律不进客户页（页面注明台内执行，写口仍 internal_compliance）。**(2) 新租户只读聚合端点** `GET /api/compliance/overview?tenant_id=`（必填非空 422；未知租户 200 空 items；不触发 Q95、不 writeAudit）；产品名仍走 profile.product_name 工程临时键，无名回退产品空间 ID 前 8 位。**(3) 仅 active frozen 每 PWS 一行**——各市场 append-only 报告取 created_at,ccr_id 双 DESC 首行，跨市场 blocked>downgrade_pending>approved>clean 从严、block_required OR；superseded/非 active/他租户干扰行实测不污染；从未清洗 ccr=null 显「尚未清洗」，不伪造 clean。**(4) 法审状态+领域+结论只读**——一 PWS 一条，pending/approved/rejected chip+domain+decided_at/conclusion，未触发显「未触发法审」，不派生 SLA 超时态。前端 compliance.module.css 新增四语义 tone 但 token 总数仍 86；测试原子 +4 集成与 check-compliance 零依赖守卫（消息键/force-dynamic/只读禁写台面/路由 Query 守卫，失败路径实测 exit 1），全套 414=410+4，ruff 干净，eval 不新增。端到端验收（/tmp 一次性 SQLite，不碰 Docker PG；uvicorn 8765 + next start 3210 + 9222 Chrome）：t1 两行（修护精华 CN/US blocked+FDA+medical 待法审；洁净面霜底座降级 焕白→提亮）、superseded/t2 干扰不出现、展开明细完整、chip 计算色为语义 danger/warning、侧栏 active=合规风控、浏览器零直连 :8000、控制台仅 favicon 既有 404；ghost 重启验空态零行；curl 空 items/422。feat da644e9 / test df2c3b8。**M12 未闭合**：余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2 占位）、15 态迁移操作页、产品模板/资料库、管理端 `/[locale]/admin` 布局与 Q92/Q93/Q95 页面、付费档额度/计费/团队账号（V2/待补）、fid 基线回填、CCR 历史报告视图/法审 SLA 倒计时/规则库界面（台内或 V2）。

> **Q102 M12 管理端首片·独立布局 + Q92 两驾驶舱只读页完成情况（2026-09-16；纯前端无迁移，Alembic 头仍 0023，后端 414 测试/101 eval 不变；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q102**（02 C1.46，“四项全按推荐（甲）”）：**(1) 独立 admin 路由组+两驾驶舱真实只读页**——新建 `app/[locale]/admin/` 与客户 `(shell)` 平级（独立 frame/侧栏/“Loom 管理端”顶栏），侧栏仅 Token 成本/人工审核工作量两项 + 返回客户前台（/workbench）；**客户 shell 八菜单注册表不放入口**，直接 URL 访问，checker 加 nav 恰 8 项与 admin 不在 (shell) 内的回归守卫；Q93 审核台页/Q95 租户页留后续。**(2) 管理员身份 server-only env 自报**——LOOM_ADMIN_ACTOR_ID + LOOM_ADMIN_ROLES（默认 platform_admin），RSC 拼 actor_id 与重复 roles query（对齐 require_admin_view），无 NEXT_PUBLIC、不进浏览器包，未配置 actor 显 unconfigured 且不发请求；V2 真认证改会话派生（与客户侧 LOOM_TENANT_ID 同批挂账）。**(3) 仅默认近 30 天窗 + 回显**——无日期筛选器/改窗 UI，页顶回显 window.from/to（工作量页另回显 snapshot_at）。**(4) 枚举码原样不翻译**——model_id/skill_id/currency_code/target_type/todo_type/state 直出，中文只覆盖标题/表头/区块/合计卡；oldest_wait_seconds 纯机械换算（天/小时/分钟，null 显 —）。两页 RSC force-dynamic，403（非管理员）/422→error 文案；admin.module.css 只消费既有语义 token，**token 总数仍 86**。测试原子为 check-admin.mjs 零依赖守卫（消息键/枚举码不得入消息表/六文件/force-dynamic/两 getter/禁 NEXT_PUBLIC/禁硬编码色/env 名与 append roles/nav 隔离/后端路由 prefix+两 GET+require_admin_view；失败路径注入硬编码色实测 exit 1）；后端零改动故无新增后端测试。端到端验收（/tmp 一次性 SQLite，不碰 Docker PG；uvicorn 8765 + next start 3210 + 9222 Chrome）：Token 页（同模型同日两行聚合 runs=2、零成本 embedding CNY 计入调用、failed 单列、窗外/requested 排除、合计 3/3500/800/1）与工作量页（pwc_combo 2 待审 1 天、atom_batch 1 待审 3 小时、open/overdue/escalated 分桶、窗内 confirmed/rejected 各 1 与 field_plan resolved 1、窗外 c1_recognition 两表均排除）全部与后端 JSON 一致；/zh-CN/admin 307→token-cost；auditor 角色重启显「没有操作权限」；去 actor 重启两页显「未配置管理员身份」；客户 /workbench 侧栏实测恰 8 链接无 admin；浏览器网络零直连后端、零业务 console 错；curl 无 actor_id 422、roles=auditor 403。feat 9ba1b5b / test 2bc8765。**M12 未闭合**：客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2 占位）、Q93 统一审核台页（含批量通过）、Q95 租户管理/Onboarding 页、15 态迁移操作页、产品模板/资料库、付费档额度/计费/团队账号（V2/待补）、fid 基线回填、管理端日期筛选器与其余四个治理驾驶舱（不进 V1）。

> **Q103 M12 管理端第二片·Q93 统一审核工作台前端页完成情况（2026-09-16；跨栈加法无迁移，Alembic 头仍 0023，后端 414 测试/101 eval 不变；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q103**（02 C1.47，“四项全按推荐（甲）”）：**(1) 形态=列表+筛选+批量通过**——新建 `app/[locale]/admin/review-queue/`（避让已占用的 /admin/review-workload），五型混排按后端既定序（risk_rank DESC、同档 created_at ASC）；仅 batch_eligible 候选可勾选，high/critical 与低置信候选勾选框禁用（title 中文提示需逐条审核），一键 POST /batch-approve 经 Server Action（actor 取自服务端 env），整批 all-or-nothing，成功 router.refresh() 回列表；逐条 confirmed/rejected/**modified 裁决（含替换 payload 编辑器）明确留下一切片**。**(2) URL searchParams 服务端筛选+简分页**——target_type 五型多选 chip（原生 GET checkbox）、risk_level 下拉（空=不限+四档英文码）、state 下拉（默认 pending_review，可切 applied/archived）、wf_id 文本，全部经 URL 驱动 RSC（白名单归一、非法回默认），浏览器零后端直连；limit=50、offset 上一页/下一页（Link 保留筛选 query），回显“第 from–to 条，共 total 条”；批量勾选与提交为最小 client 岛 BatchBar（只调 Server Action、禁 import lib/api，checker 守卫）。**(3) payload 原样 JSON 折叠**——每行 `<details><pre>JSON.stringify(payload, null, 2)` 原样直出，不为五型造字段中文名。**(4) 风险四档语义色 chip+英文码原样**——critical/high/medium/low 分别消费 danger/warning/info/success 语义 token（*-bg + state-*-text），不造“严重/高/中/低”；risk_reason 机读来源码原样副显；confidence 显百分比（null 显 —），页顶回显 batch_pass_confidence=0.85 门槛原文。后端唯一**加法**：workbench 队列视图 `_candidate_view` 补 `"payload": cand.payload`（Q93 原视图不返回 payload 且无单候选详情端点；既有测试不按键集断言无破坏，仅风险排序用例补一条 payload 断言）。前端 lib/api.ts 增 ReviewQueue(Candidate)/getReviewQueue（actor_id+重复 roles+重复 target_type 等）/batchApprove（body 内 actor:{id,roles}）；zh-CN 增 admin.reviewQueueNav + `admin.reviewQueue.*` 33 键，五型码/四风险码/三态码不入消息表（check-admin 精确串守卫）；admin-sidebar 增第三项；admin.module.css 只消费既有 **86 token 不新增**。测试原子 check-admin.mjs 扩展（消息键/枚举禁译/三文件齐备/force-dynamic/只读禁写/十二枚举码原样/data-batch-candidate/details+JSON.stringify/Server Action 403·404·409·422/client 岛禁直连/侧栏第三链接/api.ts 两端点/后端 prefix+两路由+require_workbench_view+payload 字段；失败路径实测：注入硬编码色 exit 1、client 岛注入 @/lib/api import exit 1，均还原）。端到端验收（/tmp 一次性 SQLite，不碰 Docker PG；uvicorn 8765 + next dev 3210 + 9222 Chrome）：直插六型夹具 + 真实 HTTP 走 intake→field-pools(ops)→gate approve(reviewer)→两次 /api/skill-runs（confidence 0.9/0.92 各 2 atom）造可批量候选；默认 8 行排序、四档 chip computed rgb 取证 danger/warning/success、risk_reason 原样、置信度百分比、不可批量四行禁用 title、payload 折叠等宽 JSON、?target_type=atom_batch 4 行 chip 预勾；勾选 WF-01 候选（reviewer 无 Gate 角色）→红文“没有操作权限”403 仍 pending；勾选两条真实投递候选→绿文“已批量通过 2 条候选”，刷新 pending=6；?state=applied 见新两条 reviewed_by=rev-1、勾选全禁用、无批量条；网络面板仅同源 :3210（两 Server Action POST + _rsc GET）零直连、console 零错；去 actor 重启显“未配置管理员身份”无表无请求，auditor 角色重启显“没有操作权限”；curl 无 actor 422/auditor 403/过滤口径与后端 JSON 一致。feat 8c7fe47 / test 8e4f496。**M12 未闭合**：逐条裁决（confirmed/rejected/modified + payload 替换编辑器）为紧邻下一片、Q95 租户管理/Onboarding 前端、客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2）、15 态迁移操作页、产品模板/资料库、付费档额度/“第5档”【待补】/额度拦截/计费订阅/团队账号（V2）、五型 review SLA 时限数值（72h 待补）、fid 基线回填、CSV JSON 形态/异步导出、管理端日期筛选器与其余四治理驾驶舱（不进 V1）、CCR 历史报告/法审 SLA 倒计时/规则库界面。

> **Q104 M12 管理端第三片·Q93 统一审核工作台逐条裁决完成情况（2026-09-16；跨栈加法无迁移，Alembic 头仍 0023，后端 415 测试/101 eval 不变；不闭合 M12）**：✅ 四接缝经负责人拍板记 **Q104**（02 C1.48，“四项全按推荐（甲）”）：**(1) 范围=只做逐条裁决 UI+工作台视图一个加法字段**——复用既有 `POST /api/skill-candidates/{id}/decision`（无新写口/无新端点/无迁移），后端唯一加法为 workbench `_candidate_view` 补 `"review_note": cand.review_note`；三态语义沿用既有 skill7 契约（rejected→archived+review_note；modified→替换 cand.payload+human_modified=True 再过真实适配器；confirmed→直接适配器；403 按 WF review_role 逐候选/404/409 仅 pending_review/422 modified 缺 payload·结构预校验·适配器业务校验）。**(2) 每个 pending_review 行一个 client 岛 DecisionCell 三按钮**——通过（confirmed；critical 档 window.confirm 中文二次确认，取消阻断）/修改后通过（modified；内联 JSON textarea 预填美化 JSON，前端 JSON.parse 且必须为非数组对象，非法显 invalidJson 零请求）/拒绝（rejected；理由可选），modified/rejected 面板均带可选理由；全部只经 Server Action `decideCandidateAction`（actor 取 server env、判别联合 DecideActionResult、useTransition+router.refresh），client 岛禁 @/lib/api（连 type import 也禁）与硬编码 URL。**(3) 裁决列三态都渲染**——pending_review 出岛；applied/archived 出状态 chip（stateApplied/stateArchived 语义 token）+裁决时间+已人工修改标记+裁决备注（空值不渲染），岛仅 pending_review。**(4) 枚举码原样不翻译、422 英文 detail 原样红文**——confirmed/modified/rejected 与三态码不入消息表；适配器业务错误 FastAPI {"detail"} 英文原样回显不翻译不包装；403/404/409 走既有中文 error 键，unconfigured 走 admin.unconfigured，其余 unknown。前端新增 decision-cell.tsx（唯一 client 岛，Mode idle/modify/reject），actions.ts 加 CandidateDecision 联合/errorDetail/decideCandidateAction（KNOWN_STATUSES 403·404·409·422；仅 modified 传 payload；空 id→404；trim reason；batchApproveAction 保留），lib/api.ts 加 ReviewQueueCandidate.review_note 与 CandidateDecision(Body)/decideCandidate（body actor:{id,roles}），page.tsx 加 colDecision 列；zh-CN 新增 13 键（intro 补“逐条裁决（通过 / 修改后通过 / 拒绝）”），admin.module.css 追加 7 类只消费既有 **86 token 不新增**。测试原子：check-admin.mjs 加 13 必备键/必备文件/decideCandidate+"/api/skill-candidates/" api token/review_note 字段守卫/Q104 块（decision-cell existsSync 缺失文件探针先守卫再读——直接 readFileSync 缺失会抛栈；"use client"/禁 @/lib/api·URL/decideCandidateAction/router.refresh/JSON.parse/window.confirm/三枚举；actions split 函数名后取体内含 403·404·409·422 避命中 batchApproveAction；api 正则子路径 /decision`；page <DecisionCell/colDecision/岛仅 pending_review），后端风险排序用例补 review_note is None + 新增 `test_queue_echoes_review_note_after_rejection`（rejected→archived 视图回显备注、human_modified False）。端到端验收（/tmp 一次性 SQLite loom-q104.db，不碰 Docker PG；uvicorn 8765+next dev 3210+9222 Chrome；种子走真实 HTTP：admin/tenants（0023 租户注册前置）→intakes→field-pools(ops)→gate approve(reviewer)→两次 WF-03 atom 投递+一次真实 CAT-RECOG WF-01 medical 投递 conf 0.99→critical）七项全过：①critical 二次确认取消阻断/接受放行（真实 modeling 适配器 applied；直插缺 intake_id 候选 confirm 被适配器 404，实证适配器不被绕过）②modified 非法 JSON 前端拦截零请求；适配器 422 英文 detail（dimension d-nope is not a selected dimension of the pool）原样红文；合法替换落库 human_modified+note 且 atom-candidates 出现人工改单内容 ③rejected 备注落库、archived、reviewed_at 有值 ④applied/archived tab 渲染 chip+裁决时间+已人工修改+裁决备注且无岛 ⑤reviewer 裁决 WF-01 →403“没有操作权限” ⑥无 actor env→unconfigured 中文提示零行 ⑦网络仅同源 :3210 Server Action 零直连、console 零消息。E2E 教训：React 受控 textarea/input 上 chrome-devtools fill 可能不更新 state（refocus 后提交带旧值），Ctrl/Meta+A 不选中文本、type_text 插光标中；可靠做法=evaluate_script 原生 value setter（HTMLTextAreaElement/HTMLInputElement prototype descriptor）+dispatch input 事件再 DOM click。feat 13d29f9 / test 5fa0086。**M12 未闭合**：Q95 租户管理/Onboarding 前端、客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2）、15 态迁移操作页、产品模板/资料库、付费档额度/“第5档”【原文未给出，待补】/额度拦截/计费订阅/团队账号（V2）、五型 review SLA 时限数值（72h 待补）、fid 基线回填、CSV JSON 形态/异步导出、管理端日期筛选器与其余四治理驾驶舱（不进 V1）、CCR 历史报告/法审 SLA 倒计时/规则库界面。

> **Q105 M12 管理端第四片·Q95 租户管理 + 客户入驻 Onboarding 前端页完成情况（2026-09-16；纯前端无迁移，Alembic 头仍 0023，后端零改动 415 测试/101 eval 不变；不闭合 M12）**：✅ 四接缝经负责人"推进下一片，按你的推荐来"=全按推荐（甲）拍板记 **Q105**（02 C1.49）：**(1) 范围=纯前端消费 Q95 既有端点**——新建 `app/[locale]/admin/tenants/` 列表 RSC（GET 全量）+开通租户表单岛+每行操作岛（改套餐/暂停/恢复），详情 `tenants/[tenantId]/` 只读注册表六字段+Onboarding 派生三字段；写操作全走 Server Action（provision/change-plan/pause/resume），复用 Q95 六路由，无新写口/无删除（后端无此口不造）、无迁移。**(2) 枚举码原样不翻译、状态走语义 chip**——plan（trial/basic/pro/enterprise）/status（trial/active/paused）不入消息表、下拉表格原样直出；三态 chip 套 riskChip 基类 statusTrial=info/statusActive=success/statusPaused=warning；付费档 monthly_token_quota=null 显 —，仅 trial 显 500000，付费档各档额度与"第5档"【原文未给出，待补】不造数。**(3) Onboarding 纯派生只读**——只显 intakes/product_spaces 计数与 first_modeling_started（bool(spaces)），onboardingNote 注明实时派生无独立引导状态机，不造完成率/引导步骤。**(4) 身份/错误沿用 Q104**——platform_admin server env 自报（无 actor→unconfigured 零请求），判别联合 403/404/409/422（+FastAPI {"detail"} 英文 detail 经 errorDetail JSON.parse 原样红文，本切片仅 provision InvalidPlan 走此路）/unconfigured/unknown；两 client 岛禁 @/lib/api（连 type import 也禁）与 URL，仅同源 Server Action。前端结构：tenant-codes.ts（四 plan/三 status as const 单一事实源）、actions.ts（"use server" 四导出名 provisionTenantAction/changePlanAction/pauseTenantAction/resumeTenantAction，TenantActionResult 判别联合，KNOWN_STATUSES 四码，空 id provision→422 其余→404，空名→null）、provision-form.tsx（开通岛，成功清空+router.refresh）、tenant-row-actions.tsx（行岛，改套餐同档禁用，暂停/恢复按 status 二选一）、page.tsx（force-dynamic 七列，空态 colSpan 7）、[tenantId]/page.tsx（force-dynamic，params Promise await，注册表 dl+Onboarding dl，first_modeling_started 显是/否）；lib/api.ts 增 TenantView/TenantOnboarding/TenantDetailView 与 listTenants/getTenant/provisionTenant/changeTenantPlan/pauseTenant/resumeTenant（adminPath 编码，写口 actor{id,roles}）；admin-sidebar 增第四项；zh-CN 增 tenantsNav+admin.tenants.* 29 键；admin.module.css 追加 provisionForm/provisionFields/planChip/statusTrial/statusActive/statusPaused/detailList/onboardingNote 只消费既有 **86 token 不新增**。测试原子：check-admin.mjs 扩展（tenantsNav+29 键/六文件/六枚举码入禁译表/侧栏第四项/api 六函数+prefix+/change-plan·/pause`·/resume`；Q105 块——两页面 force-dynamic/禁 NEXT_PUBLIC·URL·写词/必用 listTenants·getTenant、详情页必含派生三字段、actions "use server"+四导出名+四状态码+CURRENT_ADMIN_ACTOR_ID、两岛 "use client"/禁 @/lib/api·URL/必含 router.refresh/四 action 至少一岛引用、codes 四 plan 原样、三态 statusXxx 样式必在页面、后端 router prefix+require_admin_view+六路由探针——详情探针 `'@router.get("/{tenant_id}"'` 不带尾括号匹配 response_model 签名；失败探针向未跟踪岛追加 @/lib/api import 实测 exit 1，新文件 git checkout 报路径未匹配故 Edit 删行还原）。端到端验收（/tmp 一次性 SQLite loom-q105/loom.db，不碰 Docker PG；uvicorn 8765+next dev 3210+9222 Chrome；async 种子建表+三租户 t1 pro/active/null 带 1 intake stored+1 space general、t2 无名 trial/trial/500000、t3 basic/paused/null）八项全过：①列表三行+侧栏第四项，三态 chip computed 色 active rgb(246,255,237)/rgb(56,158,13)、trial rgb(230,244,255)/rgb(9,88,217)、paused rgb(255,251,230)/rgb(212,136,6)，额度 —/500000/—、plan 等宽原样 ②t1 详情录入单数 1/空间数 1/已开始首次建模 是 ③nope→"资源不存在"（404 中文）④原生 setter 开通 t4 enterprise"新开通企业客户"→provisionSuccess+新行 enterprise/active/—+表单重置+同档改套餐禁用 ⑤重复 t1→"操作冲突，请刷新后重试"（409）⑥t2 改套餐 trial→pro 成功且 status 保持 trial（change-plan 只动 plan 的续费口径）、额度 500000→— ⑦t4 暂停→paused chip+恢复按钮，恢复→回 active ⑧重启 LOOM_ADMIN_ROLES=auditor→"没有操作权限"（403），去 actor→unconfigured 中文零行；网络仅同源 :3210（Server Action POST+_rsc GET）零直连 :8765、console 零消息；验收后进程与 /tmp 文件全清。feat 9ffb7cf / test 058c8bb。**M12 未闭合**：客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2）、15 态迁移操作页、产品模板/资料库、付费档额度/"第5档"【原文未给出，待补】/额度拦截/计费订阅/团队账号（V2）、五型 review SLA 时限数值（72h 待补）、G2 cat='common' 18 字段 fid 基线回填、CSV JSON 形态/异步导出、管理端日期筛选器与其余四治理驾驶舱（不进 V1）、CCR 历史报告/法审 SLA 倒计时/规则库界面。

> **Q106 M12 客户前端第五片·15 态录入单客户操作 + draft 资料编辑完成情况（2026-09-16；纯前端无迁移，Alembic 头仍 0023，后端零改动 415 测试/101 eval 不变；不闭合 M12）**：✅ 四接缝经负责人"推进下一片，按你的推荐来"=全按推荐（甲）拍板记 **Q106**（02 C1.50）：**(1) 范围=纯前端消费段1 既有四端点**（GET allowed-events / POST transitions / PATCH profile / GET product-space），无新写口/无迁移；详情页按 allowed-events 与**客户事件白名单 `{submit, params_completed, quota_confirmed, resubmit}`**（docs/13 §1.1）的交集渲染按钮——ops_confirm/to_cold_start/reject/b2_* 为运营事件、wf01_*/auto_confirm 为 Skill/系统事件、send_review/review_*/start_modeling/model_*/failed_* 触发方【原文未给出，待补】故一律不外放，终态/空交集显"当前状态暂无可执行操作"。**(2) draft 资料编辑岛仅 draft 态出**——只编辑产品名（fid 沿用 Q98 工程临时键 product_name，G2 18 字段 fid 基线回填继续挂账），PATCH 409 ProfileNotEditable 走 error.409，其他态不出岛。**(3) 身份=新增 server-only `LOOM_ACTOR_ID`**——actor {id, roles:[]} 恒空（客户事件 requires_role 均 None），未配置→只读数据正常、两岛位置显 unconfigured 中文且零写请求，V2 与 LOOM_TENANT_ID/LOOM_ADMIN_ACTOR_ID 同批改会话派生。**(4) 422/只读区**——transitions 422 体 {"detail":{"missing_fids":[...]}} 解析后 fid 原样码"、"拼接中文句式回显（不翻译不造名）；产品空间存在时只读显 id/lifecycle（码原样等宽）+快照，404=null 不造；category_creating 显 docs/13 原文中性话术"资料分析中"，b2_* 滤掉无按钮。前端结构：intake-codes.ts（四码 as const 单一事实源+类型守卫，注释列三类排除理由）、intake-actions.tsx（操作岛，交集按白名单序出按钮）、draft-profile-form.tsx（编辑岛，required/≤100）、actions.ts（保留 createProductAction；IntakeActionResult 判别联合；missingFids 解析 detail 包装；transitionIntakeAction/updateDraftProfileAction）、lib/api.ts（CURRENT_ACTOR_ID+四函数，getProductSpace 404→null）、[intakeId]/page.tsx（force-dynamic，Promise.all intake+allowed-events 再取空间，draft 闸门挂编辑岛，unconfigured 不出岛）、zh-CN 增 18 键（事件码不入值）、.env.example 增 LOOM_ACTOR_ID、products.module.css 追加 5 类只消费既有 **86 token 不新增**（successText 用 --color-state-success-text）。测试原子：check-products.mjs 扩展（18 键含 {fids} 占位/三新文件/env 禁 NEXT_PUBLIC 赋值/api 五符号+四路径+LOOM_ACTOR_ID/actions 六符号/两岛 "use client"·禁 @/lib/api·fetch·URL·必含 router.refresh/白名单精确四码且禁五组运营系统前缀/详情页九符号+draft 闸门/后端 router 四路由+statemachine 四客户事件防漂移；失败探针：岛注 @/lib/api、白名单注 ops_confirm 均实测 exit 1，Edit 还原全绿）。端到端验收（/tmp 一次性 SQLite loom-q106/app.db，不碰 Docker PG；uvicorn 8766+next dev 3211 带 LOOM_ACTOR_ID=c1+9222 Chrome；async 种子 t1+g2_fields 两条 active common（product_name/brand_name）+10 单覆盖十态/modeling 带产品空间）全过：①draft 改名保存→"资料已保存。"+RSC 刷新显新值 ②draft 缺 brand_name 提交→"资料不完整，缺失字段：brand_name"（首版漏 detail 包装显通用 422，curl 核实体后修解析再验过）③ai_recognizing 交集空显"暂无可执行操作"无编辑岛 ④pending_params/pending_quota/need_more_info 三按钮→submitted"已提交"（curl 复核）⑤stored/rejected 终态零按钮 ⑥category_creating"资料分析中"零按钮 ⑦modeling 产品空间 id/lifecycle=modeling 原样+快照 ⑧不存在 id→notFound；curl 补验终态非法事件 409、非 draft PATCH 409、空 roles 触发 b2_approved→403 requires role operations；网络仅同源 :3211（11 条）零直连 :8766、console 零 error/warn；重启去 LOOM_ACTOR_ID→两处 unconfigured、零按钮、只读正常；验收后进程与 /tmp 全清。E2E 教训：client 岛与 module.css 同目录须 `./products.module.css`（tsc 不查 CSS 模块存在性，只有 next 构建报错——新接岛必须真实跑页面）。feat 64fe6f4 / test 3333e39。**M12 未闭合**：客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2）、录入单运营侧操作页（ops/review/modeling 事件管理端化，触发 actor 待补）、产品模板/资料库、付费档额度/"第5档"【原文未给出，待补】/额度拦截/计费订阅/团队账号（V2）、五型 review SLA 时限数值（72h 待补）、G2 cat='common' 18 字段 fid 基线回填（客户补参表单随之）、CSV JSON 形态/异步导出、管理端日期筛选器与其余四治理驾驶舱（不进 V1）、
> **Q107 M12 管理端第五片·录入单运营队列与跨租户运营操作完成情况（2026-09-16；后端新增唯一只读端点/无新写口、无迁移，Alembic 头仍 0023，后端 418 测试/101 eval；不闭合 M12）**：✅ 四接缝经负责人"推进下一片，按你的推荐来"=全按推荐（甲）拍板记 **Q107**（02 C1.51）：**(1) 运营事件白名单恰 6 码 `OPS_INTAKE_EVENTS = [ops_confirm, to_cold_start, reject, b2_approved, b2_parent_fallback, b2_rejected]`**（docs/13 §1.1 中 requires_role=operations 完整集合；排除 wf01_*/auto_confirm 系统事件、Q106 客户四码、send_review/review_*/start_modeling/model_*/failed_* 触发方【原文未给出，待补】不外放）。**(2) 后端新增唯一读口 `GET /api/intakes/ops-queue`**——跨租户只读，actor_id+重复 roles query，require_ops_view 放行 operations|platform_admin（customer→403，缺 actor_id→422）；可选 status（不在 15 态→422 `unknown intake status: {status}`）/limit（1-100 默认 20）/offset，created_at DESC，OpsIntakeView 在租户内 IntakeView 上加 created_at；路由先于 /{intake_id} 注册；service 无租户过滤 select+count 子查询；**无新写口、无迁移**，详情岛写操作复用 `POST /api/intakes/{id}/transitions`，403/409/422 既有口径沿用。**(3) 可看与可执行分离**——默认 LOOM_ADMIN_ROLES=platform_admin 可看队列，ops 事件硬要求 operations；Server Action 本地守卫缺 operations→missing_role 判别联合**零请求**，岛显中文缺角色说明；执行须显式配 `platform_admin,operations`；V2 会话派生。**(4) 枚举/错误/状态词**——15 态状态词零新增复用 intake.status.*，6 事件码不入值只做 admin.opsIntakes.eventLabels.* 动词语译，FastAPI {"detail"} 英文 detail 经 JSON.parse 原样红文，403/404/409/422 中文 error 键，未知 id→notFound，产品空间 404→null 不渲染。前端结构：admin/intakes/ 新建五文件——ops-intake-codes.ts（六码+15 态 as const 单一事实源+类型守卫，注释列排除理由）、actions.ts（"use server" OpsIntakeActionResult 判别联合；unconfigured/非白名单 422/missing_role 本地闸/KNOWN_STATUSES+errorDetail，调 adminTransitionIntake）、ops-intake-actions.tsx（client 岛，allowedEvents∩白名单按白名单序出按钮，空交集显 noOpsActions，useTransition+router.refresh）、page.tsx（RSC force-dynamic，15 态 URL 筛选+白名单归一+PAGE_LIMIT=50 分页，仿 review-queue）、[intakeId]/page.tsx（RSC force-dynamic，Promise.all intake+allowed-events 404→notFound，空间 404→null，基本信息/原样资料/操作岛/产品空间只读区，css 两级 `../../admin.module.css`）；lib/api.ts 原 ADMIN_ROLES 改名 ADMIN_ROLE_LIST（8 处引用同步）+getOpsIntakeQueue/adminTransitionIntake；admin-sidebar 增第五项（恰 5）；zh-CN 增 opsIntakesNav+admin.opsIntakes.* 41 键（含 eventLabels 六键，状态词零新增）；.env.example 注明 operations 角色要求；admin.module.css 零新增（全复用既有类，token 仍 86）。测试原子：test_intake_api.py 9→12（RBAC 422/403/双角色 200 空体、跨租户 t1 三单+t2 一单 total=4/created_at 存在/draft=4/submitted=0/非法 422/limit=2 两页不重不漏、队列随 ops_confirm 迁移联动 pending_confirm 清空→submitted 命中），全套 backend 418 passed；check-admin.mjs 扩展（41 键/五新文件/侧栏恰 5 项/eventLabels 恰 6 键/六码数组 JSON.stringify 精确相等且禁词扫描剥 // 注释行——白名单依据注释合法/INTAKE_STATUS_FILTERS 恰 15/两页 force-dynamic·禁 NEXT_PUBLIC·URL·写词/actions "use server"+四状态码+unconfigured/missing_role/岛 "use client"+禁 @/lib/api（连 type import）+router.refresh/api 三符号+/api/intakes/ops-queue/ADMIN_ROLE_LIST/后端 router 五探针含注册序先于 /{intake_id}/service list_ops_intakes；失败探针：codes 注 auto_confirm、岛注 @/lib/api、后端角色改单 PLATFORM_ADMIN 均实测 exit 1，git checkout 还原全绿；其余 5 checker 不回归）。端到端验收（/tmp 一次性 SQLite loom-q107.db，不碰 Docker PG；uvicorn 8017 LOOM_SCHEDULER_ENABLED=false + next dev 3017 + 9222 Chrome；种子 t1/t2 两租户 8 单覆盖 draft/pending_confirm/pending_params/category_creating/rejected/stored+product_space）全过：①队列跨租户 5 行含 t2、15 态下拉全中文 ②URL ?status=pending_confirm 过滤 1 行且下拉保持，非法 status=bogus_state 前端归一显全部 ③详情岛六事件全部真实点击：ops_confirm 待确认→已提交、to_cold_start 待确认→类目创建中（RSC 刷新换 b2 三键）、b2_approved/b2_parent_fallback 类目创建中→待补充参数（仅留驳回）、b2_rejected→需补充资料（交集空显"当前状态暂无可执行的运营操作"）、reject 待补充参数→驳回终态零按钮 ④stored 显产品空间只读区 id/lifecycle=modeling 原样码+快照折叠，无空间单不渲染 ⑤未知 id→Next 404 ⑥重启 LOOM_ADMIN_ROLES=platform_admin 点驳回→missing_role 中文红文、状态不变、API 日志零 transitions POST ⑦去 LOOM_ADMIN_ACTOR_ID→队列 unconfigured 通知、岛 unconfigured 提示零请求；网络仅同源 :3017（后端只接受服务端访问），console 零 error/warn；验收后进程与 /tmp 全清。feat 6b11897 / test 475c2b7。**M12 未闭合**：客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2）、触发方待补事件（send_review/review_*/start_modeling/model_*/failed_*）的管理端化须等 docs/13 补裁决、五型 review SLA 72h【待补】、产品模板/资料库、付费档额度/"第5档"【原文未给出，待补】/额度拦截/计费订阅/团队账号（V2）、G2 cat='common' 18 字段 fid 基线回填（客户补参表单随之）、CSV JSON 形态/异步导出、管理端日期筛选器与其余四治理驾驶舱（不进 V1）、CCR 历史报告/法审 SLA 倒计时/规则库界面。
> **Q108 M12 管理端第六片·待办 SLA 看板完成情况（2026-09-16；无新端点/无新写口/无迁移，Alembic 头仍 0023，后端 421 测试/101 eval；不闭合 M12）**：✅ 四接缝经负责人"推进下一片，按你的推荐来"=全按推荐（甲）拍板记 **Q108**（02 C1.52）：**(1) 安全加固优先——M10b 裸端点补 platform_admin 红线**：`GET /api/admin/sla/todos` 自 M10 切片 b 落地起无任何鉴权（Q75 补闸矩阵只收 POST /sla/run），本切片加 require_sla_view query 依赖（actor_id 必填缺参 422+重复 roles，require_any_role(actor, PLATFORM_ADMIN)，operations/internal_compliance/customer 全 403）；POST /run 的 body 内闸与 leader_lock 409/503 一字不动。**(2) status 白名单恰 4 值** `{open, escalated, resolved, all}`（OpsTodo.status 三态+all），非法 422 `unknown todo status: {status}`，默认 open；继续不分页、due_at ASC 全量返回，返回字段集不变，sla_state 派生仍走 core/sla/policies.py（resolved 终态优先；escalated/过 due→red；黄仅 law_review created_at+24h=knob sla.yellow_hours；其余类型到期前 green，黄色口径【原文未给出，待补】）。**(3) 前端纯只读 RSC `/admin/sla-todos`**：force-dynamic、searchParams Promise、GET 表单四筛选（URL 非法值前端归一 open 不透传）、9 列（待办/租户/类型/负责角色/关联实体/状态/SLA 态/截止时间/升级时间），时间空值显 —、长 id 截前 8 且 title 留全值；无写控件——POST /sla/run 不上页面（checker 守禁含 /sla/run、无 "use server"、无 POST/PATCH/DELETE）；身份沿用 server-only LOOM_ADMIN_ACTOR_ID/LOOM_ADMIN_ROLES（默认 platform_admin 即可看），未配显 unconfigured 不发请求，403/422 中文 error 键；todo_type/status/assignee_role/entity_type/sla_state 全部英文码原样不译，只 UI 框架中文入消息表；sla_state 四色 chip 复用 riskChip：red→riskCritical、yellow→riskMedium、green→riskLow、resolved→stateArchived；yellowNote 如实陈述黄仅法审、其他类型到期前 green、超时/升级 red，不造口径。**(4) 无新写口无迁移**：只加鉴权与白名单，头仍 0023；V2 随真实认证改会话派生。前端文件：新建 `admin/sla-todos/page.tsx`（约 166 行，四值 as const+KNOWN_ERROR_STATUSES={403,422}+asString/timeText/shortId/slaClassName+StatusFilters（重置用 i18n Link 禁硬编码 locale 路径）+TodoTable+main 三 translator）；lib/api.ts 增 SlaTodoView/getSlaTodos；admin-sidebar 增第六项（恰 6）；zh-CN 增 slaTodosNav+admin.slaTodos.* 21 键（枚举码零入值）；admin.module.css 零新增（token 仍 86）；.env.example 无需改。测试原子：test_sla_api.py 新增 3 集成（RBAC 422/三角色 403/200 默认 open 空体；非法 status 422 detail 原文；5 条种子 pws_ready green/law_review yellow/review_pwc_combo overdue red/ops_assist_category escalated red/wordlist_rescan resolved——all 恰 5 行 due_at 升序且 states 字典精确、open 恰 3、escalated/resolved 各恰 1），全套 backend **421 passed**、ruff check 净（ruff format --check 对全仓 116 个历史文件同样报告，format 从非项目门槛，不批量重排）；check-admin.mjs 扩展（slaTodosNav+21 键/requiredFiles/侧栏第六项恰 6/Q108 守卫块：force-dynamic、禁 "use server"、剥 // 注释行后禁 NEXT_PUBLIC·http(s)·/zh-CN/·写词·/sla/run，必含 getSlaTodos/四状态值/yellowNote/shortId/四色类/sla_state，api 三探针，后端 router require_sla_view+PLATFORM_ADMIN+Depends 挂载+TODO_STATUS_FILTERS+422 原文）；两失败探针（Depends 改 require_sla_view_probe、页面加 /sla/run 常量）均实测 exit 1，Edit 还原全绿；六 checker+tsc 不回归。feat 15387a9 / test 1975946。端到端验收（/tmp 一次性 SQLite loom-q108.db，不碰 Docker PG；uvicorn 8018 LOOM_SCHEDULER_ENABLED=false + next dev 3018 + 9222 Chrome；6 条种子跨 t1/t2/t3 覆盖五 todo_type）全过：默认 open 4 行 due_at 升序四态正确、四色 chip computed background/color 各异且英文码原样；resolved 恰 1 行下拉回显"已解决"、chip stateArchived；all 恰 6 行下拉"全部状态"；?status=bogus 归一 open（下拉"待处理"不触发后端 422）；open 行临时刷 resolved 后空态"当前筛选下暂无待办。"；侧栏第六项存在；yellowNote 与定稿一致；重启 LOOM_ADMIN_ROLES=operations→"没有操作权限"无表格；去 LOOM_ADMIN_ACTOR_ID→unconfigured 中文通知；浏览器侧 XHR/fetch 零条（后端仅 RSC 服务端访问）、console 零 error/warn；验收后进程与 /tmp 全清。**M12 未闭合**：客户侧余五菜单（content/templates/analytics/social-accounts/settings；前三项段 8/12/13 V2）、触发方【待补】事件管理端化须等 docs/13、五型 review SLA 72h【待补】、其他待办类型黄色口径【原文未给出，待补】、产品模板/资料库、付费档额度/"第5档"【待补】/额度拦截/计费订阅/团队账号（V2）、G2 cat='common' 18 字段 fid 基线回填、CSV JSON/异步导出、管理端日期筛选器与其余驾驶舱（不进 V1）、CCR 历史/法审倒计时/规则库界面，下一点工前不自动开工。

> **Q109 M12 管理端第七片·管理面读端点鉴权审计 + 同型补闸完成情况（2026-09-16；无新端点/无新写口/无迁移，Alembic 头仍 0023，后端 444 测试/101 eval，前端零改动；不闭合 M12）**：✅ 四接缝经负责人 2026-09-16"好的，搞"=按推荐方案拍板记 **Q109**（02 C1.53）：**(1) 先审计后补闸**——对全部 `/api/admin` GET 穷举核对 docs/05 矩阵，分三类处置；**(2) 矩阵要求角色而代码无闸的 8 个裸 GET 同型补 query actor 依赖**（actor_id 必填缺参 422、重复 roles、require_any_role 不符 403，与 Q92/Q108 同构）：GET c1/signal-weights 与 c1/industries=operations（Q2/Q7）、GET /api/categories=dictionary_admin（Q68，注意无 /admin 前缀）、GET fp-source-routes 与 fit-weights=operations（Q8/Q34）、GET compliance-wordlist=operations/internal_compliance（Q48，platform_admin 不在内）、GET ai-models=platform_admin（Q82）、GET ai-scene-routes=operations|platform_admin（Q82）；写口服务层闸一字不动；**(3) 矩阵未定读角色的三类读口不加闸，挂【原文未给出，待补】**：config 三读口（列表/单项/history）、g2-candidates、skill-prompts 三读口，待负责人拍板读角色；**(4) 矩阵明确有意开放的两列 Key 列表不动**：agent-keys（05 明文"GET 无 actor 体（同 outbound Key 列表口径）"）与 ai-models/{id}/keys（仅 key_id/fingerprint/status/时间元数据，密文不回显），fcw.csv 的 Q100 tenant_id 隔离不在列；对 Q75 补闸矩阵方法枚举只收写口与表行 GET 同组角色的张力，解释为"红线收口当片写口优先"，8 个读口不在 05 显式"有意开放"清单，按 Q108 先例以后续加固片补闸，不回溯改 Q75 原文。前端全仓 grep 确认无这 8 端点消费方（RSC/浏览器均零调用），系统内部读词表/权重/路由走服务层不经 HTTP 不受影响。交付：5 个路由文件各加本地 query 依赖（modeling 两 dep、fieldpool/platform_adaptation/compliance_wordlist 各一、model_registry 两 dep），处理器签名末位 `_: Actor = Depends(...)`、函数体不动；新文件 test_admin_read_rbac_api.py 表驱动 23 测试（8 端点缺 actor 422·customer 403·矩阵角色逐角色 200；词表 platform_admin/product_reviewer 403 而 internal_compliance 200；categories operations 403；config/g2-candidates/skill-prompts/agent-keys 无 actor 200 锁定待补/有意开放；未知 model keys 200 且不 403/422），既有 14 处裸 GET 调用补 query（c7_llm_resolve 的 _category_id 改 dictionary_admin、model_gateway 三处、platform fit-weights 一处）；全套 **444 passed**（421→444）、ruff check 净、六 checker+tsc 全绿；eval 不受影响。feat 9269315 / test 885574b。**M12 未闭合**：config/g2-candidates/skill-prompts 读角色待拍板；客户侧余五菜单、触发方【待补】事件、五型 review SLA 72h【待补】、其他待办黄色口径【待补】、产品模板/资料库、付费档额度/"第5档"【待补】/计费订阅/团队账号（V2）、G2 common fid 基线、CSV JSON/异步导出、日期筛选器与其余驾驶舱（不进 V1）、CCR 历史/法审倒计时/规则库界面，下一点工前不自动开工。
> **Q110 M12 热修·审核工作量页空态缺键（2026-09-16；纯前端一文案键+checker 守卫，无迁移，Alembic 头仍 0023，后端 444 测试/101 eval 零改动，token 仍 86；不闭合 M12）**：✅ 负责人 2026-09-16"修掉那个缺键"点工记 **Q110**（02 C1.54）。Q109 浏览器 E2E 空库回归时发现：`/admin/review-workload` 四个空表分支（候选积压/待办积压/已裁候选/已决待办）调 `admin.workload` 命名空间 `t("empty")`，但 zh-CN.json workload 块 Q102 建页时漏收该键，逐片 E2E 因带种子从未走空态分支而未暴露；顶层 admin.empty 属另一命名空间 next-intl 不回退。修法：workload 块补 `"empty": "暂无数据。"`（两快照表+两窗口产出表共用，不照搬"当前窗口暂无数据。"）；全仓其余 10 处 t("empty") 逐一核键无同类缺漏；check-admin.mjs workload 必检键清单补 empty，删键失败探针实测 exit 1。六 checker+tsc 全绿，Q109 E2E 同环境复验四空态显中文。fix 0b10ed5 / test d4059a9。挂账同 Q109，下一点工前不自动开工。

> **Q111 M12 工程基建·CI workflow 落地（2026-09-16；纯工程，无迁移，Alembic 头仍 0023，后端 445 测试/101 eval，token 仍 86；不闭合 M12）**：✅ 负责人 2026-09-16"你推进搞"点工记 **Q111**（02 C1.55），销 Q77/17 CI/CD 节挂账"CI workflow 本体随 M12/部署落地（仓库托管平台确认后）"——origin 实为 github.com:bayernjf/loom，平台定 GitHub Actions。交付 `.github/workflows/ci.yml`：backend job（Python 3.12，`pip install -e "./backend[dev]"`）跑 ruff check → pytest（sqlite，无服务容器）→ `python eval/runner.py`（Q77 闸门 101 案）；frontend job（Node 20，npm ci）跑 typecheck + 六 checker；触发 push main/dev + pull_request；零 secret 零外部服务。明确不入 CI：ruff format --check（非门槛，116 历史文件会挂）、next build/lint（非既定闸门）、CD/部署（随部署阶段）。防漂移守卫单测静态解析 YAML 断言全部闸门在位（删 check-nav 失败探针 exit 1 实测后还原）；干净 env 复跑 445（444+1）/101/六 checker/tsc 全绿。feat 44659b1 / test 5f747bf。**workflow 已在 GitHub 实际触发并全绿**（2026-09-16 订正：PR #38/#39 dev→main，dev push、pull_request、main push 共 6 次 run 全部 success；两 job = Backend gates「pytest, ruff, eval」与 Frontend gates「tsc, six checkers」，最新 main 全绿 run 35118202175）。挂账同 Q109/Q110，下一点工前不自动开工。

> **Q112 契约文档回填·13 状态机迁移表 / 11 API 规范据已落地实现补登完成情况（2026-09-17；纯文档，无代码/迁移/测试变化，Alembic 头仍 0023，后端 445 测试/101 eval，token 仍 86；不闭合 M12）**：✅ 负责人 2026-09-17"一口气搞"点工记 **Q112**（02 C1.56），销 handoff 待办④"11/13 的接口与迁移行待 M0 裁决后补齐"挂账。**(1) 13 状态机迁移表**：§1.1 productIntake15 七处【待补】迁移行据 `app/product/product_intake/statemachine.py`/`service.py`/`router.py` 补事件码、角色闸与已实现副作用（每次迁移写审计 `intake.{event}`；submit 先过动态完整度闸门缺字段 422；start_modeling 建 ProductSpace 并整体复制 Q74 快照）；原文未给业务副作用不虚构，V1 仅状态翻转+审计的行保留【待补】；标注 Q106/Q107 外放口径（客户白名单 submit/params_completed/quota_confirmed/resubmit，运营白名单 6 码，wf01_*/auto_confirm 与 send_review/review_*/start_modeling/model_*/failed_* 后端可调、前端不外放，触发方【原文未给出，待补】）；§1.5 G1 经核实 V1 仅 dictionary_admin 建 draft 类目 + 叶子模板 draft/approved，类目审核/合并/废弃/归档无迁移端点随 V3，三行【待补】维持并加补登说明；§1.7 PWC 阻断→复核、待入库 M5 未实现，加实现指针、【待补】维持；§3 核对项勾选已补、拆明仍待补。**(2) 11 API 规范**：§1 总览表回填——白名单消费升级 🟢 已落地（实际路径 `POST /api/product-spaces/{product_space_id}/pwc/consume`，业务方参考路径 /api/whitelist/consume 不采用）、白名单查询（`GET .../pwcs` 租户内池查询已落，中台页面级清单【待补】）、使用记录上报（V1 无独立端点，consume 取用即写 usage_record 并回带 usage_record_id）、Key 管理（Q88 入站 agent-keys 三端点 + Q82 出站 ai-model-keys 已落）、CSV/JSON（Q100 `GET /api/exports/fcw.csv` 已落，JSON 形态/异步导出 V2【待补】）；effect-callback 契约 🟢 但端点本体随段13 P3/V2，Q88 鉴权前置已备；新增 §2.2 实现端点规格（请求体 platform/account/slot/goals、响应 pwc/usage_record_id/platform_state/pool_health/restock_hint、404/409）、§2.3 CSV 规格（final_id 单列同步流式、只导 published、未知租户仅表头空文件 200）、§2.4 Key 治理规格（入站 SHA-256 单向哈希/明文仅签发一次/append-only 吊销，出站 Fernet 可逆加密刻意分域）；§5 核对项更新。**(3) 纪律**：凡代码未实现者（G1 审核/合并/废弃/归档流、PWC 复活与待入库、effect-callback 本体、中台 API/SDK、DB 浏览/AI 调试台/配额、JSON/异步导出）一律保留【待补】并注明去向（V2/V3），不以表模型字段预留或契约定稿冒充已实现。纯文档变更（docs/11、docs/13、docs/02 C1.56、docs/README 文档地图、handoff），无代码/迁移/测试变化（Alembic 头仍 0023），补登后逐条回读与代码实现核对一致。挂账同 M12 各块，下一点工前不自动开工。

> **Q113 M12 第八片·config/g2-candidates/skill-prompts 三族管理面读口补闸收口完成情况（2026-09-17；纯后端加固，无新端点/无新写口/无迁移，Alembic 头仍 0023，后端 455 测试/101 eval，前端零改动 token 仍 86；不闭合 M12）**：✅ 负责人 2026-09-17"按你的来，狠狠的搞完"授权按推荐方案收口 Q109 挂【待补】的三族 7 个读口，记 **Q113**（02 C1.57）。角色全部溯自已落地矩阵表行/写口同组，不新增业务口径：①config 三 GET（列表/单项/history）= platform_admin（写口 PUT/rollback 同角色，配置含 SLA 时限/各域阈值平台级敏感，同 ai-models 先例）；②g2-candidates 列表 = dictionary_admin（docs/05 表行 GET 与 promote 同行同角色 Q13/Q68，与 Q109 /categories 同型）；③skill-prompts 三 GET（指针/版本历史/单版本）= platform_admin（发布写口同角色，单版本含 template+variables 全文，同 ai-models 平台级敏感口径）。实现同 Q109：3 路由各加本地 query actor 依赖（`require_config_view`/`require_g2_candidates_view`/`require_prompts_view`），处理器末位 `_: Actor = Depends(...)`、函数体零改动，缺 actor 422/越权 403/命中 200；写口 body actor 服务层闸一字不动；agent-keys 与 ai-models/{id}/keys 矩阵有意开放仍免闸、测试继续锁定。三族前端全仓 grep 零消费、前端零改动。测试：test_admin_read_rbac_api 三族从 OPEN_READS 移入 GATED_READS，新增子路径 422/403 与三组角色反例（config 的 operations/dictionary_admin 403、g2 的 operations/platform_admin 403、prompts 的 operations/product_reviewer 403），现有 config/fieldpool/model_gateway 裸 GET 补 query；全套 **455 passed（445+10）**、ruff 净、eval 101/101、tsc+六 checker 全绿（token 仍 86）。feat 0144672 / test 36fb564。**M12 未闭合**：客户侧 templates/settings 两页 V1 口径待拍板、五型 review SLA 72h 数值/黄色窗口/触发方【待补】事件待业务方、付费档"第 5 档"额度【待补】、Key 单租户绑定口径待定，G2 common fid 基线待基准 HTML、CSV JSON/异步导出随 V2，下一点工前不自动开工。

> **Q114 M12 收尾·客户 templates/settings 两页 + 五型 SLA 数值占位完成情况（2026-09-17；跨栈加法无迁移，Alembic 头仍 0023，后端 457 测试/101 eval，token 仍 86；不闭合 M12）**：✅ 负责人 2026-09-17「都按推荐」记 **Q114**（02 C1.58）：**(1) settings=V1 只读账户面板**——后端 tenants 新增 customer_router `GET /api/tenants/{tenant_id}`（无闸只读，`CustomerTenantView` 不含 detail/onboarding，未知租户 404）+ 前端 `settings/page.tsx` 由占位改 RSC 账户面板（force-dynamic：账户信息+语言展示，语言切换随 V3；平台授权/通知/帮助/团队/账单五子项 V2 徽标），`nav.ts` templates→v2。**(2) templates=仅占位 V2 徽标**（模板库随 V2 类目）。**(3) 五型 SLA=V1 统一 72h + 黄色窗口「到期前 sla.yellow_hours（种子 24）」配置化占位**：`core/sla/policies.py` 黄色判定由「created_at+hours」改为「due_at−sla.yellow_hours」，YELLOW_BY_TYPE 改 YELLOW_TYPES 并把五型 review_* 并入（法审 due=48h 数值不变、既有测试零漂移），配置种子 source_ref 改「Q70②/Q114」值仍 72，业务方给数后经配置中心热更。**(4) 新增 `scripts/check-settings.mjs`**（npm run check-settings，零依赖一致性）。**未含/挂账**：付费档第 5 档额度、五型 SLA/黄色窗口真实数值仍待业务方输入。

> **Q115 基准 HTML 永久丢失后的溯源口径收口 + G2 fid 工程定稿完成情况（2026-09-17；纯种子迁移 0024，Alembic 头 0023→0024，后端 457 测试/101 eval，token 仍 86）**：✅ 负责人 2026-09-17 拍板记 **Q115**（02 C1.59）：**(1) `line NNNN` 溯源降级为历史痕迹**（引用保留，不再作为可重新核对的基准；docs 自身即唯一事实源）；**(2) docs/12「7 定稿 + 35 推断候选」定为最终名单**，不再挂"待 HTML 核对"；**(3) 触发方事件永久标【原文未给出，待补】**，实现口径以代码角色闸为准（Q106 客户 4 码 / Q107 运营 6 码 / 系统事件）；**(4) G2 fid 改判工程定稿**（业务事实=field_name 中文 canonical）：迁移 **0024_g2_common_fields** 落 12 个 cat='common' 字段（ON CONFLICT DO NOTHING 幂等），余 6 个不落。无后端业务代码改动；docs/04 §2.2/§2.5、docs/12 §1.2 同步收口。**未含/挂账**：6 个未知 common 字段、付费档第 5 档额度、五型 SLA/黄窗真实数值待业务方输入。

> **Q116 V2 P4 段12 内容生成第一片·文章单语言最小闭环完成情况（2026-09-17；跨栈加法 + 迁移 0025/0026，Alembic 头 0024→0026，后端 457→472 测试/101 eval，token 仍 86；P4 不闭合）**：✅ 负责人 2026-09-17「按推荐」拍板记 **Q116**（02 C1.60）：**(1) 范围=文章单语言最小闭环**，视频（video-studio 四模块）、多语言（Q58）、AI 质量分真打分（Q57 待回流校准）缓做。**(2) AI 通道=模型网关第 7 场景 ARTICLE-GEN、不走 skill7 运营 Gate**——段12 的 Gate 是客户审阅（Q59）而非运营审核，成本经 SkillRun 手动落（Q67）+ writeAudit 留痕，产 `{"body"}` 直写 `content_products.body`。**(3) 迁移 0025_content_products**（建表；content_id/tenant/product_space/final_id 只读软关联/goal/platform/slot/country/kind/language/body/review_hits/status/reject_reason/regenerate_count/审计列）+ **0026_article_gen_seed**（ARTICLE-GEN 场景路由 + Prompt v0.1，纯种子）。**(4) 新模块 `app/content/`**，main 挂 `/api/content/*`：POST /generate（operations，draft→generating→review）、/{id}/approve|reject|revise（客户，reject 原因必填 Q59）、/{id}/regenerate（operations，count+1，Q56 上限 3）；六态机 draft→generating→review→ready_for_publish/rejected/revising，revising→generating。**(5) 复检 4 项只做词库扫描**（复用 Q48 词库 + ccr_rules 三层裁决），语义级检测/施工指令核对/国家规则核对 3 项占位【待补】。测试 +6 状态机单测 +9 集成。**未含/挂账**：客户前端内容页、视频生成、多语言、AI 质量分、复检余 3 项均缓做/后续切片，下一点工前不自动开工。
> **Q118 Q117 挂账四项收口完成情况（2026-09-18；纯加固 + 一迁移；Alembic 头 0026→0027，后端 472→483 测试/101 eval，token 仍 86；不闭合任何业务包）**：✅ 负责人「一口气开搞」四项均按推荐（甲）拍板记 **Q118**（02 C1.62），销 handoff 待办 6 与 AGENTS 4 项挂账：**(1)** Q109 穷举遗漏的 4 个管理面 GET 同型补 query actor 闸（content-goals=dictionary_admin、cp-law-domains=internal_compliance、publish-slots/slot-type-defaults=operations；缺 actor 422/越权 403/命中 200；写口 body actor 闸不动；前端零消费；`require_fit_weights_view` 重构为通用 `require_operations_view`）；**(2)** publish_slots.gate 回填 V1 仅 Q42 人工直编口径（默认 approved、无 pending_gate 路径/翻转端点，AI 拓展 Gate 流随段7 完整版/V2，集成测试锁定 gate=approved）；**(3)** 迁移 **0027_product_space_fks**：category_node_id→g1_categories、active_pws_id→pws_snapshots 补 DB 级 FK（nullable，先幂等置空孤儿；PG16 全链 up/downgrade-1/up + 孤儿拒绝/NULL 合法实测）；**(4)** ci.yml frontend 六→**七 checker**（check-settings 接入，去掉过时 444 步骤名），防漂移单测新增「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除漏接。**审计外修复**：env.py 迁移前幂等预置/加宽 alembic_version 至 VARCHAR(128)（0021 起 revision id 33 字符，全新 PG 全链原在 0021 截断）。测试 +11（参数化 +8、角色边界 +2、CI 集合 +1）；ruff 净、eval 101/101、tsc+七 checker 全绿。
> **Q119 V2 P4 段12 多语言（Q58）切片完成情况（2026-09-18；加法 + 迁移 0028；Alembic 头 0027→0028，后端 483→494 测试 / eval 101 不变，token 仍 86、前端无变化；P4 不闭合）**：✅ 负责人「一口气搞了」接缝按推荐（甲）拍板记 **Q119**（02 C1.63），四项：**(1)** 语言清单表 `content_languages`（dictionary_admin CRUD 仿 content_goals、读口 query actor 闸；code=BCP-47 PK / name / markets=适用国家码 JSON〔**空数组=全市场含 country 空**〕/ status active|archived；迁移种子仅 zh-CN、markets=[] 保证单语言向后兼容，其余语言业务方未给清单不预置，由 dictionary_admin 配置）；**(2)** `ProductSpace` 加 nullable `target_languages` JSON（产品侧目标语言，NULL/空=未声明不收窄；operations `PUT target-languages` 写入口已落，段1 录入表单控件挂账）；**(3)** 交集纯函数 `covers_market`/`eligible_codes`（catalog 中 active 语言 markets 覆盖 `fcw.country`，再被 PS.target_languages 非空收窄；受限语言不覆盖 country=NULL）；**(4)** `content_products` 唯一约束 `(final_id,language,kind)`，generate 语言不交集→422（回带 eligible）、同 final+lang+kind 已存在→409，每语言一条成品天然独立复检/独立客户审（Q59 按 content_id 流程不分叉）。端点 5：GET/PUT/POST `{code}/archive` `/api/admin/content-languages`（dictionary_admin）、GET `/api/content/eligible-languages`（operations query actor）、PUT `/api/product-spaces/{id}/target-languages`（operations）。ARTICLE-GEN 模板加 `$language` 与硬性规则 4，synthetic 非 zh-CN 产出英文确定性占位，SkillRun.input_payload 记 language。测试 **+11**（交集纯函数单测 5、多语言集成 6；原 test_content_api fixture 自插 zh-CN 种子）；ruff 净、eval 101/101（golden 21/21，纯函数由 pytest 覆盖不入 eval）；迁移 0028 经 pgvector/pg16 一次性容器 up / downgrade-1（表列约束全清、回 0027）/ 再 up（恢复 zh-CN 种子与约束）实测对称。**挂账**：zh-CN 外语言清单（运营配置）、段1 目标语言录入控件、每语言成品的前端并列呈现（随 Q122 内容页）、视频生成。

> **Q120 V2 P4 段12 AI 质量分真打分（Q57）+ 重生成上限配置化（Q56）切片完成情况（2026-09-18；加法 + 迁移 0029/0030；Alembic 头 0028→0030，后端 494→511 测试 / eval 101 不变，前端无变化；P4 不闭合）**：✅ 负责人「一口气搞了」接缝按推荐（甲）拍板记 **Q120**（02 C1.64），五项：**(1)** 模型网关新增**第 8 场景 ARTICLE-QC**（只读打分不改写，variables `[body,language]`，输出 `{score,issues}`；0030 纯种子：路由→synthetic + Prompt/PromptVersion v0.1）；**(2)** 在正文生成、词库复检之后、complete 之前由新建 `app/content/quality.py` **内嵌**调用，落 `content_products.quality_score`(Float)/`quality_issues`(JSON)（0029 加两 nullable 列），**零新 HTTP 端点**，`ContentProductView` +4 字段 quality_score/issues/threshold/advisory；**(3) 定位纯 advisory**——不自动发证/驳回、不新增 needs_manual 状态、不阻断 approve（Gate 仍客户审阅 Q59，六态机不动），低于 `content.ai_quality_threshold`（默认 0.85）仅置 advisory=true 作界面提示；**(4) QC 故障/坏输出不阻断**（ModelConfigError/ModelUnavailable/BudgetExhausted/GenerationUpstreamError，或非 JSON/非对象/score 缺失越界/bool → score 留空、issues 记 `qc_error`、照常进 review；失败写审计 quality_unavailable 不落 SkillRun，成功落 SkillRun source=llm_auto、wf=WF-10 + 审计 quality_scored）；**(5) Q56 上限配置化**——`revise_allowed(status,count,limit)` 增 limit 形参，service 读 `content.regen_limit`（默认 3、运营可改），超限 revise 返回 422（detail 回带 {count}/{limit}）只能 reject；Q56 的 a 清洗区编辑 / b 作废 final_id 回池保留为人工动作，不自动作废/回池。synthetic `build_article_qc`：哨兵 `[LOW_QC]`→0.62、空 body→0.55、否则 0.92。测试 **+17**（QC 解析/阈值/上限纯函数单测 13〔tests/unit/test_content_quality.py：含边界 0/1、bool、七类坏输出〕、质检集成 4〔tests/integration/test_content_quality_api.py：真打分落 SkillRun、低分不阻断 approve、QC 故障容错、regen_limit=1 闸〕；两内容 fixture 补 QC 三件套，happy path 一次生成 SkillRun 断言由 1 改 2）；ruff 净、eval 101/101（golden 21/21，parse/advisory 为纯函数由 pytest 覆盖、QC synthetic 为测试夹具不入 eval）；迁移 0029/0030 经 pgvector/pg16 一次性容器全新库 up / 逐档 downgrade（0030 种子全清、0029 两列全删）/ 再 up 实测往返对称，物理表仍 55。**挂账**：界面「AI 评估分，仅供参考」标注与每语言成品并列（随 Q122 内容页）、段13 回流后相关性校准（Q57 后置）、Q56 a/b 人工路径前端化、视频质量分（video-studio）、复检语义级（下一片 Q121）。

> **Q121 V2 P4 段12 复检第②项·语义级检测切片完成情况（2026-09-19；加法 + 迁移 0031 纯种子；Alembic 头 0030→0031，后端 511→523 测试 / eval 101 不变，前端无变化；P4 不闭合）**：✅ 负责人点工 B 组后接缝按推荐（甲）拍板记 **Q121**（02 C1.65），四项：**(1)** 模型网关新增**第 9 场景 ARTICLE-SEMANTIC-CHECK**（与 ARTICLE-QC 同构只读检测不改写，variables `[body,language]`，输出 `{findings:[{code,message,excerpt}]}`；0031 纯种子：路由→synthetic + Prompt/PromptVersion v0.1）；**(2)** 在 ARTICLE-GEN 写 body、词库扫描之后、ARTICLE-QC 之前由新建 `app/content/semantic.py` **内嵌**调用一次（generate/regenerate 同路径），结果聚合进既有 `review_hits["semantic"]={checked,findings,?error}`，**零新 HTTP 端点 / 零新列 / 零新表**；**(3) 纯 advisory（甲）**——不抬 block_required、不阻断 approve、不新增状态（词库 ban 硬阻断与客户审阅 Gate Q59 不变，与 Q57 降级、Q66 AI 不持否决权一致），四检测 code（unsubstantiated_claim/absolute_guarantee/off_material_exaggeration/misleading_ambiguity）为 v0.1 工程口径待业务方/法审校准；**(4) 不可用/坏输出不阻断**（三类网关异常→checked:false+error 段+审计 content.semantic_unavailable、不落 SkillRun；坏 JSON/非对象/findings 非数组归一为空+错误码但仍落 SkillRun（output 带 parse_error）+审计 content.semantic_checked；findings 逐项容错）。synthetic `build_article_semantic_check`：哨兵 `[SEMANTIC_RISK]`→1 条 unsubstantiated_claim、其余空。测试 **+12**（解析纯函数单测 8〔tests/unit/test_content_semantic.py〕、语义复检集成 4〔tests/integration/test_content_semantic_api.py：三场景 SkillRun、哨兵命中不阻断且可 approve、网关异常 checked=false 无 SkillRun 仍可 approve、regenerate 重跑 2 条〕；三个内容 fixture 补 SEMANTIC 三件套，happy path SkillRun 集合断言 2→3）；ruff 净、eval 101/101（golden 21/21）；迁移 0031 经 pgvector/pg16 一次性容器 up（三件套就位）/ downgrade -1（语义三件套全清、QC 三件套保留）/ 再 up 实测往返对称，物理表仍 55。**挂账**：复检余两项（施工指令核对、国家规则核对）【原文未给出，待补】、四 code 口径业务方/法审校准与段13 命中率评估、语义发现界面提示（随 Q122）、视频语义复检（video-studio）。
> **Q122 客户「内容生产与发布」功能页 + Q56-a 人工编辑切片完成情况（2026-09-19；跨栈加法，零迁移；Alembic 头仍 0031，后端 523→528 测试 / eval 101 不变，前端第八 checker；P4 不闭合）**：✅ 三接缝在负责人未在线时沿用「按你的来/狠狠搞完」授权**按推荐（甲）落地、待追认**（02 C1.66）：**(1) 客户页能力=只读+审阅+人工改稿**——列表/详情/approve/reject/revise/Q56-a 人工编辑进客户页，generate/regenerate 维持 operations 闸不放客户页（前端访问层不得出现两端点，checker 守卫）；**(2) Q56-a=客户自助人工编辑**——仅 revising 态 PATCH /api/content/{id}/body（无闸，非 revising 409/空白 422/404），提交后重跑词库+语义复检+ARTICLE-QC，经新事件 **manual_resubmit（revising→review，六态不变）** 回 review，不调 ARTICLE-GEN、不增 regenerate_count（审计 content.body_edited）；**(3) 目标语言客户录入另开客户侧端点（见 Q123）**。后端另加无闸 GET /api/content?tenant_id=（列表项 ContentProductListItem 去 body，未知租户空）与 GET /api/content/{id}（详情）；前端新建 (shell)/content/ 列表+详情+DecisionIsland（block_required 禁通过）+BodyEditIsland+actions+css，messages content 命名空间（六态中文），nav content v2→v1，新增第八 checker check-content 并接 CI（七→八）。测试 **+5**（tests/integration/test_content_customer_api.py，SkillRun 集合 GEN+QC×2+SEMANTIC×2、count 恒 0）；ruff 净、eval 101/101、tsc+八 checker、next build 成功。**挂账**：Q56-b 作废 final_id 回池记难产原因（六态机无 discarded 态、段11 FCW 无回池原文口径）、ready 后统一发布动作不在 V1 客户页、语义四 code/QC 阈值校准。
> **Q123（B3）段1 客户目标语言录入控件切片完成情况（2026-09-19；跨栈加法，零迁移；Alembic 头仍 0031/物理表 55，后端 528→534 测试 / eval 101 不变，前端零新 token；P4 不闭合）**：✅ 接缝按推荐（甲）拍板记 **Q123**（02 C1.67）：**(1)** 新开客户侧写口 PATCH /api/intakes/{intake_id}/target-languages（按 intake 定位产品空间，客户无闸；码去重且必须 active，未知/归档/重复 422，空间未生成 404，空数组清 NULL，审计 product_space.target_languages.set 带 channel=customer_intake）；**(2)** 新开无闸只读 GET /api/content/languages（仅 active、按 code 排序，注册必须先于 GET /{content_id}，check-content 顺序断言）；**(3)** Q119 operations PUT 代设入口与 OPERATIONS 闸原样保留（回归锁 roles=[]→403），ProductSpaceView 回显 target_languages。前端录入详情产品空间段挂 TargetLanguagesForm（checkbox 多选、空选=不限制、Server Action+router.refresh），check-products/check-content 扩展守卫。测试 **+6**（tests/integration/test_target_languages_customer_api.py）；ruff 净、eval 101/101、tsc+八 checker、next build 成功。**挂账**：zh-CN 外语言清单待业务方提供后由 dictionary_admin 配置（不预置）。
>（2026-09-14，后端切片；段11 闭合，V1 主链 段1→6→10→11 全通）**：✅ 验收标准全部达成——①PT-FCW-ASM 七 Guard 全绿才 mint final_id：g1 PWS frozen / g2 block_required=false 且 cleaning_passed=true（无 CCR 报告不放行【实现补】，防"从未清洗"伪装通过）/ g3 PCP 与 CSP/CSTP/CEP 均 active 且 gate=approved / g4 六路 product_space_id 一致（Q52 配置实例归属）/ g5 tenant 一致 / g6 法审仅在被触发时要求 approved（Q49/Q53）/ g7 PWS is_active 当前版（Q53）；七项不短路全量返回，任一败 guards_passed=false 不生成 final_id，409 回带逐项结果。②E1.1 publishFCW 唯一出口（line 11036）：final_id 仅 `POST /api/fcw/assemble` 与 assembly-tasks 内部同一服务函数写入，全系统无其他写口；每次尝试（含阻断）append-only 审计 fcw.issued/fcw.assembly_blocked，issued detail 含发起人/时间/六路材料/Guard 结果/E1_owner=publishFCW。③Q54 分仅排序：score=pwc 骨架分×100×0.4 + slot fit×0.3 + 三包 conf 均值×100×0.3（0–1 量纲统一 ×100，fit 本为 0–100），任一来源缺失 score=null+incomplete=true 不凑分、永不做门槛；公式为临时口径待段13 校准（Q54）。④Q55 双入口：任务驱动（product×platform×goal×count，不给 slot_ids 自动取平台 active 发布位前 N，给则长度=count 且不重复否则 422）同步逐条组装，单项失败（Guard/材料/去重）隔离不阻断批次，results 逐项 issued/failures 留痕；手动单条同走七 Guard；角色 operations。⑤同 (pws_id,pwc_id,platform,slot_id) 去重（DB uq_fcw_same_issue；服务层补 country 同键判定），重复 409；重冻后显式指定旧版 PWS 被 g1/g7 拦截（集成测试覆盖）。⑥Q51 回填：词表生效即扫扩展命中快照关联的 draft FCW（实体接入、幂等待办+审计；V1 直接 published、draft 通道未开，通常空集，结构就绪）。交付：迁移 0009_m8_stage11（final_content_whitelists/fcw_assembly_tasks 2 表，PG16 up/downgrade-1/up 实测，JSONB/server_default/唯一约束核对）、165 条测试全绿（12 纯逻辑单测：七 Guard 逐项可败+不短路+Q54 量纲与缺失；6 HTTP 集成：全绿发证审计、403/404/422 错误口径、材料缺失不产 final_id、重复+旧版拦截、Q55 批量/失败隔离/任务审计、医疗行业法审 g6 联动）。**未含（事实纪律挂账）**：WF-09 AI Skills（V1 机械组装，AI 随 V2/M10 skill7）；draft 生命周期创建/发布端点（publish_status 列保留，V1 全自动直接 published）；段12 content_products 与客户确认流（随段12/V2）；FCW 消费侧 API/中台对接（Q71 现有消费在 M5 PWC 侧，FCW 消费契约【原文未给出，待补】）；任务异步 worker（V1 同步跑完即 completed，异步调度随 M10）；Q54 段13 反馈校准。

> **M1 完成情况（2026-09-13，后端切片）**：✅ 三项验收标准达成——productIntake15 状态机 15 态全流转（含类目创建中态，Q5；类目确认 operations 角色，Q3）、提交闸门按 `g2_fields` 中 active 的 cat='common' 行动态校验缺字段阻断（D7.1 闸门1，名单不硬编码）、Q74 双落库（申请单 `profile` 提交后不可变；start_modeling 生成 ProductSpace 时整体复制 `profile_snapshot`）。交付物：迁移 0001（g2_fields / product_intake_applications / product_spaces / audit_logs，PostgreSQL 16 实测 upgrade/downgrade 通过）、`/api/intakes` REST 端点、writeAudit append-only 审计、11 条单测/集成测试全绿。**未含**（挂后续包）：WF-01 6 Skill AI 候选通道（随 M10 skill7 通道接入）、72h SLA 升级待办（M10 SLA 引擎）、18 通用字段 fid 基线（**Q115 已收口**：基准 HTML 永久丢失，改判工程定稿——仅 12 个可从 04 枚举的字段落种子（迁移 0024），余 6 个【原文未给出，待补】）；M0 挂账的 PS.lifecycle 迁移触发条件、g2 候选租户可见性、候选状态枚举仍挂账。

### 2.3 V2 · 3–6 月 / 30–50 客户（段 7/8/9 完整 + 段 12/13 + D9 厚度）

| 包 | 范围 | 原文依据 |
|---|---|---|
| P1 | 段7/8 完整：动态信号采集 + fit_score 学习 + PLATFORM-ADAPTER（pending Skill 首次落地）+ PCP 每周触发重算/AI 候选对照单 | Q34-Q43、PT-PLATFORM-ADAPTER-V1.0、PT-PCP-V1.5、17 字段权重池 |
| P2 | 段9 D0 三包：CSP/CSTP/CEP + 按（产品×平台×目的）三元组复用 | Q44-Q47、layerSpaces、WF-07 |
| P3 | 段13 回流：发布记录 + 平台公开互动数（POST /api/effect-callback）+ 客户手工回填转化 + 人工 KUP（证据三关/回滚点）；KUP-PROPOSE 随本包首次落地 | Q60-Q65、05 §1.1.1、WF-11 |
| P4 | **段12 内容生成（2026-09-13 裁决由 V1 移入）**：WF-10 只读消费 FCW + 复检 4 项 + 客户审阅 + 重生成 ≤3 次 + 多语言=市场∩语言；**🟡 多片落地中**：Q116（2026-09-17）文章单语言最小闭环（content_products 迁移 0025 + ARTICLE-GEN 场景 0026 + 词库复检 + 客户审阅端点）；**Q119（2026-09-18）多语言 Q58 落地**（content_languages 语言清单迁移 0028 + 市场∩产品目标语言交集 + 每语言独立成品唯一约束 + 5 端点）；**Q120（2026-09-18）AI 质量分 Q57 真打分落地**（ARTICLE-QC 第 8 场景内嵌生成、纯 advisory 不阻断、阈值 content.ai_quality_threshold 与重生成上限 content.regen_limit 配置化，迁移 0029/0030）；**Q121（2026-09-19）复检第②项语义级检测落地**（ARTICLE-SEMANTIC-CHECK 第 9 场景内嵌、纯 advisory 不阻断、落 review_hits.semantic，迁移 0031 纯种子）；**Q122（2026-09-19）客户内容页 + Q56-a 人工编辑落地**（无闸列表/详情/PATCH body、manual_resubmit 边、前端内容页与第八 checker，零迁移）；**Q123（2026-09-19）段1 客户目标语言控件落地**（客户 PATCH + active 语言清单 GET，零迁移）；视频/复检余两项（施工指令核对、国家规则核对）缓做 | Q56-Q59、PT-ART-GEN-V1.5 |
| P5 | D9 V2 厚度：知识库（公共/私有/产品）、Memory 6 层、Multi-model Router、客户自助门户、反馈归因、跨表亲和度自学习、版本快照管理、API 对接中台、类目管理 | D9 V2、09 §3.13、Q67 |

### 2.4 V3 · 6–12 月 / 100–200 客户（平台化）

10 辅助系统剩余 8 个（Knowledge Graph / Evidence Center / Simulation Sandbox / Rollback Center / Drift Detection / Human Feedback Loop / Context Pack Builder 等；Evaluation+Golden 已在 V1）、完整 BI 仪表盘、Webhook 推送体系、SDK 嵌入式集成、移动端、国际化、白牌定制。范围另见 §1.4 D9.5 与 09 §3。

---

> **信息保全**：§1.1/1.2 为原文 A7/D9/D9.5 全量（历史口径，按治理规则保留不改）；§1.3 合并裁决为 2026-09-13 负责人决策（A7 骨 + D9 厚度、段 12 后置 V2、2 驾驶舱、质量两件套覆盖 D9 V3）；§2 任务包为基于原文的派生拆解（验收标准全部引用原文 Guard/约束/Q 编号），未新增业务规则。时间盒/客户目标来自 D9 原文；具体排期、人员、工时原文未给，留待排期后填入。
