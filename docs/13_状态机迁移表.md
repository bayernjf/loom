# 13. 状态机迁移表

> **状态**：🟢 已补内容（11 个状态机迁移矩阵）
> **来源**：05_契约层_API与状态机.md（状态集合与已知迁移）｜01_PRD §3（各段状态/Guard）｜02_决策记录（Q 编号约束）
> **用途**：每个状态机的完整 当前态 × 事件 → 目标态 + 条件 + 副作用 迁移说明；AI 据此实现状态流转代码（含非法迁移拦截）。
> **⚠ 说明**：原文只给出状态清单与部分流转规则（05 已标注）；**未给出的事件/迁移/副作用标【待补：原文未给出】**，属开发第 0 步①（M0 仲裁，见 08 §2.1）补齐范围。迁移表格式：`当前态 | 事件 | 条件/Guard | 目标态 | 副作用`。
> **通用规则**：所有迁移写审计（writeAudit）；状态与 RBAC 权限联动；非法迁移默认拒绝并记录【建议】。

---

## 1. 迁移矩阵

### 1.1 productIntake15（段1 · 录入申请单，line 1454 + Q3/Q4/Q5）

| 当前态 | 事件 | 条件/Guard | 目标态 | 副作用 |
|---|---|---|---|---|
| 草稿 | 提交 | 输入完整（G2 18 通用字段，D7.1 闸门1） | AI识别中 | 触发 WF-01 6 Skill 串行 |
| AI识别中 | 6 Skill 完成 | 无 blocked | 待确认 / 待补充参数 / 待确认额度 / 类目创建中 | 按 C1 三分支分发（见 1.2） |
| 待确认 | 运营确认类目（Q3） | 运营执行（非客户）；72h 未处理升级 | 已提交 / 类目创建中 | 升级动作（Q4） |
| 待补充参数 | 补全参数（`params_completed`） | 客户动作；该态可 PATCH profile | 已提交 | 补全后重提，写审计；另有运营 `reject`→驳回（05 §2.1 同载） |
| 待确认额度 | 额度确认（`quota_confirmed`） | 客户动作 | 已提交 | 写审计 |
| 已提交 | 送审（`send_review`） | 触发方【原文未给出，待补】，V1 前端不外放 | 审核中 | V1 仅状态翻转+审计，业务副作用【待补】 |
| 审核中 | 审核动作（`review_need_info` / `review_approve` / `review_reject`） | 触发方【原文未给出，待补】，V1 前端不外放 | 需补充资料 / 已通过 / 驳回 | V1 仅状态翻转+审计，业务副作用【待补】 |
| 需补充资料 | 补资料重提（`resubmit`） | 客户动作；该态可 PATCH profile | 已提交 | 写审计 |
| 已通过 | 发起建模（`start_modeling`） | 触发方【原文未给出，待补】，V1 前端不外放 | 建模中 | 创建 ProductSpace 并整体复制 profile_snapshot（Q74），写审计 |
| 建模中 | 建模结果（`model_stored` / `model_failed`，系统事件） | 系统回填，前端不外放 | 已入库 / 入库失败 | 写审计 |
| 入库失败 | 重试/退回（`failed_to_info` / `failed_reject`） | 触发方【原文未给出，待补】，V1 前端不外放 | 需补充资料 / 驳回 | V1 仅状态翻转+审计，业务副作用【待补】 |
| 已入库 | — | 终态 | — | — |
| 驳回 | — | 终态 | — | — |
| 已归档 | — | 终态 | — | — |
| 类目创建中（Q5 第 15 态） | B2 类目候选结果 | B2 通过 / 驳回 | 待补充参数 / （运营二选一：挂最近父类目继续 或 打回需补充资料） | 客户端显示中性话术"资料分析中" |

> 已知非法迁移（【建议】拦截）：终态（已入库/驳回/已归档）不接受任何事件；类目创建中只接受 B2 结果。
>
> **M2 实现补登（2026-09-13）**：高置信 direct_approve 的"正常走审核"（§1.2）落为系统两步——AI识别中 `wf01_confirm`→待确认→`auto_confirm`（系统事件，无角色要求）→已提交；仅中置信 ops_assist 在待确认态出运营待办，由 `ops_confirm`（operations）推进。`auto_confirm` 是 Q1"direct_approve 正常走审核"的机械实现，非新业务裁决。
>
> **M1 实现补登（2026-09-13 落地，2026-09-17 据实现补登迁移行）**：纯逻辑状态机 `backend/app/product/product_intake/statemachine.py`（15 态；终态 stored/rejected/archived 不接受任何事件；非法迁移 409、角色不满足 403），事件码与目标态逐条对应上表；`service.apply_event` 每次迁移写审计 `intake.{event}`（detail 带 from/to），`submit` 先过完整度闸门（按 g2_fields 中 active 的 common 行动态判定，缺字段 422，Q74 名单不硬编码），`start_modeling` 创建 ProductSpace 并整体复制 profile_snapshot（Q74），`wf01_cold_start` 可挂 category_pending_id。原文【待补】各行的事件/目标态/角色已全部由实现确定（见上表）；**原文未给的业务副作用，V1 仅状态翻转+审计，不虚构业务动作，仍标【待补】**。外放口径（Q106 客户 / Q107 运营）：客户白名单 `submit`/`params_completed`/`quota_confirmed`/`resubmit`；运营白名单 `ops_confirm`/`to_cold_start`/`reject`/`b2_approved`/`b2_parent_fallback`/`b2_rejected`（均 operations 角色）；`wf01_*`/`auto_confirm`（WF/系统）与 `send_review`/`review_need_info`/`review_approve`/`review_reject`/`start_modeling`/`model_stored`/`model_failed`/`failed_to_info`/`failed_reject`（触发方【原文未给出，待补】）后端端点可调、测试覆盖，前端不出按钮。

### 1.2 冷启动三分支（段2 · Q1/Q3，流程分支非状态机）

| 条件 | 分支 | 后续 |
|---|---|---|
| conf ≥ 行业阈值 | direct_approve | 正常走审核 |
| [0.6, 阈值) 或 Top1-Top2 差 <0.1 | ops_assist | 运营待办选定，客户无感知；72h 未处理升级；全否转 cold_start |
| conf < 0.6 | cold_start | 推 B2 生成候选类目 → 申请单进"类目创建中"（Q5） |

### 1.3 atom8（段4 · 原子实例，line 1454 + Q17-Q20）

| 当前态 | 事件 | 条件/Guard | 目标态 | 副作用 |
|---|---|---|---|---|
| 草稿 | 拓展产出 | 仅产 candidate（PT-ATOM-EXP） | 待审核 | 写候选不写实例 |
| 待审核 | 审核通过 | approveAtomGuard 10 项全过；high/critical 必走 HumanGate（critical 单条审，Q70） | 已通过 | — |
| 待审核 | 审核驳回 | — | 已驳回 | — |
| 已通过 | 冻结 | 运营权限（Q20） | 已冻结 | 单条暂停（≠PWS 冻结） |
| 已通过 | 废弃 | — | 已废弃 | 恢复=重走审核（Q20） |
| 已通过 | 驳回 | — | 已驳回 | — |
| 已冻结 | 解冻 | 运营权限；**解冻不重审**（Q20） | 已通过 | — |
| 已驳回 | 复活重提 | pending_evidence 超时自动驳回可复活（Q18） | 已通过 | 重新审核 |
| 合规暂停 | 合规角色触发/恢复 | 恢复须合规角色复核（Q20） | （双向） | 合规审计 |
| 已废弃 | — | 终态 | — | — |
| 归档 | — | 终态 | — | — |

> evidence 关联（Q18）：evidence 空 → pending_evidence（AI 自动补一轮 → 运营待办 → 7 天超时自动驳回可复活）。

> **M4 实现补登（2026-09-13）**：atom8 拆为候选侧/正式侧两段实现——草稿、待审核不落正式表：拓展批次只产 `atom_candidates`（pending_evidence/pending_review/approved/rejected/merged；merged 为 Q19 同义簇被并方，非 atom8 原文态，文本不删、作 keeper 的 alias）；Gate 通过 approveAtomGuard（9 项可机检 + ⑩审计由服务保证，违规码 409 数组返回；high/critical 批量审违反 Q70 单条审）才派生 `product_atom_instances`，正式侧实现六态：approved→（operations）frozen→（operations，免重审）approved；approved/frozen→（internal_compliance）compliance_suspended→（internal_compliance 复核）approved；approved/frozen→（operations）deprecated，废弃复活=新原子重走候选审核（无原地复活端点）；approved/frozen→（product_reviewer）rejected；deprecated/rejected→（operations）archived。Q18 证据链：无证据候选 pending_evidence + 7 天 due，补证据 → pending_review（解除 evidence_required；disabled_expression 不解），系统 sweep 超时 → rejected(reason=evidence_timeout) → revive 重走审核（事实原子复活重检全局唯一，line 11189）；sweep 定时调度随 M10。Q17：词表强制级不可人工改（risk-override 409），AI 判级可人工复核并重建冲突集。

### 1.4 skill7（横切 · Skill 输出，line 11314）

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| ai_suggested | 提交 | Critical Skill 输出必须走候选通道 | pending_review | — |
| pending_review | 人工确认 | 人工 Gate | confirmed / modified / rejected | — |
| confirmed | 应用 | — | applied / archived | 应用到正式对象 |
| modified | 应用 | 人工修改后 | applied / archived | 应用修改版 |
| rejected | 归档 | — | archived | — |
| applied | — | 应用态 | — | — |
| archived | — | 终态 | — | — |

### 1.5 G1 类目状态机（段2）

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| draft | 提交审核 | — | review | 【待补】 |
| review | 通过 / 驳回 | — | active / draft | 【待补】 |
| active | 合并 | merged_into 必填 | merged | **永久重定向**，防历史 PS 悬空（A6/Q69） |
| active | 废弃 | — | deprecated | 被历史 template 引用悬空处理（A6） |
| 任意 | 归档 | — | archived | 【待补】 |

> **M2 实现补登（2026-09-13 落地，2026-09-17 核实）**：V1 仅落最小切片——dictionary_admin 经 `POST /api/modeling/categories` 建类目（默认 draft；六态字段 active/draft/review/deprecated/archived/merged 与 merged_into 已在 g1_categories 表模型预留），`PUT /api/modeling/categories/{id}/template` 维护叶子类目模板（模板自身仅 draft/approved 两态，field_list 校验合法 active G2 fid，Q68）。**类目 draft→review→active 审核流、active→merged（Q69 永久重定向）/deprecated、任意→archived 均无迁移端点**，随 G1 完整类目管理 V3；上表三行【待补】维持，不据表模型字段虚构迁移。

### 1.6 PWS 版本状态（段6 · Q28-Q33）

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| 待冻结 | 就绪检查 | pwsReadiness 5 项全绿（line 2634） | （生成待办推 BO-07） | 就绪后 7 天未冻升级（Q28） |
| 待冻结 | BO-07 执行冻结 | 红线操作：BO-07 + 人工 Gate + 审计（line 7674） | frozen | 下游段 7-11 唯一合法输入 |
| frozen | 重冻（Q29 三档） | 强制（合规/事实变更）/ 建议（普通增量）/ 免（无关变更） | 新版本 frozen | 旧版→superseded |
| frozen | 换版（Q30 四行） | 大版本递增，全保留，同刻仅 1 active | superseded + 新版 | 草稿作废重组装 / 未发布待复查 / 已发布不追溯仅标记+合规提醒 |
| frozen | 急停 | 外部新规追溯命中（Q32） | revoked | **立即断消费**；作废后重冻新版 |
| revoked | 重冻 | 修复后 | 新版本 frozen | 审计 |

> **实现补登（2026-09-14，M6 后端切片）**：待冻结非持久态——evaluate 只求值 5 门并在全绿时写 pws_ready OpsTodo（7 天 due、幂等、sweep 升级）；快照首落即为 frozen。is_active 与 status 双维：frozen+is_active=true=当前版本（active 不另设状态值），重冻时旧版翻 superseded/is_active=false 并双写 supersede/refreeze 日志；revoke 仅对 active frozen 开放（superseded 再 revoke → 409），revoked 不可回退，"重冻"= 5 门重过后建新版（vN.0 主版本递增）。重冻须带 reason_code：forced（atom_compliance_suspend/atom_deprecated/wordlist_hit/product_fact_change）/suggested（asset_increment）/none（unrelated_change → 409 不换版）。Q30 副作用中草稿/未发布/已发布对象尚不存在， dispositions 返回占位，随 M8/段12 回填。

### 1.7 PWC 状态流转（段5 · line 1780-1806 + Q24/Q26/Q27/Q61）

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| 候选 | 漏斗产出 | 预筛→检测→评分→限量（Q21） | 待Gate | 组合生成 |
| 待Gate | 审核通过 | 人工 Gate | 待用 | 入待用池（库容默认 100，Q27） |
| 待Gate | 审核驳回/阻断 | 冲突/合规阻断（Q26） | 阻断 | 手拼不豁免合规 |
| 待用 | 消费 | 同平台+账号+发布位取用 1 次（Q24） | 已用 | 写 usage_record；跨平台可复用 |
| 待用/已用 | 冷却判定 | 同平台 7 天消费 ≥3 次 | 冷却 | 14 天回待用（Q24） |
| 待用/已用 | 爆款标注 | 一期手工；自动判定=中位数 5 倍+门槛（Q61） | 爆款 | 运营确认转正 |
| 已用 | 全平台用尽 | 所有目标平台用过 | 已用（终） | — |
| 阻断 | 复核 | — | 待用 / 归档 | 【待补】（M5 未开放复活端点，见下补登） |
| 待入库 | 入库 | 原文仅列名 | 【待补】（M5 未实现，见下补登） | 05 §2.6 已标待补 |
| 任意 | 归档 | — | 归档 | 终态 |

> **M5 实现补登（2026-09-14）**：物理上 gate_status（pending/blocked/approved）与生命周期 status（pending_gate/ready/used/blocked/archived）分两维——漏斗 ban 命中→(blocked, blocked) 不可批（409），其余→(pending, pending_gate)；Gate approve→(approved, ready)，reject→( , archived)。"冷却"不落组合状态：由 `pwc_platform_states` 按（组合,平台）置 cooldown/available（14 天到期 sweep 回 available，调度随 M10），故同组合在别的平台仍可消费。"已用"为目标平台全部用过后的终态（target_platforms 默认未配，来源【原文未给出，待补】，未配不产生）。"爆款"仅 is_hot 旗标（operations 手工，Q61 自动后置 M10），不改变 status；"待入库"仍【待补】未实现；阻断→复核路径原文【待补】，M5 未开放复活端点。

### 1.8 平台适配四态（段7 · PT-PLATFORM-ADAPTER）

> **实现状态（2026-09-17 核实，纯文档标注、非新裁决）**：段7 完整版属 **V2 P1**（Q73：V1 主链 段1→6→10→11，段 7/8 完整版在 V2；V1 只落 FCW 必需的静态底表 M11）。截至 Q116 **代码未实现**适配判定与人工审核端点（`platform/` 仅有发布位/平台规则/PCP 静态表，`platform_dynamic/` 为空包）——下表为**设计规格，待 V2 落地**。

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| （输入） | 适配判定 | 只读 frozen PWS；无 PWS 返回缺失不造假 | allow / downgrade / block / pending_review | AI 输出一律 pending_review 需 HumanGate |
| pending_review | 人工审核 | 降级动作只能从 Q38 动作字典选 | allow / downgrade / block | 禁生成 final_id、禁输出成稿 |

### 1.9 发布位/平台规则 Gate 状态（段7 · Q35）

> **Q118 裁决（2026-09-18，02 C1.62）**：负责人拍板「V1 只做 Q42 人工直编、回填默认值口径」。`publish_slots.gate` 默认 `approved`（`app/platform/platform_adaptation/models.py` 列默认 + `schemas.SlotUpsert.gate` 同默认）为 **V1 权威口径**：无 `slot_items` 表、无 `pending_gate` 产生路径、无 Gate 翻转端点（`pa_rules.GATE_PENDING="pending_gate"` 为 V2 预留常量，V1 全模块零引用）；集成测试锁定人工直建 slot 的 `gate=approved`（test_platform_api）。上表 `pending_gate → approved` 的 AI 拓展候选审核流**整体随段7 完整版/V2**，不在 V1 实现。（2026-09-17 实现核实标注的待拍板差异，经 Q118 销账。）

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| pending_gate | AI 拓展候选 | 四维全 0 | （等待审核） | line 1098-1221 |
| pending_gate | 审核通过 | 后台 CRUD | approved | 启用（Q35） |

### 1.10 客户审阅动作（段12 · Q59）

| 动作 | 条件 | 结果 | 副作用 |
|---|---|---|---|
| 通过 | — | 转"客户已确认"进发布 | 运营用托管账号发布 → 回填链接（Q60c；**回填一环 Q125 已落**：PUT /api/admin/content/{id}/publish-info，published_at 非空即已发布；Agent 抓取/推送随段13 V2） |
| 驳回 | **原因必填** | 退运营 | 原因数据回流段13 |
| 改稿 | **强制重过 CONTENT-COMPLIANCE 复检（4 项）**，不过不生效 | 更新成品 | 复检=词库扫描+语义级+施工指令+国家规则；改稿记档回流学习客户偏好（Q59） |

### 1.11 原子 evidence 状态（段4 · Q18）

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| pending_evidence | AI 自动补一轮 | — | evidence 完备 / 仍缺 | 仍缺→生成运营待办 |
| pending_evidence | 7 天超时 | 配置项（用户改自 14 天） | 自动驳回（可复活） | 复活=重走审核 |

### 1.12 字段池 Gate（段3 · FieldPool，**Gate 流转非状态机**）

> **M3 实现补登（2026-09-13）**：FieldPool 不在原文 11 组状态机清单内（Q116 后本表 §3 计为 13 组），仅一个 Gate 状态字段（04 §2.7 原列 approved/pending_gate，实现补 rejected 态），实现迁移如下：

| 当前态 | 事件 | 条件/Guard | 目标态 | 副作用 |
|---|---|---|---|---|
| （无池） | 提交方案（WF-02 候选） | PT-FP-PLAN 校验落 violations（不阻断提交） | pending_gate | 写 field_pools + fp_dimensions（Top8 selected / 余 backup）；新字段写 g2_field_candidates |
| pending_gate | 人工通过 | product_reviewer 角色；compliant=true（<3/缺角色维度等不可批） | approved | 池锁定；下游段4 唯一可消费 FieldPool |
| pending_gate | 人工驳回 | 原因留档 | rejected | — |
| pending_gate | 备选维度捞回 | 人工操作；满 8 时最低置信维度挤回 backup | pending_gate | 重算 compliant/violations（Q12） |
| rejected | 重新提交 | 仅 rejected 可重提 | pending_gate | 清旧维度行（审计保留）；approved 池不可重提/不可改 |

### 1.13 content_products 内容成品（段12 · Q56/Q59，**实现补登** Q116）

> 原文只给五态语义 + Q56 重生成 ≤3 + Q59 客户三动作，未给完整迁移表；下表为 Q116 实现补登的机械口径（`app/content/statemachine.py`，状态枚举常量为消息表禁译码）。

| 当前态 | 事件 | 条件 | 目标态 | 副作用 |
|---|---|---|---|---|
| draft | generate | operations 触发；final_id 存在 | generating | 建 content_products 行，按 FCW 只读组装材料 |
| revising | generate | 改稿后重生成（operations） | generating | `regenerate_count`+1 |
| generating | complete | 模型返回 + 词库复检完成 + 语义级复检（Q121，advisory）+ ARTICLE-QC 质检（Q120） | review | body 落库、review_hits（含 semantic 段）落库、quality_score/issues 落库（两项 AI 检测均 advisory，不驱动迁移） |
| review | approve | 客户通过 | ready_for_publish | 转发布（运营托管账号发布 → Q125 回填链接，published_at 非空即已发布；不新增 published 态） |
| review | reject | **原因必填**（空/空白 422） | rejected | reason 落 reject_reason，回流段13 |
| review | revise | `regenerate_count` < `content.regen_limit`（默认 3、运营可配，Q120 接通） | revising | 改稿须强制重过复检（Q59） |
| revising | **manual_resubmit** | **Q122/Q56-a 客户人工改正文后提交**（PATCH /api/content/{id}/body；不调 ARTICLE-GEN、不增 regenerate_count） | review | 重跑词库 + 语义复检 + ARTICLE-QC；审计 content.body_edited |
| 任意 | 达重生成上限 | `regenerate_count` ≥ `content.regen_limit`（默认 3） | 仅可 reject（Q120：人工 reject 承接，不自动作废 final_id/回池） | a 清洗区人工编辑（Q122 已落）/ b 运营显式作废（Q124 已落，见下） |
| review / revising / rejected | **discard** | **Q124 operations 显式动作**；reason 必填 1–500 字（空白 422）；draft/generating/ready_for_publish 不可作废（409） | **discarded（Q124 新终态、只读，终态无可用事件）** | 落 discard_reason + 审计 content.discarded；partial unique index `WHERE status<>'discarded'` 释放同键生成机会（回池），旧行只读保留 |

> **Q116 范围边界**：仅文章单语言（`kind=article`、`language=zh-CN`）；video 与多语言 422/缓做。**段12 的 Gate = 客户审阅（Q59）**，AI 生成不产 skill7 候选、不走运营 Gate（Q66）；复检第①项词库扫描随 Q116 落地（复用 Q48 词库 + ccr_rules，ban 硬阻断），第②项语义级检测随 Q121 落地（ARTICLE-SEMANTIC-CHECK，纯 advisory）；余 2 项（施工指令核对、国家规则核对）【原文未给出，待补】。
>
> **Q120 质量分补登（2026-09-18，02 C1.64）**：本表六态与迁移**不变**——AI 质量分（ARTICLE-QC，模型网关第 8 场景）纯 advisory，只在 complete 时随 body/review_hits 一并落 `quality_score`/`quality_issues`，**不新增状态、不自动发证/驳回、不阻断 approve**（Gate 仍为客户审阅 Q59）；低于阈值 `content.ai_quality_threshold`（默认 0.85）仅在视图置 `quality_advisory=true` 提示，QC 故障/坏输出分数留空仍进 review。重生成上限由硬编码 3 改为配置 `content.regen_limit`（默认 3、运营可改），超限只能人工 reject（Q56 的 a/b 转人工路径不自动化）。另：**多语言已于 Q119（02 C1.63）落地**，上句「多语言 422/缓做」以 Q119 为准；video 仍缓做。
>
> **Q121 语义级复检补登（2026-09-19，02 C1.65）**：本表六态与迁移**不变**——复检第②项语义级检测（ARTICLE-SEMANTIC-CHECK，模型网关第 9 场景）与 Q120 质量分同构，纯 advisory，只在 complete 时把 `{checked, findings, ?error}` 落入既有 `review_hits["semantic"]`，**不新增状态、不抬 block_required、不阻断 approve**（词库 ban 的硬阻断与客户审阅 Gate Q59 均不变）；检测不可用/坏输出不阻断生成（checked=false + error，照常进 review）。改稿 regenerate 同路径重跑（Q59 强制重过复检）。施工指令核对、国家规则核对两项仍【待补】，视频载体随 video-studio。

> **Q122 客户人工改稿补登（2026-09-19，02 C1.66）**：新增事件 `manual_resubmit`（**revising→review**），状态集合不变（仍六态）。客户在 revising 态经 `PATCH /api/content/{content_id}/body` 直接改正文（Q56-a 客户自助，无运营闸；非 revising 409、空白 422），提交后重跑词库 + 语义复检（Q121）+ ARTICLE-QC（Q120）回 review；**不调 ARTICLE-GEN、不增 regenerate_count**（人工改稿不占 AI 重生成轮次，但每轮仍须先 revise，间接受 content.regen_limit 约束）。生成 / regenerate 的 operations 闸与客户审阅三动作（approve/reject/revise）均不变；~~Q56-b 作废回池仍挂账~~ ✅ 已由 **Q124（C1.68）** 落地（第七态 discarded + partial unique index 回池 + needs-attention 队列），见下。

> **Q124 难产骨架作废回池补登（2026-09-19，02 C1.68，迁移 0032）**：状态集合由六态扩为**七态**——新增终态 **discarded（只读）**，与 rejected 分工：rejected=客户驳回、原因回流段13（可继续改），discarded=运营判定骨架不要、释放白名单键生成机会。事件 `discard`（operations 硬闸，reason 必填）：review/revising/rejected → discarded；generating/draft/ready_for_publish 不可作废；终态 allowed_events 为空。回池不做 FCW 状态回退、不自动作废，而是把唯一约束 `uq_content_final_language_kind(final_id,language,kind)` 换成 **partial unique index `WHERE status <> 'discarded'`**（postgresql_where + sqlite_where 双方言），作废后同键可重新生成、旧行保留；generate 查重另过滤 discarded。运营待处置数据源 = `GET /api/admin/content/needs-attention`（跨租户三态、created_at 升序、无 body，operations | platform_admin 读口），写口 `POST /api/content/{id}/discard`（operations）。迁移 0032 加 discard_reason Text + 换索引，pg16 实测 up/downgrade-1/up 对称（downgrade 还原普通唯一索引）。
>
> **Q125 运营发布回填与待发布队列补登（2026-09-19，02 C1.69，迁移 0033）**：ready_for_publish 之后**不新增状态**，`published_at` 非空即已发布信号；迁移 0033 加 published_url Text / platform_post_id String(128) / published_at timestamptz 三 nullable 列。写口 `PUT /api/admin/content/{id}/publish-info`（operations 硬闸、仅 ready 态 409、url 必填 422、可重复修正 url 但 published_at 仅首次落、post_id 缺省不动空串清空、审计 content.publish_info_set）；读口 `GET /api/admin/content/ready-to-publish`（operations | platform_admin，跨租户全部 ready 行，待回填/已回填两分区，回填后不离队可核对修正，行不含 body）。前端管理端 `/admin/content` 内容运营台与 Q124 共用一页（待发布 + 待处置双队列、两个 client 岛、Server Action 缺 operations 角色零请求）；客户详情页 ready 态显已回填外链。Agent 抓取、effect-callback、孤儿队列、Q60a 随段13 P3/V2。Q124/Q125 合计测试 534→554（+20）。

---

## 2. 全局迁移规则

| 规则 | 说明 | 来源 |
|---|---|---|
| 非法迁移 | 默认拒绝并记录审计【建议】；终态不接受任何事件【建议】 | — |
| 审计 | 每次迁移写 writeAudit（append-only） | D3.11 |
| RBAC 联动 | 红线迁移（PWS 冻结/原子正式化/合规废弃/Prompt 修改）必须对应角色 + 人工 Gate | line 7674 |
| 一票否决 | compliance.block_required=true 阻断 WF-09 迁移（FCW 不生成） | PT-COMPLIANCE |
| 不可变对象 | PWS/SkillRunLog/审计：状态只增不 mutate | PT-COMPLIANCE-V2.0 |

---

## 3. 与既有文档的核对项

- [x] 状态集合与 05 §2 完全一致（15 态/atom8/skill7/G1/PWS/PWC/平台四态/发布位 Gate/客户审/evidence/字段池 Gate/冷启动三分支/content_products = **13 组**；§1.13 为 Q116 实现补登）
- [x] Q4 超时兜底、Q5 类目创建中、Q18 evidence 超时、Q20 权限、Q24 冷却、Q29/Q30 重冻换版、Q32 断消费、Q53 版本校验、Q59 客户动作已落入迁移规则
- [x] productIntake15 原文【待补】迁移行（待补充参数/待确认额度/送审/审核中/需补充资料/建模中/入库失败）——2026-09-17 据 M1 实现（statemachine.py/service/router + Q106/Q107 外放口径）补登事件码、角色与已实现副作用，见 §1.1 M1 补登；原文未给的业务副作用仍标【待补】，V1 仅状态翻转+审计
- [ ] G1 类目审核/合并/废弃/归档迁移（§1.5：draft→review→active、任意→archived）——V1 仅落 dictionary_admin 建 draft 类目 + 叶子模板 draft/approved，完整类目管理随 V3（2026-09-17 核实，见 §1.5 补登）
- [ ] PWC 阻断→复核、待入库两行（§1.7）——M5 未实现，原文【待补】维持（见 §1.7 M5 补登）
