# 15. 代码结构与脚手架

> **状态**：🟢 已定稿（选型于 2026-09-13 拍板、技术栈同日确认；可作脚手架实现依据）
> **来源**：07_技术架构与工程规格.md §1（13 模块边界）/ §2（横切能力）｜06_契约层_WF与Skill协议.md（WF/Skill/Agent）｜14_技术选型决策.md（✅ 已定稿）
> **用途**：定义仓库目录结构、模块划分、可执行物（WF/Skill/Agent）落位、横切组件位置；AI 落代码时"按目录放文件、按规范接模块"。
> **说明**：模块名/可执行物名与 01/06/07 完全一致，不发明新名字。

---

## 1. 仓库总览（✅ 选型定稿后）

- 技术栈（2026-09-13 负责人确认）：
  - 后端：**Python 3.12 + FastAPI**（模块化单体；包边界即 13 模块服务边界，未来可独立化）
  - ORM/迁移：SQLAlchemy 2.x + Alembic（以 10 Schema 为源）【库选择为工程建议】
  - 编排：**自研注册表驱动编排器**（YAML 声明式，数据驱动；见 runtime/）
  - 数据层：PostgreSQL 16 + pgvector / Redis 7（缓存 + Streams）/ S3 兼容对象存储
  - 前端：**React + Next.js + TypeScript**（管理后台 + 客户前端）
  - 中台对接：Python SDK + Webhook（middleplatform/）
- 顶层目录：

```
loom/
├── docs/                 # 本 AI 文档库（唯一事实源，只读消费，不落代码）
├── backend/              # 主后端（FastAPI 模块化单体，13 模块 → packages）
│   ├── app/
│   │   ├── product/      #   product-intake / modeling / dimension / atom / condition / whitelist-center
│   │   ├── platform/     #   platform-adaptation / platform-dynamic
│   │   ├── decision/     #   layer-strategy / compliance-center（进程内独立合规模块）
│   │   ├── final/        #   final-whitelist
│   │   ├── content/      #   content-manage
│   │   ├── feedback/     #   feedback-loop
│   │   └── core/         # 横切：skill7 通道 / writeAudit / RBAC / 配置中心 / SLA 引擎 / 模型注册表
│   ├── alembic/          # 迁移脚本（与 10 Schema 实体一一对应）
│   └── tests/            # unit / integration / contract / e2e（16 测试策略）
├── runtime/              # 编排运行时（自研，YAML 数据驱动）
│   ├── workflows/        #   WF-01.yaml ~ WF-12.yaml
│   ├── skills/           #   <skill_id>/{definition.yaml, prompt/*.md, impl.py}
│   ├── agents/           #   AG-REVIEW-COPILOT / AG-PM-AUDIT / AG-SUPPORT
│   └── protocols/        #   PT-<名称>-<版本>.md（对齐 12 清单）
├── eval/                 # Evaluation Dataset + Golden Cases（14 §2.6，MVP 质量两件套；M10-Q 已落，见 §2.5）
├── frontend/             # React + Next.js（管理后台 + 客户前端，09 D5 菜单；Q96 起含 next-intl/[locale] 与 tokens，见下方实现补登）
├── middleplatform/       # 中台对接：API / Webhook / Python SDK / CSV-JSON 导出（09 D4）
└── infra/                # docker-compose / 部署 / 监控（对接 17）
```

> 模块 ↔ 07 §1 映射：product-* ×6、platform-* ×2、decision-* ×2、final ×1、content ×1、feedback ×1 = 13 后台模块（07 §1）。
> 独立化预留：whitelist-center / compliance-center / feedback-loop 三个包的对外接口按服务边界设计（14 §2.1/2.5）。

> **实现补登（2026-09-14 更新，M1–M6 后端切片）**：已建包——`app/core/`（db/`models.py` 共享 AuditLog、`audit/` append-only 写入、`actor.py` 跨域 Actor、`compliance_wordlist/` Q48 统一词库横切包；skill7/RBAC/配置中心/SLA 等横切随 M10）、`app/product/product_intake/`（段1：statemachine/service/router/models/schemas）、`app/product/modeling/`（段2：c1/c7 纯逻辑 + service/router/models/schemas）、`app/product/fieldpool/`（段3：planning 纯逻辑 + service/router/models/schemas）、`app/product/atom/`（段4：atom_rules 纯逻辑 + service/router/models/schemas）、`app/product/condition/`（段5：pwc_rules 纯逻辑 + service/router/models/schemas）、`app/product/whitelist_center/`（段6：pws_rules 纯逻辑 + service/router/models/schemas）。与上图规划名的对应：product-intake→`product_intake`、modeling→`modeling`、dimension→`fieldpool`（段3 落字段池）、段4 原子→`atom`、段5 条件包→`condition`、whitelist-center→`whitelist_center`（段6 PWS 冻结）。迁移：0001（M1 四表）/0002（M2 八表+种子）/0003（M3 三表+六路由种子）/0004（M4 词表+批次/候选/冲突/正式原子五表）/0005（M5 contentGoals+池配置+条件包+组合行+取用流水+平台状态六表，含五码种子）/0006（M6 PWS 快照+快照成分行+冻结日志三表），均在 PG16 实测 up/down/up。测试：tests/unit + tests/integration（httpx ASGITransport + in-memory SQLite），118 条全绿；runtime/ 数据驱动编排：M10 切片 e（2026-09-14）起已落 WF-04 与三个 Skill YAML + `app/core/skill7/` 注册表加载/通道/适配器；WF-02 字段池替换切片（Q78，2026-09-14）追落 WF-02 + FIELDPOOL-PLAN/DIM-SOURCE/DIM-MERGE 三 Skill YAML 与 field_plan 适配器；**WF-01 冷启动识别替换切片（Q79，2026-09-14）追落 WF-01 + PARSE/UNDERSTAND/CAT-RECOG/TYPE-MATCH/MISSING-INFO/RISK-TAG 六 Skill YAML、c1_recognition 适配器与迁移 0013（skill_runs/skill_candidates 双锚点：加 nullable intake_id、skill_candidates.product_space_id 改 nullable）**；**WF-03 原子批次替换切片（Q80，2026-09-14）追落 WF-03 + ATOM-EXPAND/ATOM-CANON/ATOM-AFFINITY/CONFLICT-PRECHECK 四 Skill YAML 与 atom_batch 适配器（无新迁移，复用 PS 锚点）**；**WF-01 C7 Layer4 替换切片（Q81，2026-09-14）在 WF-01 追挂第二个候选产出步骤 TYPE-MATCH（c7_layer4，intake 锚点，无新迁移）与第二道 operations Gate、c7_layer4 适配器，Q79-1 挂账销账**；M10 WF 替换切片四组全部完成（WF-04 试点 Q76→WF-02 Q78→WF-01 Q79/Q81→WF-03 Q80）；其余 WF/Skill 随后续切片补。**Q67/Q82 模型网关切片（2026-09-14）**：新增 `app/core/model_registry/`（models/crypto/gateway/drivers/synthetic/extraction/schemas/seeds/router 九模块）+ skill7 service 拆出 `deliver_generated_run`（系统机器 Actor，source=llm_auto，无角色门）+ 迁移 0014（五表 + skill_runs 成本四列 + synthetic/CAT-RECOG 种子），PG16 up/downgrade-1/up 实测，后端测试 253 全绿。**Q83 PWC-BUILDER 第二站切片（2026-09-14）**：model_registry 增第十模块 `pwc_build`（前置闸/有界机读变量/输出校验/模型回带分剥离/跨候选保序去重→多候选投递），synthetic 增 PWC-BUILDER 结构夹具（只配对、不造分），seeds 增 PWC-BUILDER Prompt v0.1；condition router 增 `POST /api/product-spaces/{id}/pwc/llm-build`（operations，restock_auto 不自动消费）；迁移 0015 纯种子（PWC-BUILDER 路由 + Prompt v0.1，无 schema 变更），PG16 up/downgrade-1/up 实测，后端测试 263 全绿，eval 仍 101/101 不新增。**Q84 TYPE-MATCH 第三站切片（2026-09-15）**：model_registry 增第十一模块 `c7_resolve`（intake ai_recognizing/类目 active 前置闸、有界机读变量、category_id·required_fids 回显校验、模型回带 confidence/score/fid/status/source_route 剥离→单候选 c7_layer4 投递），synthetic 增 TYPE-MATCH 覆盖率缺口结构夹具（只补缺、不打置信分），seeds 增 TYPE-MATCH Prompt v0.1；product_intake 侧 model_registry router 增 `POST /api/intakes/{id}/c7/llm-resolve`（operations，不与 CAT-RECOG 自动串联）；迁移 0016 纯种子（TYPE-MATCH 路由 + Prompt v0.1，无 schema 变更），PG16 up/downgrade-1/up 实测，后端测试 276 全绿，eval 仍 101/101 不新增。**Q85 DIM-MERGE 第四站切片（2026-09-15）**：model_registry 增第十二模块 `field_plan`（无池/rejected 池态前置闸、有界机读变量、target_atom_min/max 回显校验、role/route/fid 结构白名单、line 14081 source_ref 非空校验、confidence 按 PT-FP-PLAN 契约 0..1 透传（区别于 Q83/Q84 剥离）→整方案单候选 field_plan 投递），synthetic 增 DIM-MERGE 合规整方案结构夹具（G2 复用+资料键补维+敏感补 risk_control、固定 0.9 不模拟分布），seeds 增 DIM-MERGE Prompt v0.1；fieldpool router 增 `POST /api/product-spaces/{id}/field-pools/llm-plan`（operations，不自动串链）；迁移 0017 纯种子（DIM-MERGE 路由 + Prompt v0.1，无 schema 变更），PG16 up/downgrade-1/up 实测，后端测试 289 全绿，eval 仍 101/101 不新增。**Q86 CONFLICT-PRECHECK+ATOM-AFFINITY 第五站切片（2026-09-15）**：model_registry 增第十三模块 `atom_expand`（PS/字段池/停拓预闸、有界机读变量与回显校验、CONFLICT-PRECHECK chat → ATOM-AFFINITY embed 双调用合并为单 SkillRun（input tokens/cost 求和、output 仅 chat）、输出白名单剥离模型自报 affinity/cluster_id、ai_risk 枚举校验透传、embedding 以 `_embeddings` 机读键随候选带外投递），drivers/gateway 增 `embed` 通道（ai_models.capability=chat|embedding 能力校验、per-1M input-only 成本、行数/维度校验），synthetic 增第二个模型夹具 `synthetic-embedding`（blake2b 哈希字袋 1536 维单位向量）与 CONFLICT-PRECHECK 结构夹具（每维一对共享长前缀近似原子、不造分/不造亲和），atom_rules 增 cosine_similarity/max_affinity/assign_clusters 纯函数（进程内余弦+同维并查集成簇），模型侧新增 portable Vector TypeDecorator（PG vector(1536)/SQLite float32 LargeBinary），atom router 增 `POST /api/product-spaces/{id}/atom-batches/llm-expand`（operations，不自动串链，approve 复制向量到正式实例）；迁移 0018 带 schema（pgvector 扩展/capability/两 embedding 列/atom.cluster_line=0.9 借 Q10 同义线原文未给），pg16 pgvector 容器 up/downgrade-1/up 实测，后端测试 306 全绿，eval 仍 101/101 不新增。**Q87 M8 restock_auto worker 切片（2026-09-15）**：新增横切包 `app/core/restock/`（worker.py=`run_restock` 一轮函数 + `RestockWorker` 进程内 asyncio 循环，router.py=`POST /api/admin/restock/run` 管理端点）消费 Q71/Q76-4 的 requested/restock_auto 信号行；纯追加认领（requested 行不 mutate，成功/终态各追加 succeeded/failed llm_auto 子 run，input.restock_request_id 回链，反连接防重），仅消费 PWC-BUILDER、只产 pending_review；pwc_build 拆 HTTP 包装（OPERATIONS 闸保留）/内部入口 `invoke_pwc_build_for_restock`（system actor 无角色，受信进程内调度同 SLA sweep）/共享 `_run_pwc_build`（Q83 手动路径行为零变化）；gateway 新增 `BudgetExhausted(ModelUnavailable)` 子类区分日预算硬停（瞬态）与停用无 fallback（终态），HTTP 映射不变；config 新增 `LOOM_RESTOCK_WORKER_ENABLED`（**默认 false opt-in**）/`LOOM_RESTOCK_INTERVAL_SECONDS`=60/`LOOM_RESTOCK_BATCH_SIZE`=20；main lifespan 与 SweepScheduler 同生命周期启停（task 名 loom-restock-worker）；audit 三动作 restock_delivered/restock_failed/restock_deferred；**无 Alembic 迁移（failed 状态 Q71 已预留，头仍 0018）**，后端测试 315 全绿，eval 仍 101/101 不新增。**Q88 入站一 Agent 一 Key 凭证半套（2026-09-15）**：新增横切包 `app/core/api_keys/`（models.py=`agent_api_keys` 表只存 SHA-256+展示前缀、service.py=generate/hash_key/parse_bearer/issue/list/revoke/verify/require_agent_key 401 依赖、schemas.py=Issue/Revoke/View/Issued(secret)、router.py=`/api/admin/agent-keys` 三端点，main.py 挂载），与 model_registry（outbound Fernet 域）并列不合并；platform_admin 红线、审计 agent_api_key.issue/revoke、吊销 append-only、未知/吊销验签统一 None；迁移 **0019_agent_api_keys**（pg16 一次性容器实测），后端测试 332 全绿（+17 集成），eval 仍 101/101 不新增；effect-callback 业务端点随段13 V2，V1 无受保护消费端点为显式挂账（02 C1.32）。**Q89 多副本单实例锁（2026-09-15）**：新增横切包 `app/core/locking/`（service.py=`leader_lock` Redis SET NX PX 异步上下文管理器，uuid token + Lua 比对释放/续约 + 看门狗 TTL/3 续约；LockUnavailable/LockBackendError 两异常；override_lock_client 测试缝），SweepScheduler/RestockWorker 各抽 `_tick()` 抢循环级命名锁（loom:lock:sla-sweep / loom:lock:restock-worker），非持锁副本与 Redis 故障均跳轮；config 新增 `LOOM_DISTRIBUTED_LOCK_ENABLED`（**默认 false**，开启后 fail-closed）；两个手工 /run 端点同锁（抢不到 409、Redis 故障 503）；无 Alembic 迁移（头仍 0019），后端测试 345 全绿（+13 集成，内存 FakeRedis），eval 仍 101/101；fencing token/Streams/缓存广播仍挂账 V2（02 C1.33）；**Q90 restock 瞬态退避已落地（2026-09-15，02 C1.34，迁移 0020）**：`app/core/restock/` 增 models.py（RestockRetryState 游标表），worker 指数退避 60×2^(n−1) 封顶 1800s、BudgetExhausted 永不转终态/GenerationUpstreamError 10 次转终态、成功/终态删游标，手工 /run honor_backoff=False 绕窗口仍计数；config 新增 `LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`LOOM_RESTOCK_BACKOFF_MAX_SECONDS=1800`/`LOOM_RESTOCK_BACKOFF_MAX_ATTEMPTS=10`；后端测试 351 全绿，eval 仍 101/101。**Q91 编排并行 DAG 原语已落地（2026-09-15，02 C1.35，无迁移）**：新包 `app/core/orchestration/`（planner 拓扑分层/五类加载期校验 + engine 同层 asyncio 并发/层间屏障/fail-fast），needs 可选、线性 WF 兼容、skill7 Gate 前驱闭包不跨、处理器注入不记账，env `LOOM_ORCH_MAX_CONCURRENCY=4`；后端测试 371 全绿，eval 仍 101/101。**Q92 M12 两个驾驶舱只读聚合已落地（2026-09-15，02 C1.36，无迁移）**：新包 `app/core/dashboards/`（service 纯 SELECT 聚合 + router 两端点，main 挂载 `/api/admin/dashboards`），Token 成本（天×模型×币种，默认 30 天窗，failed 单列，多币种不换算）+ 人工审核工作量（候选/待办积压快照+窗口产出），platform_admin、管理面 GET 首次 query 携带 actor（422/403），不建表不物化无租户过滤；后端测试 378 全绿，eval 仍 101/101。**Q93 M12 统一审核工作台已落地（2026-09-15，02 C1.37，迁移 0021 纯配置种子）**：新包 `app/core/workbench/`（risk 风险四档派生 + service 队列/批量 + router），五型候选统一队列（风险降档+同档最老优先、筛选分页）与 `batch-approve` 置信批量通过（新键 review.batch_pass_confidence=0.85，high/critical 禁批量，整批单事务复用 skill7 适配器）；可见角色=注册表 WF skill7 Gate 角色并集（operations+product_reviewer，platform_admin 不放行）；后端测试 384 全绿，eval 仍 101/101。**Q94 候选审核 SLA 待办挂接已落地（2026-09-16，02 C1.38，迁移 0022 五条配置种子）**：`app/core/skill7/review_sla.py`（投递同事务建 review_{target_type} 待办、裁决 resolve），五型时限键 review.sla_hours.*（种子 72h 待补），Q49 sweep 零改动复用、升级动作 skill7.review_sla_escalated；后端测试 391 全绿，eval 仍 101/101。**Q95 租户管理+Onboarding 已落地（2026-09-16，02 C1.39，迁移 0023 建 tenants 表+存量 DISTINCT 回填 basic/active）**：`app/core/tenants/`（models 四档 plan/三态 status/试用月额度 50 万、service 开通/改套餐/暂停/恢复+assert_intake_admitted 段1 准入、router `/api/admin/tenants` 全程审计、schemas 派生 onboarding 进度）；段1 create_intake 未知租户 404/暂停 409（应用层无硬外键）；无新调度/环境变量；后端测试 401 全绿，eval 仍 101/101。

> **前端实现补登（2026-09-16，Q96 脚手架切片，docs/18 §2.3/§3.6）**：`frontend/` 落 next-intl v4 三件套——`middleware.ts`（matcher 排除 api/_next/_vercel/带点文件，无前缀请求 307 → /zh-CN，实测）、`i18n/routing.ts`（defineRouting 仅注册 zh-CN）、`i18n/request.ts`（hasLocale 兜底 + 动态 import `messages/zh-CN.json`）；页面全部迁入 `app/[locale]/`（layout 注入 NextIntlClientProvider 并 import tokens.css，旧根 app/layout.tsx、app/page.tsx 已删）；设计 Token 唯一事实源 `app/tokens.css`（Primitive+Semantic 85 变量，AntD5 借值）+ JS 镜像 `app/tokens.ts`，一致性由零依赖 `scripts/check-tokens.mjs`（npm run check-tokens）强制；next.config.mjs 接 createNextIntlPlugin。typecheck/build 通过，/zh-CN SSG，chrome-devtools MCP 9222 可视验收通过。M12 菜单页面随后续切片在 `[locale]` 段内逐片新增。**Q97 应用外壳（2026-09-16）**：路由组 `app/[locale]/(shell)/`（layout+sidebar+menu-placeholder+8 菜单占位页），导航注册表 `app/[locale]/nav.ts`、`i18n/navigation.ts`；`/[locale]` 307 到 `/workbench`；`scripts/check-nav.mjs` 三方漂移校验；新增 `--color-bg-hover` 语义变量（86 个）；管理端未来 `/[locale]/admin` 独立布局不预埋。**Q98 产品中心功能页（2026-09-16，跨栈）**：`(shell)/products/` 落 RSC 列表（force-dynamic）+ Server Action 新建 draft + `[intakeId]` 只读详情 + templates/library 占位，唯一后端访问层 `frontend/lib/api.ts`（server-only env `LOOM_API_BASE_URL`/`LOOM_TENANT_ID`，浏览器经 RSC/Action 间接访问，无 CORS），产品名走工程临时键 `product_name`（G2 fid 基线挂账）；后端段1 补 `GET /api/intakes` 租户分页只读端点；`scripts/check-products.mjs` 零依赖一致性校验。**Q99 工作台功能页（2026-09-16，跨栈）**：`(shell)/workbench/` 占位重写为 RSC 总览页（force-dynamic：录入单总数 + 15 态分组 + 快捷入口 + D5 五指标 V2 禁用卡），消息新增 `workbench.*` 与 `intake.status.*`（十五态对齐 docs/13；products 列表/详情状态 chip 同一切片中文化）；后端段1 补 `GET /api/intakes/overview` 租户状态聚合只读端点（注册序先于 /{intake_id}），lib/api.ts 增 getIntakeOverview；`scripts/check-workbench.mjs` 零依赖一致性校验（含跨栈路由顺序守卫）。**Q100 中台 CSV 导出（2026-09-16，后端纯只读）**：新横切包 `backend/app/core/exports/`（service `exported_final_ids`/`render_csv` + router，main 挂 `/api/exports`）落 `GET /api/exports/fcw.csv`——仅 final_id 单列（含表头）同步流式 CSV，tenant_id 必填空则 422、product_space_id 可选收窄，只导 published、created_at DESC；读路径不触发 Q95 准入门，未知租户 200 仅表头；无前端、无迁移（Alembic 头仍 0023）。**Q101 合规风控功能页（2026-09-16，跨栈只读）**：`(shell)/compliance/` 占位重写为 RSC（force-dynamic）——active frozen PWS 一行一产品，CCR 四态/法审三态语义 chip（compliance.module.css 四 tone 消费 --color-state-*/-text 与 --color-*-bg，token 总数仍 86），`<details>` 行展开逐市场最新报告（ban 词/`{word} → {target}` 降级）与法审 dl；页面纯只读，不挂清洗/降级/裁决/词库任何写口；消息新增 `compliance.*`（含 ccr./law. 状态键）；后端段10 补 `GET /api/compliance/overview` 租户聚合只读端点（service `overview_compliance` 每市场取最新、跨市场从严、未知租户 200 空、tenant_id 空 422），lib/api.ts 增 getComplianceOverview 与 Ccr*/LawReviewView/ComplianceOverview(Item) 类型；`scripts/check-compliance.mjs` 零依赖一致性校验（含禁写台面/禁 NEXT_PUBLIC/Query(min_length=1) 跨栈守卫）。**Q102 管理端首片（2026-09-16，纯前端）**：新增与 `(shell)` 平级的路由组 `app/[locale]/admin/`（layout+admin-sidebar+admin.module.css+index 重定向+token-cost/review-workload 两 RSC 页，force-dynamic），只读消费既有 Q92 两驾驶舱 API（回显默认近 30 天窗，无日期筛选器；合计卡+按天×模型/Skill/失败三表；候选/待办积压与窗口产出四表，等待秒数机械换算）；客户 shell 八菜单不放入口；管理员身份走 server-only env `LOOM_ADMIN_ACTOR_ID`/`LOOM_ADMIN_ROLES`（RSC 拼 actor_id+重复 roles，未配置显 unconfigured 不请求，V2 改会话派生）；枚举码原样不翻译；admin.module.css 只消费既有语义 token（仍 86 个）；lib/api.ts 增两驾驶舱全套类型与 getTokenCostDashboard/getReviewWorkloadDashboard；消息新增 `admin.*`（含 tokenCost./workload.）；`scripts/check-admin.mjs` 零依赖校验（含枚举码禁入消息表、nav 恰 8 项无 admin、admin 不在 (shell)、css 禁硬编码色、env 名与 append roles、后端 prefix/两 GET/require_admin_view 守卫）；后端零改动（414 测试不变）。**Q103 统一审核台前端页（2026-09-16，跨栈加法）**：`app/[locale]/admin/review-queue/` 新增 page.tsx（RSC force-dynamic，Next15 searchParams Promise 取值，state/target_type/risk_level/wf_id 经 URL searchParams 服务端筛选白名单归一，offset 简分页 limit 50；阈值注记回显 batch_pass_confidence）+ actions.ts（"use server" batchApproveAction：unconfigured/空选→判别联合，ApiError 映射 403/404/409/422/unknown）+ batch-bar.tsx（唯一 client 岛：useTranslations 内部自取文案（函数不得作 props 传 Client Component，E2E 500 实证后改此形态）、querySelectorAll 收集 data-batch-candidate 勾选、useTransition、成功 router.refresh，禁 import @/lib/api）；五型枚举码/四风险码/三态码原样直出（风险 critical/high/medium/low 为 danger/warning/info/success 语义色 chip，risk_reason 来源码副显，confidence 显百分比），payload 以 `<details><pre>` 原样 JSON 折叠不为五型造字段中文名；非 batch_eligible 勾选框禁用（含 applied/archived 全态），BatchBar 仅 pending_review 渲染；admin-sidebar 增第三项 /admin/review-queue；admin.module.css 增 filters/chip/filterFields/pager/batchBar/riskChip 四 tone 等只消费既有语义 token（仍 86 个）；lib/api.ts 增 ReviewQueue(Candidate) 类型、getReviewQueue（actor_id+重复 roles+state/重复 target_type/risk_level/wf_id/limit/offset）与 batchApprove（POST body 内 actor:{id,roles}）；消息新增 `admin.reviewQueueNav` 与 `admin.reviewQueue.*`（枚举码不入消息表）；后端唯一加法为 workbench `_candidate_view` 补 `"payload": cand.payload`（无迁移，414 测试不变/101 eval 不变）；`scripts/check-admin.mjs` 扩展（三文件/force-dynamic/只读禁写/十二枚举码原样/data-batch-candidate/details+JSON.stringify/Server Action 四状态/client 岛禁直连/后端 prefix+两路由+require_workbench_view+payload 字段守卫，失败路径实测后还原）。**Q104 逐条裁决（2026-09-16，跨栈加法）**：`review-queue/decision-cell.tsx` 新建为唯一 client 岛（"use client"；Mode=idle/modify/reject；通过 confirmed（risk_level==="critical" 时 window.confirm 中文二次确认）/修改后通过 modified（内联 JSON textarea 预填 JSON.stringify 两空格，JSON.parse 失败或非非数组对象显 invalidJson 零请求）/拒绝 rejected（理由可选）；useTransition 调 Server Action 成功 router.refresh；禁 import @/lib/api——连 type import 也禁——与任何硬编码 URL，checker 子串守卫）；`actions.ts` 加本地 CandidateDecision 联合、errorDetail（lib/api 把 FastAPI {"detail"} 错误体 JSON.stringify 进 Error.message，此处 JSON.parse 取回英文 detail）、判别联合 DecideActionResult（ok | 403/404/409/422+detail | unconfigured/unknown）与 decideCandidateAction（actor 取 server env 无则 unconfigured、空 id→404、trim reason、仅 modified 传 payload、KNOWN_STATUSES 映射，batchApproveAction 原样保留）；422 适配器业务错误英文 detail 原样红文不翻译，403/404/409 走既有中文 error 键；page.tsx 加裁决列 colDecision——pending_review 渲染 `<DecisionCell>`（initialPayload=美化 JSON），applied/archived 渲染 stateApplied/stateArchived chip+reviewedAt+humanModified+review_note（空值不渲染），岛仅 pending；lib/api.ts ReviewQueueCandidate 加 review_note、新增 CandidateDecision(Body) 类型与 decideCandidate（POST /api/skill-candidates/{id}/decision，body decision/payload??null/reason??null/actor{id,roles}）；消息新增 13 键（colDecision/reviewNote/decideConfirm/decideModify/decideReject/submitDecision/submitReject/cancel/reasonField/payloadReplacement/invalidJson/criticalConfirm/decideSuccess），枚举码不入消息表；admin.module.css 追加 decisionCell/decideActions/decidePanel/decideField/jsonArea/stateApplied/stateArchived 只消费既有语义 token（仍 86 个）；后端唯一加法=workbench `_candidate_view` 在 human_modified 与 reviewed_by 之间补 `"review_note": cand.review_note`（无迁移，头仍 0023；风险排序用例补 None 断言 + 新增 rejected 后 archived 视图回显备注集成一条，全套 415）；check-admin.mjs 扩展（13 键/必备文件/decideCandidate api token/review_note 守卫/Q104 块——decision-cell 文件先 existsSync 守卫再读避免缺失时抛栈、actions 用 split("decideCandidateAction")[1] 限定函数体内查四状态码避免命中 batchApproveAction、api 正则 /\/api\/skill-candidates\// 加 "/decision`" 子路径、page 岛仅 pending_review 守卫）。E2E 机械教训：chrome-devtools 对 React 受控 textarea/input 的 fill 在 refocus 后可能不更新 state，Ctrl/Meta+A 在受控 Chrome 不选中文本、type_text 插入光标中；可靠写法=evaluate_script 取 HTMLTextAreaElement/HTMLInputElement.prototype value 原生 setter 设值并 dispatch input Event，再 DOM .click() 提交。**Q105 租户管理 + Onboarding 前端页（2026-09-16，纯前端无迁移）**：新增 `app/[locale]/admin/tenants/`——tenant-codes.ts（TENANT_PLANS trial/basic/pro/enterprise + TENANT_STATUSES trial/active/paused as const 单一事实源）、actions.ts（"use server"：provisionTenantAction/changePlanAction/pauseTenantAction/resumeTenantAction 四导出，TenantActionResult 判别联合 ok | 403/404/409/422+detail | unconfigured/unknown，KNOWN_STATUSES 四码，errorDetail 复用 Q104 JSON.parse 法，actor 取 server env，空 id provision→422 其余→404，空名 trim→null）、provision-form.tsx（唯一开通 client 岛，tenantId required/name 可选/plan select 默认 trial，成功清空+router.refresh）、tenant-row-actions.tsx（每行 client 岛，plan select+改套餐按钮同档禁用，暂停/恢复按 status 二选一，统一 startTransition+router.refresh）、page.tsx（RSC force-dynamic listTenants 全量七列，planChip 等宽原样、statusTrial/statusActive/statusPaused 套 riskChip 基类 info/success/warning 语义色、quota null 显 —、created ISO slice 16 分、空态 colSpan 7）、`[tenantId]/page.tsx`（RSC force-dynamic，params Promise await，getTenant，注册表六字段 dl + Onboarding intakes/product_spaces/first_modeling_started 是/否 dl，onboardingNote 注明实时派生无引导状态机）；lib/api.ts 增 TenantView/TenantOnboarding/TenantDetailView 与 listTenants/getTenant/provisionTenant/changeTenantPlan/pauseTenant/resumeTenant（adminPath 编码，复用 Q95 `/api/admin/tenants` 六路由，写口 body actor{id,roles}，无新写口/无删除）；admin-sidebar 增第四项 /admin/tenants；消息增 admin.tenantsNav + admin.tenants.* 29 键（plan/status 枚举码不入消息值，付费档额度/"第5档"【原文未给出，待补】）；admin.module.css 追加 provisionForm/provisionFields/planChip/statusTrial/statusActive/statusPaused/detailList/onboardingNote 只消费既有语义 token（仍 86 个）；后端零改动（415 测试/101 eval 不变，头仍 0023）；check-admin.mjs 扩展（29 键/六文件/六枚举禁译/侧栏第四项/api 六函数+prefix+三子路径/Q105 块——两页面 force-dynamic·禁 NEXT_PUBLIC·URL·写词、详情派生三字段、actions 四导出名+四状态码+CURRENT_ADMIN_ACTOR_ID、两岛 "use client"/禁 @/lib/api·URL/router.refresh、四 action 至少一岛引用、codes 四 plan 原样、三态 statusXxx 必在页面、后端 router prefix+require_admin_view+六路由探针——详情探针 `@router.get("/{tenant_id}"` 不带尾括号匹配 response_model 签名；失败探针向未跟踪岛追加 @/lib/api import 实测 exit 1，新文件 git checkout 不适用故 Edit 还原）。**Q106 15 态录入单客户操作 + draft 资料编辑（2026-09-16，纯前端无迁移）**：`(shell)/products/` 增 intake-codes.ts（CUSTOMER_INTAKE_EVENTS = submit/params_completed/quota_confirmed/resubmit as const 单一事实源 + isCustomerIntakeEvent 类型守卫；运营 ops_confirm/to_cold_start/reject/b2_*、系统 wf01_*/auto_confirm、触发方【待补】send_review/review_*/start_modeling/model_*/failed_* 均排除并注释理由）、intake-actions.tsx（client 操作岛：allowedEvents∩白名单按白名单序出按钮，空交集显 noCustomerActions，成功 actionSuccess+router.refresh）、draft-profile-form.tsx（client 编辑岛，仅 draft 由页面闸门挂载，产品名 required/maxLength 100）；actions.ts 保留 createProductAction，增 IntakeActionResult 判别联合（ok | 403/404/409/422+missingFids | unconfigured/unknown）、missingFids()（解析 FastAPI `{"detail":{"missing_fids":[...]}}` 包装，非 JSON 吞掉）、transitionIntakeAction/updateDraftProfileAction（无 LOOM_ACTOR_ID→unconfigured 零请求，非白名单事件/空 id 本地 422）；lib/api.ts 增 server-only CURRENT_ACTOR_ID（env 自报 roles:[]，V2 改会话派生）、AllowedEvents/ProductSpaceView 与 getAllowedEvents/transitionIntake/patchIntakeProfile/getProductSpace（404→null）；`[intakeId]/page.tsx` 改 force-dynamic，Promise.all 取 intake+allowed-events 再取 product-space，draft 态挂 DraftProfileForm、操作区 unconfigured 不出岛、category_creating 显"资料分析中"中性话术（docs/13 原文）、空间区只读 dl（lifecycle 码原样等宽）+快照 details/pre；消息增 products.* 18 键（eventLabels 四事件为操作动词语译，事件码本身不入值；missingFields 带 {fids} 占位）；.env.example 增 LOOM_ACTOR_ID（不带 NEXT_PUBLIC）；products.module.css 追加 actions/actionButtons/successText/snapshotDetails/snapshotPre 只消费既有语义 token（仍 86 个）；后端零改动（415 测试/101 eval 不变，头仍 0023）；check-products.mjs 扩展（18 键含 {fids} 占位/三新文件/env 禁 NEXT_PUBLIC 赋值/api 五符号+四路径/actions 六符号/两岛 "use client"·禁 @/lib/api·fetch·URL·必 router.refresh/白名单精确四码+禁五组前缀/详情页九符号+draft 闸门/后端 router 四路由+statemachine 四事件防漂移；失败探针岛注 @/lib/api 与白名单注 ops_confirm 均实测 exit 1 后 Edit 还原）。E2E 机械教训：client 岛与 products.module.css 同目录时 CSS import 须 `./`，tsc 不查 CSS 模块存在性、只有 next 构建期暴露——新接岛必须真实跑页面。**Q107 管理端录入单运营队列与跨租户运营操作（2026-09-16，跨栈加法无迁移）**：后端段1 三文件——service `list_ops_intakes`（跨租户 select+可选 status、count 子查询、created_at DESC/limit/offset）、schemas OpsIntakeView(IntakeView)+created_at:datetime / OpsIntakeList{items,total,limit,offset}、router require_ops_view 依赖（Query actor_id 必填+重复 roles，require_any_role(actor, OPERATIONS, PLATFORM_ADMIN)，PermissionDenied→403）与 `GET /api/intakes/ops-queue`（status 不在 STATE_LABELS 15 态→422 `unknown intake status: {status}`，limit 1-100 默认 20；注册序在 /{intake_id} 前）；无新写口——运营操作复用既有 POST transitions。前端新增 `app/[locale]/admin/intakes/` 五文件：ops-intake-codes.ts（OPS_INTAKE_EVENTS 恰 6 码 ops_confirm/to_cold_start/reject/b2_approved/b2_parent_fallback/b2_rejected + INTAKE_STATUS_FILTERS 15 态 as const 单一事实源与类型守卫，注释列排除理由）、actions.ts（"use server" OpsIntakeActionResult 判别联合 ok | 403/404/409/422+detail | unconfigured/missing_role/unknown；无 actor→unconfigured、非白名单/空 id→本地 422、ADMIN_ROLE_LIST 不含 operations→missing_role 零请求；KNOWN_STATUSES+errorDetail JSON.parse）、ops-intake-actions.tsx（唯一 client 岛，allowedEvents∩白名单按白名单序出按钮，空交集显 noOpsActions，useTransition+router.refresh，禁 @/lib/api 连 type import）、page.tsx（RSC force-dynamic，searchParams Promise，15 态 GET 表单筛选+白名单归一，PAGE_LIMIT=50 仿 review-queue pager，四列）、`[intakeId]/page.tsx`（RSC force-dynamic，Promise.all getIntake+getAllowedEvents，404→notFound()，getProductSpace 404→null，基本信息/原样 profile dl/操作岛/产品空间只读区，CSS 两级 `../../admin.module.css`）；lib/api.ts 原 ADMIN_ROLES 改名导出 ADMIN_ROLE_LIST（8 处引用同步），增 OpsIntakeView/OpsIntakeList/getOpsIntakeQueue/adminTransitionIntake（body actor{id,roles}）；admin-sidebar 增第五项（恰 5 项）；消息增 admin.opsIntakesNav + admin.opsIntakes.* 41 键（eventLabels 六事件为动词语译、事件码不入值；15 态状态词零新增复用 intake.status.*）；.env.example 补 LOOM_ADMIN_ACTOR_ID/LOOM_ADMIN_ROLES 并注明 operations 角色硬要求；admin.module.css 零新增（全复用 table/filters/filterField/pager/planChip/detailList/payloadPre/decisionCell/decideActions/notice/msgOk/msgErr/section 等既有类，仍 86 token）；无迁移（头仍 0023，后端 418 测试/101 eval）；check-admin.mjs 扩展（41 键/五新文件/侧栏恰 5/eventLabels 恰 6 键/六码数组 JSON.stringify 精确相等+INTAKE_STATUS_FILTERS 恰 15/禁词扫描剥 // 注释行（白名单设计依据注释合法）/两页 force-dynamic·禁 NEXT_PUBLIC·URL·写词/actions 四状态码+unconfigured/missing_role/岛禁 @/lib/api+router.refresh/api getOpsIntakeQueue/adminTransitionIntake//api/intakes/ops-queue/ADMIN_ROLE_LIST/后端 router 五探针含 /ops-queue 注册位置先于 /{intake_id} 与 require_any_role(actor, OPERATIONS, PLATFORM_ADMIN)/service list_ops_intakes；三失败探针 codes 注 auto_confirm、岛注 @/lib/api、后端角色改单 PLATFORM_ADMIN 均实测 exit 1，git checkout 还原全绿）。E2E 机械教训：bash for 循环种子脚本中事件载荷变量名笔误（payload 引用未定义变量）会被后端 422/403 静默吞掉、单子停留 draft 且产品空间 404——多事件种子必须用显式 fire() helper 逐环放并断言状态推进。**Q108 管理端第六页·待办 SLA 看板（2026-09-16，跨栈加法无迁移）**：后端只改 `app/core/sla/router.py` 一文件——新增 TODO_STATUS_FILTERS 集合 {open,escalated,resolved,all} 与 require_sla_view query 依赖（actor_id Query(...)/roles Query(default_factory=list)，require_any_role(actor, PLATFORM_ADMIN)，PermissionDenied→403），GET /todos 挂 `_: Actor = Depends(require_sla_view)` 且函数首行白名单守卫 422 `unknown todo status: {status}`；**安全事实**：该端点 M10b 落地时是裸端点（Q75 补闸矩阵只收 POST /run），本切片起 platform_admin 红线；视图不分页 due_at ASC、字段集与 sla_state 派生（core/sla/policies.py）不动。前端新增 `app/[locale]/admin/sla-todos/page.tsx` 单文件（约 166 行纯只读 RSC）：force-dynamic；TODO_STATUS_FILTERS 四值 as const+DEFAULT_STATUS=open+KNOWN_ERROR_STATUSES=new Set([403,422])；helpers asString（searchParams 数组取值）/timeText（null→—，ISO slice(0,16).replace("T"," ")）/shortId（>8 截前 8，title 留全 id）/slaClassName（red→riskCritical/yellow→riskMedium/resolved→stateArchived/余 riskLow）；StatusFilters 为 GET 表单（select defaultValue=status，重置必须用 @/i18n/navigation 的 Link——首版误用硬编码 /zh-CN 字面路径提交前自查修正）；TodoTable 9 列，todo_id planChip、tenant/type/role/entity 原样码 metaLine（entity 显 `{entity_type}:{shortId}`）、status planChip、sla_state riskChip+slaClassName；main 三 translator（admin.slaTodos/admin/error），未配 CURRENT_ADMIN_ACTOR_ID→unconfigured、ApiError 403/422→中文 error 键、余 unknown；filters 区挂 yellowNote（黄仅法审 24h，其他类型黄口径【待补】如实声明不造）。lib/api.ts 增 SlaTodoView 接口（sla_state 四值字面量联合）与 getSlaTodos(status)（URLSearchParams actor_id+重复 roles+status，沿用 no-store request）；admin-sidebar 增第六项（恰 6）；消息增 admin.slaTodosNav + admin.slaTodos.* 21 键（枚举码零入值）；admin.module.css 零新增（全复用 table/tableWrap/filters/filterFields/filterField/primaryButton/secondaryButton/planChip/metaLine/riskChip 四 tone/notice/section/page/pageHeader/title/intro，仍 86 token）。无迁移（头仍 0023，后端 421 测试/101 eval）。check-admin.mjs 扩展（21 键含 nav/requiredFiles 加 sla-todos/page.tsx/侧栏第六项+恰 6 项/Q108 守卫块——页面 force-dynamic、禁 "use server"、剥 // 注释行后禁 NEXT_PUBLIC·http(s)·`/zh-CN/` 硬编码·method:·POST·PATCH·DELETE·`/sla/run`（写口不上页面），必含 getSlaTodos/CURRENT_ADMIN_ACTOR_ID/四状态双引号字面量/yellowNote/shortId/riskCritical·riskMedium·riskLow·stateArchived/sla_state；api 探针 getSlaTodos/SlaTodoView/`/api/admin/sla/todos`；后端 router 探针 def require_sla_view/require_any_role(actor, PLATFORM_ADMIN)/Depends(require_sla_view)/@router.get("/todos")/TODO_STATUS_FILTERS/`unknown todo status:`；两个失败探针——后端 Depends 改名 require_sla_view_probe、页面加 PROBE_RUN_PATH="/api/admin/sla/run" 常量均实测 exit 1，Edit 还原全绿）。机械事实：`ruff format --check` 在本仓对 116 个历史文件（含全部既有测试）同样报 would be reformatted——format 从非项目门槛，项目守 `ruff check`（All checks passed），本切片不引入 format 整改、不批量重排历史文件。E2E 教训：建表脚本必须 `import app.main` 注册全部模型元数据（单 import modeling.models 会因 c1_records→product_intake_applications 跨模块 FK 抛 NoReferencedTableError）；sessionmaker 必须 bind engine（首版模块级无 bind 工厂即写即错）。**Q109 管理面读端点鉴权审计 + 同型补闸（2026-09-16，纯后端加固无迁移）**：5 个路由文件机械补本地 query actor 依赖（不抽共享工厂，沿用 Q92/Q108 先例）——`product/modeling/router.py` 加 require_operations_view（OPERATIONS）挂 GET c1/signal-weights、GET c1/industries 与 require_dictionary_view（DICTIONARY_ADMIN）挂 GET /api/categories（注意无 /admin 前缀）；`product/fieldpool/router.py` 加 require_routes_view（OPERATIONS）挂 GET /admin/fp-source-routes；`platform/platform_adaptation/router.py` 加 require_fit_weights_view（OPERATIONS）挂 GET /admin/fit-weights；`core/compliance_wordlist/router.py` 加 require_wordlist_view（require_any_role(actor, OPERATIONS, INTERNAL_COMPLIANCE)，platform_admin 不在内）挂 GET ""；`core/model_registry/router.py` 加 require_models_view（PLATFORM_ADMIN）挂 GET /api/admin/ai-models 与 require_scene_routes_view（OPERATIONS, PLATFORM_ADMIN）挂 GET /api/admin/ai-scene-routes；均 actor_id=Query(...) 缺参 FastAPI 自动 422、重复 roles query、PermissionDenied→403，处理器签名末位 `_: Actor = Depends(...)`、函数体零改动，全部写口服务层闸一字不动。矩阵未定读角色的 config 三 GET、g2-candidates、skill-prompts 三 GET 不加闸（读角色【原文未给出，待补】），矩阵明文"GET 无 actor 体"的 agent-keys 与 ai-models/{id}/keys 不动。测试新建 `tests/integration/test_admin_read_rbac_api.py` 表驱动 23 条（8 端点缺参 422+customer 403、矩阵角色逐角色 200、词表 platform_admin/product_reviewer 403 而 internal_compliance 200、categories operations 403、4 个开放读口无参 200 锁定、未知 model keys 不返 403/422）；既有 4 个调用点补 query 参：test_c7_llm_resolve_api.py 的 _category_id 改 dictionary_admin（影响 10 用例）、test_model_gateway_api.py 三处（ai-models 两处 platform_admin、scene-routes operations）、test_platform_api.py fit-weights 一处 operations；全套 444 passed（421→444）、ruff check 净、前端六 checker+tsc 回归全绿（前端零消费方、零改动，token 仍 86）；无迁移（头仍 0023），feat 9269315/test 885574b。**Q110 审核工作量页空态缺键热修（2026-09-16，纯前端）**：review-workload/page.tsx 四个空表分支（CandidateTable/TodoTable/DecidedTable/ResolvedTable）调 `admin.workload` 命名空间 `t("empty")`，Q102 建页时 zh-CN.json workload 块漏收该键（checker 必检清单同步漏列），空库时 next-intl 原样回显 `admin.workload.empty`；顶层 admin.empty 不被跨命名空间回退。修复仅两处各 +1 行：messages/zh-CN.json workload 块补 `"empty": "暂无数据。"`（两快照表+两窗口产出表共用的通用文案）、check-admin.mjs workload 必检键清单补 "empty"（删键探针 exit 1 `missing message key: admin.workload.empty`）；全仓其余 10 处 t("empty") 逐一核键无同类缺漏；六 checker+tsc 全绿，后端零改动（444 测试/头仍 0023），fix 0b10ed5/test d4059a9。教训：带种子的逐片 E2E 走不到空态分支，空库回归能暴露必检键清单之外的漏键。**Q111 CI workflow（2026-09-16，纯工程基建）**：新增 `.github/workflows/ci.yml`（仓库首个 GitHub Actions，51 行）——`on: push(main/dev)+pull_request` 两 job：`backend`（actions/setup-python 3.12 + pip 缓存，`pip install -e "./backend[dev]"`，working-directory backend 跑 `ruff check .` 与 `python -m pytest -q`，仓库根跑 `python eval/runner.py`——runner `--root` 默认 REPO_ROOT/eval 且自插 backend 入 sys.path，cwd 无关；sqlite 测试无需 Postgres/Redis 服务容器，零 secret）；`frontend`（setup-node 20 + npm 缓存锁 frontend/package-lock.json，defaults.working-directory frontend，`npm ci` → `npm run typecheck` → 六 checker 串行）。不入流水线：ruff format --check（非门槛）、next build/lint（非既定闸门）、CD。新增 `backend/tests/unit/test_ci_workflow_contract.py`（46 行；Path 锚点 parents[3] 取仓库根，yaml.safe_load 解析，注意 YAML 1.1 布尔键 `on` 在 PyYAML 下为 Python True；断言触发器分支、backend 三命令、frontend typecheck 与六 checker 路径全在位）；全套 445 passed（444+1）、ruff 净，删 check-nav 行失败探针实测 FAILED 后还原；feat 44659b1/test 5f747bf，无迁移（头仍 0023）、前端代码零改动（token 仍 86）。

---

## 2. 可执行物落位（数据驱动，runtime/）

### 2.1 Workflow（WF-01~12）
- 落位：`runtime/workflows/WF-01.yaml ~ WF-12.yaml`（YAML 声明式，注册表演绎执行）
- 内容：所属段 / Skill 序列 / 人工 Gate 插槽 / 输入输出 Schema 引用（06 §1）
- 映射约束：段-WF 接缝按 Q72（段8→WF-06、段2→WF-01、段13→WF-11+12），**不新增 WF**

### 2.2 Skill（50+ 注册表）
- 落位：`runtime/skills/<skill_id>/`（每个 Skill：定义 yaml + prompt 模板 + 实现）
  > **Q82 修订（2026-09-14）**：真接 LLM 的 Prompt 不走本规划的 `prompt/*.md` 文件，改为 DB 版本化（`skill_prompts`/`skill_prompt_versions`，v0.1 起步，平台管理员发布 + writeAudit，仓库只携迁移种子）；definition 仍为 `skill.yaml`，`model_tier` V1 留空（模型选择由 ai_scene_routes 按 scene=skill_id 决定）。
- 注册表 11 字段（06 §4.1）：skill_id / layer / input-output_schema / 依赖知识 / 模型档位 / 阈值 / 失败策略 / 审核规则 / 成本上限 / 版本
- 实现约定：**Critical Skill 输出必走 skill7 候选通道**（ai_suggested→pending_review→…，line 11314）；历史 SkillRunLog 不可 mutate

> **实现补登（2026-09-14，M10 切片 e，Q76；WF-02 替换切片 Q78）**：首批 YAML 已落 `runtime/workflows/WF-04.yaml` 与 `runtime/skills/{PWC-BUILDER,COMBO-VALIDATE,PWC-SCORING}/skill.yaml`（按 06 §4.1 逐项列出的 10 个字段；原文"11 字段"计数差异不擅补）。加载器 `app/core/skill7/registry.py`（lru_cache，`LOOM_RUNTIME_DIR` 可覆盖，未注册引用即拒）；WF 定义 Gate 插槽的 role 供裁决服务读取。通道包 `app/core/skill7/`（models/schemas/service/router/adapters）已落 skill_runs/skill_candidates 两表与投递/裁决 API，WF-04 适配器复用 `pwc/funnel`。**WF-02 替换切片（Q78，2026-09-14）**：追落 `runtime/workflows/WF-02.yaml` 与 `runtime/skills/{FIELDPOOL-PLAN,DIM-SOURCE,DIM-MERGE}/skill.yaml`；DIM-MERGE 步骤 `produces_candidates: true, candidate_target: field_plan`（整方案单候选）；投递校验从硬编码 pwc_combo 泛化为 registry `candidate_target_for(wf,skill)` 按 WF 声明分派（pwc_combo→ComboItem / field_plan→PlanSubmitRequest 形态）；新增 `adapters.apply_field_plan` 复用 `fieldpool.submit_plan`（落 pending_gate 走既有 WF-02 Gate），router 补 ProductSpaceNotFound 404 / GateNotAllowed 409 / InvalidPlan 422 口径。**WF-01 替换切片（Q79，2026-09-14）**：追落 `runtime/workflows/WF-01.yaml`（PARSE→UNDERSTAND→CAT-RECOG→TYPE-MATCH→MISSING-INFO→RISK-TAG，CAT-RECOG 后单个 skill7 Gate role=operations）与六张 Skill YAML；CAT-RECOG 步骤 `produces_candidates: true, candidate_target: c1_recognition, single_candidate: true`；单候选约束由 Q78 硬编码泛化为步骤声明 `single_candidate`（WF-02 DIM-MERGE 同步加声明），registry 新增 `producer_step_for`；新增 `adapters.apply_c1_recognition` 复用 `modeling.submit_recognition`（Q1 三分支机械逻辑不改），router 补 IntakeNotFound 404 / RecognitionNotAllowed 409 / ConfigError·InvalidDecision 422 口径；双锚点（intake/PS 二选一，按 WF 归属校验）见迁移 0013。**WF-03 替换切片（Q80，2026-09-14）**：追落 `runtime/workflows/WF-03.yaml`（ATOM-EXPAND→ATOM-CANON→ATOM-AFFINITY→CONFLICT-PRECHECK 顺序四步）与四张 Skill YAML；仅末步 CONFLICT-PRECHECK 挂 `produces_candidates: true, candidate_target: atom_batch, single_candidate: true`（整批一条候选，投递即四步完成后的成型批次），一个 skill7 Gate 插槽 after=CONFLICT-PRECHECK role=product_reviewer（06 line 172）；投递/裁决 payload 按 BatchSubmitRequest 形态预校验（剥离 source/actor，适配器强制 source=ai）；新增 `adapters.apply_atom_batch` 复用 `atom.submit_batch`（Q14/Q15/去重/维度/line 11189/Q17/line 840/空证据全保留，applied_refs=batch_id），router 补 ProductSpaceNotFound·PoolNotFound 404 / PoolNotApproved·TargetReached·FactAtomConflict 409 / InvalidBatch 422 口径；无新迁移（PS 锚点+skill_candidates 通用表承载）。**WF-01 C7 Layer4 替换切片（Q81，2026-09-14）**：WF-01 增第二个候选产出步骤 TYPE-MATCH（`candidate_target: c7_layer4, single_candidate: true`）与第二道 skill7 Gate（after=TYPE-MATCH，role=operations；MISSING-INFO 仍非产出步骤），TYPE-MATCH/skill.yaml 输出改记整 C7 解析请求；投递 payload 按 C7ResolveRequest 形态预校验、intake 锚点集合扩 {c1_recognition,c7_layer4}；新增 `adapters.apply_c7_layer4` 薄包装复用 `modeling.resolve_c7`（L1→L4/Q6/Q68 不绕，落 L1/2/3 提案不生效，applied_refs=run_id，落 L4 的 g2_field_candidates 仍走段3 Q13 dictionary_admin），router 补 IntakeNotFound·CategoryNotFound 404 / IllegalFid·InvalidDecision 422；resolve_c7 Layer4 同步改同名全局候选复用（避免 uq(source_layer,field_name) 冲突）；无新迁移（intake 锚点+通用表承载，迁移 0013 已备）。C7 Layer4 通道挂账（Q79-1）销账，真 LLM 新字段生成本体原挂 Q67、已随 Q84 接通（见下）。**Q67/Q82 真 LLM 试点（2026-09-14）**：调用形态定稿为业务端点进程内同步调模型网关（非外部机器投递），Prompt DB 版本化不落地 `prompt/*.md`；CAT-RECOG 为首个真 LLM 生成站点，`POST /api/intakes/{id}/c1-recognition/llm-invoke`（operations）经网关调模型后以系统 Actor（roles=[]）复用同一投递通道落 pending_review；**PWC-BUILDER 第二站已随 Q83（2026-09-14）落 `POST /api/product-spaces/{id}/pwc/llm-build`（编排模块 `model_registry/pwc_build`，一次调用产多候选，模型回带分一律剥离）**；**C7 Layer4 第三站已随 Q84（2026-09-15）落 `POST /api/intakes/{id}/c7/llm-resolve`（编排模块 `model_registry/c7_resolve`，整结果单候选 c7_layer4，回显漂移/脏提案 502，模型回带分与 source_route 一律剥离）**；**WF-02 DIM-MERGE 第四站已随 Q85（2026-09-15）落 `POST /api/product-spaces/{id}/field-pools/llm-plan`（编排模块 `model_registry/field_plan`，无池/rejected 池态前置闸，整方案单候选 field_plan，target 回显漂移/role·route·fid 脏/证据空白 502，confidence 按 PT-FP-PLAN 契约 0..1 透传驱动 Q9/Q12，区别于 Q83/Q84 的模型分剥离）**；**第五站（末站）已随 Q86（2026-09-15）落 `POST /api/product-spaces/{id}/atom-batches/llm-expand`（编排模块 `model_registry/atom_expand`，CONFLICT-PRECHECK chat + ATOM-AFFINITY embedding 双调用单候选 atom_batch，模型自报亲和/成簇剥离本地按 1536 维向量余弦重算，ai_risk 契约透传，迁移 0018 为首个带 schema 的 LLM 切片，详见 02 C1.30）**；五个真 LLM 站点至此全部接通；**Q71 restock_auto 自动触发已随 Q87（2026-09-15）由 `app/core/restock/` 进程内 worker 闭环**（纯追加子 run 认领、仅 PWC-BUILDER、人工 Gate 不改，详见 02 C1.31）。

### 2.3 Agent（3 个）
- 落位：`runtime/agents/AG-REVIEW-COPILOT / AG-PM-AUDIT / AG-SUPPORT`
- 必带字段：max_turns + max_tokens + emergency_stop（line 2115）
- 边界：**不授予任何 owner/审批角色**（Q66）；AG-SUPPORT 上线前需脱敏审核

### 2.4 PT 协议
- 落位：`runtime/protocols/PT-<名称>-<版本>.md`（对齐 12 文档清单）
- 实现前状态：已定稿 7 个可编码；占位协议补齐后（12 §3 模板）方可编码

### 2.5 Evaluation Dataset + Golden Cases（M10-Q，14 §2.6 轻量版）

> **实现补登（2026-09-14，Q77；WF-02 切片 Q78 追补）**：`eval/runner.py` 独立回归 runner（不进 backend pytest 体系，对应 16 §1 回归测试行）+ `eval/targets.py` target 适配器注册表；数据集 YAML 落 `eval/datasets/skills/`（每 Skill 一份回归矩阵）与 `eval/golden_cases/skills/`（人工精选标杆，同 schema）。已覆盖：WF-04 有确定性替身的 PWC-SCORING（Q22 评分）与 COMBO-VALIDATE（Q48 词表匹配）24 案；**WF-02 替换切片（Q78）追补 DIM-MERGE 15 案（回归矩阵 12 + Golden 3），target=`fieldpool.planning.evaluate_plan`，覆盖 PT-FP-PLAN/Q8/Q9/Q10/Q11/Q12/line 14081；WF-01 替换切片（Q79）追补 CAT-RECOG 14 案（回归矩阵 11 + Golden 3），targets=`modeling.c1.weighted_conf/decide_branch`，覆盖 Q2 加权与 Q1/Q3 三分支/矛盾线/0.6 地板；**WF-03 替换切片（Q80）追补四 Skill 31 案（回归矩阵 24：ATOM-EXPAND 5/ATOM-CANON 4/ATOM-AFFINITY 7/CONFLICT-PRECHECK 8 + Golden 7），7 个 target（default_batch_size/target_reached/normalize_text/is_low_affinity/pick_cluster_keeper/grade_risk/conflicts_for），覆盖 Q14/Q15/Q16/Q17/Q19 与 line 840 三类冲突；合计 84 案全绿（66 数据集 + 18 标杆）；WF-01 C7 切片（Q81）追补 TYPE-MATCH 17 案（回归矩阵 14 + Golden 3），targets=`modeling.c7.coverage/pick_sibling/layer3_coverage_floor`，覆盖 Q6 覆盖率（空必填 1.0/0.6 边界/round 4）、L2 approved 兄弟继承与 0.6 地板；合计 101 案全绿（80 数据集 + 21 标杆）**；PWC-BUILDER 真 LLM 生成 Q83、TYPE-MATCH 真 LLM 生成 Q84、DIM-MERGE 真 LLM 生成 Q85、CONFLICT-PRECHECK/ATOM-AFFINITY 真 LLM 生成 Q86（含 synthetic embedding 哈希字袋夹具）同样不设 eval 案例（synthetic 构造器为测试夹具、非确定性业务函数，见 16 §4），eval 合计仍 101 案。运行：仓库根目录 `backend/.venv/bin/python eval/runner.py`，失败退出码非零（staging 闸门命令见 17 §1）。案例 schema、溯源与禁生产数据纪律见 `eval/README.md`。

---

## 3. 横切组件落位（backend/core/）

| 组件 | 内容 | 来源 |
|---|---|---|
| skill7 通道 | Candidate → Review/Confirm → Applied 通用管道（**M10 切片 e 已落**：skill_runs/skill_candidates + 投递/裁决 API + WF-04 pwc_combo 适配器，Q76；**WF-02 field_plan 适配器已落**：复用 fieldpool.submit_plan，Q78；**WF-01 c1_recognition 适配器+双锚点已落**：复用 modeling.submit_recognition，Q79，迁移 0013；**WF-03 atom_batch 适配器已落**：复用 atom.submit_batch（强制 source=ai，整批一条），Q80，无新迁移；**WF-01 c7_layer4 适配器已落**：复用 modeling.resolve_c7（整 C7 解析请求单候选，落 L1/2/3 提案不生效，L4 候选仍过 Q13），Q81，无新迁移） | line 11314 |
| writeAudit | append-only 审计（人工操作 + AI 调用） | D3.11 |
| RBAC | 8 角色 + 权威扩展（BO-07/字典管理员/平台级管理员/internal_compliance/internal_finance）；红线操作角色+Gate | 07 §2.3 |
| 配置中心 | 40+ 配置项 key/value/校验/出处 Q 编号/审计；权重校验器（Q2 Σ=1 / Q40 Σ≤1.0） | 02 §C2 |
| SLA 引擎 | 通用待办时限+升级（24h 黄/48h 升级，Q49） | 07 §2.5 |
| 模型注册表 | 统一 per-1M + 场景路由（Q67）。**Q82 已落 `app/core/model_registry/`**：models（ai_models/ai_model_keys/ai_scene_routes/skill_prompts/skill_prompt_versions 五表，迁移 0014）/ crypto（Fernet，env `LOOM_MASTER_KEY` 主密钥）/ gateway（注册/Key/路由/Prompt 红线服务 + invoke：路由→预算硬停→Prompt 渲染→驱动→per-1M 成本）/ drivers（synthetic 确定性替身 + OpenAI 兼容驱动，base_url 走 `LOOM_LLM_BASE_URL_<PROVIDER>`）/ synthetic（场景构造器）/ extraction（CAT-RECOG 试点编排）/ schemas/seeds/router（治理 + `/intakes/{id}/c1-recognition/llm-invoke` 端点）；skill_runs 增成本四列；真 LLM 产出以系统 Actor 复用 skill7 通道。**Q83 增第二站**：`pwc_build` 编排（第十模块）+ PWC-BUILDER synthetic 结构夹具/Prompt v0.1 种子（迁移 0015 纯数据）+ `/product-spaces/{id}/pwc/llm-build` 端点（一次调用多候选，模型回带 logic/fit/weight 一律剥离，跨候选保序去重）；**Q84 增第三站**：`c7_resolve` 编排（第十一模块）+ TYPE-MATCH synthetic 覆盖率缺口夹具/Prompt v0.1 种子（迁移 0016 纯数据）+ `/intakes/{id}/c7/llm-resolve` 端点（intake ai_recognizing + 类目 active 前置闸，整结果单候选 c7_layer4，回显漂移/脏提案 502，模型回带 confidence/score/fid/status/source_route 一律剥离）；**Q85 增第四站**：`field_plan` 编排（第十二模块）+ DIM-MERGE synthetic 合规整方案夹具/Prompt v0.1 种子（迁移 0017 纯数据）+ `/product-spaces/{id}/field-pools/llm-plan` 端点（无池/rejected 前置闸，整方案单候选 field_plan，target 回显漂移/结构白名单脏数据/line 14081 证据空白 502，confidence 0..1 契约透传而非剥离，业务违规如 below_min 照落非合规池由 WF-02 Gate 拦）；**Q86 增第五站（末站）**：`atom_expand` 编排（第十三模块）+ drivers/gateway `embed` 通道 + portable Vector TypeDecorator（PG vector(1536)/SQLite float32）+ CONFLICT-PRECHECK 结构夹具与 synthetic-embedding 哈希字袋夹具（迁移 0018 带 schema：pgvector 扩展/ai_models.capability/两表 embedding 列/atom.cluster_line 0.9 借 Q10 同义线原文未给）+ `/product-spaces/{id}/atom-batches/llm-expand` 端点（停拓预闸，chat+embedding 双调用合并单 SkillRun 单 atom_batch 候选，模型自报 affinity/cluster_id 剥离本地进程内余弦+同维并查重算，ai_risk 契约透传，approve 复制向量），五站全通、Q67 站点 backlog 闭合 | 04 §2.24 / 05 §2.3 切片 f 内 Q82–Q86 补登 / Q82 / Q83 / Q84 / Q85 / Q86 |
| 多副本单实例锁 | 进程内两个循环（SLA sweep/restock worker）多副本 leader 锁（**Q89**）：`app/core/locking/`，Redis SET NX PX+token+Lua+看门狗；两把循环级命名锁，非持锁跳轮；`LOOM_DISTRIBUTED_LOCK_ENABLED` 默认关/fail-closed；两个手工 /run 端点 409/503；无迁移 | 07 §7.5 / 05 §2.3 Q89 补登 / Q89 |
| M12 租户管理+Onboarding | D3.11①③（**Q95**）：`app/core/tenants/` 注册表四档 plan/三态 status、生命周期全审计、详情派生 Onboarding 进度；段1 准入（未知 404/暂停 409，应用层无 FK）；迁移 0023 建表+存量回填 | 05 §2 Q95 补登 / Q95 |
| M12 候选审核 SLA | Q70②（**Q94**）：`app/core/skill7/review_sla.py` 投递建单/裁决收口；五型 review_* todo_type、review.sla_hours.* 五键、Q49 升级映射；迁移 0022 配置种子 | 05 §2 Q94 补登 / Q94 |
| M12 统一审核工作台 | Q70 一期候选池（**Q93**）：`app/core/workbench/` risk+service+router；五型统一队列风险四档归一排序、置信度整批通过；WF Gate 角色并集/GET query actor/403·404·409·422；迁移 0021 配置种子 | 05 §2 Q93 补登 / Q93 |
| M12 驾驶舱只读聚合 | 两个管理面只读视图（**Q92**）：`app/core/dashboards/` service+router；天×模型×币种成本窗聚合、积压快照+窗口产出；platform_admin/GET query actor/422·403；纯 SELECT 无迁移不物化 | 05 §2 Q92 补登 / Q92 |
| 编排并行 DAG 原语 | 注册表驱动编排（**Q91**）：`app/core/orchestration/` planner+engine；needs 拓扑分层/同层并发/层间屏障/Gate 前驱闭包/fail-fast；env 信号量上限；不接真实流、无迁移 | 06 §4.2 Q91 补登 / Q91 |
| restock 退避游标 | restock 瞬态失败重试态（**Q90**）：`app/core/restock/models.py` 新表 restock_retry_state（迁移 0020）；指数退避 60→封顶 1800s，预算硬停永不转终态、上游错 10 次转终态；env 三参数运维值；手工 /run 绕窗口 | 05 §2.3 Q90 补登 / Q90 |
| 入站 Agent API Key | 一 Agent 一 Key 平台级入站凭证（Q60b 鉴权前置半套，**Q88**）：新横切包 `app/core/api_keys/`（models/service/schemas/router）；SHA-256 单向哈希（明文签发时返回一次）+ 展示前缀，吊销=状态位 append-only，不绑租户；治理 `/api/admin/agent-keys`（POST 签发/GET 列表/POST {id}/revoke，platform_admin 403/404/422），Bearer 验签依赖 require_agent_key（401）已备待段13 复用；审计 agent_api_key.issue/revoke；迁移 0019；**Q126 起已接入受保护端点 POST /api/effect-callback（见下「段13 效果回流」行）**；Q127 孤儿认领、Q128 客户回填、Q129 批量认领/解绑已落地，效果反哺校准随 V2 | 04 §2.24 / 05 §1.3 与 §2.3 Q88 补登 / 10 §2.8 / Q88 |
| 段13 效果回流（入站三片 + 操作面收口） | 全链唯一反向边数据入口（Q60，**Q126–Q131**）：横切包 `app/core/effects/`（models=`effect_records`+`effect_claims` 两表 / schemas=批入参·认领·批量·解绑·客户回填·回执·视图 / service=metrics 归一·记录校验·matched/orphan 分流·认领兜底·幂等 upsert·`_claim_one` / router=回调+认领+批量+解绑+客户回填+两只读口）。**Q126**：`POST /api/effect-callback` 复用 Q88 `require_agent_key` Bearer（首个受 Agent Key 保护业务端点，命中盖 last_used_at），整批 all-or-nothing、(external_content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏不补 0、按 content_id 精确匹配非 discarded 成品、审计 effect.batch_received（tenant `_platform`），迁移 0034 新建 effect_records（物理表 55→56）。**Q127（Q60a）**：`POST /api/admin/effects/claims`（operations 写口硬闸、actor 在体，客户/platform_admin 403，未知 record 404/非 orphan 409/未知目标 404/discarded 409），effect_claims 持久映射 external_content_id→content_id + effect_records 加 claimed_by/claimed_at 两溯源列（不新增第三状态），认领后同 ID 推送经映射兜底一律 matched 不回落孤儿、改绑重指历史 claimed 行、审计 effect.claimed，迁移 0035（物理表 56→57）。**Q128**：`POST /api/effects/backfill` 客户通道（无 Agent Key、body {tenant_id,records,actor}、source 服务端固定 customer-backfill、只命中本租户非 discarded 成品否则整批 422、绝不产生孤儿、审计 effect.customer_backfilled tenant=客户租户、received_by=客户 actor id），零迁移。**Q129**：`POST /api/admin/effects/claims/batch`（items≥1、批内重复 422、整批 all-or-nothing、回 {claimed,updated_rows}、逐条 effect.claimed 随事务）与 `/claims/unclaim`（映射不存在 404 ClaimMappingNotFound、删映射并把该外部 ID 下旧映射目标行全部回滚 orphan 含兜底新 matched 行、external_id 自匹配真自动行不动、审计 effect.claim_revoked、回 {external_content_id,reverted_rows}），service 抽 `_claim_one` 无角色闸、两口各自 operations 闸，零迁移，测试 607→618。只读 `GET /api/admin/effects/orphans` 与 `GET /api/admin/effects?content_id=`（operations\|platform_admin query actor 闸）。**Q130 前端**：`app/[locale]/admin/effects/` 五文件（page.tsx RSC force-dynamic 孤儿队列+时序两区 / actions.ts 四 Server Action / orphan-table-island.tsx 单条+勾选批量认领 / series-island.tsx content_id 查询+解绑 confirm / effect-fields.ts 七键与缺席显 “—” helpers），admin-sidebar 第 8 项 /admin/effects（check-admin 锁恰 8 项），lib/api.ts effects 段（adminQuery helper + 六函数），messages admin.effects.*。**Q131 前端**：`(shell)/content/backfill-island.tsx`（非 discarded 详情页挂载、单条手工表单、datetime-local 经 toISOString 转 UTC、空指标即缺席、六计数非负整数/read_rate 0..1 前端挡、成功 router.refresh；批量/CSV 随 V2）+ content/actions.ts backfillEffectAction（客户通道、CURRENT_TENANT_ID、422 object detail 解析），客户 nav 不增项仍 8；check-admin/check-content 各扩守卫，token 仍 86。Q54/Q57 反哺校准算法（口径【待补】）随 V2，本系统不抓取（Q62） | 04 §2.24 / 05 §1.1.1 Q126–Q131 补登 / 10 §2.8 / 11 §2.1 / 09 D3.6 与 D5 注记 / Q126–Q131 |
| 字典管理 | 目的/动作/17 池选项/通用底座（Q43/Q46） | Q25/Q38/Q43/Q46 |
| 合规词库 | Q48 单表，三关卡同源 | 04 §2.19 |
| 8 道闸门 | D7.1 实现为编排层横切校验插件（07 §7.6） | 09 §7.1 |

---

## 4. 运行与开发约定

- 配置：全部走配置中心（**禁止硬编码**，Q9）；环境变量只承载密钥/连接串
- 热更新：配置中心落库 + 变更广播，发布=新版本 + 原子切换 + writeAudit（14 §2.4）
- 数据库迁移：以 10 Schema 为源，Alembic 脚本与实体一一对应
- 异步队列：相撞/生成/批量审核走 Redis Streams（14 §2.3）
- 测试：按 16 测试策略分层落位（backend/tests 分 unit/integration/contract/e2e）；Skill 回归接 eval/ Evaluation Dataset
- 命名：模块/实体/Skill/Agent 名与 01/04/06 一致，不发明新名
- 浏览器人工验收通道（2026-09-14 起，本地约定【实现补】，非业务规格）：需在真实 Chrome 里点验页面时（M12 前端页、以后的端到端人工验收），先起带远程调试端口的独立 Chrome（`--remote-debugging-port=9222 --user-data-dir="$HOME/.chrome-claude"`，首次在该窗口登录验收环境），再由 Claude 经 chrome-devtools MCP（挂载模式 `--browserUrl=http://127.0.0.1:9222`，自身不开浏览器）驱动。本机已在 loom 项目以 `claude mcp add --scope local` 注册（存于 `~/.claude.json` 个人配置，**不写进仓库 `.mcp.json`、不提交**）；新开会话才加载。安全口径：9222 开放期间本机进程可驱动该 Chrome/读 Cookie，只登验收环境、用完关窗；约定源自 agent-world 仓库 docs/browser-verification.md。后端切片（M1–M6）验证仍以 pytest + httpx 为准，本通道不替代自动化测试。

---

## 5. 与既有文档的核对项

- [x] 目录结构与 07 §1 十三模块一一对应（product×6/platform×2/decision×2/final/content/feedback）
- [x] WF/Skill/Agent 命名与 06 一致（WF-01~12、50+ Skill、3 Agent）
- [x] 配置项均走配置中心，无硬编码（Q9）
- [x] 语言/框架已定（2026-09-13：Python 3.12 + FastAPI / React + Next.js + TS / YAML 声明式编排），见 §1
