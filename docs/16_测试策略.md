# 16. 测试策略

> **状态**：🟢 已补内容（分层测试 + 规则级测试清单说明）
> **来源**：09_全景 §7.1（8 道闸门）｜01_PRD §3（7 项 FCW Guard / approveAtomGuard 10 项 / pwsReadiness 5 项）｜13_状态机迁移表｜11_API规范｜02_决策记录（Q 规则）｜08_迭代计划（验收标准引用）
> **用途**：定义分层测试计划与**验收级测试映射**——每个 Guard、每条 Q 决策、每处状态迁移有对应测试要点；AI 落代码时边写边验证。
> **⚠ 说明**：本文档写**测试要点（做什么、验什么）**，不写测试代码；覆盖项全部可溯源到 Guard/Q/line。

---

## 1. 测试分层

| 层 | 范围 | 关键用例方向 |
|---|---|---|
| 单元测试 | 原子逻辑：评分公式、权重校验、阈值判定、配置校验器 | §3 规则级清单中的纯函数项 |
| 集成测试 | 段内 Skill 串联、状态机流转、Gate 插槽 | 13 迁移表 + 06 WF 顺序 |
| 契约测试 | effect-callback、白名单消费（对齐 11） | §2.3 |
| 端到端测试 | V1 主链链路（段 1→6→10→11，Q73 后段 12 移 V2）+ 段7/8 静态底表 | 08 §2.2 M1-M8/M11 验收标准 |
| 回归测试 | 40 个 PT 协议补齐后逐份回归；Skill 级 Evaluation Dataset + Golden Cases 回归（防 S2，M10-Q 已落独立 runner，见 §6） | Evaluation Dataset（09 D3.12）、`eval/`（Q77） |

> **浏览器人工验收通道（2026-09-14，本地约定【实现补】）**：端到端行的页面级人工验收（M12 前端页起）经 chrome-devtools MCP 挂载本机 9222 Chrome 执行（见 15 §4 运行与开发约定）；该通道为人工/半人工验收手段，不替代上表自动化分层，业务正确性仍以可重复的 unit/integration/contract/e2e 测试为准。

---

## 2. 规则级测试清单（验收标准映射）

### 2.1 8 道闸门（09 §7.1 → 编排层横切校验）

| # | 闸门 | 测试要点 |
|---|---|---|
| 1 | 输入 Schema 校验 | 缺产品名/目标语言/内容用途 → 阻断（对应 M1 验收，08） |
| 2 | 知识边界校验 | 产品层调平台规则改产品原子 → 拒绝；平台层改产品白名单 → 拒绝 |
| 3 | 输出 Schema 校验 | Skill 输出非固定 JSON → 失败 |
| 4 | 术语校验 | 输出含"语义相撞/固定五维/说明书拆解" → 拒绝 |
| 5 | 规则前置校验 | 不兼容/不合理/禁用表达 → 直接阻断 |
| 6 | 置信度阈值 | <0.85 进入人工确认（Q9 细看线） |
| 7 | 人工确认闸门 | 高风险类目/原子/规则/final_id 必须人工确认（Q70） |
| 8 | 回放与对比 | 模型输出与人工修改均记录（writeAudit 断言） |

### 2.2 7 项 FCW Guard（01 段11 + Q52/Q53）

| Guard | 测试要点 |
|---|---|
| ① PWS state=frozen | 非 frozen（pending/superseded/revoked）→ guards_passed=false，final_id 不生成 |
| ② compliance.block_required=false | block_required=true → 组装中止（一票否决） |
| ③ 所有 Package state=active | 任一包非 active → 失败 |
| ④ 6 路输入 product_space_id 一致 | 底座字典共享场景（Q52 配置实例归属） |
| ⑤ tenant_id 一致 | 跨租户组装 → 拒绝（Q33） |
| ⑥ law_review 已通过 | 法审未过 → 失败（Q49/Q53） |
| ⑦ PWS 为当前 active 版本 | superseded/revoked 引用 → 拒绝（Q53） |

### 2.3 状态机非法迁移（13 迁移表）

- 每台状态机一组：**终态接收事件 → 拒绝**；无权限角色执行红线迁移 → 拒绝（RBAC 断言）
- 重点：PWS revoked 后立即断消费（Q32）；PWC 冷却 14 天回待用（Q24）；atom 解冻不重审（Q20）

### 2.4 Q 决策关键规则

| Q | 规则 | 测试要点 |
|---|---|---|
| Q1/Q2 | conf 单指标 + 权重 Σ=1 | 权重和不=1 → 拒绝保存（校验器） |
| Q4 | 待确认 72h 未处理升级 | 超时任务升级断言 |
| Q12 | 维度 3-8 越界 | >8 留 Top8 入备选；<3 转人工 |
| Q18 | evidence 7 天超时 | 自动驳回 + 可复活 |
| Q22/Q54 | 评分公式 | (0.6+0.4)×乘数；0.4/0.3/0.3 排序不设门槛 |
| Q24 | PWC 复用边界 | 跨产品/跨客户复用 → 拒绝；同平台+账号+发布位不重复 |
| Q27 | 库容 100 | 待用池上限 + 跌破 50 自动补货（Q71 联动） |
| Q33 | 租户隔离 | 跨租户同款产品互不感知、永不驳回 |
| Q40 | Σ≤1.0 | 权重池保存/重算入库前校验 |
| Q50 | 合规优先序 | 国家 > 平台 > 底座默认（**不可配置**） |
| Q51 | 词表生效即扫 | 词表更新 → 全量扫描 active 快照/草稿/未发布，联动重冻 |
| Q56 | 重生成 ≤3 次 | 3 次仍 <0.85 → 转人工/作废 |
| Q60 | effect-callback | 缺席字段"—"不当 0；幂等覆盖；孤儿队列 |
| Q61 | 爆款判定 | 中位数 5 倍 + 绝对门槛，参数配置页 |
| Q63 | KUP 证据三关 | 30 条/0.5%/2 期全过才可提案；可回滚 |
| Q66/Q67 | AI 治理 + 模型表 | Agent 无审批权限；per-1M 统一口径 |

---

## 3. 契约测试要点（对齐 11）

- effect-callback：请求体字段缺失 → 400；未授权 Key → 401；同 content_id+captured_at 重复推送 → 幂等覆盖 200；孤儿 content_id → 进孤儿队列
- 白名单消费：按评分降序取；命中写 usage_record；跌破 critical(50) 触发补货（5min 防抖）

---

## 4. 质量门槛与数据（建议）

- 覆盖率目标：核心规则层（评分/权重/Guard/状态机）≥ 90%；全系统 ≥ 70%【建议】
- 测试数据：每租户独立 fixture；跨租户断言不可见（Q33）
- Mock 策略：外部 Agent 推送（effect-callback）mock；平台数据（catFeedback）用样例集；**禁止用生产数据**

---

## 5. 与既有文档的核对项

- [x] 每条测试要点可溯源到 Guard / Q 编号 / line（§2 各表来源列）
- [x] 测试用例未发明原文没有的规则（全部引用 01/02/05/09/11/13）
- [ ] 契约测试与 11 同步更新；PT 协议补齐后（12）追加回归用例

---

## 6. Evaluation 回归（M10-Q 实现补登，Q77）

- 落位：`eval/`（15 目录规划，独立于 backend pytest；schema 与边界见 `eval/README.md`）。
- 运行：仓库根目录 `backend/.venv/bin/python eval/runner.py`；staging 闸门调用与失败阻断语义见 17 §1。
- V1 覆盖：PWC-SCORING（Q22/Q22a/Q22b）与 COMBO-VALIDATE（Q48）的确定性替身 24 案（Evaluation Dataset 19 + Golden Cases 5）；WF-02 替换切片（Q78）追补 DIM-MERGE 15 案（evaluate_plan：PT-FP-PLAN/Q8/Q9/Q10/Q11/Q12/line 14081；Dataset 12 + Golden 3）；WF-01 替换切片（Q79）追补 CAT-RECOG 14 案（weighted_conf：Q2 加权；decide_branch：Q1 三分支/Q3 矛盾线/0.6 地板边界；Dataset 11 + Golden 3）；WF-03 替换切片（Q80）追补四 Skill 31 案（ATOM-EXPAND Q14/Q15 批次 5、ATOM-CANON 规范化 4、ATOM-AFFINITY Q16/Q19 亲和与 keeper 7、CONFLICT-PRECHECK Q17 双轨/line 840 冲突 8 = Dataset 24 + Golden 7；targets=default_batch_size/target_reached/normalize_text/is_low_affinity/pick_cluster_keeper/grade_risk/conflicts_for），合计 84 案（Dataset 66 + Golden 18）；WF-01 C7 Layer4 替换切片（Q81）追补 TYPE-MATCH 17 案（c7_coverage：Q6 覆盖率/空必填 1.0/0.6 地板边界/round 4 共 8；c7_pick_sibling：approved 兄弟最大 product_count/draft 不可继承/无 approved→None/平局保序/缺省计数共 5；c7_layer3_floor：拍板 0.6 共 1 = Dataset 14 + Golden 3；Q68 fid:'-' 与错误分支无 eval 错误用例 schema，留后端集成测试），合计 101 案（Dataset 80 + Golden 21）；**Q67/Q82 模型网关与 CAT-RECOG 真 LLM 试点（2026-09-14）不新增 eval 案例、合计仍 101 案**——网关试点不引入确定性纯业务函数（synthetic 替身为测试夹具而非业务函数），按 §4 由后端集成测试覆盖（见下集成层补登）；**Q83 PWC-BUILDER 第二站（2026-09-14）同口径不新增 eval、合计仍 101 案**（PWC-BUILDER synthetic 构造器同样是测试夹具）；**Q84 TYPE-MATCH 第三站（2026-09-15）同口径不新增 eval、合计仍 101 案**（TYPE-MATCH synthetic 构造器同样是测试夹具）；**Q85 DIM-MERGE 第四站（2026-09-15）同口径不新增 eval、合计仍 101 案**（DIM-MERGE synthetic 构造器同样是测试夹具）；**Q86 CONFLICT-PRECHECK+ATOM-AFFINITY 第五站（2026-09-15）同口径不新增 eval、合计仍 101 案**（CONFLICT-PRECHECK synthetic 结构构造器与 synthetic-embedding 哈希字袋向量构造器均为测试夹具；相似度纯函数 cosine_similarity/max_affinity/assign_clusters 走后端单测而非 eval runner）；**Q87 restock_auto worker（2026-09-15）同口径不新增 eval、合计仍 101 案**（worker 为调度/装配逻辑、无确定性纯业务函数，由集成测试覆盖）；**Q88 入站 Agent API Key 凭证半套（2026-09-15）同口径不新增 eval、合计仍 101 案**（Key 生成/哈希/Bearer 解析为薄工具函数，治理与验签由集成测试覆盖；无业务纯函数）；**Q89 多副本单实例锁（2026-09-15）同口径不新增 eval、合计仍 101 案**（分布式锁为基础设施、无业务纯函数，Redis 原语以内存 FakeRedis 走集成测试覆盖）；**Q90 restock 瞬态退避（2026-09-15）同口径不新增 eval、合计仍 101 案**（退避序列纯函数 `_backoff_delay_seconds` 走后端单测，游标 upsert/升级分流为 DB IO 走集成测试）；**Q91 编排 DAG 原语（2026-09-15）同口径不新增 eval、合计仍 101 案**（拓扑规划属注册表机械规则、执行为 asyncio 调度基础设施，同 Q89/Q90 由后端单测覆盖）；**Q92 两个驾驶舱只读聚合（2026-09-15）同口径不新增 eval、合计仍 101 案**（纯只读 SQL 聚合与 HTTP RBAC，无确定性业务纯函数，由后端集成测试覆盖）；**Q93 统一审核工作台（2026-09-15）同口径不新增 eval、合计仍 101 案**（风险四档归一为机械映射但经行业表/run join 与真实适配器，由后端集成测试端到端覆盖）；**Q94 候选审核 SLA 挂接（2026-09-16）同口径不新增 eval、合计仍 101 案**（待办建/收口挂在投递/裁决写路径、时限为配置值，由后端集成测试端到端覆盖）；**Q95 租户管理+Onboarding（2026-09-16）同口径不新增 eval、合计仍 101 案**（注册表/生命周期/段1 准入为管理与装配逻辑、无确定性纯业务函数，由后端集成测试端到端覆盖）；五个真 LLM 站点全部接通，Simulation Sandbox 后置 V2；**Q96–Q111 客户前端/管理端切片与 CI（2026-09-16）同口径不新增 eval、合计仍 101 案**（前端页面与 CSV/驾驶舱/工作台/租户/运营队列/SLA 看板等 CRUD 只读聚合无确定性纯业务函数，由后端集成测试与前端零依赖 checker 覆盖；CI workflow 契约走 tests/unit/test_ci_workflow_contract.py）；**Q112–Q120（2026-09-17/18，契约回填/读口补闸/settings·SLA 占位/G2 fid 种子/段12 文章生成/一致性审计/挂账收口/段12 多语言/段12 AI 质量分）同口径不新增 eval、合计仍 101 案**（段12 六态机纯函数走 tests/unit/test_content_statemachine.py、**多语言交集纯函数 covers_market/eligible_codes 走新增 tests/unit/test_content_l10n.py**、**Q120 质量分 parse_qc_output/quality_advisory_for 纯函数走新增 tests/unit/test_content_quality.py**，语言 CRUD/市场∩产品交集/每语言独立成品/ARTICLE-QC 内嵌质检端到端走新增集成测试，QC 的 synthetic 构造器为测试夹具，均为后端 pytest 而非 eval runner；RBAC 补闸/外键/CI 接线为加固与基建无确定性业务函数；纯文档与纯种子切片无测试变化）；**Q121–Q125（2026-09-19，语义复检/客户内容页/目标语言控件/作废回池/发布回填）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——七态状态机/队列过滤排序/RBAC 边界走 pytest 单测与集成、管理端页面走前端零依赖 checker，均无确定性纯业务函数进 eval runner；synthetic 语义/质检构造器为测试夹具；**Q126 段13 effect-callback 入站第一片（2026-09-19）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——metrics 归一/记录校验为入站 HTTP 契约的薄校验纯函数（同 Q88 Key 工具口径，无既有 eval schema），幂等时序与 matched/orphan 分流为 DB IO，均由新增 pytest 单测与集成覆盖。**Q127 孤儿认领与 Q128 客户回填（2026-09-19）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——认领映射/租户隔离为 DB IO 与 HTTP RBAC、客户回填复用 Q126 同一批纯函数，均由新增 pytest 集成覆盖。**Q129 批量认领/解绑、Q130 管理端认领台、Q131 客户回填 UI（2026-09-20）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——批量/解绑为事务边界与 HTTP RBAC（pytest 集成覆盖），前端两岛/页为 RSC+Server Action+client 岛装配（零依赖 checker 覆盖，无确定性纯业务函数进 eval runner）。**Q132 中台导出 JSON 形态 + 异步导出任务（2026-09-20）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——JSON envelope/CSV 渲染为薄序列化纯函数（走新增 tests/unit/test_export_render.py 5 例），任务落表与重渲染下载为 DB IO/HTTP 契约（pytest 集成覆盖），无既有 eval schema。**Q133 fencing token / Q134 Redis Streams 原语 / Q135 配置缓存广播（2026-09-20）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——三件均为多副本基础设施（单调令牌/Redis 消费组原语/pub-sub 失效），无确定性业务纯函数进 eval runner，Redis 原语分别以内存 FakeRedis/FakeStreamsRedis/FakeBroadcastRedis 走新增 pytest 集成覆盖，fence 单调与投递计数/死信阈值等纯函数并入集成断言。**Q136 客户批量 CSV 回填（2026-09-20）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——纯前端片：CSV 行解析/行级校验/时间转 UTC 为浏览器内装配逻辑（无确定性纯业务函数进 eval runner），整批复用 Q128 后端通道（后端零改动），由 check-content.mjs 扩守卫锁定。**Q137 导出真后台 worker / Q138 restock 信号入流 / Q139 leader_lease 协作中止 / Q140 配置缓存单 key 增量失效与断线退避（2026-09-20）同口径不新增 eval、合计仍 101 案（Dataset 80 + Golden 21）**——四件均为多副本异步队列/锁/缓存基础设施与 DB IO·HTTP 装配，无确定性业务纯函数进 eval runner；Streams/锁/广播分别以内存 FakeStreamsRedis/FakeRedis/FakeBroadcastRedis 走新增 pytest 集成覆盖（消费组认领与死信、fencing 协作中止、单 key 失效与指数退避）。
- 纪律：案例人工依据 docs/Q 编写并带 source.refs；禁止生产数据（§4），词例合成。
- **集成层补登（Q82，2026-09-14）**：CAT-RECOG 真 LLM 试点链路由 backend 集成测试以确定性 synthetic 驱动覆盖（禁真凭证/真供应商，§4）：模型/Key/路由/Prompt 治理端点 RBAC 与校验、Key 轮换不回显明文/密文、llm-invoke 全链路（状态闸→预算硬停→Prompt 渲染→投递 pending_review→operations 裁决照走 Q1 分支）、disabled→人工 fallback、预算耗尽 409 不静默切供应商、远程模型缺 Key 422、驱动坏输出 502；纯函数单测覆盖 Key 加解密与 per-1M 成本 ROUND_HALF_UP。后端测试合计 253 全绿。
- **集成层补登（Q83，2026-09-14）**：PWC-BUILDER 真 LLM 第二站同样以确定性 synthetic 驱动覆盖（禁真凭证/真供应商，§4）：llm-build 全链路（RBAC operations/客户 403、PS 不存在 404、字段池未 approved 409、approved 原子不足两维 409 且不调模型、预算耗尽路由切换 409、远程模型缺 Key 422、驱动坏输出/幽灵原子 502、happy path 多候选投递→product_reviewer 逐条裁决→漏斗落 pending_gate/score_incomplete→PWC Gate 到 ready）；模型回带 logic_score/fit_score/weight 一律剥离有专测；synthetic 结构夹具（锚定配对/至多 5 条/不造分/单维空批）走纯函数单测。后端测试合计 263 全绿（+8 集成 +2 单测）。
- **集成层补登（Q84，2026-09-15）**：TYPE-MATCH 真 LLM 第三站同样以确定性 synthetic 驱动覆盖（禁真凭证/真供应商，§4）：llm-resolve 全链路（RBAC operations/reviewer/客户 403、intake 不存在 404、类目不存在 404、非 ai_recognizing 状态 409、draft 类目 409、required_fids 含 "-" 调模型前 422、disabled 无 fallback 409→注册 fallback 成功、预算耗尽 409、远程模型缺 Key 422、驱动坏输出 502、category_id 回显不符 502、required_fids 回显漂移 502、脏提案 fid "-"/缺 field_name 502、happy path 单候选 c7_layer4 投递→operations 裁决→resolve_c7 落 L4 五条 g2_field_candidates pending_gate→Q13 转正 Gate 不绕、全覆盖→空提案合法裁决后落 L3）；模型回带 confidence/score/fid/status/source_route/空 definition 一律剥离有专测（confidence 落 NULL）；synthetic 覆盖率缺口夹具（只按缺口造"待补字段·fid"、不打置信分、全覆盖空批、确定性）走纯函数单测。后端测试合计 276 全绿（+11 集成 +2 单测）。
- **集成层补登（Q85，2026-09-15）**：DIM-MERGE 真 LLM 第四站同样以确定性 synthetic 驱动覆盖（禁真凭证/真供应商，§4）：llm-plan 全链路（RBAC operations/reviewer/客户 403、PS 不存在 404、已有非 rejected 池 409 且不调模型、rejected 池允许重新触发 201、target min>max 前置 409、disabled 无 fallback 409→注册 fallback 成功→预算耗尽 409、远程模型缺 Key 422、驱动坏输出 502、target_atom_min/max 回显漂移 502、脏方案十形 502——role 非法/路由禁用/幽灵 fid/fid "-"/confidence 越界与非数/source_ref 空白/字段名重复/超 dim_max/空批次、happy path 单候选 field_plan 投递→product_reviewer 裁决→apply_field_plan/submit_plan 落 pending_gate 合规池→WF-02 Gate approve）；confidence 契约透传有专测（0.5 落库后 Q9 needs_detail=true，区别于 Q83/Q84 剥离；未知键剥离）；2 维 below_min 业务违规照落非合规池 violations=["below_min"]、Gate approve 409；敏感行业补 risk_control 维；synthetic 合规整方案夹具（G2 复用+资料键补维+无真实 ref 不伪造占位+固定 0.9 不模拟分布+确定性）走纯函数单测。后端测试合计 289 全绿（+11 集成 +2 单测）。
- **集成层补登（Q86，2026-09-15）**：CONFLICT-PRECHECK chat + ATOM-AFFINITY embedding 第五站（末站）以确定性 synthetic 双驱动覆盖（禁真凭证/真供应商，§4）：llm-expand 全链路（RBAC operations 放行、reviewer/客户 403、PS 不存在 404、字段池缺失 404、gate≠approved 409、Q15 停拓仅 AI 批次 409 且手动端点不受限、disabled 无 fallback 409→注册 chat fallback 成功→路由远程模型缺 Key 422、embedding 模型预算耗尽 409 不留 run、embedding 路由到 chat 能力模型能力不符 422、embedding 远程缺 Key 422、驱动坏输出/向量行数不符 502 不留 run/候选、回显漂移/空批/超批/空白 content/未选维度/枚举越界/批内 normalized 重复八类结构脏 502、happy path 单候选 atom_batch 投递→product_reviewer 裁决→apply_atom_batch/submit_batch 落 pending_review/pending_evidence→逐条 approve 且 embedding 复制到 product_atom_instances）；模型自报 affinity/cluster_id/score 剥离本地重算有专测（run.output 仅白名单五键、伪 cluster 不入库）；ai_risk 枚举校验透传 + 词表命中强制只升不降（high downgrade 词 + high_risk_single_review 冲突）有专测；无向量基（首批/Q86 前历史 NULL 向量原子）affinity=None 不伪造 0；第二批对已 approve 向量算亲和（>0.5、approved_embedding_count 计数）；批内同维共享前缀对过 0.9 成簇线、跨维不成簇有单测/集成覆盖。后端测试合计 306 全绿（+10 集成 +7 单测：4 相似度纯函数 + 3 gateway/构造器）。
- **集成层补登（Q87，2026-09-15）**：M8 restock_auto worker 以合成夹具覆盖（`tests/integration/test_restock_worker_api.py`，无迁移、无真凭证）：成功交付（子 llm_auto run 回链 restock_request_id、候选挂子 run 且全 pending_review、requested 信号行原样不动）与第二轮反连接幂等（claimed=0）；非 restock_auto/他 skill 信号忽略；四类终态（approved 原子跨维 <2、池未 approved、停用无 fallback、模型脏输出）追加 failed/llm_auto 子 run + restock_failed 审计且不重试；预算硬停瞬态（BudgetExhausted）不留子 run、记 restock_deferred、下轮仍可认领；`POST /api/admin/restock/run` RBAC（customer/ops 403、platform_admin 200 触发）；RestockWorker start/stop 生命周期。后端测试合计 315 全绿（+9 集成，无新增单测——worker 逻辑为调度/分类装配，无纯函数）。
- **集成层补登（Q88，2026-09-15）**：入站 Agent API Key 凭证治理与验签由 `tests/integration/test_agent_api_keys.py` 覆盖（in-memory SQLite create_all，迁移 0019 另由 PG16 一次性容器实测；无真凭证）：Key 形态（loom_ 前缀、20 枚唯一、哈希=SHA-256 64 hex）、parse_bearer 6 形参数化、签发只存哈希不存明文（前缀不含明文、created_by）、签发 RBAC（ops/customer PermissionDenied）、空白名 ValueError、验签 active 盖 last_used_at/未知/空串/已吊销统一 None、吊销 append-only（单行翻状态+revoked_by/at）且审计动作集合恰为 {agent_api_key.issue, agent_api_key.revoke}、吊销 RBAC 与 AgentKeyNotFound、列表默认隐去吊销/include_revoked 可见；HTTP：201 secret 一次性后 GET 列表无 secret 且第二次签发 secret 不同、403×2/422、吊销 403→200/404/列表旗标。后端测试合计 332 全绿（+17 集成；Q87→315、Q88→332），eval 仍 101/101 不新增。
- **集成层补登（Q89，2026-09-15）**：多副本单实例锁由 `tests/integration/test_distributed_locks.py` 覆盖（无真 Redis——内存 FakeRedis 实现 set(NX PX)/eval 释放·续约两条 Lua 原语）：门控默认关时嵌套 `leader_lock` 直接放行不触 Redis；开启后 NX 互斥（第二持有者 LockUnavailable）、退出释放后可重入；持锁期间 key 被异 token 覆盖时释放不删别人的锁；TTL=0.06s 时看门狗 ≥1 次续约且 token 不变；锁中途过期（续约返回 0）静默不抛；set 抛 RedisError → LockBackendError fail-closed；SweepScheduler/RestockWorker 的 `_tick()` 在锁空时各跑一次、锁被占时计数不增、Redis 宕时不抛不跑；HTTP：两端点锁空 200→被占 409、Redis 宕两端点均 503。后端测试合计 345 全绿（+13 集成；Q88→332、Q89→345；无新增单测——锁原语为 IO 边界无纯函数），eval 仍 101/101 不新增。
- **集成层补登（Q90，2026-09-15）**：restock 瞬态退避游标由既有 `tests/integration/test_restock_worker_api.py` 扩 6 例覆盖（in-memory SQLite create_all，迁移 0020 另由 PG16 一次性容器实测；无真凭证/无真 Redis）：退避纯函数序列 60/120/240/480/960/封顶 1800；窗口内定时轮 claimed=0、游标 attempts=1/next_at 在未来、手动 honor_backoff=False 绕过且 attempts=2；窗口拨到过去后重新认领 attempts 累加；BudgetExhausted 连续 11 次手动强试仍只 defer（无子 run、无 restock_failed、attempts=11，验证永不转终态）；DriverError（→GenerationUpstreamError）前 9 次 defer、第 10 次转终态 failed/llm_auto 子 run + restock_failed(terminal, attempts=10) + 删游标 + 反连接排除；成功后删游标；既有游标存在时撞终态失败也删游标。Q87 预算瞬态旧用例按退避新行为更新。后端测试合计 351 全绿（345→351，+6；无新单测文件——退避纯函数断言并入同文件），eval 仍 101/101 不新增。
- **单元层补登（Q91，2026-09-15）**：新文件 `tests/unit/test_orchestration_dag.py` 20 例（临时 LOOM_RUNTIME_DIR 构造 WF/Skill YAML + 合成 async 处理器，无 DB/Redis）——规划侧：线性退化、显式 fan-out/fan-in 分层、未知 needs/依赖环/重复步骤/未注册引用/gate 锚点五类拒绝、非 skill7 gate 不截断、Gate 边界含并行分支全部前驱、仓库 WF-01..04 规划快照参数化；执行侧：前驱结果透传、信号量并发上限实测=2、层间屏障时序、fail-fast 取消兄弟且后层不执行（NodeExecutionError 携带失败节点与 completed 快照）、缺处理器 HandlerMissing、orch_run_id 注入/自动生成。后端测试合计 371 全绿（351→371，+20 单测），eval 仍 101/101 不新增。
- **集成层补登（Q92，2026-09-15）**：新文件 `tests/integration/test_dashboards_api.py` 7 例（httpx ASGI + 内存 SQLite 直插 skill_runs/skill_candidates/ops_todos，只读不写）——RBAC（无 actor 422、operations 403、非管理员 403）、默认 30 天窗按天×模型×技能聚合（requested/failed/窗外行排除、零成本 embedding 计 token 不计钱）、多币种分组不换算、坏窗与坏日期 422、候选+待办积压快照（最老等待秒数/open·overdue·escalated 拆分/totals）与窗内已裁已决产出、空库零值空列表。后端测试合计 378 全绿（371→378，+7 集成），eval 仍 101/101 不新增。
- **集成层补登（Q93，2026-09-15）**：新文件 `tests/integration/test_review_workbench_api.py` 6 例（httpx ASGI + 内存 SQLite，直插 SkillRun/SkillCandidate/C1IndustryThreshold 覆盖只读队列，真实投递+已批准字段池覆盖批量适配器）——RBAC（无 actor 422、dictionary_admin 403、platform_admin 403、operations/product_reviewer 各自 200）、风险排序（critical 敏感行业→high 原子批→low 档最老优先）与 batch_eligible 旗标（高风险/低置信/置信缺失）、target_type/wf_id/state/risk_level 筛选与分页、批量预检 high/critical 与低置信 422 且候选零写入、混 WF 角色双向 403+不存在 404+非 pending 409、两条低风险高置信 atom_batch 整批通过落 4 条 AtomCandidate 且 OPS 403。后端测试合计 384 全绿（378→384，+6 集成；迁移 0021 随 create_all 种子回落验证，PG16 容器实测随部署片），eval 仍 101/101 不新增。
- **集成层补登（Q95，2026-09-16）**：新文件 `tests/integration/test_tenants_api.py` 10 例（httpx ASGI + 内存 SQLite）——管理端 RBAC（写端点非 platform_admin 403、GET query actor 缺 actor_id 422/角色不符 403）、试用开通默认 trial/trial+月额度 50 万、付费档开通 active/额度 null、重复开通 409/未知 plan 422/未知租户 404、暂停后 POST /api/intakes 409 且恢复后 201+三类生命周期审计、改套餐只动 plan 不动 status 与额度切换、未知租户录入零写入（无单无审计）、详情派生 onboarding 计数、注册表列表、0023 口径回填行（detail.backfilled）可正常准入；既有 7 个经 HTTP 建单的测试文件（intake/c1/c7_llm_resolve/model_gateway/skill7_c7_layer4/skill7_review_sla/skill7_wf01）夹具补播 t1=basic/active 租户。后端测试合计 401 全绿（391→401，+10 集成；迁移 0023 回落与回填 PG16 容器实测随部署片），eval 仍 101/101 不新增。
- **集成层补登（Q94，2026-09-16）**：新文件 `tests/integration/test_skill7_review_sla_api.py` 7 例（httpx ASGI + 内存 SQLite）——投递即建待办（todo_type=review_{target_type}、assignee=WF Gate 角色、due≈+72h、detail 链接 run/candidate_index）、PWC 一次两候选两条待办、reject 收口 resolution=archived、approved 字段池下 confirmed 落原子并收口 resolution=applied、置过 due 跑 Q49 引擎验证 escalated+`skill7.review_sla_escalated` 审计且升级后裁决仍可收口、工作台批量通过同步 resolved 两条、驾驶舱积压自动出现 review_atom_batch 的 overdue 分组；既有 `test_skill7_wf01_api.py` 两例 OpsTodo 总数断言改为按 ops_assist_category 类型计数（审核待办为 Q94 新行为，行保留）。后端测试合计 391 全绿（384→391，+7 集成；迁移 0022 随种子回落验证，PG16 容器实测随部署片），eval 仍 101/101 不新增。
- **前端切片补登（Q96，2026-09-16）**：next-intl v4 脚手架与 tokens.css 三层 85 变量落地，新增首个零依赖前端静态闸门 `frontend/scripts/check-tokens.mjs`（npm run check-tokens，逐值校验 tokens.css↔tokens.ts 镜像一致，删/改变量即挂）；typecheck/build、无前缀 307 与 Chrome 可视验收覆盖。后端零改动、测试合计 401 不变，eval 仍 101/101。
- **前端切片补登（Q97，2026-09-16）**：(shell) 240px 侧栏+八菜单导航骨架落地，新增 `scripts/check-nav.mjs` 三方漂移校验（nav 注册表↔路由↔消息键，恰 8 菜单、段8/12/13 V2 徽标占位、/[locale] 307）；tokens.css 85→86（新增 --color-bg-hover）；两级 307/8×200 与 Chrome 验收。后端零改动、测试合计 401 不变，eval 仍 101/101。
- **集成层补登（Q98，2026-09-16）**：段1 新增租户分页只读 `GET /api/intakes`（IntakeList；未知/暂停租户空列表 200 不触发 Q95 准入门、分页参数 422、注册序先于 /{intake_id}）由 `tests/integration/test_intake_api.py` 扩 3 例覆盖；前端产品中心页配套新增 `scripts/check-products.mjs`（消息键/裸状态码/禁直连守卫，失败探针实测）+Chrome 端到端（含 CJK 新建 draft 闭环）。后端测试合计 404 全绿（401→404，+3 集成），eval 仍 101/101。
- **集成层补登（Q99，2026-09-16）**：段1 新增租户状态聚合只读 `GET /api/intakes/overview`（IntakeOverview；15 态分组计数、仅渲染计数>0、未知/暂停租户零值）由 test_intake_api.py 扩 3 例覆盖；D5 原文五指标（今日生成/发布/互动/趋势/健康度，数据源段12/13）渲染禁用态 V2 卡、不显任何数字（含 0）有断言；新增 `scripts/check-workbench.mjs`（含跨栈路由顺序守卫）、check-products 增裸状态码守卫；Chrome 端到端（真实分组/五卡/ghost 空态/零直连）。后端测试合计 407 全绿（404→407，+3 集成），eval 仍 101/101。
- **集成层补登（Q100，2026-09-16）**：新横切包 `app/core/exports/` 挂 `GET /api/exports/fcw.csv`，新文件 `tests/integration/test_exports_api.py` 3 例（httpx ASGI+内存 SQLite）——final_id 单列含表头同步流式、tenant_id 必填（空 422）/product_space_id 可选收窄、只导 published（draft 排除）、created_at DESC、未知租户 200 仅表头空文件、读路径不触发准入门不 writeAudit、Content-Disposition filename=fcw-\<tenant\>.csv；curl 一次性 SQLite 端到端复核。后端测试合计 410 全绿（407→410，+3 集成），eval 仍 101/101。
- **集成层补登（Q101，2026-09-16）**：段10 新增租户只读聚合 `GET /api/compliance/overview`，新文件 `tests/integration/test_compliance_overview_api.py` 4 例——仅 active frozen PWS 一行一产品、各市场 append-only CCR 取最新行后跨市场从严（blocked>downgrade_pending>approved>clean）、block_required OR、附单条 LawReview（无报告/无法审分别 null 不伪造 clean）、产品名走 profile.product_name 临时键否则回退 PS id 前 8 位、tenant_id 必填 422、未知租户 200 空 items、superseded/他租户干扰与 ghost 空态；前端新增 `scripts/check-compliance.mjs`（消息键/force-dynamic/禁写台面/Query 守卫）+Chrome 端到端。后端测试合计 414 全绿（410→414，+4 集成），eval 仍 101/101。
- **前端切片补登（Q102，2026-09-16）**：新建与客户 (shell) 平级的 `app/[locale]/admin/` 独立布局（frame/侧栏/顶栏），Token 成本、人工审核工作量两 RSC 只读页消费既有 Q92 API（默认近 30 天窗、合计卡、按天×模型/Skill/失败三表、候选/待办积压+窗口产出四表）；新增 `scripts/check-admin.mjs`（消息键/枚举码禁入消息表/force-dynamic/禁 NEXT_PUBLIC 与硬编码色/nav 恰 8 项隔离/后端路由守卫，失败路径实测）；管理员身份 server-only env 自报（未配显 unconfigured 不请求）；Chrome 端到端（307、auditor 403、浏览器零直连）。后端零改动、测试合计 414 不变，eval 仍 101/101。
- **集成层补登（Q103，2026-09-16）**：统一审核工作台前端三文件消费 Q93 API（五型多选/风险下拉/state 切换/分页/风险降档+同档最老排序），后端唯一加法=workbench `_candidate_view` 增 payload 字段，断言并入既有 `tests/integration/test_review_workbench_api.py`（industry=medical 一例，无新增计数）；check-admin 扩展守卫（两失败探针实测 exit 1）；Chrome 端到端（默认 8 行、?target_type=atom_batch 过滤、批量 8→6、applied 全禁用、混角色 403）。后端测试合计 414 不变，eval 仍 101/101。
- **集成层补登（Q104，2026-09-16）**：审核工作台逐条裁决三按钮（confirmed/modified/rejected）经 Server Action 复用既有 `POST /api/skill-candidates/{id}/decision`，后端唯一加法=`_candidate_view` 补 review_note，`tests/integration/test_review_workbench_api.py` 增 1 例（rejected→archived 视图回显备注、排序用例补 None 断言）；modified 非法 JSON 前端拦截零请求有探针；Chrome 端到端（critical 二次确认、modified 落 atom-candidates、rejected 备注落库、applied/archived 无岛）。后端测试合计 415 全绿（414→415，+1 集成），eval 仍 101/101。
- **前端切片补登（Q105，2026-09-16）**：管理端租户管理+Onboarding 前端页（listTenants 七列表、开通/改套餐/暂停/恢复四 Server Action、详情派生 Onboarding 进度）消费 Q95 既有六路由，后端零改动；plan 四档/status 三态枚举码原样不译、trial 显 500000/付费档额度 null 显 —（额度【待补】不造数）；check-admin 扩展（29 键/六文件/枚举禁译/侧栏第四项/探针守卫，失败探针实测）；Chrome 端到端八项（三态 chip 计算色、重复开通 409、改套餐不动 status、暂停 409/恢复 201、auditor 403）。后端测试合计 415 不变，eval 仍 101/101。
- **前端切片补登（Q106，2026-09-16）**：录入单 15 态客户操作+draft 资料编辑页落地，纯前端消费段1 既有四端点（allowed-events/transitions/PATCH profile/product-space），客户事件白名单恰 4 码 {submit,params_completed,quota_confirmed,resubmit}（运营/系统事件不外放、触发方【待补】事件不外放）；422 detail.missing_fids 以"、"拼接中文句式回显、枚举 fid 不入消息表；check-products.mjs 整体重写（18 键/三新文件/env 守卫/白名单数组精确比对禁含 ops_confirm 等/两失败探针实测）；Chrome 端到端十态（draft 改名、缺字段 422、三客户按钮推进、终态零按钮、非法事件 409）。后端零改动、测试合计 415 不变，eval 仍 101/101。
- **集成层补登（Q107，2026-09-16）**：段1 新增唯一只读端点 `GET /api/intakes/ops-queue`（require_ops_view：query actor 必填 422、require_any_role(operations,platform_admin)、customer 403；跨租户 select+count、status 白名单 15 态非法 422、limit 1-100 默认 20、注册序先于 /{intake_id}），`tests/integration/test_intake_api.py` 9→12（RBAC 双角色 200 空体、跨租户 t1/t2 计数与 draft 过滤、非法 status 422、limit=2 分页不重不漏、ops_confirm 推进后出队）；运营事件白名单恰 6 码前端有 JSON.stringify 精确相等探针；Chrome 端到端（15 态筛选、详情岛六事件真实点击、missing_role 零请求守卫、零直连）。后端测试合计 418 全绿（415→418，+3 集成），eval 仍 101/101。
- **集成层补登（Q108，2026-09-16）**：M10b 起裸奔的 `GET /api/admin/sla/todos` 补 require_sla_view query 闸（actor 必填 422、仅 platform_admin、余角色 403），status 扩白名单恰 4 值 {open,escalated,resolved,all} 非法 422、默认 open、due_at ASC；`tests/integration/test_sla_api.py` 增 3 例（RBAC、非法 status detail 原文、5 种子 due_at 升序与 open 恰 3/escalated·resolved 各恰 1、sla_state 字典精确）；前端第六页纯只读 RSC（无写控件、POST /sla/run 不上页有 checker 守卫、yellowNote 如实声明黄色仅法审）；check-admin 扩展（21 键/侧栏第六项恰 6/Q108 守卫块/失败探针实测）；Chrome 端到端 6 种子跨三租户全过。后端测试合计 421 全绿（418→421，+3 集成），eval 仍 101/101。
- **集成层补登（Q109，2026-09-16）**：管理面读端点鉴权审计——穷举全部 /api/admin GET 后确认 8 个矩阵标注角色而代码仅写口挂闸的裸读口，统一按 Q92/Q108 query actor 同型补闸（signal-weights/industries=operations、categories=dictionary_admin、fp-source-routes/fit-weights=operations、compliance-wordlist=operations|internal_compliance、ai-models=platform_admin、ai-scene-routes=operations|platform_admin）；新文件 `tests/integration/test_admin_read_rbac_api.py` 表驱动 23 条（8 端点无参 422+customer 403、矩阵角色逐个 200、词表 platform_admin/product_reviewer 403 而 internal_compliance 200、categories 对 operations 403、config/g2-candidates/skill-prompts/agent-keys 无参 200 锁定当时待补/有意开放结论），14 处既有裸 GET 调用补 query（c7_llm_resolve、model_gateway、platform 等 5 文件）；矩阵明文有意开放的 agent-keys 与 ai-models/{id}/keys 测试继续锁定免闸。后端测试合计 444 全绿（421→444，+23 集成），eval 仍 101/101。
- **前端切片补登（Q110，2026-09-16，热修）**：Q109 浏览器 E2E 空库回归发现 /zh-CN/admin/review-workload 四空态原样显示消息键 admin.workload.empty（Q102 四空态分支调 t("empty") 而 zh-CN.json workload 块漏键、check-admin 必检清单同步漏列；顶层 admin.empty 属另一命名空间 next-intl 不向下回退不可复用）；workload 块补 "empty": "暂无数据。"，全仓其余 10 处 t("empty") 逐一核键存在，check-admin workload 必检清单补 empty，删键失败探针实测 exit 1 后还原；E2E 同环境复验四空态显中文无原始键泄漏。后端零改动、测试合计 444 不变，eval 仍 101/101。
- **单元层补登（Q111，2026-09-16）**：仓库首个 CI workflow `.github/workflows/ci.yml` 落地，新文件 `tests/unit/test_ci_workflow_contract.py`（46 行）静态解析 workflow YAML——断言触发=push main/dev+pull_request、backend job 三命令（ruff check / pytest -q sqlite 无服务容器 / eval runner 101 案）、frontend job typecheck+六 checker 全部在位、两 job 零 secret 零外部服务；删闸即挂（删 check-nav 行失败探针实测 1 failed 后还原）。后端测试合计 445 全绿（444→445，+1 单测），eval 仍 101/101。
- **纯文档切片（Q112，2026-09-17）**：13 状态机迁移表/11 API 规范据已落地实现回填（无代码/迁移/测试变化），测试基线不变。后端测试合计 445 不变，eval 仍 101/101。
- **集成层补登（Q113，2026-09-17）**：Q109 挂【待补】三族 7 个读口按矩阵/写口同组补 query actor 闸——config 三 GET=platform_admin（require_config_view）、g2-candidates=dictionary_admin（require_g2_candidates_view）、skill-prompts 三 GET=platform_admin（require_prompts_view），处理器末位 Depends、函数体零改动，写口 body actor 服务层闸不动；`tests/integration/test_admin_read_rbac_api.py` 三族从 OPEN_READS 移入 GATED_READS（子路径缺 actor 422/越权 403/命中 200、config operations·dictionary_admin 403、g2 operations·platform_admin 403、prompts operations·product_reviewer 403 三组反例），test_config_center_api.py、test_fieldpool_api.py、test_model_gateway_api.py 既有调用补 query；agent-keys/ai-models keys 有意开放仍免闸锁定；前端全仓 grep 零消费。后端测试合计 455 全绿（445→455，+10 集成），eval 仍 101/101。
- **集成/单元层补登（Q114，2026-09-17）**：settings V1 只读账户面板——tenants customer_router 新增无闸只读 `GET /api/tenants/{tenant_id}`（CustomerTenantView 不含 detail/onboarding），`tests/integration/test_tenants_api.py` 增例（客户自读 200、视图不含 detail/onboarding 字段）；五型 SLA 黄色窗口配置化占位（policies.py 黄判由 created_at+hours 改 due_at−sla.yellow_hours、YELLOW_BY_TYPE→YELLOW_TYPES 并入五型 review_*、法审 due=48h 数值不变），`tests/unit/test_sla_policies.py` 增例（五型黄窗判定、法审零漂移）；templates 仅占位 V2 徽标无后端端点；前端新增第七个静态闸门 `scripts/check-settings.mjs`（npm run check-settings）。后端测试合计 457 全绿（455→457，+1 集成 +1 单测），eval 仍 101/101。
- **纯种子迁移切片（Q115，2026-09-17）**：基准 HTML 永久丢失后溯源口径收口（line NNNN 降级历史痕迹、12 名单定稿、触发方永久【待补】）；G2 fid 工程定稿落迁移 0024_g2_common_fields（12 个 cat='common' 字段，ON CONFLICT DO NOTHING 幂等），无后端业务代码改动、无新增测试（迁移按 MIGRATION_CONVENTION 由 pgvector/pg16 一次性容器 up/downgrade-1/up 实测，SQLite 侧 create_all 种子回落）。后端测试合计 457 不变，eval 仍 101/101。
- **单元/集成层补登（Q116，2026-09-17，V2 P4 段12 文章生成首片）**：新模块 `app/content/` 六态机（draft→generating→review→ready_for_publish/rejected/revising）——新文件 `tests/unit/test_content_statemachine.py` 6 例（合法迁移/终态拒事件/越权角色/revising→generating/regenerate 计数等纯状态机）；新文件 `tests/integration/test_content_api.py` 9 例（两提交合计，httpx ASGI+内存 SQLite）：POST /generate operations 闸+ARTICLE-GEN synthetic 经 SkillRun 手动落成本+writeAudit、产 {"body"} 直写 content_products.body 不走 skill7 Gate、approve/reject（原因必填 Q59）/revise 客户闸、regenerate operations 且 count+1（Q56 上限 3）、final_id 只读软关联、词库扫描复检 4 项（复用 Q48 词库+ccr_rules 三层）命中阻断、语义级/施工指令/国家规则 3 项占位不测；迁移 0025/0026 由 PG16 容器实测。后端测试合计 472 全绿（457→472，+6 单测 +9 集成），eval 仍 101/101（状态机为新业务纯函数但走后端单测而非 eval runner）。
- **纯文档切片（Q117，2026-09-17）**：文档⇄代码一致性审计与 11 文件纯事实回填（先取实测 472/ruff 净/eval 101/head=0026/7 checker/86 token 基线再订正），无代码/迁移/测试变化；4 项实现差异只挂账（后由 Q118 收口）。后端测试合计 472 不变，eval 仍 101/101。
- **单元/集成层补登（Q118，2026-09-18，Q117 挂账四项收口）**：①4 个漏网管理面 GET 补闸（content-goals=dictionary_admin、cp-law-domains=internal_compliance、publish-slots/slot-type-defaults=operations，require_fit_weights_view 重构为通用 require_operations_view），`tests/integration/test_admin_read_rbac_api.py` 参数化扩 8 例+角色边界 2 例（缺 actor 422/越权 403/命中 200，test_ccr_api.py、test_pwc_api.py 既有调用同步补 query）；②publish_slots.gate 默认 approved=V1 仅 Q42 人工直编口径，`test_platform_api.py` 锁定人工直建 gate=approved（GATE_PENDING 零引用）；③迁移 0027_product_space_fks 两道 nullable FK 按 MIGRATION_CONVENTION 由 pgvector/pg16 一次性容器实测（全链 0001→0027 up、downgrade-1/up 往返、孤儿 INSERT/UPDATE 被两 FK 分别拒、NULL 合法），不入 pytest；④CI 六→七 checker，`tests/unit/test_ci_workflow_contract.py` 增 1 例「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除漏接（枚举加 settings）；⑤Alembic 版本表加宽修复由迁移全链容器实测覆盖。后端测试合计 483 全绿（472→483，+10 集成 +1 单测），eval 仍 101/101，前端 tsc+七 checker 全绿、token 仍 86。
- **单元/集成层补登（Q119，2026-09-18，V2 P4 段12 多语言 Q58）**：①新文件 `tests/unit/test_content_l10n.py` 5 例（交集纯函数：空 markets 全覆盖含 country 空、受限语言不覆盖 country 空、市场匹配/不匹配、PS 目标语言非空收窄与空=不限、保清单序）；②新文件 `tests/integration/test_content_l10n_api.py` 6 例（语言清单 CRUD 的 RBAC：dictionary_admin 可写/operations 403/读口缺 actor 422/越权 403、upsert 空 name 422、archive 404、upsert 复活；eligible-languages 交集与 query actor 闸；PUT target-languages operations 可设/customer 403/ghost 404/重复 422/清空=不限；generate 未配置 en-US 422、受限语言不覆盖通用发布位 422、fcw-us 英文独立成品 201 且 body 含 language=en-US、同 final+lang+kind 重复 409、同 final zh-CN 另一条独立成品、不传 language 默认 zh-CN 向后兼容）；③既有 `tests/integration/test_content_api.py` fixture 自插 zh-CN ContentLanguage 种子（create_all 不跑迁移种子，否则交集为空 happy path 422）。④迁移 0028_content_languages 按 MIGRATION_CONVENTION 由 pgvector/pg16 一次性容器实测（全新库 up head 建 content_languages+zh-CN 种子/PS.target_languages jsonb/唯一约束、downgrade-1 表列约束全清回 0027、再 up 恢复），不入 pytest。后端测试合计 **494 全绿（483→494，+5 单测 +6 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端无变化（tsc+七 checker、token 仍 86）。
- **单元/集成层补登（Q120，2026-09-18，V2 P4 段12 AI 质量分 Q57 + 重生成上限 Q56）**：①新文件 `tests/unit/test_content_quality.py` 13 例（`parse_qc_output` 合法/非法 JSON/七类坏 score 参数化〔非对象 ×2、非数值 ×3 含 bool、越界 ×2〕/issues 非数组归一/0 与 1 边界、`quality_advisory_for` 高分/低分/恰等阈/None、`revise_allowed` 默认上限 3 与自定义 limit=1 及非 review 态）；②新文件 `tests/integration/test_content_quality_api.py` 4 例（generate 后真打分 quality_score=0.92/threshold 0.85/advisory False 且落一条 ARTICLE-QC SkillRun〔source=llm_auto、wf=WF-10、output score〕；哨兵 `[LOW_QC]`=0.62 低分仍可客户 approve→ready_for_publish、advisory True 不阻断；monkeypatch 使 QC 抛 ModelConfigError 时 generate 仍 201 进 review、score 为 None/issues 记 qc_error/advisory None；`config_cache.apply({"content.regen_limit":1})` 后 generate→revise→regenerate(count=1)→再 revise 返回 422 且 detail 含 1/1，autouse fixture teardown 还原进程级配置缓存）；③既有 `tests/integration/test_content_api.py` happy path 一次生成 SkillRun 断言由 1 改 2（ARTICLE-GEN+ARTICLE-QC），两个内容集成 fixture 自插 ARTICLE-QC 场景三件套（create_all 不跑迁移种子）。④迁移 0029/0030 按 MIGRATION_CONVENTION 由 pgvector/pg16 一次性容器实测（全新库 up 两列 + QC 种子、downgrade 0030→0029 种子全清列保留、0029→0028 两列全删、再 up 恢复），不入 pytest。后端测试合计 **511 全绿（494→511，+13 单测 +4 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端无变化（零端点新增、纯后端切片）。
- **单元/集成层补登（Q121，2026-09-19，V2 P4 段12 复检第②项语义级检测 Q59，纯 advisory）**：①新文件 `tests/unit/test_content_semantic.py` 8 例（`parse_semantic_output`：合法 findings 原样、非法 JSON→invalid_json、非对象→not_object、findings 非数组→findings_not_array〔参数化 4 类坏形态〕、逐项丢弃非法项〔非对象/缺 code/空 code〕、message/excerpt 非字符串丢弃、code 去空白）；②新文件 `tests/integration/test_content_semantic_api.py` 4 例（happy：generate 后 review_hits.semantic={checked:true,findings:[]} 且一次生成落 GEN+QC+SEMANTIC 三条 SkillRun〔集合断言〕；哨兵 `[SEMANTIC_RISK]` 命中 1 条 unsubstantiated_claim 但 block_required 不抬、客户 approve 照常 200；monkeypatch 使语义网关抛 ModelConfigError 时 generate 仍 201、checked=false/error/无 SEMANTIC SkillRun 且可 approve；regenerate 后语义复检重跑共 2 条 SkillRun）；③三个既有内容集成 fixture（test_content_api/test_content_quality_api/test_content_l10n_api）自插 ARTICLE-SEMANTIC-CHECK 场景三件套（create_all 不跑迁移种子），test_content_api happy path SkillRun 集合断言 2→3。④迁移 0031_article_semantic_seed 按 MIGRATION_CONVENTION 由 pgvector/pg16 一次性容器实测（全新库 up 三件套就位、downgrade -1 回 0030 语义三件套全清 0/0/0 而 ARTICLE-QC 三件套保留 1/1/1、再 up 恢复 1/1/1），不入 pytest。后端测试合计 **523 全绿（511→523，+8 单测 +4 集成）**，eval 仍 101/101（golden 21/21，内嵌编排+纯函数走 pytest、不入 eval），ruff 净，前端无变化（零端点/零列新增、review_hits 为既有 view 字段）。
- **集成层补登（Q122，2026-09-19，客户内容页 + Q56-a 人工编辑，零迁移）**：①新文件 `tests/integration/test_content_customer_api.py` 5 例（`_scene_triplet` helper 自插 GEN/QC/SEMANTIC 九场景三件套 + zh-CN/en-US 两语言；CUSTOMER actor roles=[]、OPS roles=[operations]）：列表租户隔离/列表项不含 body/未知租户返空/缺 tenant_id 422；详情带 body/未知 content 404；人工编辑 happy path（revising 态 PATCH body 200 回 review、SkillRun 集合恰为 GEN+QC×2+SEMANTIC×2 共 5 条、regenerate_count 恒 0 不增）；非 revising 态 PATCH 409、空白 body 422、未知 content 404；编辑后正文含词库 ban 哨兵与语义哨兵仍回 review〔advisory 不阻断、block_required 由词库抬〕）。②前端新增第八个零依赖 checker `scripts/check-content.mjs`（消息键含六态/文件齐备/占位移除/nav v1/client 岛禁服务端访问层与裸 fetch/后端路由与 manual_resubmit 防漂移/生成端点不得出现在客户访问层），ci.yml seven→eight 接线，`test_ci_workflow_contract.py` 枚举加 check-content（磁盘集合一致性断言自动覆盖新 checker）。后端测试合计 **528 全绿（523→528，+5 集成）**，eval 仍 101/101（golden 21/21，薄编排与页面不入 eval），ruff 净，前端 tsc+八 checker 全绿、next build 成功、token 仍 86。
- **集成层补登（Q123/B3，2026-09-19，段1 客户目标语言控件，零迁移）**：新文件 `tests/integration/test_target_languages_customer_api.py` 6 例（内存 sqlite，zh-CN/en-US active + fr-FR archived + ps-1）：GET /api/content/languages 无 actor 200 仅 active 且按 code 排序、不含归档；客户 roles=[] PATCH /api/intakes/int-1/target-languages 200 且 GET product-space 回显；空数组清为 null；未知码/归档码/重复码均 422；产品空间未生成 404；**Q119 operations PUT 对客户 roles=[] 仍 403、operations 200 的回归锁定**。前端 check-products 扩展（targetLanguages 四键/新岛文件/api token/岛纪律/后端 /target-languages 路由），check-content 加 /api/content/languages 端点与「必须先于 {content_id} 注册」顺序断言。后端测试合计 **534 全绿（528→534，+6 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端 tsc+八 checker 全绿、next build 成功、token 仍 86，Alembic 头仍 0031、业务物理表仍 55。
- **单元/集成层补登（Q124，2026-09-19，段12 难产骨架作废回池 Q56-b，迁移 0032）**：①`tests/unit/test_content_statemachine.py` 扩第七态 discarded（review 外放事件含 discard 共 4 个、discarded 终态 allowed_events 为空、review/revising/rejected 三态可作废而 draft/generating/ready_for_publish 四态不可）；②新文件 `tests/integration/test_content_discard_api.py` 9 例（内存 sqlite，模型 Index 带 sqlite_where 故 create_all 直接建 partial index 可测回池；zh-CN/en-US 两语言）：happy（operations 作废 review 200、落 discard_reason、审计 content.discarded、列表回带）、客户 roles=[] 403、未知 content 404、revising/rejected 可作废、ready/draft 409、空白 reason 422、**回池后同 final_id+language+kind 可重新生成新行且两行独立、未作废时重复同键仍 409**、discarded 终态 approve/reject/revise/regenerate/discard 全拒、needs-attention 队列（仅 review/revising/rejected、跨租户、created_at 升序、行不含 body、缺 actor 422/客户 403/platform_admin 200）。③迁移 0032_content_discard 按 MIGRATION_CONVENTION 由 pgvector/pg16 一次性容器实测（全新库 up head：discard_reason 列与 partial unique index `WHERE status<>'discarded'` 就位；discarded+live 同键共存而第三条 live 同键 duplicate key 409；downgrade -1 回 0031 列消失、partial index 还原为普通唯一索引；再 up 恢复），不入 pytest。
- **单元/集成层补登（Q125，2026-09-19，段12 运营发布回填 Q60c，迁移 0033）**：新文件 `tests/integration/test_content_publish_api.py` 9 例（内存 sqlite；`_fcw` 用 final_id suffix 派生独立包 id/slot 规避跨租户 FCW 唯一约束；sqlite 往返丢 tz，时间按去 Z 串比较）：happy（operations PUT publish-info 200、落三列、published_at 首次落、审计 content.publish_info_set）、客户 403、未知 404、非 ready_for_publish 409、空白 url 422、**重复回填覆盖 url 但 published_at 不重置、post_id 缺省不动 / 空串清空**、ready-to-publish 队列跨租户且 created_at 升序、排除非 ready 与 discarded、行不含 body、**回填后仍在队列且带链接（待回填/已回填两分区）**、缺 actor 422/客户 403、platform_admin 读口 200 写口 403。迁移 0033_content_publish 由 pgvector/pg16 一次性容器实测（up 三列就位、downgrade -1 回 0032 三列消失而 discard_reason 保留、再 up 恢复），不入 pytest。前端新增管理端 `/admin/content` 内容运营台（page.tsx RSC 双队列 + actions.ts 两 Server Action + publish-info-island/discard-island 两 client 岛），check-admin 扩守卫（sidebar 6→7、requiredKeys/Files、force-dynamic 只读 RSC、Server Action 四状态码与 missing_role、岛纪律、api 四函数与四路径、后端路由与静态 GET 先于参数路由），check-content 扩守卫（content.status.discarded/publishedLink 键、客户 actions 禁 operations 端点 token、EVENT_DISCARD/CONTENT_DISCARDED、toneDiscarded），**checker 总数仍八、无新 checker 文件，CI 接线与 test_ci_workflow_contract 不变**。后端测试合计 **554 全绿（534→554，+2 单测 +18 集成）**，eval 仍 101/101（golden 21/21，状态迁移与队列过滤走 pytest、页面走 checker 不入 eval），ruff 净，前端 tsc+八 checker 全绿、next build 成功、token 仍 86，Alembic 头 **0033_content_publish**、业务物理表仍 55（两迁移纯加列/换索引）。
- **单元/集成层补登（Q126，2026-09-19，段13 反馈回流入站第一片 effect-callback，迁移 0034 新表）**：①新文件 `tests/unit/test_effect_records.py` 24 例——normalize_metrics（None/空对象→None、非对象拒、未知键拒、显式 null 丢弃不补 0、六计数非负整数且 bool/float/负值拒、read_rate 0..1 且 bool/串/越界拒，参数化）、validate_record（+08:00 归一 UTC、空白 content_id/platform_post_id 拒、naive captured_at 拒）；②新文件 `tests/integration/test_effect_callback_api.py` 13 例（内存 sqlite，issue_key 造 active/revoked 两 Key、造 ready 与 discarded 两成品）：401 三态（无头/坏 Key/吊销明文）、matched+orphan+discarded 分流与 tenant 回填/稀疏 metrics 不补 0/received_by 落 key_id、customer-backfill source 接受、幂等覆盖（upserted=1、不新增行、metrics/post_id 覆盖）+新采集点时序追加 + captured_at 升序、批内重复 (content_id,captured_at) 422、五坏例整批 all-or-nothing（未知键/负计数/read_rate 越界/bool/naive）合法行不落、空 records 与空白 source 422、孤儿队列与成品时序 query actor 闸（缺 actor 422/客户 403/operations·platform_admin 200、时序缺 content_id 422）。③迁移 0034_effect_records 由 pgvector/pg16 一次性容器实测（up：12 列/metrics=jsonb/captured_at=timestamptz/4 业务索引+主键/FK fk_effect_records_content，业务物理表 55→56；downgrade -1 回 0033 表·四索引·FK 全清回 55；再 up 恢复 56），不入 pytest。**无前端改动、checker 仍八、CI 接线与 test_ci_workflow_contract 不变**（段13 首片为纯后端入站；前端孤儿认领台/效果视图随续片）。后端测试合计 **591 全绿（554→591，+24 单测 +13 集成）**，eval 仍 101/101（golden 21/21，理由见 §4 eval 汇总口径），ruff 净，Alembic 头 **0034_effect_records**、业务物理表 55→**56**（0034 新增 effect_records 1 表）。
- **单元/集成层补登（Q127/Q128，2026-09-19，段13 孤儿认领 + customer-backfill 客户通道，迁移 0035 新表+加列）**：①新文件 `tests/integration/test_effect_claims_api.py` 8 例（内存 sqlite，造 ready×2 跨两租户 + discarded 成品、issue_key 造 Agent Key 推孤儿）：认领绑定后孤儿队列清空/成品时序可见/claimed_by·claimed_at 溯源/effect_claims 落映射、认领后新采集点与同点幂等覆盖均 matched 不回落孤儿、旧目标 discarded 后重新认领到新目标的改绑重指（历史 claimed 行 + 新 orphan 行 updated_rows=2）、未知 record 404、非 orphan 记录 409、未知目标 404 且不落半成品、discarded 目标 409、客户与 platform_admin 写口 403（写口仅 operations）；②新文件 `tests/integration/test_effect_customer_backfill_api.py` 8 例：无 Agent Key happy（source 固定 customer-backfill、received_by=客户 actor id、tenant 回填）、幂等覆盖与时序追加、他租户 content 422 不落库、未知/discarded content 双 422 且零孤儿、一条坏整批拒回带 index、metrics/captured_at 五坏例 all-or-nothing、空 tenant/空 records 422、批内重复点 422。③迁移 0035_effect_claims 由 pgvector/pg16 一次性容器实测（up：effect_claims 5 列/PK/FK fk_effect_claims_content、effect_records 两 nullable 列，业务物理表 56→57；downgrade -1 回 0034 表与两列全清回 56；再 up 恢复 57），不入 pytest；Q128 零迁移。**无前端改动、checker 仍八、CI 接线不变**（两片均纯后端；认领台/客户回填 UI 随续片）。后端测试合计 **607 全绿（591→607，+16 集成）**，eval 仍 101/101（golden 21/21），ruff 净，Alembic 头 **0035_effect_claims**、业务物理表 56→**57**（0035 新增 effect_claims 1 表）。
- **单元/集成层补登（Q129，2026-09-20，段13 批量认领 + 取消认领/解绑，零迁移）**：`tests/integration/test_effect_claims_api.py` 8→**19 例（+11）**——unclaim 5 例（解绑删映射、旧目标 claimed 行全部回滚 orphan 清四列并回 reverted_rows、认领后经兜底 matched 的新推送行一并回滚〔实现中发现按 claimed_by 非空筛会漏、已改按旧目标 content_id 筛〕、external_id 自匹配真自动行不受影响、映射不存在 404 ClaimMappingNotFound、客户/platform_admin 403）；batch 6 例（多条 happy 回 {claimed,updated_rows}、整批 all-or-nothing 任一 404/409 整批回滚零落库、批内重复 record_id 422 带 detail{index,field,message}、空 items pydantic 422、越权 403、逐条 effect.claimed 审计随事务）。service 抽 `_claim_one`（无角色闸），claim_orphan/batch/unclaim 各自先 require_any_role(OPERATIONS)。**Q130/Q131 为纯前端片无后端测试变化**：Q130 管理端 `/admin/effects`（孤儿队列单条/批量认领岛 + 成品时序查询/解绑岛）由 check-admin.mjs 新增守卫锁定（sidebar 恰 8 项且含 /admin/effects、effects 五文件齐备、页 force-dynamic 只读、两岛 "use client"+禁直连 @/lib/api+router.refresh、api.ts 六函数与后端 effects router 六路由齐备、admin.effects.* 键含 status.matched/orphan 与 metric 七键）；Q131 客户详情页回填岛由 check-content.mjs 扩守卫（api.ts 含 customerBackfillEffects 与 /api/effects/backfill、详情页挂 BackfillIsland、actions 含 backfillEffectAction/CURRENT_TENANT_ID、岛纪律与 datetime-local/toISOString 探针、后端 backfill 路由串）。后端测试合计 **618 全绿（607→618，+11 集成）**，eval 仍 101/101（golden 21/21），ruff 净；前端 tsc 净、**八 checker 全过**（check-admin sidebar 口径 7→8、check-content 扩回填，无新 checker 文件、CI 集合契约不变）、`npm run build` 通过；Alembic 头仍 **0035_effect_claims**、业务物理表仍 **57**（三片均零迁移）。
- **单元/集成层补登（Q132，2026-09-20，中台导出 JSON + 异步导出任务，迁移 0036）**：新增 `tests/unit/test_export_render.py` **5 例**（空 envelope/带 ids+count、csv 与 json payload 可解析、文件名 fcw-{tenant}.{ext}）；`tests/integration/test_exports_api.py` 由 3→**13 例（+10）**（fcw.json 空租户 envelope/published 隔离/空 tenant 422、csv job completed 且下载等同同步 CSV、json job 下载等同 JSON 口、默认 format=csv、非法 format/缺 actor/空 tenant 三 422、未知 job 状态口与下载双 404、下载重渲染反映新 published 数据而 row_count 留痕创建值 0、审计 export.job_created 字段断言）。后端测试合计 **633 全绿（618→633，+5 单测 +10 集成）**，eval 仍 101/101（golden 21/21），ruff 净；前端零改动（tsc/八 checker 不变、token 仍 86）；Alembic 头 **0036_export_jobs**、业务物理表 **58**（迁移 0036 经 pgvector/pg16 容器 5433 一次性 up/downgrade-1/up 实测：export_jobs 11 列/pkey/ix_export_jobs_tenant_id，57→58→57→58 往返对称）。
- **集成层补登（Q133，2026-09-20，多副本 fencing token，零迁移）**：新文件 `tests/integration/test_fencing_lease.py` **8 例**（内存 FakeRedis 补 set/incr/eval）——门控关闭时 `leader_lease` 给 fence=None·恒 held 空租约且不触 Redis、`leader_lock` 仍字面 yield False；开启后 fence 跨重入单调 1→2、不同锁名独立序列；INCR 失败 fail-closed（抛 LockBackendError 且释放刚取得的锁，不持无令牌锁）；fence 键过期/看门狗续约故障置 lost（held=False、raise_if_lost 抛 LockLost、wait_lost 返回）；锁易主后新持有者 fence 严格更大。既有 `test_distributed_locks.py` 的局部 FakeRedis 补 `incr` 原语（counters），原 13 例零回归。后端测试合计 **641 全绿（633→641，+8 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0036_export_jobs**、业务物理表仍 **58**（无迁移）。
- **集成层补登（Q134，2026-09-20，Redis Streams 细粒度认领原语，零迁移）**：新文件 `tests/integration/test_redis_streams.py` **8 例**（内存 FakeStreamsRedis 实现 XADD/XGROUP CREATE/XREADGROUP/XACK/XPENDING RANGE/XCLAIM）——纯函数 delivery_exhausted 阈值（max=3 第 4 次超限）；XADD id 单调递增且近似 MAXLEN 裁剪；XGROUP mkstream 自动建流、BUSYGROUP 幂等返回 False；XREADGROUP `>` 读即入 PEL、未 ACK 不重投、ACK 移出；两消费组隔离各自 PEL（同消息各消费一次）；崩溃消费者空闲行 XCLAIM 被接管且投递计数 = times_delivered+1；idle 不足阈值不接管；超限行 move_to_dead 进死信流（附 _dead_reason/_dead_origin_id）并 XACK 原流；Redis 故障统一包装 StreamBackendError。**只测原语，不接 worker/不落 env/无占位端点**。后端测试合计 **649 全绿（641→649，+8 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0036**、物理表仍 **58**（无迁移）。
- **集成层补登（Q135，2026-09-20，配置缓存多副本失效广播，零迁移）**：新文件 `tests/integration/test_config_broadcast.py` **7 例**（内存 FakeBroadcastRedis + FakePubSub）——门控关闭 spawn_invalidation 不触 Redis；开启后 after_commit 发布 key 与全量 `*`（fire-and-forget 任务跑完 PUBLISH）；PUBLISH 抛 RedisError 被 best-effort 吞掉不污染提交；真实 after_commit 钩子（in-memory sqlite create_all + _schedule_cache_apply + commit）全链触发广播；订阅者 start 后收到消息用独立会话 config_cache.reload（reloads 计数）、stop 后 running=False；订阅轮询 get_message 故障 poll_once 不抛返回 False 且可复用（懒重连）；publish_invalidation 返回在线订阅者数。既有 test_config_center_api/test_config_bootstrap 零回归。后端测试合计 **656 全绿（649→656，+7 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0036**、物理表仍 **58**（无迁移、无新配置键，仅 env LOOM_CONFIG_CACHE_BROADCAST_ENABLED 默认关）。
- **前端片补登（Q136，2026-09-20，客户批量 CSV 回填，零后端改动/零迁移）**：**纯前端片无后端测试变化**——后端 Q128 `POST /api/effects/backfill` 的 `records[]`（min_length=1、整批 all-or-nothing、422 detail 带 index、绝不产生孤儿）早已满足批量，不建上传端点、不加测试。前端由 check-content.mjs 扩守卫锁定（必需 13 个 content.backfillBatch* 消息键与占位符、必需文件 backfill-batch-island.tsx、详情页挂 BatchBackfillIsland、actions 含 batchBackfillEffectsAction、岛纪律循环纳入批量岛〔"use client"/禁 @/lib/api/禁裸 fetch·URL/router.refresh〕、批量岛必含 FileReader/readAsText/toISOString/all-or-nothing token）；`npx tsc --noEmit` 净、**八 checker 全过**（check-tokens 仍 86，CSS 零新增）、`npm run build` 通过。后端测试合计 **656 全绿（不变）**，eval 仍 101/101（golden 21/21）；Alembic 头仍 **0036_export_jobs**、业务物理表仍 **58**。
- **集成层补登（Q137，2026-09-20，中台导出真后台 worker，零迁移）**：新建 `tests/integration/test_export_worker.py` **7 例**（内存 SQLite + 共享 FakeStreamsRedis）——门控开新消息消费成功置 completed 并 XACK、崩溃 PEL 空闲超阈被 reclaim 接管、未超投递上限的瞬态异常留 PEL 下轮重试、超 max_deliveries move_to_dead 并置 failed、终态作业重复投递幂等 ACK 不重复渲染、job 行不可见时留 PEL 不误置失败、门控关走原同步路径；`test_exports_api.py` **+3 例**（异步 POST 落 queued 并入流回 download_url、`GET /jobs` 列表 envelope 与 limit 1..200 闸、queued/running download 409）；把 test_redis_streams.py 内 FakeStreamsRedis 抽到共享 `tests/integration/stream_fakes.py`（test_redis_streams 改 import，原 8 例零回归）。后端测试合计 **666 全绿（656→666，+10 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0036_export_jobs**、业务物理表仍 **58**（状态用 String(16) 常量、无 PG enum，无迁移）。
- **集成层补登（Q138，2026-09-20，restock 信号 XADD 生产侧，零迁移）**：新建 `tests/integration/test_restock_stream_notify.py` **3 例**（内存 SQLite + FakeStreamsRedis）——门控开 requested 行 commit 后幂等建组且 XADD 一条 request_id（排空 after_commit fire-and-forget 任务断言）、门控关不触 Redis 不建组但 requested 行照常落库、XADD 故障（fail=True）best-effort 被吞且 requested 行仍为事实源（status=requested/source=restock_auto）；既有 test_restock_worker_api.py 15 例零回归。后端测试合计 **669 全绿（666→669，+3 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；头仍 **0036**、表仍 **58**（无迁移）。
- **集成层补登（Q139，2026-09-20，两循环 leader_lease 协作中止，零迁移）**：新建 `tests/integration/test_lease_cooperative_abort.py` **5 例**（复用 test_fencing_lease 的 FencingRedis 开关注入）——run_jobs 第 N 作业前 checkpoint 抛 LockLost 则已提交作业保留/后续作业本轮不跑且 LockLost 冒泡、无 checkpoint 恒跑完、run_restock 第 2 条信号前抛 LockLost 则只处理第 1 条不继续花钱、scheduler._tick 与 restock worker._tick 捕获 LockLost 不冒泡（循环继续）；test_distributed_locks.py 三处假 run_jobs 与 fake_run_restock、test_scheduler.py 假函数签名补 `**_kwargs` 兼容新增 checkpoint kwarg（Q89/Q133 既有 21 例零回归）。后端测试合计 **674 全绿（669→674，+5 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；头仍 **0036**、表仍 **58**（无迁移）。
- **集成层补登（Q140，2026-09-20，配置缓存单 key 增量失效 + 广播断线退避，零迁移）**：`tests/integration/test_config_broadcast.py` 由 7→**11 例（+4）**——`*` 走全量 reload 且不误用 reload_keys、具体 key 走 reload_keys([key]) 且不触发全量、连续 poll 故障累加 consecutive_failures 并给出指数退避延迟、恢复后无消息即重置计数、故障下 _loop 按退避等待而非忙转（固定时窗内 get_message 次数受限且两次间隔≥退避；回归实测健康路径若无每轮 sleep(0) 零拍让出会饿死 asyncio 定时器、连 wait_for 超时都不触发）、reload_keys 对更新/删除（从快照 pop）/新增/未涉及 key 的增改删合并（真实 in-memory SQLite ConfigItem）；既有发布侧 5 例与自愈用例零回归。后端测试合计 **678 全绿（674→678，+4 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0036_export_jobs**、业务物理表仍 **58**（无迁移、无新配置键，复用 Q135 门控）。
- **集成层补登（Q141，2026-09-20，配置缓存版本号门控 + TTL 兜底，零迁移）**：`tests/integration/test_config_broadcast.py` 由 11→**17 例（+6）**——旧版本增量快照被拒而新版本接受（skipped/accepted）、apply 不带 versions 时版本向量保持不变、TTL 到期且无失效消息也回源全量 reload、TTL 未到期不触发、首次成功装载前不触发且缺 reload factory 不报错、默认 TTL=300s。后端测试合计 **684 全绿（678→684，+6 集成）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0036_export_jobs**、业务物理表仍 **58**（无迁移；新 env `LOOM_CONFIG_CACHE_TTL_SECONDS` 默认 300 为运维旋钮）。
- **单测/集成补登（Q142，2026-09-20，中台导出分页 + 行数硬上限，零迁移）**：`tests/unit/test_export_render.py` 5→**7 例（+2 单测，JSON envelope 分页字段 total/limit/offset/has_more 与 page 透传）**；`tests/integration/test_exports_api.py` **+6 集成**——JSON limit/offset 分页、CSV `X-Export-*` 响应头、limit 超硬上限 422、不传 limit 被硬上限截断（truncated）、非法分页参数 422、同步 job 截断审计 total/truncated；空 envelope 精确断言按新字段更新。后端测试合计 **692 全绿（684→692，单测 +2、集成 +6）**，eval 仍 101/101，ruff 净，前端零改动；头仍 **0036**、表仍 **58**（无迁移；新 env `LOOM_EXPORT_MAX_ROWS` 默认 100000）。
- **集成层补登（Q143，2026-09-20，下游 PG 行级 fence 接 restock 花钱路径，迁移 0037）**：新建 `tests/integration/test_restock_fencing.py` **4 例原语**（首次认领 acquired/同 fence 重入 held、更大 fence 接管而更小 fence 判 lost、fence=None 全程 no-op 不落行且门恒开、接管后旧 fence 门闭新 fence 门开，顺序独立事务覆盖跨连接真实条件 UPDATE）；`tests/integration/test_restock_worker_api.py` **+3 端到端**（带 fence 成功并落 restock_claims 行、信号已被更新 fence 预占时花钱前 skipped_lost 不产子 run、提交前门关闭时回滚已生成的子 run/候选）；`test_lease_cooperative_abort.py` 的 fake_process 补 `**_kwargs` 兼容新 fence/owner 形参。后端测试合计 **699 全绿（692→699，原语 +4、worker 端到端 +3）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头 **0037_restock_claims**、业务物理表 **58→59**（迁移经 pgvector/pgvector:pg16 容器 upgrade head→downgrade-1 删表→upgrade 重建往返实测）。**测试替身边界**：sqlite 内存库单连接无法在业务事务打开时由另一会话并发提交接管，故 worker「花钱中途易主」端到端用 monkeypatch 关闭 fence_current 确定性验证回滚路径，跨连接接管的真实条件 UPDATE 语义由上述 4 个顺序事务原语测试覆盖；真 PG 多副本 + 真 Redis 易主→中止/拒写集成验证留 V2。

- **部署/运维片补登（Q145–Q147，2026-09-21，private beta Checklist #4/#5/#6，零业务迁移）**：Q145 最小部署制品（backend/frontend Dockerfile + entrypoint 启动前 `alembic upgrade head` + compose backend 内网/frontend 唯一发布口 3000 + service_healthy 依赖序）落 **5 例静态契约测试**守卫制品不回退（699→704）；Q146 最小备份监控（db-backup sidecar 每日 pg_dump+7 天保留、日志轮转、watchdog 探活+webhook、frontend healthcheck）配套契约测试 **+6**（704→710，备份脚本/探活配置经一次性 PG16 容器实跑验证）；Q147 staging 彩排（`infra/docker-compose.staging.yml` overlay，`!reset` 清宿主端口避本机 atlas、minio profile 排除，`-p loom-staging` 6 服务 healthy、真实 Chrome SSR 零 console 错误、段1 API 冒烟、`down -v` 清理）overlay 契约 **+1**（710→711）。Q144（上线形态裁决）、Q148（真 LLM 接线）、Q149（阈值确认）为纯文档/配置片，后端测试数不变（Q148 真实 CAT-RECOG 635/231 tokens 冒烟为人工 staging 实测、不进自动化套件；ATOM-AFFINITY 仍 synthetic）。
- **单测/集成补登（Q150，2026-09-21，A2A 封臣 plan 模式任务层，零迁移/零新 env）**：新建 `tests/unit/test_a2a_vassal.py` **12 例单测**（Agent Card fealty 与三 skills 清单、plan-only 免费承诺 cost.llmTokens=0、runId 在全部事件含 artifact-update 回显 `x-zeus`、tasks/send|get|cancel 完整生命周期、缺参 input-required、重复 cancel -32002、未知任务 -32001/未知方法 -32601/非法消息错误码）；新建 `tests/integration/test_a2a_vassal_api.py` **10 例集成**（三发现路径 `/api/a2a/agent-card`、`/.well-known/agent-card.json`、`/.well-known/agent.json` 公开可达且无需 Bearer、无 Key/错 Key/吊销 Key 均 401、tasks/send 完成 plan 任务并断言审计 `a2a.task` 落库 tenant=`_platform`、input-required 同样留审计、非法 JSON-RPC -32602、get/cancel 生命周期错误码、sendSubscribe SSE 流 text/event-stream 多 data 块末条 completed）。后端测试合计 **733 全绿（711→733，单测 +12、集成 +10）**，eval 仍 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 0037、业务物理表仍 59。**边界**：任务存储进程内存（reset_for_tests 测试缝），无持久化/跨实例测试；真 LLM、链路执行、Zeus↔loom 真机联调均不在第一阶段，随第二阶段另立切片。
- **集成层补登（Q151，2026-09-21，SLA sweep 写路径 PG 行级 fence，迁移 0038）**：新建 `tests/integration/test_sweep_fencing.py` **8 例**——认领/提交前门原语 4（首认 acquired→同 fence held、更大 fence 接管后旧值 lost、fence=None 全程 no-op 不落 sweep_tick_claims 行且门恒开、接管后旧门闭新门开，顺序独立事务覆盖真实条件 UPDATE）；runner 提交前门 2（门开作业 marker 行正常提交、门闭作业回滚且抛 LockLost、后续作业本轮不执行）；scheduler 接线 2（开门时认领落行且 checkpoint/commit_guard 均透传、预置更大 fence 行 claim LOST 时本轮静默放弃不跑任何作业）。适配既有 `test_distributed_locks.py`（lock-free tick 用 null session factory + monkeypatch claim_tick）与 `test_lease_cooperative_abort.py`（sweep LockLost 吞咽用例同 patch）共 3 处。后端测试合计 **741 全绿（733→741，+8 集成）**，eval 101/101，ruff 净，前端零改动；Alembic 头 **0038_sweep_tick_claims**、业务物理表 **59→60**（迁移经 pgvector/pgvector:pg16 容器 55432 端口 upgrade head→downgrade-1 表消失且 restock_claims 仍在→upgrade 重建往返实测，五列/pkey 正确）。**测试替身边界**（同 Q143）：sqlite 内存库单连接无法在作业事务打开时跨会话并发接管，「作业中途易主」以顺序预置更大 fence 行确定性复现；真 PG 多副本+真 Redis 易主→中止/拒写集成验证留 V2。
- **单测/集成补登（Q152，2026-09-21，导出 worker 多 consumer 并发度/批量配置，零迁移）**：`tests/integration/test_export_worker.py` 由 7→**13 例（+6）**——默认工厂单 worker 且 consumer 前缀/count=20/block 5000 为 V1 默认值、concurrency=3 时 consumer 名唯一且共享前缀带序号 1..3 且 count/block/min_idle 全透传、concurrency=0/-2 与 count=0 参数化 3 例均 ValueError（启动期 fail-loud）、两 consumer count=3 对 6 条 queued 新任务确定性各处理 3 条且全部 ACK、无重复、PEL 清空、6 job 全 completed（FakeStreamsRedis 按 group new_index 推进天然模拟多 consumer 分片）。后端测试合计 **747 全绿（741→747，+6）**，eval 101/101（golden 21/21），ruff 净，前端零改动；Alembic 头仍 **0038_sweep_tick_claims**、业务物理表仍 **60**（无迁移；新 env `LOOM_EXPORT_WORKER_CONCURRENCY` 默认 1、`LOOM_EXPORT_STREAM_COUNT` 默认 20，默认值保持 V1 单 worker 行为）。**边界**：真 Redis 多 consumer/多副本分片与 XCLAIM 接管集成验证留 V2（当前 FakeStreams 内存替身）。
- **纯文档/实测片（Q153，2026-09-21，docs/10 第二轮 DBA 全量列复核，零代码/零迁移/零测试新增）**：把首轮（head 0036/58 表）之后新增的 0037 restock_claims、0038 sweep_tick_claims 两 fence 表纳入全量列复核——一次性 pgvector/pgvector:pg16 容器 `alembic upgrade head` 到 0038 后导出 information_schema.columns / pg_indexes / pg_constraint，并 `import app.main` 注册全量 ORM 后用 Base.metadata 逐表逐列比对，实测 **db=60/orm=60（无单边表）、637 列（首轮 627＋两表各 5 列闭合）、212 索引（首轮 210＋两表各 1 主键索引）、PK60/FK32/UQ20（FK+UQ=52 与首轮持平，两新表各仅 1 主键无 FK/UQ）、列漂移表数=0**，两新表五列/pkey 与 docs/10 §2.8 逐字一致；结论回填 docs/10 头部/§2/§4。后端测试合计 **747 不变**（纯文档实测，不新增测试），eval 仍 101/101。
- **集成层补登（Q154，2026-09-21，真 Redis/真 PG 多副本锁易主→中止/拒写集成验证，零生产代码/零迁移）**：新建 `tests/integration/test_real_infra.py` **8 例、函数级 skipif、无 env 全 skip 不进 CI 常规路径**——env 门 `LOOM_TEST_REAL_PG_DSN`（asyncpg DSN）门 4 例、`LOOM_TEST_REAL_REDIS_URL` 门 4 例，key/流名带 uuid 收尾删除可反复跑。真容器（pgvector/pgvector:pg16 宿主 55433 + redis:7 宿主 6380）带 env **8/8 全过（52.6s）**、无 env **8 skipped**：真 PG 两独立连接池跨连接认领/升级与条件 UPDATE 拒旧 fence（restock/sweep 参数化）、真并发行锁串行化（A 的 fence_current 持行锁未提交时 B 同行 UPDATE 阻塞、A rollback 后 B 才 acquired）；真 Redis leader_lease SET NX 互斥/INCR 严格 +1 单调/fence 序列键无 TTL、短 TTL(0.6s) 易主后看门狗置 lost＋raise LockLost＋安全释放不误删新主锁、Streams 两 consumer `XREADGROUP '>'` 对 6 条各分 3 条不重叠/XACK=3/XCLAIM(idle=0) 接管投递计数升 2/move_to_dead 死信附 `_dead_reason`/`_dead_origin_id`、pub/sub 跨连接 PUBLISH '*' 投递且 subscriber 收到触发 reload。**真容器立即兑现价值、抓到 sqlite/Fake 替身覆盖不到的差异**：restock.request_id 为 varchar(36)，初版测试 key 带前缀达 49 字符撞真 PG `StringDataRightTruncation`（sqlite 不强制 varchar 长度故替身从不报错），统一改 32 字符 `uuid.uuid4().hex`（同时满足 lock_name varchar64）后全过；唯一失败是测试数据问题、**零生产代码改动**。**默认收集口径**：后端常规全量＝**747 passed + 8 skipped**（real-infra 默认 skip，总收集 755），带两 env＝**755 passed**；eval 仍 101/101（golden 21），ruff 净，前端零改动；Alembic 头仍 **0038**、业务物理表仍 **60**。**替身边界升级**：Q143/Q151/Q152 共同挂账的「真 PG/真 Redis 多副本易主→中止/拒写」关键原语语义已由真容器验证（不再是全 Fake 内存替身）；**仍挂 V2** 的仅为真多副本多 worker 进程持续负载端到端部署演练（本片在单进程内用两独立连接复现全部原语语义）、restock 消费组水平并行（花钱隔离需重裁 Q87/Q89）。复跑方式见 docs/17 §7。
- **集成层补登（Q155，2026-09-21，台内白名单卡片 6 层原料包全量 JSON，零迁移）**：新建 `tests/integration/test_fcw_material_api.py` **3 例**——green path 反解析六路齐六层（product=PwsSnapshot、platform=PcpWeightTable+PublishSlot、strategy/structure/expression=三包 csp/cstp/cep、compliance=CcrReport）、含已批准 LawReview 的合规层、未知 final_id 404；fixture 本文件自带（跨文件只 import helper/常量，避免同名 fixture ruff F811）。后端测试合计 **750（747→750，+3 集成）**，eval 仍 101/101（golden 21），ruff 净，前端零改动；Alembic 头仍 **0038**、业务物理表仍 **60**。
- **集成层补登（Q156，2026-09-21，客户批量回填服务端 CSV 上传，零迁移/零新依赖）**：新建 `tests/integration/test_effect_backfill_upload_api.py` **7 例**——happy（BOM/双引号转义/稀疏指标/纯空行/偏移时区）、(content_id,captured_at) 幂等覆盖+异点追加、逐行全坏聚合一次回 422（不首错即停）、naive/纯日期 captured_at 拒、表头缺列/未知列/空文件/仅表头、unknown/discarded/他租户成品整批 422、行数上限（monkeypatch `backfill_upload_max_rows=2`）；fixture 自带 c1(t1,ready)/c2(t1,discarded)/c3(t2,ready) 三成品。后端测试合计 **757（750→757，+7 集成）**，eval 101/101，ruff 净，前端零改动（岛切换挂 V2）；头仍 **0038**/表 **60**。
- **集成层补登 + 端到端演练（Q157，2026-09-21，真多副本多 worker 持续负载部署演练 + XREAD BLOCK 超时缺陷修复，零迁移）**：`tests/integration/test_redis_streams.py` 由 8→**12 例（+4）**——内置 TimeoutError 与 redis.exceptions.TimeoutError 两类在 block 模式归一返回空、非 block 模式两类均 fail-closed 抛 StreamBackendError（新增 `_XReadTimeoutClient` 替身固定抛异常、顶部 `import redis`）。后端常规测试合计 **761 passed + 8 real-infra skipped**（757→761，+4 集成；带 LOOM_TEST_REAL_PG_DSN/LOOM_TEST_REAL_REDIS_URL 两 env 时 real-infra 8 例另计），eval 101/101（golden 21），ruff 净，前端零改动；Alembic 头仍 **0038_sweep_tick_claims**、业务物理表仍 **60**。**端到端部署演练不进 CI**（同 Q147 staging 彩排/Q154 真容器测试，本地手动）：`infra/ha-rehearsal.sh`（overlay `infra/docker-compose.ha.yml`，backend 2 副本 + 真 PG/Redis、synthetic 不花钱）四阶段 **16/16 PASS**——迁移咨询锁串行（合计 38 条/一副本 no-op）、24 导出 job 两 consumer 分片 12/12 且 tick skipped=0、SLA/restock 手工 /run 抢 leader 锁 200/409 无 5xx、配置广播对端 reload 命中并还原；演练抓到 FakeStreams 测不到的 XREAD BLOCK 超时误判缺陷（redis-py 8.1.0 抛 redis.exceptions.TimeoutError 被当后端故障），复跑与断言明细见 docs/17 §7.1。
- **端到端演练（Q158，2026-09-21，异机备份恢复演练，零迁移/零生产代码/零新增 pytest）**：纯 infra shell 演练脚本 `infra/restore-rehearsal.sh`（本地手动、**不进 CI**，同 Q147/Q154/Q157 惯例），不起应用、不花 LLM 费用。两个独立一次性 pg16 容器（源 55440/异机 55441，不触 atlas-pg）：源库全链 `alembic upgrade head` 到 0038 + 造 tenants/export_jobs/effect_records 共 8 行业务数据（覆盖 JSONB/timestamptz/可空 metrics），用 Q146 同款 `pg_dump --format=custom --compress=9` 备份，异机 drop/create 空库后 `pg_restore --no-owner` 恢复。**bash 层 12/12 PASS**（含源/异机 12 项比对子断言：alembic_version 均 0038、业务表 60/总 61 集合一致、全表逐表行数零差异、结构 indexes=213/PK=61/FK=32/UQ=20/vector 列=2 一致、关键表整行逐字段一致、config_items 61/QC 0.85 一致、import app.main ORM 只读成功、恢复库再 upgrade head 幂等 no-op）。后端常规测试数**不变＝761 passed + 8 real-infra skipped**（本片不改 backend/app、不加迁移、不加 pytest），eval 101/101、前端零改动；Alembic 头仍 0038/业务物理表 60。复跑与断言明细见 docs/17 §7.2。
- **纯前端片（Q159，2026-09-22，客户批量回填岛切服务端 CSV 上传，零后端测试/零迁移）**：仅改 frontend（`content/actions.ts` server action、`backfill-batch-island.tsx`、`lib/api.ts`、zh-CN 文案、`scripts/check-content.mjs` 守卫扩），后端 pytest 数不变；前端门禁仍 tsc＋八 checker（check-content 静态断言新端点与逐行错误渲染）。
- **集成层补登（Q160，2026-09-22，客户批量回填 Excel .xlsx 服务端上传，零迁移）**：新建 `tests/integration/test_effect_backfill_excel_api.py` **8 例**——happy（openpyxl read_only 解析活动 sheet、单元格归一化、持久化＋逐行回执）、幂等覆盖/异点追加、坏行全收集 422 不落库、纯日期单元格（naive）逐行拒、表头/空文件错、非法 base64/非 xlsx 422、unknown/discarded/他租户 422、行数硬上限（复用 `LOOM_BACKFILL_UPLOAD_MAX_ROWS` 默认 10000）422。
- **集成层补登（Q161，2026-09-22，客户批量回填异步导入任务，迁移 0039 import_jobs）**：新建 `tests/integration/test_imports_api.py` **8 例**（门控关同步 csv/xlsx completed、同步校验失败 failed+errors、unknown 成品 failed 不孤儿、载体/payload 不匹配 422、列表/详情租户+content 限定、门控开异步 queued 携 payload、入流失败 failed+503）＋`tests/integration/test_import_worker.py` **9 例**（新 csv job completed+ACK、xlsx job、确定性校验失败 failed+ACK 不重试不进死信、崩溃 pending XCLAIM 接管、终态重复幂等 ACK、不可见未超限留 PEL、超限 move_to_dead、基础设施失败未超限留 PEL、超限死信并置 failed），**共 +17**，全 FakeStreams 内存替身。
- **集成层补登（Q162，2026-09-22，客户合规风控页扩展三件套，零迁移）**：新建 `tests/integration/test_compliance_history_api.py` **5 例**（空租户 200 空/分页过滤/详情租户收窄 404/SLA 派生 sla_remaining_seconds+sla_state/词库 active+level+layer 过滤），全内存 sqlite。
- **集成层补登（Q163，2026-09-22，客户 Onboarding 前端引导页，零迁移）**：`tests/integration/test_tenants_api.py` 新增 `test_customer_tenant_onboarding_reflects_intakes` **1 例**（原 `test_customer_tenant_read` 改写断言 CustomerTenantView 含 onboarding 字段且反映 intakes 数），+1 净增。
- **集成层补登（Q165，2026-09-22，FCW 组装异步 worker，零迁移）**：新建 `tests/integration/test_fcw_worker.py` **11 例**（门控关同步回归/门控开 queued 不组装/worker 消费到 completed+ACK/queued→running→completed 状态流转/终态重复投递幂等 ACK/per-slot 失败隔离任务仍 completed/幽灵 task 直接 ACK/入流失败 503 无残留/基础设施异常留 PEL/超限死信+failed/worker 路径 fcw.assembly_blocked 审计完整），全 FakeStreams 内存替身。
- **基线刷新（2026-09-22，Q162–Q165 后）**：后端常规 **803 passed + 8 real-infra skipped**（786→803＝Q162 +5、Q163 +1、Q165 +11＝+17，Q164 纯前端 0；总收集 811），eval 101/101、ruff 净、前端 tsc＋八 checker 全绿、token 86；Alembic 头 **0039_import_jobs**、业务物理表 **61**（Q162–Q165 均零迁移）。
- **集成层补登（Q166，2026-09-22，客户效果 analytics 只读聚合，零迁移）**：新建 `tests/integration/test_effect_analytics_api.py` **5 例**——未知租户 200 空且不写审计、稀疏六计数（plays/likes/comments/shares/inquiries/conversions）按存在键累加＋read_rate 仅对含键记录平均（round 4、分母 read_rate_samples）＋by_content 按 plays 降序 top 50、租户隔离排除 orphan 与他租户记录、日期窗口过滤（date_to 含当天＝排他上界次日 00:00 UTC）＋from>to 422、缺 tenant_id 422，全内存 sqlite。后端测试合计 **808（803→808，+5 集成）**；Q167/Q168 纯前端消费既有端点、零后端测试。
- **基线刷新（2026-09-22，Q166–Q168 后）**：后端常规 **808 passed + 8 real-infra skipped**（803→808＝Q166 analytics +5；Q167 Agent Key 治理页、Q168 导出 job 管理页均纯前端消费既有 Q88/Q132/Q137 端点、零后端测试；总收集 816，带两 env 816 全过），eval 101/101（dataset 80＋golden 21）、ruff 净、前端 tsc＋八 checker＋next build 全绿、token 86；Alembic 头仍 **0039_import_jobs**、业务物理表 **61**（三片均零迁移）。
- **e2e 层新建（Q169/Q172，2026-09-22，全链 golden path，零迁移）**：新建 `tests/e2e/test_fullchain_golden_path.py` 两例——synthetic 全链（sqlite 内存入常规 pytest；真 PG 由 `LOOM_E2E_PG_DSN` 接一次性迁移库）与真 agnes 全链 gated（默认 skip，需 `LOOM_E2E_REAL_LLM=1`+`LOOM_E2E_AGNES_KEY`）；一个产品沿真实 API 走段1→13 并断言 final_id/content_id/matched=1。后端常规合计 **809 passed + 9 skipped**（808→809＝synthetic e2e +1；skip 8 real-infra＋1 real-LLM e2e；总收集 818）；真 PG 演练经 `infra/fullchain-rehearsal.sh`（本地手动不进 CI，两 e2e 46.3s 全过）。
- **集成层补登（Q173，2026-09-23，持续高压负载演练 + jobs 列表 offset 缺口修复，零迁移）**：新增分页集成测试 `test_list_jobs_paginates_with_limit_and_offset` **1 例**（3 job、limit=2 两页不重叠且并集闭合、响应回显 offset）；纯 infra 演练脚本 `infra/load-rehearsal.sh` 本地手动不进 CI（同 Q147/Q157/Q158 惯例）。后端常规合计 **810 passed + 9 skipped**（809→810＝offset 分页 +1；总收集 819），eval 101/101、ruff 净、前端零改动；Alembic 头仍 **0039_import_jobs**、业务物理表 **61**（零迁移）。正式 5 分钟演练 9/9 PASS：562 export job 提交==completed、零 failed/5xx，`/run` 200=414/409=1842，两副本 healthy/零重启/无 OOMKill/无 Traceback；复跑与断言明细见 docs/17 §7.3。
- **Q174/Q175 零后端测试增量（2026-09-23，零迁移）**：Q174 前端批量回填/Excel 导入岛切异步任务轮询为纯前端（消费 Q161 已落 imports jobs 契约）、Q175 RPO·RTO 量化认证为 infra shell 计时演练（`infra/rpo-rto-rehearsal.sh`，本地手动不进 CI，同 Q158 先例），两片均不新增后端 pytest，常规基线维持 **810 passed + 9 skipped**；前端 tsc＋八 checker＋next build 全绿。

- **集成层补登（Q177，2026-09-23，D3.5 管理端白名单卡片只读首片，零迁移）**：新建 `tests/integration/test_fcw_admin_api.py` **7 例**——列表口缺 actor 422、OWNER/NOBODY 越权 403（parametrize）、operations 跨租户见 2 条＋tenant_id 过滤＋platform_admin 可读、limit/offset 分页不重不漏＋limit=201 判 422、内嵌六层口 operations/platform_admin 200 且六层齐全且**无 content-disposition 下载头**、material 越权 403/缺 actor 422/未知 id 404；全内存 sqlite（造数复用 test_fcw_api helper，同库多 FCW 用不同 publish_slots.code 避全局唯一冲突）。Q176 ATOM-AFFINITY KNN 索引经 grounding 判定应用层零 DB 层 KNN 查询、裸建为无消费方死索引，**暂缓待负责人拍板、零代码零迁移零测试**（02 C1.120）。后端常规合计 **817 passed + 9 skipped**（810→817＝Q177 +7；总收集 826），eval 101/101（golden 21）、ruff 净、前端 tsc＋八 checker＋next build 全绿、token 仍 86、客户 nav 仍 8；Alembic 头仍 **0039_import_jobs**、业务物理表 **61**（零迁移）。

- **集成层补登（Q178，2026-09-24，身份层甲案内部运营 PAT，迁移 0040 staff_api_keys）**：新建 `tests/integration/test_staff_auth_api.py` **25 例**——纯函数（token 前缀区分 `loom_staff_`/`loom_`、normalize_roles 去重保序、空/未知/客户角色 whitelist_owner→422）、服务层（issue/list/revoke、SHA-256 哈希、明文仅签发回一次、命中记 last_used_at、审计 `staff_api_key.issue/revoke`、tenant=`_platform`）、**门控关引导**（自报 platform_admin 签发首个令牌、`/api/auth/me` 门控关 400、agent-keys GET 门控关有意免闸）、**门控开**（无令牌打内部口 401 NotAuthenticated、坏/吊销 staff 令牌 401、角色不足 403 PermissionDenied、持低权令牌时 query 自报 platform_admin 提权失效且 actor 被覆盖为令牌 staff 身份、审计落真实 staff_id、机器 `loom_` 前缀打 effect-callback/A2A 与客户 whitelist_owner 口/公开口无令牌均放行不受 staff 门控影响、internal_gate 门控关 no-op/门控开要求 staff、auth.me 有效回显/无令牌 401）。**关键夹具**：内存 sqlite 用 `StaticPool`（check_same_thread=False）共享同一连接，同时 override `get_session` 与 `get_auth_session` 两个会话依赖（认证走独立短事务）；门控时序——先在门控关 `_bootstrap` 自报签发令牌，再 `monkeypatch.setattr(settings, "staff_auth_enabled", True)`（settings 为 db.py 模块级单例，rbac/deps/router 均 import 同一对象，函数级自动还原防跨测泄漏），切勿在 fixture setup 阶段提前开闸导致引导签发得 401。后端常规合计 **842 passed + 9 skipped**（817→842＝+25；skip＝8 real-infra＋1 real-LLM e2e；总收集 851），eval 101/101（golden 21）、ruff 净；迁移 0040 经 pg16 容器（宿主 55442）up/downgrade-1/up 实测（不进 pytest）。前端 tsc＋八 checker（check-admin 扩 staff-keys/login 专属守卫段＋后端 staff_auth 路径断言、sidebar navCount 11→12）＋next build 全绿、token 仍 86、客户 nav 仍 8。门控默认关＝V1 自报行为不变。

- **Q179 零后端测试增量（2026-09-24，零迁移；第四轮 DBA 全量列复核）**：Q179 为一次性 pg16 容器（loom-dba4/宿主 55448）`alembic upgrade head` 到 0040 后的只读列复核（同 Q171 先例），不新增 pytest，后端常规基线维持 **842 passed + 9 skipped**（总收集 851）；结论 62 业务表/666 列/219 业务索引/PK62·FK32·UQ21，模型⇄迁移列名零漂移。
