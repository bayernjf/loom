# 17. 部署与运维

> **状态**：🟢 已定稿（选型于 2026-09-13 拍板、技术栈同日确认；可作环境搭建依据）
> **来源**：07_技术架构与工程规格.md §2.6（多租户）/§6.3（系统管理）｜09_全景 §D3.11（租户/计费/审计）｜14_技术选型决策.md（✅ 已定稿）｜15（技术栈）｜08_迭代计划（V1 5-10 客户 → V3 100-200 客户）
> **用途**：环境、CI/CD、多租户部署、密钥与安全、监控与成本治理的运维说明；AI 据此搭建发布与运行环境。
> **说明**：计费数值来自原文（09 D3.11 套餐价）；具体版本号为 2026-09-13 选型落地建议，小版本随脚手架确定。

---

## 1. 环境与发布

| 环境 | 用途 | 数据 | 说明（建议） |
|---|---|---|---|
| dev | 开发联调 | 样例数据（16 测试数据） | 本地/单实例 |
| staging | 验收 + Evaluation Dataset 回归 | 脱敏数据 | 发布前闸门 |
| prod | 正式（SaaS 多租户） | 生产数据 | 隔离实例或强隔离 |

- **技术栈与中间件（14 定稿 + 15 技术栈）**：应用 Python 3.12 + FastAPI 单进程模块化单体；数据 PostgreSQL 16（+pgvector）/ Redis 7（缓存；多副本 leader 锁：`LOOM_DISTRIBUTED_LOCK_ENABLED=true` 开启，Q89，默认关；restock 瞬态退避 `LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`LOOM_RESTOCK_BACKOFF_MAX_SECONDS=1800`/`LOOM_RESTOCK_BACKOFF_MAX_ATTEMPTS=10`，Q90；DAG 编排同层并发上限 `LOOM_ORCH_MAX_CONCURRENCY=4`，Q91；Streams 异步队列后置 V2）/ S3 兼容对象存储；前端 React + Next.js（Node 运行时仅构建/SSR）；编排定义为 YAML（无独立编排服务）。V1 用 docker-compose 单机即可承载 5-10 客户，K8s/多实例随 V2 客户量评估。M12 驾驶舱只读端点（Q92，`/api/admin/dashboards/token-cost|review-workload`，platform_admin）为 skill_runs/skill_candidates/ops_todos 三表实时 SELECT 聚合，不引入环境变量、后台调度任务或物化表。M12 统一审核工作台（Q93，`/api/review-workbench/candidates|batch-approve`）为 skill_candidates/skill_runs/c1_industry_thresholds 实时 SELECT+内存风险归一并发，新增配置键 `review.batch_pass_confidence`（0.85，迁移 0021 纯配置种子幂等，无业务表 schema 变更），同样不引入环境变量、后台调度任务或物化表。M12 候选审核 SLA 挂接（Q94）复用既有 Q49 sweep/SweepScheduler，无新调度任务；五型时限为配置键 `review.sla_hours.{pwc_combo,field_plan,c1_recognition,atom_batch,c7_layer4}`（种子 72h【待补】，迁移 0022 纯配置种子，无业务表 schema 变更），不引入环境变量或物化表。租户注册表（Q95）为普通业务表 `tenants`（迁移 0023 建表+存量 DISTINCT tenant_id 回填 basic/active，应用层准入无硬外键），无新环境变量、后台调度任务或物化视图；多租户仍为共享实例+tenant_id 逻辑隔离口径（5-10 客户单机），物理隔离/备份分租户等随 V2。SLA 黄色窗口配置化（Q114）：黄判由 created_at+固定小时改为 due_at − 单一配置键 `sla.yellow_hours`（int，种子 24，配置中心可热更），有黄窗的待办类型扩为五型 review_*（法审 law_review due=48h 数值不变）。G2 通用字段工程定稿（Q115）落迁移 0024_g2_common_fields（12 个 cat='common' 字段种子，ON CONFLICT DO NOTHING 幂等）。V2 P4 段12 文章生成首片（Q116）落迁移 0025_content_products（content_products 新业务表）/0026_article_gen_seed（ARTICLE-GEN 编排种子）与 `app/content/` 模块，无新环境变量、后台调度或物化表。Q118 落迁移 0027_product_space_fks（product_spaces 两道 nullable 软引用补 DB 级外键：NULL 合法、孤儿 INSERT/UPDATE 拒写），并顺带修复 Alembic 版本表版本号列长过窄缺陷。V2 P4 段12 多语言（Q119）落迁移 0028_content_languages（新建 content_languages 语言清单表含 zh-CN 全市场种子、product_spaces 加 nullable target_languages JSONB、content_products 加唯一约束 uq_content_final_language_kind(final_id,language,kind)）。AI 质量分（Q120）落迁移 0029_content_quality（content_products 加 nullable quality_score Float 与 quality_issues JSONB 两列）与 0030_article_qc_seed（模型网关第 8 场景 ARTICLE-QC→synthetic 路由及 SkillPrompt/PromptVersion v0.1 纯种子，不建表）。复检第②项语义级检测（Q121，纯 advisory）落迁移 0031_article_semantic_seed（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK→synthetic 路由及 SkillPrompt/PromptVersion v0.1 纯种子，不建表），均无新环境变量、后台调度或物化表。Q122 客户内容页（列表/详情/审阅/Q56-a 人工编辑，新事件 manual_resubmit）与 Q123 段1 客户目标语言控件（客户 PATCH + active 语言清单 GET）均为**零迁移**（复用既有 content_products / content_languages / product_spaces 表）。Q124 难产骨架作废回池（Q56-b）落迁移 0032_content_discard（content_products 加 nullable discard_reason Text；普通唯一约束 uq_content_final_language_kind 改为 **partial unique index `WHERE status <> 'discarded'`**，模型层 postgresql_where + sqlite_where 双方言，作废后同 final_id/language/kind 可重新生成=回池；downgrade 还原普通唯一索引、不处理同键冲突，V1 无生产数据）。Q125 运营发布回填（Q60c）落迁移 0033_content_publish（content_products 加 nullable published_url Text / platform_post_id String(128) / published_at timestamptz 三列，published_at 非空即已发布信号、不新增 published 态；downgrade drop 三列），均无新环境变量、后台调度或物化表，业务物理表仍 55（纯加列/换索引）。段13 反馈回流入站第一片（Q126，Q60）落迁移 **0034_effect_records（新增 effect_records 1 表）**：外部 Agent 经 `POST /api/effect-callback`（Q88 Bearer 验签，首个受 Agent Key 保护业务端点）推送效果时序，按 content_id 匹配非 discarded 成品分流 matched/orphan、(external_content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏不补 0、整批 all-or-nothing，运营只读孤儿队列与成品时序；无新环境变量、后台调度或物化表，业务物理表 55→**56**。段13 续片 Q127（Q60a 孤儿人工认领，2026-09-19）落迁移 **0035_effect_claims（新增 effect_claims 1 表：external_content_id PK→content_id FK 的持久认领映射 + claimed_by/claimed_at；effect_records 另加 claimed_by/claimed_at 两 nullable 溯源列，不新增第三状态）**：`POST /api/admin/effects/claims`（operations 写口硬闸、actor 在体）认领后同 external_content_id 后续推送（覆盖/新采集点）经映射兜底一律 matched 不回落孤儿，审计 effect.claimed（tenant `_platform`），业务物理表 56→**57**；Q128（customer-backfill 客户专用通道，2026-09-19）为 `POST /api/effects/backfill`（无 Agent Key、body {tenant_id,records,actor}、source 服务端固定、只命中本租户非 discarded 成品否则整批 422、绝不产生孤儿、审计 effect.customer_backfilled tenant=客户租户），**零迁移**。段13 操作面收口三片 Q129（批量认领 `POST /api/admin/effects/claims/batch` 整批 all-or-nothing、取消认领/解绑 `POST /api/admin/effects/claims/unclaim` 删映射并回滚旧目标行，审计 effect.claim_revoked，纯后端零迁移）、Q130（管理端 `/admin/effects` 效果回流运营台，sidebar 第 8 项，纯前端）、Q131（客户内容详情页效果回填岛消费 Q128 通道，纯前端）均于 2026-09-20 落地，**三片均零迁移**、无新环境变量、后台调度或物化表；check-admin/check-content 仅扩守卫，CI checker 集合不变。中台导出 JSON 形态与异步导出任务（Q132，2026-09-20）落迁移 **0036_export_jobs（新增 export_jobs 1 表：导出任务记录 job_id PK/tenant 索引/product_space_id 可空/format/status server_default completed/row_count/file_name/requested_by/error/created_at/completed_at，不存文件 payload、下载按任务参数重渲染；V1 任务请求内同步置 completed，queued/running 真后台 worker 随 V2 Redis Streams）**，业务物理表 57→**58**，无新环境变量、后台调度或物化表。**当前迁移头：0036_export_jobs（2026-09-20，down_revision=0035_effect_claims；Q128–Q131 均零迁移）**；迁移一律按 MIGRATION_CONVENTION 经 pgvector/pg16 容器一次性 up / downgrade-1 / up 实测，不进 pytest。
- **CI/CD 流程**：提交 → 单元/契约测试（16）→ 构建 → staging 部署 → Evaluation Dataset 回归（防红旗 S2"Skill 从未运行"）→ 人工 Gate → prod
  - **Evaluation 回归命令（M10-Q 实现补登，Q77）**：staging 闸门执行 `backend/.venv/bin/python eval/runner.py`（仓库根目录），扫描 `eval/datasets/skills/` 与 `eval/golden_cases/skills/` 全部 YAML 案例，任一失败/错误退出码非零即阻断发布；案例集与 target 契约见 `eval/README.md`。**CI workflow 已落地（Q111，2026-09-16，GitHub Actions；Q118 扩为七 checker、Q122 扩为八 checker）**：`.github/workflows/ci.yml` 在 push main/dev 与 pull_request 时跑两 job——backend（Python 3.12，仓库根 `pip install -e "./backend[dev]"`；working-directory=backend 下 `ruff check .` → `python -m pytest -q`（sqlite、无服务容器，当前 618 测试全绿）；仓库根 `python eval/runner.py` 跑 Q77 的 101 案回归；零 secret、零外部服务）与 frontend（Node 20，working-directory=frontend，`npm ci` → `npm run typecheck` → 八 checker：tokens/nav/content/products/workbench/compliance/admin/settings，前六个 Q96–Q102 落地、settings 为 Q114 新增并于 Q118 接入 CI、**content 为 Q122 新增（客户内容页消息/文件/岛纪律/后端防漂移守卫；Q131 扩回填岛守卫）；admin 于 Q130 扩 effects 运营台守卫（sidebar 7→8 项），checker 文件集合不变**）；守卫单测 `backend/tests/unit/test_ci_workflow_contract.py` 静态锁定 workflow 结构（触发/双 job/命令/零 secret），Q118 增「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除 checker 漏接，删闸即挂。不含 `ruff format --check`（历史文件尚未全量格式化）、`next build` 与 lint（均非既定门槛；Q122/Q123 切片本地另行 `npm run build` 实测通过）与 CD（部署仍随本流程后续阶段）。**已实际运行**：2026-09-16 落地至 2026-09-18 累计 24 次 run（dev push / pull_request / main merge，含 PR #42–#45）全部 success；最新 main run 35253008023（2026-09-17，Q118 随 PR #45 合并）；Q119–Q128 提交已由负责人 push（dev 同步于 a6908ac），Q129–Q131 提交本地 dev 未 push，尚无新 run。
- **版本与回滚**：应用版本 + PWS/FCW 快照版本分离；回滚中心（09 D3.12 Rollback Center）覆盖模板/原子/规则/Skill/Prompt
- **发布纪律**：AI 相关变更（Skill/Prompt/权重）必须可回滚、可审计（A4 Prompt 治理 + Q63 KUP 回滚）

---

## 2. 多租户部署

| 项 | 说明 | 来源 |
|---|---|---|
| 隔离模型 | V1 共享实例 + tenant_id 强隔离（Q33）；客户量大后评估独立实例 | Q33 / 14 §2.3 |
| 数据隔离 | 业务表 tenant_id；知识层共享表无 tenant_id（平台级） | 10 §1.1 |
| 互不感知 | 跨客户不查重、永不驳回、客户看不见彼此 | Q33 |
| 套餐/试用/续费/暂停 | 租户管理 + 计费（Basic $999 / Pro $2999 / Enterprise $9999，D3.11） | 09 D3.11 |
| Onboarding | 注册→套餐→团队→第一产品建模→上线（D3.11） | 09 D3.11 |

---

## 3. 密钥与安全

- 密钥管理：API Key 一 Agent 一 Key 可吊销（Q60b/Q67）；**密钥不入库、不进文档**（本项目 AGENTS.md 红线）
- 访问控制：RBAC 8 角色 + 权威扩展（07 §2.3）；红线操作（PWS 冻结/原子正式化/合规废弃/Prompt 修改）= 角色 + 人工 Gate（line 7674）
- 审计：writeAudit append-only（所有人工操作 + AI 调用）
- 外部回调安全：effect-callback 鉴权 + 限流 + 幂等（11 §2.1/§3）
- 脱敏：AG-SUPPORT 上线前脱敏审核（Q66）；staging 数据脱敏

---

## 4. 监控与成本治理

| 监控项 | 内容 | 来源 |
|---|---|---|
| 应用监控 | 接口错误率/延迟/SLA 引擎超时（24h 黄/48h 升级） | Q49 |
| token 成本 | 10 条省 token 策略落地验证 + Token 成本看板（系统管理员） | 09 D3.9 / D7.2 |
| 模型单价 | 统一 per-1M 口径（Q67）；日预算 Opus 50 / Sonnet 30 / Haiku 10 美元 | line 2103 |
| 反馈校准 | 两张常设月度校准报表（FCW score vs 互动、AI 质量分 vs 互动），分档样本 ≥50 提示可校准 | Q65 |
| 治理看板 | 6 个治理驾驶舱（知识库健康/Skill 质量/Token 成本/白名单表现/平台规则/人工审核） | 09 D3.9 |

---

## 5. 备份与恢复（建议）

- 数据备份：关系库每日全量 + WAL 增量；对象存储（素材/成品）跨区冗余
- 快照不可变：PWS/SkillRunLog/审计 append-only，备份需支持历史版本追溯（Q31 全保留）
- 灾难恢复：RPO ≤ 24h / RTO ≤ 4h【建议】，随 V2/V3 客户规模收紧

---

## 6. 与既有文档的核对项

- [x] 部署形态与 14 选型一致（模块化单体/共享实例起步，PG+pgvector+Redis，引用 14 §2.1/2.3）
- [x] 监控项覆盖 09 §7 成本治理、Q65 校准报表、Q67 单价口径
- [x] 无密钥、令牌、敏感信息出现在文档中（AGENTS.md 红线）
- [x] §1 具体环境/中间件已补（2026-09-13：FastAPI / PG16+pgvector / Redis7 / S3 / Next.js；选型定稿后）
