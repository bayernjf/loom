# 17. 部署与运维

> **状态**：🟢 已定稿（选型于 2026-09-13 拍板、技术栈同日确认；可作环境搭建依据）
> **来源**：07_技术架构与工程规格.md §2.6（多租户）/§6.3（系统管理）｜09_全景 §D3.11（租户/计费/审计）｜14_技术选型决策.md（✅ 已定稿）｜15（技术栈）｜08_迭代计划（V1 5-10 客户 → V3 100-200 客户）
> **用途**：环境、CI/CD、多租户部署、密钥与安全、监控与成本治理的运维说明；AI 据此搭建发布与运行环境。
> **说明**：计费数值来自原文（09 D3.11 套餐价）；具体版本号为 2026-09-13 选型落地建议，小版本随脚手架确定。

---

## 1. 环境与发布

| 环境 | 用途 | 数据 | 说明（建议） |
|---|---|---|---|
| dev | 开发联调 | 样例数据（16 测试数据） | 本地/单实例 |
| staging | 验收 + Evaluation Dataset 回归 | 脱敏数据 | 发布前闸门 |
| prod | 正式（SaaS 多租户） | 生产数据 | 隔离实例或强隔离 |

- **技术栈与中间件（14 定稿 + 15 技术栈）**：应用 Python 3.12 + FastAPI 单进程模块化单体；数据 PostgreSQL 16（+pgvector）/ Redis 7（缓存；多副本 leader 锁：`LOOM_DISTRIBUTED_LOCK_ENABLED=true` 开启，Q89，默认关；restock 瞬态退避 `LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`LOOM_RESTOCK_BACKOFF_MAX_SECONDS=1800`/`LOOM_RESTOCK_BACKOFF_MAX_ATTEMPTS=10`，Q90；DAG 编排同层并发上限 `LOOM_ORCH_MAX_CONCURRENCY=4`，Q91；**多副本 V2 基建三件（2026-09-20 Q133–Q135，均默认关/不接线、零迁移）**：①Q133 fencing token 随 leader 锁落地——抢锁成功后 `INCR loom:fence:{name}` 取跨易主单调令牌（序列键不设 TTL），`leader_lease()` 看门狗易主/续约故障置 lost，INCR 失败 fail-closed，下游 PG 行级 fence 条件更新随 V2 写者；②Q134 Redis Streams 消费组细粒度认领原语已落 `app/core/queue/`（XADD 近似 MAXLEN/XGROUP mkstream 幂等/XREADGROUP `>` 读即入 PEL/XACK/XPENDING+XCLAIM 崩溃接管/超限进死信流，`StreamBackendError` fail-closed），原语片当时只落原语；**restock requested 信号 XADD 生产侧已由 Q138 接线**（`LOOM_RESTOCK_STREAM_ENABLED=false` 默认关，after_commit best-effort 入流、DB requested 行为唯一事实源、DB 轮询兜底，worker 主认领仍走 DB 轮询；消费组水平并行因 restock 花钱须 leader 单实例防双花而留 V2 重评隔离）；③Q135 配置缓存多副本失效广播（Redis pub/sub 频道 `loom:config:invalidate`，发布侧 after_commit fire-and-forget PUBLISH 为 best-effort、订阅侧 ConfigBroadcastSubscriber 收消息 `*` 全量 reload、具体 key 经 Q140 走 reload_keys 单 key 增量失效（DB 已删 key 同步从快照移除），连续故障指数退避 1s 起封顶 30s、故障懒重连），开关 `LOOM_CONFIG_CACHE_BROADCAST_ENABLED=false` 默认关，开启后 lifespan 启停订阅者、启动失败不阻断应用）/ S3 兼容对象存储；前端 React + Next.js（Node 运行时仅构建/SSR）；编排定义为 YAML（无独立编排服务）。V1 用 docker-compose 单机即可承载 5-10 客户，K8s/多实例随 V2 客户量评估。M12 驾驶舱只读端点（Q92，`/api/admin/dashboards/token-cost|review-workload`，platform_admin）为 skill_runs/skill_candidates/ops_todos 三表实时 SELECT 聚合，不引入环境变量、后台调度任务或物化表。M12 统一审核工作台（Q93，`/api/review-workbench/candidates|batch-approve`）为 skill_candidates/skill_runs/c1_industry_thresholds 实时 SELECT+内存风险归一并发，新增配置键 `review.batch_pass_confidence`（0.85，迁移 0021 纯配置种子幂等，无业务表 schema 变更），同样不引入环境变量、后台调度任务或物化表。M12 候选审核 SLA 挂接（Q94）复用既有 Q49 sweep/SweepScheduler，无新调度任务；五型时限为配置键 `review.sla_hours.{pwc_combo,field_plan,c1_recognition,atom_batch,c7_layer4}`（种子 72h【待补】，迁移 0022 纯配置种子，无业务表 schema 变更），不引入环境变量或物化表。租户注册表（Q95）为普通业务表 `tenants`（迁移 0023 建表+存量 DISTINCT tenant_id 回填 basic/active，应用层准入无硬外键），无新环境变量、后台调度任务或物化视图；多租户仍为共享实例+tenant_id 逻辑隔离口径（5-10 客户单机），物理隔离/备份分租户等随 V2。SLA 黄色窗口配置化（Q114）：黄判由 created_at+固定小时改为 due_at − 单一配置键 `sla.yellow_hours`（int，种子 24，配置中心可热更），有黄窗的待办类型扩为五型 review_*（法审 law_review due=48h 数值不变）。G2 通用字段工程定稿（Q115）落迁移 0024_g2_common_fields（12 个 cat='common' 字段种子，ON CONFLICT DO NOTHING 幂等）。V2 P4 段12 文章生成首片（Q116）落迁移 0025_content_products（content_products 新业务表）/0026_article_gen_seed（ARTICLE-GEN 编排种子）与 `app/content/` 模块，无新环境变量、后台调度或物化表。Q118 落迁移 0027_product_space_fks（product_spaces 两道 nullable 软引用补 DB 级外键：NULL 合法、孤儿 INSERT/UPDATE 拒写），并顺带修复 Alembic 版本表版本号列长过窄缺陷。V2 P4 段12 多语言（Q119）落迁移 0028_content_languages（新建 content_languages 语言清单表含 zh-CN 全市场种子、product_spaces 加 nullable target_languages JSONB、content_products 加唯一约束 uq_content_final_language_kind(final_id,language,kind)）。AI 质量分（Q120）落迁移 0029_content_quality（content_products 加 nullable quality_score Float 与 quality_issues JSONB 两列）与 0030_article_qc_seed（模型网关第 8 场景 ARTICLE-QC→synthetic 路由及 SkillPrompt/PromptVersion v0.1 纯种子，不建表）。复检第②项语义级检测（Q121，纯 advisory）落迁移 0031_article_semantic_seed（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK→synthetic 路由及 SkillPrompt/PromptVersion v0.1 纯种子，不建表），均无新环境变量、后台调度或物化表。Q122 客户内容页（列表/详情/审阅/Q56-a 人工编辑，新事件 manual_resubmit）与 Q123 段1 客户目标语言控件（客户 PATCH + active 语言清单 GET）均为**零迁移**（复用既有 content_products / content_languages / product_spaces 表）。Q124 难产骨架作废回池（Q56-b）落迁移 0032_content_discard（content_products 加 nullable discard_reason Text；普通唯一约束 uq_content_final_language_kind 改为 **partial unique index `WHERE status <> 'discarded'`**，模型层 postgresql_where + sqlite_where 双方言，作废后同 final_id/language/kind 可重新生成=回池；downgrade 还原普通唯一索引、不处理同键冲突，V1 无生产数据）。Q125 运营发布回填（Q60c）落迁移 0033_content_publish（content_products 加 nullable published_url Text / platform_post_id String(128) / published_at timestamptz 三列，published_at 非空即已发布信号、不新增 published 态；downgrade drop 三列），均无新环境变量、后台调度或物化表，业务物理表仍 55（纯加列/换索引）。段13 反馈回流入站第一片（Q126，Q60）落迁移 **0034_effect_records（新增 effect_records 1 表）**：外部 Agent 经 `POST /api/effect-callback`（Q88 Bearer 验签，首个受 Agent Key 保护业务端点）推送效果时序，按 content_id 匹配非 discarded 成品分流 matched/orphan、(external_content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏不补 0、整批 all-or-nothing，运营只读孤儿队列与成品时序；无新环境变量、后台调度或物化表，业务物理表 55→**56**。段13 续片 Q127（Q60a 孤儿人工认领，2026-09-19）落迁移 **0035_effect_claims（新增 effect_claims 1 表：external_content_id PK→content_id FK 的持久认领映射 + claimed_by/claimed_at；effect_records 另加 claimed_by/claimed_at 两 nullable 溯源列，不新增第三状态）**：`POST /api/admin/effects/claims`（operations 写口硬闸、actor 在体）认领后同 external_content_id 后续推送（覆盖/新采集点）经映射兜底一律 matched 不回落孤儿，审计 effect.claimed（tenant `_platform`），业务物理表 56→**57**；Q128（customer-backfill 客户专用通道，2026-09-19）为 `POST /api/effects/backfill`（无 Agent Key、body {tenant_id,records,actor}、source 服务端固定、只命中本租户非 discarded 成品否则整批 422、绝不产生孤儿、审计 effect.customer_backfilled tenant=客户租户），**零迁移**。段13 操作面收口三片 Q129（批量认领 `POST /api/admin/effects/claims/batch` 整批 all-or-nothing、取消认领/解绑 `POST /api/admin/effects/claims/unclaim` 删映射并回滚旧目标行，审计 effect.claim_revoked，纯后端零迁移）、Q130（管理端 `/admin/effects` 效果回流运营台，sidebar 第 8 项，纯前端）、Q131（客户内容详情页效果回填岛消费 Q128 通道，纯前端）均于 2026-09-20 落地，**三片均零迁移**、无新环境变量、后台调度或物化表；check-admin/check-content 仅扩守卫，CI checker 集合不变。中台导出 JSON 形态与异步导出任务（Q132，2026-09-20）落迁移 **0036_export_jobs（新增 export_jobs 1 表：导出任务记录 job_id PK/tenant 索引/product_space_id 可空/format/status server_default completed/row_count/file_name/requested_by/error/created_at/completed_at，不存文件 payload、下载按任务参数重渲染；V1 任务请求内同步置 completed；**queued/running 真后台 worker 已由 Q137 落地**——`LOOM_EXPORT_WORKER_ENABLED=false` 默认关，开启后 ExportWorker 经 Redis Streams 消费组异步处理（只读幂等作业可水平并行、崩溃 PEL 接管、超限进死信，阻塞节拍 `LOOM_EXPORT_STREAM_BLOCK_SECONDS=5.0`），并补 `GET /api/exports/jobs` 列表口；门控关仍走 Q132 同步路径）**，业务物理表 57→**58**，无新环境变量、后台调度或物化表。**多副本接线四件 Q137（导出 Streams 真后台 worker + 任务列表口）/Q138（restock requested 信号 XADD 生产侧）/Q139（SLA sweep、restock 两循环与两个手工 /run 由 leader_lock 换 leader_lease，作业/信号间 checkpoint=raise_if_lost 协作中止，手工 /run 锁中途丢失返 409）/Q140（配置缓存 reload_keys 单 key 增量失效 + 订阅断线指数退避）均于 2026-09-20 落地，四件全部零迁移、env 默认关、不改 V1 单副本默认行为；新增运维 env `LOOM_EXPORT_WORKER_ENABLED`/`LOOM_EXPORT_STREAM_BLOCK_SECONDS`/`LOOM_RESTOCK_STREAM_ENABLED`（按 Q135 先例不入 backend/.env.example，该文件只保留 LOOM_REDIS_DSN）；Q141 配置缓存 TTL 兜底新增 `LOOM_CONFIG_CACHE_TTL_SECONDS=300`（订阅器无失效消息超此秒数回源全量 reload，仅广播门控开启且曾成功装载时生效）、Q142 导出硬上限新增 `LOOM_EXPORT_MAX_ROWS=100000`（中台导出每页/每次最大 final_id 行数，limit 超限 422；二者均为运维防护旋钮、非 Q9 业务阈值、零迁移，同样不入 .env.example），Q143 无新 env**。**Q141–Q143（2026-09-20，02 C1.85–C1.87）**：Q141 配置缓存 per-key `ConfigItem.version` 版本向量门控（reload_keys 拒旧快照回灌）+ 订阅器 TTL 兜底、Q142 中台导出 limit/offset 分页与行数硬上限（JSON envelope 加 total/limit/offset/has_more、CSV 走 X-Export-* 响应头、同步/异步 worker/下载同一上限）均**零迁移**；Q143 落迁移 **0037_restock_claims（新增 restock_claims 1 表：request_id String36 PK/fence Integer NOT NULL/claimed_by String64 可空/claimed_at、updated_at timestamptz 默认 now——下游 PG 行级 fence 接 restock 花钱路径，花钱前 claim_request 认领、提交前 fence_current 条件 UPDATE 拒旧 leader 迟到写）**，经 pgvector/pgvector:pg16 容器 upgrade/downgrade-1/upgrade 往返实测。**private beta 上线准备片（2026-09-21，Q144–Q149）**：Q144 裁决首发形态＝受控 private beta（后端内网/网关后、平台代运营，公网自助 NO-GO，身份层压 V2 第一项）；Q145 落最小部署制品——backend Dockerfile + entrypoint（`alembic upgrade head` 先于 uvicorn）、frontend 三阶段 Dockerfile（Next standalone）、compose 增 backend（**不发布宿主端口**）/frontend（唯一发布口 3000）与 service_healthy 依赖序、`.dockerignore`（TLS 由部署环境在 frontend 口外终结）；Q146 落最小备份监控——db-backup sidecar 每日 pg_dump + 7 天保留（pgbackups 卷）、全服务日志 10m×3 轮转、watchdog 探活 + 可选 webhook best-effort 告警、frontend healthcheck（异机备份/恢复演练/指标监控随 V2）；Q147 落 `infra/docker-compose.staging.yml` overlay（`!reset` 清空 infra 宿主端口避本机 atlas 容器、minio profile 排除，`-p loom-staging` 起 6 服务全 healthy，`down -v` 清理），staging 彩排全通过；Q148 真实 LLM 接线——agnes OpenAI 兼容网关 `https://apihub.agnes-ai.com/v1`、模型 agnes-2.5-flash、计费币种 USD、日预算 50 USD/天，出站 Key 沿用 Q82 Fernet（env `LOOM_MASTER_KEY`），8 个 chat 场景切真模型、ATOM-AFFINITY 仍 synthetic（agnes 无 embedding），模型单价待供应商给（先记 token/调用数、金额 0.0）；Q149 为阈值确认与 docs/19 主数据录入模板（纯文档）。**A2A 封臣片（Q150，2026-09-21，零迁移/零新 env）**：新增三个**公开只读**发现端点 `GET /api/a2a/agent-card`、`GET /.well-known/agent-card.json`、`GET /.well-known/agent.json`（Cache-Control max-age=300，卡片 url 取 `LOOM_PUBLIC_BASE_URL`，未配置回退请求 base_url）；任务端点 `POST /api/a2a/tasks` 复用 Q88 Agent Key Bearer 验签（为 Zeus 联邦签发专用 Key，无/错/吊销 Key 401），plan 模式不触链、不花 token、不越 Gate；任务存储为进程内存（TTL 30min/上限 500），**多副本部署时任务层不跨实例**，持久化与真机联调随第二阶段。**Q151（2026-09-21）** 落迁移 **0038_sweep_tick_claims（新增 sweep_tick_claims 1 表：lock_name String64 PK/fence Integer NOT NULL/claimed_by String64 可空/claimed_at、updated_at timestamptz 默认 now——SLA sweep 写路径 PG 行级 fence，每把循环锁一行，Q143 restock_claims 的同型收口）**，无新 env（复用 `LOOM_DISTRIBUTED_LOCK_ENABLED`：门控关 fence=None 不写表、行为同 V1），经 pgvector/pg16 容器（55432 避 atlas-pg）up/downgrade-1/up 往返实测。**Q152（2026-09-21，零迁移）** 导出 worker 单进程多 consumer 配置化，新增运维 env `LOOM_EXPORT_WORKER_CONCURRENCY=1`（单进程 ExportWorker/consumer 数，默认 1 保持 V1 单 worker，消费组原生分片）与 `LOOM_EXPORT_STREAM_COUNT=20`（每轮 XREADGROUP 拉取批量，对齐 queue.DEFAULT_COUNT），非法值启动期 ValueError fail-loud；二者均运维防护参数、非 Q9 业务阈值，按 Q135 起先例不入 backend/.env.example。**Q153–Q157（2026-09-21，全部零迁移，02 C1.97–C1.101）**：Q153 为 docs/10 第二轮 DBA 全量列复核（纯文档实测，见下）；Q154 为真容器多副本原语级集成验证（test_real_infra.py，见 §7）；Q155 台内白名单卡片 6 层原料包 JSON（只读端点 `GET /api/fcw/{final_id}/material.json`，台内卡片口径非中台导出面，无新表/env）；Q156 客户批量回填服务端 CSV 上传（`POST /api/effects/backfill/upload`，标准库 csv、零新依赖，新增运维 env `LOOM_BACKFILL_UPLOAD_MAX_ROWS=10000`〔上传行数硬上限、非 Q9 业务阈值，按 Q135 先例不入 .env.example〕）；Q157 真多副本多 worker 持续负载端到端部署演练（见 §7.1）——**多副本并发启动迁移安全**由 `alembic/env.py` 的 PostgreSQL 咨询锁（常量 `MIGRATION_ADVISORY_LOCK_KEY=0x4C4F4F4D`，独立 NullPool 长连接 `pg_advisory_lock` 串行化、仅 postgresql 方言、finally unlock）保证，**不改迁移表结构**；多副本演练用 overlay `infra/docker-compose.ha.yml`（backend `deploy.replicas:2`，仅组合既有分布式锁/worker/广播 env、不新增业务 env，LLM base_url 置空走 synthetic）与脚本 `infra/ha-rehearsal.sh`，另新增 `backend/uvicorn-log.json` 并由 docker-entrypoint 的 uvicorn `--log-config` 挂载以让容器内 app.* 日志可见。**当前迁移头：0038_sweep_tick_claims（2026-09-21，down_revision=0037_restock_claims；Q150/Q152/Q153–Q157 零迁移、Q151 新增 sweep_tick_claims 1 表，业务物理表 59→60；含 alembic_version 时 public 共 61 表）**；迁移一律按 MIGRATION_CONVENTION 经 pgvector/pg16 容器一次性 up / downgrade-1 / up 实测，不进 pytest。
- **CI/CD 流程**：提交 → 单元/契约测试（16）→ 构建 → staging 部署 → Evaluation Dataset 回归（防红旗 S2"Skill 从未运行"）→ 人工 Gate → prod
  - **Evaluation 回归命令（M10-Q 实现补登，Q77）**：staging 闸门执行 `backend/.venv/bin/python eval/runner.py`（仓库根目录），扫描 `eval/datasets/skills/` 与 `eval/golden_cases/skills/` 全部 YAML 案例，任一失败/错误退出码非零即阻断发布；案例集与 target 契约见 `eval/README.md`。**CI workflow 已落地（Q111，2026-09-16，GitHub Actions；Q118 扩为七 checker、Q122 扩为八 checker）**：`.github/workflows/ci.yml` 在 push main/dev 与 pull_request 时跑两 job——backend（Python 3.12，仓库根 `pip install -e "./backend[dev]"`；working-directory=backend 下 `ruff check .` → `python -m pytest -q`（sqlite、无服务容器，当前 761 测试全绿（另 8 个 real-infra 例默认 skip，见 §7））；仓库根 `python eval/runner.py` 跑 Q77 的 101 案回归；零 secret、零外部服务）与 frontend（Node 20，working-directory=frontend，`npm ci` → `npm run typecheck` → 八 checker：tokens/nav/content/products/workbench/compliance/admin/settings，前六个 Q96–Q102 落地、settings 为 Q114 新增并于 Q118 接入 CI、**content 为 Q122 新增（客户内容页消息/文件/岛纪律/后端防漂移守卫；Q131 扩回填岛守卫）；admin 于 Q130 扩 effects 运营台守卫（sidebar 7→8 项），checker 文件集合不变**）；守卫单测 `backend/tests/unit/test_ci_workflow_contract.py` 静态锁定 workflow 结构（触发/双 job/命令/零 secret），Q118 增「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除 checker 漏接，删闸即挂。不含 `ruff format --check`（历史文件尚未全量格式化）、`next build` 与 lint（均非既定门槛；Q122/Q123 切片本地另行 `npm run build` 实测通过）与 CD（部署仍随本流程后续阶段）。**已实际运行**：2026-09-16 落地至 2026-09-18 累计 24 次 run（dev push / pull_request / main merge，含 PR #42–#45）全部 success；最新 main run 35253008023（2026-09-17，Q118 随 PR #45 合并）；Q119–Q140 提交均已由负责人 push（dev 与 origin/dev 同步于 a339816）；push 后最新 CI run 状态以 GitHub Actions 页为准（本地不可见）。
- **版本与回滚**：应用版本 + PWS/FCW 快照版本分离；回滚中心（09 D3.12 Rollback Center）覆盖模板/原子/规则/Skill/Prompt
- **发布纪律**：AI 相关变更（Skill/Prompt/权重）必须可回滚、可审计（A4 Prompt 治理 + Q63 KUP 回滚）

---

## 2. 多租户部署

| 项 | 说明 | 来源 |
|---|---|---|
| 隔离模型 | V1 共享实例 + tenant_id 强隔离（Q33）；客户量大后评估独立实例 | Q33 / 14 §2.3 |
| 数据隔离 | 业务表 tenant_id；知识层共享表无 tenant_id（平台级） | 10 §1.1 |
| 互不感知 | 跨客户不查重、永不驳回、客户看不见彼此 | Q33 |
| 套餐/试用/续费/暂停 | 租户管理 + 计费（Basic $999 / Pro $2999 / Enterprise $9999，D3.11） | 09 D3.11 |
| Onboarding | 注册→套餐→团队→第一产品建模→上线（D3.11） | 09 D3.11 |

---

## 3. 密钥与安全

- 密钥管理：API Key 一 Agent 一 Key 可吊销（Q60b/Q67）；**密钥不入库、不进文档**（本项目 AGENTS.md 红线）
- 访问控制：RBAC 8 角色 + 权威扩展（07 §2.3）；红线操作（PWS 冻结/原子正式化/合规废弃/Prompt 修改）= 角色 + 人工 Gate（line 7674）
- 审计：writeAudit append-only（所有人工操作 + AI 调用）
- 外部回调安全：effect-callback 鉴权 + 限流 + 幂等（11 §2.1/§3）
- 脱敏：AG-SUPPORT 上线前脱敏审核（Q66）；staging 数据脱敏

---

## 4. 监控与成本治理

| 监控项 | 内容 | 来源 |
|---|---|---|
| 应用监控 | 接口错误率/延迟/SLA 引擎超时（24h 黄/48h 升级） | Q49 |
| token 成本 | 10 条省 token 策略落地验证 + Token 成本看板（系统管理员） | 09 D3.9 / D7.2 |
| 模型单价 | 统一 per-1M 口径（Q67）；日预算 Opus 50 / Sonnet 30 / Haiku 10 美元 | line 2103 |
| 反馈校准 | 两张常设月度校准报表（FCW score vs 互动、AI 质量分 vs 互动），分档样本 ≥50 提示可校准 | Q65 |
| 治理看板 | 6 个治理驾驶舱（知识库健康/Skill 质量/Token 成本/白名单表现/平台规则/人工审核） | 09 D3.9 |

---

## 5. 备份与恢复（建议）

- 数据备份：关系库每日全量 + WAL 增量；对象存储（素材/成品）跨区冗余
- 快照不可变：PWS/SkillRunLog/审计 append-only，备份需支持历史版本追溯（Q31 全保留）
- 灾难恢复：RPO ≤ 24h / RTO ≤ 4h【建议】，随 V2/V3 客户规模收紧
- 备份制品与恢复验证（已落地）：Q146 的 db-backup sidecar 每日全量 `pg_dump`（custom/compress=9）+ 7 天保留（脚本 `infra/backup/backup.sh`，compose 服务 db-backup）；异机完整恢复已经 Q158 端到端演练验证（`infra/restore-rehearsal.sh`，见 §7.2）。WAL 增量、对象存储跨区冗余、RPO/RTO 量化达标仍为【建议】，随 V2/V3。

---

## 6. 与既有文档的核对项

- [x] 部署形态与 14 选型一致（模块化单体/共享实例起步，PG+pgvector+Redis，引用 14 §2.1/2.3）
- [x] 监控项覆盖 09 §7 成本治理、Q65 校准报表、Q67 单价口径
- [x] 无密钥、令牌、敏感信息出现在文档中（AGENTS.md 红线）
- [x] §1 具体环境/中间件已补（2026-09-13：FastAPI / PG16+pgvector / Redis7 / S3 / Next.js；选型定稿后）

---

## 7. 真容器多副本验证（Q154，可选门禁；2026-09-21）

Q133–Q143/Q151/Q152 的多副本锁、Redis Streams、配置广播、下游 PG 行级 fence 在常规 CI 中一律以内存替身（FakeRedis/FakeStreams/FakeBroadcast、sqlite）验证，**不依赖外部服务**。Q154 新增一组**默认 skip、仅在显式提供真容器 env 时运行**的集成测试 `backend/tests/integration/test_real_infra.py`（8 例），用于在本机/CI 手动复跑真 Redis/真 PG 下的多副本原语语义；它**不是**常规门禁（`pytest -q` 无 env 时 8 例全 skip），也**不替代** V2 的真多副本多 worker 持续负载端到端部署演练。

- **覆盖（8 例）**：真 PG 两独立连接池跨连接认领/升级与条件 UPDATE 拒旧 fence（restock `restock_claims` 与 sweep `sweep_tick_claims` 参数化）、真并发行锁串行化；真 Redis `leader_lease` SET NX 互斥/INCR 单调/短 TTL 易主看门狗置 lost＋raise LockLost＋安全释放、Streams 两 consumer `XREADGROUP '>'` 不重叠分片/XACK/XCLAIM 接管/move_to_dead 死信、pub/sub 失效广播跨连接投递触发 reload。
- **一次性真容器（`--rm`，端口避开本机 atlas-pg 的 5432；验证完 `docker stop` 自删）**：
  - PG（fencing 用，fixture 只 create_all 两 fence 表）：`docker run -d --rm --name loom-realinfra-pg -e POSTGRES_USER=loom -e POSTGRES_PASSWORD=loom -e POSTGRES_DB=loom -p 55433:5432 pgvector/pgvector:pg16`
  - Redis：`docker run -d --rm --name loom-realinfra-redis -p 6380:6379 redis:7`
- **运行（在 `backend/` 目录）**：
  `LOOM_TEST_REAL_PG_DSN="postgresql+asyncpg://loom:loom@localhost:55433/loom" LOOM_TEST_REAL_REDIS_URL="redis://localhost:6380/0" .venv/bin/python -m pytest tests/integration/test_real_infra.py -v`
  → 8 passed（基线实跑 52.6s）；不设两个 env 直接 `pytest -q` → 8 skipped（总收集 769＝761 常规 + 8 real-infra；747→750 Q155、→757 Q156、→761 Q157）。
- **清理**：`docker stop loom-realinfra-pg loom-realinfra-redis`（`--rm` 自动删除，勿触碰本机 atlas-pg/atlas-db-1）。
- **已知替身差异（真容器首次实跑抓到）**：`restock_claims.request_id` 为 varchar(36)，测试 key 不得带超长前缀（基线初版 49 字符撞真 PG `StringDataRightTruncation`，sqlite 替身不强制 varchar 长度故从不报错），统一用 32 字符 `uuid.uuid4().hex`（同时满足 `sweep_tick_claims.lock_name` varchar(64)）。
- **仍挂 V2**：真多副本、多 worker 进程的持续负载端到端部署演练（本节在单进程内用两独立连接复现全部原语语义）、restock worker 主认领切消费组水平并行（花钱隔离，需重裁 Q87/Q89 leader 单实例防双花）。
- 另：Q153 docs/10 第二轮 DBA 全量列复核用同类一次性 PG16 容器（宿主 55432）跑全链 `alembic upgrade head`（到 0038）后导出 information_schema/pg_indexes/pg_constraint 并与全量 ORM metadata 比对，实测 60 表/637 列/212 索引/PK60·FK32·UQ20、列漂移 0；该复核为一次性人工实测、脚本不入库，结论见 docs/10 §4。

### 7.1 真多副本多 worker 持续负载端到端部署演练（Q157，2026-09-21，本地手动、不进 CI）

Q154 销原语级信任缺口后仍挂账的「真多副本、多 worker 进程持续负载端到端部署演练」由 Q157 落地（02 C1.101），与 Q147 staging 彩排同属**本地手动演练、不进 CI**；全程不注入 LLM key（synthetic 不花真钱）、不造业务主数据（空导出也能验「每 job 恰被一 consumer 处理一次」）、不拉 frontend（SSR 已由 Q147 验）。

- **演练制品（入库）**：`infra/docker-compose.ha.yml`（叠加在基础 compose + staging overlay 之上：backend `deploy.replicas:2`，开 `LOOM_DISTRIBUTED_LOCK_ENABLED`/`LOOM_RESTOCK_WORKER_ENABLED`/`LOOM_RESTOCK_STREAM_ENABLED`/`LOOM_EXPORT_WORKER_ENABLED`（两副本各 1 consumer 同消费组验分片）/`LOOM_CONFIG_CACHE_BROADCAST_ENABLED`，LLM base url 置空保 synthetic；只起 postgres/redis/backend，不发布宿主端口）；`infra/ha-rehearsal.sh`（子命令 `up`/`run`/`logs`/`down`，四阶段 16 项断言；compose project=`loom-ha`，跑完 `down -v` 清卷）。
- **多副本并发启动迁移串行化**：`backend/alembic/env.py` 对 PostgreSQL 方言用独立长连接（NullPool）`SELECT pg_advisory_lock(0x4C4F4F4D)`（"LOOM"）包住「确保版本表 + 迁移」、finally `pg_advisory_unlock`，其他方言/offline 原行为；多副本 entrypoint 并发 `alembic upgrade head` 时一个全量升级、其余阻塞获锁后发现已 head 而 no-op（零迁移，改的是迁移运行方式）。
- **容器应用日志可见性**：新增 `backend/uvicorn-log.json`（root level INFO→stdout、disable_existing_loggers=false、uvicorn.* propagate），docker-entrypoint.sh 的 uvicorn 加 `--log-config /app/uvicorn-log.json`；默认 uvicorn 配置只输出 uvicorn.* 日志，app.core.* 的 worker/锁/广播 INFO 行会被 root 的 WARNING 级别过滤。
- **运行（在 `infra/` 目录，需本机 Docker；不触碰本机 atlas-pg）**：`./ha-rehearsal.sh up`（构建/起栈并跑全部断言）、`./ha-rehearsal.sh run`（栈已在跑只重跑断言）、`./ha-rehearsal.sh logs`、`./ha-rehearsal.sh down`（脚本本身不带 -v，清卷手动加 -v）。
- **实测（2026-09-21，全新库 down -v 后，HA_JOBS=24）16/16 PASS**：① 迁移两副本合计恰 38 条 Running upgrade（一副本 38、另一 0=no-op）、无 already exists/duplicate key、两副本 healthy；② 24 个导出 job 全 completed、job_id 唯一 24、两副本 consumer 分片 **12/12**、合计恰 24（每条处理一次）、两副本空轮询 tick skipped=0；③ SLA `/api/admin/sla/run` 8 并发＝2×200+6×409、restock `/api/admin/restock/run`＝6×200+2×409（抢不到 leader 锁 409、无 5xx；restock 空库无 requested 信号，只验单 leader 锁互斥，「不双花」由 Q87/Q89 leader 单实例 + Q143/Q151 PG fence 保证）；④ PUT `content.ai_quality_threshold`=0.82 后对端副本日志命中 `config cache reloaded key ... from invalidation broadcast`，末尾自动还原 0.85。
- **演练抓到并修复的真实缺陷（FakeStreams 替身测不到，同 Q154 抓 request_id 截断先例）**：redis-py 8.1.0 asyncio 客户端把 `XREADGROUP BLOCK` 到期（无消息）抛成 `redis.exceptions.TimeoutError`（"Timeout reading from redis"，RedisError 子类、**非**内置 TimeoutError 子类；部分版本直接抛内置 asyncio.TimeoutError）；`queue/streams.read_new` 原只 `except redis.RedisError` 转 StreamBackendError，致 worker 每 block 周期 tick skipped、退避 5s、空转不消费也不 reclaim，多副本下只有未踩退避窗口的 consumer 通吃（首跑 24 job 全落一副本、两副本各 13 次 tick skipped）。修复：block 模式下两类 TimeoutError 归一返回 `[]`（BLOCK 到期协议语义＝本轮无消息，与同步客户端返回 None 一致），非 block 读取超时仍 fail-closed，真连接故障 ConnectionError（非 TimeoutError）仍走 RedisError 分支不被吞；`test_redis_streams.py` 8→12 例。
- **仍挂 V2**：restock worker 主认领切消费组水平并行（花钱隔离，需重裁 Q87/Q89 leader 单实例防双花）；Q157 空库不验真实 final_id 的持续负载全链（待业务方按 docs/19 回填主数据，见 handoff 待办 7②）。

### 7.2 异机备份恢复端到端演练（Q158，2026-09-21，本地手动、不进 CI）

Q146（C1.90）落了每日全量 pg_dump 备份与保留，但备份能否在一台**全新异机** PG 上完整恢复从未端到端演练；Q158 补齐这条恢复腿（02 C1.102），与 Q147 staging 彩排、Q157 HA 演练同属本地手动演练、不进 CI；全程不起应用镜像、不注入 LLM key（synthetic 不花真钱）、不触碰本机 atlas-pg。

- **演练制品（入库）**：`infra/restore-rehearsal.sh`（子命令 `up`/`down`，`KEEP=1` 跑完保留容器排查；风格同 `ha-rehearsal.sh`）。两个相互独立的一次性 `pgvector/pgvector:pg16` 容器：源库 `loom-restore-src`（宿主 55440）、异机库 `loom-restore-dst`（宿主 55441），不同容器/端口/数据卷，`--rm` 跑完自删。
- **流程**：源库 A 经宿主 backend/.venv 设 `LOOM_DATABASE_DSN` 跑 `alembic upgrade head`（到 0038，38 条）；再造 tenants×2/export_jobs×3/effect_records×3（覆盖 JSONB/timestamptz/可空 metrics/任务/时序），叠加迁移种子共 19 张非空表；用与 `infra/backup/backup.sh` 相同的 `pg_dump --no-password --format=custom --compress=9` 备份（**custom 二进制，恢复必须用 pg_restore、不能 `psql <`**），dump 离源经宿主再进异机；异机 B drop/create 空 loom 库后 `pg_restore --no-owner`。
- **运行（在 `infra/` 目录，需本机 Docker 与 backend/.venv）**：`./restore-rehearsal.sh up`（全流程并清理）、`./restore-rehearsal.sh down`（清理残留容器）。
- **实测（2026-09-21，全新库）12/12 PASS**：源库迁移 38 条 rc=0；dump 155110 字节、TOC 含 61 条 TABLE DATA；异机 pg_restore rc=0、error 行=0；两库 alembic_version 均 `0038_sweep_tick_claims`；业务表 60/含版本表共 61 且集合 A==B；全表逐表行数 A==B（非空表 19、零差异）；结构指纹 indexes=213/PK=61/FK=32/UQ=20/vector 列=2/pgvector 扩展 A==B；tenants/export_jobs/effect_records 演练行整行（JSONB/timestamptz/可空 metrics）逐字段一致；config_items 61/61、QC 阈值 0.85 一致；`import app.main` ORM 连恢复库只读成功；恢复库再 `alembic upgrade head` 幂等 no-op（0 条 Running upgrade）。
- **仍挂 V2**：RPO ≤24h/RTO ≤4h 的量化达标认证（需备份历史 + 故障注入 + 恢复计时；§5 仍为【建议】，本片不升级其状态）、WAL 增量备份、对象存储（素材/成品）跨区冗余、指标级监控、持续高压压测（Q157 留）。

