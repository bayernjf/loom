# 17. 部署与运维

> **状态**：🟢 已定稿（选型于 2026-09-13 拍板、技术栈同日确认；可作环境搭建依据）
> **来源**：07_技术架构与工程规格.md §2.6（多租户）/§6.3（系统管理）｜09_全景 §D3.11（租户/计费/审计）｜14_技术选型决策.md（✅ 已定稿）｜15（技术栈）｜08_迭代计划（V1 5-10 客户 → V3 100-200 客户）
> **用途**：环境、CI/CD、多租户部署、密钥与安全、监控与成本治理的运维说明；AI 据此搭建发布与运行环境。
> **说明**：计费数值来自原文（09 D3.11 套餐价）；具体版本号为 2026-09-13 选型落地建议，小版本随脚手架确定。

---

## 1. 环境与发布

| 环境 | 用途 | 数据 | 说明（建议） |
|---|---|---|---|
| dev | 开发联调 | 样例数据（16 测试数据） | 本地/单实例 |
| staging | 验收 + Evaluation Dataset 回归 | 脱敏数据 | 发布前闸门 |
| prod | 正式（SaaS 多租户） | 生产数据 | 隔离实例或强隔离 |

- **技术栈与中间件（14 定稿 + 15 技术栈）**：应用 Python 3.12 + FastAPI 单进程模块化单体；数据 PostgreSQL 16（+pgvector）/ Redis 7（缓存；多副本 leader 锁：`LOOM_DISTRIBUTED_LOCK_ENABLED=true` 开启，Q89，默认关；restock 瞬态退避 `LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`LOOM_RESTOCK_BACKOFF_MAX_SECONDS=1800`/`LOOM_RESTOCK_BACKOFF_MAX_ATTEMPTS=10`，Q90；DAG 编排同层并发上限 `LOOM_ORCH_MAX_CONCURRENCY=4`，Q91；**多副本 V2 基建三件（2026-09-20 Q133–Q135，均默认关/不接线、零迁移）**：①Q133 fencing token 随 leader 锁落地——抢锁成功后 `INCR loom:fence:{name}` 取跨易主单调令牌（序列键不设 TTL），`leader_lease()` 看门狗易主/续约故障置 lost，INCR 失败 fail-closed，下游 PG 行级 fence 条件更新随 V2 写者；②Q134 Redis Streams 消费组细粒度认领原语已落 `app/core/queue/`（XADD 近似 MAXLEN/XGROUP mkstream 幂等/XREADGROUP `>` 读即入 PEL/XACK/XPENDING+XCLAIM 崩溃接管/超限进死信流，`StreamBackendError` fail-closed），原语片当时只落原语；**restock requested 信号 XADD 生产侧已由 Q138 接线**（`LOOM_RESTOCK_STREAM_ENABLED=false` 默认关，after_commit best-effort 入流、DB requested 行为唯一事实源、DB 轮询兜底，worker 主认领仍走 DB 轮询；消费组水平并行因 restock 花钱须 leader 单实例防双花而留 V2 重评隔离）；③Q135 配置缓存多副本失效广播（Redis pub/sub 频道 `loom:config:invalidate`，发布侧 after_commit fire-and-forget PUBLISH 为 best-effort、订阅侧 ConfigBroadcastSubscriber 收消息 `*` 全量 reload、具体 key 经 Q140 走 reload_keys 单 key 增量失效（DB 已删 key 同步从快照移除），连续故障指数退避 1s 起封顶 30s、故障懒重连），开关 `LOOM_CONFIG_CACHE_BROADCAST_ENABLED=false` 默认关，开启后 lifespan 启停订阅者、启动失败不阻断应用）/ S3 兼容对象存储；前端 React + Next.js（Node 运行时仅构建/SSR）；编排定义为 YAML（无独立编排服务）。V1 用 docker-compose 单机即可承载 5-10 客户，K8s/多实例随 V2 客户量评估。M12 驾驶舱只读端点（Q92，`/api/admin/dashboards/token-cost|review-workload`，platform_admin）为 skill_runs/skill_candidates/ops_todos 三表实时 SELECT 聚合，不引入环境变量、后台调度任务或物化表。M12 统一审核工作台（Q93，`/api/review-workbench/candidates|batch-approve`）为 skill_candidates/skill_runs/c1_industry_thresholds 实时 SELECT+内存风险归一并发，新增配置键 `review.batch_pass_confidence`（0.85，迁移 0021 纯配置种子幂等，无业务表 schema 变更），同样不引入环境变量、后台调度任务或物化表。M12 候选审核 SLA 挂接（Q94）复用既有 Q49 sweep/SweepScheduler，无新调度任务；五型时限为配置键 `review.sla_hours.{pwc_combo,field_plan,c1_recognition,atom_batch,c7_layer4}`（种子 72h【待补】，迁移 0022 纯配置种子，无业务表 schema 变更），不引入环境变量或物化表。租户注册表（Q95）为普通业务表 `tenants`（迁移 0023 建表+存量 DISTINCT tenant_id 回填 basic/active，应用层准入无硬外键），无新环境变量、后台调度任务或物化视图；多租户仍为共享实例+tenant_id 逻辑隔离口径（5-10 客户单机），物理隔离/备份分租户等随 V2。SLA 黄色窗口配置化（Q114）：黄判由 created_at+固定小时改为 due_at − 单一配置键 `sla.yellow_hours`（int，种子 24，配置中心可热更），有黄窗的待办类型扩为五型 review_*（法审 law_review due=48h 数值不变）。G2 通用字段工程定稿（Q115）落迁移 0024_g2_common_fields（12 个 cat='common' 字段种子，ON CONFLICT DO NOTHING 幂等）。V2 P4 段12 文章生成首片（Q116）落迁移 0025_content_products（content_products 新业务表）/0026_article_gen_seed（ARTICLE-GEN 编排种子）与 `app/content/` 模块，无新环境变量、后台调度或物化表。Q118 落迁移 0027_product_space_fks（product_spaces 两道 nullable 软引用补 DB 级外键：NULL 合法、孤儿 INSERT/UPDATE 拒写），并顺带修复 Alembic 版本表版本号列长过窄缺陷。V2 P4 段12 多语言（Q119）落迁移 0028_content_languages（新建 content_languages 语言清单表含 zh-CN 全市场种子、product_spaces 加 nullable target_languages JSONB、content_products 加唯一约束 uq_content_final_language_kind(final_id,language,kind)）。AI 质量分（Q120）落迁移 0029_content_quality（content_products 加 nullable quality_score Float 与 quality_issues JSONB 两列）与 0030_article_qc_seed（模型网关第 8 场景 ARTICLE-QC→synthetic 路由及 SkillPrompt/PromptVersion v0.1 纯种子，不建表）。复检第②项语义级检测（Q121，纯 advisory）落迁移 0031_article_semantic_seed（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK→synthetic 路由及 SkillPrompt/PromptVersion v0.1 纯种子，不建表），均无新环境变量、后台调度或物化表。Q122 客户内容页（列表/详情/审阅/Q56-a 人工编辑，新事件 manual_resubmit）与 Q123 段1 客户目标语言控件（客户 PATCH + active 语言清单 GET）均为**零迁移**（复用既有 content_products / content_languages / product_spaces 表）。Q124 难产骨架作废回池（Q56-b）落迁移 0032_content_discard（content_products 加 nullable discard_reason Text；普通唯一约束 uq_content_final_language_kind 改为 **partial unique index `WHERE status <> 'discarded'`**，模型层 postgresql_where + sqlite_where 双方言，作废后同 final_id/language/kind 可重新生成=回池；downgrade 还原普通唯一索引、不处理同键冲突，V1 无生产数据）。Q125 运营发布回填（Q60c）落迁移 0033_content_publish（content_products 加 nullable published_url Text / platform_post_id String(128) / published_at timestamptz 三列，published_at 非空即已发布信号、不新增 published 态；downgrade drop 三列），均无新环境变量、后台调度或物化表，业务物理表仍 55（纯加列/换索引）。段13 反馈回流入站第一片（Q126，Q60）落迁移 **0034_effect_records（新增 effect_records 1 表）**：外部 Agent 经 `POST /api/effect-callback`（Q88 Bearer 验签，首个受 Agent Key 保护业务端点）推送效果时序，按 content_id 匹配非 discarded 成品分流 matched/orphan、(external_content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏不补 0、整批 all-or-nothing，运营只读孤儿队列与成品时序；无新环境变量、后台调度或物化表，业务物理表 55→**56**。段13 续片 Q127（Q60a 孤儿人工认领，2026-09-19）落迁移 **0035_effect_claims（新增 effect_claims 1 表：external_content_id PK→content_id FK 的持久认领映射 + claimed_by/claimed_at；effect_records 另加 claimed_by/claimed_at 两 nullable 溯源列，不新增第三状态）**：`POST /api/admin/effects/claims`（operations 写口硬闸、actor 在体）认领后同 external_content_id 后续推送（覆盖/新采集点）经映射兜底一律 matched 不回落孤儿，审计 effect.claimed（tenant `_platform`），业务物理表 56→**57**；Q128（customer-backfill 客户专用通道，2026-09-19）为 `POST /api/effects/backfill`（无 Agent Key、body {tenant_id,records,actor}、source 服务端固定、只命中本租户非 discarded 成品否则整批 422、绝不产生孤儿、审计 effect.customer_backfilled tenant=客户租户），**零迁移**。段13 操作面收口三片 Q129（批量认领 `POST /api/admin/effects/claims/batch` 整批 all-or-nothing、取消认领/解绑 `POST /api/admin/effects/claims/unclaim` 删映射并回滚旧目标行，审计 effect.claim_revoked，纯后端零迁移）、Q130（管理端 `/admin/effects` 效果回流运营台，sidebar 第 8 项，纯前端）、Q131（客户内容详情页效果回填岛消费 Q128 通道，纯前端）均于 2026-09-20 落地，**三片均零迁移**、无新环境变量、后台调度或物化表；check-admin/check-content 仅扩守卫，CI checker 集合不变。中台导出 JSON 形态与异步导出任务（Q132，2026-09-20）落迁移 **0036_export_jobs（新增 export_jobs 1 表：导出任务记录 job_id PK/tenant 索引/product_space_id 可空/format/status server_default completed/row_count/file_name/requested_by/error/created_at/completed_at，不存文件 payload、下载按任务参数重渲染；V1 任务请求内同步置 completed；**queued/running 真后台 worker 已由 Q137 落地**——`LOOM_EXPORT_WORKER_ENABLED=false` 默认关，开启后 ExportWorker 经 Redis Streams 消费组异步处理（只读幂等作业可水平并行、崩溃 PEL 接管、超限进死信，阻塞节拍 `LOOM_EXPORT_STREAM_BLOCK_SECONDS=5.0`），并补 `GET /api/exports/jobs` 列表口；门控关仍走 Q132 同步路径）**，业务物理表 57→**58**，无新环境变量、后台调度或物化表。**多副本接线四件 Q137（导出 Streams 真后台 worker + 任务列表口）/Q138（restock requested 信号 XADD 生产侧）/Q139（SLA sweep、restock 两循环与两个手工 /run 由 leader_lock 换 leader_lease，作业/信号间 checkpoint=raise_if_lost 协作中止，手工 /run 锁中途丢失返 409）/Q140（配置缓存 reload_keys 单 key 增量失效 + 订阅断线指数退避）均于 2026-09-20 落地，四件全部零迁移、env 默认关、不改 V1 单副本默认行为；新增运维 env `LOOM_EXPORT_WORKER_ENABLED`/`LOOM_EXPORT_STREAM_BLOCK_SECONDS`/`LOOM_RESTOCK_STREAM_ENABLED`（按 Q135 先例不入 backend/.env.example，该文件只保留 LOOM_REDIS_DSN）；Q141 配置缓存 TTL 兜底新增 `LOOM_CONFIG_CACHE_TTL_SECONDS=300`（订阅器无失效消息超此秒数回源全量 reload，仅广播门控开启且曾成功装载时生效）、Q142 导出硬上限新增 `LOOM_EXPORT_MAX_ROWS=100000`（中台导出每页/每次最大 final_id 行数，limit 超限 422；二者均为运维防护旋钮、非 Q9 业务阈值、零迁移，同样不入 .env.example），Q143 无新 env**。**Q141–Q143（2026-09-20，02 C1.85–C1.87）**：Q141 配置缓存 per-key `ConfigItem.version` 版本向量门控（reload_keys 拒旧快照回灌）+ 订阅器 TTL 兜底、Q142 中台导出 limit/offset 分页与行数硬上限（JSON envelope 加 total/limit/offset/has_more、CSV 走 X-Export-* 响应头、同步/异步 worker/下载同一上限）均**零迁移**；Q143 落迁移 **0037_restock_claims（新增 restock_claims 1 表：request_id String36 PK/fence Integer NOT NULL/claimed_by String64 可空/claimed_at、updated_at timestamptz 默认 now——下游 PG 行级 fence 接 restock 花钱路径，花钱前 claim_request 认领、提交前 fence_current 条件 UPDATE 拒旧 leader 迟到写）**，经 pgvector/pgvector:pg16 容器 upgrade/downgrade-1/upgrade 往返实测。**private beta 上线准备片（2026-09-21，Q144–Q149）**：Q144 裁决首发形态＝受控 private beta（后端内网/网关后、平台代运营，公网自助 NO-GO，身份层压 V2 第一项）；Q145 落最小部署制品——backend Dockerfile + entrypoint（`alembic upgrade head` 先于 uvicorn）、frontend 三阶段 Dockerfile（Next standalone）、compose 增 backend（**不发布宿主端口**）/frontend（唯一发布口 3000）与 service_healthy 依赖序、`.dockerignore`（TLS 由部署环境在 frontend 口外终结）。**（2026-09-25 Q200 部署形态收口，02 C1.144，负责人点工）**：base compose 数据面收紧——postgres 5432／redis 6379／minio 9000-9001 宿主映射全绑 `127.0.0.1`（本机 psql/redis-cli/mc 仍可达），`POSTGRES_PASSWORD`/`MINIO_ROOT_PASSWORD` 改 env 插值（DSN/db-backup/db-basebackup/wal-archiver 同步消费），三 datastore 补 `restart: unless-stopped`；**`LOOM_MASTER_KEY` 为空时 backend entrypoint 启动即 fail-fast**（刻意不落 compose `${VAR:?}`——解析期全局求值会打断彩排/监控 overlay，Q185 教训），部署引导＝**先设 LOOM_MASTER_KEY 再起栈**；ha/load/alerting 三彩排脚本注入 rehearsal-only 测试密钥 `loom-rehearsal-only-master-key`。契约守卫 `test_default_compose_hardening_contract.py` 5 例钉端口回环/口令插值/fail-fast 时序/彩排注入/restart。Q145 原「唯一发布口 3000」句仍是 frontend 侧事实，本变更不涉。；Q146 落最小备份监控——db-backup sidecar 每日 pg_dump + 7 天保留（pgbackups 卷）、全服务日志 10m×3 轮转、watchdog 探活 + 可选 webhook best-effort 告警、frontend healthcheck（异机备份/恢复演练/指标监控随 V2）；Q147 落 `infra/docker-compose.staging.yml` overlay（`!reset` 清空 infra 宿主端口避本机 atlas 容器、minio profile 排除，`-p loom-staging` 起 6 服务全 healthy，`down -v` 清理），staging 彩排全通过；Q148 真实 LLM 接线——agnes OpenAI 兼容网关 `https://apihub.agnes-ai.com/v1`、模型 agnes-2.5-flash、计费币种 USD、日预算 50 USD/天，出站 Key 沿用 Q82 Fernet（env `LOOM_MASTER_KEY`），8 个 chat 场景切真模型、ATOM-AFFINITY 仍 synthetic（agnes 无 embedding），模型单价待供应商给（先记 token/调用数、金额 0.0）；Q149 为阈值确认与 docs/19 主数据录入模板（纯文档）。**A2A 封臣片（Q150，2026-09-21，零迁移/零新 env）**：新增三个**公开只读**发现端点 `GET /api/a2a/agent-card`、`GET /.well-known/agent-card.json`、`GET /.well-known/agent.json`（Cache-Control max-age=300，卡片 url 取 `LOOM_PUBLIC_BASE_URL`，未配置回退请求 base_url）；任务端点 `POST /api/a2a/tasks` 复用 Q88 Agent Key Bearer 验签（为 Zeus 联邦签发专用 Key，无/错/吊销 Key 401），plan 模式不触链、不花 token、不越 Gate；任务存储为进程内存（TTL 30min/上限 500），**多副本部署时任务层不跨实例**，持久化与真机联调随第二阶段。**Q151（2026-09-21）** 落迁移 **0038_sweep_tick_claims（新增 sweep_tick_claims 1 表：lock_name String64 PK/fence Integer NOT NULL/claimed_by String64 可空/claimed_at、updated_at timestamptz 默认 now——SLA sweep 写路径 PG 行级 fence，每把循环锁一行，Q143 restock_claims 的同型收口）**，无新 env（复用 `LOOM_DISTRIBUTED_LOCK_ENABLED`：门控关 fence=None 不写表、行为同 V1），经 pgvector/pg16 容器（55432 避 atlas-pg）up/downgrade-1/up 往返实测。**Q152（2026-09-21，零迁移）** 导出 worker 单进程多 consumer 配置化，新增运维 env `LOOM_EXPORT_WORKER_CONCURRENCY=1`（单进程 ExportWorker/consumer 数，默认 1 保持 V1 单 worker，消费组原生分片）与 `LOOM_EXPORT_STREAM_COUNT=20`（每轮 XREADGROUP 拉取批量，对齐 queue.DEFAULT_COUNT），非法值启动期 ValueError fail-loud；二者均运维防护参数、非 Q9 业务阈值，按 Q135 起先例不入 backend/.env.example。**Q153–Q157（2026-09-21，全部零迁移，02 C1.97–C1.101）**：Q153 为 docs/10 第二轮 DBA 全量列复核（纯文档实测，见下）；Q154 为真容器多副本原语级集成验证（test_real_infra.py，见 §7）；Q155 台内白名单卡片 6 层原料包 JSON（只读端点 `GET /api/fcw/{final_id}/material.json`，台内卡片口径非中台导出面，无新表/env）；Q156 客户批量回填服务端 CSV 上传（`POST /api/effects/backfill/upload`，标准库 csv、零新依赖，新增运维 env `LOOM_BACKFILL_UPLOAD_MAX_ROWS=10000`〔上传行数硬上限、非 Q9 业务阈值，按 Q135 先例不入 .env.example〕）；Q157 真多副本多 worker 持续负载端到端部署演练（见 §7.1）——**多副本并发启动迁移安全**由 `alembic/env.py` 的 PostgreSQL 咨询锁（常量 `MIGRATION_ADVISORY_LOCK_KEY=0x4C4F4F4D`，独立 NullPool 长连接 `pg_advisory_lock` 串行化、仅 postgresql 方言、finally unlock）保证，**不改迁移表结构**；多副本演练用 overlay `infra/docker-compose.ha.yml`（backend `deploy.replicas:2`，仅组合既有分布式锁/worker/广播 env、不新增业务 env，LLM base_url 置空走 synthetic）与脚本 `infra/ha-rehearsal.sh`，另新增 `backend/uvicorn-log.json` 并由 docker-entrypoint 的 uvicorn `--log-config` 挂载以让容器内 app.* 日志可见。**效果回填上传链与 FCW 异步化（2026-09-22，Q159–Q165，02 C1.103–C1.109）**：Q159 客户批量回填岛由 Q136 前端解析切到 Q156 服务端 CSV 端点（纯前端，零迁移/零 env）；Q160 新增 Excel .xlsx 服务端上传 `POST /api/effects/backfill/upload-excel`（零迁移，pyproject 加依赖 `openpyxl>=3.1.5`，行数硬上限复用既有 `LOOM_BACKFILL_UPLOAD_MAX_ROWS=10000`，无新 env）；Q161 落迁移 **0039_import_jobs（新增 import_jobs 1 表：job_id PK / tenant_id·content_id 两索引 / format String8 / payload Text 落 CSV 原文或 xlsx base64 可重放 / filename 可空 / status server_default completed / received·matched·orphan·upserted·row_count 五计数 / requested_by / error·errors 可空 / created_at·completed_at，业务物理表 60→**61**，经 pgvector/pg16 容器 55432 up / downgrade-1 / up 往返实测）**，并新增异步导入 worker 门控 `LOOM_IMPORT_WORKER_ENABLED=false`（默认关＝请求内同步跑到终态、201 直接回 completed/failed；开＝queued 提交后 XADD 由 ImportWorker 经 Redis Streams 消费组异步置 running→completed/failed，入流基础设施失败 fail-closed 置 failed 回 503；确定性校验失败直接 failed+ACK 不重试不进死信）与三旋钮 `LOOM_IMPORT_STREAM_BLOCK_SECONDS=5.0`/`LOOM_IMPORT_WORKER_CONCURRENCY=1`/`LOOM_IMPORT_STREAM_COUNT=20`（按 Q135 起先例运维旋钮不入 backend/.env.example，该文件只保留 LOOM_REDIS_DSN）；Q162 客户合规风控页扩展三件套（CCR 历史 limit/offset 分页、法审 SLA 倒计时服务端派生 sla_remaining_seconds/sla_state 复用 48h、客户词库只读，4 个无闸只读 GET）、Q163 客户 Onboarding 引导（CustomerTenantView 追加 onboarding 派生复用 Q95 服务、工作台引导岛/紧凑进度条）、Q164 管理端驾驶舱日期筛选器 UI（纯前端）三片均**零迁移、零新 env**；Q165 FCW 组装异步 worker（**零迁移**，复用 fcw_assembly_tasks.status 列）镜像 Export/Import worker 消费循环，新增门控 `LOOM_FCW_WORKER_ENABLED=false`（默认关＝Q55 请求内同步行为不变；开＝queued＋commit＋XADD 由 FcwWorker 异步，入流失败 delete task＋503 fail-closed 无残留）与三旋钮 `LOOM_FCW_STREAM_BLOCK_SECONDS=5.0`/`LOOM_FCW_WORKER_CONCURRENCY=1`/`LOOM_FCW_STREAM_COUNT=20`（同样不入 .env.example）；main.py lifespan 按门控启停 import/fcw worker（启动序追加、关闭反序 stop），门控默认全关、不改 V1 单副本默认行为。**身份层 P0-① 第一切片（Q178，2026-09-24，02 C1.122，甲案内部运营 PAT，迁移 0040_staff_api_keys）**：在 Q88 机器 Agent Key（`loom_` 前缀，保护 effect-callback/A2A）之外新增**内部运营个人访问令牌**（`loom_staff_` 前缀，新表 `staff_api_keys`，业务物理表 61→62、含 alembic_version 时 public 共 63 表）。门控 env `LOOM_STAFF_AUTH_ENABLED=false`（默认关；按 Q135 先例运维旋钮不入 backend/.env.example）：
  - **关（默认，V1 行为）**：不解析人员 Authorization 令牌，管理端 actor 仍走 query/body 自报，行为与此前完全一致；Q88 机器 Agent Key 认证（effect-callback/A2A）不受影响。
  - **开**：app 级全局依赖 `staff_auth_context` 仅验 `loom_staff_` 令牌（独立短事务、命中记 last_used_at），无令牌或 `loom_` 机器前缀直接放行（不误伤客户口/公开口/机器口）；`rbac.require_any_role` 为唯一授权收口——内部角色口无认证人 401（NotAuthenticated）、角色不足 403（PermissionDenied）、通过则**就地用令牌身份覆盖自报 actor**（防 `?actor_id&roles=platform_admin` 提权，审计落真实人员）；纯客户角色（whitelist_owner）口维持自报。新增 staff-keys 三端点（POST/GET /api/admin/staff-keys、POST /api/admin/staff-keys/{key_id}/revoke，platform_admin 红线）与 `GET /api/auth/me`；前端 `/admin/login` 录入令牌（写 httpOnly cookie `loom_staff_token`，不进 sidebar）、`/admin/staff-keys` 治理页（sidebar 第 12 项），lib/api 对 `/api/admin` 口自动带 `Authorization: Bearer`、401 跳 /admin/login。
  - **启用引导顺序（鸡生蛋）**：① 门控保持关，用自报 platform_admin 调 `POST /api/admin/staff-keys` 签发首个 platform_admin 人员令牌（明文仅返回一次，立即保存）；② 前端 /admin/login 粘贴令牌经 /api/auth/me 自检写 cookie；③ 再置 `LOOM_STAFF_AUTH_ENABLED=true` 重启；此后内部口只认真令牌、自报提权失效。每位运营签发独立令牌（staff_id 一人多 token 可轮换）。
  - **回滚**：置 `LOOM_STAFF_AUTH_ENABLED=false` 重启即恢复 V1 自报（纯加法，表与令牌保留不删）；吊销单个令牌用 `POST /api/admin/staff-keys/{key_id}/revoke`。
  - **边界**：客户侧真实认证、actor↔tenant 绑定（Q88 口径待补）、账号密码/会话/注册找回（乙案）仍 V2；公网自助仍 NO-GO（Q144）。门控开时管理读口 actor_id query、写口 body.actor 仍是必填 schema 形状（缺失 422），其值被令牌覆盖。
- **Q196 审计身份与导出任务归属（2026-09-25，02 C1.140，零迁移零新 env）**：写审计的唯一口 ``append_audit`` 现在按**"已验真凭证 > 请求自报"**取操作人——本次请求只要验真过 staff PAT 或入站 Agent Key，审计里的 ``actor_id``/``actor_roles`` 就是凭证身份，调用方在 query/body 里写的 anything 一律不采信（被推翻的自报值留在 ``detail.declared_actor`` 备查）；无任何凭证的调用（beta 客户侧）保持自报，但 ``detail._actor_via`` 明写 ``declared``，对账方能分辨"这条审计是谁说的"。运维读法：**``_actor_via`` 只有三种值**（``staff_token``/``agent_key``/``declared``）。中台导出任务两条读口 ``GET /api/exports/jobs/{job_id}`` 与 ``/download`` 现要求 ``tenant_id`` 且按任务行归属收口（跨租户＝404），任务 ID 改 ``uuid4``。

**客户卡片视图·指标监控·WAL 归档（Q180–Q182，2026-09-24，02 C1.124–C1.126，三片均零迁移）**：
- Q180 客户白名单卡片视图：客户侧集合口 `GET /api/fcw?tenant_id=`（无闸、强制单租户；区别于 Q177 管理口 `/api/admin/fcw` 读闸可跨租户），单条六层包沿用 Q155 material.json；前端为 content 菜单**子页** `/content/cards`（客户 nav 不增项、仍 8 项，经 content 页 subnav 进入）。
- Q181 指标级监控（**采集面**）：**零依赖自写**进程内指标库 `app/core/metrics/`（Counter/Gauge/Histogram，Prometheus 文本格式），纯 ASGI 中间件按**路由模板**记 `http_requests_total{method,path,status}` / `http_request_duration_seconds`（path 取 scope["route"].path，避免原始 id 高基数），暴露 `GET /metrics`（默认不鉴权，同 /healthz）。**注（Q185 时点）**：当时全仓除该中间件自身外零业务打点，真实注册指标只有 `up` / `http_requests_total` / `http_request_duration_seconds` 三族，故消费侧只能引用这三族。**该状态已由 Q188 终结**：四类业务信号（作业失败 / 队列积压 / 锁易主 / LLM 预算与上游延迟）在 `app/core/metrics/business.py` 落七族真实打点，注册面扩至十族，`alert_rules.yml` 相应新增 `loom-operations` 七条规则与 `loom-operations` 看板（02 C1.132，见 §7.6）。契约测试仍双向硬守：规则/面板不得引用未注册名，且**注册了却无人消费的族一律判红**。
- Q185 指标消费侧（**告警规则 + 看板 + 投递**，零迁移零后端应用改动）：`infra/monitoring/alert_rules.yml` 三条 RED 规则（`LoomHigh5xxRatio` 5xx>5% 且带零流量守卫、`LoomHighP99Latency` P99>1s、`LoomBackendUnreachable` `up{job="loom-backend"}==0`）。监控栈整体移入**独立 overlay `infra/docker-compose.monitoring.yml`**（prometheus 挂 alert_rules ＋ grafana 文件式置备数据源 `loom-prom` 与看板 `loom-http` ＋ 给 watchdog 注入 `PROMETHEUS_ALERTS_URL`），起法 `docker compose -f docker-compose.yml -f docker-compose.monitoring.yml --profile monitoring up -d`。**两条运维硬事实**：① compose 的 `${VAR:?}` 插值在**解析期全局求值、与 profile 激活无关**，故 Grafana 口令 `LOOM_GRAFANA_ADMIN_PASSWORD`（**无默认值、缺失即报错、拒绝内置弱口令**）的必填护栏只能与 opt-in overlay 同处一域，放 base 会打断不开监控的默认部署路径；② **不开 Alertmanager 容器**——0.34.1 的 `webhook_configs.url` 既不展开 `$(VAR)`（崩溃重启循环 `invalid URL: unsupported scheme ""`）也不支持 `env://`（`amtool check-config` FAILED），只剩 `url_file` 需运维在 `LOOM_ALERT_WEBHOOK` 之外另挂一个 URL 文件、配置面翻倍；改为 **watchdog 轮询 `Prometheus /api/v1/alerts` 转发进同一 `LOOM_ALERT_WEBHOOK`**（复用 Q146 投递路径，零新容器），语义为「只转发 `state=firing`、按 `alertname|instance|activeAt` 去重」，代价是放弃 grouping/silencing/inhibition。演练见 §7.6。**（2026-09-25 Q192 变更：负责人拍板后 Grafana 改发布宿主回环口 `127.0.0.1:3001`——只绑回环、且刻意避开 frontend 占用的 3000；绑错网卡或撞端口由契约测试判红，宿主可达由 `alerting-rehearsal.sh` 阶段 3 两条宿主侧断言守着。本句记录的"不新增发布口"是 Q185 当时状态，不再成立。）**
- Q188 业务级指标打点（**消费面的前提**，零迁移零新 env 零前端改动）：七族序列全部挂在真实产出点——作业失败取五个 DB 写点（`fail_export_job` / import 两条确定性校验分支＋`fail_import_job` / `fail_fcw_task`，故 router 入流 fail-closed 的 503 路径自动被覆盖），死信取 `move_to_dead` 唯一漏斗（写死信＋ACK 双成功后才计，防重试双计），锁易主取 `LockLease._mark_lost`（看门狗两条判丢分支的共同漏斗，不在四个 catch 各计一次），队列深度新增只读原语 `stream_depth`（`XPENDING` 汇总＋`XLEN`，与 `reclaim_pending` 那条带 idle 过滤且被 count 截断的 RANGE 不是一回事）在三处 worker `_tick` 末尾取样，LLM 取网关 `invoke`/`embed` 的预算硬停与驱动往返（`observe_llm_call` 只计时不改异常语义，outcome 标签保住超时这类最该看的耗时）。**不打 tenant/model_id 标签**防高基数；LLM 直方图显式用 1..120s 桶（默认桶上限 10s 会让真供应商调用整批落 `+Inf`）。**restock 流刻意不采**——Q138 只接生产侧无消费者，采它是造一条永远正确的假积压告警。详见 02 C1.132。
- Q182 WAL 增量＋对象存储归档＋PITR：postgres 开归档（archive_mode=on / wal_level=replica / archive_timeout=300，归档到 walarchive 卷）；db-basebackup 服务经 `wal/base-backup.sh` 周期 `pg_basebackup`；wal-archiver 服务（mc 镜像）经 `wal/archive-wal.sh` 把 WAL 段与基础备份（**显式 `mc alias set` 后**）推 MinIO（WAL 成功后删本地、base 推 base/ 前缀）；首次 init 经 `wal/init-replica-hba.sh` 补 `host replication` pg_hba。**归档卷属主自 Q185 起改由 compose 一次性 root 服务 `wal-archive-init` 预备**（postgres `depends_on: service_completed_successfully`），不再由 initdb.d 脚本 chown——官方镜像以 **postgres（uid 999）** 身份执行 initdb.d 脚本，对 root:root 的空命名卷 chown/chmod 双双 EPERM 会被 `set -e` 打死、导致**全新卷 `docker compose up` 必挂**（Q185 演练抓到并修复）。**关键运维事实**：① 官方 pg 镜像默认 pg_hba 不含 replication 条目，pg_basebackup 跨容器须显式加；② 命名卷默认 root，归档卷须 chown 999（postgres）、MinIO 卷 chown 1000；③ mc 跑 shell 须 `--entrypoint sh`、MC_HOST env 在 sh 场景不加载，统一显式 alias；④ Docker Hub 用户命名空间镜像（minio/mc、prometheus）本机拉取被拒，**统一用 quay.io 源**（grafana/grafana 为 library 位、docker.io 可正常拉取）；⑤ **wal-archiver 的 minio 依赖须 `required: false`**——staging/ha overlay 把 minio 挪进非激活 profile 时，`depends_on` 指向未定义服务会让整个 compose project 判 `invalid compose project`（即 §7.1/§7.3 彩排命令不可用，Q185 修）；overlay 同时把 wal-archiver 一并移出，否则无 minio 时它无限重启并拖住 `--wait`。PITR 端到端演练见 §7.5。
- Q187 段12 discarded 成品保留期清理（**第五个 SLA sweep 作业**，迁移 0041 纯种子、无 schema 变更）：`app/core/sla/jobs.py` JOBS 追加 `discard_purge`，实现在 `app/content/service.py::purge_expired_discarded`。**破坏性动作、env 门控 `LOOM_DISCARD_PURGE_ENABLED` 默认 false**（关＝作业空转返回 0、V1 既有 sweep 行为一字不变；同 Q87/Q137/Q161 先例）；批量上限 `LOOM_DISCARD_PURGE_BATCH=200`（运维防护参数）；保留窗口走配置中心键 `content.discard_retention_days`（默认 180 天，**热更、运营可改**，非 env）。两 env 旋钮按 Q135 先例**不入 backend/.env.example**。不新增端点、不新增管理页——手工触发复用既有 `POST /api/admin/sla/run`（platform_admin），自动触发复用 SweepScheduler（并继承 Q139 checkpoint 协作中止与 Q151 commit_guard 行级 fence）。**安全边界**：只清理**无任何下游引用**的 discarded 行（`effect_records.matched_content_id`/`effect_claims.content_id` 两硬 FK 与 `import_jobs.content_id` 软引用一律排除并永久留档），逐行写 `content.discard_purged` 审计。

**当前迁移头：0041_discard_retention_seed（2026-09-25，down_revision=0040_staff_api_keys；Q187 纯配置种子迁移、**无 schema 变更**，业务物理表仍 **62**、列仍 666）**；上一头 0040_staff_api_keys（Q178 新增 staff_api_keys 1 表，业务物理表 61→62；含 alembic_version 时 public 共 63 表）；迁移一律按 MIGRATION_CONVENTION 经 pgvector/pg16 容器一次性 up / downgrade-1 / up 实测（0040 经宿主端口 55442 往返实测），不进 pytest。
- **CI/CD 流程**：提交 → 单元/契约测试（16）→ 构建 → staging 部署 → Evaluation Dataset 回归（防红旗 S2"Skill 从未运行"）→ 人工 Gate → prod
  - **Evaluation 回归命令（M10-Q 实现补登，Q77）**：staging 闸门执行 `backend/.venv/bin/python eval/runner.py`（仓库根目录），扫描 `eval/datasets/skills/` 与 `eval/golden_cases/skills/` 全部 YAML 案例，任一失败/错误退出码非零即阻断发布；案例集与 target 契约见 `eval/README.md`。**CI workflow 已落地（Q111，2026-09-16，GitHub Actions；Q118 扩为七 checker、Q122 扩为八 checker）**：`.github/workflows/ci.yml` 在 push main/dev 与 pull_request 时跑两 job——backend（Python 3.12，仓库根 `pip install -e "./backend[dev]"`；working-directory=backend 下 `ruff check .` → `python -m pytest -q`（sqlite、无服务容器，当前 842 测试全绿（另 8 个 real-infra 例＋1 个 real-LLM e2e 例默认 skip，见 §7））；仓库根 `python eval/runner.py` 跑 Q77 的 101 案回归；零 secret、零外部服务）与 frontend（Node 20，working-directory=frontend，`npm ci` → `npm run typecheck` → 八 checker：tokens/nav/content/products/workbench/compliance/admin/settings，前六个 Q96–Q102 落地、settings 为 Q114 新增并于 Q118 接入 CI、**content 为 Q122 新增（客户内容页消息/文件/岛纪律/后端防漂移守卫；Q131 扩回填岛守卫）；admin 于 Q130 扩 effects 运营台守卫（sidebar 7→8 项）、Q167 扩 Agent Key 治理页守卫（8→9）、Q168 扩导出 job 管理页守卫（9→10），checker 文件集合不变（仍八 checker，仅扩断言）**）；守卫单测 `backend/tests/unit/test_ci_workflow_contract.py` 静态锁定 workflow 结构（触发/双 job/命令/零 secret），Q118 增「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除 checker 漏接，删闸即挂。不含 `ruff format --check`（历史文件尚未全量格式化）、`next build` 与 lint（均非既定门槛；Q122/Q123 切片本地另行 `npm run build` 实测通过）与 CD（部署仍随本流程后续阶段）。**已实际运行**：2026-09-16 落地至 2026-09-18 累计 24 次 run（dev push / pull_request / main merge，含 PR #42–#45）全部 success；最新 main run 35253008023（2026-09-17，Q118 随 PR #45 合并）；Q119–Q140 提交均已由负责人 push（dev 与 origin/dev 同步于 a339816）；push 后最新 CI run 状态以 GitHub Actions 页为准（本地不可见）。
- **版本与回滚**：应用版本 + PWS/FCW 快照版本分离；回滚中心（09 D3.12 Rollback Center）覆盖模板/原子/规则/Skill/Prompt
- **发布纪律**：AI 相关变更（Skill/Prompt/权重）必须可回滚、可审计（A4 Prompt 治理 + Q63 KUP 回滚）

---

## 2. 多租户部署

| 项 | 说明 | 来源 |
|---|---|---|
| 隔离模型 | V1 共享实例 + tenant_id 强隔离（Q33）；客户量大后评估独立实例 | Q33 / 14 §2.3 |
| 数据隔离 | 业务表 tenant_id；知识层共享表无 tenant_id（平台级） | 10 §1.1 |
| 互不感知 | 跨客户不查重、永不驳回、客户看不见彼此 | Q33 |
| 套餐/试用/续费/暂停 | 租户管理 + 计费（Basic $999 / Pro $2999 / Enterprise $9999，D3.11） | 09 D3.11 |
| Onboarding | 注册→套餐→团队→第一产品建模→上线（D3.11） | 09 D3.11 |

---

## 3. 密钥与安全

- 密钥管理：API Key 一 Agent 一 Key 可吊销（Q60b/Q67）；**密钥不入库、不进文档**（本项目 AGENTS.md 红线）
- 访问控制：RBAC 8 角色 + 权威扩展（07 §2.3）；红线操作（PWS 冻结/原子正式化/合规废弃/Prompt 修改）= 角色 + 人工 Gate（line 7674）
- 审计：writeAudit append-only（所有人工操作 + AI 调用）
- **数据端口令必填（Q203）**：`infra/docker-compose.yml` 里 `POSTGRES_PASSWORD` / `MINIO_ROOT_PASSWORD` **没有仓内默认值**（原 `:-loom`／`:-minioadmin` 回退已删），未设就由 postgres/minio 容器启动失败，而不是静默用公开弱口令。刻意不用 `${VAR:?}`——compose 在解析期全局求值，会打断没设这两个变量的 overlay/彩排（Q185 同型坑）；`LOOM_MASTER_KEY` 空值则由 backend entrypoint 启动即 `exit 1`（Q200 #33）。三套起 compose 栈的彩排（ha/load/alerting）各自注入演练专用值。**口径范围要划清（Q204）**：这条只约束 base `docker-compose.yml` 的**数据端凭证**——opt-in 的 `docker-compose.monitoring.yml` 里 Grafana 口令**恰恰**用 `${LOOM_GRAFANA_ADMIN_PASSWORD:?}`，因为那层栈本来就只在显式带 `--profile monitoring` 调用时才需要该变量，不存在"打断别的调用"的问题。契约测试 `test_every_compose_file_is_free_of_credential_defaults` 扫的是 base＋四个 overlay 全部文件，任何一份日后塞回口令默认值都判红。
- **发证口需内部令牌（Q203）**：`POST /api/fcw/assemble`、`POST /api/fcw/assembly-tasks` 在默认形态（门控关）下也要求 `loom_staff_` Bearer，运维脚本无令牌即 401——上线引导顺序见 docs/21 §2。
- 外部回调安全：effect-callback 鉴权 + 限流 + 幂等（11 §2.1/§3）
- 脱敏：AG-SUPPORT 上线前脱敏审核（Q66）；staging 数据脱敏

---

## 4. 监控与成本治理

| 监控项 | 内容 | 来源 |
|---|---|---|
| 应用监控 | 接口错误率/延迟/SLA 引擎超时（24h 黄/48h 升级） | Q49 |
| token 成本 | 10 条省 token 策略落地验证 + Token 成本看板（系统管理员） | 09 D3.9 / D7.2 |
| 模型单价 | 统一 per-1M 口径（Q67）；日预算 Opus 50 / Sonnet 30 / Haiku 10 美元 | line 2103 |
| 反馈校准 | 两张常设月度校准报表（FCW score vs 互动、AI 质量分 vs 互动），分档样本 ≥50 提示可校准 | Q65 |
| 治理看板 | 6 个治理驾驶舱（知识库健康/Skill 质量/Token 成本/白名单表现/平台规则/人工审核） | 09 D3.9 |

---

## 5. 备份与恢复（建议）

- 数据备份：关系库每日全量 + WAL 增量；对象存储（素材/成品）跨区冗余
- 快照不可变：PWS/SkillRunLog/审计 append-only，备份需支持历史版本追溯（Q31 全保留）
- 灾难恢复：RPO ≤ 24h / RTO ≤ 4h ✅ **已量化达标（Q175，见 §7.4）**；更严目标随 V2/V3 客户规模收紧
- 备份制品与恢复验证（已落地）：Q146 的 db-backup sidecar 每日全量 `pg_dump`（custom/compress=9）+ 7 天保留（脚本 `infra/backup/backup.sh`，compose 服务 db-backup）；异机完整恢复已经 Q158 端到端演练验证（`infra/restore-rehearsal.sh`，见 §7.2）；**RPO/RTO 已经 Q175 故障注入＋计时演练量化达标（`infra/rpo-rto-rehearsal.sh`，见 §7.4）**。**WAL 增量归档＋基础备份/WAL 推对象存储（MinIO）已经 Q182 落地**（postgres 归档 + db-basebackup + wal-archiver，见 §1 与 §7.5 PITR 演练），分钟级 RPO 与时点恢复（PITR）能力就位；指标级监控已经 Q181 落地（GET /metrics，见 §1）。对象存储跨 region 冗余仍为【建议】，随 V2/V3。

---

## 6. 与既有文档的核对项

- [x] 部署形态与 14 选型一致（模块化单体/共享实例起步，PG+pgvector+Redis，引用 14 §2.1/2.3）
- [x] 监控项覆盖 09 §7 成本治理、Q65 校准报表、Q67 单价口径
- [x] 无密钥、令牌、敏感信息出现在文档中（AGENTS.md 红线）
- [x] §1 具体环境/中间件已补（2026-09-13：FastAPI / PG16+pgvector / Redis7 / S3 / Next.js；选型定稿后）

---

## 7. 真容器多副本验证（Q154，可选门禁；2026-09-21）

Q133–Q143/Q151/Q152 的多副本锁、Redis Streams、配置广播、下游 PG 行级 fence 在常规 CI 中一律以内存替身（FakeRedis/FakeStreams/FakeBroadcast、sqlite）验证，**不依赖外部服务**。Q154 新增一组**默认 skip、仅在显式提供真容器 env 时运行**的集成测试 `backend/tests/integration/test_real_infra.py`（9 例），用于在本机/CI 手动复跑真 Redis/真 PG 下的多副本原语语义；它**不是**常规门禁（`pytest -q` 无 env 时 9 例全 skip），也**不替代** V2 的真多副本多 worker 持续负载端到端部署演练。

- **覆盖（9 例＝真 PG 4／真 Redis 5）**：真 PG 两独立连接池跨连接认领/升级与条件 UPDATE 拒旧 fence（restock `restock_claims` 与 sweep `sweep_tick_claims` 参数化）、真并发行锁串行化；真 Redis `leader_lease` SET NX 互斥/INCR 单调/短 TTL 易主看门狗置 lost＋raise LockLost＋安全释放、Streams 两 consumer `XREADGROUP '>'` 不重叠分片/XACK/XCLAIM 接管/move_to_dead 死信、pub/sub 失效广播跨连接投递触发 reload；**Q188 补第 9 例**：真 Redis 下 `stream_depth` 的 ``XPENDING`` 汇总与 ``XLEN`` 语义（pending 只数已投递未 ACK／XACK 后 pending 回落而 XLEN 不变／组不存在时 NOGROUP 归一成 `StreamBackendError` 且被取样层吞掉保持旧值）——该语义此前只在自建假件上验过，而假件按假设写成、证不了伪。
- **一次性真容器（`--rm`，端口避开本机 atlas-pg 的 5432；验证完 `docker stop` 自删）**：
  - PG（fencing 用，fixture 只 create_all 两 fence 表）：`docker run -d --rm --name loom-realinfra-pg -e POSTGRES_USER=loom -e POSTGRES_PASSWORD=loom -e POSTGRES_DB=loom -p 55433:5432 pgvector/pgvector:pg16`
  - Redis：`docker run -d --rm --name loom-realinfra-redis -p 6380:6379 redis:7`
- **运行（在 `backend/` 目录）**：
  `LOOM_TEST_REAL_PG_DSN="postgresql+asyncpg://loom:loom@localhost:55433/loom" LOOM_TEST_REAL_REDIS_URL="redis://localhost:6380/0" .venv/bin/python -m pytest tests/integration/test_real_infra.py -v`
  → **9 passed**（本文件 §7 开头的「9 例」是本批复核后的当前数；下句括注里的 8 是 Q188 加第 9 例之前的历史快照，不回改）。不设两个 env 直接 `pytest -q` → **9 skipped**（总收集 769＝761 常规 + 8 real-infra；747→750 Q155、→757 Q156、→761 Q157）。
- **Q209 起这 9 例进 CI，不再是「可选手动门禁」**：`.github/workflows/ci.yml` 新增第四个 job `real-infra`（`pgvector/pgvector:pg16` ＋ `redis:7-alpine` 两 service，注入两个 env），本机实跑 **6.5–7.9s**。job 内带 **skip 哨兵**：这一族靠 env 门控，env 没设时 pytest 是「全 skip + exit 0」，不加哨兵就是永远绿而从没跑过一行真断言；哨兵取 summary 末行，命中 `skipped`/`no tests ran`/无 `passed` 任一情形即 `exit 1`（已双向证伪：撤 env 判红、env 齐判绿）。刻意**不钉用例数**（加一例就假红，与 Q207 不钉表数同理由）。本节的一次性容器命令仍保留，供本地复现与调试。
- **清理**：`docker stop loom-realinfra-pg loom-realinfra-redis`（`--rm` 自动删除，勿触碰本机 atlas-pg/atlas-db-1）。
- **已知替身差异（真容器首次实跑抓到）**：`restock_claims.request_id` 为 varchar(36)，测试 key 不得带超长前缀（基线初版 49 字符撞真 PG `StringDataRightTruncation`，sqlite 替身不强制 varchar 长度故从不报错），统一用 32 字符 `uuid.uuid4().hex`（同时满足 `sweep_tick_claims.lock_name` varchar(64)）。
- **仍挂 V2**：真多副本、多 worker 进程的持续负载端到端部署演练（本节在单进程内用两独立连接复现全部原语语义）、restock worker 主认领切消费组水平并行（花钱隔离，需重裁 Q87/Q89 leader 单实例防双花）。
- 另：Q153 docs/10 第二轮 DBA 全量列复核用同类一次性 PG16 容器（宿主 55432）跑全链 `alembic upgrade head`（到 0038）后导出 information_schema/pg_indexes/pg_constraint 并与全量 ORM metadata 比对，实测 60 表/637 列/212 索引/PK60·FK32·UQ20、列漂移 0；该复核为一次性人工实测、脚本不入库，结论见 docs/10 §4。

### 7.1 真多副本多 worker 持续负载端到端部署演练（Q157，2026-09-21，本地手动、不进 CI）

Q154 销原语级信任缺口后仍挂账的「真多副本、多 worker 进程持续负载端到端部署演练」由 Q157 落地（02 C1.101），与 Q147 staging 彩排同属**本地手动演练、不进 CI**；全程不注入 LLM key（synthetic 不花真钱）、不造业务主数据（空导出也能验「每 job 恰被一 consumer 处理一次」）、不拉 frontend（SSR 已由 Q147 验）。

- **演练制品（入库）**：`infra/docker-compose.ha.yml`（叠加在基础 compose + staging overlay 之上：backend `deploy.replicas:2`，开 `LOOM_DISTRIBUTED_LOCK_ENABLED`/`LOOM_RESTOCK_WORKER_ENABLED`/`LOOM_RESTOCK_STREAM_ENABLED`/`LOOM_EXPORT_WORKER_ENABLED`（两副本各 1 consumer 同消费组验分片）/`LOOM_CONFIG_CACHE_BROADCAST_ENABLED`，LLM base url 置空保 synthetic；只起 postgres/redis/backend，不发布宿主端口）；`infra/ha-rehearsal.sh`（子命令 `up`/`run`/`logs`/`down`，四阶段 16 项断言；compose project=`loom-ha`，跑完 `down -v` 清卷）。
- **多副本并发启动迁移串行化**：`backend/alembic/env.py` 对 PostgreSQL 方言用独立长连接（NullPool）`SELECT pg_advisory_lock(0x4C4F4F4D)`（"LOOM"）包住「确保版本表 + 迁移」、finally `pg_advisory_unlock`，其他方言/offline 原行为；多副本 entrypoint 并发 `alembic upgrade head` 时一个全量升级、其余阻塞获锁后发现已 head 而 no-op（零迁移，改的是迁移运行方式）。
- **容器应用日志可见性**：新增 `backend/uvicorn-log.json`（root level INFO→stdout、disable_existing_loggers=false、uvicorn.* propagate），docker-entrypoint.sh 的 uvicorn 加 `--log-config /app/uvicorn-log.json`；默认 uvicorn 配置只输出 uvicorn.* 日志，app.core.* 的 worker/锁/广播 INFO 行会被 root 的 WARNING 级别过滤。
- **运行（在 `infra/` 目录，需本机 Docker；不触碰本机 atlas-pg）**：`./ha-rehearsal.sh up`（构建/起栈并跑全部断言）、`./ha-rehearsal.sh run`（栈已在跑只重跑断言）、`./ha-rehearsal.sh logs`、`./ha-rehearsal.sh down`（脚本本身不带 -v，清卷手动加 -v）。
- **实测（2026-09-21，全新库 down -v 后，HA_JOBS=24）16/16 PASS**：① 迁移两副本合计恰 38 条 Running upgrade（一副本 38、另一 0=no-op）、无 already exists/duplicate key、两副本 healthy；② 24 个导出 job 全 completed、job_id 唯一 24、两副本 consumer 分片 **12/12**、合计恰 24（每条处理一次）、两副本空轮询 tick skipped=0；③ SLA `/api/admin/sla/run` 8 并发＝2×200+6×409、restock `/api/admin/restock/run`＝6×200+2×409（抢不到 leader 锁 409、无 5xx；restock 空库无 requested 信号，只验单 leader 锁互斥，「不双花」由 Q87/Q89 leader 单实例 + Q143/Q151 PG fence 保证）；④ PUT `content.ai_quality_threshold`=0.82 后对端副本日志命中 `config cache reloaded key ... from invalidation broadcast`，末尾自动还原 0.85。
- **演练抓到并修复的真实缺陷（FakeStreams 替身测不到，同 Q154 抓 request_id 截断先例）**：redis-py 8.1.0 asyncio 客户端把 `XREADGROUP BLOCK` 到期（无消息）抛成 `redis.exceptions.TimeoutError`（"Timeout reading from redis"，RedisError 子类、**非**内置 TimeoutError 子类；部分版本直接抛内置 asyncio.TimeoutError）；`queue/streams.read_new` 原只 `except redis.RedisError` 转 StreamBackendError，致 worker 每 block 周期 tick skipped、退避 5s、空转不消费也不 reclaim，多副本下只有未踩退避窗口的 consumer 通吃（首跑 24 job 全落一副本、两副本各 13 次 tick skipped）。修复：block 模式下两类 TimeoutError 归一返回 `[]`（BLOCK 到期协议语义＝本轮无消息，与同步客户端返回 None 一致），非 block 读取超时仍 fail-closed，真连接故障 ConnectionError（非 TimeoutError）仍走 RedisError 分支不被吞；`test_redis_streams.py` 8→12 例。
- **仍挂 V2**：restock worker 主认领切消费组水平并行（花钱隔离，需重裁 Q87/Q89 leader 单实例防双花）；Q157 空库不验真实 final_id 的持续负载全链（待业务方按 docs/19 回填主数据，见 handoff 待办 7②）。

### 7.2 异机备份恢复端到端演练（Q158，2026-09-21，本地手动、不进 CI）

Q146（C1.90）落了每日全量 pg_dump 备份与保留，但备份能否在一台**全新异机** PG 上完整恢复从未端到端演练；Q158 补齐这条恢复腿（02 C1.102），与 Q147 staging 彩排、Q157 HA 演练同属本地手动演练、不进 CI；全程不起应用镜像、不注入 LLM key（synthetic 不花真钱）、不触碰本机 atlas-pg。

- **演练制品（入库）**：`infra/restore-rehearsal.sh`（子命令 `up`/`down`，`KEEP=1` 跑完保留容器排查；风格同 `ha-rehearsal.sh`）。两个相互独立的一次性 `pgvector/pgvector:pg16` 容器：源库 `loom-restore-src`（宿主 55440）、异机库 `loom-restore-dst`（宿主 55441），不同容器/端口/数据卷，`--rm` 跑完自删。
- **流程**：源库 A 经宿主 backend/.venv 设 `LOOM_DATABASE_DSN` 跑 `alembic upgrade head`（到 0038，38 条）；再造 tenants×2/export_jobs×3/effect_records×3（覆盖 JSONB/timestamptz/可空 metrics/任务/时序），叠加迁移种子共 19 张非空表；用与 `infra/backup/backup.sh` 相同的 `pg_dump --no-password --format=custom --compress=9` 备份（**custom 二进制，恢复必须用 pg_restore、不能 `psql <`**），dump 离源经宿主再进异机；异机 B drop/create 空 loom 库后 `pg_restore --no-owner`。
- **运行（在 `infra/` 目录，需本机 Docker 与 backend/.venv）**：`./restore-rehearsal.sh up`（全流程并清理）、`./restore-rehearsal.sh down`（清理残留容器）。
- **实测（2026-09-21，全新库）12/12 PASS**：源库迁移 38 条 rc=0；dump 155110 字节、TOC 含 61 条 TABLE DATA；异机 pg_restore rc=0、error 行=0；两库 alembic_version 均 `0038_sweep_tick_claims`；业务表 60/含版本表共 61 且集合 A==B；全表逐表行数 A==B（非空表 19、零差异）；结构指纹 indexes=213/PK=61/FK=32/UQ=20/vector 列=2/pgvector 扩展 A==B；tenants/export_jobs/effect_records 演练行整行（JSONB/timestamptz/可空 metrics）逐字段一致；config_items 61/61、QC 阈值 0.85 一致；`import app.main` ORM 连恢复库只读成功；恢复库再 `alembic upgrade head` 幂等 no-op（0 条 Running upgrade）。
- **仍挂 V2**：WAL 增量备份、对象存储（素材/成品）跨区冗余、指标级监控、持续高压压测（Q157 留；RPO/RTO 量化认证已随 Q175 落地见 §7.4）。


**Q169–Q172（2026-09-22，02 C1.113–116，全部零迁移）**：Q169 全链 golden path e2e（段1→13 单产品真 API 贯通，sqlite 入 CI / 真 PG 走 `infra/fullchain-rehearsal.sh` 一次性容器，本地手动不进 CI）；Q170 运营 SOP 落 docs/21；Q171 DBA 第三轮全量列复核（head=0039：61 表/654 列/215 业务索引/PK61·FK32·UQ20，零漂移）；Q172 真 LLM 全链演练（agnes CAT-RECOG+ARTICLE-GEN 真模型，gated e2e 默认 skip），远程驱动新增 env `LOOM_LLM_HTTP_TIMEOUT_SECONDS=60`（默认值＝原硬编码 60s，演练取 180；运维旋钮按 Q135 先例不入 .env.example）。

### 7.3 持续高压负载端到端演练（Q173，2026-09-23，本地手动、不进 CI）

Q157 HA 演练只做短时突发（24 jobs、瞬时锁竞争），「持续高压压测」在 Q157/Q158 两次复盘后仍挂账；Q173 在同一 HA 形态（2 backend 副本 + 真 PG/Redis，synthetic 不花真钱）上施加**持续数分钟**的混合负载并补齐缺口（02 C1.117），与 Q147/Q157/Q158 同属本地手动演练、不进 CI。

- **演练制品（入库）**：`infra/load-rehearsal.sh`（子命令 `up`/`run`/`down`；compose project=`loom-load`，每轮负载用唯一租户 `load-rehearsal-<ts>` 隔离，跑完不自动清卷）。两个 driver 容器各自并发执行 producer（按节奏 `POST /api/exports/jobs`）+ reader（job 列表/token-cost 驾驶舱）+ churner（每 tick 扇出 8 个 SLA/restock `/run` 抢锁）；sampler 每 30s 记录健康副本数与队列分布。
- **harness 隔离口径（关键工程接缝）**：driver/sampler/drain 全部跑在一次性 `docker run --rm` 容器里（`--entrypoint python` 绕过镜像的迁移 ENTRYPOINT，挂 `${PROJECT}_default` 网络经 `backend:8000` DNS 负载均衡打两副本），**绝不 docker exec 进 app 容器**：exec 进程落在 app 容器 cgroup 内，其内存膨胀/OOM 会在 app 容器上永久钉 `OOMKilled=true`（污染应用断言），且 app 容器 PID1（uvicorn）不 reaper，exec 结束后留僵尸。driver 另加 `--memory`（env `LOOM_DRIVER_MEMORY` 默认 1g）限制自身内存。
- **演练抓到并修复的真实产品缺陷（feat b1f73c5/test cb68059）**：`GET /api/exports/jobs` 自 Q137 起页大小硬顶 `le=200`、**没有 offset**——Q142 只给 fcw.csv/json 加了分页，任务列表口租户超 200 个 job 后旧任务永远翻不到。演练分页器假设 offset 存在，反复拿同一首页（200 行）无限追加，driver RSS 一路涨到 6.2GB 被宿主全局 OOM 杀，drain 也随之失败。修复：service/router 透传 `offset`（`ge=0`），响应附 `offset`；新增分页不重叠集成测试。
- **运行（在 `infra/` 目录，需本机 Docker；不触碰本机 atlas-pg）**：`./load-rehearsal.sh up`（构建/起栈并跑）、`./load-rehearsal.sh run`（栈已在跑只重跑）、`./load-rehearsal.sh down`。可调：`LOOM_LOAD_MINUTES`（默认 5）、`LOOM_LOAD_RATE`（默认 1.0 job/s/副本）、`LOOM_CHURN_EVERY`（默认 2s）、`LOOM_CHURN_BURST`（默认 8）。
- **实测（2026-09-23，head=0039 既有库，正式 5 分钟）9/9 PASS**：两 driver 共提交 **562** 个 export job、post_fail=0，全部 completed（提交数==completed，无丢单、UNIQUE 无重），停流后队列 drain 干净（queued+running=0）、failed=0；reader 562 读零失败；churn 合计 `/run` **200=414、409=1842**（持续并发抢锁单 leader 互斥）、run_other=0；**全程零 5xx**；结束时两副本均 healthy、RestartCount 合计 0、无 OOMKill、日志无未处理 Traceback。
- **仍挂 V2**：真实 final_id 数据下的持续负载（待业务方按 docs/19 回填主数据，见 handoff 待办 7②）、restock 消费组水平并行（花钱隔离，需重裁 Q87/Q89）、正式 QPS/P99 容量口径（Q173 负载演练腿已落）。RPO·RTO 量化认证已随 Q175 落地（见 §7.4）。

### 7.4 RPO·RTO 量化达标演练（Q175，2026-09-23，本地手动、不进 CI）

Q146 落了每日全量备份、Q158 验证了异机恢复内容零差异，但「故障后丢多少（RPO）、多久恢复（RTO）」未量化；Q175 以一次性 PG16 容器做故障注入＋计时恢复（02 C1.119），与 Q158/Q173 同属本地手动、不进 CI。

- **演练制品（入库）**：`infra/rpo-rto-rehearsal.sh`（子命令 `up`/`down`，宿主端口 55446/55447 避本机 atlas-pg，KEEP=1 保留容器）。流程：源库 alembic upgrade head（真实 head/61 表）＋造 tenants 2 行 → Q146 同款 pg_dump（custom/compress=9，记 T_DUMP）→ 备份后再写 1 行（模拟备份后新增）→ **故障注入 `docker rm -f` 源库**（记 T_FAIL）→ 新机起替代空库 drop/create＋pg_restore＋应用 ORM 栈只读冒烟（恢复动作全程计时）。
- **量化口径**：RPO_OBSERVED = T_FAIL − T_DUMP（备份间隔即最坏丢失窗口；备份后那行在恢复中确认丢失，恢复后 tenants=2）；RTO_OBSERVED = 恢复动作（起库＋restore＋应用只读确认）耗时。
- **达标论证（诚实边界）**：RPO——生产每日 1 次全量备份，最坏丢失窗口＝备份间隔＝24h，故 RPO ≤ 24h 由备份频率保证，本次观测仅数秒；RTO——本演练认证「工程恢复动作」≤ 4h，不含人员发现/决策（生产由 Q146 watchdog 探活＋webhook 缩短发现段）。
- **实测（2026-09-23，head=0039）12/12 PASS**：`RPO_RTO_CERT {"rpo_target_seconds":86400,"rpo_observed_seconds":1.919,"rpo_pass":true,"rto_target_seconds":14400,"rto_observed_seconds":70.111,"rto_pass":true}`；恢复到备份点 tenants=2、备份后新增行确认丢失、应用栈 `import app.main` 连恢复库只读成功。
- **仍挂 V2**：WAL 增量备份（缩短 RPO 到分钟级）、对象存储跨区冗余、指标级监控。（**2026-09-24 更新**：WAL 增量归档/对象存储备份已经 Q182 落地、指标级监控已经 Q181 落地，见 §7.5 与 §1；仅剩对象存储跨 region 冗余仍 V2。）

### 7.5 WAL 归档 + 对象存储 PITR 端到端演练（Q182，2026-09-24，本地手动、不进 CI）

Q146/Q158 的 pg_dump 逻辑备份只到日级、无增量无 PITR；Q182 补齐 WAL 增量归档、基础备份与 WAL 推对象存储，并以自包含演练验证时点恢复（02 C1.126），与 §7.1–7.4 同属本地手动演练、不进 CI。

- **演练制品（入库）**：`infra/wal/pitr-rehearsal.sh`（自包含：临时 docker 网络 + 全命名卷，trap 自动清理；镜像全部 quay.io/官方，本地已缓存可离线重跑）。
- **顺序铁律**：基础备份（pg_basebackup）**必须早于**恢复目标时间 T——先起归档源库、补 replication pg_hba、做空库基础备份，再造 before 行 / 取目标时间 T / 造 after 行；待 WAL 归档后把基础备份与 WAL 推 MinIO，再从 MinIO 拉回（模拟灾备），解 base.tar.gz/pg_wal.tar.gz、touch recovery.signal、postgresql.auto.conf 写 restore_command / recovery_target_time=T / recovery_target_action=promote，起恢复库前滚到 T。
- **实测（2026-09-24）PASS**：基础备份 + WAL 经 MinIO 往返恢复，断言 **before=1（T 前事务在）、after=0（T 后事务不在）**，输出「PITR rehearsal OK ... rolled forward to T exactly」。
- **运维坑（实录）**：① 官方 postgres/pgvector 镜像 pg_setup_hba_conf 只追加 `host all all all`，**不含 host replication**，pg_basebackup 跨容器 FATAL → init-replica-hba.sh 补条目（演练 docker exec 追加 trust + reload）；② 命名卷默认 root 755，归档卷 chown 999、MinIO 卷 chown 1000；③ mc entrypoint=mc 跑 shell 须 `--entrypoint sh`，MC_HOST env 在 sh -c 不加载（写入不落 MinIO）→ 显式 `mc alias set`；④ Docker Hub 用户命名空间镜像拉取被拒 → quay.io。
- **能力提升**：归档持续推送下 RPO 从日级（pg_dump 24h）缩短到 WAL 归档间隔（archive_timeout=300s + 归档轮询，分钟级），并具备 PITR；正式 RPO 目标重裁、跨 region 对象存储冗余随 V2。

### 7.6 指标消费侧端到端演练（Q185，2026-09-25，本地手动、不进 CI）

Q181 只落了采集面（`GET /metrics`），`infra/monitoring/` 除 watchdog 探活外无任何规则/看板/投递。Q185 补消费侧并以真栈演练证明「采集 → 规则求值 → 转发投递 → 看板查询」四段真通（02 C1.129），与 §7.1–7.5 同属本地手动、不进 CI。

- **演练制品（入库）**：`infra/alerting-rehearsal.sh`（子命令 `up`/`run`/`down`；compose project=`loom-alerting`，叠 `docker-compose.yml` + `docker-compose.staging.yml`（重置宿主端口避本机 atlas-pg）+ `docker-compose.monitoring.yml`；HTTP 探针一律 `docker exec` 进 **watchdog** 容器用 python 打网格内地址——阶段 4 会停 backend，从 backend 发探针会随之失效，且 Grafana 刻意不发布宿主端口）。
- **五阶段断言**：① 静态校验 `promtool check config` / `check rules`（Q185 时 `SUCCESS: 3 rules found`，Q188 补业务规则后为 **`SUCCESS: 10 rules found`**）＋ Prometheus/Grafana readiness；② 采集面真通：backend target `health=up`、`http_requests_total` 与 `http_request_duration_seconds_bucket` 两族确实进 TSDB；③ 消费面就位：**十规则**装载且 **`health` 全为 `ok`**（Q188 业务族在无流量时本就无序列，空向量求值仍应为 ok——这条正是「无数据 ≠ 求值失败」的实证）（在真 TSDB 上表达式可求值，强于 promtool 的纯语法校验）、Grafana 看板 `loom-http` 与数据源 `loom-prom` 文件式置备可查、**经 Grafana 数据源代理查回 `up` 真值**（证明看板→Prometheus 链路通而非只落配置文件）、watchdog 转发 env 就位；④ **端到端触发**：`docker compose stop backend` → `up{job="loom-backend"}` 由 1 转 0 → `LoomBackendUnreachable` 转 firing → **watchdog 把该告警转发进 `LOOM_ALERT_WEBHOOK`，且 180s 内约 6 轮轮询里恰好命中 1 次（去重生效）** → 恢复 backend 后重新可抓取；**⑤ 业务告警实触发（Q193）**：演练叠加层 `docker-compose.alerting-rehearsal.yml` 开 `LOOM_EXPORT_WORKER_ENABLED`，停 redis 让 `POST /api/exports/jobs` 走 Q137 真实入流 fail-closed（503＋落 failed），在轮询内断言**曾观测到 `LoomJobFailed` firing 且带 `kind=export`**、watchdog 转发恰 1 次。**这是 Q188 那七条业务规则唯一的端到端证明**——此前只证过 `health=ok`（能求值），而实打第一次跑就抓出「懒创建 counter 首个样本非零 ⇒ `increase()` 恒 0 ⇒ 首次事件系统性不可告警」整类盲点（修法见 02 C1.137）。
- **实测（2026-09-25）**：Q185 首跑 **24/24 PASS、exit 0**；Q188 补业务规则/看板后重跑 **32/32**、Q192 加宿主侧断言后 **34/34**、Q193 加业务告警实触发后 **39/39 PASS、exit 0**（干净栈：容器 0 卷 0；firing 于 75s 观测、转发于 105s 恰 1 次）（新增断言＝10 条规则全部装载、`promtool check rules` 报 `SUCCESS: 10 rules found`、**七条业务规则在无任何业务序列的空栈上 `health` 仍全为 `ok`**——这就是「无数据 ≠ 求值失败」的实证，也是为什么裸 Gauge 规则不会在默认部署里误报；另加 `loom-operations` 看板置备可查）。
- **运维坑（实录，供复现）**：① **Alertmanager 不可用**——0.34.1 `webhook_configs.url` 不展开 `$(VAR)`（容器崩溃重启循环，日志 `invalid URL: unsupported scheme ""`）、`env://` 亦被 `amtool check-config` 判 `unsupported scheme "env"`，仅剩 `url_file` 需运维额外维护 URL 文件 → **撤除该容器，改由 watchdog 轮询 `/api/v1/alerts` 复用既有 `LOOM_ALERT_WEBHOOK` 转发**；② **compose `${VAR:?}` 在解析期全局求值、与 profile 无关**，故 Grafana 口令必填护栏必须与 opt-in overlay 同处一域，留 base 会打断不开监控的默认部署路径；③ **全新卷 `docker compose up` 必挂**（Q182 遗留）：官方镜像以 postgres（uid 999）身份跑 initdb.d 脚本，对 root:root 空命名卷 chown/chmod 双双 EPERM → `set -e` 打死 postgres → 改由一次性 root 服务 `wal-archive-init` 预备卷属主；④ **staging 彩排命令整体失效**（Q182 遗留）：wal-archiver `depends_on: minio` 遇 staging 把 minio 挪进非激活 profile → `invalid compose project` → 依赖标 `required: false` 且 overlay 同步移出 wal-archiver；⑤ Prometheus 规则 API 参数只认 `type=alert|record|all`（写 `alerting` 直接 400）；⑥ Grafana 数据源代理透传的 `/api/v1/query` 要 **GET + query-string**，POST JSON body 会被 Prometheus 判缺参；⑦ 告警名含数字（`LoomHigh5xxRatio`/`LoomHighP99…`）时用 `[A-Za-z]*` 收尾的正则会在数字处截断而误判条数。
- **顺带销账**：docs/20 §6.6 #14「指标级监控」的**消费侧**（采集已由 Q181 落）。
- **值班处置面**：10 条规则的「先查哪里／要不要停线」见 **docs/21 §5（Q191）**；本节只讲栈怎么起，不重复讲怎么响应。
- **仍挂**：① ~~业务级指标与对应告警全部待补~~ **已随 Q188 落地销账**（七族打点＋`loom-operations` 七条规则＋第二张看板，02 C1.132）；但**阈值合理性未经真实负载校准**——空栈演练只能证明「表达式可求值」，100/5000/30s 三个工程默认值须待业务方按 docs/19 回填主数据、有真队列与真 failed 后重看；② grouping/silencing/inhibition 随真实告警平台需求再评；③ Alertmanager→IM 之外的通知模板与静默窗口未做；④ 正式 QPS/P99 容量验收口径仍待业务给（同 §7.3）。

### 7.7 CI 内的真 PG 迁移门（Q207，2026-09-26）

四轮 DBA 全量列复核（Q153／Q171／Q179 等）每次都现场起一次性 pg16 容器、手工比对、然后把比对逻辑丢掉，
于是"模型⇄迁移零漂移"每隔几十片就要人工重新证明一次；而 CI 的两个 job 全跑在 sqlite 替身上，
**没有任何一步真正连过 PostgreSQL**。本批把这件事变成每次 push 的门。

- **落法**：`.github/workflows/ci.yml` 新增第三个 job `Migration gate`，service 用 `pgvector/pgvector:pg16`
  （embedding 列需要 vector 类型，官方 postgres 镜像不够），四步＝`alembic upgrade head` →
  `python scripts/dba_schema_check.py` → `alembic downgrade -1` → 再 `upgrade head`。
- **检查器**：`backend/scripts/dba_schema_check.py` 比对 表／列可空性与粗类型类／PK／FK 列对／唯一约束／索引；
  **多重集**比对（按集合会比不出"同列重复索引"）、**按列集不按名字**（本仓无 metadata 命名约定且迁移手写）。
  25 例单测含「验证验证器」：往库里种一条多出的列与一条重复索引必须判红，撤掉必须回绿。
- **不用 `alembic check`**：实测在 HEAD 上报 **19 条差异，全是索引命名**（`ix_audit_entity` vs
  `ix_audit_logs_entity_id`）加一处"唯一约束 vs 唯一索引"，属**永久红**，当门只会逼人把它关掉。
- **顺带修掉一个隐患**：`alembic/env.py` 此前只 import 24 个 `app.**.models` 中的 14 个 ⇒ `target_metadata`
  只见 44／62 张表；库里不缺东西（迁移全手写），但**一次 `--autogenerate` 就会提议 DROP 那 18 张表**。
  已补齐，并由 `test_every_model_module_is_visible_to_alembic` 钉住。
- **不检查的（别读大）**：列长度与精度（`String(36)` 与 `Text` 同类）、索引存储方法、默认值、CHECK 约束。
  Q154 那类 `varchar(36)` 长度回归**仍不在这一门内**，仍归 real-infra 用例。
- **Q209 起另一个真容器门也进 CI 了**：第四个 job `real-infra`（PG16 ＋ Redis 两 service，跑 `test_real_infra.py`
  9 例，详见 §7）。**仍在 CI 外的只剩下面这一条。**
- **仍未进 CI**：五套全栈演练（ha／load／alerting／restore／pitr）——它们要起多容器、断言依赖宿主网络，
  进 CI 会变 flaky 源，本批刻意不动；仍是本地手动，实测结果见 §7.1-§7.6。
- **门本身已被验证过在 runner 上会跑**：推送后 run `36231423209`（加门的 `30ead28`）与 `36232944989`（文档提交 `2364c82`）
  三个 job 全 success；`Migration gate` 的漂移步在 runner 上打印「表 62／列 666／PK 62／FK 32／唯一约束或唯一索引 24／
  普通索引 133」与 `NO DRIFT`，随后 `downgrade 0041 → 0040` 与再 `upgrade 0040 → 0041` 均成功。运维接手时**不必**再手工起
  pg16 容器做列复核——但要知道这台的 pg 是 `pgvector/pgvector:pg16`，与生产 compose 同镜像。
