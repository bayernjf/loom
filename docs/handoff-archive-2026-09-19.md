# Handoff Archive — 2026-09-19

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-19），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；2026-09-18 及更早归档见 `docs/handoff-archive-2026-09-18.md`。

## 最近进度归档（2026-09-18 滚出）

- **Q118 Q117 挂账四项收口（后端加固 + 迁移 0027 + CI；feat/test/chore/docs 原子提交；Alembic 头 0026→0027，后端 472→483 测试/101 eval，前端 tsc+七 checker 全绿，token 仍 86）**：负责人 2026-09-18「一口气开搞」，Q117 挂账 4 项均按推荐（甲）拍板记 Q118（02 C1.62），handoff 待办 6 与 AGENTS 4 项挂账全销。(1) **4 个漏网管理面 GET 补 query actor 闸**（Q109/Q113 同构，缺 actor_id 422/越权 403/命中 200，写口 body actor 服务层闸不动；前端全仓 grep 零消费）：content-goals=dictionary_admin（`require_content_goals_view`）、cp-law-domains=internal_compliance（`require_law_domains_view`）、publish-slots 与 slot-type-defaults=operations（原 `require_fit_weights_view` 重构为通用 `require_operations_view`，三 operations 读口共用）。(2) **publish_slots.gate 回填 V1 仅 Q42 人工直编**：默认 approved（模型列 + SlotUpsert 双默认）即权威口径，无 slot_items 表/无 pending_gate 产生路径/无 Gate 翻转端点，`pa_rules.GATE_PENDING` 为 V2 预留零引用，13 §1.9 表 AI 拓展 Gate 流整体随段7 完整版/V2；test_platform_api 锁定人工直建 gate=approved。(3) **迁移 0027_product_space_fks**：product_spaces.category_node_id→g1_categories.category_id、active_pws_id→pws_snapshots.pws_id 补真 FK（均 nullable，显式约束名，先 NOT EXISTS 幂等置空孤儿；两列 V1 代码零写入）；pgvector/pgvector:pg16 一次性容器实测全新库全链 0001→0027 up、downgrade-1/up 往返、孤儿 INSERT/UPDATE 被两 FK 分别拒、NULL 合法。(4) **CI 六→七 checker**：ci.yml 接入 check-settings（job/步骤名 six→seven，去掉 pytest 步骤名过时 444 硬编码），test_ci_workflow_contract 除枚举加 settings 外新增「磁盘 check-*.mjs 集合 == CI 接线集合」断言根除漏接。(5) **审计外修复**：Alembic 默认版本表 version_num VARCHAR(32) 容不下自 0021（33 字符）起的 revision id，全新 PG 全链 upgrade 必在 0021 截断（历史开发库未走全新全链故未暴露）；env.py 迁移前以独立事务幂等 CREATE IF NOT EXISTS + PG ALTER 加宽至 VARCHAR(128)，不动已发布迁移。测试 +11（参数化 +8、角色边界 +2、CI 集合 +1）；文档同步 02 C1.62/05 四端点行+补登段/10 §2.1+迁移登记/13 §1.9 转裁决/08 §2.2 基线与完成情况/handoff/AGENTS。下一点工前不自动开工；待点工切片仍是段12 余 3 项（视频/多语言/质量分复检）与 V2 余量（DIM-SOURCE 七路、配置广播/fencing token、Streams 认领、段13 effect-callback、CSV JSON/异步）。

## 最近进度归档（2026-09-19 续写滚出）

- **Q119 段12 多语言（Q58）切片（后端加法 + 迁移 0028；feat/test/chore/docs 原子提交、**本地 dev 未 push**；Alembic 头 0027→0028，后端 483→494 测试 / eval 101·golden 21 不变，ruff 净，前端无变化〔tsc+七 checker、token 86〕）**：负责人 2026-09-18「一口气搞了」，接缝按推荐（甲）拍板记 Q119（02 C1.63）。四项：① 语言清单表 `content_languages`（dictionary_admin CRUD 仿 content_goals、读口 query actor 闸；code=BCP-47 PK/name/markets=适用国家码数组〔**空数组=全市场含 country=NULL**〕/status active|archived；迁移种子仅 zh-CN、markets=[] 保证单语言向后兼容，其余语言业务方未给清单不预置，由 dictionary_admin 配置）；② `ProductSpace` 加 nullable `target_languages` JSON（产品侧目标语言，NULL/空=未声明不收窄；operations `PUT target-languages` 写入口已落，段1 录入表单控件挂账）；③ 交集纯函数 `covers_market`/`eligible_codes`（active 语言 markets 覆盖 `fcw.country`，再被 PS 目标语言非空收窄）；④ `content_products` 唯一约束 `(final_id,language,kind)`，generate 语言不交集→422（回带 eligible）、同 final+lang+kind 已存在→409，每语言一条成品天然独立复检/独立客户审（Q59 按 content_id 流程不分叉）。5 端点（admin/content-languages 的 GET/PUT/POST archive + content/eligible-languages + product-spaces/{id}/target-languages）；ARTICLE-GEN 模板加 `$language` 与硬性规则 4（须用目标语言撰写），synthetic 非 zh-CN 产出英文确定性占位，SkillRun.input_payload 记 language。测试 +11（交集纯函数单测 5 + 多语言集成 6；原 test_content_api fixture 自插 zh-CN 种子，否则交集为空 happy path 422）；迁移 0028 经 pgvector/pgvector:pg16 一次性容器 up / downgrade-1（表列约束全清回 0027）/ 再 up（恢复）实测对称。**挂账**：zh-CN 外语言清单（运营配置）、段1 目标语言录入控件、每语言成品的前端并列呈现（随 Q122 内容页）、视频生成。下一步 Q120 AI 质量分（Q57）/ Q121 复检语义级检测 / Q122 客户前端内容页待点工。
- **Q115/Q117/Q118 后文档状态残留订正（2026-09-18，纯文档收口；无新 Q、无代码/迁移/测试变化）**：负责人点工「一口气搞」，订正 11 处状态栏漏网——① handoff 文档清单表：12 号行 🟡 占位名单待 HTML 核对→🟢 Q115 定稿（7+35 即最终名单、保留【推断】标注），16 号行 🟡 与 12 联动→🟢 台账至 Q118；② README：两处 Q1–Q117→Q1–Q118，治理铁律第 4 条 line 溯源补 Q115 口径（基准 HTML 永久丢失、行号降级为历史痕迹）；③ docs/12：§1.2 标题「33 个需核对」→「35 个 Q115 定稿」、头部计数口径 33→35、缺口说明删「以基准 HTML 核对后为准」、§4 核对项勾选并注 Q115；④ docs/06：§3 占位名单【待补：从基准 HTML 导出】改 Q115 收口口径（以 12 号最终名单为准）、文末信息保全行同步。基线不变：后端 483 passed、ruff 净、eval 101/101、Alembic 头 0027、前端七 checker、token 86。

- **docs/17 补登 Q119/Q120（2026-09-18，纯文档收口；无新 Q、无代码/迁移/测试变化）**：负责人点工「一口气搞」，补 docs/17 部署运维 §1 两处 Q119/Q120 落地后漏同步——① 技术栈段补迁移 0028_content_languages（语言清单表+zh-CN 种子、PS.target_languages、成品唯一约束）与 0029_content_quality（quality_score/quality_issues 两 nullable 列）/0030_article_qc_seed（ARTICLE-QC 纯种子不建表）两句，当前迁移头 0027→0030（down=0029）；② CI 段后端测试数 483→511。CI 累计 run 数/最新 run id 为历史快照（Q119/Q120 本地未 push、无新 run），未改。复跑基线不变：后端 511 passed、ruff 净、eval 101/101、Alembic 头 0030。
- **Q120 段12 AI 质量分真打分（Q57）+ 重生成上限配置化（Q56）切片（后端加法 + 迁移 0029/0030；feat/test/chore/docs 原子提交、**本地 dev 未 push**；Alembic 头 0028→0030，后端 494→511 测试 / eval 101·golden 21 不变，ruff 净，前端零变化）**：负责人 2026-09-18「一口气搞了」，接缝按推荐（甲）拍板记 Q120（02 C1.64）。五项：① 模型网关新增第 8 场景 ARTICLE-QC（只读打分不改写，variables [body,language]，输出 {score,issues}；0030 纯种子：路由→synthetic + Prompt/PromptVersion v0.1）；② 正文生成 + 词库复检后、complete 前由新建 app/content/quality.py 内嵌调用，落 content_products.quality_score(Float)/quality_issues(JSON)（0029 两 nullable 列），零新 HTTP 端点，ContentProductView +4 字段 quality_score/issues/threshold/advisory；③ 定位纯 advisory——不自动发证/驳回、不新增 needs_manual 状态、不阻断 approve（Gate 仍客户审阅 Q59、六态机 Q116 不动），低于 content.ai_quality_threshold（默认 0.85）仅置 advisory=true 作界面提示；④ QC 故障/坏输出不阻断（ModelConfigError/ModelUnavailable/BudgetExhausted/GenerationUpstreamError，或非法 JSON/非对象/score 缺失越界/bool → score 留空、issues 记 qc_error、照常进 review；失败写审计 quality_unavailable 不落 SkillRun，成功落 SkillRun llm_auto/WF-10 + 审计 quality_scored）；⑤ Q56 上限配置化——revise_allowed 增 limit 形参、service 读 content.regen_limit（默认 3、运营可改），超限 revise 返回 422（detail 回带 {count}/{limit}）只能 reject；Q56 的 a 清洗区编辑 / b 作废 final_id 回池保留为人工动作、不自动作废。synthetic build_article_qc：哨兵 [LOW_QC]→0.62、空 body→0.55、否则 0.92。测试 +17（QC 解析/阈值/上限纯函数单测 13〔test_content_quality.py〕、质检集成 4〔test_content_quality_api.py：真打分落 SkillRun / 低分不阻断 approve / QC 故障容错 / regen_limit=1 闸〕；两内容 fixture 补 QC 三件套，happy path 一次生成 SkillRun 断言 1→2）；迁移 0029/0030 经 pgvector/pg16 一次性容器全新库 up / 逐档 downgrade（0030 种子全清、0029 两列全删）/ 再 up 实测对称，物理表仍 55。**挂账**：界面「AI 评估分，仅供参考」标注与每语言成品并列（随 Q122 内容页）、段13 回流后相关性校准（Q57 后置）、Q56 a/b 人工路径前端化、视频质量分（video-studio）、复检语义级检测（下一片 Q121）。下一步 Q121 复检语义级 / Q122 客户前端内容页待点工。
- **Q121 段12 复检第②项·语义级检测切片（后端加法 + 迁移 0031 纯种子；feat f30c747/test 2f1c585/chore 6e8bd1c/docs 原子提交、**本地 dev 未 push**；Alembic 头 0030→0031，后端 511→523 测试 / eval 101·golden 21 不变，ruff 净，前端零变化）**：负责人 2026-09-19「A 和 B 吧」点工 B 组，接缝按推荐（甲）拍板记 Q121（02 C1.65）。四项：① 模型网关新增第 9 场景 ARTICLE-SEMANTIC-CHECK（与 ARTICLE-QC 同构只读检测不改写，variables [body,language]，输出 {findings:[{code,message,excerpt}]}；0031 纯种子：路由→synthetic + Prompt/PromptVersion v0.1，四检测 code unsubstantiated_claim/absolute_guarantee/off_material_exaggeration/misleading_ambiguity 为 v0.1 工程口径待业务方/法审校准）；② ARTICLE-GEN 写 body、词库扫描后、ARTICLE-QC 前由新建 app/content/semantic.py 内嵌调用一次（generate/regenerate 同路径），结果聚合进既有 review_hits["semantic"]={checked,findings,?error}，零新 HTTP 端点/零新列/零新表；③ 纯 advisory——不抬 block_required、不阻断 approve、不新增状态（词库 ban 硬阻断原样保留，与 Q57 降级、Q66 AI 不持否决权一致，Gate 仍客户审阅 Q59）；④ 不可用/坏输出不阻断（三类网关异常→checked:false+error 段+审计 content.semantic_unavailable、不落 SkillRun；坏 JSON/非对象/findings 非数组归一为空+错误码但仍落 SkillRun（output 带 parse_error）+审计 content.semantic_checked；findings 逐项容错）。synthetic build_article_semantic_check：哨兵 [SEMANTIC_RISK]→1 条 unsubstantiated_claim、其余空。测试 +12（解析纯函数单测 8〔test_content_semantic.py〕、语义复检集成 4〔test_content_semantic_api.py：三场景 SkillRun 集合 {GEN,QC,SEMANTIC}、哨兵命中不阻断且可 approve、网关异常 checked=false 无 SkillRun 仍可 approve、regenerate 重跑 2 条〕；三个内容 fixture 补 SEMANTIC 三件套，happy path SkillRun 集合断言 2→3）；迁移 0031 经 pgvector/pg16 一次性容器 up（三件套就位）/ downgrade -1（语义三件套全清 0/0/0、QC 三件套保留 1/1/1）/ 再 up（恢复 1/1/1）实测对称，物理表仍 55。**挂账**：复检余两项（施工指令核对、国家规则核对）【原文未给出，待补】、四 code 口径业务方/法审校准与段13 命中率评估、语义发现界面提示（随 Q122）、视频语义复检（video-studio）。下一步 Q122 客户前端内容页 / B3 语言控件+Q56-a 人工路径待点工。

## 最近进度归档（2026-09-19 续写滚出·Q122/Q123）

- **Q123（B3）段1 客户目标语言录入控件切片（跨栈加法，零迁移；后端 feat a9193a1/test 50fa594、前端 feat c3af8bf/test 76fef22 原子提交、**本地 dev 未 push**；Alembic 头仍 0031/业务物理表 55，后端 528→534 测试 / eval 101·golden 21 不变，ruff 净，前端 tsc+八 checker、next build 成功，token 86 不变）**：与 Q122 同轮点工（负责人 2026-09-19「A 和 B 吧」+「按你的来/狠狠搞完」授权），接缝按推荐（甲）拍板记 Q123（02 C1.67，Q58 产品侧客户自助）。三项：① 新开客户侧写口 `PATCH /api/intakes/{intake_id}/target-languages`（按 **intake 维度**定位产品空间，客户 actor roles 恒空、无运营闸；码 strip/去重且必须在 active content_languages 内，未知/归档/重复 422，产品空间未生成 404，空数组=未声明写 NULL，刷新 updated_at，审计 product_space.target_languages.set 带 channel=customer_intake；product_intake 模块 service 内部 commit 惯例）；② 新开无闸只读 `GET /api/content/languages`（仅 active、按 code 排序、不含归档；管理面含归档清单仍走 /api/admin/content-languages + dictionary_admin 闸；**注册必须先于 GET /api/content/{content_id}**，否则 "languages" 被路径参数吞掉，check-content 加注册顺序断言）；③ Q119 operations 代设入口 `PUT /api/product-spaces/{id}/target-languages` 与 OPERATIONS 闸**原样保留**（回归测试锁 roles=[]→403、operations→200），ProductSpaceView 加 target_languages 回显（GET product-space 同步）。前端：lib/api.ts 加 ContentLanguageView/getContentLanguages/setIntakeTargetLanguages；products/actions.ts 加 setTargetLanguagesAction（"use server"，空码 422、403/404/409/422 同型映射）；新建 client 岛 target-languages-form.tsx（active 语言 checkbox 多选、回显已选、空选=不限制提示、useTransition+router.refresh、禁直连服务端访问层）；录入详情产品空间段挂岛（空间未生成不渲染，RSC 取清单，CURRENT_ACTOR_ID 未配置显错误提示）；products.module.css 追加 .subsectionTitle/.languageList/.languageOption；messages products.targetLanguages 四键；check-products 扩键/文件/api token/岛纪律/后端守卫，check-content 加语言端点与注册顺序守卫。测试 +6（tests/integration/test_target_languages_customer_api.py，内存 sqlite、zh-CN/en-US active + fr-FR archived、ps-1/int-1：清单无闸排除归档按 code 排序、客户 PATCH 200+GET 回显、空清单清 NULL、未知/归档/重复 422、无 ps 404、operations PUT 对客户 403 且 operations 200）。**挂账**：zh-CN 外语言清单待业务方提供后由 dictionary_admin 配置（不预置）。下一步：段12 余量（视频/复检余两项施工指令核对·国家规则核对）随 V2、新切片待点工。

- **Q122 客户「内容生产与发布」功能页 + Q56-a 客户人工改稿切片（跨栈加法，零迁移；后端 feat e346198/test bbbd4ae、前端 feat e41bad3/test ecdfec5 原子提交、**本地 dev 未 push**；Alembic 头仍 0031，后端 523→528 测试 / eval 101·golden 21 不变，ruff 净，前端 tsc+**八 checker**、next build 成功，token 86 不变）**：负责人 2026-09-19「A 和 B 吧」点工 B 组并多次授权「按你的来/狠狠搞完」；开工前就三接缝交互提问超时（负责人未在线），沿用授权**全部按推荐（甲）落地、待负责人回来追认**（02 C1.66；若不认可甲，客户页角色闸与页面需返工）：① **客户页能力=只读+审阅+人工改稿**——列表/详情/approve/reject/revise/Q56-a 进客户页，generate/regenerate 维持 operations 闸不放客户页（Q116 定稿不改，前端访问层禁出现两端点字面量，check-content 守卫）；② **Q56-a=客户自助人工编辑**——六态机加事件 `manual_resubmit`（revising→review，六态不变），`PATCH /api/content/{id}/body` 客户无闸（仅 revising 否则 409、空白 422、404），换正文后重跑词库+语义复检（Q121）+ARTICLE-QC（Q120）回 review，**不调 ARTICLE-GEN、不增 regenerate_count**（每轮仍须先 revise，间接受 content.regen_limit 闸），审计 content.body_edited 带 actor_roles；③ 目标语言客户录入另开客户侧端点（见 Q123）。另加无闸 `GET /api/content?tenant_id=`（列表项 ContentProductListItem 去 body、created_at DESC、未知租户空、缺 tenant_id 422）与 `GET /api/content/{id}`（详情含 body、404）。前端新建 (shell)/content/ 六文件（列表 RSC：六态 chip/AI 分 0–100/复检旗标 ban 红·降级与语义黄·无异常灰；详情：正文 pre、质量卡「仅供参考」、复检卡 bans/downgrades/语义 code 原样不翻译、checked=false 显暂不可用、驳回原因、review 挂 DecisionIsland〔block_required 禁通过、reject 原因必填〕、revising 挂 BodyEditIsland、ready 显待平台统一发布、404 notFound；actions.ts 四写 Server Action；两 client 岛仅同源 Action+router.refresh；css）；lib/api.ts content 段（客户 actor roles 恒空）；messages content 命名空间 +61 行（六态中文）；nav.ts content v2→v1；**新增第八 checker scripts/check-content.mjs**（消息键/文件/岛纪律/nav v1/后端路由与 manual_resubmit 防漂移/生成端点不外放），ci.yml seven→eight 接线，test_ci_workflow_contract 枚举同步。测试 +5（tests/integration/test_content_customer_api.py：列表租户隔离/列表项无 body/未知租户空/缺参 422、详情 404、人工编辑 happy path SkillRun 集合恰 GEN+QC×2+SEMANTIC×2 且 regenerate_count 恒 0、状态/空白/404 闸、编辑后词库 ban 与语义哨兵仍回 review）。**挂账**：Q56-b 作废 final_id 回池记难产原因（六态机无 discarded 态、段11 FCW 状态机无回池原文口径）、ready 后统一发布动作不在 V1 客户页、语义四 code 与 QC 阈值待业务方/法审校准。

## 最近进度归档（2026-09-20 续写滚出·Q124/Q125/Q126）

- **Q126 段13 反馈回流入站第一片·effect-callback（后端加法 + 迁移 0034 新建 effect_records 表；后端 feat 63a84c8/test 2b72768/chore 83d7ef6/docs 原子提交、**本地 dev 未 push**；Alembic 头 0033→0034/业务物理表 55→56，后端 554→591 测试〔+24 单测 +13 集成〕/ eval 101·golden 21 不变，ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人 2026-09-19「一口气再推进一些任务」，选 13 段链最后一段「段13 反馈回流」入站第一片（Q60 契约完整、Q88 验签半套与 Q125 发布回填已铺路、纯本地可闭环），接缝按推荐（甲）拍板记 Q126（02 C1.70）。九项：① 新横切包 `app/core/effects/`（models/schemas/service/router），单表 `effect_records` 用 status=matched|orphan 分流（不分两表），迁移 0034_effect_records（down=0033_content_publish）；② `POST /api/effect-callback` 复用 Q88 `require_agent_key` Bearer（缺失/错误/吊销统一 401，**首个受 Agent Key 保护的业务端点**，命中盖 last_used_at 同事务提交），请求 `{source,records[]}`；仅按推送 content_id 精确匹配**非 discarded** 成品——命中 status=matched 回填 matched_content_id(nullable FK)/tenant_id（Key 不绑租户，租户由 content.tenant_id 解析），查无或命中 discarded→orphan（tenant 留空），platform_post_id 只留存、不自动绑定（人工绑留给 Q60a）；③ metrics 纪律：六计数 plays/likes/comments/shares/inquiries/conversions 须非负整数、read_rate 须 0..1 数值，bool 显式拒绝、未知键拒绝、缺席键与显式 null 省略不落（**绝不当 0、不估算**，缺席记 NULL「—」），整组 metrics 可空；④ captured_at 须带时区（naive 422）、服务端归一 UTC，同 (content_id,captured_at) 重推幂等覆盖（不新增行、刷 updated_at）、不同 captured_at 追加为时间序列；⑤ 整批 all-or-nothing（同 Q93）：source 必填非空白、records≥1、逐条校验回 422 detail={index,field,message}、批内重复幂等键 422，成功 200 回执 {received,matched,orphan,upserted}；⑥ 运营只读 `GET /api/admin/effects/orphans`（跨租户孤儿、captured_at 升序）与 `GET /api/admin/effects?content_id=`（成品时序），operations|platform_admin 的 query actor 闸（缺 actor 422/越权 403，limit 1..100 默认 20、offset 0..500 默认 0）；⑦ 每批审计 effect.batch_received（tenant=_platform、actor_id=key_id、actor_roles=[]、detail 含 source 与四计数、entity_type=effect_batch/entity_id=uuid4）；⑧ 测试：tests/unit/test_effect_records.py 24 例（metrics 归一参数化/记录校验/UTC 归一）+ tests/integration/test_effect_callback_api.py 13 例（401 三态/matched·orphan·discarded 分流/稀疏不补 0/customer-backfill/幂等覆盖+时序追加/批内重复/五坏例整批不落/空批空 source/两只读口角色闸）；迁移 0034 经 pgvector/pgvector:pg16 一次性容器 up（12 列/jsonb/timestamptz/4 业务索引+主键/FK，物理表 55→56）/ downgrade -1 回 0033（表·四索引·FK 全清回 55）/ 再 up 恢复实测对称；⑨ **明确不落（随段13 续片/V2）**：Q60a 孤儿**人工认领动作**端点（续片需解决认领后不被推送冲回）、customer-backfill 客户专用通道（source 值已接受透传但不另建客户端点）、content_id↔platform_post_id 自助映射、**效果反哺知识与 Q54/Q57/Q61/Q65 评分阈值校准算法（口径原文未给【待补】，不臆造公式）**、Agent 抓取与发布自动化（Q62 本系统不抓取）；前端无改动、checker 仍八。下一步：段13 续片（Q60a 孤儿认领台/反哺校准待业务口径）或段12 视频支线（video-studio）/DIM-SOURCE 七路/V2 基建，待点工。

- **Q125 段12 运营发布回填与待发布队列切片 Q60c（后端加法 + 迁移 0033 + 前端管理台；后端 feat d55e4d5/test 7a9af70/chore 30dba15/fix fe82255、前端 feat 4d6f0b9/test 2bb3abd 原子提交、**本地 dev 未 push**；Alembic 头 0032→0033/业务物理表 55，后端 545→554 测试 / eval 101·golden 21 不变，ruff 净，前端 tsc+八 checker、next build 成功，token 86 不变）**：负责人对 Q60c 挂账点留言「给我推荐方案」后回复「好的，按你推荐的来」拍板记 Q125（02 C1.69）。五项：① **不新增 published 态**——ready_for_publish 仍终态，published_at 非空即已发布信号；迁移 0033_content_publish 加 nullable published_url Text / platform_post_id String(128) / published_at timestamptz 三列（down=0032 drop 三列）；② 写口 `PUT /api/admin/content/{id}/publish-info`（operations 硬闸：仅 ready_for_publish 否则 409、url 必填非空白 422、未知 404、客户 403；可重复修正 url 但 published_at 仅首次落、post_id 缺省不动/空串清空；审计 content.publish_info_set）；③ 读口 `GET /api/admin/content/ready-to-publish`（operations | platform_admin 双角色、跨租户 ready 行、created_at 升序、行不含 body；fe82255 定稿口径=**返全部 ready 行含已回填**，前端分待回填/已回填两分区，回填后不离队以便核对修正）；两个静态 GET 注册在参数 PUT/POST 之前（checker 顺序断言）；④ 前端新建管理端 `/admin/content` 内容运营台（与 Q124 共用一页四文件：page.tsx force-dynamic RSC 双队列 Promise.all、actions.ts 两 Server Action 含 unconfigured/missing_role/403/404/409/422 映射、publish-info-island.tsx、discard-island.tsx 含 window.confirm；sidebar 6→7 项；messages admin.contentOps.* 全套 + content.status.discarded + publishedLink*/discardedReasonTitle；客户列表 STATUS_TONE 加 discarded、.toneDiscarded 灰+删除线、客户详情页 ready 态显已回填外链否则「待平台统一发布」、discarded 态显作废原因；lib/api.ts 加四函数与 ListItem 四字段；check-admin/check-content 扩守卫，**无新 checker 文件、八 checker 总数不变**）；⑤ **明确不落（随段13/P3/V2）**：Agent 抓取、effect-callback 端点与 effect_records 时序、孤儿发布队列、Q60a 客户自助发布、发布自动化。测试 +9（tests/integration/test_content_publish_api.py：happy+审计、客户 403、未知 404、非 ready 409、空白 url 422、重复回填覆盖 url 但 published_at 不重置/空串清空 post_id、队列跨租户排序排除非 ready 且回填后仍在队列带链接、角色闸、platform_admin 读 200 写 403）；迁移 0033 经 pgvector/pgvector:pg16 一次性容器 up / downgrade -1（三列消失、discard_reason 保留）/ 再 up 实测对称。下一步：段13 effect-callback 整切片随 P3/V2、新切片待点工。

- **Q124 段12 难产骨架作废回池切片 Q56-b（后端加法 + 迁移 0032 + 前端管理台；后端 feat ccc23e6/ef6074b/test 370e94c/a45b635/chore 36c8133/fix a3ebf96、前端随 4d6f0b9/2bb3abd 原子提交、**本地 dev 未 push**；Alembic 头 0031→0032，后端 534→545 测试 / eval 101·golden 21 不变，ruff 净，前端 tsc+八 checker、next build 成功，token 86 不变）**：负责人对 Q56-b 挂账点留言「给我推荐方案」后回复「好的，按你推荐的来」拍板记 Q124（02 C1.68）。四项：① 内容状态机六态→**七态**，新增终态 **discarded（只读）**，与 rejected 严格分工（rejected=客户驳回、原因回流段13、可继续 revise/regenerate；discarded=运营判定骨架不要、释放白名单键生成机会）；事件 `discard`（operations 硬闸、reason 必填 1–500 字、空白 422）仅 review/revising/rejected → discarded，draft/generating/ready_for_publish 不可作废（409），终态 allowed_events 为空、客户 403、未知 404；② 新端点 `POST /api/content/{id}/discard`（落 discard_reason Text + 审计 content.discarded）与跨租户读口 `GET /api/admin/content/needs-attention`（review/revising/rejected、created_at 升序、行不含 body，operations | platform_admin 双角色；router `_query_actor` 改 *roles 可变参，a3ebf96 修正双角色读口）；③ **回池机制**=迁移 0032_content_discard 把普通唯一约束 uq_content_final_language_kind(final_id,language,kind) 换成 **partial unique index `WHERE status <> 'discarded'`**（模型 postgresql_where + sqlite_where 双方言），作废后同键可重新生成新成品、旧行只读保留，service generate 查重另加 status != discarded 双保险；不做 FCW 状态回退、不自动作废（达上限仍只 422，作废是运营显式动作）；④ 运营台作废岛与 Q125 共用 /admin/content 一页（见 Q125 条）。测试 +11（test_content_statemachine.py 扩 discarded 终态与三态可作废/四态不可约 +2 单测；新建 test_content_discard_api.py 9 集成：happy+审计+列表回带、客户 403、未知 404、revising/rejected 可作废、ready/draft 409、空白 reason 422、回池后同键重新生成两行独立且未作废重复仍 409、终态动作全拒、needs-attention 队列过滤排序与角色闸）；迁移 0032（down=0031：drop discard_reason、partial index 还原普通唯一索引；downgrade 不处理同键冲突，V1 无生产数据）经 pgvector/pgvector:pg16 一次性容器实测：全新库 up、discarded+live 同键共存而第三条 live 同键 duplicate key 409、downgrade -1 回 0031、再 up 恢复，往返对称。**挂账**：达上限自动转人工工单、discarded 行物理清理/归档（V2）。
