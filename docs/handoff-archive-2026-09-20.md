# Handoff Archive — 2026-09-20

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-20），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；2026-09-19 及更早归档见 `docs/handoff-archive-2026-09-19.md`。

## 最近进度归档（2026-09-20 滚出·Q128/Q129/Q130）

- **Q128 段13 customer-backfill 客户专用效果回填通道（后端加法，零迁移；后端 feat 5e60ef3/test 9447e33/docs 原子提交、**本地 dev 未 push**；Alembic 头仍 0035/业务物理表 57，后端 599→607 测试〔Q127+Q128 合计 +16 集成〕/ eval 101·golden 21 不变，ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人 2026-09-19「一口气搞了」点工段13 续片第三件，接缝按推荐（甲）拍板记 Q128（02 C1.72）。五项：① 新端点 `POST /api/effects/backfill`，**无 Agent Key**，body `{tenant_id, records[], actor}`（同 Q122 客户写口：actor 在体、客户 roles 恒空、无运营闸；tenant_id 口径同 `GET /api/content?tenant_id=`）；② source 服务端固定 `customer-backfill`，不接受客户端传入（对应 Q60 降级口径「客户手工回填转化」）；③ **客户通道绝不产生孤儿**——content_id 必须命中本租户非 discarded 成品，否则该条 422 detail `{index,field:"content_id",message}`，整批 all-or-nothing，且不查 effect_claims 认领映射兜底（认领是运营动作，客户通道不旁路）；④ captured_at 带时区（naive 422）归一 UTC、(content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏纪律与 Agent 通道完全一致（复用 Q126 抽出的 _normalize_records/_resolve_contents(tenant_id=)/_persist_records）；received_by=客户 actor.id（区别 Agent 通道 key_id）；审计 `effect.customer_backfilled`（**tenant=客户租户**，区别 Agent 通道 _platform）；回执 {received,matched,orphan,upserted}，orphan 恒 0；⑤ 测试 tests/integration/test_effect_customer_backfill_api.py 8 例（无 Key happy 落 source/received_by/tenant、幂等覆盖与时序追加、他租户 content 422 不落库、未知/discarded 双 422 且零孤儿、一条坏整批拒回带 index、metrics/captured_at 五坏例 all-or-nothing、空 tenant/空 records 422、批内重复点 422）。**不落**：客户回填 UI、效果反哺 KUP/校准算法（口径【待补】禁臆造）、Agent 抓取（Q62 不抓取）。

## 最近进度归档（2026-09-20 滚出·Q129）

- **Q129 段13 孤儿批量认领 + 取消认领/解绑（后端加法，零迁移；后端 feat bb7c8c2/test 2078bf3、**本地 dev 未 push**；Alembic 头仍 0035/业务物理表 57，后端 607→618 测试〔+11 集成〕/ eval 101·golden 21 不变，ruff 净，前端零变化）**：同批三件之一（02 C1.73，甲案待追认），销 Q127 挂账「取消认领/解绑、批量认领」。① 批量 `POST /api/admin/effects/claims/batch`，body `{items:[{record_id,content_id}](≥1), actor}`，逐条复用单条认领校验（未知记录 404/非 orphan 409/未知目标 404/discarded 目标 409），批内 record_id 重复 422 detail={index,field:"record_id",message}、空 items pydantic 422，**整批 all-or-nothing**（任一失败事务回滚零落库），回 {claimed, updated_rows}，逐条 effect.claimed 审计随事务；② 解绑 `POST /api/admin/effects/claims/unclaim`，body {external_content_id, actor}（operations 硬闸，客户/platform_admin 403，映射不存在 404 新异常 ClaimMappingNotFound）：删 effect_claims 映射行，并把该 external_content_id 下 **matched_content_id 等于旧映射目标 content_id** 的全部行回滚 orphan（清 matched_content_id/tenant_id/claimed_by/claimed_at 四列；实现中发现按 claimed_by 非空筛会漏掉认领后经兜底 matched 的新推送行，故按旧目标 content_id 筛；external_id 自匹配的真自动行天然不受影响），回 {external_content_id, reverted_rows}，审计 effect.claim_revoked（tenant=_platform、entity_type=effect_claim、detail 旧 content_id+reverted_rows）；③ service 抽 `_claim_one`（无角色闸），claim_orphan/batch/unclaim 各自先 require_any_role(OPERATIONS)；测试 test_effect_claims_api.py 8→19 例（unclaim 5 + batch 6）。

## 最近进度归档（2026-09-20 滚出·Q130）

- **Q130 管理端效果回流运营台 /admin/effects（纯前端加法，零迁移；前端 feat d668da2、**本地 dev 未 push**；后端 618 测试/eval 101·golden 21 不变，ruff 净，前端 tsc+八 checker、next build 成功，token 86 不变）**：同批三件之二（02 C1.74，甲案待追认）。新建 `app/[locale]/admin/effects/` 五文件：page.tsx（RSC force-dynamic，getEffectOrphans({limit:100}) 首屏孤儿队列 + 成品时序两区，缺 LOOM_ADMIN_ACTOR_ID 显 unconfigured、缺 operations 写口本地拦 missing_role）、actions.ts（"use server"：claimEffectAction/batchClaimEffectsAction/unclaimEffectAction/queryEffectSeriesAction，errorDetail 兼容批量 422 object detail）、orphan-table-island.tsx（单条认领 + 勾选批量认领，整批成功或整批拒绝，router.refresh）、series-island.tsx（content_id 查询时序，人工认领行显 claimed_by 与「取消认领」按钮，window.confirm 后解绑并按 queriedId 重查）、effect-fields.ts（METRIC_FIELDS 七键顺序、metricText/capturedAtText helper，缺席显 "—" 绝不显 0）；admin-sidebar 加第 8 项 /admin/effects（恰 8 项，labelKey admin.effectsNav）；lib/api.ts 加 effects 段（EffectRecordView 等类型、私有 adminQuery 拼 query actor、getEffectOrphans/getEffectSeries/claimEffect/batchClaimEffects/unclaim 管理员 actor、customerBackfillEffects 客户 actor 供 Q131）；messages 加 effectsNav 与完整 admin.effects.* 段（status.matched/orphan 枚举码原样不翻译）；check-admin.mjs 扩守卫（sidebar 恰 8 且含 /admin/effects、effects 五文件、admin.effects.* 键含 metric 七键、页 force-dynamic 只读、两岛 "use client"+禁 @/lib/api 裸 fetch+router.refresh、api.ts 六函数、后端 effects router 六路由齐备）。**V2 D3.6 五个分析二级页（归因/自学习/KUP）不提前实现**，本页是运营操作台而非分析页。
