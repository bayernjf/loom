# Handoff Archive — 2026-09-20

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-20），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；2026-09-19 及更早归档见 `docs/handoff-archive-2026-09-19.md`。

## 最近进度归档（2026-09-20 滚出·Q128）

- **Q128 段13 customer-backfill 客户专用效果回填通道（后端加法，零迁移；后端 feat 5e60ef3/test 9447e33/docs 原子提交、**本地 dev 未 push**；Alembic 头仍 0035/业务物理表 57，后端 599→607 测试〔Q127+Q128 合计 +16 集成〕/ eval 101·golden 21 不变，ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人 2026-09-19「一口气搞了」点工段13 续片第三件，接缝按推荐（甲）拍板记 Q128（02 C1.72）。五项：① 新端点 `POST /api/effects/backfill`，**无 Agent Key**，body `{tenant_id, records[], actor}`（同 Q122 客户写口：actor 在体、客户 roles 恒空、无运营闸；tenant_id 口径同 `GET /api/content?tenant_id=`）；② source 服务端固定 `customer-backfill`，不接受客户端传入（对应 Q60 降级口径「客户手工回填转化」）；③ **客户通道绝不产生孤儿**——content_id 必须命中本租户非 discarded 成品，否则该条 422 detail `{index,field:"content_id",message}`，整批 all-or-nothing，且不查 effect_claims 认领映射兜底（认领是运营动作，客户通道不旁路）；④ captured_at 带时区（naive 422）归一 UTC、(content_id,captured_at) 幂等覆盖/新点追加、metrics 七键稀疏纪律与 Agent 通道完全一致（复用 Q126 抽出的 _normalize_records/_resolve_contents(tenant_id=)/_persist_records）；received_by=客户 actor.id（区别 Agent 通道 key_id）；审计 `effect.customer_backfilled`（**tenant=客户租户**，区别 Agent 通道 _platform）；回执 {received,matched,orphan,upserted}，orphan 恒 0；⑤ 测试 tests/integration/test_effect_customer_backfill_api.py 8 例（无 Key happy 落 source/received_by/tenant、幂等覆盖与时序追加、他租户 content 422 不落库、未知/discarded 双 422 且零孤儿、一条坏整批拒回带 index、metrics/captured_at 五坏例 all-or-nothing、空 tenant/空 records 422、批内重复点 422）。**不落**：客户回填 UI、效果反哺 KUP/校准算法（口径【待补】禁臆造）、Agent 抓取（Q62 不抓取）。
