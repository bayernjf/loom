# Handoff Archive — 2026-09-22

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-22），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；更早归档见 `docs/handoff-archive-2026-09-{18,19,20,21}.md`。

## 最近进度归档（2026-09-22 滚出·Q149）

- **Q149 阈值确认 + 录入模板（零迁移/零代码；纯文档 89b0666 + docs 提交，均已 push（origin/dev）；后端 711 测试不变）**：负责人 2026-09-21 逐项拍板：阈值沿用占位值（72h/24h/48h、0.85、0.9）、G2 以 12 字段为基线、付费档额度划掉；docs/19 模板供业务方回填主数据。**#6 仅余 #2 主数据**。

## 2026-09-22 续写：Q150–Q152 进度条目滚出（原文）

- **Q150–Q152 A2A 封臣 + SLA sweep fence + 导出多 consumer 三片收口（2026-09-21；Q150 零迁移/零新 env、Q151 一迁移、Q152 零迁移；Q150 feat 0ba84d3〔含单测 12+集成 10〕/docs 6404b99〔并行会话合入〕、Q151 feat b06f902/test 3f36b9b、Q152 feat a41a174/test 977e193，**均已 push（origin/dev）**；Alembic 头 0037→**0038_sweep_tick_claims**/业务物理表 59→**60**，后端 711→**747** 测试〔Q150 +22＝单测 12·集成 10、Q151 +8、Q152 +6〕/ eval 101·golden 21、ruff 净，前端零变化〔三片均不动前端，tsc+八 checker、token 86〕）**：负责人 2026-09-21「好的，开搞」授权 A1+A2+A3 纯技术收口三片，接缝按推荐甲自行拍板（同 Q118–Q143 先例）。① **Q150 loom 作为 Zeus 联邦第二封臣·A2A 第一阶段 plan 模式（02 C1.94，设计草案 docs/design-a2a-vassal.md 状态翻 ✅）**：新横切包 `app/core/a2a/`（card/skills/rpc/router + __init__）——三公开只读发现端点 `GET /api/a2a/agent-card`、`/.well-known/agent-card.json`、`/.well-known/agent.json`（Agent Card + x-zeus-fealty swornTo=zeus/domain=content-production/dataRealms=[enterprise]/dataPolicy=read-task-scope/ack 10s，Cache-Control 300s，url 取 settings.public_base_url）；`POST /api/a2a/tasks` 复用 Q88 Agent Key Bearer（为 Zeus 签发专用 Key，无/错/吊销 401），JSON-RPC 2.0 tasks/send 同步、sendSubscribe SSE、get、cancel，进程内存任务存储 TTL 30min/上限 500（reset_for_tests 测试缝），状态机 submitted→working→completed/input-required/failed/canceled，错误码 -32700/-32600/-32601/-32602/-32603/-32001/-32002；三 skills（generate-content/compliance-check/effect-backfill）全 plan 模式纯函数执行，**不触链、不花 token（cost.llmTokens=0）、不越 Gate**；每入站 Key 调用落审计 action=`a2a.task`/entity_type=`a2a_task`/tenant=`_platform`（input-required 也留痕，集成测试首版按"不审计"断言失败 1 例后对齐代码口径）。单测 test_a2a_vassal.py 12 例 + 集成 test_a2a_vassal_api.py 10 例（本会话所写 259 行，并行会话在全量测试期间合入 feat+test 单笔 0ba84d3，违反 feat/test 分笔惯例但既成事实不改写历史；C1.94 首版漏登 10 集成测试，本次文档合龙已补登）。边界：任务进程内存跨实例不保证、真 LLM/链路执行/持久化/Zeus↔loom 真机联调随第二阶段（Zeus 侧 C2 待定）。② **Q151 SLA sweep 写路径 PG 行级 fence（02 C1.95，Q143 restock fence 同型收口）**：销 C1.83/C1.87 挂账"SLA sweep 行级 fence"——Q139 checkpoint 只管作业开始前，本片挡作业**执行期间**锁易主的迟到提交。新表 `sweep_tick_claims`（迁移 0038，down=0037，pg16 容器 55432 up→down-1→up 往返实测；lock_name String64 PK/fence Integer/claimed_by/claimed_at/updated_at，每循环锁一行 upsert，区别于 restock_claims per-request）+ 新模块 `app/core/sla/fencing.py`（claim_tick tick 前独立短事务认领返回 TICK_ACQUIRED/HELD/LOST 并先提交；fence_current 作业同事务 `UPDATE...WHERE lock_name AND fence=:token` rowcount=1 放行；fence=None 全程 no-op 不写表）；runner.run_jobs 加异步 commit_guard（门 False 回滚该作业并抛 LockLost 中止本轮）；scheduler._tick 与 POST /api/admin/sla/run 均 lease 内先认领（定时 lost 静默 warning 返回、手工 lost 409）再透传门；新模型 sla/models.py SweepTickClaim 经 fencing import 链注册；测试新 test_sweep_fencing.py 8 例（原语 4/runner 门 2/scheduler 接线 2），适配 test_distributed_locks 与 test_lease_cooperative_abort 共 3 处。sqlite 单连接替身边界同 Q143（顺序预置更大 fence 复现），真 PG+真 Redis 多副本验证仍 V2。③ **Q152 导出 worker 单进程多 consumer 并发度/批量配置（02 C1.96）**：销 Q137/Q142 挂账"多 ExportWorker 并发度配置"——config 加 `LOOM_EXPORT_WORKER_CONCURRENCY` 默认 1 与 `LOOM_EXPORT_STREAM_COUNT` 默认 20（运维参数非 Q9 旋钮、不入 .env.example）；worker.py 加纯工厂 build_export_workers（N 个同消费组 ExportWorker，N>1 姊妹 consumer 共享随机 hex8 前缀带 1..N 序号 export-{hex8}-{i}、count/block/min_idle/max_deliveries 全透传，concurrency/count<1 启动 ValueError fail-loud，concurrency=1 沿用构造器默认命名保 V1 行为）；main lifespan 单例 _export_worker 改 _export_workers 列表逐个 start、shutdown reversed() 反序 stop；消费组 XREADGROUP `>` 原生分片、PEL 接管按 consumer 独立；test_export_worker 7→13 例（+6，含两 consumer 对 6 任务各 3 确定性分片）；零迁移前端零改动。真 Redis 多 consumer/多副本验证仍 V2（FakeStreams 内存替身）。

## 2026-09-22 续写：Q153–Q154 进度条目滚出（原文）

- **Q153–Q154 DBA 第二轮全量列复核 + 真 Redis/真 PG 多副本锁易主验证（2026-09-21；Q153 纯文档/实测零迁移零测试新增、Q154 新增 test_real_infra.py 零生产代码零迁移；Alembic 头仍 0038/业务物理表 60，后端默认 747 passed＋8 real-infra skip〔带真容器 env 755 全过〕/ eval 101·golden 21、ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人「一口气推进能推进的」授权连续落地 A 类两片。① **Q153 docs/10 第二轮 DBA 全量列复核（02 C1.97）**：把首轮（head 0036/58 表）后新增的 0037 restock_claims、0038 sweep_tick_claims 两 fence 表纳入——一次性 pgvector/pgvector:pg16（55432 避 atlas-pg）`alembic upgrade head` 到 0038 后导出 information_schema/pg_indexes/pg_constraint，并 `import app.main` 注册全量 ORM 逐表逐列比对，实测 **db=60/orm=60、637 列（首轮 627＋两表各 5 列闭合）、212 索引、PK60/FK32/UQ20（FK+UQ=52 持平）、列漂移 0**，两新表五列/pkey 与 §2.8 逐字一致；docs/10 五处回填。② **Q154 真 Redis/真 PG 多副本锁易主→中止/拒写集成验证（02 C1.98）**：销 Q143/Q151/Q152 共同挂账的 FakeRedis/FakeStreams/FakeBroadcast 内存替身信任缺口——新建 `backend/tests/integration/test_real_infra.py` **8 例、函数级 skipif、无 env 全 skip 不进 CI**（`LOOM_TEST_REAL_PG_DSN` 门 4 例、`LOOM_TEST_REAL_REDIS_URL` 门 4 例，key/流名带 uuid 收尾删除可反复跑）；另起真容器 pg16（55433）+ redis:7（6380），带 env **8/8 全过（52.6s）**：真 PG 两独立连接池跨连接认领/升级与条件 UPDATE 拒旧 fence（restock/sweep 参数化）、真并发行锁串行化；真 Redis leader_lease SET NX 互斥/INCR 严格 +1 单调/短 TTL 易主看门狗置 lost＋raise LockLost＋安全释放不误删新主锁、Streams 两 consumer `XREADGROUP '>'` 对 6 条各分 3 条不重叠/XACK/XCLAIM 接管/move_to_dead 死信、pub/sub 跨连接 PUBLISH '*' 触发 reload。**真容器首跑即抓到 sqlite/Fake 替身盲区**：restock.request_id 为 varchar(36)，初版测试 key 带前缀达 49 字符撞真 PG `StringDataRightTruncation`（sqlite 不强制 varchar 长度故替身从不报错），统一改 32 字符 `uuid.uuid4().hex`（同时满足 lock_name varchar64）后全过；唯一失败是测试数据问题、**零生产代码改动**。复跑见 docs/17 §7、台账见 docs/16。**仍挂 V2**：真多副本多 worker 进程持续负载端到端部署演练（本片在单进程内用两独立连接复现全部原语语义）、restock 消费组水平并行（花钱隔离需重裁 Q87/Q89）。

## 2026-09-22 续写：Q155–Q157 进度条目滚出（原文）

- **Q155–Q157 V2 挂账销项三片（Q155/Q156/Q157 均零迁移；feat ba3c1df/test d56e432、feat d565dec/test abb4c45、fix c2f1b38/chore 0390bd5 六笔代码原子提交＋docs 同步一笔，**均已 push（origin/dev，头 883e4d7）**；Alembic 头仍 0038/物理表 60，后端 747→761 测试〔Q155 +3、Q156 +7、Q157 streams 8→12 净 +4〕/ eval 101·golden 21 全绿，ruff 净，前端零改动〔tsc+八 checker、token 86〕；Q157 多副本演练 16/16）**：负责人 2026-09-21「3件一起推」点工三件纯技术收口（无外部依赖、有原文挂账），接缝按推荐甲落地并在 02 标待追认（同 Q118–Q154 先例）。① **Q155 台内白名单卡片 6 层原料包全量 JSON（02 C1.99，feat ba3c1df/test d56e432，零迁移）**：销 docs/08 beta checklist「6 层原料包全量 JSON」V2 挂账——**台内 FCW 卡片口径**（产品+平台+策略+结构+表达+合规六层，docs/09:87「每条白名单=完整 6 层提示词原料包」、01 line 14「final_id=6 层快照相加」），与中台 /api/exports 的 final_id-only 面（Q100/Q132/Q142 三处定论）严格分界；新建 final/final_whitelist/material.py `build_material_pack`（schema `loom.fcw.material-pack.v1`，反解析 PwsSnapshot/PcpWeightTable+PublishSlot/三包 Package/CcrReport+LawReview，引用行物理缺失回 `{_ref,available:false}`+warning 不 500），只读端点 `GET /api/fcw/{final_id}/material.json`（JSON 下载、无 RBAC 闸/不触发 Guard/不写审计/未知 404，同 GET fcw 读纪律）；前端 D3.5 白名单组装引擎菜单整片未建，同 Q100 先例前端不做；+3（test_fcw_material_api）。② **Q156 批量回填服务端化（02 C1.100，feat d565dec/test abb4c45，零迁移）**：把 Q136 纯前端批量岛升级为服务端能力——新建 effects/csv_io.py 纯函数 `parse_backfill_csv`（标准库 csv、固定 9 列表头按名定位、去 BOM、跳空行、captured_at 强制 tz-aware〔naive/纯日期逐行报错守 Q128 铁律〕、计数 `^\d+$`、read_rate 0..1、空指标缺席不落 0、全坏行聚合一次回 422），无 Agent Key 客户通道新增 `POST /api/effects/backfill/upload`（**零新依赖**：JSON body 携 CSV 文本而非 multipart，整份挂单一 content_id，复用 Q128 ingest 故绝不产生孤儿，回执加 filename/逐行 rows），行数硬上限 env `LOOM_BACKFILL_UPLOAD_MAX_ROWS` 默认 10000（运维旋钮按 Q135 先例不入 C2/.env.example）；**接缝待追认**：服务端强制时区与 Q136 前端浏览器本地时区转换的两输入面差异已在 02/05/11/16 标注；Excel(openpyxl)/异步导入任务/前端岛切新端点仍挂 V2，本批只销后端能力；+7（test_effect_backfill_upload_api）。③ **Q157 真多副本多 worker 持续负载端到端部署演练（02 C1.101，fix c2f1b38/chore 0390bd5，零迁移）**：Q154 test_real_infra 是原语级（默认 skip），本片做部署级演练——alembic/env.py 加 PG 咨询锁（0x4C4F4F4D「LOOM」独立 NullPool 长连接 pg_advisory_lock，仅 postgresql，串行化多副本并发迁移）；新建 infra/docker-compose.ha.yml（backend replicas:2 开分布式锁/restock/export worker/配置广播、EXPORT_WORKER_CONCURRENCY=1 两副本各一 consumer 同组、LLM_BASE_URL 置空 synthetic 不花真钱）+ infra/ha-rehearsal.sh 四阶段断言；新建 uvicorn-log.json（root INFO→stdout）经 entrypoint `--log-config` 接线修容器内 app.* 日志不可见，export worker 日志带 consumer 名。**三轮迭代抓到一真实缺陷**：空轮询 `XREADGROUP BLOCK 5000` 到期 redis-py 8.1.0 抛 redis.exceptions.TimeoutError（RedisError 子类但**非**内置 TimeoutError 子类），read_new 原只 except RedisError→StreamBackendError，worker 每 block 周期误入退避、从不在服务端阻塞，致单 consumer 通吃且削弱 reclaim；fix c2f1b38 在 RedisError 前 except 两类 TimeoutError（block 归一返回 []、nonblock 仍 fail-closed，真连接故障 ConnectionError 不被吞），streams 测试 8→12。最终 **16/16 全绿**：迁移 38/0 串行无 DDL 竞争；24 export jobs 全 completed/UNIQUE 24、两副本 consumer 分片 12/12、空轮询 tick failed=0；SLA 并发 2×200+6×409、restock 6×200+2×409 均 0×5xx（单 leader 互斥、restock 无 requested 空转验不双花）；PUT content.ai_quality_threshold 0.82 后对端命中 `config cache reloaded key ... from invalidation broadcast` 并自动还原（0.85/version 3）；演练栈已 down -v 拆除。**仍挂 V2**：持续高压压测/异机备份恢复演练、restock 消费组水平并行（Q138 花钱隔离需重裁 Q87/Q89）、Q156 的 Excel/异步导入/前端岛切换、Q155 台内卡片 UI（随 D3.5 菜单）。
