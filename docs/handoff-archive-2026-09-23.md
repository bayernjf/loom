# Handoff Archive — 2026-09-23

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-23），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；更早归档见 `docs/handoff-archive-2026-09-{18,19,20,21,22}.md`。

## 最近进度归档（2026-09-23 滚出·Q158）

- **Q158 异机备份恢复端到端演练（零迁移/零生产代码；纯 infra 演练脚本一笔（5bd04e5，已 push origin/dev）；Alembic 头仍 0038/物理表 60，后端常规测试数不变＝761＋8 real-infra skip/eval 101·golden 21，ruff 净，前端零变化〔tsc+八 checker、token 86〕；bash 层 12/12 PASS）**：负责人 2026-09-21「一口气搞了」点工，销 Q146 挂账「异机备份/恢复演练」的**恢复腿**（Q146 只落每日 pg_dump 备份，恢复从未在全新异机端到端演练），02 C1.102。新增可复跑脚本 `infra/restore-rehearsal.sh`（up/down，KEEP=1 保留排查，风格同 ha-rehearsal.sh，跑完容器 --rm 自删）：起两个相互独立的一次性 pgvector/pgvector:pg16 容器（源 loom-restore-src 宿主 55440、异机 loom-restore-dst 宿主 55441，不触 atlas-pg 5432），源库经宿主 backend/.venv 设 LOOM_DATABASE_DSN 跑 `alembic upgrade head` 到 0038（恰 38 条），再造 tenants×2/export_jobs×3/effect_records×3（覆盖 JSONB detail/metrics、timestamptz captured_at、可空 metrics、completed/failed 任务）叠加迁移种子共 19 张非空表；用与 `infra/backup/backup.sh` 完全相同的 `pg_dump --no-password --format=custom --compress=9` 备份（custom 二进制、扩展名 .sql.gz 但恢复必须 pg_restore 不能 psql<），dump 离源经宿主再进异机模拟跨机；异机 drop/create 空 loom 库后 `pg_restore --no-owner`。**实测全绿**：源迁移 38 条 rc=0、dump 155110 字节/TOC 61 TABLE DATA、异机 pg_restore rc=0 且 error 行=0；两库 alembic_version 均 0038_sweep_tick_claims、业务表 60/含版本表共 61 集合 A==B、全表逐表行数零差异（非空 19 表）、结构指纹 indexes=213/PK=61/FK=32/UQ=20/vector 列=2/pgvector 扩展一致（含版本表比 Q153 业务口径 212/PK60 各 +1 自洽）、tenants/export_jobs/effect_records 演练行整行（JSONB/timestamptz/可空）逐字段一致、config_items 61/61 且 QC 阈值 0.85 一致、`import app.main` ORM 连恢复库只读成功、恢复库再 upgrade head 幂等 no-op（0 条 Running upgrade）。**仍挂 V2**：RPO≤24h/RTO≤4h 量化达标认证（docs/17 §5 仍【建议】，本片不升级状态）、WAL 增量/对象存储跨区冗余、指标级监控、持续高压压测（Q157 留）；脚本本地手动不进 CI（同 Q147/Q154/Q157）。
