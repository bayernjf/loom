# Handoff Archive — 2026-09-23

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-23），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；更早归档见 `docs/handoff-archive-2026-09-{18,19,20,21,22}.md`。

## 最近进度归档（2026-09-23 滚出·Q158）

- **Q158 异机备份恢复端到端演练（零迁移/零生产代码；纯 infra 演练脚本一笔（5bd04e5，已 push origin/dev）；Alembic 头仍 0038/物理表 60，后端常规测试数不变＝761＋8 real-infra skip/eval 101·golden 21，ruff 净，前端零变化〔tsc+八 checker、token 86〕；bash 层 12/12 PASS）**：负责人 2026-09-21「一口气搞了」点工，销 Q146 挂账「异机备份/恢复演练」的**恢复腿**（Q146 只落每日 pg_dump 备份，恢复从未在全新异机端到端演练），02 C1.102。新增可复跑脚本 `infra/restore-rehearsal.sh`（up/down，KEEP=1 保留排查，风格同 ha-rehearsal.sh，跑完容器 --rm 自删）：起两个相互独立的一次性 pgvector/pgvector:pg16 容器（源 loom-restore-src 宿主 55440、异机 loom-restore-dst 宿主 55441，不触 atlas-pg 5432），源库经宿主 backend/.venv 设 LOOM_DATABASE_DSN 跑 `alembic upgrade head` 到 0038（恰 38 条），再造 tenants×2/export_jobs×3/effect_records×3（覆盖 JSONB detail/metrics、timestamptz captured_at、可空 metrics、completed/failed 任务）叠加迁移种子共 19 张非空表；用与 `infra/backup/backup.sh` 完全相同的 `pg_dump --no-password --format=custom --compress=9` 备份（custom 二进制、扩展名 .sql.gz 但恢复必须 pg_restore 不能 psql<），dump 离源经宿主再进异机模拟跨机；异机 drop/create 空 loom 库后 `pg_restore --no-owner`。**实测全绿**：源迁移 38 条 rc=0、dump 155110 字节/TOC 61 TABLE DATA、异机 pg_restore rc=0 且 error 行=0；两库 alembic_version 均 0038_sweep_tick_claims、业务表 60/含版本表共 61 集合 A==B、全表逐表行数零差异（非空 19 表）、结构指纹 indexes=213/PK=61/FK=32/UQ=20/vector 列=2/pgvector 扩展一致（含版本表比 Q153 业务口径 212/PK60 各 +1 自洽）、tenants/export_jobs/effect_records 演练行整行（JSONB/timestamptz/可空）逐字段一致、config_items 61/61 且 QC 阈值 0.85 一致、`import app.main` ORM 连恢复库只读成功、恢复库再 upgrade head 幂等 no-op（0 条 Running upgrade）。**仍挂 V2**：RPO≤24h/RTO≤4h 量化达标认证（docs/17 §5 仍【建议】，本片不升级状态）、WAL 增量/对象存储跨区冗余、指标级监控、持续高压压测（Q157 留）；脚本本地手动不进 CI（同 Q147/Q154/Q157）。

## 最近进度归档（2026-09-23 续写·Q159–Q161）

- **Q159–Q161 客户批量回填收口三片 + Q156 接缝追认 + 项目级 MVP 评审（2026-09-22；Q159 纯前端零迁移、Q160 零迁移、Q161 迁移 0039 import_jobs 物理表 60→61；代码六笔 8440cdc/ab2c531/8d95309/55d5f47/0abcdbd/6bbf5d3 先于文档落地、本批补登记；后端 761→**786** 测试〔Q160 +8、Q161 API 8+worker 9＝+17、Q159 纯前端 0〕＋8 real-infra skip/eval 101/ruff 净/前端 tsc+八 checker；02 C1.103–105）**：Q156 原挂 V2 的三件全部销账——① **Q159 批量回填岛切服务端 CSV**（8440cdc：前端 actions.ts `uploadBackfillCsv` 提交 CSV 原文到 Q156 `/upload`、不再浏览器解析 records[]、422 逐行错误结构化渲染〔line＝含表头物理行号〕、check-content 扩守卫，解除前端 500 行软上限）；② **Q160 Excel .xlsx 上传**（ab2c531/8d95309/55d5f47：新端点 `POST /api/effects/backfill/upload-excel` 与 CSV 同契约同回执、新模块 effects/excel_io.py 用 openpyxl `read_only` 读活动 sheet、单元格归一化后复用 csv_io 同一校验、旧 .xls/损坏文件加载期 422、依赖 `openpyxl>=3.1.5`、行数上限复用 env 默认 1 万、审计复用 effect.customer_backfilled、+8 集成、前端岛增 .xlsx 入口）；③ **Q161 异步导入任务**（0abcdbd/6bbf5d3：新包 `app/core/imports/`、`POST/GET /api/effects/backfill/jobs`＋`GET /jobs/{job_id}`、迁移 **0039 import_jobs**〔payload Text 落 CSV 原文/xlsx base64 可重放、queued/running/completed/failed〕、ImportWorker 经 Redis Streams 消费组〔确定性校验失败 failed+ACK 不重试不进死信、基础设施异常留 PEL/XCLAIM/死信〕、门控 `LOOM_IMPORT_WORKER_ENABLED` 默认关〔关＝请求内同步跑到终态保 V1 行为、开＝异步、入流失败 fail-closed 503〕、另三旋钮 BLOCK_SECONDS=5.0/CONCURRENCY=1/STREAM_COUNT=20 不入 .env.example、审计 import.job_created/completed/failed、+17 集成＝API 8+worker 9，前端零改动）。**Q156 三接缝（强制 tz-aware/行数硬上限/JSON body 零 multipart）连同 Q159–161 同类工程默认（xlsx base64、payload 落库可重放、worker 门控默认关）已经负责人 2026-09-22「A和B吧，尽量搞一下」追认销账**（02 C1.100 追认注记）。另完成**项目级 MVP 评审**（[docs/20_MVP评审_2026-09-22.md](docs/20_MVP评审_2026-09-22.md)，45d7517）：✅ 受控 private beta 有条件 GO——V1 内核（段1→6→10→11 至 final_id）达标且段12/13/A2A 超额前置，上线 Checklist 5/6，唯一阻塞＝业务方按 docs/19 回填首批主数据。**docs/20 §6 回读更新（2026-09-22 Q161 后）已完成**：确认文档滞后已全部闭合（handoff/docs02/docs16/CHANGELOG 均同步、代码已 push、Q156 三接缝已追认），新增 13 段链逐段完整度表 + 可一口气推进任务清单（A1 合规风控页扩展/A2 驾驶舱日期筛选器/A3 Onboarding引导页/A4 FCW异步worker）+ 30 项不可推进清单 + 6 项边界模糊项，MVP 判定与唯一阻塞不变。

## 最近进度归档（2026-09-23 续写·Q162–Q165）

- **Q162–Q165 MVP 评审「一口气推掉」四片（2026-09-22；A1/A3 零迁移、A2 零迁移零后端、A4 零迁移；代码八笔 cf47e9e/1d77471/24d9b0a/4d310ea/eefbb2d/9ac6245/4c4902e/7d8f4dc，**均已 push（origin/dev，负责人手动）**；后端 786→**803** 测试〔A1+5 合规集成、A3+1 租户集成、A4+11 FCW worker 集成＝+17，A2 纯前端 0〕＋8 real-infra skip/eval 101/ruff 净/前端 tsc+八 checker/token 86；02 C1.106–109，**十接缝已经负责人 2026-09-22 追认**）**：负责人 2026-09-22「那你一口气推掉」授权销 docs/20 §6 列出的 4 个可一口气推进任务。① **Q162 A1 客户合规风控页扩展三件套**（C1.106，后端 cf47e9e＋测试 1d77471＋前端 eefbb2d）：新增 4 个无闸客户侧只读 GET——`GET /api/compliance/ccr-history`（全租户 CCR 历史分页）、`GET /api/compliance/ccr/{ccr_id}`（详情租户收窄）、`GET /api/compliance/law-reviews`（服务端派生 sla_remaining_seconds/sla_state，复用 48h）、`GET /api/compliance/wordlist`（只读 active 词条可选 level/layer）；前端 compliance 页扩 CCR 历史/法审 SLA/词库三子岛，messages +35 键，check-content 扩守卫；+5 集成。② **Q163 A3 客户 Onboarding 前端引导页**（C1.107，后端 24d9b0a＋测试 4d310ea＋前端 eefbb2d）：CustomerTenantView 追加 onboarding 派生字段（复用 Q95 onboarding_progress，甲案不新建端点）；工作台内嵌四步引导岛（注册→产品建模→AI拆解→内容上线，intakes=0 完整卡/intakes>0 紧凑条，客户 nav 不增项），messages +10 键，check-content 扩守卫；+1 集成。③ **Q164 A2 管理端驾驶舱日期筛选器 UI**（C1.108，前端 9ac6245）：新建共享 DateRangeFilter 组件（纯服务端 form GET，date_from/date_to 与后端 Q92 契约对齐，默认近 30 天），token-cost/review-workload 两页读 searchParams 传参，messages +7 键，check-admin 扩守卫；纯前端零后端零迁移。④ **Q165 A4 FCW 组装异步 worker**（C1.109，后端 4c4902e＋测试 7d8f4dc）：新模块 app/final/fcw_worker/（FcwWorker 镜像 ExportWorker/ImportWorker，流 loom:fcw-assembly/组 fcw-assembly-group）；create_task 重构拆为 create_task_record＋process_task；门控 LOOM_FCW_WORKER_ENABLED 默认关（关=V1 同步不变、开=queued+XADD 异步），另三旋钮 BLOCK_SECONDS=5.0/CONCURRENCY=1/STREAM_COUNT=20 不入 .env.example；入流失败 delete task+503 fail-closed 无残留；PT-FCW-ASM 七 Guard 原子性不弱化（assemble_one 无独立 commit，worker 路径 process_task 末尾统一 commit，测试实证 fcw.assembly_blocked 审计完整）；+11 集成（FakeStreams 内存替身）；零迁移（fcw_assembly_tasks.status String(16) 已够），前端零改动。**四片共 10 项接缝已经负责人 2026-09-22「好的，开搞，C1、C2、C3也搞」追认销账**（02 C1.106–109；A1 三接缝：CCR 分页甲案/法审倒计时服务端派生/客户词库只读；A3 两接缝：CustomerTenantView 追加 onboarding/内容上线步近似；A4 五接缝：actor 重建/入流失败策略/幽灵 task ACK/concurrency 预留/失败无 error 列）。
