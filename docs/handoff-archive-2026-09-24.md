# Handoff Archive — 2026-09-24

> 从 `handoff.md`「最近进度」区滚出的已完成条目详细过程归档（2026-09-24），按归档惯例（见 handoff.md `## Conventions`）执行：主文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度，超出的详细记录全文迁此，**逐条原文搬移、一字未改**。权威逐条台账（更细粒度）：`docs/02` 决策日志（Q73 起 C1.17）、`docs/08` 各 Q 完成情况、`docs/16` 测试台账、`CHANGELOG.md [Unreleased]`；更早归档见 `docs/handoff-archive-2026-09-{18,19,20,21,22,23}.md`。

## 最近进度归档（2026-09-24 滚出·Q166–Q168）

- **Q166–Q168 客户 analytics 页 + Agent Key 治理页 + 导出 job 管理页，及文档合龙 D1–D4（2026-09-22；三片均零迁移、Alembic 头仍 0039/业务物理表 61；代码与文档本批原子提交、**均已 push（origin/dev；2026-09-22 复核 origin/dev..dev=0）**；后端 803→**808** 测试〔仅 Q166 +5，Q167/Q168 纯前端 0〕＋8 real-infra skip/eval 101/ruff 净/前端 tsc+八 checker+next build 全绿/token 86；02 C1.110–112；Q162–165 十接缝已经负责人当日追认）**：负责人 2026-09-22「好的，开搞，C1、C2、C3也搞」一口气授权三片功能＋四片文档合龙。① **Q166（C1.110）客户效果 analytics 数据分析页**：后端 effects 域无闸只读 `GET /api/effects/analytics`（tenant_id 必填 query、date_from/date_to 可选 YYYY-MM-DD、from>to 422、date_to 含当天＝排他上界次日 00:00 UTC），customer_analytics 只统计 status=matched 且本租户（orphan 运营面不向客户暴露、他租户不串、未知租户 200 空、不写审计），六计数 plays/likes/comments/shares/inquiries/conversions 稀疏累加（缺席不补 0）、read_rate 比率不可求和仅对含键记录平均（round 4，分母 read_rate_samples）、by_content 按 plays 降序 top 50、扫描上限 ANALYTICS_SCAN_LIMIT=10000 置 truncated；前端 `(shell)/analytics` 由 V2 占位转真 RSC（force-dynamic、searchParams Promise 需 await；nav.ts phase v2→v1，客户 nav 仍恰 8 项），内联服务端日期表单＋KPI 卡＋六计数卡＋by_content 表，新增 analytics.module.css（全语义 token），check-content 加 Q166 守卫；test_effect_analytics_api.py 5 例；**翻转 Q97/Q114「analytics 随 V2」裁决、提前入 V1 private beta**。② **Q167（C1.111）入站 Agent Key 治理前端管理页**：新建 admin/agent-keys 四件（page.tsx 只读 RSC 列表 name/key_prefix/status/时间、issue-island.tsx 签发 secret 仅成功时一次性驻留展示＋复制、revoke-island.tsx window.confirm 终态吊销、actions.ts 两 Server Action 本地 guardPlatform 验 platform_admin），纯消费 Q88 三端点（签发 201 secret 仅一次/列表免闸不返 secret/吊销，service 层硬闸 platform_admin），admin-sidebar 加第 9 项，messages admin.agentKeys(25)，check-admin 计数 8→9 加 Q167 守卫。③ **Q168（C1.112）中台导出 job 管理前端页**：新建 admin/exports 四件（page.tsx 只读 RSC 顶部纯 GET 租户过滤＋jobs 表、create-island.tsx 选 csv|json＋选填发布位创建、download-island.tsx 仅 completed 可点、经 Server Action 取文本后 Blob＋URL.createObjectURL＋a.download 落盘、queued/running/failed 禁用 409 notReady、actions.ts），纯消费 Q132/Q137，lib/api 新增 **requestText 文本下载通道**（解析 Content-Disposition filename，不经裸 URL/NEXT_PUBLIC），admin-sidebar 加第 10 项，messages admin.exports(25)，check-admin 9→10 加 Q168 守卫。④ **文档合龙 D1–D4**：docs/10 补登迁移 0039 import_jobs（业务物理表 61，经 pg16 容器 up/downgrade-1/up 往返实测 17 列/3 索引）、docs/11 补 Q160 upload-excel 与 Q161 异步导入 jobs 三端点契约并订正 Q156 尾句、docs/17 刷迁移头 0039/61 表并补 Q159–Q165 运维 env（LOOM_IMPORT_WORKER_*/LOOM_FCW_WORKER_* 门控默认关、不入 .env.example）、docs/02 补 Q166–Q168 三 ADR（C1.110–112）并把 Q162–Q165 十接缝由「待追认」统一改为**已追认（2026-09-22）**、docs/20 基线刷 808 并补本批落地注记；收尾 docs/16 台账刷 808、docs/17 CI 段测试数与 sidebar 10 项订正、CHANGELOG [Unreleased] 补 Q166–168。


## 最近进度归档（2026-09-24 续写滚出·Q169–Q172）

- **Q169–Q172 全链 golden-path e2e / 运营 SOP / 第三轮 DBA 复核 / 真 agnes 全链演练四片（2026-09-22；四片零迁移、Alembic 头仍 0039/业务物理表 61；B1 代码已先落 13a73f2，本批 feat/test/docs 原子提交补齐，**均已 push（origin/dev）**；后端 808→**809** 常规测试＋**9 个默认 skip**〔8 real-infra＋1 real-LLM e2e〕，eval 101/ruff 净/前端零改动；02 C1.113–116）**：负责人「B1、B2、B3 一口气搞了，B4 也做」授权。① **Q169（C1.113）全链 golden-path e2e**：新增 `backend/tests/e2e/test_fullchain_golden_path.py`——一条产品经真实 API 走完整段 1→13（录入→建模→字段池→原子→PWC→发布位/PCP/三包→PWS 冻结→CCR→FCW 组装发 `final_id`→段12 生成/客户 approve〔ready_for_publish〕→段13 effect-callback matched），数据全为显式标注的 **synthetic 测试夹具**（1 slot+PCP+3 包，非业务主数据）；支持 sqlite 内存/真 PG（`LOOM_E2E_PG_DSN`）/真 LLM（`LOOM_E2E_REAL_LLM=1`）三模式；配套 `infra/fullchain-rehearsal.sh` 起一次性 pg16 容器（默认 55442，不触 atlas-pg，本地手动不进 CI）。② **Q170（C1.114）运营全链 SOP**：新增 [docs/21_运营SOP_全链操作手册.md](docs/21_运营SOP_全链操作手册.md)——逐段谁点哪个端点/页、Gate 裁决、冻结/发证、角色分工与常见 4xx/5xx 含义、日常节奏（SLA sweep/三队列/审计）。③ **Q171（C1.115）第三轮 DBA 全量列复核**：把迁移 0039 import_jobs 纳入，pg16 容器 upgrade head 实测 information_schema 与全量 ORM metadata 比对：**61 表/654 列（637+17）/215 业务索引（212+3）/PK61·FK32·UQ20、列漂移 0**，结论落 docs/10 §4。④ **Q172（C1.116）真 agnes 全链演练**：经 B1 e2e 真 LLM 模式跑通真 PG＋真 agnes（agnes-2.5-flash）——CAT-RECOG 真候选、真文章生成、effect matched，两 e2e 用例 46.3s 通过，花费美分级在日预算内，Key 仅运行时注入/Fernet 入库、不入仓；演练抓到文章生成偶发超 60s httpx 硬超时，新增 env `LOOM_LLM_HTTP_TIMEOUT_SECONDS`（默认 60 保 V1 行为，演练传 180，不入 .env.example）。


## 最近进度归档（2026-09-24 续写滚出·Q173–Q175）

- **Q174 前端批量回填/Excel 导入岛切异步任务轮询 + Q175 RPO·RTO 量化达标认证（2026-09-23；两片零迁移、Alembic 头仍 0039/业务物理表 61；代码与文档本批原子提交、**均已 push（origin/dev）**；后端常规测试仍 **810**＋9 skip〔Q174 纯前端、Q175 infra 均 0 增量〕，eval 101/ruff 净/前端 tsc+八 checker+next build 全绿/token 86；02 C1.118–119）**：负责人「一口气搞了」授权 A1–A4。① **Q174（C1.118）**：销 docs/11 唯一 V2 尾项「前端岛切异步任务轮询」。lib/api 加 BackfillImportJobView 与 submitBackfillImportJob/getBackfillImportJob；content/actions 加 submit/poll 两 Server Action（re-export 视图类型）、删同步 batchBackfill* 两 action；批量岛提交 `/jobs` 后 useEffect 每 2s 轮询〔封顶 180s；404 置失败、瞬时错误重试〕，completed 显 matched＋router.refresh、failed 渲染 job.errors 逐行；messages 加 processing/pollTimeout；check-content 守卫锁定。门控关提交即终态、门控开 queued 轮询。② **Q175（C1.119）**：销 docs/20 §6「RPO/RTO 量化达标」。新增 `infra/rpo-rto-rehearsal.sh`〔故障注入＋计时：源库造数→pg_dump T_DUMP→备份后再写1行→docker rm -f T_FAIL→新机 pg_restore＋应用只读冒烟〕，实测 **12/12 PASS**：RPO 观测 1.919s〔每日备份频率保证 ≤24h，备份后新增行确认丢失、恢复 tenants=2〕、RTO 观测 70.111s〔≤4h；认证工程恢复动作、不含人员响应〕。docs/17 §5 升级＋新增 §7.4。③ A1 docs/20 定点刷新〔基线 810、管理端 10 页、§6.6 压测/RPO 两行注记、第三次更新注记〕、A4 任务板 #43 两腿销账。
- **Q173 持续高压负载端到端演练 + jobs 列表 offset 补全（2026-09-23；零迁移、Alembic 头仍 0039/业务物理表 61；代码三笔 b1f73f2/cb68059/fc3fdc2〔feat/test/infra〕先落、文档本批补登记，**均已 push（origin/dev）**；后端 809→**810** 常规测试＋**9 个默认 skip**，eval 101/ruff 净/前端零改动；02 C1.117）**：负责人「那你推进」点工，销 docs/20 §6「持续高压压测」V2 挂账的负载演练腿。新增可复跑脚本 `infra/load-rehearsal.sh`（up/run/down）：在 Q157 两副本 HA 栈上，两个驱动器副本各以 1 req/s 提交 export job（POST /api/exports/jobs）+ 随机读（jobs 列表/token-cost 驾驶舱），每 2 秒向两副本 fan-out 8 个 `/api/admin/{sla,restock}/run` 锁抢占；采样器每 30s 记录健康与队列。**演练首跑抓到真实产品缺陷**：`GET /api/exports/jobs` 有 limit（le=200）但**无 offset**（Q142 只给 fcw.csv/json 加分页），演练「翻到短页」循环反复取回同一首页、200 行 dict 无限累积，驱动容器涨到 6.2GB 被宿主 OOM 杀掉；补 offset 贯通 service/router + 分页测试后 5 分钟正式跑 **9/9 PASS**：562 job 提交==562 completed（不丢单）、drain 后 active=0、failed=0、读 562 零失败、全程零 5xx、`/run` 200=414 且 409=1842（持续锁抢占两态都在）、两副本 healthy/零重启/无 OOMKill/日志无未处理 Traceback。演练进程一律 `docker run --rm --entrypoint python` 一次性容器（docker exec 进 app 容器会共享其 cgroup、OOM 永久标记 OOMKilled，且 uvicorn PID1 不 reap 留僵尸），详见 docs/17 §7.3。仍 V2：RPO·RTO 量化达标、restock 消费组水平并行。
