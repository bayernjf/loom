# Handoff 进度归档 · 2026-09-25

> 本文件按 handoff「归档惯例」小节建立：只收**已从 handoff.md 滚出的进度条目原文**（逐条一字未改，可作历史检索）。
> 权威逐条台账仍是 docs/02（C1 日志）/ docs/08 / docs/16。同日多次续写本档，不覆盖、不删除。

## 滚出条目（2026-09-25：Q176/Q177 追认批 ＋ Q177 D3.5 运营只读首片）

- **Q176 KNN 索引丙案裁决 ＋ Q177 三接缝追认 ＋ 过期 push 标注订正（2026-09-24；纯文档收口、零代码零迁移；基线不变＝后端 817 常规＋9 默认 skip、eval 101、Alembic 头 0039/物理表 61、前端 tsc＋八 checker、token 86；02 C1.120–121 追认注记）**：负责人「那你一口气搞了」授权。① **Q176（C1.120）按工程推荐丙案（暂不做 / YAGNI）拍板销账**：不写迁移 0040、不建 HNSW/IVFFlat、不改 Q86 应用层内存余弦计算路径；甲（只建预备 HNSW，迁移 0040）/乙（下推 DB KNN、改 Q86、须 eval/golden 回归）两档归档备查，待真出现 DB 层 KNN 查询消费方或 D3.7 RAG 检索落地再重评，待办 #9 销账。② **Q177（C1.121）三接缝甲案追认销账**：入口落管理端运营台（/admin/fcw sidebar 第 11 项，读 operations|platform_admin）、新增跨租户只读列表口、六层内嵌面与 Q155 导出口共用 build_material_pack 但分两口（内嵌过读闸、JSON 无 attachment；导出带 attachment、无闸保 V1）。③ **订正 4 处过期「未 push」标注**：Q169–Q172、Q173、Q174/Q175、Q177 各笔经核实均已 push（dev 与 origin/dev 同步、origin/dev 头 1cce400）。D3.5 客户卡片视图等余项仍随菜单点工，本轮不做。
- **Q177 D3.5 白名单组装引擎运营只读首片（管理端白名单卡片台）＋ Q176 ATOM-AFFINITY KNN 索引丙案裁决不落地（2026-09-23 评估/2026-09-24 裁决；Q177 零迁移、Alembic 头仍 0039/业务物理表 61；代码与文档本批原子提交、**均已 push（origin/dev）**；后端常规 810→**817**＋9 skip〔Q177 +7 集成、Q176 零代码〕，eval 101/ruff 净/前端 tsc+八 checker+next build 全绿/token 86/客户 nav 仍 8；02 C1.120–121，Q177 三接缝甲案已经负责人 2026-09-24 追认）**：负责人「一口气搞了」授权档 A 两件，grounding 后一件落地、一件回退待裁。① **Q177（C1.121）**：销 docs/20 §6 边界项 #10「Q155 台内卡片 UI」的运营只读腿。后端 final_whitelist 加两口（读闸 operations|platform_admin，query actor_id 必填＋roles 可重复，缺 actor 422/越权 403）：`GET /api/admin/fcw` 跨租户分页列表（tenant_id 可选过滤、limit 1..200 默认 50、offset，envelope {count,total,limit,offset,has_more,items:fcw_view 元信息}，service.list_fcw_admin 按 created_at/final_id 排序）与 `GET /api/admin/fcw/{final_id}/material` 六层包**内嵌 JSON**（复用 Q155 build_material_pack、**无** Content-Disposition 下载头、未知 id 404、纯只读不触发 Guard 不写审计）；Q155 原导出口 material.json（attachment、无闸）保留不动；集成 test_fcw_admin_api.py 7 例（422/403/跨租户/过滤/分页/limit 越界/内嵌六层无下载头/404）。前端新建 admin/fcw/（page.tsx force-dynamic 纯只读 RSC 跨租户列表＋分页＋租户过滤、material-island.tsx client 岛点击才经 Server Action getFcwMaterialAction 拉六层 details/pre 展示＋warnings/guards、actions.ts 本地角色闸与 403/404/409/422 映射），lib/api 加 listAdminFcw/getAdminFcwMaterial，sidebar 加**第 11 项** /admin/fcw，messages admin.fcw*，check-admin 加 Q177 守卫段、sidebar 计数 10→11；枚举 platform/goal/publish_status 原样直出。三接缝甲案（入口落管理端运营台／新增跨租户只读列表口／内嵌面与导出口共用 build_material_pack 但分两口）**已经负责人 2026-09-24 追认（见 09-24 条）**；D3.5 其余（组装工作台/6 层可视化编辑/合理性校验/审核台/冻结管理/客户卡片视图/复制 ID）仍随菜单点工。② **Q176（C1.120，✅ 2026-09-24 按丙案裁决不落地，详见 09-24 条）**：docs/20 §6 C5「ATOM-AFFINITY KNN 索引」经 grounding 推翻「纯性能工程」定性——全仓 grep 对 `<=>`/cosine_distance/l2_distance/max_inner_product **零命中**，affinity/cluster_id 按 Q86 在应用层内存确定性重算（atom_expand.py 同维 basis＋批内一次 embed、max_affinity 本地余弦＋同维并查集、模型自报值剥离）；裸建 HNSW/IVFFlat 是**无查询消费方的死索引**，下推 DB KNN 则改 Q86 计算路径属设计变更。列三档待负责人拍板（甲只建预备 HNSW 迁移 0040／乙建索引＋下推改 Q86／丙暂不做，工程推荐丙），本轮零代码零迁移、不写 0040。【2026-09-24 负责人「那你一口气搞了」按丙案裁决销账，见上 09-24 条】

## 滚出条目（2026-09-25 第二次续写：Q188 批后按「最近 5 条」上限滚出六条）

> 滚出动作由 Q189 文档对账批执行。以下六条为 handoff.md 原行**逐字**搬入，一字未改；权威逐条台账仍是 docs/02 C1。滚出后主文件保留 Q188／Q184–Q187／Q183／Q180–Q182／Q179 共五条。

- **Q178 身份层 P0-① 甲案第一切片·内部运营人员令牌 PAT（2026-09-24；迁移 0040 staff_api_keys、业务物理表 61→62；本切片代码与文档原子提交、**已 push**（origin/dev，12a7885 等 4 笔）；后端常规 817→**842**＋9 skip〔+25 集成〕、eval 101/101、ruff 净、迁移经 pg16 容器宿主 55442 up/downgrade-1/up 实测；前端 tsc＋八 checker＋next build 全绿、管理 sidebar 11→**12 项**、token 仍 86/客户 nav 仍 8；02 C1.122）**：负责人经 interaction.ask 就 docs/20 §6.6「需重裁」第一项身份层拍板**甲案＝内部运营个人访问令牌（复用 Q88 底座）**，纯加法、门控 env 默认关、不破坏现有 actor 自报、可回滚；第一切片仅内部运营管理端，**客户侧真实认证与 actor↔tenant 绑定仍后置 V2**。① **后端新包 `app/core/staff_auth/`**（models/context/service/schemas/deps/router）：表 staff_api_keys（迁移 0040；staff_id 非唯一索引支持一人多 token 轮换、roles jsonb、key_hash SHA-256 unique、明文 `loom_staff_`+token_urlsafe(30) 仅签发回一次、与机器 `loom_` agent_api_keys 分表分前缀不可互换、无 tenant_id 审计恒 `_platform`），可签发角色限五内部角色（operations/platform_admin/product_reviewer/dictionary_admin/internal_compliance，不含 whitelist_owner）；端点 POST/GET `/api/admin/staff-keys`、POST `/api/admin/staff-keys/{key_id}/revoke`（platform_admin 红线）、GET `/api/auth/me`，agent-keys GET 补 `internal_gate`。② **认证与授权分离、端点签名零改动**：认证＝app 级全局依赖 `staff_auth_context`（门控 `LOOM_STAFF_AUTH_ENABLED` 默认关；开则仅验 `loom_staff_` Bearer、独立短事务验真并把人员 Actor 写入请求级 contextvar，无令牌/机器 `loom_`/客户口/公开口放行，坏或吊销 staff 令牌 401）；授权＝唯一收口 `rbac.require_any_role`（门控开内部口：无认证人 401 NotAuthenticated、角色不足 403 PermissionDenied、通过即**就地用令牌身份覆盖自报 actor**——拼 `?actor_id&roles=platform_admin` 提权失效、审计落真实 staff_id；门控关或纯客户 whitelist_owner 维持 V1 自报）。③ **前端**：lib/staff-auth.ts httpOnly cookie、lib/api 自动注入 Bearer 且管理口 401 跳 /admin/login；新建 /admin/login（令牌录入/登出）与 /admin/staff-keys（签发/列表/吊销）两页、sidebar 第 12 项（/admin/login 不进 sidebar）；check-admin 扩 staff-keys/login 专属守卫段＋后端 staff_auth 路径断言。④ **引导（鸡生蛋）**：门控仍关时由自报 platform_admin 签发首个令牌 → 前端 /admin/login 录入 → 置 LOOM_STAFF_AUTH_ENABLED=true 重启；回滚＝关 env。⑤ 测试 test_staff_auth_api.py 25 例（StaticPool 共享认证/业务两 session、override get_session 与 get_auth_session；先门控关 _bootstrap 签发再 monkeypatch 开闸，防 settings 单例跨测泄漏）。**工程接缝（旋钮按 Q135 先例不入 .env.example、认证授权分离零端点签名改动、客户侧后置 V2）已经 Q179 追认（02 C1.123，见下 Q179 条）**。文档合龙 02/05/07/09/10/11/16/17/20 ＋ handoff（含归档）＋ CHANGELOG ＋ README ＋ AGENTS。
- **Q176 丙案裁决 ＋ Q177 三接缝追认 ＋ 过期 push 标注订正（2026-09-24，纯文档零代码零迁移；02 C1.120–121）已滚出，原文见 [docs/handoff-archive-2026-09-25.md](docs/handoff-archive-2026-09-25.md)。
- **Q177 D3.5 白名单组装引擎运营只读首片（管理端 /admin/fcw 跨租户列表＋六层卡片；后端两口、sidebar 第 11 项、零迁移；02 C1.121，三接缝甲案已经负责人 2026-09-24 追认）已滚出，原文见同档。
- **Q169–Q172 全链 golden-path e2e / 运营 SOP（docs/21）/ 第三轮 DBA 复核（61 表 654 列）/ 真 agnes 全链演练四片（2026-09-22，四片零迁移、头 0039/物理表 61，后端 808→809＋9 skip，均已 push；02 C1.113–116）已滚出，原文见 [docs/handoff-archive-2026-09-24.md](docs/handoff-archive-2026-09-24.md)。
- **Q166–Q168 客户 analytics 页 / Agent Key 治理页 / 导出 job 管理页三片＋文档合龙 D1–D4（2026-09-22，零迁移；后端 803→808〔Q166 +5，Q167/Q168 纯前端 0〕，均已 push；02 C1.110–112）已滚出，原文见 [docs/handoff-archive-2026-09-24.md](docs/handoff-archive-2026-09-24.md)。
- **Q162–Q165 MVP 评审「一口气推掉」四片（2026-09-22，02 C1.106–109，A1 合规三件套/A2 驾驶舱日期筛选/A3 Onboarding 引导/A4 FCW 异步 worker，后端 786→803；十接缝已经负责人 2026-09-22 追认）已滚出，原文见 [docs/handoff-archive-2026-09-23.md](docs/handoff-archive-2026-09-23.md)。

- **Q179 第四轮 DBA 全量列复核（2026-09-24；零迁移、Alembic 头仍 0040/业务物理表 62；纯只读复核、**已 push**（origin/dev e04c379）；后端常规测试仍 **842**＋9 skip〔零 pytest 增量〕、eval 101/ruff 净；02 C1.123）**：负责人「一口气搞了」授权，销 docs/10 挂账「0040 纳入下一轮全量列复核」。一次性 pgvector/pg16 容器 loom-dba4（宿主 55448 避 atlas-pg）`alembic upgrade head` 到 0040，导出 information_schema.columns / pg_indexes / pg_constraint 并与 ORM `Base.metadata`（import app.main，Base 在 app/core/db.py）逐表逐列比对：①业务表 db=62 / orm=62，无单边表；②总列数 **666**（第三轮 654＋staff_api_keys 12 列对账闭合）、业务索引 **219**（215＋staff_api_keys 四索引：staff_api_keys_pkey、ix_staff_api_keys_staff_id、ix_staff_api_keys_key_hash、uq_staff_api_key_hash）、约束 **PK 62 / FK 32 / UQ 21**（UQ +1＝uq_staff_api_key_hash，FK 持平）；③**62 表模型⇄迁移列名零漂移**；④staff_api_keys 实测 12 列/4 索引与 §2.8 逐字一致。同期 Q178 三接缝（旋钮按 Q135 先例不入 .env.example、认证授权分离零端点签名改动、客户侧后置 V2）已经负责人追认。

## 滚出条目（2026-09-25 第三次续写：Q190 追认批后按上限滚出 Q180–Q182）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.124–C1.126。

- **Q180–Q182 客户卡片视图 / 指标监控 / WAL 归档＋PITR 三片（2026-09-24；三片零迁移、Alembic 头仍 0040/业务物理表 62；代码与文档原子提交、**已 push**（2026-09-25 Q184 复核 dev == origin/dev）；后端常规 842→**855**＋9 skip〔Q180 +3、Q181 +10，Q182 infra 0〕、eval 101/101、ruff 净、前端 tsc＋八 checker＋next build 全绿/token 86/客户 nav 8/admin sidebar 12；02 C1.124–126）**：负责人「一口气搞了」授权 A–D。**Q180（C1.124）** D3.5 客户白名单卡片视图：后端客户集合口 `GET /api/fcw?tenant_id=`（无闸强制单租户，复用 list_fcw_admin），六层沿用 Q155 material.json，test_fcw_admin +3；前端 content 子页 `/content/cards`（客户 nav 不增项仍 8，RSC 卡片列表＋CardMaterialIsland 按需展开，经 Server Action 不直连），lib/api 加 FcwCardView/listMyFcw/getFcwMaterial（复用 Q177 FcwMaterialPack），messages content.cards*，check-content 加守卫。**Q181（C1.125）** 零依赖指标库 `app/core/metrics`（Counter/Gauge/Histogram，Prometheus 文本，标签转义）＋ASGI MetricsMiddleware（path 取路由模板低基数）＋`GET /metrics`（不鉴权同 healthz），可选 prometheus（quay，compose profile `monitoring` 默认不启）；test_metrics_registry 8＋test_metrics_api 2。**Q182（C1.126）** postgres 开归档（walarchive 卷）＋db-basebackup（pg_basebackup）＋wal-archiver（mc 显式 alias 推 MinIO，WAL/base 成功后清理）＋init-replica-hba（补 host replication、chown 归档卷）；pitr-rehearsal 端到端实测 **before=1/after=0**（先基础备份→造数取 T→推 MinIO→拉回前滚到 T）。工程坑：官方镜像 pg_hba 不含 replication、命名卷默认 root 须 chown 999/1000、mc 须 --entrypoint sh 且 MC_HOST env 不加载、Docker Hub 用户命名空间镜像被拒改 quay.io。RPO 由日级缩到分钟级并具备 PITR；跨 region 冗余仍 V2。

## 滚出条目（2026-09-25 第四次续写：Q191 批后按上限滚出 Q183）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.127。

- **Q183 文档收口：运营 SOP 第二版＋README/AGENTS 同步 Q179–Q182＋全量门禁复跑（2026-09-24；零代码零迁移、Alembic 头仍 0040/业务物理表 62；纯文档与验证批次、**已 push**（2026-09-25 Q184 复核 dev == origin/dev）；后端常规复跑 **855** passed＋9 skip〔156.75s，零 pytest 增量〕、eval 101/101（golden 21）、ruff 净、前端 tsc 零错＋八 checker 全 OK＋next build 通过/token 86/客户 nav 8/admin sidebar 12；02 C1.127）**：负责人「一口气搞了」授权。Q178–Q182 落地后 docs/21（Q170 首版）身份口径过期、README/AGENTS 停在 Q178。① **docs/21 第二版**：§0 身份说明订正（Q178 起管理端有可选 staff PAT 认证层、门控 `LOOM_STAFF_AUTH_ENABLED` 默认关、开门控后自报 actor 被令牌身份覆盖防提权；客户侧真实认证仍 V2）＋新增 **§0.1 运营登录与身份**（`loom_staff_` 人员令牌与 `loom_` 机器 Agent Key 分表分前缀不可互换、限五内部角色、鸡生蛋引导三步、/admin/staff-keys 与 /admin/login、回滚＝关 env）；角色表管理台 10→12 页、platform_admin 入口补 staff-keys/exports/fcw；段 11 补 Q177/Q180 白名单卡片查看（运营 /admin/fcw 跨租户、客户 /content/cards 强制单租户）；§3 401 行补 staff 令牌场景；§4 补 `GET /metrics`（Q181，prometheus 走 compose `monitoring` profile 默认不启）与备份恢复双轨（逻辑 pg_dump＋`restore-rehearsal.sh`／物理 WAL＋`wal/pitr-rehearsal.sh`）、ha/load 演练脚本。② README「当前状态」/AGENTS「当前阶段」补 Q179–Q182 四片与 855/0040/62 表基线，Q177 三接缝、Q178 三接缝「待追认」订正为已经追认（C1.121/C1.123）。③ docs/16（Q179–Q182 与 855 台账）、docs/17（§1/§7.5 Q181/Q182）核对完整无需补。④ **同日补遗**：docs/20 MVP 评审追加第六次更新（追记 Q179–Q182 四片、§6 待办指标监控/WAL·PITR/RPO·RTO 销线，MVP 判定不变）；handoff L3/L81/L82 push 状态核对订正（实测 Q178/Q179 已随 origin/dev 推送、Q180–Q183 未 push）、交接完成定义 Q 范围刷至 Q1–Q183。

## 滚出条目（2026-09-25 第五次续写：Q192 批后按上限滚出 Q184–Q187）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.128–C1.131。

- **Q184–Q187 文档对账／指标消费侧／D3.5 复制存盘／discarded 清理甲案四片（2026-09-25；迁移 0041 **纯配置种子、无 schema 变更**、业务物理表仍 **62**；本批代码与文档原子提交**并已推送**（2026-09-25 负责人 push，`dev == origin/dev == 7079bff`，四片＝7 笔原子 commit）；后端常规 855→**879**＋9 skip〔Q185 +13、Q187 +11，Q184/Q186 零增量〕、eval 101/101、ruff 净、前端 tsc＋八 checker＋next build 全绿/token 86/客户 nav 8/admin sidebar 12；02 C1.128–131）**：负责人「执行选项 B」授权。① **Q184（C1.128）** 订正 handoff L3/L78/L80 与 docs/20 §6.5 第六次更新中过期的「Q180–Q183 未 push」（实测 `dev == origin/dev == de08de2` 早已全推），并把 §6.5 按 post-Q183 现状整体重写（A1–A4 与 C1–C3 转历史注记、C4 随 Q187 收口、C6 随 Q165 关闭、新增本批四件表与唯一余项 R1）。② **Q185（C1.129）** `infra/monitoring/alert_rules.yml` 三条 RED 规则＋Grafana 文件式看板/数据源＋监控栈拆独立 opt-in overlay；**改判撤 Alertmanager**（实测 0.34.1 `webhook_configs.url` 不展开 `$(VAR)`、不支持 `env://`）改由 watchdog 轮询 `/api/v1/alerts` 转发进既有 `LOOM_ALERT_WEBHOOK`（firing-only ＋ 按 alertname|instance|activeAt 去重）；**事实纪律修正**：全仓真实指标只有 `up`/`http_requests_total`/`http_request_duration_seconds` 三族、零业务打点，原拟「队列积压/job failed/LockLost/LLM 预算」四类规则属臆造指标名一律不写，加契约测试硬守「规则与面板不得引用未注册指标」；**顺带修掉 Q182 两个致 compose 整体不可用的遗留缺陷**（全新卷 postgres 必挂 → 一次性 root `wal-archive-init` 预备卷属主；staging 彩排 `invalid compose project` → wal-archiver 的 minio 依赖 `required:false` 且随 minio 移出）；演练 24/24（含停 backend→firing→转发且 180s 去重命中恰 1 次）；契约测试 6→19。③ **Q186（C1.130）** `/admin/fcw` final_id 单条＋本页多选复制（表格下沉 client 岛承载选中态）＋六层原料包 JSON 存盘，`/content/cards` 同步补齐；纯前端零迁移零后端。④ **Q187（C1.131）** discarded 成品保留期清理＝**第五个 SLA sweep 作业**，甲案：配置中心 `content.discard_retention_days`＝180 天（迁移 0041 纯种子，pg16 宿主 55449 全链 up/down/up 实测；**注意 0010 运行时全量播种 CONFIG_SEEDS，故 0041 只对存量库生效、全新库幂等空转**，与 0021/0022 同型）、物理删＋逐行 `content.discard_purged` 审计、不动 partial index、**有下游引用者留档不删**（effect_records/effect_claims 硬 FK ＋ import_jobs 软引用）、env `LOOM_DISCARD_PURGE_ENABLED` 默认关、复用既有手工 `/api/admin/sla/run` 不新增端点/页面；+11 集成。**三批接缝（Q185 撤 Alertmanager／Q186 表格下沉岛／Q187 六项）均按推荐甲落地，已经负责人 2026-09-25 概括追认（02 C1.134）（Q190/C1.134）。**

## 滚出条目（2026-09-25 第六次续写：Q193 批后按上限滚出 Q189）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.133。

- **Q189 文档对账批：docs/08 落地状态总览刷新 ＋ handoff 滚档收口 ＋ 修一处上轮自伤（2026-09-25；**纯文档、零代码零迁移零测试变化**；基线仍 892 passed＋9 skip／迁移头 0041／物理表 62／eval 101／前端八 checker；02 C1.133）**：① **docs/08 §2.2 补「当前落地状态（截至 2026-09-25，最新 Q188）」权威行**——该节原最后一行总览停在「截至 2026-09-20（最新 Q136）／后端 699 测试／迁移头 0037／物理表 55→59」，与真实基线差 45 片；旧行不删、就地标注为历史快照备查，新行给全当前基线（0041/62 表/892+9/101/token 86/nav 8/sidebar 12/演练面 32-16-12）与七项未闭合清单，并写明 Q160 之后的逐片权威记录在 docs/02 C1.106–C1.132、本文档不再双写。② **§2.2 时间参考行续到 Q188**（原止于 Q161）。③ **handoff「最近进度」按上限收口**：实测那六条超窗条目（Q178/Q176＋Q177 追认/Q177 首片/Q169–Q172/Q166–Q168/Q162–Q165）的正文片段在本机全部归档里**检索不到**，即并非已滚出的重复残留，故**逐字搬入** `docs/handoff-archive-2026-09-25.md` 后再从主文件删除并补滚出说明；加本条后同法滚出 Q179，主文件维持五条。④ **修一处上轮自伤**：Q188 条目当时以 Edit 前缀替换的方式插入，误将 Q184–Q187 条目的标题头一并替换掉，致其正文成为无归属续行——已按原文复原该标题头（基线数字与 02 编号回补，无内容损失）。**教训入档**：对超长单行台账做前缀式 Edit 必须先确认 old_string 不吞掉相邻条目身份。⑤ 顺带核对：docs/09/10/12/19 无本期漂移；README 当前状态已在 Q188 批内刷至当前基线。

## 滚出条目（2026-09-25 第七次续写：Q194 批后按上限滚出 Q188）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.132。

- **Q188 R1 收口：四类业务信号真实打点 ＋ `loom-operations` 七条告警 ＋ 第二张看板 ＋ 契约守卫改双向（2026-09-25；**零迁移、零新 env、零前端改动**、Alembic 头仍 **0041**、业务物理表仍 **62**；后端常规 879→**892** passed＋9 skip〔+11 新单测／契约 19→21 净增 2／既有 worker 与网关测试内联扩断言不计数〕、ruff 净、eval 101/101、前端 tsc＋八 checker＋next build 全绿（前端零改动）/token 86/客户 nav 8/admin sidebar 12；`infra/alerting-rehearsal.sh` 真栈 **32/32 PASS、exit 0**（Q185 首跑 24/24，本片扩至 10 规则/2 看板；七条业务规则在空栈无序列时 health 仍全 ok＝「无数据 ≠ 求值失败」实证）；02 C1.132）**：负责人「按你建议来」授权，落 docs/20 §6.5 唯一余项 R1 与 docs/17 §7.6 仍挂①。新模块 `backend/app/core/metrics/business.py` 七族全部挂在**真实产出点**：作业失败＝五个 DB 写点（`fail_export_job`／import 两条确定性校验分支＋`fail_import_job`／`fail_fcw_task`，router 入流 fail-closed 因调同一函数被自动覆盖）；死信＝`move_to_dead` 唯一漏斗（写死信＋ACK 双成功后才计，防重试双计）；锁易主＝`LockLease._mark_lost`（看门狗两分支共同漏斗，**不在四个 catch 各计一次**）；队列深度＝新只读原语 `stream_depth`（`XPENDING` 汇总＋`XLEN`）在三处 worker `_tick` 末尾取样；LLM＝网关 `invoke`/`embed` 的预算硬停与驱动往返（`observe_llm_call` 只计时不改异常语义）。**grounding 推翻草案六条**：代码根在 `backend/`、**有第三个 worker（Q165 FCW）**、入流 fail-closed 免费覆盖、**restock 流刻意不采**（Q138 只接生产侧无消费者，采它＝造一条永远正确的假积压告警）、`reclaim_pending` 那条带 idle 过滤且被 count 截断的 RANGE 不代表积压总量、契约测试两处会挡路（规则名集合相等＋`_registered_metric_names` 只 import `middleware` 看不见业务族）。守卫改**双向**（注册了无人消费＝死打点，同 Q176 教训），指标名正则扩至认**裸比较**（原式漏过 `loom_stream_pending > 100` 这类 Gauge 引用），看板守卫改为遍历 provisioning/dashboards 全部 JSON。LLM 直方图显式 1..120s 桶（默认桶上限 10s 会让真供应商调用整批落 `+Inf`）；`sample_stream_depth` 吞后端故障（观测读不得干扰消费循环）。**甲案接缝（已经负责人 2026-09-25 概括追认＝Q190/C1.134）**：死信与锁易主的 severity 分档、作业失败不带 reason 标签、restock 流排除在深度采样外、不建管理端看数页（入口＝Grafana）、不加 env 门控、延迟只在网关层测不在 httpx 驱动层测。**但阈值数值 100/5000/30s 原文未给出——追认只结设计选择，其合理性仍待首批主数据落地后校准。****空栈演练只证明「表达式可求值」，阈值校准待首批主数据回填后重看。**

## 滚出条目（2026-09-25 第八次续写：Q195 批后按上限滚出 Q190）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.134。

- **Q190 四批甲案接缝追认销账（2026-09-25；**零代码逻辑改动、零迁移、零测试变化**，仅一处配置播种文案；基线仍 892 passed＋9 skip；02 C1.134）**：负责人对「还剩什么任务」回复选项 **2**，把此前按推荐甲落地并标注「待负责人追认」的 **Q185／Q186／Q187／Q188 四批工程接缝全部追认**：① Q185 撤 Alertmanager 改 watchdog 轮询转发（代价＝无 grouping/silencing/inhibition）＋「只写真实注册指标名、宁缺毋臆造」口径；② Q186 `/admin/fcw` 表格下沉 client 岛＋剪贴板沿用密钥岛范式＋JSON 文件名；③ Q187 六项（窗口落配置中心故多出纯种子 0041／物理删＋逐行审计／取 `updated_at` 计时／有下游引用者留档不删／门控默认关不新增端点页／180 天·200 行）；④ Q188 七项（severity 分档／作业失败仅 `kind` 一维标签／restock 流排除采样／不建看数页入口＝Grafana／不加门控／延迟只在网关层测）。**追认只结设计选择、不结数值校准**：180 天保留窗口与 100/5000/30s 三阈值原文未给出，其合理性仍待业务方按 docs/19 回填主数据、有真队列与真 failed 后重看（空栈演练只证明表达式可求值）。唯一非文档改动＝`config_center/seeds.py` 里 `content.discard_retention_days` 的 `source_ref` 由「待追认」改标「2026-09-25 负责人追认」，**注释已留痕：该字段只在播种时写入，存量库那行仍显示旧文案，本仓无种子回写机制，故不为此补数据迁移**（属可见陈旧）；全仓无测试断言该串。**扫描中的自我更正一处**：AGENTS 旧句把 Q122 三接缝写成「待追认」，我初稿据此记为未追认项；回查 docs/02 C1.66 发现该批 **2026-09-19 已明确追认销账**（条目内有【追认销账】整条），真实缺陷是**入口文档措辞过期**而非决策挂起，已就地订正并在 02 留痕。教训：扫到「待追认」字样必须先回查 02 有无后续销账条，不能以入口文档措辞为准。

## 滚出条目（2026-09-25 第九次续写：Q196 批后按上限滚出 Q191）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.135。

- **Q191 补值班面：docs/21 新增 §5 告警与值班处置（2026-09-25；零代码零迁移零测试变化；基线仍 892 passed＋10 默认 skip；02 C1.135）**：负责人问「还要干嘛」，于是 diff「已具备的能力 ⇄ 运营真正可达的面」，查出 Q185／Q188 亲手造出的缺口——**10 条告警规则已立，而唯一给值班人看的 docs/21 里「告警」0 次、「Grafana」0 次**：平台第一次能自己报出事，却没人被告诉先查哪里、要不要停线。§5 承重内容：① 三条最容易误导人的前提（默认部署一条都不发／**一个故障只通知一次**故静默≠恢复／worker 默认关时队列规则永不触发是事实不是漏报）；② 10 条规则→首查位置→是否停线表（只引用已核实的端点、审计 action、死信流字段、门控 env）；③ 数值来源纪律（100/5000/30s 与 180 天原文未给出，**追认不豁免校准**）；④ `LOOM_GRAFANA_ADMIN_PASSWORD` 无默认值即报错的运维事实。**刻意不做**：Grafana 未发布宿主端口，看板可达需临时转发，该配方未实测故不写进手册当标准步骤；开不开 `127.0.0.1` 回环端口属发布面收敛口径，**待拍板已登记**。**自查纠正**：§5 初稿写了不存在的 `LOOM_LLM_HTTP_TIMEOUT_*`，回查源码实为 `LOOM_LLM_HTTP_TIMEOUT_SECONDS`（默认 60s）并补注「超时打满会同时顶到 30s P99 规则」——与 Q190 同一课：**写进手册的每个标识符必须回源码点名核过**。

## 滚出条目（2026-09-25 第十次续写：Q197 批后按上限滚出 Q192）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.136。

- **Q192 Grafana 发布宿主回环端口 `127.0.0.1:3001`（2026-09-25；零迁移零后端应用代码；02 C1.136）**：Q185 按 Q145「唯一发布口」刻意不给 Grafana 开宿主口，两张看板置备了却没人能从宿主打开——Q191 写值班手册时被这个矛盾顶出来（手册只能写「需临时转发、未实测故不给步骤」，等于告警消费面最后一公里没通）。负责人拍板「开，只绑回环」。两条约束写死：**必须 127.0.0.1**（Grafana 唯一的门就是管理员口令，绑 0.0.0.0＝裸露全网扫描）、**绝不能是 3000**（base compose 里宿主 3000 已是 frontend，撞上去直接端口冲突），故取 3001（全仓核过无占用）。守卫：契约测试拆两条，新增 `test_grafana_is_loopback_only`（恰一个映射／IP 字面量须为 127.0.0.1／容器口 3000／宿主口≠3000／与 frontend 宿主口求交为空），并按「守卫必须能被证伪」自检——内存造四种坏配置（缺 IP／撞 3000／开两口／显式 0.0.0.0）全判红、真配置判绿；**首次证伪尝试无效**（用标量替换让 `ports` 变成字符串，"19 ports" 其实是 19 个字符），重做才对；中途「改真实 compose 植缺陷再回滚」被权限层拦下，本就该在内存里造。演练：`alerting-rehearsal.sh` 阶段 3 加两条**宿主侧**断言（宿主机 curl 3001/api/health 200、`docker port` 只绑回环）——用网格内可达证明这次改动等于没证明，全量 **34/34 PASS**（原 32/32，+2），拆栈无残留。docs/21 §5 可达性段改写为实测事实，并明确**远程看盘需自起隧道、该做法未实测故不充当标准步骤**；隧道/反代与匿名只读放行均不在本次范围。基线 **893 passed＋10 默认 skip**（原 892；Q192 把监控契约测试一条拆两条，**passed 净增 1**，非新增被测行为）（全量实测 665s）。

## 滚出条目（2026-09-25 第十一次续写：Q198 批后按上限滚出 Q193）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.137。

- **Q193 业务告警实触发：查出并修掉「首次事件系统性不可告警」（2026-09-25；零迁移；后端 893→**898 passed**＋10 默认 skip；演练 34→**39/39**；02 C1.137）**：把 Q188 那七条业务规则从「表达式可求值」推到「真出事前会发生」：演练叠加层 `docker-compose.alerting-rehearsal.yml` 开 `LOOM_EXPORT_WORKER_ENABLED`，停 redis → `POST /api/exports/jobs` 走 Q137 真实入流 fail-closed（503＋落 failed）。**修复前实测**：`/metrics` 已 `loom_job_failed_total{kind="export"} 1`、Prometheus 也抓到 1，但 `increase(…[15m])` 恒 **0**、alerts 为空、转发 0 次。**根因**：counter 懒创建，该标签组合首个样本就是 1，Prometheus 从未看见 0→1 跳变 ⇒ **进程内第一次发生的该类事件系统性不可告警**——而 private beta 低流量要报的恰恰就是这一次；既有单测只验注册表增量、替身 Redis 只验语义形状，两者都盖不住（与「假件证不了假设」同课）。**修法＝预置零序列，且由拥有词表的模块各自登记**（避免 metrics 反向 import 域包成环）：作业三类／两把循环锁／死信随被消费的流（只有 worker 在跑才可能产生）／预算按 `seeds.all_scene_codes()` 九场景。**刻意不换函数**：`min_over_time(...)>0` 对累计计数器会在事件后永久常亮，是另一种坏。四条新守卫均**顺序无关**（REGISTRY 进程全局，故只断言必备组合⊆已登记、且用自建探针标签验 exposition 先 0 后 1），另加两条漂移守卫：扫 `app/` 全部 `record_job_failed("…")` 字面量必须都在已预置集合内、`all_scene_codes()` 必须等于 `SCENE_*` 常量集。**顺手抓出并修掉演练自身三处自伤**：① 阶段 4「恢复后可抓取」原判据是「规则列表里出现该名字」——那名字恒在、**永远绿**，换真判据后又立刻暴露固定 `sleep 45` 不够（entrypoint 先跑 alembic，冷启动随迁移数增长），改轮询收敛（实测 30s）；② `docker logs` **跨轮次累积**，容器未重建时上一轮的转发行会被本轮计入 → 去重断言假失败，两处计数改 `--since 本阶段起点`；③ **我宣布的「已清空重跑」其实没清空**：手敲 `docker compose down -v` 因 Grafana `:?` 口令在解析期插值而整条命令失败（正是 Q185 记过的行为），留下 10 容器 7 卷，于是同一轮里同时出现「假失败（去重数到 2）」与「假通过（15s 就 firing，实为上一轮事件还挂在 15 分钟窗口里）」。脚本现起栈前自检残留、非 0 即 ABORT。**教训**：拆除要验，「clean slate」本身是一条需要被证明的断言，不能凭命令退出码宣布。最终干净栈（容器 0 卷 0）**39/39 PASS**：firing 于 **75s** 观测（正对 `for:1m`）、转发于 **105s** 命中且恰 1 次、`kind=export` 确在告警负载内。**固有局限注明不修**：直方图无法预置空序列 ⇒ `LoomLlmUpstreamSlow` 需窗口内 ≥2 次调用才有分位，单次慢调用不触发，这是分位数性质非缺陷。

## 滚出条目（2026-09-25 第十二次续写：Q199 批后按上限滚出 Q194）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.138。

- **Q194 对账批：docs/20 五处「已落地却仍写作待办」＋ docs/02 续编名单断在 Q187（2026-09-25；**纯文档，零代码零迁移零测试变化**；后端复跑 898 passed＋10 skipped、ruff 净、eval 101/101；02 C1.138）**：回答「迭代计划还剩什么」时逐字读 docs/08 §2.2/2.3/2.4、docs/20 §6.6 全三十行与 handoff 待办，对出同文自相矛盾——§6.5 的「重写缘由（Q184 对账）」注记（现 L179）与「2026-09-22 第二次更新」注记早已声明「A1–A4 与边界模糊项 C1/C2/C3 已随 Q166–Q168 落地」，而**边界模糊项条目本身、§6.6 #29/#30、「不可一口气推进（摘要）」第三条、§6.7 第 1/3/4 条仍是落地前的措辞**（与 Q184/Q190 同型：一条逻辑记录＝数千字符长行，改一处漏一处）。订正＝原文加删除线保留＋补 ✅ 收口注记（C1＝Q166/02 C1.110 并注明「原句自陈的前置条件『需改裁决』确已发生：负责人该批拍板把 analytics 提前入 V1、翻转 Q97/Q114 徽标裁决」；C2＝Q167/C1.111 `/admin/agent-keys`；C3＝Q168/C1.112 `/admin/exports`；#30＝Q187/C1.131 六项并经 C1.134 追认，解锁条件改写为「**追认只结设计选择**，180 天数值仍待校准」；§6.7 第 3 条「文档滞后已全部闭合」注明**不是一条永久性质——本批即对它的一次证伪**）。**顺带查出第六处**：docs/02 文末「信息保全」续编名单断在 C1.131 Q187，缺 C1.132–C1.137，即权威台账自己的索引过期了，已续至 C1.138。**自我订正两处计数**：口头汇报说「三处」，实测六处；第一次跑门禁把 eval 路径写成 `backend/eval`（实在仓库根 `eval/`），ruff 因此报出一个由我的命令造成的 error，照 CI 原文重跑即净——**门禁命令照 CI 抄，不凭记忆构造**。§6.6 其余未落地行（#4 DIM-SOURCE、#5 video-studio、#6 复检余两项、#7 反哺校准、#15 单价、#16 embedding、#17 图像、#18 五型 SLA、#19 G2 余 6 字段、#20 slotType/平台目录、#21 触发方、#22 语义四 code、#23 Router fallback、#24 draft 通道、#25 G1、#26 PWC 复核、#27 Q70 二期、#28 多币种）**一律维持原状不动**；判「已落地」的唯一依据是 02 存在对应 Q 条目，未找到即不改。


## 滚出条目（2026-09-25 第十三次续写：Q200 批后按上限滚出 Q195）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.139。

- **Q195 身份层第二切片的前置口径决策请求（2026-09-25；**⬜ 待负责人拍板**、纯文档零代码零迁移零测试变化；新增 docs/design-identity-layer-v2.md；02 C1.139）**：Q194 文档对账后按约定转向身份层，但 §6.6 #1 那句「客户侧真实认证、`actor↔tenant` 绑定（Q88 口径待补）」**不可直接落码**——Q88 当年刻意让 Key 不绑租户并留了「归属由 content_id→FCW.tenant_id 解析【原文未给出，待补】」。于是本条**只提问不裁决**：把绕不开的三个口径各写成甲乙丙（**A** 归属来源＝服务端反查/客户 Key 绑租户/账号会话；**B** 自报身份还能不能进审计＝受保护写口服务端派生/从 92 个模型删字段/只闭写口；**C** beta 期要不要真客户登录＝维持 Q144 代运营/现在做），推荐 **A甲＋B甲＋C甲**；并**用命令把现状量出来**供拍板估代价（205 条路由里**只有 4 处**校验真实凭证、**92 个**请求体模型自带 `actor`、`append_audit(` 122 处、`assert_intake_admitted` 全仓**仅 1 个**业务入口在调、测试面 53/96 文件 714 处 JSON `"actor"`）。**顺带查出两处既有缺口（只登记不自动修）**：① **导出任务状态口/下载口既不鉴权也不按租户归属**（`core/exports/router.py:166,179`＋`service.get_job` 无租户条件），而同族导入口用 `get_scoped_job(..., tenant_id)`（`core/imports/router.py:87`）＝**同族两口归属口径不一致**，且 `job_id` 是 `uuid1()`（含时钟与节点分量、非密码学随机）不宜独担授权凭证；② 客户写路径不过 RBAC，`core/effects/service.py:393-401` 把 `batch.actor.id/roles` **原样写进审计**。两者已入 docs/20 §6.6 新行 **#31**；补闸虽属 Q113/Q118 已追认两次的「管理面读口同型补闸」，但**是收紧既有 HTTP 契约**，按纪律等点工。

## 滚出条目（2026-09-26 第十四次续写：Q202 批后按上限滚出 Q197 与 Q196）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.141／C1.140。
> 补记：并行会话补 Q200/Q201 时未滚档，handoff「最近进度」一度到 6 条，本批一次滚出两条恢复纪律。

- **Q197 Q196 三接缝追认销账（2026-09-25；**纯文档，零代码零迁移零测试变化**；基线仍 **912 passed＋10 skipped**；02 C1.141）**：负责人一句「追认」结掉 Q196 的三条甲案接缝——**①②** 审计来源标签与降级留痕住 `detail` 保留键 `_actor_via`/`declared_actor`、**不建列不建迁移**（代价写进台账：按来源筛选要走 JSON 条件）；**③** 导出任务两条读口以「`tenant_id` 必填＋按任务行归属收口」闭合、**不加内部角色闸**（Q168 记该面「中台无 RBAC 闸」、轮询方是外部中台）。**口径同 C1.134：追认只结设计选择**——C1.140 里「本切片没闭合什么」四条原样继续挂着（客户读面仍以声明 tenant 过滤、92 个自带 actor 的模型保留、11 处非授权路径 uuid1 未动、阈值与 180 天窗口仍待主数据校准），本次**不新解锁任何东西**。改标范围只动三处活状态（02 内联、docs/20 §6.6 #31 解锁列、设计文档落地记录），**逐片历史不回改**（归档档/CHANGELOG 当时条目/docs/10 0041 注/seeds.py 播种注）。
- **Q196 身份层第二切片落地＝A甲＋B甲＋C甲（2026-09-25；**零迁移、零新 env、零新表**；后端 898→**912 passed**＋10 默认 skip；02 C1.140）**：负责人「好的，按你推荐来」按 Q195 的推荐组合拍板。**落点刻意选在引擎级**——「已验真凭证 > 请求自报」只写在**全仓唯一审计口** `append_audit` 里，122 个调用点一行未改即全部生效（逐端点改正是本仓 Q117/Q118 抓过的「漏网补闸」来源）；新增 `app/core/identity.py` 承载请求级已验真凭证（staff PAT 与入站 Agent Key 都算），`staff_auth_context` 每请求入口**先清零**再判定，`require_agent_key` 验真通过后置入；审计 detail 落 `_actor_via`∈{staff_token, agent_key, declared}，被推翻的自报值降级为 `declared_actor` 留证（零迁移的代价：这两个键住在 JSON 里而非列上）。**A甲**＝客户回填审计的租户取自命中成品行而非调用方声明（今天二者恒等，故用 monkeypatch 伪造「过滤被放宽后的世界」才钉得住——正常用例证不到差异）。**#31 两处补齐**＝导出任务状态口/下载口 `tenant_id` 必填＋新增 `get_scoped_job` 按任务行归属收口（跨租户与不存在**同回 404**，不做存在性探针）、`job_id` 由 uuid1 改 uuid4（导出同步/异步＋导入三处）；**刻意不加内部角色闸**（Q168 记「中台无 RBAC 闸」且轮询方是外部中台，加闸会打断设计中的消费路径）。**证伪自检抓出一处假守卫**：植入「状态口退回不校验租户」后 **24 条用例全绿**——我的新用例只钉了下载口的跨租户分支，状态口那条断言压根不存在；补断言后才判红。九种植入缺陷最终全部判红、每处按字节还原校验。**没闭合什么写清楚**：客户侧读面仍以声明 `tenant_id` 过滤（闭合需客户凭证，C甲＝beta 不做登录，属**已接受风险**非遗漏）、92 个自带 `actor` 的请求体模型保留（B乙未选，采信与否由服务端定）、另有 11 处非授权路径上的 uuid1 主键未动。

## 滚出条目（2026-09-26 第十五次续写：Q203 批后按上限滚出 Q198）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.142。

- **Q198 文档一致性扫描批（2026-09-25；**纯文档，零代码零迁移零测试变化**；基线仍 **912 passed＋10 skipped**；02 C1.142）**：负责人「更新项目文档」触发又一轮 Q117 型对账——把 docs 里**活着的**陈述逐条对回代码。13 份文档就地订正 **24 处**（`git diff` 删除行数＝24，一删一增一条）＋补登 **4 张从未登记的物理表**（`staff_api_keys`/`import_jobs`/`restock_claims`/`sweep_tick_claims`，此前在 docs/04 零命中）、**4 行横切组件**（Q161 imports／Q178 staff_auth／Q181·Q188 metrics／Q196 identity）、**1 节选型补记**（docs/14 新增 §5：可观测性＝零依赖自写指标＋刻意不引 prometheus-client/Alertmanager；内部认证＝PAT＋认证授权分离＋审计侧凭证优先）。典型过期态：docs/05 导出任务两行仍写「V2 真异步后供轮询」且无 `tenant_id`、docs/10 权威小节标题停在「迁移 0001–0040」、docs/15 `app/core/` 注释停在 6 项手写字（实为 24 包）、docs/04 实体表仍说入站 Key「待后续切片」、docs/11 两个 §2.4 抢同一个号、BENCHMARK 还写着「当前处于文档阶段，无代码可测」、MIGRATION_CONVENTION 教人用 `--autogenerate`（实测 41 个迁移零个是自动生成的）。**纪律**：只改活状态，逐片历史记录一字不动；【待补】/【待拍板】口径一处未转正。**复核拦住调研四处错**：`sweep_tick_claims` 主键被写成 `tick_id`（实为 `lock_name`）、把 Key 段改 §2.5 时撞上已有 Q155 段（落到真正空出的 2.7）、docs/15 补登行少写一列、一次批量插入把注记投到了文件末尾——**批量脚本打印成功不等于落对位置**，落点/列数都要独立核。本轮也是「工程侧无可推进项」这句话第五次被自己的扫描推翻（前四次：Q189、Q191、Q194、Q195→Q196）。

## 滚出条目（2026-09-26 第十六次续写：Q204 批后按上限滚出 Q199）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.143。
> 补记：本条正文里的「三条缺陷只登记不自动修」是 Q199 时点状态，其后 Q200/Q203 已逐条修完（见 C1.144/C1.147）。

- **Q199 项目级评审（功能性／完整度／可上线；2026-09-25；**纯评审，零代码零迁移零测试变化**；基线实测 **912 passed＋10 skipped**；02 C1.143）**：负责人要求重判「是否达到产品核心完全可用的 MVP」。**做法是不引用自家结论**——两份只读调研铺面后逐条回源码复测（种子 5/6/2/4 行、41 个迁移 downgrade 全非空、0008 仅两处 bulk_insert、`_get_content` 裸主键、compose 三条数据端口发布、临时 Fernet 键分支，全部本次实测）。**三层判定拆开给**：① **功能覆盖达标**（13 段全有可运行路径、段1→6→10→11 真链路 e2e、912 测试＋eval 101 支撑）；② **「核心完全可用」未达标**——除主数据外部阻塞外查出**三条此前任何文档都没登记的工程缺陷**（docs/20 §6.6 新行 #32/#33/#34）：**#32 多租户隔离可被单个客户穿透**（`content/service.py:133-137` 裸 `session.get`，approve/reject 无角色无归属校验，读路径 id 直接来自 URL ⇒ 客户可裁决**他人租户**成品，`test_content_api.py` 零条跨租户用例）；**#33 默认 compose 把 5432/6379/9000 发布到宿主**、口令写死、`LOOM_MASTER_KEY` 默认空 ⇒ 重启后供应商 Key 不可解（docs/17「唯一发布口 3000」对 base compose 不成立，契约测试只看 backend 看不见数据面）；**#34 治理不变式默认不生效**（门控关 ⇒ 自报 `operations` 即可 mint `final_id`，「唯一出口」只是注释约定且测试里已有 ORM 直插先例，beta 前提「后端在网关后」仓内无产物）；③ **可上线未达标**。可承诺最小集＝#32＋#33＋#34＋主数据回填（`0008` 对 `publish_slots`/`pcp_weight_tables`/`packages`/`platform_rules` **零行播种** ⇒ 全新库 Guard③ 与发布位解析必败）。**三条缺陷只登记不自动修**——每条都收紧既有契约或改动默认部署形态，须点工。**本轮又抓出自己两处错**：上一批我插 docs/20 更新块时插到了「第七次」之前且编号写成「第九次」（序列里没有第八次），本轮实测顺序后订正；本次先在 §6.7 第 5 条之前插入新结论会打乱序号，复测后改到末尾。

## 滚出条目（2026-09-26 第十七次续写：Q205 批后按上限滚出 Q200）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.144。
> 补记两处后事：本条里「fcw 两写口补 `require_any_role(INTERNAL_ROLES)`」的转述不准确，实际代码是 `require_any_role(body.actor, OPERATIONS)`（勘误见 C1.146／docs/20 §8.5）；而「门控关维持 V1 自报」这条承诺已由 Q203 在这两个口撤除（C1.147）。

- **Q200 #32/#33/#34 三条 P0 修复落地（2026-09-25；**代码＋测试＋文档，零迁移**；基线 **912→926 passed＋10 skipped**（总收集 936；＋14＝隔离 7＋fcw 判红 2＋契约 5）、ruff 净、eval 101/101、前端 tsc＋八 checker＋next build 全绿；02 C1.144）**：负责人「那你开搞」点工 Q199 §7.4 最小集前三项，全部按判据修到先红后绿，**Q199 判定 ②③ 的工程项销账，唯一剩余阻塞＝主数据回填**。**#32**：五个带 id 客户口（详情＋approve/reject/revise＋PATCH body）与 intake target-languages 全加 `tenant_id: str = Query(min_length=1)`，service `_get_content` 归属不符统一 `ContentNotFound`（404，与 #31「跨租户与不存在同回 404、不提供存在性探针」口径一致）、intake 同型；运营口（regenerate/discard/publish-info/两个 admin 读队列）不动；前端 `lib/api.ts` 四组客户调用附 `CURRENT_TENANT_ID`（server env 派生）零签名改动；新建 `test_content_tenant_isolation.py` 7 用例全绿（修前必红正是 §7.2 探针断言：跨租户读/裁 404、A 侧状态**未被改**仍 review、缺 tenant_id 422、同租户读/裁 200 至 ready_for_publish、intake 语言口跨租户 404＋同租户 200）；存量 **110 处/23 文件**逐一适配 tenant_id。**#33**：三数据端口 `5432/6379/9000-9001` 全绑 `127.0.0.1:`（本机 psql/redis-cli/mc 仍可达）、`POSTGRES_PASSWORD`/`MINIO_ROOT_PASSWORD` env 插值（DSN/db-backup/db-basebackup/wal-archiver 同步消费）、三 datastore 补 `restart: unless-stopped`（§7.4 判据项）、空 `LOOM_MASTER_KEY` 由 entrypoint **启动即 fail-fast**（刻意不落 compose `${VAR:?}`——解析期全局求值打断彩排/监控 overlay，Q185 教训）、三彩排脚本注入 rehearsal-only 测试密钥；新建 `test_default_compose_hardening_contract.py` 5 例全绿。**#34**：`final_whitelist/router.py` 两写口补 `require_any_role(INTERNAL_ROLES)`（门控开无令牌→401、无内部角色→403；门控关维持 V1 自报＝Q144 beta 默认形态），`service._require_ops` 保留作 worker 内部兜底，新增判红 2 例钉「唯一出口」；ORM 直插测试桩（`test_content_api.py:60-62`）保留。**未闭合**：§7.4 项 4 **主数据回填仍为唯一外部硬阻塞**（业务方按 docs/19）；Q199 判定三「仓内无网关/TLS 产物」属部署侧决策非本批可修。零迁移、物理表仍 62、Alembic 头仍 0041。

## 滚出条目（2026-09-26 第十八次续写：Q206 批后按上限滚出 Q201）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.145。
> 补记：本条所订正的 README／docs/05／docs/21 活状态，其后又被 Q203（发证口需令牌）与 Q206（最小可发证清单）分别追加过一次口径，见 C1.147／C1.150。

- **Q201 文档对账更新批（2026-09-25；**纯文档零代码零迁移零测试变化**；02 C1.145）**——README「当前状态」补至 Q200（基线 912→926 passed＋10 skipped、两处 Q1–Q189→Q1–Q200、阻塞口径更新为「主数据回填＋网关/TLS 属部署侧决策」）；docs/05 段12 内容口六行补 Q200 租户契约（`tenant_id` 必填 query、跨租户与不存在同回 404）；docs/21 §3 错误码表补内容口/intake 语言口 404/422 口径。只改活状态，评审当日基线（842/912）与历史记录一字不动。
## 滚出条目（2026-09-26 第十九次续写：Q207 批后按上限滚出 Q202）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.146。
> 补记三处后事：本条判定的「#34 半闭」已由 Q203 收口（C1.147：出口守卫＋发证两写口无条件验真），其中「4 个测试文件直插 ORM」的低估在同批实测为 **9 文件 16 写入点**；本条四条残留里的「本次 CI 实际结果从未被读过」已由 Q204 结清（C1.148），「真 PG 迁移不在 CI」由 Q207 结清（C1.151），仍未进 CI 的是五套全栈演练与 real-infra 8 例。

- **Q202 2026-09-26 复评（**纯评审，零代码零迁移零测试变化**；实测 **926 passed＋10 skipped**；docs/20 §8；02 C1.146）**：再判「是否达到产品核心完全可用的 MVP」。**先与 git 对账**——HEAD 已从 `8e3804a` 前移到 `174604f`（并行会话按「那你开搞」落了 Q200 三条 P0 修复），故不复述其结论、逐条对当前代码复测。**执行证据**：抽出 §7.2 那份探针再跑，读口 200→404、写口 422；**但 422 只证明 `tenant_id` 变必填、不证明归属被强制**，遂补探针 #2（攻击者带自己合法的 `tenant_id`）：五条越权 **404**、discard **403**，而 A 自己读／裁决均 200 且状态推到 `ready_for_publish` ⇒ 拒绝来自归属判定而非功能被整体打死，**#32 判闭合（执行级）**。**#33 闭合**（数据端口绑 `127.0.0.1`、口令 env 化、datastore 补 restart、空 `LOOM_MASTER_KEY` 启动 fail-fast、新契约测试跑绿）。**#34 半闭**（角色面仅门控开时成立，默认关＝opt-in；`final_id` 唯一出口仍无运行期守卫、4 处测试仍直插 `FinalContentWhitelist(...)`）。**复判＝① 达标／② 仍未达标／③ 仍未达标**，卡点收敛为**主数据零行 ＋ #34 另一半**；四条残留已登记（弱默认口令、门控默认关、唯一出口无守卫、演练与真 PG 迁移不在 CI 且**本次 CI 未被读过**）。§7.1/§7.4 与 §6.6 三行属活状态，原文不动只加指针与闭合注。**顺带修一处并行会话文档债**：补 Q200/Q201 时未滚档，「最近进度」一度到 6 条，本批一次滚出最旧两条恢复纪律。

## 滚出条目（2026-09-26 第二十次续写：Q208 批后按上限滚出 Q203）

> 逐字搬入，一字未改；权威逐条台账仍是 docs/02 C1.147。
> 补记：本条末句「另有网关/TLS 与四条残留；**CI 结果依然没被读过**」是 Q203 时点状态——前半句仍在（网关与 TLS 归属待负责人裁决，AGENTS 2026-09-26 条），后半句已由 Q204 首次读到 step 级（C1.148）、并由 Q207 把真 PG 迁移门送进 CI 且在 runner 上读到绿灯（C1.151）结清。

- **Q203 #34 两半与弱默认口令收口（2026-09-26；**代码＋测试＋文档，零迁移零新表零新 env**；基线 **926→938 passed＋10 skipped**（总收集 948；＋12＝出口守卫 6＋发证口凭证净 4＋compose 契约 2）、ruff 净、eval 101/101（golden 21）、前端 tsc＋八 checker＋next build 全绿；docs/20 §9；02 C1.147）**：负责人「按你推荐的来」点工 Q202 三条。**① 唯一出口成运行期事实**——新 `app/final/final_whitelist/exit_guard.py`（`issue_scope()` 协程作用域 ＋ `FinalContentWhitelist` mapper `before_insert`），装载点在 `models.py` 类定义之后（只 import 模型也带着守卫），生产端唯一入口仍是 `service.assemble_one`；`test_fcw_single_exit_guard.py` 6 例，**证伪＝把装载行标注释后 3 条拒绝类断言判红**，许可类照绿；**覆盖边界实测**：Core `insert()`/裸 SQL 不经 mapper 事件，由 `test_core_insert_bypasses_the_mapper_guard` 钉住（全仓今日对该表无 Core 写入）。顺带改正 Q202 的低估：「四处直插」实为 **9 文件 16 写入点**，统一改走新夹具 `tests/integration/fcw_rows.py::add_fcw`（伪造签发作用域要显式声明，不悄悄绕守卫）。**② 发证两写口只认已验真令牌**——新 `require_internal_actor(*roles)`（`staff_auth/deps.py`，与 `internal_gate` 相反：门控关时自行验真），两口缺/坏/吊销 Bearer → 401、令牌无 operations → 403、已验真身份覆盖 body 自报 `actor`（Q196 存证口径不变）；`get_auth_session` 改为「门控关但带 Bearer 也要给连接」；撤除两口内的 `require_any_role` 与随之失效的局部 401/403 分支。**取消 Q200 的「门控关维持自报」承诺在这两个口的适用性＝本批唯一破坏性契约变更**，docs/19 验收第③步、docs/21 §2、docs/05 §1.3/§1.4 同步加注。新 6 例（含`test_write_routes_stay_wired_to_the_credential_dependency` 接线自检 ＋ 只读口反向对照）；**证伪受限如实记**：「摘掉安全依赖」式植入被本仓权限层拦下（一次性 worktree 亦拦），故改以永久接线自检替代。**③ 口令无默认**——`infra/docker-compose.yml` 六处 `:-loom`/`:-minioadmin` 删除（postgres/minio/backend DSN/db-backup/db-basebackup/wal-archiver），未设即由容器自己启动失败；不用 `${VAR:?}`（解析期全局求值会打断没设变量的 overlay/彩排，Q185 坑）；ha/load/alerting 三套彩排注入 `loom-rehearsal-only-{pg,minio}`；compose 契约测试 5→7 例；`.env.example`／docs/17 §3 改口径。**自我更正**：Q202 登记的「docs/06／docs/09 三处内容缺口」经 grep 复核**不成立**（属二手结论未回原文，本仓「转述未复核」第三次发作）；真缺口在 docs/21 令牌口径与 docs/19 验收步骤，两处已补。**复判 ① 达标／② 仍未达标（工程侧卡点清零，只剩主数据零行）／③ 仍未达标（另有网关/TLS 与四条残留；CI 结果依然没被读过）**。
