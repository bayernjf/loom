# Q98 产品中心服务端配置（不带 NEXT_PUBLIC 前缀：仅服务端可见，不进入浏览器包）
# FastAPI 基址；浏览器经 RSC/Server Action 间接访问，后端无需 CORS。
LOOM_API_BASE_URL=http://localhost:8000
# V1 无真实认证，当前租户由服务端 env 固定；V2 认证上线后改由会话派生。
LOOM_TENANT_ID=t1
# Q106 客户侧写操作（提交/补资料/编辑草稿）的操作人身份，服务端自报；
# roles 恒空（客户事件无角色要求），未配置时页面只读、不发写请求。V2 改由会话派生。
LOOM_ACTOR_ID=c1
# Q102 管理端管理员身份（Q107 起含录入单运营队列）。默认 platform_admin 可看全部管理页；
# 录入单运营操作（6 个 ops 事件）状态机硬要求 operations 角色，不含该角色时队列只读、
# 操作岛零请求并提示缺角色。多角色逗号分隔。V2 改由会话派生。
LOOM_ADMIN_ACTOR_ID=admin-1
LOOM_ADMIN_ROLES=platform_admin,operations
