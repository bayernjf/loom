# Handoff — Loom

> **State of Loom as of 2026-09-16**（设计/文档定稿；M1–M8 + M11 后端切片已落地（实现序 M7→M11→M8，V1 主链 段1→6→10→11 已闭合），M10 切片 a 配置中心底座、切片 b 通用 SLA 引擎+定时调度、切片 c 缓存引导+常量消费迁移、切片 d RBAC 红线收口（Q75）、切片 e skill7 AI 候选通道·WF-04 试点（Q76，Q71 补货接通）已落地，M10-Q 质量两件套（Q77）已落地，WF-02 字段池占位替换切片 1/3（Q78，DIM-MERGE field_plan 候选）已落地，WF-01 冷启动识别占位替换切片 2/3（Q79，仅 C1 识别，CAT-RECOG c1_recognition 候选 + intake 锚点迁移 0013）已落地，WF-03 字段下原子占位替换切片 3/3（Q80，CONFLICT-PRECHECK 末步产 atom_batch 整批候选，product_reviewer 准入，无新迁移）已落地，WF-01 C7 Layer4 占位切片（Q81，TYPE-MATCH 第二产出步骤产 c7_layer4 整 C7 解析请求单候选，operations 准入，Q79-1 挂账销账，无新迁移）已落地，**Q67/Q82 模型网关+CAT-RECOG 真 LLM 试点（模型底座五表迁移 0014、进程内同步复用 skill7 通道、outbound Key DB 加密+env 主密钥、Prompt DB 版本化）已落地，Q83 PWC-BUILDER 第二站（迁移 0015 纯种子、一次调用多候选、模型回带分一律剥离、llm-build operations 端点）已落地，**Q84 TYPE-MATCH/C7 Layer4 第三站（迁移 0016 纯种子、整结果单候选 c7_layer4、回显漂移与脏提案 502、模型回带 confidence/score/source_route 一律剥离、llm-resolve operations 端点）已落地，Q85 DIM-MERGE/WF-02 字段池方案第四站（迁移 0017 纯种子、整方案单候选 field_plan、target 回显与结构脏/证据空白 502、confidence 按 PT-FP-PLAN 契约 0..1 透传驱动 Q9/Q12、业务违规照落非合规池 Gate 拦截、llm-plan operations 端点）已落地，Q86 ATOM-EXPAND 原子补池第五站/末站（迁移 0018——首个带 schema 的 LLM 切片：pgvector 扩展/ai_models.capability chat|embedding/两表 vector(1536) embedding 列/atom.cluster_line=0.9 借 Q10 同义线原文未给；CONFLICT-PRECHECK chat + ATOM-AFFINITY embedding 双调用合并单 SkillRun、单 atom_batch 候选复用 Q80；模型自报 affinity/cluster_id 剥离、本地进程内余弦+同维并查重算、ai_risk 契约枚举透传；llm-expand operations 端点、花 token 前停拓等预闸）已落地，Q87 M8 restock_auto worker（进程内第二个 asyncio 循环同构 SweepScheduler，默认 false 部署 opt-in + platform_admin 手工 /api/admin/restock/run；纯追加 succeeded/failed 的 llm_auto 子 run 反连接认领、requested 信号行永不 mutate；仅自动消费 PWC-BUILDER 只产 pending_review 人工 Gate 不改，终态追加 failed 子 run、预算硬停/上游错瞬态下轮重试；无迁移，头仍 0018；四接缝 02 C1.31 负责人拍板）已落地，Q88 入站一 Agent 一 Key 凭证半套（Q60b effect-callback 鉴权前置；迁移 0019 新表 agent_api_keys，SHA-256 单向哈希、明文仅签发返回一次、platform_admin 治理 /api/admin/agent-keys、Bearer 401 验签依赖已备；effect-callback 端点与段13 回流本体仍 V2/P3；四接缝 02 C1.32 负责人“四项全按推荐（甲）”拍板）已落地，Q89 多副本单实例锁（Redis SET NX PX 两把循环级 leader 锁 loom:lock:sla-sweep/restock-worker，看门狗续约；LOOM_DISTRIBUTED_LOCK_ENABLED 默认关、fail-closed 跳轮；两个手工 /run 同锁 409/503；无迁移；四接缝 02 C1.33 负责人四项全按甲拍板）已落地，Q90 restock 瞬态失败重试退避（新表 restock_retry_state 游标/迁移 0020，指数退避 60→1800s 封顶，预算硬停永不转终态、上游错 10 次转终态 failed 子 run，env 三参数非 Q9 旋钮，手工 /run 绕窗口仍计数；四接缝 02 C1.34 负责人四项全按甲拍板）已落地，Q91 注册表驱动编排并行 DAG 原语（新包 app/core/orchestration/：needs 拓扑分层/同层 asyncio 并发/层间屏障/fail-fast/skill7 Gate 前驱闭包不跨/处理器注入不记账，env LOOM_ORCH_MAX_CONCURRENCY=4，纯代码无迁移；四接缝 02 C1.35 负责人四项全按甲拍板）已落地，Q92 M12 首片·两个驾驶舱只读聚合（新包 app/core/dashboards/：Token 成本天×模型×币种默认 30 天窗、failed 单列、多币种不换算；人工审核=候选/待办积压快照+窗口产出；platform_admin 只读实时 PG 聚合，管理面 GET 首次 query 携带 actor，无表无迁移；四接缝 02 C1.36 负责人四项全按甲拍板、Q93（M12 第二片统一审核工作台：app/core/workbench/ 五型候选风险四档归一队+置信度整批通过，WF Gate 角色并集，迁移 0021 纯配置种子）、Q94（M12 第三片候选审核 SLA 待办自动挂接：skill7/review_sla 投递建单/裁决 resolve，五型 review_* todo_type+review.sla_hours.* 五键种子 72h 待补，迁移 0022 纯配置种子）、Q95（M12 第四片租户管理+Onboarding：app/core/tenants/ 注册表四档 plan/三态 status、platform_admin 生命周期端点全审计、详情派生 Onboarding 进度、段1 准入未知 404/暂停 409 应用层无硬 FK，试用月额度 50 万付费档待补，迁移 0023 建表+存量 DISTINCT 回填）已落地**——M10 WF 替换切片四组完成、五个真 LLM 站点全部接通（Q67 站点 backlog 闭合）、Q71 restock_auto 补货闭环、入站 Key 凭证底座就位，下一步 M12 余量（两个驾驶舱只读 API〔Q92〕、统一审核工作台候选池+置信批量通过〔Q93〕、候选审核 SLA 待办自动挂接〔Q94/Q70②〕、租户管理+Onboarding〔Q95〕已落地（Q70 一期三件齐；五型时限具体数值仍待业务方补）；客户前端 Q96 i18n+Token 脚手架、Q97 应用外壳/D5 八菜单导航、Q98 产品中心功能页（我的产品列表/新建 draft/只读详情，跨栈补 GET /api/intakes 租户列表端点）、Q99 工作台功能页（录入单总览/15 态分组，跨栈补 GET /api/intakes/overview 聚合端点）、Q100 中台 CSV 导出（新包 app/core/exports/，GET /api/exports/fcw.csv 仅 final_id 单列同步流式，D4 CSV 契约收口、docs/09:87 6 层包口径分界已注）、Q101 合规风控只读功能页（跨栈：GET /api/compliance/overview 租户聚合——active frozen 一行一产品、各市场取最新报告、跨市场从严、法审单条只读；前端 (shell)/compliance 行展开明细，清洗/降级/裁决/词库写口不进客户页）已落（18 号文档 ✅ 门控解除），余五菜单功能页（content/templates/analytics/social-accounts/settings，下一菜单未点工、不自动开工）+管理端工作台/驾驶舱/租户/引导页面、CSV JSON 形态与异步导出（挂后续/V2）、付费档额度/计费订阅/团队账号（V2/待补），各块新决策需逐片点工）或 M10/V2 余量（真实 fan-out 随 DIM-SOURCE 七路连接器、段13 effect-callback 整切片 P3/V2、Streams/配置广播/fencing token），新任务仍需负责人点工）。
> 续工时**先读本文件**，再读 [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md)（场景导航 + 治理规则）。历史/规格细节一律以 docs 内文档为准，本文件只保留"当前状态 + 活跃任务 + 最近变更"。

## 项目一句话

Loom = 私域内容生产白名单平台（SaaS 后台）：把"产品信息 → 可发布内容"拆成 **13 段链（CHAIN_13）** 可编排生产链，AI 只产候选、人工 Gate 裁决、`final_id` 唯一出口、反馈回流反哺知识。三端：用户前端 / 管理后端（13 模块）/ 系统后台 API。

## 项目文档（完整清单，单一事实源）

> 完整清单与一句话描述**只以本节为准**；[docs/README](docs/README_文档地图与治理.md) 只做场景导航，不重复清单。

| 文档 | 一句话描述 | 状态 |
|---|---|---|
| [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md) | 文档地图（场景导航）+ 治理规则 + 信息保全映射表 | ✅ 权威 |
| [docs/01_PRD_产品需求规格.md](docs/01_PRD_产品需求规格.md) | 13 段链全量规格：各段功能/实体/规则/硬闸/数值约束 | ✅ 权威（Part A 全量） |
| [docs/02_决策记录_ADR_Q1-Q72.md](docs/02_决策记录_ADR_Q1-Q72.md) | 决策日志 Q1–Q72 + Q73（路线图合并）/Q74（18 字段双落库）/Q75（RBAC 端点角色收口）/Q76（skill7 通道接缝）/Q77（质量两件套形态）/Q78（WF-02 DIM-MERGE field_plan 候选）/Q79（WF-01 CAT-RECOG c1_recognition 候选 + intake 锚点；C7 Layer4 挂账）/Q80（WF-03 CONFLICT-PRECHECK atom_batch 整批候选，product_reviewer 准入）/Q81（WF-01 TYPE-MATCH c7_layer4 整 C7 解析请求，operations 准入，Q79-1 销账；真 LLM 生成挂 Q67）/Q82（Q67 首切片：模型底座+CAT-RECOG 真 LLM 试点；进程内同步复用 skill7 通道、outbound Key DB 加密+env 主密钥、Prompt DB 版本化，迁移 0014）/Q83（PWC-BUILDER 真 LLM 第二站：operations 显式端点、一次调用多候选、模型回带分剥离、跨候选保序去重，迁移 0015 纯种子）/Q84（C7 Layer4 TYPE-MATCH 真 LLM 第三站：operations 显式端点不自动串联、整结果单候选 c7_layer4、回显/脏提案 502、模型回带 confidence/score/source_route 剥离，迁移 0016 纯种子）/Q85（WF-02 DIM-MERGE 字段池方案真 LLM 第四站：operations 显式端点不自动串链、整方案单候选 field_plan、target 回显漂移/结构脏/line 14081 证据空白 502、confidence 契约透传而非剥离驱动 Q9/Q12、业务违规照落非合规池 Gate 拦截，迁移 0017 纯种子）/Q86（WF-03 原子补池真 LLM 第五站/末站：operations llm-expand 端点花 token 前预闸不自动串链、CONFLICT-PRECHECK chat+ATOM-AFFINITY embedding 双调用单 SkillRun 单 atom_batch 候选复用 Q80、模型自报亲和/成簇剥离本地按 1536 维向量余弦重算、ai_risk 契约透传词表只升不降，迁移 0018 带 schema：pgvector/capability/embedding 列/cluster_line 0.9 借 Q10 原文未给）/Q87（M8 restock_auto worker：进程内第二个 asyncio 循环默认 false opt-in + platform_admin 手工触发端点；requested 信号行永不 mutate、纯追加 succeeded/failed 的 llm_auto 子 run 反连接认领；仅自动消费 PWC-BUILDER 只产 pending_review 人工 Gate 不改；BudgetExhausted 区分预算瞬态与停用终态；无 Alembic 迁移头仍 0018）/Q88（入站一 Agent 一 Key 凭证半套：Q60b effect-callback 鉴权前置，迁移 0019 agent_api_keys，SHA-256 单向哈希明文仅签发返回一次、platform_admin 签发/列表/吊销、Bearer 401 依赖已备、Key 不绑租户；effect-callback 本体/effect 时序/孤儿队列/Q60a 回填随段13 P3 V2）/Q89（多副本单实例锁：Redis SET NX PX 两把循环级 leader 锁，env 默认关 fail-closed，手工 /run 同锁 409/503，无迁移）/Q90（restock 瞬态退避：restock_retry_state 游标表迁移 0020，指数退避封顶 30min，预算硬停永不转终态/上游错 10 次转终态，env 三参数，手工 /run 绕窗口）/Q91（编排并行 DAG 原语：app/core/orchestration/ needs 拓扑分层+同层 asyncio 并发+Gate 前驱闭包+fail-fast，env 信号量，无迁移）/Q92（M12 首片两个驾驶舱只读聚合：app/core/dashboards/ 成本天×模型×币种 30 天窗+审核积压快照/窗口产出，platform_admin、管理面 GET 首次 query actor，无表无迁移）/Q93（M12 第二片统一审核工作台：app/core/workbench/ 五型候选统一队列风险四档归一+置信度整批通过，迁移 0021 纯配置种子）/Q94（M12 第三片候选审核 SLA 挂接：skill7/review_sla 投递建单/裁决收口，五型 review_* todo_type，迁移 0022 五条时限种子）/Q95（M12 第四片租户管理+Onboarding：app/core/tenants/ 注册表+生命周期全审计+段1 准入+派生引导进度，迁移 0023 建表+存量回填）+ 46 配置化清单（Q95 无新配置键） | ✅ 权威（Part C 全量） |
| [docs/03_技术风险与红旗清单.md](docs/03_技术风险与红旗清单.md) | 红旗 S/A/B 三级（S 4 / A 11 / B 4）+ 裁决状态 + 业务方参考映射 | ✅ 权威（Part B 全量） |
| [docs/04_契约层_数据模型.md](docs/04_契约层_数据模型.md) | 实体与字段级数据模型（AI 落代码用） | ✅ 已提取 |
| [docs/05_契约层_API与状态机.md](docs/05_契约层_API与状态机.md) | API 契约 + 全部状态机定义 | ✅ 已提取 |
| [docs/06_契约层_WF与Skill协议.md](docs/06_契约层_WF与Skill协议.md) | Workflow / PT 协议 / Skill 注册表 / Agent 协议 | ✅ 已提取 |
| [docs/07_技术架构与工程规格.md](docs/07_技术架构与工程规格.md) | 模块边界 / 编排运行时 / 配置中心 / RBAC / 成本治理 / 工程建议 | ✅ 已提取 |
| [docs/08_迭代计划与任务包.md](docs/08_迭代计划与任务包.md) | 路线图（两套已裁决合并，Q73）+ V1/V2/V3 任务包（V1=段 1→6→10→11） | ✅ 已按合并裁决重排 |
| [docs/09_全景体系_展示口径.md](docs/09_全景体系_展示口径.md) | 展示口径全景：4 端/6 层/13 菜单/权限矩阵/中台/反馈 | ✅ 参考口径 |
| [docs/10_数据模型Schema_建表定义.md](docs/10_数据模型Schema_建表定义.md) | 35 实体建表要素（类型/约束/索引/关系） | 🟡 **待 DBA 复核** |
| [docs/11_API规范_OpenAPI.md](docs/11_API规范_OpenAPI.md) | effect-callback/白名单消费规格 + OpenAPI 通用规范 | 🟡 **待补接口待 M0** |
| [docs/12_PT协议补齐清单.md](docs/12_PT协议补齐清单.md) | 7 定稿 + 35 占位候选 + 补齐模板 | 🟡 **占位名单待 HTML 核对** |
| [docs/13_状态机迁移表.md](docs/13_状态机迁移表.md) | 11 组状态机迁移矩阵 | 🟡 **待补迁移行待 M0** |
| [docs/14_技术选型决策.md](docs/14_技术选型决策.md) | 6 项技术选型（模块化单体/自研编排器/PG+pgvector+Redis/DB 热更新/进程内合规模块/质量两件套） | ✅ **2026-09-13 已拍板定稿** |
| [docs/15_代码结构与脚手架.md](docs/15_代码结构与脚手架.md) | 目录/模块/可执行物落位 + 技术栈（FastAPI/Next.js/YAML） | 🟢 **已定稿（选型+技术栈已落）** |
| [docs/16_测试策略.md](docs/16_测试策略.md) | 分层测试 + 规则级测试清单 | 🟡 与 12 联动 |
| [docs/17_部署与运维.md](docs/17_部署与运维.md) | 环境/CI-CD/多租户/监控说明（含中间件版本） | 🟢 **已定稿（选型已落）** |
| [docs/18_前端i18n与设计Token方案.md](docs/18_前端i18n与设计Token方案.md) | 界面 i18n（next-intl，V1 zh-CN）+ 设计 Token 三层模型（CSS Modules/CSS 变量，色值借 AntD5）；与 Q58 内容多语言划界 | ✅ **Q96 已拍板定稿（2026-09-16，02 C1.40），§2.4/§3.6 可直接落码** |

> 根目录文档：README.md（项目介绍）/ AGENTS.md（AI 工作规范）/ CLAUDE.md（指向 AGENTS）/ handoff.md（本文件）/ git-commit-message.md（提交规范）/ CHANGELOG.md / CONTRIBUTING.md / MIGRATION_CONVENTION.md / BENCHMARK.md。

## 当前状态

- **阶段**：设计/文档阶段完成度高；代码骨架已搭（2026-09-13，按 15/17 定稿：backend FastAPI 13 模块包 + core 横切包 + Alembic + 分层 tests；runtime workflows/skills/agents/protocols 空目录；eval 两件套目录；middleplatform；frontend Next.js TS 最小页；infra docker-compose = PG16-pgvector/Redis7/MinIO）。**M1（段1 产品录入）+ M2（段2 C1 识别）+ M3（段3 字段池规划）+ M4（段4 字段下原子）+ M5（段5 PWC）+ M6（段6 PWS 冻结）+ M7（段10 合规清洗）后端切片已落地**：M1 productIntake15 + 录入 API + 迁移 0001；M2 信号权重/行业阈值配置、conf 三分支、ops 待办 72h、C7 四层兜底 + 迁移 0002；M3 来源路由表、方案提交/越界处置、WF-02 Gate、Q13 候选转正 + 迁移 0003；M4 Q48 统一词库、拓展批次/risk 双轨/三类冲突/approveAtomGuard、evidence 超时可复活、同义簇 alias、事实原子引用数=1、atom8 生命周期 + 迁移 0004；M5 contentGoals 字典、库容配置、WF-04 漏斗（预筛/Q48 合规检测/Q22 评分/限量 50）、Q23 疑重备选、HumanGate、Q71 按分消费+池健康、Q24 三元组去重/per-platform 冷却/全平台用尽、Q61 爆款手工标 + 迁移 0005；M6 pwsReadiness 5 门机械求值、全绿出 pws_ready 待办（BO-07 红线角色）、不可变快照物化（原子/PWC 成分行）、Q29 重冻三档、Q30 换版处置结构（实体随 M8/段12）、Q31 vN.0 版本管理、Q32 revoke 急停+作废后重冻新版、Q33 同租户指纹查重仅提示 + 迁移 0006；M7 同源 Q48 词库三关卡（layer 列承载 Q50 国家>平台>底座，同级 Q36 从严）、CCR 分市场清洗报告（block_required 一票否决）、降级建议人工 approval、Q49 敏感领域自动法审（48h 待办卡 Guard⑥）、Q51 词表生效即扫出 wordlist_rescan 强制重冻待办 + 迁移 0007；134 测试全绿（详见 08 §2.2 M1–M7 完成情况）；M11（段7/8 静态底表 + 段9 三包静态实例，实现序先于 M8——七 Guard 需要静态 PCP/三包可消费）发布位档案 CRUD（Q35，四维分明示人工评估/来源链接，14 平台目录原文待 line 1098-1221 基准 HTML 找回，不虚构种子）、Q34 目的权重矩阵（Σ=1 硬校验，fit_score 派生不落库/缺矩阵不凑分）、Q36 平台规则 4 层选择器（同级查重 409+overwrite 覆盖、match 高优先级覆盖同级从严、country 横切）、slotType 默认值（约 40 列明细待补）、Q39 四模板种子 + PCP 实例（Q40 17 键 Σ≤1.0、PT-PCP-V1.5 平台不共享唯一约束、Q42 人工直编通道）、Q45/Q52 三包 CSP/CSTP/CEP 静态实例（payload 定键、产品×平台×目的×包型一条 active、带 tenant/PS 供 Guard④⑤）+ 迁移 0008；147 测试全绿（详见 08 §2.2 M11 完成情况）；**M8（段11 FCW 组装/final_id 发证，Q73 V1 主链终点）后端切片已落地**：PT-FCW-ASM 七 Guard（PWS frozen/合规 clean 且 block_required=false【无报告不放行，实现补】/PCP+三包 active 且 gate=approved/六路 PS 一致 Q52/tenant 一致/法审条件放行 Q49/PWS active 版 Q53，不短路全量返回，任一败不产 final_id 且 409 回带明细）；E1.1 publishFCW 唯一出口（line 11036，全系统无第二写口，成功 fcw.issued/失败 fcw.assembly_blocked 全审计含发起人/时间/六路材料/Guard 结果）；Q54 临时评分 0.4/0.3/0.3（量纲统一 ×100，缺失 null+incomplete 不凑分、仅排序永不做门槛，待段13 校准）；Q55 任务驱动批量（product×platform×goal×count，自动配料/显式 slot_ids，单项失败隔离逐项留痕，任务同步跑完）+ 手动单条，operations 角色；同键去重 409，重冻旧版被 g1/g7 拦截；Q51 词表扫描回填 draft FCW（实体接入，V1 直接 published、draft 通道未开）+ 迁移 0009；165 测试全绿（详见 08 §2.2 M8 完成情况）；**M10 切片 a（配置中心底座）后端切片已落地**：`config_items` + `config_item_versions` 两表（迁移 0010，54 项 02 §C2 标量种子，typed JSONB/min·max·choices 校验/出处 Q 编号，uq(key,version)，种子即 v1），发布严格按 14 §2.4=新版本+writeAudit(config.update/rollback)+after_commit 进程内快照原子切换，回滚以新版本重发历史值、历史 append-only，`/api/admin/config` 五端点仅 platform_admin（403/404/422），176 测试全绿；**M10 切片 b（通用 SLA 引擎 + 定时调度）后端切片已落地**：`app/core/sla/`（引擎/作业/runner/调度器/路由）统一四作业（待办到期升级按类型分流审计/Q18 证据超时/Q24 冷却释放/Q51 未来生效词到点激活补扫），异常隔离；`POST /api/admin/sla/run` 手工触发、`GET /api/admin/sla/todos` 看板 sla_state（黄色仅法审 24h，热读 sla.yellow_hours）；lifespan 300s asyncio 循环；词表开放生效期录入+activated_at（迁移 0011）；183 测试全绿；**M10 切片 c（缓存引导 + 常量迁移）后端切片已落地**：新增 `knob(key)` 统一访问器（读 config_cache、未引导回落种子默认值），M1–M8 全部硬编码拍板值（c1 0.6/0.1/72h、c7 0.6、fieldpool 0.85/0.9/3-8/15-30、atom 20·50/0.5/7d、pwc 50/100·70·50/5min/100/0.6·0.4/0.5·0.5/1.0/0.8/7·3·14、pws 3·1/7d、ccr 48h、sla 黄 24h、fcw 0.4·0.3·0.3）迁为零参函数，lifespan 启动先 reload 缓存（失败告警不阻断），188 测试全绿、无新迁移；**M10 切片 d（RBAC 红线收口，Q75）后端切片已落地**：统一 `app/core/rbac`（角色常量/`require_any_role`/`PermissionDenied`→403，闸放服务层，系统内部调用不经 HTTP），手工 sweep（/sla/run、/admin/ops-todos/sweep）归 platform_admin、G1 类目+模板归 dictionary_admin、ccr/run 归 internal_compliance，并为信号权重/行业阈值（Q2/Q7）、来源路由（Q8）既有裁决补 operations 闸；pws/evaluate 与 atom revive 经裁决有意开放（Q75）；194 测试全绿、无新迁移；**M10 切片 e（skill7 AI 候选通道·WF-04 试点，Q76）后端切片已落地**：接缝五项经负责人拍板追加 Q76（02 C1.20）——范围=通道核心+WF-04 试点（`app/core/skill7/` + runtime WF-04/3 Skill YAML 注册表，未注册引用即拒）；外部投递边界（不建模型注册表/LLM/API Key，真接模型另立切片）；通用 `skill_candidates` + target_type 适配器（pwc_combo 复用 pwc/funnel，预筛/合规/评分/限量/Gate 不绕，ai_suggested 不持久化）；Q71 补货=requested run（source=restock_auto，5 分钟防抖，不造候选，consume 回带 restock_run_id）；投递 operations、裁决 product_reviewer（Gate 插槽角色驱动）；迁移 0012（skill_runs/skill_candidates，PG16 up/downgrade-1/up），203 测试全绿；**M10-Q（质量两件套轻量版，Q77）已落地（任务包闭合）**：`eval/runner.py` 独立回归 runner（target 适配器 `eval/targets.py`，YAML 案例，子集断言+容差+失败退出码 1），Evaluation Dataset 19 案 + Golden Cases 5 案（首批打 WF-04 确定性替身：PWC-SCORING Q22 公式、COMBO-VALIDATE Q48 词表；PWC-BUILDER 无实现不造案挂账），staging 闸门命令 `backend/.venv/bin/python eval/runner.py`（24/24），runner 3 条闸门单测进 backend；共 206 测试全绿、无迁移；**WF-02 字段池占位替换切片 1/3（Q78）已落地**：runtime WF-02 + FIELDPOOL-PLAN/DIM-SOURCE/DIM-MERGE 三 Skill YAML，DIM-MERGE 整方案单候选 field_plan，通道校验按 WF candidate_target 泛化（不再硬编码 pwc_combo），field_plan 适配器复用 fieldpool.submit_plan 落 pending_gate 走既有 WF-02 Gate，配套 DIM-MERGE eval 15 案（39/39）；**WF-01 冷启动识别占位替换切片 2/3（Q79）已落地**：runtime WF-01 + PARSE/UNDERSTAND/CAT-RECOG/TYPE-MATCH/MISSING-INFO/RISK-TAG 六 Skill YAML，CAT-RECOG 识别整结果单候选 c1_recognition（intake 锚点，Q79-4 迁移 0013：两表加 nullable intake_id、skill_candidates.product_space_id 改 nullable，PG16 up/downgrade-1/up 实测），Gate 角色 operations（Q79-3），适配器复用 modeling.submit_recognition（Q1 三分支机械逻辑一字不改，Q79-2），C7 Layer4 通道留挂账（Q79-1）；单候选约束泛化为 WF 步骤声明 single_candidate（Q78 field_plan 硬编码一并泛化）；配套 CAT-RECOG eval 14 案（合计 53/53）；**WF-03 字段下原子占位替换切片 3/3（Q80）已落地**：runtime WF-03 + ATOM-EXPAND/ATOM-CANON/ATOM-AFFINITY/CONFLICT-PRECHECK 四 Skill YAML（顺序四步，仅末步 CONFLICT-PRECHECK 挂 produces_candidates/candidate_target=atom_batch/single_candidate），四项接缝经负责人拍板追加 Q80（02 C1.24）——(1) 整批一条候选（payload=去 actor 的 BatchSubmitRequest 形态），适配器复用 atom.submit_batch 强制 source=ai，Q14/Q15/去重/维度/line 11189/Q17/line 840/空证据一项不绕，入库后逐条 approveAtomGuard/Q70/Q18/Q19/Q20 原样保留；(2) Gate 角色 product_reviewer（06 line 172）；(3) CONFLICT-PRECHECK 末步产出；(4) PS 锚点、无新迁移；router 补 404/409/422 atom 口径，适配器失败候选留 pending_review；配套四 Skill eval 31 案（Dataset 24 + Golden 7，7 个 target 覆盖 Q14/Q15/Q16/Q17/Q19/line 840），eval 合计 84/84；**WF-01 C7 Layer4 占位切片（Q81）已落地**：WF-01 增第二个候选产出步骤 TYPE-MATCH（c7_layer4 整 C7 解析请求单候选，intake 锚点）与第二道 operations Gate，四项接缝经负责人拍板追加 Q81（02 C1.25）——(1) TYPE-MATCH 产出、MISSING-INFO 仍非产出；(2) 整 C7ResolveRequest 单候选，适配器复用 modeling.resolve_c7（L1→L4/Q6/Q68 不绕，落 L1/2/3 提案不生效，L4 候选仍走 Q13 dictionary_admin）；(3) operations 准入，与 Q13 转正不合并；(4) eval 只打 c7 纯函数，Q68/错误分支留集成测试；Layer4 盲插顺手修为同名全局候选复用；配套 TYPE-MATCH eval 17 案（14+3，eval 合计 101/101）；现 238 测试全绿、ruff 通过、Alembic 0001–0013 无新迁移；M10 WF 替换切片四组完成（Q76 试点→Q78→Q79→Q80→Q81）；**Q67/Q82 模型网关+CAT-RECOG 真 LLM 试点切片已落地**：接缝四项经负责人拍板追加 Q82（02 C1.26）——(1) 范围=模型底座+CAT-RECOG 单站试点，其余四生成站点后续切片复用；(2) 业务端点进程内同步调模型网关，系统机器 Actor（system:llm-gateway，roles=[]，Q66）复用 skill7 投递通道落 pending_review，operations 人工 Gate 不改，异步 worker 留 M8；(3) outbound Key DB Fernet 对称加密 + env 主密钥 LOOM_MASTER_KEY（仅 env 不入库不入仓，注册表页轮换、明文不回显；入站一 Agent 一 Key 随 Q60）；(4) Prompt DB 版本化（skill_prompts/skill_prompt_versions，v0.1 起步 append-only、platform_admin+writeAudit，仓库只携迁移种子，不用 runtime prompt/*.md）；机械补登：场景键=skill_id、日预算按模型全局硬停不静默切供应商、fallback 仅注册态+人工、成本列 decimal+currency_code（币种【原文未给出，待补】）、仓内无真凭证测试用 synthetic 确定性替身；迁移 0014_model_gateway（ai_models/ai_model_keys/ai_scene_routes/skill_prompts/skill_prompt_versions 五表 + skill_runs 成本四列 + synthetic/CAT-RECOG 种子，PG16 up/downgrade-1/up 实测），`app/core/model_registry/` 九模块 + skill7 拆 deliver_generated_run（修 SkillRun.source 硬编码缺陷）+ 8 治理端点与 `POST /api/intakes/{id}/c1-recognition/llm-invoke`（403/404/409/422/502 齐）；253 测试全绿（+5 纯函数单测 +10 HTTP 集成）、ruff 通过；eval 不新增（仍 101/101，无确定性纯业务函数，16 §4 由集成测试覆盖）；**Q83 PWC-BUILDER 真 LLM 第二站切片已落地**：接缝四项经负责人拍板追加 Q83（02 C1.27）——(1) 仅 operations 显式端点 `POST /api/product-spaces/{id}/pwc/llm-build`，Q71 restock_auto 不自动消费（随 M8 worker）；(2) WF-04 PWC-BUILDER 未声明 single_candidate，一次调用产整批、每条组合各投一条 pwc_combo 候选，超 Q21 批 50 拒、同原子集跨候选编排层保序去重；(3) Prompt 机读变量白名单有界，前置闸=字段池 approved+approved 原子跨 ≥2 维（不足 409 不调模型），synthetic 只结构配对不造分，模型回带 logic/fit/weight 一律剥离，Q22b 子分缺失→score_incomplete→人工 Gate；(4) 预算/失败语义沿用 Q82；交付迁移 0015_pwc_builder_seed（纯种子：PWC-BUILDER 路由+Prompt v0.1，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/pwc_build.py`（第十模块）、synthetic 结构夹具，263 测试全绿（+2 单测 +8 集成）、ruff 通过、eval 仍 101/101；**Q84 C7 Layer4 TYPE-MATCH 真 LLM 第三站切片已落地**：接缝四项经负责人拍板追加 Q84（02 C1.28）——(1) 仅 operations 显式端点 `POST /api/intakes/{id}/c7/llm-resolve`，不与 CAT-RECOG llm-invoke 自动串联（链式/DAG V2）；(2) WF-01 TYPE-MATCH 已声明 single_candidate，整结果单候选 c7_layer4（payload=去 actor 的 C7ResolveRequest），复用 Q81 apply_c7_layer4→resolve_c7（L1/L2/L3 提案自然 no-op，仅 L4 写 g2_field_candidates）；(3) Prompt 变量有界白名单（类目/必填 fid/L1 approved 模板/L2 approved 兄弟计数/L3 active 覆盖+0.6 floor），前置闸=intake 存在+ai_recognizing+类目存在且 active+required_fids 过 c7.validate_fids（调模型前 422），synthetic 只按覆盖率缺口造结构提案不打置信分，模型回带 confidence/score/fid/status/source_route 一律剥离（只透传 field_name/非空 definition，confidence 落 NULL，source_route 枚举【原文未给出，待补】v0.1 不接模型值），category_id/required_fids 回显漂移、脏提案 502，空 l4_proposals 合法（裁决后落 L3），Q13 dictionary_admin 转正 Gate 不绕；(4) 预算/失败语义沿用 Q82/Q83；交付迁移 0016_type_match_seed（纯种子：TYPE-MATCH 路由+Prompt v0.1，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/c7_resolve.py`（第十一模块）、synthetic 覆盖率缺口夹具，276 测试全绿（+2 单测 +11 集成）、ruff 通过、eval 仍 101/101；**Q85 第四站 DIM-MERGE 字段池方案**：场景键=DIM-MERGE（WF-02 唯一 field_plan 产出步骤，FIELDPOOL-PLAN 不产候选、DIM-SOURCE 外部连接器属 V2），端点 `POST /api/product-spaces/{id}/field-pools/llm-plan`（触发体 `{target_atom_min?, target_atom_max?, actor}`，缺省 15/30），池态闸=无池或 rejected（否则 409，与 submit_plan 同口径），整方案单候选复用 Q78 apply_field_plan→submit_plan，target 回显漂移/role·route·fid 脏/证据空白 502，confidence 是 PT-FP-PLAN 契约 AI 字段（Q9/Q12 消费）故 0..1 透传而非剥离、synthetic 固定 0.9，业务违规（below_min/缺 product_attribute/敏感缺 risk_control）照落非合规 pending_gate 池、Gate approve 409；交付迁移 0017_dim_merge_seed（纯种子 3 行，7 变量，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/field_plan.py`（第十二模块）、synthetic 合规整方案夹具，289 测试全绿（+2 单测 +11 集成）、ruff 通过、eval 仍 101/101；**Q86 第五站/末站 CONFLICT-PRECHECK+ATOM-AFFINITY 原子补池**：端点 `POST /api/product-spaces/{id}/atom-batches/llm-expand`（触发体 `{batch_size?, actor}`，缺省 Q14 敏感 20/非敏感 50），花 token 前预闸 PS/池 404、gate≠approved 409、Q15 停拓（仅 AI 批次）409；双调用=CONFLICT-PRECHECK chat（单 atom_batch 候选复用 Q80 apply_atom_batch）+ ATOM-AFFINITY embedding（capability=embedding 不挂 Prompt 不产候选），合并单 SkillRun（input 求和）；模型自报 affinity/cluster_id/score 剥离，本地进程内余弦 max（无基 None）+批内同维并查集（atom.cluster_line=0.9 借 Q10 同义线原文未给【待补】），ai_risk 枚举校验透传、词表只升不降仍在 submit_batch；结构脏/向量行数维度不符 502 不留痕，业务违规不 502；交付迁移 0018_atom_affinity_embedding（首个带 schema 的 LLM 切片，pgvector 镜像 up/downgrade-1/up 实测）、`app/core/model_registry/atom_expand.py`（第十三模块）、gateway/drivers embed 通道、synthetic-embedding 夹具、portable Vector TypeDecorator，306 测试全绿（+4 相似度纯函数单测 +3 网关单测 +10 HTTP 集成）、ruff 通过、eval 仍 101/101；**Q87 M8 restock_auto worker（Q71 补货闭环，无迁移纯代码切片）已落地**：四接缝经负责人拍板追加 Q87（02 C1.31，均选推荐项）——(1) 形态=进程内第二个 asyncio 循环 RestockWorker（同构 SweepScheduler，V1 单进程；docs/07 §7.5 队列选型裁决 V1=进程内轮询，Redis Streams/多副本锁 V2），LOOM_RESTOCK_WORKER_ENABLED 默认 false opt-in（自动花真 token）/间隔 60s/每轮 ≤20（env 实现值非 Q9 旋钮），管理端 POST /api/admin/restock/run（platform_admin，同 Q75）；(2) 纯追加认领：requested/restock_auto 信号行永不 mutate，成功追加 succeeded/llm_auto 子 run（input.restock_request_id 回链，候选挂子 run），终态失败追加 failed/llm_auto 子 run（Q71 预留 failed 首次启用），认领=反连接子 run，瞬态不留子 run 下轮重试；(3) 范围仅 PWC-BUILDER（最早优先），只产 pending_review、product_reviewer Gate 一字不改，内部入口 invoke_pwc_build_for_restock（system:llm-gateway roles=[]，OPERATIONS 闸只留 HTTP /pwc/llm-build），全量预闸/白名单同 Q83，不自动串 ATOM-EXPAND；(4) 终态（PS 消失/池未批/not-ready/结构脏/ModelConfigError/停用无 fallback）failed 子 run+restock_failed(terminal)，瞬态（新增 BudgetExhausted(ModelUnavailable) 日预算硬停、GenerationUpstreamError）restock_deferred 下轮重试，HTTP 409 映射不变，不静默切供应商，V1 无重试退避；每信号独立会话/提交；新增 app/core/restock/（worker/router）、pwc_build 拆 HTTP 包装/内部入口/_run_pwc_build（Q83 手动路径零变化）、main lifespan 启停；315 测试全绿（+9 集成，无新单测）、ruff 通过、eval 仍 101/101 不新增；M10 余量：五个真 LLM 站点已全部接通（Q67 站点 backlog 闭合）、Q71 restock_auto 自动补货已闭环，余入站 Key（Q60 效果回调，建议下一棒）、编排并行/DAG（V2）、多副本广播/调度+补货锁与瞬态重试退避【挂账】；**Q88 入站一 Agent 一 Key 凭证半套（Q60b effect-callback 鉴权前置）已落地**：四接缝经负责人拍板追加 Q88（02 C1.32，均选推荐甲案）——(1) 范围只做 Key 半套：新表 agent_api_keys + platform_admin 治理端点 + Bearer 验签函数/依赖 + 审计，POST /api/effect-callback、effect_records 时序、孤儿队列、Q60a 运营回填、customer-backfill 全部随段13/P3（V2）不提前落，V1 无受保护业务端点为显式挂账（同 Q82 底座先行）；(2) SHA-256 单向哈希（明文 loom_+token_urlsafe(32) 仅签发响应返回一次，DB 存 hash unique+索引与前 12 字符展示前缀；刻意区别 outbound Fernet 可逆加密——入站只需比对，库泄露不暴露可用 Key），吊销=active/revoked 状态位+revoked_by/at，append-only 不物理删除；(3) Key 不绑租户（平台级凭证，未来归属由 content_id→FCW.tenant_id 解析；单租户绑定原文未给【待补】不臆造 tenant 列）；(4) 新横切包 app/core/api_keys/（models/service/schemas/router）与 model_registry 并列，路径 /api/admin/agent-keys（201 secret 仅一次/列表默认隐吊销且无 secret/吊销幂等 404/越权 403/空名 422），verify_key 未知·空·吊销统一 None 防探测、命中盖 last_used_at，require_agent_key 401 依赖留待段13，审计 agent_api_key.issue/revoke（tenant _platform）；交付迁移 0019_agent_api_keys（pg16 一次性容器 up/downgrade-1/up 实测，未触 atlas-pg）、332 测试全绿（+17 集成）、ruff 通过、eval 仍 101/101 不新增；**Q89 多副本单实例锁已落地（Q87 挂账“多副本单实例调度+补货锁”销账，env 默认关不改 V1 单副本形态，无迁移纯代码切片）**：四接缝经负责人拍板追加 Q89（02 C1.33，均选推荐甲案）——(1) Redis SET NX PX + uuid owner token + Lua 比对释放/续约 + 看门狗 TTL/3 续约，新横切包 app/core/locking/，崩溃靠 TTL 60s 放锁；(2) 循环级两把命名锁 loom:lock:sla-sweep / loom:lock:restock-worker，非持锁副本跳轮，不做每信号行细粒度认领（Q87 requested 行不 mutate 不冲击；并行消费留 V2）；(3) LOOM_DISTRIBUTED_LOCK_ENABLED 默认 false，开启后 Redis 故障 fail-closed 跳轮+error 日志，绝不无锁双跑；(4) 两个手工 /run 端点同锁，抢不到 409、Redis 故障 503，RBAC 403 在前；看门狗发现锁易主/续约失败仅 error 不打断在跑一轮（fencing token 退场挂账 V2）；345 测试全绿（+13 集成，内存 FakeRedis）、ruff 通过、eval 仍 101/101 不新增；**Q90 restock 瞬态失败重试退避已落地（Q87/Q89 挂账销账，迁移 0020）**：四接缝经负责人拍板追加 Q90（02 C1.34，均选推荐甲案）——(1) 重试态落新表 restock_retry_state 游标（request_id PK/attempts/next_attempt_at/last_reason，requested 信号行永不 mutate 不变、瞬态不留子 run 不变，成功/转终态删游标）；(2) 指数退避 60×2^(n−1) 封顶 1800s，BudgetExhausted 永不转终态（UTC 日界自愈）、GenerationUpstreamError 连续 10 次约 17h 转终态 failed 子 run+终态审计转人工并被反连接排除；(3) env 三参数（BASE 60/MAX 1800/MAX_ATTEMPTS 10）运维值非 Q9 旋钮；(4) 手工 /run honor_backoff=False 绕窗口但瞬态仍计数重排窗口；迁移 0020_restock_retry_state（pg16 一次性容器实测，未触 atlas-pg）、351 测试全绿（Q87 旧用例更新 +6 集成/纯函数）、ruff 通过、eval 仍 101/101；**Q91 编排并行 DAG 原语已落地（14 §2.2 复杂 DAG 二期原语部分销账，纯代码无迁移，头仍 0020）**：四接缝经负责人拍板追加 Q91（02 C1.35，均选推荐甲案）——(1) 只落原语不接真实流：新包 app/core/orchestration/（planner Kahn 分层+五类校验/engine 同层并发+屏障+fail-fast），needs 可选、无 needs 退化顺序链（WF-01..04 零改动），五站点显式端点与 Q84 不串链口径不变，DIM-SOURCE 七路 fan-out 随 V2 连接器接入；(2) 同层任一失败取消兄弟整轮失败、引擎不自动重试（重试归 Q90 一类调用侧）；(3) env 信号量 LOOM_ORCH_MAX_CONCURRENCY=4 运维值非 Q9 旋钮、预算硬停不绕；(4) skill7 Gate 前驱闭包不跨人工裁决、不建新表不写 skill_runs，处理器注入、orch_run_id+wf_id 经 NodeContext 关联；371 测试全绿（+20 单测 test_orchestration_dag.py）、ruff 通过、eval 仍 101/101；**Q92 M12 首片·两个驾驶舱只读聚合 API 已落地（Q73 收敛的 Token 成本+人工审核，02 C1.36 四接缝均选甲案，无迁移头仍 0020）**：新包 app/core/dashboards/（service 纯 SELECT+router），`GET /api/admin/dashboards/token-cost|review-workload` 仅 platform_admin、管理面 GET 首次 query 携带 actor（actor_id 必填 422/roles 重复，依赖判 403，真认证 V2 自报口径同写端点）；成本=默认近 30 天可选 from/to 半开 UTC，succeeded 且 model_id 非空按天×模型×币种+技能聚合（embedding Q86 起计入、价 0 计调用数），failed 单列不计费，多币种分组不换算（币种【待补】，单价归 Q67 不重算）；工作量=积压快照（pending_review 按 target_type+最老等待秒数，待办 open 未到期/overdue/escalated）+窗口产出（reviewed_at/resolved_at），Q70 派单/10% 抽检/6 场景细分二期不做；不建表/不物化/无调度器/无租户过滤（当时租户实体不存在；Q95 已落 tenants 注册表，Q33 误链已于 02 C1.39 勘误）；378 测试全绿（+7 集成 tests/integration/test_dashboards_api.py）、ruff 通过、eval 仍 101/101；下一步 M12 余量（候选 SLA 待办自动挂接 Q70②/租户 Onboarding/前端 8 菜单+工作台与驾驶舱页面受 18 号文档 🟡 门控/CSV 中台契约【待补】，工作台列表+批量（Q93）、候选 SLA 挂接（Q94）、租户管理+Onboarding（Q95，迁移 0023 tenants 建表+存量回填、段1 准入未知 404/暂停 409）已落地，余 CSV 导出/前端 8 菜单与页面（18 🟡 门控）/付费档额度·额度拦截·计费订阅·团队账号（V2/待补），各块需逐片点工）或 V2 真实 fan-out（DIM-SOURCE 七路连接器），段13 effect-callback 整切片随 P3/V2；Streams 细粒度认领/配置缓存广播/fencing token 随 V2；新任务仍需负责人点工。仓库**已 git init**（分支 `dev`，`main` 为主干）。
- **文档体系**：原唯一事实源 `Loom_核心业务主链梳理_v3.md` 已于 2026-09-13 归档删除，内容 **100% 迁入 docs/**（231 段段落级核验零丢失）；01–09 为 v3 忠实切分（Part A/B/C/D/E 全覆盖），10–17 为同日新增工程骨架文档，18 为同日新增前端 i18n/设计 Token 工程建议（🟡 待拍板）。
- **权威口径**：13 段链（CHAIN_13，01 PRD）为唯一权威；03-A~E 展示口径（09）并存不混用，冲突以 01 为准。
- **已定稿事实基线**：Q1–Q72 均已有决策记录（02，含正式采纳与"临时采纳"两类）；S 级红旗 4 条经 Q34/Q60 等解决（02 收官注、03 已标销账）。**M0 已于 2026-09-13 完成**：Q2/Q22/Q60(a/b/c) 转定稿（02 新增"✅定稿"状态与转正注记）；新裁 Q22a（分项打分口径）/Q22b（多样性函数 + AI 失败兜底），评分细则同步 01 段5、06 §1.1、§C2；业务方 3 份素材采纳经负责人确认（02 C1.16）；段 1-6 缺口实体 ProductSpace / g2FieldCandidates 已补字段（04/10）。挂账 4 子项（不阻塞 M1）：PS.lifecycle 迁移触发、~~18 通用字段值归属（M1 前定）~~ ✅ Q74 已裁决（两处落库 profile + profile_snapshot）、候选实体多租户可见性、候选状态枚举。**两套路线图口径冲突已于 2026-09-13 由负责人裁决合并（Q73，08 §1.3）：A7 为骨 + D9 厚度，段 12 后置 V2，V1 驾驶舱只留 Token 成本 + 人工审核 2 个；08 §2 已重排。至此原文挂起的待裁决事项全部清零。** 技术选型 6 项**已拍板定稿**（14，2026-09-13：模块化单体 / 自研注册表编排器 / PG+pgvector+Redis / DB+广播热更新 / 进程内合规模块 / Evaluation+Golden 两件套），技术栈 = FastAPI + Next.js + YAML 编排；下游 15/17/08/10/06 已同步刷新。
- **V1 范围（Q73 合并后权威）**：0–3 月 / 5–10 客户；主链段 1→6→10→**11**（到 `final_id` 发证闭环，**不含段 12**）；段 7/8 仅 FCW 必需的静态底表基础版（M11，无动态信号/fit_score 学习）；横切 skill7/writeAudit/RBAC/配置中心/SLA + 审核工作台 + 租户/Onboarding + 前端 8 菜单基础版 + CSV（M12）+ M10-Q 质量两件套 + 2 驾驶舱。段 12/13 与段 7/8/9 完整版在 V2。

## 最近进度（2026-09-14 ~ 2026-09-16）

- **Q101 M12 客户前端切片 4·合规风控只读功能页落地（跨栈无迁移；feat da644e9 / test df2c3b8；Alembic 头仍 0023、414 测试/101 eval、token 仍 86）**：四接缝全甲（02 C1.45）：(1) 只读总览+行展开明细——active frozen PWS 一行一产品，行内 CCR 四态 chip（blocked/downgrade_pending/approved/clean）+ block_required 阻断标记 + 法审三态 chip，展开逐市场最新报告（ban 命中词、`{word} → {target}` 降级建议）与法审 dl（领域/状态/结论/裁决时间）；清洗触发/降级批准/法审裁决/CP-LAW 词库 CRUD 不进客户页，intro 注明平台合规团队台内执行；(2) 新租户只读聚合端点 `GET /api/compliance/overview?tenant_id=`（service `overview_compliance`；tenant_id 必填非空 422；未知租户 200 空 items；不触发 Q95 准入门、不 writeAudit；产品名仍走 profile.product_name 临时键，无名回退产品空间 ID 前 8 位）；(3) 仅 active frozen 每 PWS 一行——报告 created_at,ccr_id 双 DESC 后每市场首见即最新（country=None=底座，排序在前），跨市场 blocked>downgrade_pending>approved>clean 从严、block_required OR；superseded/非 active/他租户干扰行实测不污染；从未清洗 ccr=null 显「尚未清洗」（不伪造 clean）；(4) 法审状态+领域+结论只读，未触发 null 显「未触发法审」，不派生 SLA 超时态。测试：+4 集成（ghost 空/聚合大考两市场+降级底座+双干扰行+法审/null+null/422，全套 414=410+4），ruff 干净，eval 不新增；check-compliance 零依赖守卫（compliance.*+ccr./law. 状态键、force-dynamic、词边界 getComplianceOverview、禁 NEXT_PUBLIC 与四类写/管台面、Query(min_length=1) 路由守卫；失败路径实测 exit 1）。端到端（/tmp 一次性 SQLite，不碰 Docker PG；uvicorn 8765+next start 3210+9222 Chrome）：t1 两行（修护精华 CN/US blocked+FDA+medical 待法审；洁净面霜底座 焕白→提亮）、干扰不现、展开完整、chip 计算色 danger rgb(207,19,34)/warning rgb(212,136,6)、侧栏 active=合规风控、浏览器零直连 :8000、控制台仅 favicon 既有 404；ghost 重启空态零行。M12 不闭合：余五菜单（前三项段8/12/13 V2 占位）、15 态迁移操作页、产品模板/资料库、管理端布局与 Q92/Q93/Q95 页、CSV JSON/异步导出等，下一点工前不自动开工。
- **Q100 M12 中台导出·FCW final_id 单列 CSV 落地（后端纯只读无迁移；feat 12884b7 / test e67c604；Alembic 头仍 0023、410 测试/101 eval）**：四接缝全甲（02 C1.44）：(1) 排期——CSV 导出独立切片先做，客户前端八菜单各自点工（下一切片预告合规风控，未开工）；(2) CSV 仅 final_id 单列含表头，守 M12「中台只消费 final_id」，不拼 6 层原料包——docs/09:87 的 6 层包属台内卡片详情/复制口径（09 已就地加注分界），docs/05 D4 行【待补】就 CSV 部分收口（JSON 形态仍挂后续）；(3) 同步 GET 流式 `GET /api/exports/fcw.csv`（新横切包 app/core/exports/；tenant_id 必填非空否则 422、product_space_id 可选收窄；text/csv; charset=utf-8 + attachment filename=fcw-<tenant>.csv；created_at DESC；不分页不异步，异步任务/对象存储挂 V2）；(4) 读路径纪律同 Q98/Q99——不触发 Q95 准入门、不 writeAudit，未知租户 200 仅表头；只导 published（draft 排除，Q32 revoked 落地后同过滤自然生效）。测试：+3 集成（ghost 仅表头+头断言、5 行播种验 published 过滤/严格单列/租户隔离/空间收窄、422，全套 410=407+3），ruff 干净，eval 不新增。curl 端到端（/tmp 一次性 SQLite，不碰 Docker PG）：t1 表头+3 条、ps-other 收窄与库内 final_id 一致、t2 隔离、ghost 仅表头、空 tenant 422，验收后进程与临时文件全清。M12 不闭合，下一菜单功能页等点工。
- **Q99 M12 客户前端切片 3·工作台功能页落地（跨栈无迁移；feat 43f4fcb/659d959 / test 1f05c27/a0a6ca3；Alembic 头仍 0023、407 测试/101 eval）**：四接缝全甲（02 C1.43）：(1) 跨栈最小总览——后端段1 新增 `GET /api/intakes/overview`（IntakeOverview{total,by_status}，group by 单次聚合；读路径不触发 Q95 准入门，未知/暂停租户零值；注册序先于 /{intake_id} 防吞），工作台 RSC（force-dynamic）落总数+15 态分组+快捷入口；(2) D5 原文五指标（今日生成/发布/互动/趋势/健康度，数据源段12/13）渲染禁用态 V2 卡，不显任何数字含 0；(3) `intake.status.*` 15 键对齐 docs/13 权威中文名（枚举码不翻译），products 列表/详情状态 chip 同切片中文化（docs/18 §7 英文码临时口径收口）；(4) 聚合独立端点不污染 IntakeList。测试：后端 +3 集成（零值/跨态分组+隔离/422，全套 407=404+3），check-workbench.mjs（含跨栈路由顺序守卫）、check-products.mjs 增裸码守卫，失败路径均实测。9222 Chrome 端到端（真实 4 单分组/五卡无数字/跳转/零直连/控制台零错）+ ghost 空态验收。菜单功能页继续逐片点工。
- **Q98 M12 客户前端切片 2·产品中心功能页落地（跨栈无迁移；feat 6693c18 / test a9f4c4a；Alembic 头仍 0023、404 测试/101 eval）**：四接缝全甲（02 C1.42）：(1) 跨栈最小闭环——后端段1 新增 `GET /api/intakes?tenant_id=&limit=&offset=`（IntakeList 分页，created_at DESC；读路径不触发 Q95 准入门，未知/暂停租户 200 空列表；tenant_id 必填 422、limit 1..100），前端 `(shell)/products/` 落我的产品 RSC 列表（force-dynamic）、新建产品（Server Action 建 draft）、`[intakeId]` 只读详情（404→notFound），templates/library V1 占位，不含 15 态迁移；(2) server-only env `LOOM_TENANT_ID` 固定当前租户（不做切换器，V2 改会话派生）；(3) `frontend/lib/api.ts` 唯一服务端访问层（server-only `LOOM_API_BASE_URL`，RSC/Action 代理，浏览器零直连、后端不加 CORS）；(4) 表单仅产品名，profile 工程临时键 `product_name` 显式挂账（G2 cat='common' fid 基线未回填，基线落地对齐），错误映射 error.403/404/409/422/unknown。测试原子：后端 +3 集成（空列表/分页不重不漏+租户隔离/422，全套 404=401+3），前端 `scripts/check-products.mjs`（消息键/文件/禁 NEXT_PUBLIC/指令边界，失败路径实测）。验收：FastAPI（/tmp 一次性 SQLite+t1）+ next start 生产模式，9222 Chrome 实测真实列表、CJK 新建→详情闭环、未知 id 404、占位 subnav、零直连 :8000、控制台零错。菜单功能页继续逐片点工。
- **Q97 M12 客户前端切片 1·应用外壳+D5 八菜单导航骨架落地（纯前端；feat 345e15e / test 09deafd；无后端变更，迁移头仍 0023、401 测试/101 eval 不变）**：(1) 路由组 `app/[locale]/(shell)/`——240px 侧栏（nav.ts 注册表 8 项：workbench/products/content/templates/analytics/social-accounts/compliance/settings）+64px 顶栏+1280 内容列，sidebar client 组件走 next-intl Link/usePathname（active 加 aria-current），8 页共享 menu-placeholder；(2) 接缝①八菜单全显，content/analytics/social-accounts（挂段12/13/8，Q73 V2）带“V2”徽标+V2 占位文案，其余五项 V1 占位；(3) 接缝③`/[locale]` 307→`/[locale]/workbench`，Q96 欢迎页与 home 命名空间删除，消息改 common/nav/shell/error；(4) 接缝④管理端（Q92/Q93/Q95 页面）未来 `/[locale]/admin/*` 独立布局本切片不预埋；(5) Semantic 新增 `--color-bg-hover`（86 token 两侧一致），check-tokens 仍绿；测试原子 `scripts/check-nav.mjs`（npm run check-nav，注册表↔消息↔页面三方漂移 exit 1，失败路径实测）；typecheck/build（8 页 SSG）、curl `/`→/zh-CN→/zh-CN/workbench 两级 307 与 8 路由 200、9222 Chrome 可视验收全部通过，控制台零错误。菜单真实功能页与管理端页面逐片点工。
- **Q96 前端 i18n+tokens.css 脚手架落地（M12 客户前端前置切片；feat 946a774 / test 8b614b6；无后端变更，迁移头仍 0023、401 测试/101 eval 不变）**：(1) **next-intl v4 三件套**——`frontend/i18n/routing.ts`（defineRouting 仅注册 zh-CN）、`i18n/request.ts`（hasLocale 兜底 defaultLocale、动态 import 消息 JSON）、`middleware.ts`（matcher 排除 api/_next/_vercel/带点文件；无前缀请求 **307**→/zh-CN 为 next-intl createMiddleware 默认实测，docs/18 原写 308 已更正）；`next.config.mjs` 接 createNextIntlPlugin("./i18n/request.ts")；页面全部在 `app/[locale]/`（layout：generateStaticParams/notFound/setRequestLocale/NextIntlClientProvider/import tokens.css；旧根 app/layout.tsx、app/page.tsx 删除）。(2) **消息表** `messages/zh-CN.json` 目前仅 common/home/error 三命名空间（error 含 403/404/409/422/unknown 兜底），M12 页面随片按 docs/18 §2.3 步骤 3 域扩展；枚举码/fid/审计码不进消息表。(3) **设计 Token**——`app/tokens.css` 唯一事实源（Primitive+Semantic 共 85 变量，AntD5 借值，prefers-reduced-motion 收口），`app/tokens.ts` 同值只读镜像；Semantic 比 §3.6 多一个 `--color-brand-bg: var(--color-brand-1)`（组件禁直引 Primitive，badge 需要语义出口，已补登 docs/18 §5）；CSS Modules 示范页只消费 Semantic/刻度。(4) **一致性校验** `scripts/check-tokens.mjs`（零依赖，多行值空白归一化后双向逐值比对，漂移 exit 1；npm run check-tokens；失败路径开发期实测过）。(5) **验收**：typecheck/build 通过（/zh-CN SSG），生产模式 curl `/`→307 Location `/zh-CN`、`/zh-CN`→200；chrome-devtools MCP 9222 独立 Chrome 可视验收通过（文案全走消息表、html lang=zh-CN、badge 计算背景 rgb(230,244,255)=#e6f4ff 经语义变量解析；控制台仅 favicon.ico 404，裸脚手架无图标非缺陷）。M12 客户前端菜单页面仍逐片点工，不自动开工。
- **Q96 前端 i18n+设计 Token 方案拍板定稿（纯文档裁决；18 号文档 🟡→✅，02 C1.40）**：四接缝全甲——①next-intl + `[locale]` 前缀路由（V1 仅 zh-CN，无前缀 307 实测——拍板时预期 308，以下条实现补登为准；V3 首外语工程默认 en-US），否掉自研字典；②CSS Modules + tokens.css 三层 Token（Primitive→Semantic→Component），否掉 Tailwind 与 AntD 组件库；③V1 仅 zh-CN、运营数据字典多语言延 V3 不预埋 name_i18n 列（Q58 内容多语言继续分治）；④色值缺口借 AntD5 默认调色板作工程初值（只抄值不装依赖：文本黑透明四阶、品牌蓝 #1677ff、四状态色 strong/bg 齐 AA），设计产出后只换 primitive。18 号文档补 §2.4 落码目录/步骤（middleware/i18n 三件套/zh-CN.json 命名空间/error.<code>）与 §3.6 全量 CSS 初值，可直接照做；docs/README 同步升 ✅。编号勘误：18 旧文“拍板后 Q75”作废（Q75 已用于 RBAC），本项=Q96。无代码/迁移/测试变化（401 测试、101 eval 不变）；深色/白牌/stylelint/W3C token 链/RTL/外语/TMS 仍 V3。M12 客户前端方案前置已就绪，实际开工逐片点工。
- **Q95 M12 第四片·租户管理+客户 Onboarding 落地（09 D3.11①③；四接缝经负责人拍板追加 Q95/02 C1.39，“四项全按推荐（甲）”；迁移 0023 tenants 建表+存量回填，Alembic 头 0022→0023）**：(1) **范围=注册表+生命周期端点+段1 准入门**——新横切包 `app/core/tenants/`（models/schemas/service/router，main 挂 `/api/admin/tenants`）；platform_admin 开通/列表/详情/改套餐（续费）/暂停/恢复全程审计 tenant.provisioned/plan_changed/paused/resumed；GET query actor（422/403 同 Q92 先例），写 actor 在体（403/404/409/422）；`POST /api/intakes` 写单前 `assert_intake_admitted`：未知租户 404、暂停租户 409，应用层闸不加硬外键，链内端点仍从 intake/PS 锚点派生租户；不含团队账号（Actor 无租户归属随真实认证 V2）与前端（18 号文档 🟡 门控）。(2) **plan 四档 trial/basic/pro/enterprise、status 三态 trial/active/paused**——试用开通 trial+月 token 额度 50 万（line 1466 唯一有值档位），付费档开通 active/额度 null，改套餐只动 plan 不动 status，暂停→paused/恢复→active；Basic $999/Pro $2999/Enterprise $9999 仅展示不计费不拦截；付费档额度与 §C2“5 档”疑义【原文未给出，待补】。(3) **Onboarding=派生进度、无状态机无引导表**——注册/套餐由开通覆盖，第一产品建模→AI 拆解→上线复用既有 intake→PS→13 段链，详情回带 `onboarding={intakes,product_spaces,first_modeling_started}`；团队设置随 V2 账号。(4) **0023 回填** product_intake_applications/product_spaces 的 DISTINCT tenant_id 并集为 basic/active+detail.backfilled+created_by=system-migration-0023，存量数据与既有测试零阻断。**勘误**：租户实体此前误链 Q33（Q33 实为同租户指纹查重仅提示），以 Q95 为准。**401 测试全绿**（391→401，+10 集成 tests/integration/test_tenants_api.py：RBAC/开通默认值/生命周期与准入/改套餐口径/零写入/派生进度/列表/回填行；7 个 HTTP 建单既有测试夹具补播 t1=basic/active）、ruff 通过、**eval 不新增（仍 101/101，注册表/生命周期/准入为管理与装配逻辑无确定性纯函数，同 Q87–Q94 口径）**。M12 未闭合：付费档额度/额度消耗拦截/计费订阅/团队账号（V2/待补）、CSV 中台导出（契约【待补】）、客户前端 8 菜单+工作台/驾驶舱/租户/引导页面（18 号文档 🟡 门控）；五型审核 SLA 时限具体数值待业务方补；Q70 二期派单/10% 抽检/6 场景细分随 V2。
- **Q94 M12 第三片·候选审核 SLA 待办自动挂接落地（Q70②；四接缝经负责人拍板追加 Q94/02 C1.38，“四项全按推荐（甲）”；迁移 0022 五条纯配置种子，Alembic 头 0021→0022）**：(1) **五型时限各一键**——`review.sla_hours.{pwc_combo,field_plan,c1_recognition,atom_batch,c7_layer4}` 种子统一 72h 且 source_ref 标 Q70②/待补（原文无小时数不编造，配置中心热更）。(2) **一候选一 todo、裁决即 resolve**——投递（人工 delivery 与 llm_auto 共用 _persist_delivery）flush 后同事务建 OpsTodo（entity_type=skill_candidate、assignee_role=注册表该 WF review_role、detail 带 wf_id/target_type/run_id/candidate_index、due=投递时刻+时限）；confirmed/modified→resolution=applied、rejected→archived；escalated 不阻断裁决；工作台批量复用 decide_candidate 自动收口；挂接前存量不回填、resolve 查无静默。(3) **五型五个 todo_type**——review_pwc_combo/…/review_c7_layer4，Q92 驾驶舱积压按 todo_type 自动分型（open/overdue/escalated），Q49 引擎升级审计统一 `skill7.review_sla_escalated`；黄色窗口原文未给→到期前 green 挂账；sweep/runner/SweepScheduler 零改动。(4) **新模块** `app/core/skill7/review_sla.py`（create_review_todos/resolve_review_todo）+ service 投递/裁决接线 + sla.engine 动作映射。**391 测试全绿**（384→391，+7 集成 tests/integration/test_skill7_review_sla_api.py：建单粒度/角色/72h/链接、多候选多条、reject/apply 收口、置过 due 升级审计+升级后裁决、批量收口、驾驶舱 overdue 分组；test_skill7_wf01_api 两例总数断言改按 ops_assist_category 类型计数）、ruff 通过、**eval 不新增（仍 101/101，写路径挂接+配置时限由集成端到端覆盖，同 Q89–Q93 口径）**。M12 未闭合：五型时限具体数值与黄色窗口待业务方补、租户管理+Onboarding（Q33）、CSV 中台导出（契约【待补】）、客户前端 8 菜单+工作台/驾驶舱页面（18 号文档 🟡 门控）；Q70 二期派单/10% 抽检/6 场景细分随 V2。
- **Q93 M12 第二片·统一审核工作台候选池落地（Q70 审核运营机制一期①③；四接缝经负责人拍板追加 Q93/02 C1.37，“四项全按推荐（甲）”；迁移 0021 纯配置种子，Alembic 头 0020→0021）**：(1) **范围=五型候选统一列表+置信批量通过**——新包 `app/core/workbench/`（risk/service/router/schemas，main 挂 `/api/review-workbench`）；Q70②候选投递自动挂 Q49 SLA 待办（建 todo/裁决 resolve/各型时限配置）留下一切片，智能派单/10% 抽检/6 场景细分维持二期。(2) **跨型风险四档归一**——critical/high/medium/low：atom_batch 取批内 items.ai_risk 最高档、c1_recognition 行业命中 c1_industry_thresholds.sensitive 归 critical（Q70“敏感行业/critical”）否则 low、pwc_combo/field_plan/c7_layer4 无信号归 low（risk_reason=no_risk_signal 不编造）；排序 risk_rank DESC, created_at ASC（同档等待最久优先），筛 state（默认 pending_review）/target_type 可重复/wf_id/risk_level + limit(50,≤200)/offset，每条回带 confidence/risk_level/risk_rank/risk_reason/wait_seconds/batch_eligible。(3) **可见角色=WF skill7 Gate 角色并集（数据驱动）**——注册表动态取（当前 operations+product_reviewer，YAML 加角色自动生效），platform_admin 非裁决角色 403；GET query actor_id 必填（422）/roles 可重复/依赖判 403（同 Q92 管理面先例）；批量再逐条按所属 WF Gate 角色校验，混角色批整批 403。(4) **批量门槛=新配置键+整批成败一致**——`review.batch_pass_confidence=0.85`（category=review，Q70/Q9，不字面挪用 fieldpool.needs_detail_conf 命名空间）；仅 pending_review 且 run.confidence 严格 >线且 risk_rank<high 入批（high/critical 含敏感行业禁批量，承接 Q80；confidence 缺失/≤线逐条）；预检 404→409→403→422 全部零写入，过后单事务复用 skill7 decide_candidate(confirmed)，适配器规则（Q14/Q15/查重/词表/Q18 等）一项不绕、任一失败整批回滚，modified/rejected 仍走逐条端点；补 workbench.batch_approved 汇总审计。交付 `GET /api/review-workbench/candidates`、`POST /api/review-workbench/batch-approve`（只允许 confirmed 原样通过，返回 approved 全视图+count）。**384 测试全绿**（378→384，+6 集成 tests/integration/test_review_workbench_api.py：RBAC 含 platform_admin 403、风险排序与资格旗标、筛选分页、预检 422/403/404/409 零写入、真实两 atom_batch 批量通过落 4 条 AtomCandidate）、ruff 通过、**eval 不新增（仍 101/101，风险归一机械映射经 DB 行业表/run join 由集成端到端覆盖，同 Q89–Q92 口径）**。M12 未闭合：候选 SLA 挂接（下一片需新接缝）、租户管理+Onboarding（Q33）、CSV 中台导出（契约【待补】）、客户前端 8 菜单+工作台/驾驶舱页面（18 号文档 🟡 门控）。
- **Q92 M12 首片·两个驾驶舱只读聚合 API 落地（Q73“V1 驾驶舱收敛为 Token 成本+人工审核 2 个”首个落地块；四接缝经负责人拍板追加 Q92/02 C1.36，“四项全按推荐（甲）”；纯只读无迁移，Alembic 头仍 0020）**：(1) **M12 首片只做两驾驶舱只读 API**——统一审核工作台候选池（Q70 统一审核列表跨表形态待提案）、租户管理+Onboarding（无租户实体表，Q33 隔离待裁决）、客户前端 8 菜单（18 号文档 🟡 门控）、CSV 中台导出（契约“中台手动用【待补】”）均留后续切片。(2) **Token 成本**：`GET /api/admin/dashboards/token-cost?date_from&date_to`，默认近 30 天（today−29…today）、半开 UTC `[from 00:00,to+1d 00:00)`、from>to/坏日期 422；计费行=skill_runs succeeded 且 model_id 非空（Q86 embedding 运行计入，合成 embedding 价 0 计调用数与 token），按天×model_id×currency_code 聚合 runs/input·output_tokens/input·output/total_cost，另附 by_skill；failed 不计费按 skill 单列 failed_by_skill；requested 补货行无 model 排除；多币种**分组不换算**（币种原文【待补】沿用 Q82；per-1M 单价归 Q67，本视图只消费已落实际成本）。(3) **人工审核工作量=积压快照+窗口产出**：`GET /api/admin/dashboards/review-workload`——backlog 实时快照不受窗参影响：pending_review 候选按 target_type 计数+oldest_wait_seconds（SQLite 回读 naive 按 UTC 归一），ops_todos 按 todo_type 拆 open(未到期)/overdue(open 且 due_at 过)/escalated，totals 四类合计；window_output=窗内候选 reviewed_at 按 state(applied/archived)、待办 resolved 且 resolved_at 按 todo_type 计数；snapshot_at UTC ISO8601；空库零值空列表；Q70 二期项（智能派单/10% 抽检/6 场景细分）明确不做。(4) **platform_admin 只读+实时 PG 聚合**——管理面 GET 首次携带 actor（query actor_id 必填缺省 FastAPI 422、roles 可重复，require_admin_view 依赖把 PermissionDenied 映射 403；真实会话/JWT 中间件随 V2，V1 actor 请求自报同写端点）；对 append-only 三表只 SELECT，不建新表/不物化/无调度器，无租户过滤参数（不臆造 tenant 维度）。交付：新包 `app/core/dashboards/{__init__,service,router}.py` + main 挂载；**378 测试全绿**（新文件 tests/integration/test_dashboards_api.py +7 集成：RBAC 422/403×2、默认窗天×模型×技能聚合与 requested/failed/窗外排除、零成本 embedding 计 token、多币种不换算、坏窗/坏日期 422、积压快照+窗口产出、空库零值）、ruff 通过、**eval 不新增（仍 101/101，纯只读 SQL 聚合/HTTP RBAC 无确定性业务纯函数，同 Q89–Q91 口径）**。M12 未闭合：审核工作台、租户+Onboarding、CSV、客户前端与驾驶舱页面（18 🟡 门控）待续。详见 08 §2.2 Q92 完成情况。
- **Q91 注册表驱动编排并行 DAG 原语落地（14 §2.2“复杂 DAG 二期”原语部分销账；四接缝经负责人拍板追加 Q91/02 C1.35，“四项全按推荐（甲）”；纯代码无迁移，Alembic 头仍 0020）**：(1) **只落原语不接真实流**——新包 `app/core/orchestration/`（planner.py 纯规划 + engine.py 异步执行）；YAML 步骤新增可选 `needs: [skill_id,...]`，全部不声明时退化为声明序顺序链（WF-01..04 零改动可规划）；Kahn 拓扑分层（同层并发/层间屏障），加载期五类校验 DagValidationError（未注册 skill 引用/未知 needs/依赖环/重复步骤/gate after 锚点缺失）；五个真 LLM 站点显式端点、Q84 CAT-RECOG→TYPE-MATCH 不自动串链口径一字不改，DIM-SOURCE 七路 fan-out 连接器 V2 来时声明 needs 即接入。(2) **同层 fail-fast、层间屏障、引擎不重试**——任一节点抛错取消同层兄弟、整轮 NodeExecutionError（携带失败节点/异常类名/此前已完成节点）、后层不执行；异常分类沿用处理器既有口径，重试/退避归调用侧（如 Q90 游标），不双层烧 token。(3) **env 信号量 `LOOM_ORCH_MAX_CONCURRENCY` 默认 4**（同 Q87/Q89/Q90 一档运维参数，非 Q9 C2 旋钮），日预算硬停仍由 gateway 全局闸门兜底。(4) **skill7 Gate 不跨、不建新表**——只规划第一道 channel=skill7 gate 的 after 步骤+前驱闭包（WF-01 止于 CAT-RECOG、第二道 gate 的 TYPE-MATCH 不进本轮；WF-04 仅 PWC-BUILDER；WF-03 至 CONFLICT-PRECHECK），候选投递即止；引擎不调模型/不投递/不写 skill_runs，节点处理器按 skill_id 注入，NodeContext 携带 orch_run_id(uuid hex)/wf_id/results 前驱返回值，真实接入切片各自经 skill7 通道记账（orch_run_id 进 run.input）。交付：`app/core/orchestration/{__init__,planner,engine}.py` + config `orch_max_concurrency=4`；**371 测试全绿**（新文件 tests/unit/test_orchestration_dag.py +20：规划 12 例含四既有 WF 快照参数化、执行 8 例含并发上限实测=2/屏障时序/兄弟取消/HandlerMissing/run_id）、ruff 通过、**eval 不新增（仍 101/101）**。挂账：Redis Streams/每信号行细粒度认领、配置缓存多副本广播、fencing token、DIM-SOURCE 七路采集连接器（首个真实 fan-out 接入方）；LangGraph 迁移回滚口径不变（14 §2.2）。详见 08 §2.2 Q91 完成情况。
- **Q90 restock 瞬态失败重试退避落地（Q87/Q89 挂账“瞬态失败重试退避”销账；四接缝经负责人拍板追加 Q90/02 C1.34，“四项全按推荐（甲）”；迁移 0020）**：(1) **状态=新表 upsert 游标 `restock_retry_state`**（新包内文件 app/core/restock/models.py：request_id String36 PK=信号行 run_id/attempts/next_attempt_at timestamptz 索引/last_reason String64/updated_at）——Q87 requested 信号行 append-only 永不 mutate、瞬态不留子 run 两条纪律一字不改，可变重试态只落侧表；信号成功或转终态删除游标行（审计 restock_deferred/restock_failed 在 audit_logs 不受影响）；(2) **指数退避 base×2^(attempts−1) 封顶 1800s**（60/120/240/480/960/1800…），定时轮 honor_backoff=True 跳过 next_attempt_at 未到的信号；**BudgetExhausted 永不转终态**（UTC 日界预算必然恢复，转终态反而永久漏补，无限按封顶间隔续试），**GenerationUpstreamError 连续累计 10 次（约 17h）转终态**——追加 failed/llm_auto 子 run + audit restock_failed(terminal=true, attempts=10) 供人工介入，删游标，反连接排除不再认领；(3) **env 运维参数非 Q9 C2 旋钮**——`LOOM_RESTOCK_BACKOFF_BASE_SECONDS=60`/`_MAX_SECONDS=1800`/`_MAX_ATTEMPTS=10`（与 Q87 interval/batch、Q89 锁开关同档）；(4) **手工 `POST /api/admin/restock/run` honor_backoff=False 绕过退避窗口**（platform_admin 显式“立即试一次”，409/503 锁语义 Q89 不变），但瞬态结果仍 attempts+1 并重排窗口、成功清游标——绕过的是等待不是计数。交付：迁移 **0020_restock_retry_state**（down=0019；pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up + psql 表结构核对，未触本地 atlas-pg）、worker 退避纯函数/游标 upsert/升级分流/成功终态清理、router honor_backoff=False；**351 测试全绿**（Q87 预算瞬态旧用例按新行为更新 +6：退避序列与封顶、窗口到期重认领、预算 11 连试不转终态、上游 DriverError 第 10 次转终态且删游标/反连接排除、成功清游标、终态清已存在游标）、ruff 通过、**eval 不新增（仍 101/101）**。挂账：Redis Streams/每信号行细粒度认领、配置中心多副本缓存广播、fencing token、编排并行/DAG（均 V2）。详见 08 §2.2 Q90 完成情况。
- **Q89 多副本单实例锁落地（Q87 挂账“多副本单实例调度+补货锁”销账；四接缝经负责人拍板追加 Q89/02 C1.33，“四项全按推荐（甲）”；无迁移纯代码）**：(1) **机制=Redis SET NX PX 分布式锁**——新横切包 `app/core/locking/`（service.py：`leader_lock` 异步上下文管理器 + LockUnavailable/LockBackendError），uuid4 owner token，释放/续约均 Lua 比对 token（TTL 到期易主不误删），看门狗每 TTL/3=20s 续约，进程崩溃 TTL 60s 自动放锁；(2) **粒度=循环级两把命名锁** `loom:lock:sla-sweep`、`loom:lock:restock-worker`，SweepScheduler/RestockWorker 各抽 `_tick()` 抢锁，持锁副本才跑、抢不到跳过；不做每信号行细粒度认领（Q87 append-only 认领一字不改，水平并行消费/Redis Streams 留 V2）；(3) **`LOOM_DISTRIBUTED_LOCK_ENABLED` 默认 false + fail-closed**——单副本/本地无 Redis 依赖、行为与 Q87 一致，开启后 Redis 故障/超时跳过本轮+error 日志，绝不降级无锁双跑（并发窗口双跑 restock 会重复花真 token）；(4) **两个手工端点同锁**——`POST /api/admin/sla/run`、`POST /api/admin/restock/run` 抢锁失败 409（already running，RBAC 403 判定顺序在前）、Redis 故障 503。看门狗发现锁易主/续约失败只记 error 退出续约、不打断进行中一轮（fencing token 强制退场如实挂账 V2）。交付：无 Alembic 迁移（头仍 0019）、345 测试全绿（+13 集成：内存 FakeRedis——门控关闭放行/NX 互斥重入/异 token 不删/看门狗续约/锁丢失静默/获取故障 fail-closed；两循环三态；两端点 200→409、503×2）、ruff 通过、**eval 不新增（仍 101/101）**。挂账：瞬态失败重试退避、Redis Streams/每信号行细粒度认领、配置中心多副本缓存广播、fencing token、编排并行/DAG（均 V2）。详见 08 §2.2 Q89 完成情况。
- **Q88 入站一 Agent 一 Key 凭证半套落地（Q60b effect-callback 鉴权前置；四接缝经负责人拍板追加 Q88/02 C1.32，“四项全按推荐（甲）”；迁移 0019）**：(1) **范围=只做 Key 半套**——新表 + platform_admin 治理端点（签发/列表/吊销）+ Bearer 验签函数/依赖 + 审计；POST /api/effect-callback、effect_records 时序（content_id+captured_at 幂等、缺席指标“—”不当 0）、孤儿队列人工认领、Q60a content_id↔platform_post_id 运营回填、customer-backfill 全部随段13/P3（V2）不提前落；V1 无受 Key 保护业务端点是显式已知挂账（同 Q82 底座先行），不建占位回调端点；(2) **SHA-256 单向哈希**——明文 `loom_`+secrets.token_urlsafe(32) 仅签发响应返回一次，DB 存 sha256 hex（unique+索引）与前 12 字符展示前缀，与 outbound Fernet 可逆加密刻意分域（入站只需比对，库泄露不暴露可用 Key）；吊销=active/revoked 状态位+revoked_by/revoked_at，append-only 不物理删除；(3) **Key 不绑租户**——平台级凭证 platform_admin 签发，未来回调租户归属由 content_id 解析 FCW.tenant_id（对不上进孤儿队列），单租户绑定口径原文未给【待补】不臆造 tenant 列；(4) **新横切包 `app/core/api_keys/`**（models/service/schemas/router，与 model_registry 并列），`POST/GET /api/admin/agent-keys`、`POST /api/admin/agent-keys/{id}/revoke`（403/422/404 齐；列表默认仅 active、永不含 secret），parse_bearer 严格解析、verify_key 未知/空/吊销统一 None 防探测侧信道、命中盖 last_used_at，require_agent_key 401 依赖已备待段13 复用；审计 agent_api_key.issue/revoke（tenant_id=`_platform`）。交付：迁移 **0019_agent_api_keys**（pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触本地 atlas-pg）、332 测试全绿（+17 集成）、ruff 通过、**eval 不新增（仍 101/101）**。挂账：effect-callback 端点与段13 回流整切片（P3/V2，鉴权半套已就绪）、Key 单租户绑定口径【待补】、编排并行/DAG V2、多副本 Redis 广播/单实例调度+补货锁（含瞬态退避）、M12、DIM-SOURCE 第七路/source_route L4/成本币种【待补】、真 embedding 维度演进、两个基线 HTML 缺块。详见 08 §2.2 Q88 完成情况。
- **Q87 M8 restock_auto worker 落地（Q71 补货闭环，四接缝经负责人拍板追加 Q87/02 C1.31，均选推荐项；无迁移纯代码切片）**：(1) **形态=进程内第二个 asyncio 循环** `RestockWorker`（同构 SweepScheduler，task 名 loom-restock-worker，V1 单进程；**docs/07 §7.5 队列选型就此裁决 V1=进程内轮询**，Redis Streams/多副本锁后置 V2）；`LOOM_RESTOCK_WORKER_ENABLED` **默认 false 部署 opt-in**（自动花真 token）、间隔 60s、每轮 ≤20（env 实现值非 Q9 旋钮），管理端 `POST /api/admin/restock/run`（platform_admin，同 Q75 sla/run）；(2) **纯追加认领、append-only 不放松**：requested/restock_auto 信号行永不 mutate——成功追加 succeeded/llm_auto 子 run（`input.restock_request_id` 回链，候选挂子 run）、终态失败追加 failed/llm_auto 子 run（Q71 预留 failed 首次启用），认领=反连接已有子 run，瞬态不留子 run 下轮重试；`?status=requested` 持续含已消费信号行（join 子 run 判断处理状态）；(3) **范围仅 PWC-BUILDER**（唯一 requested 写者，最早优先），只产 pending_review 候选、product_reviewer Gate 一字不改；内部入口 invoke_pwc_build_for_restock（system:llm-gateway roles=[]，OPERATIONS 角色闸只在 HTTP /pwc/llm-build 保留），全量预闸/白名单同 Q83，不自动串 ATOM-EXPAND（跨维 <2 终态失败转人工 llm-expand），一次信号一次调用（≤Q21 cap 50）；(4) **失败/预算分类**：终态=PS 消失/池未批/PwcBuildNotReady/结构脏 PwcBuildOutputInvalid/ModelConfigError 缺路由·Prompt·Key·能力不符/停用无 fallback → failed 子 run + audit skill7.restock_failed(terminal)；瞬态=日预算硬停（新增 BudgetExhausted(ModelUnavailable) 子类区分，HTTP 409 映射不变）/上游 GenerationUpstreamError → audit restock_deferred、下轮重试；成功 audit restock_delivered；不静默切供应商、fallback 仅注册表已注册；V1 不设重试退避（挂账 V2）；每信号独立会话/提交互不污染。交付：`app/core/restock/`（worker.py run_restock+RestockWorker、router.py）、pwc_build 拆 HTTP 包装/内部入口/_run_pwc_build（Q83 手动路径零变化）、gateway BudgetExhausted、config 三 env、main lifespan 启停；315 测试全绿（+9 集成：成功交付+信号行不动+反连接幂等、非 restock/他 skill 信号忽略、四类终态 failed 子 run、预算瞬态无子 run 可重试、admin RBAC、worker 生命周期）、ruff 通过；**eval 不新增（仍 101/101）**。挂账：入站一 Agent 一 Key（Q60 效果回调）；编排并行/DAG（V2）；多副本 Redis 广播/单实例调度+补货锁（含瞬态重试退避）；M12 平台厚度；DIM-SOURCE 第七路/source_route L4/成本币种枚举【原文未给出，待补】；真 embedding 模型维度演进；两个基线 HTML 缺块。详见 08 §2.2 Q87 完成情况。
- **Q86 WF-03 原子批量补池真 LLM 第五站/末站落地（chat + embedding 双调用，Q82–Q85 模式复用；五站全通）**：接缝四项经负责人拍板追加 Q86（02 C1.30，均选推荐项）——(1) **仅显式业务端点、operations 角色、花 token 前全量预闸**：`POST /api/product-spaces/{id}/atom-batches/llm-expand`（触发体 `{batch_size?, actor}`，缺省 Q14 敏感 20/非敏感 50），PS 404/字段池 404（与手动批次同口径）/gate≠approved 409/已通过+冻结正式原子 ≥ target_atom_max 409 TargetReached（Q15 停拓仅约束 AI 批次，手动追加端点不变），restock_auto 不自动串链（M8 worker）；(2) **两次模型调用、一个 atom_batch 候选**：CONFLICT-PRECHECK chat（WF-03 唯一 `candidate_target: atom_batch, single_candidate: true` 步骤，Prompt 挂此 skill，6 模板变量 + 2 合成替身机读键）产整批，ATOM-AFFINITY embedding（**首个 capability=embedding 场景：不挂 Prompt、不产候选**，网关新 `embed(scene, texts)` 通道，能力不符 422）向量化 item 文本+同维 approved/frozen 有向量基文本；只落 1 条 SkillRun（model_id=chat 模型，两次 input tokens/cost 求和、output 仅 chat，embedding 模型身份记 run.input 的 embedding_scene/embedding_model_id）；整批经 Q80 apply_atom_batch→submit_batch（强制 source=ai，Q14/Q15/批内 normalized 去重/维度必选/line 11189 全局事实唯一/Q17 词表双轨/line 840/Q18 缺证降级一项不绕，有证 pending_review/无证 pending_evidence），1536 维向量以机读 `_embeddings` 键随候选 payload 带外传递（skill7 校验与适配器双侧 pop，不进 BatchSubmitRequest/run.output/SkillCandidate 之外），approve 时候选 embedding 复制到 product_atom_instances，逐条 approveAtomGuard/Q70/Q18/Q19/Q20 Gate 原样；(3) **Q16 相似度本体 V1 口径**：迁移 0018 为首个带 schema 的 LLM 切片（PG `CREATE EXTENSION vector`/ai_models.capability chat|embedding/atom_candidates 与 product_atom_instances 两列 nullable vector(1536)/SQLite float32 LargeBinary portable TypeDecorator）；V1 相似度=进程内纯 Python 余弦（无 KNN 索引无 `<=>`，千万级切换按 14 §2.3 后置），affinity=同维有向量 approved/frozen 原子 max（6 位小数；无基含 Q86 前历史 NULL 向量原子→None 不伪造 0，另记 approved_atom_count/approved_embedding_count，历史行不 backfill），cluster_id=批内同维并查集（余弦 ≥ `atom.cluster_line` 才连边、仅 ≥2 成员成分发 uuid4、跨维不成簇）；**成簇线 0.9 为本次拍板借 Q10 维度同义线（原文未给【待补】，进配置中心可后台调）**，Q16 low_affinity_line=0.5 仍只标不淘汰；synthetic embedding=blake2b 哈希字袋 1536 维单位向量（同文本必同向量）；(4) **输出契约**：白名单 `{batch_size 必须原样回传, items:[{content, dimension_id, ai_risk, evidence?, fact_type?}]}`；模型自报 affinity/cluster_id/score/status 一律剥离本地重算；**ai_risk 与前四站"模型分全剥"不同——PT-ATOM-EXP/Q17 契约内 AI 字段，枚举（critical/high/medium/low）校验通过即透传作兜底档，词表命中仍由 submit_batch 强制只升不降**；结构脏数据（非 JSON/回显漂移/items 空或超批/空白 content/批内 normalized 重复/维度非本池 selected/fact_type·ai_risk 枚举越界）与 embedding 行数不符/维度≠1536 在投递前 502、不留 run/候选；业务违规（line 11189 冲突/事实重复）不 502，照由 submit_batch 既有 409/422 处理、适配器失败回滚留 pending_review；预算/失败沿用 Q82–Q85（日预算按模型全局硬停 409 含 chat 成功后 embedding 触顶、disabled 仅走已注册 fallback、非合成缺 active Key 422、上游坏 502、系统 Actor `system:llm-gateway` roles=[] Q66、source=llm_auto、成本四列照记）。交付：迁移 **0018_atom_affinity_embedding**（pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触本地 atlas-pg；cluster_line 种子用 on_conflict_do_nothing 兼容 0010 CONFIG_SEEDS 快照全新库）、`app/core/model_registry/atom_expand.py`（第十三模块）、gateway/drivers `embed` 通道（per-1M input-only 成本）、第二个合成模型 synthetic-embedding、atom_rules cosine_similarity/max_affinity/assign_clusters 纯函数、portable Vector TypeDecorator、atom router 新端点（201；403/404/409/422/502 齐）；306 测试全绿（+4 相似度纯函数单测 +3 网关/构造器单测 +10 HTTP 集成）、ruff 通过；**eval 不新增（仍 101/101）**。**M10 五个真 LLM 站点全部接通，Q67 站点 backlog 闭合**。挂账：restock_auto 自动触发（M8 worker）；入站一 Agent 一 Key（Q60）；编排并行/DAG（V2）；多副本 Redis 广播/单实例调度+补货锁；DIM-SOURCE 第七路/source_route L4/成本币种枚举【原文未给出，待补】；真 embedding 模型接入时维度按模型实际演进。详见 08 §2.2 Q86 完成情况。
- **Q85 WF-02 DIM-MERGE 字段池方案真 LLM 第四站落地（Q82–Q84 模式复用）**：接缝四项经负责人拍板追加 Q85（02 C1.29，均选推荐项）——(1) **仅显式业务端点、operations 角色**：`POST /api/product-spaces/{id}/field-pools/llm-plan`（触发体 `{target_atom_min?, target_atom_max?, actor}`，缺省 Q15 旋钮 15/30），不自动串链（链式/DAG 后置 V2）；场景键=DIM-MERGE（WF-02 唯一 `produces_candidates: true, candidate_target: field_plan, single_candidate: true` 步骤；FIELDPOOL-PLAN 明确不产候选，DIM-SOURCE 七路外部连接器属 V2，一次调用覆盖框架+采集+合并整结果）；前置闸=PS 存在 404、池态（无池或 gate=rejected，否则 GateNotAllowed 409，与 submit_plan 同口径）、target min≤max；(2) **整方案单候选复用 Q78 形态**：payload=去 actor 的 PlanSubmitRequest，经既有 apply_field_plan→fieldpool.submit_plan（Q8 启用路由/PT-FP-PLAN/Q9/Q10/Q12/G2 候选一项不绕）落 pending_gate 走 WF-02 product_reviewer Gate；模型必须原样回传 target_atom_min/max（漂移 502）；<3 维/缺 product_attribute/敏感缺 risk_control 是**业务违规**不是结构脏——照落非合规 pending_gate 池（violations 保留），人工 Gate approve 被机械拦截 409；(3) **Prompt 机读变量有界白名单 7 个**（product_profile/sensitive_industry/industry_tag/enabled_routes/active_g2_fields/dim_range/target_atom_range + `_` 前缀机读变量），synthetic 驱动确定性造合规整方案（active G2 fid 复用维+资料键补维+敏感补 risk_control；无任何真实 ref 时不伪造占位 ref；固定 confidence 0.9 不模拟分布）；结构白名单：role ∈ planning.DIMENSION_ROLES、source_route ∈ 启用路由、fid 必须 ∈ active G2（Q68 "-"/幻觉 fid 502）、批内字段名非空去重、批非空且 ≤dim_max、未知键剥离；证据红线 line 14081 在生成边界强制执行（source_ref 空白 502；只许引用有界输入 g2:/product_profile:/industry_tag:，禁止编造）；**confidence 口径区别于 Q83/Q84——DimensionItem.confidence 是 PT-FP-PLAN 契约内 AI 字段（Q9 细看线/Q12 Top8 直接消费），0..1 数值校验后透传，缺失/越界/非数 502**，similarity/related_fid/definition 可选透传（Q10 仅标记不阻断）；(4) **预算/失败语义沿用前三站**：日预算硬停 409、不静默切供应商、非 synthetic 缺 active Key 422、坏输出/回显漂移/结构脏 502、disabled 无注册 fallback 409，系统 Actor `system:llm-gateway` roles=[]（Q66）、source=llm_auto、成本四列照记。交付：迁移 **0017_dim_merge_seed**（纯数据 3 行：DIM-MERGE→synthetic 路由 + skill_prompts/skill_prompt_versions v0.1 含 7 变量，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/field_plan.py`（model_registry 第十二模块：角色/PS/池态/target 前置闸→有界变量→gateway.invoke→严格 JSON/target 回显/逐维结构与证据校验/confidence 透传/未知键剥离→deliver_generated_run 单候选，SkillRun.input 只存有界摘要）、schemas 增 FieldPlanInvokeRequest、seeds 增 DIM-MERGE Prompt v0.1、synthetic 增 build_dim_merge、fieldpool router 增 llm-plan 端点（201；403/404/409/422/502 齐）；289 测试全绿（+2 纯函数单测：合规整方案 1 fid 维+资料键维+占位维+风险维的 ref/0.9/回显/确定性、非敏感 10 活跃字段封顶 8 维全无 risk_control；+11 HTTP 集成：happy path 合成 3 维→pending_review field_plan→product_reviewer confirm→落 pending_gate 合规池→Gate approve、敏感行业补 risk_control、target 12/20 回显、RBAC reviewer/客户 403、PS 404、现有非 rejected 池 409、rejected 池重新触发 201、disabled 409→注册 fallback 201→预算 0 路由 409、远程 openai 无 Key 422、坏驱动非 JSON 502、target 漂移 502、脏方案十形 502、confidence 0.5 透传裁决后 needs_detail=true+未知键剥离、2 维 below_min 非合规池 Gate approve 409）、ruff 通过；**eval 不新增（仍 101/101）**。挂账：余末站点真 LLM（ATOM-EXPAND·ATOM-AFFINITY 含 Q16 pgvector 语义相似度本体，切片较重需重新提案）；DIM-SOURCE 第七路枚举【原文未给出，待补】；restock_auto 自动触发随 M8；入站一 Agent 一 Key（随 Q60）；编排并行/DAG（V2）；多副本广播/锁；source_route L4 枚举【原文未给出，待补】；成本币种【原文未给出，待补】。详见 08 §2.2 Q85 完成情况。

- **Q84 C7 Layer4 TYPE-MATCH 真 LLM 第三站落地（Q82/Q83 模式复用）**：接缝四项经负责人拍板追加 Q84（02 C1.28，均选推荐项）——(1) **仅显式业务端点、operations 角色**：`POST /api/intakes/{id}/c7/llm-resolve`（触发体 `{category_id, required_fids, actor}`），不与 CAT-RECOG llm-invoke 自动串联（链式/DAG 后置 V2）；前置闸=intake 存在 404、状态 ai_recognizing（TYPE-MATCH 属 WF-01 识别内步骤，与 CAT-RECOG llm-invoke 同状态闸，机械补登非新业务规则）、类目存在 404/active（draft 409）、required_fids 过 c7.validate_fids（Q68，调模型前 422）；(2) **整结果单候选复用 Q81 形态**：WF-01 TYPE-MATCH 已声明 `single_candidate: true, candidate_target: c7_layer4`；payload=去 actor 的 C7ResolveRequest `{category_id, required_fids, l4_proposals[]}`，经既有 apply_c7_layer4→modeling.resolve_c7（L1/L2/L3 提案自然 no-op，仅 L4 创建 g2_field_candidates）；模型必须原样回显 category_id 与 required_fids 集合（漂移→502），空 l4_proposals 合法（裁决后自然落 L3）；(3) **Prompt 机读变量有界白名单**（目标类目/必填 fid/本类目 approved L1 模板/approved 兄弟计数/L3 G2 active 覆盖清单+0.6 floor，6 变量），synthetic 驱动只按覆盖率缺口确定性造"待补字段·{fid}"结构提案、**不打置信分**；模型回带的 confidence/score/weight/fid/status/source_route 编排层一律剥离（仅 field_name+非空 definition 透传，G2FieldCandidate.confidence 落 NULL；source_route 枚举【原文未给出，待补】，v0.1 不接模型值）；L4 转正仍走 Q13 dictionary_admin 人工 Gate；(4) **预算/失败语义沿用 Q82/Q83**：日预算硬停 409、不静默切供应商、非 synthetic 缺 active Key 422、坏输出/回显漂移/脏提案 502、disabled 无注册 fallback 409，系统 Actor `system:llm-gateway` roles=[]（Q66）、source=llm_auto、成本四列照记。交付：迁移 **0016_type_match_seed**（纯数据 3 行种子：TYPE-MATCH→synthetic 路由 + skill_prompts/skill_prompt_versions v0.1，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/c7_resolve.py`（model_registry 第十一模块：前置闸→有界变量→gateway.invoke→严格 JSON/回显/脏提案校验/剥离→deliver_generated_run 单候选）、seeds 增 TYPE-MATCH Prompt v0.1、synthetic 增覆盖率缺口夹具、model_registry router 增 llm-resolve 端点（403/404×2/409×3/422×2/502 齐）；276 测试全绿（+2 纯函数单测：缺口提案只含未覆盖 fid 且无 confidence/score/fid、全覆盖空提案/空输入形态；+11 HTTP 集成：happy path 5 缺口→pending_review c7_layer4→operations 裁决→resolve_c7 落 L4 五条 pending_gate、全覆盖空提案落 L3、RBAC reviewer/客户 403、intake/类目 404、draft intake/draft 类目 409、fid "-" 422、disabled 无 fallback 409→注册 fallback 成功、预算 0 路由 409、远程模型缺 Key 422、坏驱动输出/类目回显不符/required_fids 漂移 502、脏提案 502、score/route 剥离专测）、ruff 通过；**eval 不新增（仍 101/101）**——synthetic 构造器是测试夹具而非确定性业务函数（16 §4）。挂账：余两站点真 LLM（FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY 含 Q16 pgvector 语义相似度本体）；restock_auto 自动触发随 M8；入站一 Agent 一 Key（随 Q60）；编排并行/DAG（V2）；多副本广播/锁；source_route L4 枚举【原文未给出，待补】；成本币种【原文未给出，待补】。详见 08 §2.2 Q84 完成情况。
- **Q83 PWC-BUILDER 真 LLM 第二站落地（Q82 模式复用）**：接缝四项经负责人拍板追加 Q83（02 C1.27，均选推荐项）——(1) **仅显式业务端点、operations 角色**：`POST /api/product-spaces/{id}/pwc/llm-build`，Q71 `restock_auto` SkillRun(requested) 本切片不自动消费（自动触发随 M8 worker）；(2) **一次调用产整批、多候选**：WF-04 PWC-BUILDER 步骤未声明 `single_candidate`，每条组合各成一条 `target_type=pwc_combo` 候选（payload `{"combos":[{atom_ids,goals}]}`，既有 apply_pwc_combo 适配器循环 payload["combos"]），输出超 Q21 `pwc.funnel_batch_limit`=50 拒；per-candidate 漏斗不再共享 run 级去重，故同原子集重复由编排层机械去重（保序）；Q22/Q23 重合仍由漏斗计算；(3) **Prompt 机读变量有界白名单**（approved 原子/库容/ready 数/active contentGoals/目标平台），前置闸=PS 存在 404、字段池 gate=approved 409、approved 原子跨 ≥2 维 409（不足不调模型），synthetic 驱动只产结构配对不造分，模型回带 logic_score/fit_score/weight 编排层始终剥离，Q22b 缺子分→score_incomplete→人工 Gate；(4) **预算/失败语义原样沿用 Q82**：日预算硬停 409、不静默切供应商、非 synthetic 缺 active Key 422、模型坏输出 502、disabled 无注册 fallback 409，系统 Actor `system:llm-gateway` roles=[]（Q66）、source=llm_auto、成本四列照记。交付：迁移 **0015_pwc_builder_seed**（纯数据 3 行种子：PWC-BUILDER→synthetic 路由 + skill_prompts/skill_prompt_versions v0.1，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/pwc_build.py`（model_registry 第十模块：前置闸→gateway.invoke→输出校验/剥离/去重→deliver_generated_run 多候选）、seeds 增 PWC-BUILDER Prompt v0.1、synthetic 增结构夹具（锚定配对/至多 5 条）、condition router 增 llm-build 端点（403/404/409×2/422/502 齐）；263 测试全绿（+2 纯函数单测：结构夹具只配对不造分/封顶 5/单维空批；+8 HTTP 集成：RBAC/404/池未 approved 409/单维 409/预算 409/缺 Key 422/坏输出 502/幽灵原子 502/模型回带分剥离/happy path 逐条裁决→漏斗 pending_gate/score_incomplete→PWC Gate 到 ready）、ruff 通过；**eval 不新增（仍 101/101）**——synthetic 构造器是测试夹具而非确定性业务函数（16 §4）。挂账：余三站点真 LLM（FIELDPOOL-PLAN·DIM-SOURCE/ATOM-EXPAND·ATOM-AFFINITY 含 Q16 语义相似度本体/C7 L4 新字段生成）；restock_auto 自动触发随 M8；入站一 Agent 一 Key（随 Q60）；编排并行/DAG（V2）；多副本广播/锁；成本币种【原文未给出，待补】。详见 08 §2.2 Q83 完成情况。
- **Q67/Q82 模型网关 + CAT-RECOG 真 LLM 试点落地（Q67 首个真 LLM 切片）**：接缝四项经负责人拍板追加 Q82（02 C1.26，均选推荐项）——(1) **范围=模型底座 + CAT-RECOG 单站试点**，其余四生成站点（PWC-BUILDER / FIELDPOOL-PLAN·DIM-SOURCE / ATOM-EXPAND·ATOM-AFFINITY / C7 L4）后续切片复用本模式；(2) **进程内同步调用、复用 skill7 通道**：业务端点进程内同步调模型网关，以系统机器 Actor（`system:llm-gateway`，roles=[]，Q66）经既有投递逻辑落 skill_runs/skill_candidates(pending_review)，operations 人工 Gate 一字不改，异步 worker 留 M8；(3) **outbound Key：DB 对称加密（Fernet）+ env 主密钥**（`LOOM_MASTER_KEY` 仅 env，不入库不入仓；模型注册表页创建/轮换、旧 Key 自动吊销、明文/密文不回显只回指纹；入站"一 Agent 一 Key 可吊销"随 Q60 效果回调切片）；(4) **Prompt 模板 DB 版本化**（v0.1 起步、v0.N+1 append-only、platform_admin + writeAudit；仓库只携迁移种子；不走 15 §1 规划的 runtime `prompt/*.md`）。机械补登（非裁决）：场景路由键=skill_id；日预算按模型全局硬停、用尽 409 不静默切供应商；fallback V1 仅注册态+人工（disabled 才走 fallback，无自动故障转移）；skill_runs 加 model_id/input_cost/output_cost/currency_code（成本 decimal(12,6)，种子币种【原文未给出，待补】）；仓内不放真凭证，测试/本地用确定性 synthetic 替身驱动。交付：迁移 **0014_model_gateway**（五表 ai_models/ai_model_keys/ai_scene_routes/skill_prompts/skill_prompt_versions + skill_runs 四列 + synthetic 模型/CAT-RECOG 路由/CAT-RECOG v0.1 Prompt 种子，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/` 九模块（models/crypto/gateway/drivers/synthetic/extraction/schemas/seeds/router）、skill7 拆 `deliver_generated_run`（系统 Actor、source=llm_auto、无角色门；顺手修 SkillRun.source 原硬编码 delivery 的潜在缺陷）、8 治理端点 + `POST /api/intakes/{id}/c1-recognition/llm-invoke`（403/404/409/422/502 口径齐）；253 测试全绿（+5 纯函数单测：Key 加解密/指纹/错钥、per-1M 成本 ROUND_HALF_UP、synthetic 确定性；+10 HTTP 集成：治理 RBAC/校验、Key 轮换不回显、Prompt 版本化、llm-invoke 全链路落 pending_review 后 operations 裁决照走 Q1 分支、disabled→fallback、预算硬停、远程模型无 Key 422、坏输出 502、配置错误）、ruff 通过；**eval 不新增（仍 101/101）**——无确定性纯业务函数，试点按 16 §4 由后端集成测试（synthetic 替身）覆盖。挂账：其余四站点真 LLM 切片；入站一 Agent 一 Key（随 Q60）；M8 worker；编排并行/DAG（V2）；多副本广播/锁；成本币种【原文未给出，待补】。详见 08 §2.2 Q82 完成情况。
- **WF-02 字段池占位替换切片 1/3 落地（DIM-MERGE field_plan 候选，Q78）**：M10 余量首个 WF 替换切片；接缝经负责人拍板追加 Q78（02 C1.22）——DIM-MERGE 为候选产出者，外部侧完成 7 路采集/合并后投递**整方案单候选**，`target_type=field_plan`（payload=去 actor 的 PlanSubmitRequest：dimensions + target_atom_min/max），FIELDPOOL-PLAN/DIM-SOURCE 登记不带 candidate_target。通道泛化：投递校验从硬编码 pwc_combo 改为按 WF 步骤 `candidate_target` 分派（registry 新增 `candidate_target_for`；非产出步骤带候选/target 不符/field_plan 非单条均 422；payload 按 ComboItem 或 PlanSubmitRequest 形态预校验），裁决 modified 同按 target_type 校验；新增 `adapters.apply_field_plan` 复用既有 `fieldpool.submit_plan`（Q8 启用路由/PT-FP-PLAN/Q9/Q10/Q12/G2 候选不绕，落 pending_gate 再过既有 WF-02 端点 Gate），router 补 404/409/422 业务口径，适配器失败回滚候选留 pending_review。交付：`runtime/workflows/WF-02.yaml`（skill7+fieldpool 双 Gate 插槽）+ 三 Skill YAML（10 字段）；eval 每切片自带（Q77 模式）：`evaluate_plan` target + DIM-MERGE 回归 12 案 + Golden 3 案（Q8/Q9/Q10/Q11/Q12/line 14081/fid），合计 39/39；backend +6 HTTP 集成 +1 注册表单测，共 213 测试全绿、ruff 通过、无迁移。挂账：WF-01（M2 C1/C7）、WF-03（M4 原子批次）同模式切片（各带 eval 案例）；Q67；编排并行/DAG；M8 worker；多副本锁。详见 08 §2.2 WF-02 完成情况。
- **WF-01 冷启动识别占位替换切片 2/3 落地（CAT-RECOG c1_recognition 候选，Q79；范围仅 C1 识别，C7 Layer4 挂账→Q81 销账）**：M10 余量第二个 WF 替换切片；四项接缝经负责人拍板追加 Q79（02 C1.23）——(1) CAT-RECOG 为候选产出者，识别**整结果单候选** `target_type=c1_recognition`（payload=去 actor 的 C1RecognitionRequest：signals/candidates/industry?/category_pending_id?），C7 Layer4 新字段提案通道随后续切片；(2) Q1 接缝：平台铁律 AI 只产候选，统一过一次人工 confirm（skill7 Gate=AI 产出准入），准入后 Q1 三分支机械逻辑一字不改（direct_approve→auto_confirm submitted / ops_assist→pending_confirm+72h OpsTodo / cold_start→需 category_pending_id）；(3) Gate 角色 operations（与 ops-decision、Q3/Q4 一致）；(4) 锚点：段2 先于 ProductSpace，skill_runs/skill_candidates 加 nullable `intake_id`、skill_candidates.product_space_id 改 nullable（迁移 0013，PG16 up/downgrade-1/up 实测），投递 PS/intake 锚点二选一并按 WF 归属校验。通道泛化：单候选约束由 WF 步骤 `single_candidate` 声明驱动（registry 新增 producer_step_for，WF-02 DIM-MERGE 同步加声明，去 Q78 硬编码）；新增 `adapters.apply_c1_recognition` 复用既有 `modeling.submit_recognition`，router 补 IntakeNotFound 404 / RecognitionNotAllowed 409 / ConfigError·InvalidDecision 422，适配器失败回滚候选留 pending_review；列表/视图加 intake_id。交付：`runtime/workflows/WF-01.yaml`（六顺序 Skill + CAT-RECOG 后单个 skill7 Gate role=operations，failure_policy low→cold_start/mid→ops_assist）+ 六 Skill YAML（10 字段、model_tier 空、cost_limit null）；eval 每切片自带（Q77 模式）：weighted_conf/decide_branch 两 target + CAT-RECOG 回归 11 案（Q2 加权 4 + Q1/Q3 分支边界 7）+ Golden 3 案，合计 53/53；backend +7 HTTP 集成 +1 注册表单测，共 221 测试全绿、ruff 通过、迁移 0013 PG16 实测。挂账：WF-03（M4 原子批次）同模式切片；C7 Layer4 新字段提案通道（Q79-1）；Q67；编排并行/DAG；M8 worker；多副本锁。详见 08 §2.2 WF-01 完成情况。
- **WF-03 字段下原子占位替换切片 3/3 落地（CONFLICT-PRECHECK atom_batch 整批候选，Q80；M10 WF 替换切片全部完成）**：M10 余量第三个（末个）WF 替换切片；四项接缝经负责人拍板追加 Q80（02 C1.24）——(1) **整批一条候选** `target_type=atom_batch`、`single_candidate: true`，payload=去 actor 的 BatchSubmitRequest 形态（items[{content,dimension_id,ai_risk,affinity?,evidence?,fact_type?,cluster_id?}] + 可选 batch_size），通道只接 AI 拓展批次，新增 `adapters.apply_atom_batch` 复用既有 `atom.submit_batch` 并强制 source=ai（Q15 达标停拓仅对 AI 生效，防伪装 manual），Q14 20/50、Q15 target 停拓、同批 normalized 去重、维度属本池 selected、line 11189 事实原子全局唯一、Q17 词表双轨、line 840 三类冲突、PT-ATOM-EXP 空证据降级一项不绕，applied_refs=batch_id；入库后每条 AtomCandidate 的 approveAtomGuard（9 机检+审计）/Q70 high·critical 禁批量/Q18 证据/Q19 keeper/Q20 生命周期全部原样保留（集成测试验证整批入库后逐条 approve 可过）；(2) **Gate 角色 product_reviewer**（06 line 172 原子审核=产品审核员；投递仍归 operations）；(3) **CONFLICT-PRECHECK 为末步候选产出者**——投递即 ATOM-EXPAND→CANON→AFFINITY→CONFLICT-PRECHECK 四步 AI 处理全部完成后的最终成型批次，ATOM-EXPAND/CANON/AFFINITY 登记普通步骤不挂 candidate_target；(4) **锚点 product_space_id**，复用 Q79-4 PS 分支，无新迁移。通道泛化：投递/裁决 payload 按 BatchSubmitRequest 契约预校验（剥离 source/actor）；router 补 ProductSpaceNotFound·PoolNotFound 404 / PoolNotApproved·TargetReached·FactAtomConflict 409 / InvalidBatch 422，适配器失败回滚候选留 pending_review（Q15 达标 409 时批次未入库、候选留待重决）。交付：`runtime/workflows/WF-03.yaml`（顺序四步+末步单 skill7 Gate；failure_policy=Q15 stop_expand/空证据 pending_evidence）+ 四 Skill YAML（10 字段、model_tier 空、cost_limit null）；eval 每切片自带（Q77 模式）：7 target（default_batch_size/target_reached/normalize_text/is_low_affinity/pick_cluster_keeper/grade_risk/conflicts_for）+ 四 Skill 回归 24 案（EXPAND 5/CANON 4/AFFINITY 7/CONFLICT 8）+ Golden 7 案，覆盖 Q14/Q15/Q16/Q17/Q19/line 840，合计 84/84；backend +8 HTTP 集成（投递闸七连/落 pending 无批次/confirmed 整批入库后逐条 Gate/rejected/modified+human_modified/池未批 409/Q15 达标 409 留 pending/无证据 pending_evidence）+1 注册表单测，共 230 测试全绿、ruff 通过、**无新迁移**（仍 0001–0013）。挂账：C7 Layer4（Q79-1）~~（已由 Q81 销账，见下条）~~；Q67 真 LLM（原子拓展生成本体同挂此项）；编排并行/DAG；M8 worker；多副本锁。详见 08 §2.2 WF-03 完成情况。
- **WF-01 C7 Layer4 占位切片落地（TYPE-MATCH c7_layer4 整 C7 解析请求候选，Q81，Q79-1 销账）**：M10 余量第四个 WF 替换切片（同一 WF 第二个产出步骤）；四项接缝经负责人拍板追加 Q81（02 C1.25）——(1) **产出步骤 TYPE-MATCH、Gate 在其后**：其 skill.yaml 本自陈 failure_policy=fallback_to_c7，registry 按"WF+步骤"逐步骤校验，同一 WF 两个产出者（CAT-RECOG→c1_recognition、TYPE-MATCH→c7_layer4），MISSING-INFO 仍非产出（缺字段清单驱动 pending_params）；(2) **整 C7 解析请求单候选** `target_type=c7_layer4`、`single_candidate: true`（payload=去 actor 的 C7ResolveRequest：category_id/required_fids/l4_proposals[]），新增 `adapters.apply_c7_layer4` 薄包装复用既有 `modeling.resolve_c7`——L1 缓存/L2 approved 兄弟/Q6 覆盖率 0.6 地板/Q68 fid:'-' 一项不绕，实际落 L1/2/3 时提案自然不生效（Q15 同口径），落 L4 才写 g2_field_candidates(pending_gate)，段3 Q13 dictionary_admin 转正闸原样保留，applied_refs=run_id；(3) **Gate 角色 operations**（与 Q79-3 同档），准入与 Q13 转正两道人工 Gate 不合并（Q66 AI 无角色）；(4) **eval 只打 c7 确定性纯函数**，Q68/错误分支留集成测试，真 LLM 新字段生成体挂 Q67。通道泛化：intake 锚点集合扩 {c1_recognition,c7_layer4}，投递 payload 按 C7ResolveRequest 预校验；router 补 IntakeNotFound·CategoryNotFound 404 / IllegalFid·InvalidDecision 422，适配器失败回滚候选留 pending_review；顺手修潜在缺陷——resolve_c7 Layer4 盲插违反 uq(source_layer,field_name)，改同名 c7_layer4 全局候选复用（与 wf02_dim_source 同口径，跨单复用集成测试覆盖）。交付：WF-01.yaml 第二产出步骤+第二 Gate、TYPE-MATCH/skill.yaml 同步（10 字段不变）；eval 每切片自带（Q77 模式）：c7_coverage/c7_pick_sibling/c7_layer3_floor 三 target + TYPE-MATCH 回归 14 案（地板 1/覆盖率 8/兄弟 5）+ Golden 3 案，合计 101/101（80+21）；backend +8 HTTP 集成 + 既有注册表单测扩双产出断言，共 238 测试全绿、ruff 通过、**无新迁移**（仍 0001–0013）。挂账：Q67 真 LLM（五处真生成侧：PWC-BUILDER/字段维度/CAT-RECOG 信号/原子拓展/C7 L4 新字段，通道均已通）；编排并行/DAG；M8 worker；多副本锁。详见 08 §2.2 C7 Layer4 完成情况。
- **M10-Q（质量两件套轻量版，Q77）落地——任务包闭合**：14 §2.6 的 Evaluation Dataset + Golden Cases 首次实体化；三接缝经负责人拍板追加 Q77（02 C1.21）——①首批案例只打 WF-04 有确定性替身的两个 Skill（PWC-SCORING→`score_combo`/重合度/疑重，COMBO-VALIDATE→Q48 `match_words`；PWC-BUILDER 外部生成不造案）；②独立 `eval/runner.py` + YAML 数据集（不进 pytest，对应 16 §1 回归行；`eval/targets.py` 注册 target，dict 子集断言+float 容差，全量错误汇报，失败退出码 1）；③本次只落可调用命令+文档、不建 CI（托管平台未确认，CI workflow 随 M12）。交付：Evaluation Dataset 19 案（评分 13/词表 6）+ Golden Cases 5 案，每案 source.refs 溯 Q/line、词例全合成；`eval/README.md` 记 schema/边界/挂账；staging 闸门命令入 17 CI/CD（`backend/.venv/bin/python eval/runner.py`，现 24/24）；15 §2.5、16 §6 补登。backend 新增 3 条 runner 闸门单测（套件绿/失败集/空集退出码），共 206 测试全绿、ruff 通过、无迁移。挂账：PWC-BUILDER 与 WF-01/02/03 案例随真 LLM/占位替换切片；COMBO-VALIDATE 结构预筛内联 DB 漏斗未抽纯 target（集成测试覆盖）；Simulation Sandbox/Drift Detection V2/V3。详见 08 §2.2 M10-Q 完成情况。
- **M10 切片 e（skill7 AI 候选通道·WF-04 试点，Q76）后端切片落地**：05 §2.3 skill7 状态机（line 11314）与 06 §4 注册表/编排器首次落代码；五接缝经负责人拍板追加 Q76（02 C1.20）。①范围=通道核心+WF-04 试点：`app/core/skill7/`（registry/models/schemas/service/router/adapters）+ `runtime/workflows/WF-04.yaml`、`runtime/skills/{PWC-BUILDER,COMBO-VALIDATE,PWC-SCORING}/skill.yaml`（lru_cache 加载器，`LOOM_RUNTIME_DIR` 可覆盖，未注册引用即拒；06 §4.1"11 字段"逐项列 10 项，按 10 项实现不擅补）；②外部投递边界：无模型注册表/LLM 客户端/API Key（Q67 机器通道契约仍【待补】，随真接 LLM 切片）；③通用 `skill_candidates`：投递即 pending_review（ai_suggested 生产者侧态不持久化），confirmed/modified 经 target_type 适配器写既有流程——`pwc_combo` 适配器复用 `pwc/funnel`(source=ai)，预筛/Q48/Q22/限量 50/PWC Gate 全保留，applied_refs 回写 pwc id；rejected→archived；适配器失败回滚候选留 pending_review 可重裁；run 无更新/删除路径（PT-COMPLIANCE 不可 mutate）；④Q71 补货：consume 致 ready<critical 按 `pwc.restock_cooldown_minutes`（5min）防抖落 `skill_runs(status=requested,source=restock_auto)`，只记 run 不造候选，consume 响应回带 restock_run_id（M5/切片 b 挂账销账）；⑤投递 operations、裁决 product_reviewer（WF Gate 插槽角色驱动，403/404/409/422 口径齐）。端点 `POST/GET /api/skill-runs`、`GET /api/skill-runs/{id}`、`GET /api/skill-candidates`、`POST /api/skill-candidates/{id}/decision`。迁移 0012_m10_skill7（PG16 up/downgrade-1/up 实测），203 测试全绿（+3 注册表单测 +6 HTTP 集成：投递闸/未注册 422/confirmed 适配落库/rejected 不落 PWC/modified 422+human_modified/适配器失败留 pending/补货 requested+防抖）。挂账：WF-01/02/03（M2 C1/C7、M3 维度、M4 原子批次）占位入参按同模式逐一切片替换；Q67 模型注册表/真 LLM/API Key 鉴权；编排器并行与复杂 DAG（二期）；M8 异步 worker；多副本补货防抖锁/配置广播/单实例调度锁。详见 08 §2.2 M10 完成情况（切片 e）。
- **M10 切片 d（RBAC 红线收口，Q75）后端切片落地**：端点级角色映射缺口经负责人拍板追加 Q75（02 C1.19）——①`POST /api/admin/sla/run` 与既有 `POST /api/admin/ops-todos/sweep` 手工触发归 **platform_admin**（请求体带 actor，缺省 422；定时调度为系统内部调用不经闸）；②G1 `POST /categories`、`PUT /categories/{id}/template` 归 **dictionary_admin**（CategoryCreate 请求体新增 actor；模板影响该类目未来全部新产品，Q69）；③`POST /pws/{id}/ccr/run` 归 **internal_compliance**（闸先于 PWS 存在性，错角色 403）；④pws/evaluate（Q28 系统提请机械求值）与 atom revive（前置状态即闸）经裁决**有意开放**，05 实现补登。既有裁决同步补闸（非新决策）：信号权重 PUT=operations（Q2）、行业阈值 CRUD=operations（Q7）、来源路由 PUT/DELETE=operations（Q8）。实现：新建 `app/core/rbac`（6 角色常量 + require_any_role + 单一 PermissionDenied，路由统一 403；既有各模块 RoleNotAllowed 不合并），闸在服务层。无 schema 变更/无新迁移（仅请求契约加 actor）。194 测试全绿（新增 6 HTTP 集成 403 矩阵；15 条既有 sweep/ccr/category 测试补正角色）。挂账：skill7（Q71 补货/M8 异步 worker）、多副本广播/调度锁。详见 08 §2.2 M10 完成情况（切片 d）。
- **M10 切片 c（缓存引导 + 常量迁移）后端切片落地**：新增消费侧统一入口 `app/core/config_center/knobs.py`——`knob(key)` 读进程内 config_cache，缓存未引导（测试/启动前）时回落 `SEED_BY_KEY` 种子拍板值，默认值单一事实源不二次抄写。M1–M8 全部硬编码数值常量迁为零参访问器函数（c1 冷启动 0.6/TopGap 0.1/ops 72h、c7 L3 覆盖 0.6、fieldpool 细看 0.85/疑重 0.9/3–8 维/目标原子 15–30〔请求契约改 Field(default_factory) 按请求取值〕、atom 20·50/0.5/7 天、pwc 50/100·70·50/5min/100/0.6·0.4/0.5·0.5/1.0/0.8/7 天 3 次/14 天、pws 3·1/7 天、ccr 法审 48h、sla 黄 24h、fcw 0.4·0.3·0.3）；状态字符串/原因码映射不动，纯函数权重入参 None 缺省解析、显式传值仍可覆盖。lifespan 启动在调度器前以独立会话 reload 缓存，失败仅告警不阻断（回落种子，healthz 不依赖引导）。无 schema 变更/无新迁移。188 测试全绿（+3 knob 单测：种子回落/热更翻转冷却与池档位；+2 lifespan 集成：DB 引导取覆盖值、引导失败仍启动；旧 1 条常量溯源测试改访问器口径）。挂账：skill7（Q71 补货/M8 异步 worker）、RBAC（sweep 端点暂无角色闸）、多副本广播/调度锁。详见 08 §2.2 M10 完成情况（切片 c）。
- **M10 切片 b（通用 SLA 引擎 + 定时调度）后端切片落地**：`app/core/sla/` 五件——`engine.py` 通用到期升级（所有 open ops_todo 过 due_at→escalated，审计动作按 todo_type 分流：c1/pws/law_review/wordlist_rescan，M2 旧函数变薄包装）；`jobs.py` 登记四作业（待办升级、Q18 证据超时驳回、Q24 冷却到期回 available、Q51 未来生效词激活补扫）；`runner.py` 每作业独立会话提交、异常隔离回滚（坏作业不阻断其余）；`scheduler.py` lifespan asyncio 循环默认 300s（LOOM_SWEEP_INTERVAL_SECONDS/LOOM_SCHEDULER_ENABLED 可控，轮内出错只记日志）；`router.py` 给 `POST /api/admin/sla/run`（回带每作业 changed/error）与 `GET /api/admin/sla/todos`（派生 sla_state；yellow 仅法审 created_at+sla.yellow_hours=24h，热读配置中心、空缓存回退默认；其余类型黄色口径【待补】）。Q51 未来生效词条闭环：词表 API 开放 effective_from/until（窗口倒置 422），新列 activated_at（迁移 0011，存量 active 回填 now，PG16 up/downgrade-1/up 实测），未来生效创建待扫、到点置位复用 rescan_for_entry。183 测试全绿（+3 单测 +4 集成）。挂账：Q71 自动补货（依赖 skill7 AI 漏斗，不构造虚拟候选）、M8 异步 worker、缓存启动引导+常量迁移、skill7、RBAC（sweep 端点暂无角色闸）、多副本广播/调度锁。详见 08 §2.2 M10 完成情况（切片 b）。
- **M10 切片 a（配置中心底座）后端切片落地**：迁移 0010_m10_config_center 两表（`config_items`：key PK/category/typed JSONB value/value_type/validation(min·max·choices)/source_ref/version/updated_by/时间戳；`config_item_versions`：uq(key,version)，种子 v1 用确定性 uuid5），PG16 up/downgrade-1/up 实测 54+54 行 JSONB 核对通过。54 项种子覆盖 02 §C2 全部纯标量旋钮（c1/fieldpool/atom/pwc/pws/sla/fcw/content/feedback/platform/agent，含后置阶段 Q56/Q57/Q61/Q63/Q65 与 line2119 agent 限额——配置页面先行）；已有专用 CRUD 表的 C2 项（行业阈值/信号权重/词库/CP-LAW/字典/发布位/PCP 模板/模型注册表等）不入种子，避免双事实源。发布语义（`app/core/config_center/`）：coerce 强类型（bool 必须真布尔、int 拒浮点/字符串）+边界/枚举校验→version+1→版本留痕→writeAudit（config.update/config.rollback，entity_type=config_item）→SQLAlchemy after_commit 后进程内 `config_cache` 快照原子切换；事务失败/回滚不触缓存（有测试）；回滚=以新版本号重发历史值，历史永不改写。HTTP：`/api/admin/config` 列表(?category=)/单项/历史/PUT 改值/POST rollback，仅 `platform_admin`（Q46/Q64 平台级管理员英文码【实现补】），403/404/422 齐。176 测试全绿（+6 纯逻辑 +5 HTTP 集成）。**M10 余量挂账**：缓存启动引导与 M1–M8 代码常量消费迁移、通用 SLA 引擎+定时调度（接通 M2 72h/M4 evidence/M5 冷却补货/M6 pws_ready/M7 法审与未来生效词/M8 任务异步化）、skill7 候选-审核通道、RBAC 红线复核、多副本 Redis 广播（V1 单进程仅进程内热更新）。详见 08 §2.2 M10 完成情况（切片 a）。
- **M8（段11 FCW 组装 / final_id 发证闭环）后端切片落地——V1 主链 段1→6→10→11 全通**：2 表迁移 0009_m8_stage11（final_content_whitelists / fcw_assembly_tasks，PG16 up/downgrade-1/up 实测，JSONB/server_default/`uq_fcw_same_issue` 核对）。①PT-FCW-ASM 七 Guard 纯逻辑模块 `fcw_rules.py`：g1 PWS frozen、g2 block_required=false 且 cleaning_passed=true（无 CCR 报告不放行【实现补】，gate_view 默认 False 不得伪装）、g3 PCP + CSP/CSTP/CEP active 且 gate=approved、g4 六路 product_space_id 一致（Q52）、g5 tenant 一致、g6 法审仅被触发时要求 approved（Q49）、g7 PWS is_active（Q53）；七项不短路，任一败 guards_passed=false 不 mint final_id，409 detail 回带逐项 guards。②E1.1 publishFCW 唯一出口（line 11036）：final_id 仅 assemble/assembly-tasks 同一服务函数写入；fcw.issued 审计 detail 含 E1_owner=publishFCW + 六路材料，阻断尝试 fcw.assembly_blocked 由 router 提交留痕。③Q54：pwc×100×0.4 + fit×0.3 + 三包 conf 均值×100×0.3（PWC/conf 0–1 归一，fit 本 0–100），缺失 null+incomplete，仅排序。④Q55：`POST /api/fcw/assembly-tasks`（count≤200，slot_ids 长度须=count 且不重复否则 422，不给则取平台 active 发布位前 N），同步逐条组装、MaterialMissing/GuardsFailed/DuplicateIssuance 单项隔离，results.issued/failures 每条留痕 + fcw.assembly_task 审计；`POST /api/fcw/assemble` 手动单条；operations 角色（403/404/409/422 口径统一）。⑤骨架 PWC 从不可变冻结快照 items 按 payload.goals 交集 + score 降序选取【实现补】。⑥Q51 rescan 扩展命中 pws_id 关联的 draft FCW（幂等 wordlist_rescan 待办，assignee=operations）。165 测试全绿（12 纯逻辑：七 Guard 逐项可败/不短路/Q54 量纲与缺失；6 HTTP：全绿发证审计/错误口径/材料缺失不产行/重复+重冻旧版 g1g7 拦截/Q55 批量失败隔离/医疗法审 g6 联动）。未含：WF-09 AI Skills（V2）、draft 生命周期端点（V1 直接 published）、段12 content_products、FCW 消费侧 API（契约【待补】）、任务异步 worker（随 M10）、Q54 段13 校准。详见 08 §2.2 M8 完成情况。
- **M11（段7/8 静态底表 + 段9 三包静态实例）后端切片落地（实现序先于 M8）**：7 表迁移 0008（publish_slots/goal_fit_weights/platform_rules/slot_type_defaults/pcp_templates/pcp_weight_tables/packages，PG16 up/down/up 实测，种子集中 `app/platform/platform_adaptation/seeds.py` 供迁移与测试共用）。①Q35 发布位档案 operations CRUD（code 唯一 409/软归档/source_url/score_source=manual_eval；**无平台种子**——line 1098-1221 基准 HTML 不在仓库，标【原文未给出，待补】不虚构）；②Q34 fit 权重矩阵（种子 ENGAGEMENT/CONVERSION，4 维 Σ=1 硬校验，fit_score 派生不落库，目的缺权重返回 incomplete 不凑分）；③Q36 规则 4 层 selector（层级必填 422、同级同条件异结论 409 回冲突行、overwrite=true 归档旧行；match 高优先级覆盖+同级从严+country 横切；native 默认不存）；④slotType 默认值仅日限量两列+defaults JSON（约 40 列【待补】）；⑤Q39 四模板种子（17 键 Σ=1.0 实现期初值草稿），PCP 实例走 Q40 校验器、uq_pcp_ps_platform 落实平台不共享、PUT 为 Q42 人工直编（清 template_code+before/after 审计）；⑥Q45 段9 三包 V1 静态切片（payload 键 04 §2.17 定死，产品×平台×目的×包型 active 唯一，Q52 带 tenant_id/product_space_id 供 M8 Guard④⑤；WF-07 AI 选包/20 次重配/usage_count 递增均 V2）。147 测试全绿（7 纯逻辑 + 6 HTTP 集成新增）。未含：Q37/Q41/Q42 动态信号与每周重算、fit_score 学习、PLATFORM-ADAPTER Skill（均 V2）、跨层 R-X01~X05（后续任务包）、3 个目的 fit 权重（待补）。详见 08 §2.2 M11 完成情况。
- **M7（段10 合规清洗）后端切片落地**：CCR 复用 M4 同源 Q48 词表（零复制），迁移 0007 增 `layer` 列承载 Q50 三层优先序（国家>平台>底座不可配置，同级 Q36 从严 ban>downgrade、critical>high）；`POST /api/pws/{id}/ccr/run` 仅 active frozen 可跑，按行业+市场取词、分市场独立判（country 维度），ban→`blocked/block_required=true` 一票否决、downgrade→`downgrade_pending` 只出建议、无命中→`clean`，报告 append-only（PT-COMPLIANCE-V2.0）；降级人工 approval（internal_compliance，仅 downgrade_pending 可批，批后 cleaning_passed）；Q49 敏感领域（CP-LAW 六码种子小表，internal_compliance CRUD）自动触发 law_reviews（pws_id 唯一幂等）+ 48h 法审待办，decision 端点录入线下律师结论（approved 放行 Guard⑥ / rejected 维持否决 / 已决再判 409）；`GET .../ccr/gate` 机械视图供段11 Guard②⑥ 消费（M8 接入）；Q51 词表保存生效同事务扫全部 active frozen 快照，命中幂等出 `wordlist_rescan` 待办（whitelist_owner，reason_code=wordlist_hit），重冻仍须 BO-07 人工（红线）。迁移 0007（layer 列 + 3 表，PG16 up/down/up 通过）；134 测试全绿（8 纯逻辑 + 8 HTTP 集成）。未含：WF-08 三 Skill AI 通道（V1 纯机械）、SLA 统一引擎/定时调度、Q51 对 draft FCW/未发布成品扫描（实体随 M8/段12）、未来生效词条定时扫描、拍板值配置化（均 M10）。详见 08 §2.2 M7 完成情况。
- **M6（段6 PWS 冻结）后端切片落地**：pwsReadiness 5 门机械求值（approved 字段池 / ≥3 已批准原子 / ≥1 active PWC=approved+ready/used【实现补】 / 0 未解决 blocked 冲突 / 0 pending_gate 池【实现补】，回带计数）；Q28 全绿只出 pws_ready OpsTodo（assignee=whitelist_owner、7 天 due、幂等、复用 M2 sweep 升级），冻结只能 BO-07 人工执行（角色码 `whitelist_owner`【实现补】，非属主 403、不全绿 409）；不可变快照 v1.0 起 vN.0 主版本递增（Q31，(PS,version) 唯一，同刻 1 active，原子/PWC 物化为有序成分行，源头变更旧版不动）；Q29 重冻三档（forced: atom_compliance_suspend/atom_deprecated/wordlist_hit/product_fact_change；suggested: asset_increment；none: unrelated_change→409；重冻须原因码，缺失/未知 422），重冻即建新版、旧版 superseded+双日志；Q30 换版处置结构返回（草稿/未发布/已发布实体随 M8/段12，占位空列表/0/forced 布尔，不溯及既往）；Q32 revoke 急停（仅 active frozen 可作废，is_active=false+原因留痕，立即断消费，作废后重冻新版）；Q33 sha256 指纹仅同租户其他 PS 比对、dup_hints 永不阻断，跨租户不比对。迁移 0006（pws_snapshots/pws_snapshot_items/pws_freeze_logs 3 表，PG16 up/down/up 通过，JSONB）；118 测试全绿（5 纯逻辑 + 9 HTTP 集成）。未含：WF-05 Skill（12 #15 占位，段6 纯机械无 AI）、Q51 词表生效触发强制重冻（随 M7/M10）、待办升级定时调度（随 M10 SLA）、Q53 FCW 引用 active 版（随 M8）、拍板值配置化（均 M10）。详见 08 §2.2 M6 完成情况。
- **M5（段5 PWC 条件包）后端切片落地**：Q25 contentGoals 字典（5 码种子，dictionary_admin 维护，软归档）+ Q27 库容配置（默认 100、可配无上限）；WF-04 漏斗四步——预筛（本 PS 已通过原子/同租户/跨 ≥2 维度【实现补】/同批去重）、Q48 词表合规检测（ban→blocked 不评分，手拼 Q26 不豁免，逻辑/搭配检测留插拔位）、Q22 评分（(合理性0.6+多样性0.4)×品类乘数，合理性=logic/fit 各 0.5，多样性=1−与待用池最大重合；AI 子分缺失不凑分转人工 Gate，score 仅排序；重合=原子集合 Jaccard【实现补】）、单批限量 50；Q23 重合 ≥0.8 标疑重、低分进备选、人工可改判；HumanGate（product_reviewer，入库受库容 409）；Q71 消费 score 降序、同平台+账号+发布位去重、跨平台可复用、goals 交集过滤、响应带 pool_health（target100/low70/critical50）与 restock_hint；Q24 同平台 7 天 ≥3 次→14 天冷却（per-platform state，sweep 服务就绪调度随 M10）、目标平台用尽→used（平台清单来源待补）、high_reuse_n 高复用旗标；Q61 爆款 V1 仅手工标；取用流水带 tenant 隔离。迁移 0005（6 表+5 码种子，PG16 up/down/up 通过）；104 测试全绿（13 纯逻辑 + 17 HTTP 集成）。未含：WF-04 三 Skill AI 通道、sweep/自动补货定时调度、Q61 自动检测、语义向量相似度、逻辑/搭配检测插件、target_platforms 来源、拍板值配置化（均 M10 或待补）。详见 08 §2.2 M5 完成情况。
- **M4（段4 字段下原子）后端切片落地**：Q48 统一合规词库横切包先行（CRUD + 软归档 + 审计，段4 先消费、段5 已 M5 接入、段10 待 M7）；拓展批次仅产候选（PT-ATOM-EXP），Q14 批次 20/50 可覆盖、Q15 AI 达标停拓手动放行、同批去重；Q17 risk 双轨（词表命中强制定级人工不可改，AI 判级可人工复核）；AtomConflict 三类（disabled_expression/high_risk_single_review/evidence_required）；approveAtomGuard 9 项机检 + 审计，Q70 high/critical 单条审、批量整批 409；Q18 无证据 pending_evidence + 7 天 sweep 自动驳回可复活（调度随 M10）；Q19 同义簇 keeper 终裁、merged 词条作 alias 并入正式原子；line 11189 事实原子（容量/成分/浓度/疣类型/品牌）(fact_type,normalized) 全局唯一、跨租户拦截、reference_count=1；Q20 生命周期（operations 冻结/解冻/废弃/归档，解冻不重审；internal_compliance 暂停/恢复；product_reviewer 驳回；废弃恢复=新原子重审）。迁移 0004（5 表，PG16 up/down/up 通过）；74 测试全绿。未含：WF-03 四 Skill AI 通道、sweep 定时调度、Q51 生效即扫联动段 5/10、Q16 相似度本体、拍板值配置化（均 M10）。详见 08 §2.2 M4 完成情况。
- **M3（段3 字段池规划）后端切片落地**：Q8 来源路由表 CRUD（6 路种子、引用保护+审计）；方案提交一品一池——每维度强制角色/启用路由/依据标注（line 14081 红线）、fid 必须为 active G2（Q68）、新字段进 g2_field_candidates（wf02_dim_source，同名全局复用）；PT-FP-PLAN 校验（product_attribute 恒 ≥1、risk_control 仅敏感行业 Q11、>8 Top8+备选档可捞回 Q12、<3 不达标转人工）；conf<0.85 标需细看（Q9）、相似度≥0.9 仅标疑重人工终裁（Q10）、目标原子数 15-30（Q15）；WF-02 HumanGate（product_reviewer，approved 锁定、rejected 可重提）；Q13 dictionary_admin 候选转正并回填池维度。迁移 0003（PG16 up/down/up 通过）；45 测试全绿。未含：WF-02 三 Skill AI 通道（M10）、灵感库两层实体、转正保护期加权（第二期）、cat=extension 占位待基准 HTML 核对、拍板值配置化（M10）。详见 08 §2.2 M3 完成情况。
- **M2（段2 C1 识别）后端切片落地**：信号权重配置表（Σ=1 强校验，Q2，文本三信号 0.50/0.33/0.17 种子）、行业阈值 CRUD（general 默认档不可删+审计，Q7）、conf 单指标三分支（Q1：高置信系统 auto_confirm 直送已提交；中置信 ops_assist 运营待办附 AI Top 候选，72h 未处理 sweep 升级，Q3/Q4，选定/全否；低置信带 B2 候选单进类目创建中）、C7 四层兜底（L1 模板缓存→L2 兄弟继承（product_count 最大，Q6）→L3 G2 覆盖率 60% 线→L4 新字段进 g2_field_candidates；Q68 fid:'-' 在模板写入与 L4 提案入口拦截）。迁移 0002（8 表+种子，PG16 up/down/up 通过）；AuditLog 上移 core 横切包；26 测试全绿。未含：WF-01 六 Skill AI 通道与定时调度（M10）、G1 完整类目管理（V3）、拍板值配置化迁移（M10 配置中心）。详见 08 §2.2 M2 完成情况。
- **M1（段1 产品录入）后端切片落地 + Q74 裁决**：负责人对挂账子项"18 通用字段值归属"拍板（02 C1.18/Q74）——申请单 `profile`（G2 fid 键控，提交后不可变）+ ProductSpace `profile_snapshot`（start_modeling 时整体复制，同 PWS 快照哲学 Q31）；完整度闸门按 g2_fields active 行动态判定不硬编码名单。代码：纯逻辑 productIntake15 状态机（15 态/终态/B2 三分支/operations 角色闸）、service + `/api/intakes` REST（缺字段 422、越权 403、非法迁移 409、审核后资料不可改 409）、Alembic 0001 四表（PG16 实测 up/down/up 通过）、11 条单测/集成测试全绿（SQLite in-memory；venv 经 uv 装 Python 3.12）。未含：WF-01 6 Skill 候选通道（随 M10）、72h SLA（M10）、真实 fid 种子（待基准 HTML 找回）。详见 08 §2.2 M1 完成情况。
- **代码脚手架落地**：按 15/17 定稿建最小骨架——backend（Python 3.12 + FastAPI 模块化单体，13 模块包映射 product×6/platform×2/decision×2/final/content/feedback，core 9 横切包，Alembic 异步 env，tests 四层目录，.env.example）、runtime/eval/middleplatform 数据驱动目录（.gitkeep 占位）、frontend（Next.js 15 + React 19 + TS 最小页）、infra/docker-compose（pgvector/pg16 + redis:7 + MinIO）；.gitignore 补 Python/.next；MIGRATION_CONVENTION 补 Alembic 现行口径。仅骨架可跑 `/healthz`，无业务逻辑。
- **路线图合并裁决（Q73）**：负责人逐项拍板——①A7 为骨 + D9 厚度（时间盒/客户目标采用 D9）；②段 12 内容生成移出 V1（V1 到段 11 final_id 闭环，段 12 随段 13 在 V2，原 M9 改为 P4）；③段 7/8 V1 仅静态底表（新增 M11）；④V1 驾驶舱收敛为 Token 成本 + 人工审核 2 个；⑤质量两件套维持 V1 覆盖 D9 V3 口径。08 §1.3 由"待裁决"改为裁决记录 + 合并后权威路线图表，§2 重排（V1=M1–M8+M10/M10-Q+M11/M12，V2=P1–P5，V3 平台化）；02 新增 C1.17/Q73；README 治理规则 4 与待裁决清单、AGENTS、本文件同步；原文挂起待裁决事项清零。
- **选型下游文档刷新**：技术栈确认（Python 3.12 + FastAPI / React + Next.js + TS / YAML 声明式编排）；按 14 §3 刷新 15（目录/技术栈/eval 落位）、17（PG16+pgvector/Redis7/S3 中间件）、08（新增 M10-Q 质量两件套任务包）、10（§1.3 存储拓扑 + 配置中心版本化热更新 + MVP 辅助系统范围）、06（§4.2 编排器选型）；15/17 升 🟢 已定稿。
- **技术选型 6 项拍板**：负责人逐项拍板，全部采纳 docs/14 建议——模块化单体 / 自研注册表编排器最小集（协议解耦留 LangGraph 迁移）/ PG+pgvector+Redis（Streams）+对象存储 / DB+变更广播热更新 / 进程内独立合规模块 / MVP 仅 Evaluation Dataset+Golden Cases。14 升 ✅ 定稿；下一步按 §3 同步 15/17/08/10/06。
- **M0 完成**：业务方 3 份素材采纳经负责人确认（02 C1.16 注记）；段 1-6 缺口实体 ProductSpace / g2FieldCandidates 字段补齐（04 §2.2/§2.6，同步 10）；03 S1-S4 全部加销账注记；AGENTS 待裁决清单移除 M0。4 个实现时挂账子项登记在 08 §2.1。
- **M0 三项转正 + 评分规格补写**：Q2（C1 信号权重/图像后置）、Q60/a/b/c（外部 Agent 推送制回流契约）、Q22（PWC 评分公式、品类乘数一期恒 1.0 回流后校准）经负责人复核转定稿；新裁 Q22a（合规前置/合理性加权/score 仅排序不做门槛）、Q22b（多样性=1−最大重合占比、AI 失败转人工不凑分），同步 01 段5、06 §1.1、02 §C2。
- **M0 口径对齐**：核对 02/03 后订正 08 §2.1 / AGENTS / 本文件——S 级 4 条已有 Q 决策（Q34 正式，Q2/Q22/Q60 临时采纳），素材 3 份已采纳销账；M0 真实剩余 = 3 个临时采纳转定稿 + 04 §3 规格补写 + 素材采纳告知确认。
- **基准 HTML 核验**：确认全景图 HTML 与 V6.0 需求说明 HTML 均不在本机、未入 git 历史（详见"待办"第 4 条）；同步订正本文件中"尚未 git init"的过期状态（实际已 init，`dev` 分支）。
- **v3 归档删除**：`Loom_核心业务主链梳理_v3.md` 经段落级核验（231 段、业务信息零丢失）后删除；docs 成为唯一事实源体系。
- **AI 导向切分（ai-docs）**：按消费场景把 v3 五层 + HTML 渲染内容切分为 9 份子文档（01–09）+ 文档地图 README；信息保全映射表逐项可核验（README §4）。
- **工程骨架补齐**：新增 10–17 号工程文档（Schema 建表 / OpenAPI / PT 协议补齐清单 / 状态机迁移表 / 技术选型 / 代码结构 / 测试策略 / 部署运维），均为建议/待补状态（见上方状态列）。
- **治理规则确立**：唯一事实源 = docs；修改先改文档、新决策追加 Q 编号、行号引用指向基准 HTML。

## 待办 / 下一步（按依赖顺序）

1. **M0 规格仲裁（08 §2.1）— ✅ 2026-09-13 完成**：Q2/Q22/Q60(a/b/c) 转定稿；Q22a/Q22b 评分分项规格新裁决并同步 01/06/§C2；素材采纳经负责人确认（02 C1.16 注记）；段 1-6 缺口实体 ProductSpace / g2FieldCandidates 已补字段（04 §2.2/§2.6，同步 10）。挂账不阻塞 M1 的 4 个子项：PS.lifecycle 迁移触发条件、~~18 通用字段值归属（M1 前定）~~ ✅ Q74 已裁决、候选实体多租户可见性、候选状态完整枚举。KUP-PROPOSE 随第二阶段段 13 补。
2. **技术选型下游同步 — ✅ 2026-09-13 完成**：14 六选型拍板（模块化单体/自研编排器/PG+pgvector+Redis/DB+广播热更新/进程内合规模块/质量两件套）；技术栈同日确认（Python 3.12 + FastAPI / React + Next.js + TS / YAML 声明式编排）；已刷新 15（目录+技术栈）、17（中间件/部署）、08（新增 M10-Q 质量两件套）、10（§1.3 存储拓扑 + 配置中心热更新 + 辅助系统 MVP 范围）、06（§4.2 编排器选型）。
3. **排期填表（08 §2）— 待启动**：路线图已合并（Q73），V1 任务包为 M1–M8、M10、M10-Q、M11（段7/8 静态底表）、M12（平台厚度）；各包工期/人员/优先级原文未给，待负责人提供资源信息后排期。排定后即可按 15 搭脚手架进入编码。
4. **待核验缺口**：
   - 两个基准 HTML 文件**经核验均不在本机**（2026-09-13，仓库目录 + Spotlight 全盘 + git 全历史）：
     ① `Loom_SaaS后台全景图.html`——docs/README 第 3/38 行称"保留在上级目录/`../`（仓库根）"，实际不存在，且从未进入 git；
     ② `Docs/Loom_后台_V6.0-需求说明（不是原型).html`——AGENTS.md 行号溯源（`line NNNN`）基准文件，`Docs/` 目录本身不存在。
     被 8 个文档引用（AGENTS / README / MIGRATION_CONVENTION / handoff / docs 01·09·12·README）。**待用户确认**：文件是否在其他设备/网盘；找回前 12 的 PT 占位名单核对与一切 `line NNNN` 溯源均被阻塞；不擅自修改或移除这些引用。
   - 12 的 PT 占位候选名单待与基准 HTML 核对（依赖上面的文件找回）；
   - 11/13 的接口与迁移行待 M0 裁决后补齐；
   - 10 的建表要素待 DBA 复核。
5. **仓库基建 — 部分完成**：git（`dev` 分支，提交规范见 [git-commit-message.md](git-commit-message.md)）；脚手架已按 15 落地（backend/runtime/eval/middleplatform/frontend/infra）。backend venv 已用 uv + Python 3.12 建好（`uv venv --python 3.12 backend/.venv && uv pip install --python backend/.venv/bin/python -e 'backend[dev]'`），`pytest` 378 绿、ruff 通过、Alembic 0001–0020 对 PG16 验证通过（0020 restock_retry_state：pg16 一次性容器 up/downgrade-1/up + psql 核对；Q89 无迁移纯代码）（0014 模型网关五表+成本四列，0015 PWC-BUILDER、0016 TYPE-MATCH、0017 DIM-MERGE 均纯种子无 schema 变更，0018 为首个带 schema 的 LLM 切片——pgvector 扩展/ai_models.capability/两表 vector(1536) embedding 列/cluster_line 种子，用 pgvector/pgvector:pg16 一次性容器 up/downgrade-1/up 实测，未触本地 atlas-pg）；Evaluation 回归 101/101（`backend/.venv/bin/python eval/runner.py`，M10-Q/Q77 + WF-02/Q78 + WF-01/Q79/Q81 + WF-03/Q80；Q82–Q92 无确定性纯业务函数（五个真 LLM 站点 + Q87 restock worker 调度/装配 + Q88 凭证治理/验签 + Q89 分布式锁 IO 基础设施 + Q90 退避游标状态 IO，退避序列纯函数走后端单测 + Q91 注册表拓扑规划/asyncio 调度基础设施走单测 + Q92 驾驶舱只读 SQL 聚合/HTTP RBAC 走集成测试），不新增 eval 案例）。浏览器人工验收通道已就绪：chrome-devtools MCP 以 local scope 注册到本项目（`~/.claude.json`，挂载本机 9222 Chrome + 独立 profile `~/.chrome-claude`，约定见 15 §4；新会话生效，不进仓库）。**段11 已闭合、M10 切片 a（配置中心）+ 切片 b（SLA 引擎/调度）+ 切片 c（缓存引导/常量迁移）+ 切片 d（RBAC 红线收口，Q75）+ 切片 e（skill7 通道/WF-04 试点/Q71 补货，Q76）+ M10-Q（质量两件套，Q77）+ WF 替换切片四组（WF-02 Q78、WF-01 Q79/Q81、WF-03 Q80）+ 五个真 LLM 站点（Q82 CAT-RECOG、Q83 PWC-BUILDER、Q84 TYPE-MATCH、Q85 DIM-MERGE、Q86 CONFLICT-PRECHECK+ATOM-AFFINITY——Q67 站点 backlog 已闭合）+ Q87 M8 restock_auto worker（Q71 补货信号→自动消费→pending_review 候选→人工 Gate 闭环，02 C1.31）+ Q88 入站一 Agent 一 Key 凭证半套（Q60b 鉴权前置，迁移 0019 agent_api_keys/SHA-256 哈希/治理端点/401 验签依赖，02 C1.32）+ Q89 多副本单实例锁（Redis SET NX PX 两把循环级 leader 锁/看门狗/env 默认关 fail-closed/手工 /run 409·503，02 C1.33）+ Q90 restock 瞬态退避（restock_retry_state 游标表迁移 0020/指数退避封顶 30min/预算硬停永不转终态·上游错 10 次转终态/env 三参数/手工 /run 绕窗口仍计数，02 C1.34）+ Q91 编排并行 DAG 原语（app/core/orchestration/ needs 拓扑分层/同层 asyncio 并发/层间屏障/Gate 前驱闭包/fail-fast/处理器注入，env LOOM_ORCH_MAX_CONCURRENCY=4，无迁移，02 C1.35）+ Q92 M12 两个驾驶舱只读聚合（app/core/dashboards/ Token 成本天×模型×币种 30 天窗/failed 单列/多币种不换算 + 审核积压快照/窗口产出，platform_admin 只读、管理面 GET 首次 query actor，无表无迁移，02 C1.36）均已落地，V1 剩余任务包（08 §2.2）**：① **M10 余量**（Q71 restock_auto 自动补货 worker 已随 Q87 落地——进程内轮询 V1 单进程，默认关闭 opt-in，详见 02 C1.31；**入站 Key 半套已随 Q88 落地——effect-callback 端点/effect 时序/孤儿队列/Q60a 回填随段13 P3/V2，鉴权已就绪**；**多副本单实例锁已随 Q89 落地——多副本部署开 LOOM_DISTRIBUTED_LOCK_ENABLED 即单实例 tick**；**瞬态失败重试退避已随 Q90 落地——restock_retry_state 游标/指数退避/上游错 10 次转终态/手工绕窗口**；**编排并行 DAG 原语已随 Q91 落地——needs 拓扑分层/同层并发/Gate 前驱闭包/fail-fast（DIM-SOURCE 七路等真实 fan-out 随 V2 连接器接入，无迁移）**；**M12 两个驾驶舱只读 API 已随 Q92 落地——成本天×模型×币种窗聚合+审核积压快照/窗口产出（工作台/租户 Onboarding/前端/CSV 待后续切片，前端受 18 号文档 🟡 门控，无迁移）**；建议下一棒：M12 余量四块之一（需逐片点工）或 V2 余量（配置缓存多副本广播、fencing token、Streams 细粒度认领、DIM-SOURCE 七路连接器）；新任务仍需负责人点工，不自动开工；DIM-SOURCE 第七路枚举与 source_route L4 枚举、成本币种、Key 单租户绑定口径【原文未给出，待补】；真 embedding 模型接入时维度按模型实际演进）；② **M12** 平台厚度（审核工作台/租户 Onboarding/前端 8 菜单/CSV 中台对接/Skill 注册表+编排器/CI workflow/2 驾驶舱；前端遵循 18，🟡 待拍板；**进展：Skill 注册表+自研编排器最小集已随 Q76/Q91 落地，2 驾驶舱只读 API 已随 Q92 落地（02 C1.36），余工作台/租户 Onboarding/前端/CSV 四块待逐片点工**）。

## 关键约束（接手者不得违反）

- **唯一事实源** = docs/；不得以摘要/记忆/二手来源替代文档原文。
- **不擅自裁决**：原文挂起的三项（两套路线图合并、业务方素材采纳、技术选型）均已由负责人拍板（2026-09-13，Q73 / 02 C1.16 / 14）；今后新出现的挂起事项仍须由用户/负责人拍板，禁止静默替换或默认值，并在 02 追加 Q 记录。
- **事实纪律**：文档空缺处标注【原文未给出，待补】，禁止编造业务事实；数字/比例/状态词改动需溯源（line 行号 / Q 编号）。
- **两套口径**：13 段链（权威）与展示口径（03-A~E）分置 01/09，不混用。
- **AI 治理红线**（02 Q66 等）：AI 只产候选，人工 Gate 裁决；红线操作（如 PWS 冻结）需指定角色 + 人工 Gate。

## 交接完成定义

接手者在开始编码前应能明确回答：

- Loom 的 13 段链是哪 13 段？`final_id` 由哪 6 层快照相加、唯一出口是什么？
- 为什么"白名单"是相撞产物而不是手写词表？frozen PWS 在链条中的作用是什么？
- S 级红旗 4 条分别是什么？M0 为什么必须在写代码前完成？
- 两套路线图（A7 vs D9）原来为什么并存？合并裁决（Q73）的结论是什么——V1 为什么不含段 12、段 7/8 在 V1 的最小切片是什么？
- 技术选型 6 项建议分别是什么？当前是"建议"还是"决策"？
- Q1–Q72 里哪些是硬约束（配置化/审计/AI 治理/多租户/成本/不静默生效）？
- 当前文档体系里哪些是"权威"、哪些是"待补/待拍板"？修改一份规格文档的正确流程是什么？
