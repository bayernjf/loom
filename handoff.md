# Handoff — Loom

> **State of Loom as of 2026-09-21**（设计/文档定稿；M1–M8 + M11 后端切片已落地（实现序 M7→M11→M8，V1 主链 段1→6→10→11 已闭合），M10 切片 a 配置中心底座、切片 b 通用 SLA 引擎+定时调度、切片 c 缓存引导+常量消费迁移、切片 d RBAC 红线收口（Q75）、切片 e skill7 AI 候选通道·WF-04 试点（Q76，Q71 补货接通）已落地，M10-Q 质量两件套（Q77）已落地，WF-02 字段池占位替换切片 1/3（Q78，DIM-MERGE field_plan 候选）已落地，WF-01 冷启动识别占位替换切片 2/3（Q79，仅 C1 识别，CAT-RECOG c1_recognition 候选 + intake 锚点迁移 0013）已落地，WF-03 字段下原子占位替换切片 3/3（Q80，CONFLICT-PRECHECK 末步产 atom_batch 整批候选，product_reviewer 准入，无新迁移）已落地，WF-01 C7 Layer4 占位切片（Q81，TYPE-MATCH 第二产出步骤产 c7_layer4 整 C7 解析请求单候选，operations 准入，Q79-1 挂账销账，无新迁移）已落地，**Q67/Q82 模型网关+CAT-RECOG 真 LLM 试点（模型底座五表迁移 0014、进程内同步复用 skill7 通道、outbound Key DB 加密+env 主密钥、Prompt DB 版本化）已落地，Q83 PWC-BUILDER 第二站（迁移 0015 纯种子、一次调用多候选、模型回带分一律剥离、llm-build operations 端点）已落地，**Q84 TYPE-MATCH/C7 Layer4 第三站（迁移 0016 纯种子、整结果单候选 c7_layer4、回显漂移与脏提案 502、模型回带 confidence/score/source_route 一律剥离、llm-resolve operations 端点）已落地，Q85 DIM-MERGE/WF-02 字段池方案第四站（迁移 0017 纯种子、整方案单候选 field_plan、target 回显与结构脏/证据空白 502、confidence 按 PT-FP-PLAN 契约 0..1 透传驱动 Q9/Q12、业务违规照落非合规池 Gate 拦截、llm-plan operations 端点）已落地，Q86 ATOM-EXPAND 原子补池第五站/末站（迁移 0018——首个带 schema 的 LLM 切片：pgvector 扩展/ai_models.capability chat|embedding/两表 vector(1536) embedding 列/atom.cluster_line=0.9 借 Q10 同义线原文未给；CONFLICT-PRECHECK chat + ATOM-AFFINITY embedding 双调用合并单 SkillRun、单 atom_batch 候选复用 Q80；模型自报 affinity/cluster_id 剥离、本地进程内余弦+同维并查重算、ai_risk 契约枚举透传；llm-expand operations 端点、花 token 前停拓等预闸）已落地，Q87 M8 restock_auto worker（进程内第二个 asyncio 循环同构 SweepScheduler，默认 false 部署 opt-in + platform_admin 手工 /api/admin/restock/run；纯追加 succeeded/failed 的 llm_auto 子 run 反连接认领、requested 信号行永不 mutate；仅自动消费 PWC-BUILDER 只产 pending_review 人工 Gate 不改，终态追加 failed 子 run、预算硬停/上游错瞬态下轮重试；无迁移，头仍 0018；四接缝 02 C1.31 负责人拍板）已落地，Q88 入站一 Agent 一 Key 凭证半套（Q60b effect-callback 鉴权前置；迁移 0019 新表 agent_api_keys，SHA-256 单向哈希、明文仅签发返回一次、platform_admin 治理 /api/admin/agent-keys、Bearer 401 验签依赖已备；effect-callback 端点与段13 回流本体仍 V2/P3；四接缝 02 C1.32 负责人“四项全按推荐（甲）”拍板）已落地，Q89 多副本单实例锁（Redis SET NX PX 两把循环级 leader 锁 loom:lock:sla-sweep/restock-worker，看门狗续约；LOOM_DISTRIBUTED_LOCK_ENABLED 默认关、fail-closed 跳轮；两个手工 /run 同锁 409/503；无迁移；四接缝 02 C1.33 负责人四项全按甲拍板）已落地，Q90 restock 瞬态失败重试退避（新表 restock_retry_state 游标/迁移 0020，指数退避 60→1800s 封顶，预算硬停永不转终态、上游错 10 次转终态 failed 子 run，env 三参数非 Q9 旋钮，手工 /run 绕窗口仍计数；四接缝 02 C1.34 负责人四项全按甲拍板）已落地，Q91 注册表驱动编排并行 DAG 原语（新包 app/core/orchestration/：needs 拓扑分层/同层 asyncio 并发/层间屏障/fail-fast/skill7 Gate 前驱闭包不跨/处理器注入不记账，env LOOM_ORCH_MAX_CONCURRENCY=4，纯代码无迁移；四接缝 02 C1.35 负责人四项全按甲拍板）已落地，Q92 M12 首片·两个驾驶舱只读聚合（新包 app/core/dashboards/：Token 成本天×模型×币种默认 30 天窗、failed 单列、多币种不换算；人工审核=候选/待办积压快照+窗口产出；platform_admin 只读实时 PG 聚合，管理面 GET 首次 query 携带 actor，无表无迁移；四接缝 02 C1.36 负责人四项全按甲拍板、Q93（M12 第二片统一审核工作台：app/core/workbench/ 五型候选风险四档归一队+置信度整批通过，WF Gate 角色并集，迁移 0021 纯配置种子）、Q94（M12 第三片候选审核 SLA 待办自动挂接：skill7/review_sla 投递建单/裁决 resolve，五型 review_* todo_type+review.sla_hours.* 五键种子 72h 待补，迁移 0022 纯配置种子）、Q95（M12 第四片租户管理+Onboarding：app/core/tenants/ 注册表四档 plan/三态 status、platform_admin 生命周期端点全审计、详情派生 Onboarding 进度、段1 准入未知 404/暂停 409 应用层无硬 FK，试用月额度 50 万付费档待补，迁移 0023 建表+存量 DISTINCT 回填）已落地**——M10 WF 替换切片四组完成、五个真 LLM 站点全部接通（Q67 站点 backlog 闭合）、Q71 restock_auto 补货闭环、入站 Key 凭证底座就位，下一步 M12 余量（两个驾驶舱只读 API〔Q92〕、统一审核工作台候选池+置信批量通过〔Q93〕、候选审核 SLA 待办自动挂接〔Q94/Q70②〕、租户管理+Onboarding〔Q95〕已落地（Q70 一期三件齐；五型时限具体数值仍待业务方补）；客户前端 Q96 i18n+Token 脚手架、Q97 应用外壳/D5 八菜单导航、Q98 产品中心功能页（我的产品列表/新建 draft/只读详情，跨栈补 GET /api/intakes 租户列表端点）、Q99 工作台功能页（录入单总览/15 态分组，跨栈补 GET /api/intakes/overview 聚合端点）、Q100 中台 CSV 导出（新包 app/core/exports/，GET /api/exports/fcw.csv 仅 final_id 单列同步流式，D4 CSV 契约收口、docs/09:87 6 层包口径分界已注）、Q101 合规风控只读功能页（跨栈：GET /api/compliance/overview 租户聚合——active frozen 一行一产品、各市场取最新报告、跨市场从严、法审单条只读；前端 (shell)/compliance 行展开明细，清洗/降级/裁决/词库写口不进客户页）已落（18 号文档 ✅ 门控解除），余五菜单功能页（content/templates/analytics/social-accounts/settings，下一菜单未点工、不自动开工）；管理端独立 `/[locale]/admin` 布局 + Q92 两驾驶舱只读页 Q102 已落（env 自报 LOOM_ADMIN_ACTOR_ID/ROLES、客户 shell 无入口、枚举码不翻译、默认 30 天窗回显），Q103 统一审核台前端页已落（队列+URL 服务端筛选/分页+风险四档语义 chip+payload 原样 JSON 折叠+Server Action 批量通过；feat 8c7fe47/test 8e4f496），Q104 逐条裁决已落（pending_review 行 DecisionCell 岛：confirmed/modified/rejected + payload 替换编辑器，critical 二次确认，复用 /api/skill-candidates/{id}/decision 无新写口，applied/archived 出 chip+裁决时间+已人工修改+裁决备注；feat 13d29f9/test 5fa0086，415 测试/86 token 不变），Q105 Q95 租户管理+Onboarding 前端页已落（tenants 列表 RSC+开通表单岛+每行改套餐/暂停/恢复岛、[tenantId] 详情只读注册表+派生 Onboarding 三字段，写操作全走同源 Server Action 复用 Q95 六路由，plan/status 码原样、三态 chip info/success/warning、付费档额度 null 显 —；feat 9ffb7cf/test 058c8bb，纯前端无迁移、后端零改动，415 测试/86 token 不变），Q106 录入单 15 态客户操作（白名单 submit/params_completed/quota_confirmed/resubmit 与 allowed-events 取交集；ops/wf01/b2_* 与触发方【待补】事件不外放）+ draft 产品名编辑已落（纯前端消费段1 既有四端点 allowed-events/transitions/PATCH profile/product-space，身份走 server-only LOOM_ACTOR_ID roles 恒空，422 missing_fids fid 原样中文回显，category_creating 显"资料分析中"，空间只读；feat 64fe6f4/test 3333e39，后端零改动，415 测试/86 token 不变），Q107 管理端第五片录入单运营队列与跨租户运营操作已落（新增唯一只读端点 GET /api/intakes/ops-queue 跨租户 RBAC operations|platform_admin+15 态 status 422+分页，前端 /admin/intakes 列表与 [intakeId] 详情操作岛按 allowed-events∩恰 6 码白名单 ops_confirm/to_cold_start/reject/b2_approved/b2_parent_fallback/b2_rejected 出按钮，写复用 POST transitions 无新写口，platform_admin 默认只读缺 operations 本地 missing_role 零请求，状态词复用 intake.status.* 零新增、无迁移头仍 0023；feat 6b11897/test 475c2b7，418 测试/86 token 不变），Q108 管理端第六片待办 SLA 看板已落（M10b 裸端点 GET /api/admin/sla/todos 补 require_sla_view 仅 platform_admin、缺 actor 422/越权 403，status 白名单 open/escalated/resolved/all 非法 422，纯只读 RSC /admin/sla-todos 9 列四筛选、枚举码原样、四色 sla_state chip、yellowNote 黄仅法审其余黄口径【待补】、/sla/run 不上页，无写口无迁移头仍 0023；feat 15387a9/test 1975946，421 测试/86 token 不变），Q109 管理端第七片管理面读端点鉴权审计+8 个裸 GET 同型补闸已落（c1/signal-weights、c1/industries=operations，categories=dictionary_admin，fp-source-routes、fit-weights=operations，compliance-wordlist=operations/internal_compliance，ai-models=platform_admin，ai-scene-routes=operations|platform_admin；缺 actor 422/越权 403；config 三读口/g2-candidates/skill-prompts 读角色【待补】不加闸，agent-keys 与 ai-models/{id}/keys 矩阵明文有意开放锁定；feat 9269315/test 885574b，444 测试/86 token 不变/头仍 0023，纯后端前端零改动），Q110 审核工作量页空态缺键热修已落（Q102 起 admin.workload.empty 漏键、空库四空态显原始键，Q109 空库 E2E 发现；补"暂无数据。"+check-admin 必检守卫，fix 0b10ed5/test d4059a9，444 测试/86 token 不变/头仍 0023），Q111 CI workflow 已落（GitHub Actions `.github/workflows/ci.yml`：backend ruff/pytest(sqlite)/eval 101 + frontend tsc/六 checker，零 secret；守卫单测防删闸，445=444+1；feat 44659b1/test 5f747bf；**已随 PR #38/#39 在 GitHub 实际触发（2026-09-16，dev push/PR/main 合并共 6 次 run 全 success：Backend gates pytest+ruff+eval、Frontend gates tsc+六 checker）**；销 Q77/17 挂账，02 C1.55），**Q112 11/13 契约文档据已落地代码回填（13 号 productIntake15 七处迁移行补事件码/角色/副作用+G1/PWC 核实维持待补，11 号白名单消费/CSV 导出/入站出站 Key 治理端点规格回填；纯文档无代码无迁移头仍 0023，销 handoff 待办④"11/13 待 M0 后补齐"挂账，02 C1.56）已落地，**Q113 三族管理面读口补闸收口（config 三 GET 与 skill-prompts 三 GET=platform_admin〔写口同组〕、g2-candidates=dictionary_admin〔05 表行 GET/promote 同行 Q13/Q68〕，3 路由加 query actor 依赖同 Q109、写口 body actor 服务层闸不动，agent-keys 与 ai-models/{id}/keys 矩阵有意开放仍免闸；三族前端零消费；455 测试=445+10、101 eval、ruff 净、tsc+六 checker 绿、token 86、头仍 0023，02 C1.57）已落地**；客户侧剩余占位经 2026-09-16 核实分两组：content/analytics/social-accounts 已带 V2 徽标（段8/12/13 V1 不做），templates/settings 经 Q114 拍板「都按推荐」（2026-09-17，02 C1.58）：settings=V1 只读账户面板（客户侧 GET /api/tenants/{id} 无闸复用 Q95 注册表）+ 语言展示（切换随 V3）+ 平台授权/通知/帮助/团队/账单五子项 V2 徽标，templates=仅占位 V2 徽标（模板库随 V2 类目），五型 SLA=V1 统一 72h + 黄色窗口到期前 24h 配置化占位（真实数值仍待业务方输入）；另有 CSV JSON 形态与异步导出（挂后续/V2）、付费档额度/计费订阅/团队账号（V2/待补），各块新决策需逐片点工）或 M10/V2 余量（真实 fan-out 随 DIM-SOURCE 七路连接器、段13 effect-callback 整切片 P3/V2、Streams/配置广播/fencing token），新任务仍需负责人点工；**Q115 基准 HTML 永久丢失后的溯源口径收口（2026-09-17，02 C1.59）：line 溯源降级为历史痕迹、12 PT 名单定为最终名单（7 定稿+35 推断）、触发方事件永久【原文未给出，待补】、G2 fid 改判工程定稿（迁移 0024 落 12 个 cat='common' 字段，余 6 字段【待补】）**）；**V2 P4 段12 内容生成第一片已落地（Q116，2026-09-17，02 C1.60）**：文章单语言最小闭环——content_products 迁移 0025 + ARTICLE-GEN 场景 0026（模型网关第 7 场景，不走 skill7 运营 Gate，客户审阅即 Gate Q59）+ 词库复检（复用 Q48/ccr_rules）+ 客户审阅端点（approve/reject/revise/regenerate，Q56 重生成≤3），测试 472、eval 101/101；视频/多语言/质量分/复检余 3 项缓做；**Q117 文档⇄代码一致性审计与纯事实回填（2026-09-17，02 C1.61）：把实测基线（472 passed / Alembic 头 0026 / 7 个 checker / token 86）回填进 handoff·08·README·AGENTS 与 04/05/10/11/13，补登迁移 0021–0026 与段12 content_products 契约；4 项实现差异只挂账不裁决（待办 6），无代码/迁移/测试变化**；**Q118 Q117 挂账四项收口（2026-09-18，02 C1.62，负责人「一口气开搞」均按推荐甲拍板）：① 4 个漏网管理面 GET（content-goals/cp-law-domains/publish-slots/slot-type-defaults）补 query actor 闸；② publish_slots.gate 回填 V1 仅 Q42 人工直编口径（默认 approved，AI 拓展 Gate 流随 V2）；③ product_spaces 两道软引用补 DB 级 FK（迁移 0027，PG16 实测）；④ check-settings 接入 CI（六→七 checker + 磁盘集合一致性防漂移）；审计外修复 Alembic 版本表 VARCHAR(32) 容不下 0021 起 revision id 致全新 PG 全链截断（env.py 幂等加宽 128）；472→483 测试、头 0027、CI 七 checker、token 86，待办 6 销账；**Q119 段12 多语言 Q58 落地（content_languages 语言清单配置化、发布位市场∩产品目标语言交集、content_products 每语言独立成品唯一约束，迁移 0028，483→494 测试，视频/复检语义级缓做，2026-09-18）**；**Q120 段12 AI 质量分真打分 Q57 + 重生成上限配置化 Q56 落地（模型网关第 8 场景 ARTICLE-QC 内嵌生成、纯 advisory 不阻断发证、阈值 content.ai_quality_threshold 与上限 content.regen_limit 接通消费，迁移 0029/0030，494→511 测试，视频/复检语义级缓做，2026-09-18）**。
**Q121 复检第②项语义级检测落地（模型网关第 9 场景 ARTICLE-SEMANTIC-CHECK 内嵌生成、纯 advisory 不抬 block_required/不阻断 approve、零端点零列零表，迁移 0031 纯种子，511→523 测试，复检余施工指令核对/国家规则核对两项缓做，2026-09-19）**。
**Q122 客户「内容生产与发布」功能页 + Q56-a 客户人工改稿落地（无闸列表/详情/PATCH body 三端点、新事件 manual_resubmit revising→review、前端内容列表/详情/审阅/改稿岛与第八 checker check-content，零迁移，523→528 测试；三接缝交互超时按推荐甲落地并**已经负责人 2026-09-19「一口气搞了」追认**（02 C1.66 销账、无需返工），2026-09-19）**、**Q123（B3）段1 客户目标语言录入控件落地（客户 PATCH /api/intakes/{id}/target-languages + 无闸 GET /api/content/languages、ProductSpaceView 回显 target_languages，零迁移，528→534 测试，2026-09-19）**、**Q124 难产骨架作废回池 Q56-b 落地（第七态 discarded 终态 + POST /api/content/{id}/discard reason 必填 + GET /api/admin/content/needs-attention 队列、partial unique index 释放同键生成机会，迁移 0032，534→545 测试，2026-09-19）**、**Q125 运营发布回填与待发布队列 Q60c 落地（PUT /api/admin/content/{id}/publish-info + GET /api/admin/content/ready-to-publish、published_url/platform_post_id/published_at 三列、不新增 published 态，迁移 0033，管理端 /admin/content 内容运营台与 Q124 共用一页，545→554 测试，2026-09-19）**。
**Q126 段13 反馈回流入站第一片·effect-callback 落地（POST /api/effect-callback + effect_records 时序 matched/orphan 分流 + 运营只读孤儿队列/成品时序，复用 Q88 Agent Key 为首个受保护业务端点，metrics 缺席记“—”不补 0、(content_id,captured_at) 幂等覆盖/新点追加，迁移 0034 新建 effect_records 表，物理表 55→56，554→591 测试，2026-09-19）**。
**Q127 Q60a 孤儿效果记录人工认领/绑定落地（POST /api/admin/effects/claims operations 写口硬闸、effect_claims 持久映射 external_content_id→content_id + effect_records 加 claimed_by/claimed_at 两溯源列不新增第三状态、认领后同 ID 推送经映射兜底不回落孤儿、支持旧目标 discarded 后改绑重指、effect.claimed 审计，迁移 0035 物理表 56→57，591→599 测试，2026-09-19）**、**Q128 customer-backfill 客户专用效果回填通道落地（POST /api/effects/backfill 无 Agent Key、body {tenant_id,records,actor} 同 Q122 客户写口、source 服务端固定、只命中本租户非 discarded 成品否则整批 422、绝不产生孤儿、effect.customer_backfilled 审计 tenant=客户租户，零迁移，599→607 测试，2026-09-19）**；同日负责人「一口气搞了」追认 Q122 三接缝甲案（02 C1.66 销账）；效果反哺 Q54/Q57/Q61/Q65 校准算法（口径【待补】）、认领/回填前端 UI、取消认领带段13 续片。
**Q129 孤儿批量认领 + 取消认领/解绑落地（POST /api/admin/effects/claims/batch 整批 all-or-nothing、claims/unclaim 删 effect_claims 映射并把旧映射目标行回滚 orphan 含兜底新 matched 行、external_id 自匹配真自动行不动、新异常 ClaimMappingNotFound 404、effect.claim_revoked 审计，service 抽 _claim_one，零迁移，607→618 测试，2026-09-20，02 C1.73）**、**Q130 管理端效果回流运营台落地（新建 /admin/effects 为 sidebar 第 8 项：孤儿队列单条/勾选批量认领岛 + 成品 content_id 时序查询/人工认领行解绑岛，读 operations|platform_admin、写 operations，指标缺席显 “—”，check-admin 锁 sidebar 恰 8 项与后端六路由，零迁移，2026-09-20，02 C1.74）**、**Q131 客户内容详情页效果数据回填岛落地（消费 Q128 无 Agent Key 客户通道、仅非 discarded 成品挂载、V1 单条手工、datetime-local 转 UTC、指标留空即缺席、批量/CSV 随 V2、客户 nav 不增项仍 8，check-content 扩守卫，零迁移，2026-09-20，02 C1.75）**；三片接缝按推荐甲落地、**已经负责人 2026-09-20 追认**（02 C1.73–C1.75）；效果反哺校准算法（口径【待补】）与回填批量/CSV 随 V2，须点工。
**Q132 中台导出 JSON 形态 + 异步导出任务落地（GET fcw.json envelope 与 CSV 同口径、POST /api/exports/jobs V1 同步落 completed + 状态/下载口、export_jobs 不存 payload 下载按参数重渲染，迁移 0036 物理表 57→58，618→633 测试，2026-09-20，02 C1.76）**、**Q133–Q135 多副本 V2 基建三件原语落地（Q133 fencing token：leader_lease 抢锁后 INCR 取跨易主单调令牌、看门狗易主/续约故障置 lost、INCR 失败 fail-closed，02 C1.77；Q134 Redis Streams 消费组细粒度认领原语：新包 app/core/queue 落 XADD/XREADGROUP/XCLAIM/ACK/死信、只落原语不接 worker 不落 env，02 C1.78；Q135 配置缓存多副本 pub/sub 失效广播：after_commit fire-and-forget PUBLISH best-effort + ConfigBroadcastSubscriber 收消息全量 reload、env 默认关，02 C1.79；三件零迁移，633→656 测试，2026-09-20；生产接线/worker 循环/多副本验证随 V2）**。
**Q136 客户效果批量 CSV 回填落地（内容详情页非 discarded 成品在单条岛下新增批量岛：纯前端 CSV 解析+行级校验+预览、整批走 Q128 records[] 客户通道 all-or-nothing、前端软上限 500 行、captured_at 转 UTC、空指标缺席；甲案不建后端端点/无迁移/无新依赖，check-content 扩守卫，后端 656 不变，2026-09-20，02 C1.80；服务端上传/Excel/异步导入/逐行回执随 V2）**。
**E＝docs/10 建表要素首轮 DBA 复核完成（一次性 PG16 容器 upgrade head 实测 627 列/210 索引/52 FK·UQ + ORM metadata 比对，58 表模型⇄迁移零漂移，仅订正 content_products.status String24→32、tenants.detail JSON→JSONB 两处旧稿笔误，2026-09-20，销「待 DBA 复核」挂账；纯文档零迁移）**。
**Q137–Q140 多副本/异步生产接线四件已落地（2026-09-20，02 C1.81–C1.84，八笔 feat/test 原子提交 + docs a339816，均已 push（远端头 a339816）；四件零迁移、env 默认关、不改 V1 单副本默认行为，后端 656→678 测试、eval 101/101、ruff 净、前端零改动）**：Q137 导出 queued/running 真后台 worker（Redis Streams 消费组 ExportWorker 水平并行 + `GET /jobs` 列表口；导出只读、下载按参数重渲染幂等，可安全并行）；Q138 restock requested 信号 after_commit XADD **只接生产侧**（restock 自动花真 token、Q87/Q89 已裁决 leader 单实例防双花，worker 主认领不切消费组、水平并行留 V2 重评花钱隔离）；Q139 SLA sweep/restock 两循环与两个手工 /run 由 leader_lock 换 leader_lease + checkpoint，锁一轮中途易主抛 LockLost 协作中止（两 _tick 静默返回、手工口 409）；Q140 配置缓存 reload_keys 单 key 增量失效 + 订阅断线指数退避（1s 起封顶 30s）。下游 PG 行级 fence/restock 消费组并行/真 Redis 多副本验证仍挂 V2。**Q141–Q143 V2 挂账收口三件已落地（2026-09-20，02 C1.85–C1.87，本地 dev 未 push；Q141/Q142 零迁移、Q143 迁移 0037 物理表 58→59，后端 678→699 测试）**：Q141 配置缓存 per-key `ConfigItem.version` 版本向量门控（reload_keys 拒旧快照回灌）+ 订阅器 TTL 兜底（env `LOOM_CONFIG_CACHE_TTL_SECONDS` 默认 300，无消息超 TTL 回源全量）；Q142 中台导出 limit/offset 分页 + env `LOOM_EXPORT_MAX_ROWS` 默认 10 万行数硬上限（JSON envelope 加 total/limit/offset/has_more、CSV 走 X-Export-* 响应头、同步/异步/下载同一上限，6 层原料包全量 JSON 不属中台导出仍 V2）；Q143 下游 PG 行级 fence 接 restock 花钱路径（新表 restock_claims + restock/fencing.py 花钱前 claim_request/提交前 fence_current 两段式，锁在模型调用期间易主则旧 leader 迟到成功写被条件更新拒、skipped_lost 不调模型，fence=None 行为同 Q87/Q139；SLA sweep 行级 fence、真 PG/Redis 多副本验证仍 V2）。
- **Q144 项目级 MVP / 可上线性评审与首发形态裁决（2026-09-21，02 C1.88；纯文档零迁移）**：三判准——docs-V1 内核（段1→6→10→11）✅ 达标且超额、公网多租户自助 SaaS ⛔ NO-GO、受控试点 ✅ 有条件 GO；负责人裁决首发＝**受控 private beta**（后端内网/网关后、平台代运营），P0-① 身份层**压到 V2 第一项**；上线前＝真 LLM 接线（B 待供应商/币种裁决）+ 首批平台主数据与阈值（C 待业务输入）+ 最小部署制品；Checklist 见 08 §2.2 Q144。
- **Q149 Checklist #3 阈值确认 + G2 基线（2026-09-21，02 C1.93；零迁移/零代码；后端 711 测试不变；裁决 C 第一部分销账）**：SLA 72h/黄 24h/红 48h、QC 0.85、cluster_line 0.9 沿用为 beta 口径（配置热更、beta 协议认账）；G2 以 12 字段为基线（余 6 维持【待补】）；付费档额度因平台代运营/无计费划掉（V2）；新增 [docs/19 主数据录入模板](docs/19_private_beta主数据录入模板.md)。**#6 现唯一阻塞＝#2 主数据业务回填**。
- **Q148 Checklist #1 真实 LLM 接线落地（2026-09-21，02 C1.92；零迁移；头仍 0037/59 表，后端 711 测试不变；裁决 B 完成）**：agnes OpenAI 兼容网关（`https://apihub.agnes-ai.com/v1`）+ agnes-2.5-flash + **USD** + 日预算 **50 USD/天**；8 chat 场景经管理端切真模型、Key Fernet 加密入库；真实 CAT-RECOG 实跑产 635/231 tokens 候选，成本看板按 USD 记录。挂账：单价待供应商给（先记 token/调用数）、ATOM-AFFINITY 维持 synthetic（agnes 无 embedding）、图像/视频随 V2；随片修复镜像漏打 `runtime/`。**#6 现仅阻塞于裁决 C（#2 主数据 + #3 阈值）**。
- **Q147 Checklist #6 staging 彩排（2026-09-21，02 C1.91；零迁移；头仍 0037/59 表，后端 710→711 测试；#6 验收本身不销账）**：新增 `infra/docker-compose.staging.yml` overlay（`!reset` 清空 infra 宿主端口、minio profile 排除，与本机 atlas 容器并存）；`-p loom-staging` 起栈 6 服务全 healthy、迁移至 0037、backend 对宿主不可达、备份产出、watchdog 无告警、真实 Chrome 验证两页 SSR 零 console 错误、段1 API 冒烟通过，`down -v` 全量清理。阻塞确认：彩排库 publish_slots=0/packages=0、仅合成模型，真实 final_id 全链仍待裁决 B（LLM 供应商/币种）与 C（主数据 + 阈值）。
- **Q146 private beta Checklist #5 最小备份与监控落地（2026-09-21，02 C1.90；零迁移；头仍 0037/59 表，后端 704→710 测试）**：db-backup sidecar 每日 pg_dump + 7 天保留（pgbackups 卷，一次性 PG16 实跑验证）、全服务日志 10m×3 轮转、watchdog 探活+可选 webhook best-effort 告警、frontend 补 healthcheck；异机备份/恢复演练/指标监控随 V2。
- **Q145 private beta Checklist #4 最小部署制品落地（2026-09-21，02 C1.89；零迁移；头仍 0037/59 表，后端 699→704 测试）**：backend Dockerfile + entrypoint（`alembic upgrade head` 先于 uvicorn）、frontend 三阶段 Dockerfile（Next standalone，next.config 加 `output`）、compose 增 backend（**不发布宿主端口**）/frontend（唯一发布口 3000）两服务与 service_healthy 依赖序、`.dockerignore`；5 例静态契约测试守卫。未含：TLS 独立网关容器（由部署环境在 frontend 口外终结）、备份监控（#5）。
> 续工时**先读本文件**，再读 [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md)（场景导航 + 治理规则）。历史/规格细节一律以 docs 内文档为准，本文件只保留"当前状态 + 活跃任务 + 最近变更"。

## 项目一句话

Loom = 私域内容生产白名单平台（SaaS 后台）：把"产品信息 → 可发布内容"拆成 **13 段链（CHAIN_13）** 可编排生产链，AI 只产候选、人工 Gate 裁决、`final_id` 唯一出口、反馈回流反哺知识。三端：用户前端 / 管理后端（13 模块）/ 系统后台 API。

## 项目文档（完整清单，单一事实源）

> 完整清单与一句话描述**只以本节为准**；[docs/README](docs/README_文档地图与治理.md) 只做场景导航，不重复清单。

| 文档 | 一句话描述 | 状态 |
|---|---|---|
| [docs/README_文档地图与治理.md](docs/README_文档地图与治理.md) | 文档地图（场景导航）+ 治理规则 + 信息保全映射表 | ✅ 权威 |
| [docs/01_PRD_产品需求规格.md](docs/01_PRD_产品需求规格.md) | 13 段链全量规格：各段功能/实体/规则/硬闸/数值约束 | ✅ 权威（Part A 全量） |
| [docs/02_决策记录_ADR_Q1-Q72.md](docs/02_决策记录_ADR_Q1-Q72.md) | 决策日志 Q1–Q72 + Q73（路线图合并）/Q74（18 字段双落库）/Q75（RBAC 端点角色收口）/Q76（skill7 通道接缝）/Q77（质量两件套形态）/Q78（WF-02 DIM-MERGE field_plan 候选）/Q79（WF-01 CAT-RECOG c1_recognition 候选 + intake 锚点；C7 Layer4 挂账）/Q80（WF-03 CONFLICT-PRECHECK atom_batch 整批候选，product_reviewer 准入）/Q81（WF-01 TYPE-MATCH c7_layer4 整 C7 解析请求，operations 准入，Q79-1 销账；真 LLM 生成挂 Q67）/Q82（Q67 首切片：模型底座+CAT-RECOG 真 LLM 试点；进程内同步复用 skill7 通道、outbound Key DB 加密+env 主密钥、Prompt DB 版本化，迁移 0014）/Q83（PWC-BUILDER 真 LLM 第二站：operations 显式端点、一次调用多候选、模型回带分剥离、跨候选保序去重，迁移 0015 纯种子）/Q84（C7 Layer4 TYPE-MATCH 真 LLM 第三站：operations 显式端点不自动串联、整结果单候选 c7_layer4、回显/脏提案 502、模型回带 confidence/score/source_route 剥离，迁移 0016 纯种子）/Q85（WF-02 DIM-MERGE 字段池方案真 LLM 第四站：operations 显式端点不自动串链、整方案单候选 field_plan、target 回显漂移/结构脏/line 14081 证据空白 502、confidence 契约透传而非剥离驱动 Q9/Q12、业务违规照落非合规池 Gate 拦截，迁移 0017 纯种子）/Q86（WF-03 原子补池真 LLM 第五站/末站：operations llm-expand 端点花 token 前预闸不自动串链、CONFLICT-PRECHECK chat+ATOM-AFFINITY embedding 双调用单 SkillRun 单 atom_batch 候选复用 Q80、模型自报亲和/成簇剥离本地按 1536 维向量余弦重算、ai_risk 契约透传词表只升不降，迁移 0018 带 schema：pgvector/capability/embedding 列/cluster_line 0.9 借 Q10 原文未给）/Q87（M8 restock_auto worker：进程内第二个 asyncio 循环默认 false opt-in + platform_admin 手工触发端点；requested 信号行永不 mutate、纯追加 succeeded/failed 的 llm_auto 子 run 反连接认领；仅自动消费 PWC-BUILDER 只产 pending_review 人工 Gate 不改；BudgetExhausted 区分预算瞬态与停用终态；无 Alembic 迁移头仍 0018）/Q88（入站一 Agent 一 Key 凭证半套：Q60b effect-callback 鉴权前置，迁移 0019 agent_api_keys，SHA-256 单向哈希明文仅签发返回一次、platform_admin 签发/列表/吊销、Bearer 401 依赖已备、Key 不绑租户；effect-callback 本体/effect 时序/孤儿队列/Q60a 回填随段13 P3 V2）/Q89（多副本单实例锁：Redis SET NX PX 两把循环级 leader 锁，env 默认关 fail-closed，手工 /run 同锁 409/503，无迁移）/Q90（restock 瞬态退避：restock_retry_state 游标表迁移 0020，指数退避封顶 30min，预算硬停永不转终态/上游错 10 次转终态，env 三参数，手工 /run 绕窗口）/Q91（编排并行 DAG 原语：app/core/orchestration/ needs 拓扑分层+同层 asyncio 并发+Gate 前驱闭包+fail-fast，env 信号量，无迁移）/Q92（M12 首片两个驾驶舱只读聚合：app/core/dashboards/ 成本天×模型×币种 30 天窗+审核积压快照/窗口产出，platform_admin、管理面 GET 首次 query actor，无表无迁移）/Q93（M12 第二片统一审核工作台：app/core/workbench/ 五型候选统一队列风险四档归一+置信度整批通过，迁移 0021 纯配置种子）/Q94（M12 第三片候选审核 SLA 挂接：skill7/review_sla 投递建单/裁决收口，五型 review_* todo_type，迁移 0022 五条时限种子）/Q95（M12 第四片租户管理+Onboarding：app/core/tenants/ 注册表+生命周期全审计+段1 准入+派生引导进度，迁移 0023 建表+存量回填）/Q96（前端 i18n+设计 Token 拍板：next-intl + `[locale]` + 三层 CSS Token，18 号文档 ✅）/Q97（客户外壳 + D5 八菜单导航骨架）/Q98（产品中心功能页 + GET /api/intakes 租户列表）/Q99（工作台功能页 + GET /api/intakes/overview 聚合）/Q100（中台 FCW final_id 单列 CSV 导出）/Q101（合规风控只读页 + GET /api/compliance/overview 聚合）/Q102（管理端独立布局 + 2 驾驶舱只读页）/Q103（统一审核台队列 + URL 服务端筛选 + 批量通过）/Q104（逐条裁决 confirmed/modified/rejected + payload 替换编辑器）/Q105（租户管理 + Onboarding 前端页）/Q106（录入单 15 态客户操作 + draft 资料编辑）/Q107（管理端运营队列 + 跨租户运营操作）/Q108（待办 SLA 看板）/Q109（管理面读端点鉴权审计 + 8 个裸 GET 同型补闸）/Q110（审核工作量页空态缺键热修）/Q111（CI workflow 落地：GitHub Actions）/Q112（11/13 契约文档据实现回填）/Q113（config/g2-candidates/skill-prompts 三族读口补闸收口）/Q114（客户 templates/settings 两页 + 五型 SLA 72h/黄窗占位）/Q115（基准 HTML 永久丢失收口 + G2 fid 工程定稿）/Q116（V2 P4 段12 首片：文章单语言最小闭环，迁移 0025/0026）/Q117（文档⇄代码一致性审计与纯事实回填：迁移 0021–0026 登记、段12 契约补登、状态类文档基线订正；4 项实现差异只挂账）/Q118（Q117 挂账四项收口：4 个管理面 GET 补 query actor 闸、publish_slots.gate 回填 V1 人工直编口径、product_spaces 两 FK 迁移 0027、CI 六→七 checker；另修 Alembic 版本表列长）/Q119（段12 多语言 Q58：content_languages 语言清单配置化、发布位市场∩产品目标语言交集、每语言独立成品唯一约束，迁移 0028）/Q120（段12 AI 质量分 Q57：第 8 场景 ARTICLE-QC 内嵌生成、纯 advisory 不阻断发证、阈值与重生成上限配置化，迁移 0029/0030）/Q121（复检第②项语义级检测：第 9 场景 ARTICLE-SEMANTIC-CHECK 内嵌、纯 advisory 落 review_hits.semantic，迁移 0031 纯种子）/Q122（客户内容页只读+审阅+Q56-a 人工改稿：无闸列表/详情/PATCH body、manual_resubmit 边、第八 checker，零迁移）/Q123（段1 客户目标语言控件：客户 PATCH + active 语言清单 GET，零迁移）/Q124（难产骨架作废回池 Q56-b：第七态 discarded 终态 + discard 端点 + needs-attention 队列 + partial unique index 回池，迁移 0032）/Q125（运营发布回填 Q60c：publish-info PUT + ready-to-publish 队列 + 发布三列、不新增 published 态，迁移 0033）/Q126（段13 effect-callback 入站第一片：Q88 Agent Key Bearer 验签回调 + effect_records 时序 matched/orphan 分流 + 运营只读孤儿队列/成品时序，metrics 缺“—”不补 0、(content_id,captured_at) 幂等，迁移 0034 新建 effect_records 表）+ 46 配置化清单（Q95 无新配置键）；Q127–Q143（C1.71–C1.87：段 13 认领/回填/管理台/批量岛、中台导出 JSON·异步任务·worker·分页上限、多副本基建与生产接线、配置版本 TTL、restock PG fence）见正文 | ✅ 权威（Part C 全量） |
| [docs/03_技术风险与红旗清单.md](docs/03_技术风险与红旗清单.md) | 红旗 S/A/B 三级（S 4 / A 11 / B 4）+ 裁决状态 + 业务方参考映射 | ✅ 权威（Part B 全量） |
| [docs/04_契约层_数据模型.md](docs/04_契约层_数据模型.md) | 实体与字段级数据模型（AI 落代码用） | ✅ 已提取 |
| [docs/05_契约层_API与状态机.md](docs/05_契约层_API与状态机.md) | API 契约 + 全部状态机定义 | ✅ 已提取 |
| [docs/06_契约层_WF与Skill协议.md](docs/06_契约层_WF与Skill协议.md) | Workflow / PT 协议 / Skill 注册表 / Agent 协议 | ✅ 已提取 |
| [docs/07_技术架构与工程规格.md](docs/07_技术架构与工程规格.md) | 模块边界 / 编排运行时 / 配置中心 / RBAC / 成本治理 / 工程建议 | ✅ 已提取 |
| [docs/08_迭代计划与任务包.md](docs/08_迭代计划与任务包.md) | 路线图（两套已裁决合并，Q73）+ V1/V2/V3 任务包（V1=段 1→6→10→11） | ✅ 已按合并裁决重排 |
| [docs/09_全景体系_展示口径.md](docs/09_全景体系_展示口径.md) | 展示口径全景：4 端/6 层/13 菜单/权限矩阵/中台/反馈 | ✅ 参考口径 |
| [docs/10_数据模型Schema_建表定义.md](docs/10_数据模型Schema_建表定义.md) | 35 实体建表要素（类型/约束/索引/关系） | 🟢 **首轮 DBA 复核已完成（2026-09-20，head 0036/58 表零漂移，见 §4）**；Q143 新增 restock_claims（头 0037/59 表）在该轮复核之后，已经 pg16 往返实测、待下轮纳入全量列复核 |
| [docs/11_API规范_OpenAPI.md](docs/11_API规范_OpenAPI.md) | effect-callback/白名单消费规格 + OpenAPI 通用规范 | 🟢 已落地接口已回填（Q112：消费/CSV/Key 治理）；effect-callback 入站与操作面已随 Q126–Q131 落地（认领/解绑/批量/管理台/客户回填 UI 齐；反哺校准随 V2；批量 CSV 导入已随 Q136 落纯前端形态，服务端上传/Excel/异步导入随 V2；中台导出 queued/running 真后台 worker 已随 Q137 落地 env 默认关、导出 limit/offset 分页与行数硬上限已随 Q142 落地）、中台接口随 V2/V3【待补】 |
| [docs/12_PT协议补齐清单.md](docs/12_PT协议补齐清单.md) | 7 定稿 + 35 占位候选 + 补齐模板 | 🟢 **Q115 定稿：7+35 即最终名单（保留【推断】标注），不再待 HTML 核对** |
| [docs/13_状态机迁移表.md](docs/13_状态机迁移表.md) | 11 组状态机迁移矩阵 | 🟢 productIntake15 迁移行已据 M1 实现补登（Q112）；G1 审核/合并/归档流随 V3、PWC 复核/待入库两行【待补】 |
| [docs/14_技术选型决策.md](docs/14_技术选型决策.md) | 6 项技术选型（模块化单体/自研编排器/PG+pgvector+Redis/DB 热更新/进程内合规模块/质量两件套） | ✅ **2026-09-13 已拍板定稿** |
| [docs/15_代码结构与脚手架.md](docs/15_代码结构与脚手架.md) | 目录/模块/可执行物落位 + 技术栈（FastAPI/Next.js/YAML） | 🟢 **已定稿（选型+技术栈已落）** |
| [docs/16_测试策略.md](docs/16_测试策略.md) | 分层测试 + 规则级测试清单 | 🟢 已补（台账至 Q143；12 号名单 Q115 已定稿，联动项收口） |
| [docs/17_部署与运维.md](docs/17_部署与运维.md) | 环境/CI-CD/多租户/监控说明（含中间件版本） | 🟢 **已定稿（选型已落）** |
| [docs/18_前端i18n与设计Token方案.md](docs/18_前端i18n与设计Token方案.md) | 界面 i18n（next-intl，V1 zh-CN）+ 设计 Token 三层模型（CSS Modules/CSS 变量，色值借 AntD5）；与 Q58 内容多语言划界 | ✅ **Q96 已拍板定稿（2026-09-16，02 C1.40），§2.4/§3.6 可直接落码** |
| [docs/handoff-archive-2026-09-20.md](docs/handoff-archive-2026-09-20.md) | handoff 进度归档（2026-09-20：Q128 customer-backfill、Q129 批量认领/解绑、Q130 管理端认领台、Q131 客户回填岛滚出条目原文；续写：Q132 中台导出 JSON/异步任务滚出条目原文） | 归档 |
| [docs/handoff-archive-2026-09-19.md](docs/handoff-archive-2026-09-19.md) | handoff 进度归档（2026-09-19：Q118–Q123 滚出条目；2026-09-20 续写：Q124/Q125/Q126/Q127 滚出条目原文） | 归档 |
| [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md) | handoff 进度归档（2026-09-18 首次：09-13 ~ 09-17 进度条目 + 待办 5/6 详细过程，逐条原文；只读历史，权威台账见 02/08/16） | ✅ 归档 |

> 根目录文档：README.md（项目介绍）/ AGENTS.md（AI 工作规范）/ CLAUDE.md（指向 AGENTS）/ handoff.md（本文件）/ git-commit-message.md（提交规范）/ CHANGELOG.md / CONTRIBUTING.md / MIGRATION_CONVENTION.md / BENCHMARK.md。

## 当前状态

- **阶段**：设计/文档已定稿（状态表见 docs/README §1）；代码骨架已搭（2026-09-13，按 15/17 定稿：backend FastAPI 13 模块包 + core 横切包 + Alembic + 分层 tests；runtime workflows/skills/agents/protocols 空目录；eval 两件套目录；middleplatform；frontend Next.js TS 最小页；infra docker-compose = PG16-pgvector/Redis7/MinIO）。**M1（段1 产品录入）+ M2（段2 C1 识别）+ M3（段3 字段池规划）+ M4（段4 字段下原子）+ M5（段5 PWC）+ M6（段6 PWS 冻结）+ M7（段10 合规清洗）后端切片已落地**：M1 productIntake15 + 录入 API + 迁移 0001；M2 信号权重/行业阈值配置、conf 三分支、ops 待办 72h、C7 四层兜底 + 迁移 0002；M3 来源路由表、方案提交/越界处置、WF-02 Gate、Q13 候选转正 + 迁移 0003；M4 Q48 统一词库、拓展批次/risk 双轨/三类冲突/approveAtomGuard、evidence 超时可复活、同义簇 alias、事实原子引用数=1、atom8 生命周期 + 迁移 0004；M5 contentGoals 字典、库容配置、WF-04 漏斗（预筛/Q48 合规检测/Q22 评分/限量 50）、Q23 疑重备选、HumanGate、Q71 按分消费+池健康、Q24 三元组去重/per-platform 冷却/全平台用尽、Q61 爆款手工标 + 迁移 0005；M6 pwsReadiness 5 门机械求值、全绿出 pws_ready 待办（BO-07 红线角色）、不可变快照物化（原子/PWC 成分行）、Q29 重冻三档、Q30 换版处置结构（实体随 M8/段12）、Q31 vN.0 版本管理、Q32 revoke 急停+作废后重冻新版、Q33 同租户指纹查重仅提示 + 迁移 0006；M7 同源 Q48 词库三关卡（layer 列承载 Q50 国家>平台>底座，同级 Q36 从严）、CCR 分市场清洗报告（block_required 一票否决）、降级建议人工 approval、Q49 敏感领域自动法审（48h 待办卡 Guard⑥）、Q51 词表生效即扫出 wordlist_rescan 强制重冻待办 + 迁移 0007；134 测试全绿（详见 08 §2.2 M1–M7 完成情况）；M11（段7/8 静态底表 + 段9 三包静态实例，实现序先于 M8——七 Guard 需要静态 PCP/三包可消费）发布位档案 CRUD（Q35，四维分明示人工评估/来源链接，14 平台目录原文待 line 1098-1221 基准 HTML 找回，不虚构种子）、Q34 目的权重矩阵（Σ=1 硬校验，fit_score 派生不落库/缺矩阵不凑分）、Q36 平台规则 4 层选择器（同级查重 409+overwrite 覆盖、match 高优先级覆盖同级从严、country 横切）、slotType 默认值（约 40 列明细待补）、Q39 四模板种子 + PCP 实例（Q40 17 键 Σ≤1.0、PT-PCP-V1.5 平台不共享唯一约束、Q42 人工直编通道）、Q45/Q52 三包 CSP/CSTP/CEP 静态实例（payload 定键、产品×平台×目的×包型一条 active、带 tenant/PS 供 Guard④⑤）+ 迁移 0008；147 测试全绿（详见 08 §2.2 M11 完成情况）；**M8（段11 FCW 组装/final_id 发证，Q73 V1 主链终点）后端切片已落地**：PT-FCW-ASM 七 Guard（PWS frozen/合规 clean 且 block_required=false【无报告不放行，实现补】/PCP+三包 active 且 gate=approved/六路 PS 一致 Q52/tenant 一致/法审条件放行 Q49/PWS active 版 Q53，不短路全量返回，任一败不产 final_id 且 409 回带明细）；E1.1 publishFCW 唯一出口（line 11036，全系统无第二写口，成功 fcw.issued/失败 fcw.assembly_blocked 全审计含发起人/时间/六路材料/Guard 结果）；Q54 临时评分 0.4/0.3/0.3（量纲统一 ×100，缺失 null+incomplete 不凑分、仅排序永不做门槛，待段13 校准）；Q55 任务驱动批量（product×platform×goal×count，自动配料/显式 slot_ids，单项失败隔离逐项留痕，任务同步跑完）+ 手动单条，operations 角色；同键去重 409，重冻旧版被 g1/g7 拦截；Q51 词表扫描回填 draft FCW（实体接入，V1 直接 published、draft 通道未开）+ 迁移 0009；165 测试全绿（详见 08 §2.2 M8 完成情况）；**M10 切片 a（配置中心底座）后端切片已落地**：`config_items` + `config_item_versions` 两表（迁移 0010，54 项 02 §C2 标量种子，typed JSONB/min·max·choices 校验/出处 Q 编号，uq(key,version)，种子即 v1），发布严格按 14 §2.4=新版本+writeAudit(config.update/rollback)+after_commit 进程内快照原子切换，回滚以新版本重发历史值、历史 append-only，`/api/admin/config` 五端点仅 platform_admin（403/404/422），176 测试全绿；**M10 切片 b（通用 SLA 引擎 + 定时调度）后端切片已落地**：`app/core/sla/`（引擎/作业/runner/调度器/路由）统一四作业（待办到期升级按类型分流审计/Q18 证据超时/Q24 冷却释放/Q51 未来生效词到点激活补扫），异常隔离；`POST /api/admin/sla/run` 手工触发、`GET /api/admin/sla/todos` 看板 sla_state（黄色仅法审 24h，热读 sla.yellow_hours）；lifespan 300s asyncio 循环；词表开放生效期录入+activated_at（迁移 0011）；183 测试全绿；**M10 切片 c（缓存引导 + 常量迁移）后端切片已落地**：新增 `knob(key)` 统一访问器（读 config_cache、未引导回落种子默认值），M1–M8 全部硬编码拍板值（c1 0.6/0.1/72h、c7 0.6、fieldpool 0.85/0.9/3-8/15-30、atom 20·50/0.5/7d、pwc 50/100·70·50/5min/100/0.6·0.4/0.5·0.5/1.0/0.8/7·3·14、pws 3·1/7d、ccr 48h、sla 黄 24h、fcw 0.4·0.3·0.3）迁为零参函数，lifespan 启动先 reload 缓存（失败告警不阻断），188 测试全绿、无新迁移；**M10 切片 d（RBAC 红线收口，Q75）后端切片已落地**：统一 `app/core/rbac`（角色常量/`require_any_role`/`PermissionDenied`→403，闸放服务层，系统内部调用不经 HTTP），手工 sweep（/sla/run、/admin/ops-todos/sweep）归 platform_admin、G1 类目+模板归 dictionary_admin、ccr/run 归 internal_compliance，并为信号权重/行业阈值（Q2/Q7）、来源路由（Q8）既有裁决补 operations 闸；pws/evaluate 与 atom revive 经裁决有意开放（Q75）；194 测试全绿、无新迁移；**M10 切片 e（skill7 AI 候选通道·WF-04 试点，Q76）后端切片已落地**：接缝五项经负责人拍板追加 Q76（02 C1.20）——范围=通道核心+WF-04 试点（`app/core/skill7/` + runtime WF-04/3 Skill YAML 注册表，未注册引用即拒）；外部投递边界（不建模型注册表/LLM/API Key，真接模型另立切片）；通用 `skill_candidates` + target_type 适配器（pwc_combo 复用 pwc/funnel，预筛/合规/评分/限量/Gate 不绕，ai_suggested 不持久化）；Q71 补货=requested run（source=restock_auto，5 分钟防抖，不造候选，consume 回带 restock_run_id）；投递 operations、裁决 product_reviewer（Gate 插槽角色驱动）；迁移 0012（skill_runs/skill_candidates，PG16 up/downgrade-1/up），203 测试全绿；**M10-Q（质量两件套轻量版，Q77）已落地（任务包闭合）**：`eval/runner.py` 独立回归 runner（target 适配器 `eval/targets.py`，YAML 案例，子集断言+容差+失败退出码 1），Evaluation Dataset 19 案 + Golden Cases 5 案（首批打 WF-04 确定性替身：PWC-SCORING Q22 公式、COMBO-VALIDATE Q48 词表；PWC-BUILDER 无实现不造案挂账），staging 闸门命令 `backend/.venv/bin/python eval/runner.py`（24/24），runner 3 条闸门单测进 backend；共 206 测试全绿、无迁移；**WF-02 字段池占位替换切片 1/3（Q78）已落地**：runtime WF-02 + FIELDPOOL-PLAN/DIM-SOURCE/DIM-MERGE 三 Skill YAML，DIM-MERGE 整方案单候选 field_plan，通道校验按 WF candidate_target 泛化（不再硬编码 pwc_combo），field_plan 适配器复用 fieldpool.submit_plan 落 pending_gate 走既有 WF-02 Gate，配套 DIM-MERGE eval 15 案（39/39）；**WF-01 冷启动识别占位替换切片 2/3（Q79）已落地**：runtime WF-01 + PARSE/UNDERSTAND/CAT-RECOG/TYPE-MATCH/MISSING-INFO/RISK-TAG 六 Skill YAML，CAT-RECOG 识别整结果单候选 c1_recognition（intake 锚点，Q79-4 迁移 0013：两表加 nullable intake_id、skill_candidates.product_space_id 改 nullable，PG16 up/downgrade-1/up 实测），Gate 角色 operations（Q79-3），适配器复用 modeling.submit_recognition（Q1 三分支机械逻辑一字不改，Q79-2），C7 Layer4 通道留挂账（Q79-1）；单候选约束泛化为 WF 步骤声明 single_candidate（Q78 field_plan 硬编码一并泛化）；配套 CAT-RECOG eval 14 案（合计 53/53）；**WF-03 字段下原子占位替换切片 3/3（Q80）已落地**：runtime WF-03 + ATOM-EXPAND/ATOM-CANON/ATOM-AFFINITY/CONFLICT-PRECHECK 四 Skill YAML（顺序四步，仅末步 CONFLICT-PRECHECK 挂 produces_candidates/candidate_target=atom_batch/single_candidate），四项接缝经负责人拍板追加 Q80（02 C1.24）——(1) 整批一条候选（payload=去 actor 的 BatchSubmitRequest 形态），适配器复用 atom.submit_batch 强制 source=ai，Q14/Q15/去重/维度/line 11189/Q17/line 840/空证据一项不绕，入库后逐条 approveAtomGuard/Q70/Q18/Q19/Q20 原样保留；(2) Gate 角色 product_reviewer（06 line 172）；(3) CONFLICT-PRECHECK 末步产出；(4) PS 锚点、无新迁移；router 补 404/409/422 atom 口径，适配器失败候选留 pending_review；配套四 Skill eval 31 案（Dataset 24 + Golden 7，7 个 target 覆盖 Q14/Q15/Q16/Q17/Q19/line 840），eval 合计 84/84；**WF-01 C7 Layer4 占位切片（Q81）已落地**：WF-01 增第二个候选产出步骤 TYPE-MATCH（c7_layer4 整 C7 解析请求单候选，intake 锚点）与第二道 operations Gate，四项接缝经负责人拍板追加 Q81（02 C1.25）——(1) TYPE-MATCH 产出、MISSING-INFO 仍非产出；(2) 整 C7ResolveRequest 单候选，适配器复用 modeling.resolve_c7（L1→L4/Q6/Q68 不绕，落 L1/2/3 提案不生效，L4 候选仍走 Q13 dictionary_admin）；(3) operations 准入，与 Q13 转正不合并；(4) eval 只打 c7 纯函数，Q68/错误分支留集成测试；Layer4 盲插顺手修为同名全局候选复用；配套 TYPE-MATCH eval 17 案（14+3，eval 合计 101/101）；现 238 测试全绿、ruff 通过、Alembic 0001–0013 无新迁移；M10 WF 替换切片四组完成（Q76 试点→Q78→Q79→Q80→Q81）；**Q67/Q82 模型网关+CAT-RECOG 真 LLM 试点切片已落地**：接缝四项经负责人拍板追加 Q82（02 C1.26）——(1) 范围=模型底座+CAT-RECOG 单站试点，其余四生成站点后续切片复用；(2) 业务端点进程内同步调模型网关，系统机器 Actor（system:llm-gateway，roles=[]，Q66）复用 skill7 投递通道落 pending_review，operations 人工 Gate 不改，异步 worker 留 M8；(3) outbound Key DB Fernet 对称加密 + env 主密钥 LOOM_MASTER_KEY（仅 env 不入库不入仓，注册表页轮换、明文不回显；入站一 Agent 一 Key 随 Q60）；(4) Prompt DB 版本化（skill_prompts/skill_prompt_versions，v0.1 起步 append-only、platform_admin+writeAudit，仓库只携迁移种子，不用 runtime prompt/*.md）；机械补登：场景键=skill_id、日预算按模型全局硬停不静默切供应商、fallback 仅注册态+人工、成本列 decimal+currency_code（币种【原文未给出，待补】）、仓内无真凭证测试用 synthetic 确定性替身；迁移 0014_model_gateway（ai_models/ai_model_keys/ai_scene_routes/skill_prompts/skill_prompt_versions 五表 + skill_runs 成本四列 + synthetic/CAT-RECOG 种子，PG16 up/downgrade-1/up 实测），`app/core/model_registry/` 九模块 + skill7 拆 deliver_generated_run（修 SkillRun.source 硬编码缺陷）+ 8 治理端点与 `POST /api/intakes/{id}/c1-recognition/llm-invoke`（403/404/409/422/502 齐）；253 测试全绿（+5 纯函数单测 +10 HTTP 集成）、ruff 通过；eval 不新增（仍 101/101，无确定性纯业务函数，16 §4 由集成测试覆盖）；**Q83 PWC-BUILDER 真 LLM 第二站切片已落地**：接缝四项经负责人拍板追加 Q83（02 C1.27）——(1) 仅 operations 显式端点 `POST /api/product-spaces/{id}/pwc/llm-build`，Q71 restock_auto 不自动消费（随 M8 worker）；(2) WF-04 PWC-BUILDER 未声明 single_candidate，一次调用产整批、每条组合各投一条 pwc_combo 候选，超 Q21 批 50 拒、同原子集跨候选编排层保序去重；(3) Prompt 机读变量白名单有界，前置闸=字段池 approved+approved 原子跨 ≥2 维（不足 409 不调模型），synthetic 只结构配对不造分，模型回带 logic/fit/weight 一律剥离，Q22b 子分缺失→score_incomplete→人工 Gate；(4) 预算/失败语义沿用 Q82；交付迁移 0015_pwc_builder_seed（纯种子：PWC-BUILDER 路由+Prompt v0.1，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/pwc_build.py`（第十模块）、synthetic 结构夹具，263 测试全绿（+2 单测 +8 集成）、ruff 通过、eval 仍 101/101；**Q84 C7 Layer4 TYPE-MATCH 真 LLM 第三站切片已落地**：接缝四项经负责人拍板追加 Q84（02 C1.28）——(1) 仅 operations 显式端点 `POST /api/intakes/{id}/c7/llm-resolve`，不与 CAT-RECOG llm-invoke 自动串联（链式/DAG V2）；(2) WF-01 TYPE-MATCH 已声明 single_candidate，整结果单候选 c7_layer4（payload=去 actor 的 C7ResolveRequest），复用 Q81 apply_c7_layer4→resolve_c7（L1/L2/L3 提案自然 no-op，仅 L4 写 g2_field_candidates）；(3) Prompt 变量有界白名单（类目/必填 fid/L1 approved 模板/L2 approved 兄弟计数/L3 active 覆盖+0.6 floor），前置闸=intake 存在+ai_recognizing+类目存在且 active+required_fids 过 c7.validate_fids（调模型前 422），synthetic 只按覆盖率缺口造结构提案不打置信分，模型回带 confidence/score/fid/status/source_route 一律剥离（只透传 field_name/非空 definition，confidence 落 NULL，source_route 枚举【原文未给出，待补】v0.1 不接模型值），category_id/required_fids 回显漂移、脏提案 502，空 l4_proposals 合法（裁决后落 L3），Q13 dictionary_admin 转正 Gate 不绕；(4) 预算/失败语义沿用 Q82/Q83；交付迁移 0016_type_match_seed（纯种子：TYPE-MATCH 路由+Prompt v0.1，无 schema 变更，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/c7_resolve.py`（第十一模块）、synthetic 覆盖率缺口夹具，276 测试全绿（+2 单测 +11 集成）、ruff 通过、eval 仍 101/101；**Q85 第四站 DIM-MERGE 字段池方案**：场景键=DIM-MERGE（WF-02 唯一 field_plan 产出步骤，FIELDPOOL-PLAN 不产候选、DIM-SOURCE 外部连接器属 V2），端点 `POST /api/product-spaces/{id}/field-pools/llm-plan`（触发体 `{target_atom_min?, target_atom_max?, actor}`，缺省 15/30），池态闸=无池或 rejected（否则 409，与 submit_plan 同口径），整方案单候选复用 Q78 apply_field_plan→submit_plan，target 回显漂移/role·route·fid 脏/证据空白 502，confidence 是 PT-FP-PLAN 契约 AI 字段（Q9/Q12 消费）故 0..1 透传而非剥离、synthetic 固定 0.9，业务违规（below_min/缺 product_attribute/敏感缺 risk_control）照落非合规 pending_gate 池、Gate approve 409；交付迁移 0017_dim_merge_seed（纯种子 3 行，7 变量，PG16 up/downgrade-1/up 实测）、`app/core/model_registry/field_plan.py`（第十二模块）、synthetic 合规整方案夹具，289 测试全绿（+2 单测 +11 集成）、ruff 通过、eval 仍 101/101；**Q86 第五站/末站 CONFLICT-PRECHECK+ATOM-AFFINITY 原子补池**：端点 `POST /api/product-spaces/{id}/atom-batches/llm-expand`（触发体 `{batch_size?, actor}`，缺省 Q14 敏感 20/非敏感 50），花 token 前预闸 PS/池 404、gate≠approved 409、Q15 停拓（仅 AI 批次）409；双调用=CONFLICT-PRECHECK chat（单 atom_batch 候选复用 Q80 apply_atom_batch）+ ATOM-AFFINITY embedding（capability=embedding 不挂 Prompt 不产候选），合并单 SkillRun（input 求和）；模型自报 affinity/cluster_id/score 剥离，本地进程内余弦 max（无基 None）+批内同维并查集（atom.cluster_line=0.9 借 Q10 同义线原文未给【待补】），ai_risk 枚举校验透传、词表只升不降仍在 submit_batch；结构脏/向量行数维度不符 502 不留痕，业务违规不 502；交付迁移 0018_atom_affinity_embedding（首个带 schema 的 LLM 切片，pgvector 镜像 up/downgrade-1/up 实测）、`app/core/model_registry/atom_expand.py`（第十三模块）、gateway/drivers embed 通道、synthetic-embedding 夹具、portable Vector TypeDecorator，306 测试全绿（+4 相似度纯函数单测 +3 网关单测 +10 HTTP 集成）、ruff 通过、eval 仍 101/101；**Q87 M8 restock_auto worker（Q71 补货闭环，无迁移纯代码切片）已落地**：四接缝经负责人拍板追加 Q87（02 C1.31，均选推荐项）——(1) 形态=进程内第二个 asyncio 循环 RestockWorker（同构 SweepScheduler，V1 单进程；docs/07 §7.5 队列选型裁决 V1=进程内轮询，Redis Streams/多副本锁 V2），LOOM_RESTOCK_WORKER_ENABLED 默认 false opt-in（自动花真 token）/间隔 60s/每轮 ≤20（env 实现值非 Q9 旋钮），管理端 POST /api/admin/restock/run（platform_admin，同 Q75）；(2) 纯追加认领：requested/restock_auto 信号行永不 mutate，成功追加 succeeded/llm_auto 子 run（input.restock_request_id 回链，候选挂子 run），终态失败追加 failed/llm_auto 子 run（Q71 预留 failed 首次启用），认领=反连接子 run，瞬态不留子 run 下轮重试；(3) 范围仅 PWC-BUILDER（最早优先），只产 pending_review、product_reviewer Gate 一字不改，内部入口 invoke_pwc_build_for_restock（system:llm-gateway roles=[]，OPERATIONS 闸只留 HTTP /pwc/llm-build），全量预闸/白名单同 Q83，不自动串 ATOM-EXPAND；(4) 终态（PS 消失/池未批/not-ready/结构脏/ModelConfigError/停用无 fallback）failed 子 run+restock_failed(terminal)，瞬态（新增 BudgetExhausted(ModelUnavailable) 日预算硬停、GenerationUpstreamError）restock_deferred 下轮重试，HTTP 409 映射不变，不静默切供应商，V1 无重试退避；每信号独立会话/提交；新增 app/core/restock/（worker/router）、pwc_build 拆 HTTP 包装/内部入口/_run_pwc_build（Q83 手动路径零变化）、main lifespan 启停；315 测试全绿（+9 集成，无新单测）、ruff 通过、eval 仍 101/101 不新增；M10 余量：五个真 LLM 站点已全部接通（Q67 站点 backlog 闭合）、Q71 restock_auto 自动补货已闭环，余入站 Key（Q60 效果回调，建议下一棒）、编排并行/DAG（V2）、多副本广播/调度+补货锁与瞬态重试退避【挂账】；**Q88 入站一 Agent 一 Key 凭证半套（Q60b effect-callback 鉴权前置）已落地**：四接缝经负责人拍板追加 Q88（02 C1.32，均选推荐甲案）——(1) 范围只做 Key 半套：新表 agent_api_keys + platform_admin 治理端点 + Bearer 验签函数/依赖 + 审计，POST /api/effect-callback、effect_records 时序、孤儿队列、Q60a 运营回填、customer-backfill 全部随段13/P3（V2）不提前落，V1 无受保护业务端点为显式挂账（同 Q82 底座先行）；(2) SHA-256 单向哈希（明文 loom_+token_urlsafe(32) 仅签发响应返回一次，DB 存 hash unique+索引与前 12 字符展示前缀；刻意区别 outbound Fernet 可逆加密——入站只需比对，库泄露不暴露可用 Key），吊销=active/revoked 状态位+revoked_by/at，append-only 不物理删除；(3) Key 不绑租户（平台级凭证，未来归属由 content_id→FCW.tenant_id 解析；单租户绑定原文未给【待补】不臆造 tenant 列）；(4) 新横切包 app/core/api_keys/（models/service/schemas/router）与 model_registry 并列，路径 /api/admin/agent-keys（201 secret 仅一次/列表默认隐吊销且无 secret/吊销幂等 404/越权 403/空名 422），verify_key 未知·空·吊销统一 None 防探测、命中盖 last_used_at，require_agent_key 401 依赖留待段13，审计 agent_api_key.issue/revoke（tenant _platform）；交付迁移 0019_agent_api_keys（pg16 一次性容器 up/downgrade-1/up 实测，未触 atlas-pg）、332 测试全绿（+17 集成）、ruff 通过、eval 仍 101/101 不新增；**Q89 多副本单实例锁已落地（Q87 挂账“多副本单实例调度+补货锁”销账，env 默认关不改 V1 单副本形态，无迁移纯代码切片）**：四接缝经负责人拍板追加 Q89（02 C1.33，均选推荐甲案）——(1) Redis SET NX PX + uuid owner token + Lua 比对释放/续约 + 看门狗 TTL/3 续约，新横切包 app/core/locking/，崩溃靠 TTL 60s 放锁；(2) 循环级两把命名锁 loom:lock:sla-sweep / loom:lock:restock-worker，非持锁副本跳轮，不做每信号行细粒度认领（Q87 requested 行不 mutate 不冲击；并行消费留 V2）；(3) LOOM_DISTRIBUTED_LOCK_ENABLED 默认 false，开启后 Redis 故障 fail-closed 跳轮+error 日志，绝不无锁双跑；(4) 两个手工 /run 端点同锁，抢不到 409、Redis 故障 503，RBAC 403 在前；看门狗发现锁易主/续约失败仅 error 不打断在跑一轮（fencing token 退场挂账 V2）；345 测试全绿（+13 集成，内存 FakeRedis）、ruff 通过、eval 仍 101/101 不新增；**Q90 restock 瞬态失败重试退避已落地（Q87/Q89 挂账销账，迁移 0020）**：四接缝经负责人拍板追加 Q90（02 C1.34，均选推荐甲案）——(1) 重试态落新表 restock_retry_state 游标（request_id PK/attempts/next_attempt_at/last_reason，requested 信号行永不 mutate 不变、瞬态不留子 run 不变，成功/转终态删游标）；(2) 指数退避 60×2^(n−1) 封顶 1800s，BudgetExhausted 永不转终态（UTC 日界自愈）、GenerationUpstreamError 连续 10 次约 17h 转终态 failed 子 run+终态审计转人工并被反连接排除；(3) env 三参数（BASE 60/MAX 1800/MAX_ATTEMPTS 10）运维值非 Q9 旋钮；(4) 手工 /run honor_backoff=False 绕窗口但瞬态仍计数重排窗口；迁移 0020_restock_retry_state（pg16 一次性容器实测，未触 atlas-pg）、351 测试全绿（Q87 旧用例更新 +6 集成/纯函数）、ruff 通过、eval 仍 101/101；**Q91 编排并行 DAG 原语已落地（14 §2.2 复杂 DAG 二期原语部分销账，纯代码无迁移，头仍 0020）**：四接缝经负责人拍板追加 Q91（02 C1.35，均选推荐甲案）——(1) 只落原语不接真实流：新包 app/core/orchestration/（planner Kahn 分层+五类校验/engine 同层并发+屏障+fail-fast），needs 可选、无 needs 退化顺序链（WF-01..04 零改动），五站点显式端点与 Q84 不串链口径不变，DIM-SOURCE 七路 fan-out 随 V2 连接器接入；(2) 同层任一失败取消兄弟整轮失败、引擎不自动重试（重试归 Q90 一类调用侧）；(3) env 信号量 LOOM_ORCH_MAX_CONCURRENCY=4 运维值非 Q9 旋钮、预算硬停不绕；(4) skill7 Gate 前驱闭包不跨人工裁决、不建新表不写 skill_runs，处理器注入、orch_run_id+wf_id 经 NodeContext 关联；371 测试全绿（+20 单测 test_orchestration_dag.py）、ruff 通过、eval 仍 101/101；**Q92 M12 首片·两个驾驶舱只读聚合 API 已落地（Q73 收敛的 Token 成本+人工审核，02 C1.36 四接缝均选甲案，无迁移头仍 0020）**：新包 app/core/dashboards/（service 纯 SELECT+router），`GET /api/admin/dashboards/token-cost|review-workload` 仅 platform_admin、管理面 GET 首次 query 携带 actor（actor_id 必填 422/roles 重复，依赖判 403，真认证 V2 自报口径同写端点）；成本=默认近 30 天可选 from/to 半开 UTC，succeeded 且 model_id 非空按天×模型×币种+技能聚合（embedding Q86 起计入、价 0 计调用数），failed 单列不计费，多币种分组不换算（币种【待补】，单价归 Q67 不重算）；工作量=积压快照（pending_review 按 target_type+最老等待秒数，待办 open 未到期/overdue/escalated）+窗口产出（reviewed_at/resolved_at），Q70 派单/10% 抽检/6 场景细分二期不做；不建表/不物化/无调度器/无租户过滤（当时租户实体不存在；Q95 已落 tenants 注册表，Q33 误链已于 02 C1.39 勘误）；**Q92 时点 378 测试全绿**（+7 集成 tests/integration/test_dashboards_api.py）、ruff 通过、eval 仍 101/101。**此后 M12 余片与 V2 P4 首片继续落地（Q93–Q116，逐片记录见 02 C1.37–C1.60）**：工作台列表+批量（Q93）、候选 SLA 挂接（Q94）、租户管理+Onboarding（Q95，迁移 0023）、客户前端 Q96–Q101、管理端 Q102–Q110、CI（Q111）、契约回填（Q112）、三族读口补闸（Q113）、客户 templates/settings 与五型 SLA 占位（Q114）、基准 HTML 收口+G2 工程 fid（Q115，迁移 0024）、段12 P4 首片（Q116，迁移 0025+0026）。**当前基线（2026-09-20 复跑）：后端 699 passed（Q127/Q128 +16 集成、Q129 再 +11、Q132 +5 单测 +10 集成、Q133–Q135 再 +23 集成〔fencing 8 + streams 8 + broadcast 7〕、Q137–Q140 再 +22 集成〔导出 worker 10 + restock 入流 3 + lease 协作中止 5 + 缓存增量/退避 4〕、Q141 再 +6 集成〔配置版本门控/TTL，test_config_broadcast 11→17〕、Q142 再 +8〔单测 2+集成 6，导出分页/硬上限〕、Q143 再 +7〔fencing 原语 4+restock worker 端到端 3〕；effects 域合计 64 片＝test_effect_records 24 单测 + callback 13 + claims 19 + customer-backfill 8 集成）、ruff 净、eval 101/101（golden 21）、Alembic 头 0037_restock_claims（业务物理表 59；Q128–Q131、Q133–Q135、Q137–Q142 共十三片零迁移、Q143 迁移 0037 restock_claims〔+1 表〕；0036 export_jobs 任务表、0035 effect_claims 映射表+两溯源列、0034 effect_records 时序表、0033 发布三列、0032 discarded partial index，0032–0037 均经 pgvector/pgvector:pg16 容器 up/downgrade-1/up 实测（0037 restock_claims 五列/pkey，业务表 58→59）；0031 语义场景纯种子、0029/0030 质量两列与 QC 种子、0028 content_languages）、前端 tsc + 8 个 checker（CI 八 checker 全接线，Q130 扩 check-admin 至 sidebar 8 项、Q131/Q136 扩 check-content 回填与批量导入守卫，无新 checker 文件，token 仍 86）+ next build 通过、token 86。** 余量：付费档额度·计费订阅·团队账号（V2/待补）、V2 真实 fan-out（DIM-SOURCE 七路连接器），段13 入站与操作面 Q126–Q131 已落（Agent 推送/孤儿认领/客户回填/批量/解绑/管理台/客户回填 UI 齐），续片仅余效果反哺 KUP 与 Q54/Q57/Q61/Q65 校准算法（**口径原文未给【待补】，禁止臆造**）、~~回填批量表格/CSV~~ ✅ **Q136 纯前端批量岛已落**（02 C1.80，2026-09-20；后端 records[] 整批通道零改动，服务端上传/Excel/异步导入随 V2）；~~Streams 细粒度认领/配置缓存广播/fencing token~~ ✅ **Q133–Q135 原语已落、Q137–Q140 生产接线已落**（02 C1.77–C1.84；导出 worker 完整接消费组、restock 仅生产侧入流、两循环 leader_lease 协作中止、缓存单 key 增量+断线退避、~~配置版本号/TTL 防陈旧~~ ✅ Q141 已落（02 C1.85 版本门控+TTL）、~~fence 下游 PG 条件更新（restock 花钱路径）~~ ✅ Q143 已落（02 C1.87，restock_claims 两段式 claim/fence_current，迁移 0037）；restock 消费组水平并行〔花钱隔离〕/SLA sweep 行级 fence/真 Redis 多副本验证随 V2）；新任务仍需负责人点工。仓库**已 git init**（分支 `dev`，`main` 为主干）。
- **文档体系**：原唯一事实源 `Loom_核心业务主链梳理_v3.md` 已于 2026-09-13 归档删除，内容 **100% 迁入 docs/**（231 段段落级核验零丢失）；01–09 为 v3 忠实切分（Part A/B/C/D/E 全覆盖），10–17 为同日新增工程骨架文档，18 为同日新增前端 i18n/设计 Token 工程建议（Q96 已拍板 ✅）。
- **权威口径**：13 段链（CHAIN_13，01 PRD）为唯一权威；03-A~E 展示口径（09）并存不混用，冲突以 01 为准。
- **已定稿事实基线**：Q1–Q72 均已有决策记录（02，含正式采纳与"临时采纳"两类）；S 级红旗 4 条经 Q34/Q60 等解决（02 收官注、03 已标销账）。**M0 已于 2026-09-13 完成**：Q2/Q22/Q60(a/b/c) 转定稿（02 新增"✅定稿"状态与转正注记）；新裁 Q22a（分项打分口径）/Q22b（多样性函数 + AI 失败兜底），评分细则同步 01 段5、06 §1.1、§C2；业务方 3 份素材采纳经负责人确认（02 C1.16）；段 1-6 缺口实体 ProductSpace / g2FieldCandidates 已补字段（04/10）。挂账 4 子项（不阻塞 M1）：PS.lifecycle 迁移触发、~~18 通用字段值归属（M1 前定）~~ ✅ Q74 已裁决（两处落库 profile + profile_snapshot）、候选实体多租户可见性、候选状态枚举。**两套路线图口径冲突已于 2026-09-13 由负责人裁决合并（Q73，08 §1.3）：A7 为骨 + D9 厚度，段 12 后置 V2，V1 驾驶舱只留 Token 成本 + 人工审核 2 个；08 §2 已重排。至此原文挂起的待裁决事项全部清零。** 技术选型 6 项**已拍板定稿**（14，2026-09-13：模块化单体 / 自研注册表编排器 / PG+pgvector+Redis / DB+广播热更新 / 进程内合规模块 / Evaluation+Golden 两件套），技术栈 = FastAPI + Next.js + YAML 编排；下游 15/17/08/10/06 已同步刷新。
- **V1 范围（Q73 合并后权威）**：0–3 月 / 5–10 客户；主链段 1→6→10→**11**（到 `final_id` 发证闭环，**不含段 12**）；段 7/8 仅 FCW 必需的静态底表基础版（M11，无动态信号/fit_score 学习）；横切 skill7/writeAudit/RBAC/配置中心/SLA + 审核工作台 + 租户/Onboarding + 前端 8 菜单基础版 + CSV（M12）+ M10-Q 质量两件套 + 2 驾驶舱。段 12/13 与段 7/8/9 完整版在 V2。

## 最近进度（2026-09-19 ~ 2026-09-21）

- **Q149 阈值确认 + 录入模板（零迁移/零代码；纯文档 89b0666 + docs 提交，本地 dev 未 push；后端 711 测试不变）**：负责人 2026-09-21 逐项拍板：阈值沿用占位值（72h/24h/48h、0.85、0.9）、G2 以 12 字段为基线、付费档额度划掉；docs/19 模板供业务方回填主数据。**#6 仅余 #2 主数据**。
- **Q148 Checklist #1 真实 LLM 接线（零迁移；feat 1279e21/fix bafc529/test ef975cb·c117755，本地 dev 未 push；头仍 0037/59 表，后端 711 测试、ruff 净，前端零变化）**：负责人 2026-09-21 提供 agnes 供应商信息并完成裁决 B（USD、50 USD/天、全部场景）。staging 实测真实 CAT-RECOG：635/231 tokens、候选 pending_review。**#6 现仅卡裁决 C**。缺口挂账：模型单价（先 0，记 token/调用数）、ATOM-AFFINITY 用 synthetic（agnes 无 embedding）、图像/视频 V2。
- **Q147 Checklist #6 staging 彩排（零迁移；feat 4e01912/test ef25c74，本地 dev 未 push；头仍 0037/59 表，后端 710→711 测试〔overlay 契约 +1〕，ruff 净，前端零变化）**：负责人 2026-09-21「跑吧」批准彩排。新增 staging overlay（`!reset []` 清 infra 端口避 atlas、minio profile 排除），真实 compose 栈 6 服务 healthy，迁移/备份/探活/前端 SSR（真实 Chrome 零 console 错误）/段1 API 冒烟全通过，`down -v` 清理。**#6 不销账**：彩排库 publish_slots=0、packages=0、仅合成模型，真实 final_id 全链仍待 B（LLM 供应商/出站密钥/日预算/币种）与 C（平台主数据 + 阈值）。
- **Q141–Q143 V2 挂账收口三件（Q141/Q142 零迁移、Q143 一迁移；feat/test/chore 原子提交 hash 见各片，**本地 dev 未 push**；Alembic 头 0036→0037_restock_claims/物理表 58→59，后端 678→699 测试〔Q141 +6 集成、Q142 单测 +2·集成 +6、Q143 原语 4+worker 端到端 3〕/ eval 101·golden 21 不变，ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人 2026-09-20「一口气给我搞了」授权连续推进，接缝按推荐甲自行拍板（同 Q118–Q140 先例），三片各销一条既有 V2 挂账。① **Q141 配置缓存版本号门控 + TTL 兜底（02 C1.85，feat 692b435/test 63d9b4c，零迁移）**：销 C1.84「配置版本号/TTL 防陈旧」——cache 快照携带 per-key `ConfigItem.version` 版本向量，`reload_keys` 单调门控（DB 行版本更旧即 skipped 拒旧快照回灌、显式删除 removed、新版本 accepted，返回三计数），全量 reload 仍以 DB 整体替换；after_commit 本进程 apply 携权威发布版本；订阅器加 TTL 兜底（env `LOOM_CONFIG_CACHE_TTL_SECONDS` 默认 300，无失效消息且超 TTL 回源全量 reload，仅曾成功装载且广播门控开启时触发，单副本 after_commit 行为不变）；cache 加 loaded_at/version_of；+6 集成（test_config_broadcast 11→17）。② **Q142 中台导出分页 + 行数硬上限（02 C1.86，feat 992b7e8/test 8a0f820，零迁移）**：销「导出大结果集分页/行数硬上限」——env `LOOM_EXPORT_MAX_ROWS` 默认 100000（运维防护旋钮、非 Q9 业务阈值），fcw.csv/fcw.json 加 limit(1..cap)/offset(≥0)、limit 超限 422、不传 limit 回落硬上限截断；新增 count_exported_final_ids 与 fetch_export_page 返回 total/limit/offset/has_more/truncated（total 不受分页影响、truncated＝未显式分页却有更多行）；JSON envelope 追加 total/limit/offset/has_more（count 仍本页行数、向后兼容加字段），CSV 正文守 Q100 严格单列、分页信息走 X-Export-Total/Limit/Offset/Has-More/Truncated 响应头；同步 job/Q137 异步 worker/下载全走 page 受同一上限、export_jobs 不加分页列、审计 detail 加 total/limit/truncated，render_job 改三元组；**6 层原料包全量 JSON 明确不属中台导出**（Q100/Q132 中台只 final_id、6 层包属台内卡片口径 docs/09:87）仍 V2；+8（单测 2/集成 6，test_export_render 5→7、test_exports_api +6）。③ **Q143 下游 PG 行级 fence 接 restock 花钱路径（02 C1.87，chore fe2a62a/feat ab672b8/test 78ac7d4，迁移 0037）**：销 C1.77/C1.83「业务游标携带 fence 做条件更新拒旧持有者迟到写」中最关键的 restock 部分——新表 `restock_claims`（每 requested 信号至多一行记当前获准花钱的最大 fence：request_id PK/fence/claimed_by/claimed_at/updated_at 五列）、新模块 `restock/fencing.py` 两段式：`claim_request` 花钱**前**独立短事务认领（无行插入/更小 fence 单调升到自己＝acquired、同 fence 重入 held、已存在更大 fence＝lost，先提交让易主新 leader 可见），`fence_current` 在成功结果**同一业务事务**内 `UPDATE ... WHERE request_id AND fence=:token`、rowcount=0 即被抢占回滚整轮；worker 花钱前认领 lost 直接 skipped_lost **不调模型**、提交前门闭回滚 succeeded 子 run/候选/审计；run_restock 透传 lease.fence/owner_token、report 加 skipped_lost，定时 _tick 与手工 /run 均透传；fence=None（V1 默认锁关）不写表不开门、行为同 Q87/Q139；外部模型已花费用不可撤销，fence 只保下游 DB 不重复；迁移 0037 经一次性 pgvector/pgvector:pg16 容器（55432 避 atlas-pg）upgrade head→downgrade-1（drop 表/to_regclass 空）→upgrade 重建往返实测、五列/pkey 正确，业务表 58→59；**测试替身边界如实入账**：sqlite 内存库单连接无法在业务事务打开时跨会话并发提交接管，worker「花钱中途易主」端到端用 monkeypatch 关门确定性验证回滚，跨连接接管的真实条件 UPDATE 语义由新 test_restock_fencing.py 4 个顺序事务原语测试覆盖，+7（原语 4/worker 3，test_restock_worker_api +3、test_lease_cooperative_abort fake_process 补 **_kwargs）。**仍挂 V2**：SLA sweep 写路径 PG 行级 fence、真 PG 多副本+真 Redis 锁易主→中止/拒写集成验证（当前全 FakeRedis/FakeStreams/FakeBroadcast 内存替身）、restock worker 主认领切消费组水平并行（花钱隔离，Q87/Q89 leader 单实例防双花需重新裁决）、6 层原料包全量 JSON 体量治理、多 ExportWorker 并发度/批量配置、服务端导入（multipart/Excel/逐行回执）。
- **Q137–Q140 多副本/异步生产接线四件（纯代码零迁移；A feat 96d3d58/test f8e1f28、B feat 820b876/test 3d26722、C feat 1ea9f6d/test 45fe6f5、D feat 310789a/test 743748b 八笔 feat/test 原子提交 + docs a339816，**均已 push（远端头 a339816）**；Alembic 头仍 0036/物理表 58，后端 656→678 测试〔A +10/B +3/C +5/D +4 集成〕/ eval 101·golden 21 不变，ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人 2026-09-20 授权「A+B+C+D V2 多副本/异步生产接线族」，把 Q133–Q135 原语与 Q132 预留态接到生产路径（纪律：env 默认关、FakeRedis/FakeStreams 内存替身、不改 V1 单副本默认行为、四片零 PG 迁移）。① **Q137 导出真后台 worker（02 C1.81）**：导出只读、下载按参数重渲染幂等，可安全用消费组水平并行、无需 leader 锁；models 加 JOB_QUEUED/RUNNING/TERMINAL（String(16) 无 PG enum），service 加 EXPORT_STREAM/组/死信与 create_queued/enqueue/process/fail/list_jobs（保留同步 create_export_job），新建 exports/worker.py `ExportWorker`（read_new delivery=1 + reclaim_pending 崩溃接管，成功 ACK/未超限留 PEL/超限 move_to_dead 置 failed/终态重复幂等 ACK/job 不可见留 PEL），router POST 门控分同步·异步 + `GET /jobs` 列表口 + download 对 queued/running 409；env `LOOM_EXPORT_WORKER_ENABLED` 默认关、`LOOM_EXPORT_STREAM_BLOCK_SECONDS`=5.0；FakeStreamsRedis 抽共享 tests/integration/stream_fakes.py；+10（test_export_worker 7 + api 3；测试期 min_idle_ms=0 同 tick 误 reclaim，死信用例改两 worker）。② **Q138 restock 信号 XADD 生产侧（02 C1.82，范围收敛）**：restock 自动花真 token、Q87/Q89 已裁决 leader 单实例防双花，与消费组水平并行语义冲突，故**只落生产侧入流**（新 restock/notify.py，maybe_request_restock after_commit event.listen once=True best-effort XADD、DB requested 行唯一事实源/DB 轮询兜底/XADD 失败只告警不影响落库），**worker 主认领不切消费组**（与 Q137 只读幂等接完整 worker 形成对照），水平并行留 V2 重评花钱隔离（按产品空间分区串行/预算 fencing）；env `LOOM_RESTOCK_STREAM_ENABLED` 默认关；+3（test_restock_stream_notify）。③ **Q139 两循环 leader_lease 协作中止（02 C1.83）**：四持锁点（sla/scheduler._tick、restock/worker._tick、sla/router run_sweep、restock/router run_restock_once）leader_lock→leader_lease，run_jobs/run_restock 加零参 checkpoint=lease.raise_if_lost（run_jobs 在每作业独立会话**之外**防被作业内 BLE001 except 吞、run_restock 每条 requested 信号处理前），锁一轮中途易主抛 LockLost 协作中止；两 _tick except LockLost warning 静默返回不拖垮循环、两手工 /run LockLost→409（LockUnavailable 409/LockBackendError 503 不变）；门控关 lease 恒 held（fence=None）行为同 Q89、leader_lock 保留；复用 LOOM_DISTRIBUTED_LOCK_ENABLED 无新 env；+5（test_lease_cooperative_abort；全量回归暴露两处旧测试假函数签名缺 checkpoint，test_distributed_locks 三处与 test_scheduler 假函数补 **_kwargs，test_scheduler 那处 amend 入本片 test 提交，曾致 677+1 failed）。④ **Q140 单 key 增量失效 + 断线退避（02 C1.84）**：cache.reload_keys（in_ 一次查回、DB 仍在覆盖/**DB 已删 key 从快照 pop**、未涉及保持、原子合并），poll_once 解析 payload——`*`/缺失全量 reload、具体 key 走 reload_keys([key])（bytes/str 兼容），consecutive_failures 指数退避 backoff_base 1s×2^(n-1) 封顶 30s、wait_for(stop.wait()) 可立即唤醒、一次成功重置；**实测交互 bug**：首版删光健康路径 sleep 致替身 get_message 不真正阻塞而纯忙转、连 asyncio.wait_for 的 5s 超时定时器都被饿死（最小复现脚本 hang），修为健康路径每轮保留一次 sleep(0) 零拍让出（真 Redis get_message(timeout=) 自身阻塞，仅替身/立即返回路径需要）；复用 LOOM_CONFIG_CACHE_BROADCAST_ENABLED 无新 env；+4（test_config_broadcast 7→11）。四件均无 PG 迁移（头仍 0036/表 58）、新 env 按 Q135 先例不入 backend/.env.example（该文件只留 LOOM_REDIS_DSN）、前端零改动。**仍挂 V2**：下游 PG 行级 fence 条件更新、restock worker 主认领切消费组（花钱隔离）、真 Redis/多副本集成验证（当前全 FakeRedis/FakeStreams/FakeBroadcast 内存替身）、导出大结果集分页/行数硬上限/6 层原料包全量 JSON/多 ExportWorker 并发度配置、配置版本号/TTL 防陈旧。

- **E docs/10 建表要素首轮 DBA 复核（纯文档，零代码/零迁移；docs 单笔提交 e5adef2、**已 push**；head 仍 0036_export_jobs/物理表 58/后端 656·eval 101·golden 21 全不变）**：负责人 2026-09-20「好的，开搞A到E」批次 E 项收口。方法＝一次性 PG16（pgvector/pgvector:pg16，5433 避 atlas-pg）容器 `alembic upgrade head` 后导出 information_schema.columns（**627 列**）/ pg_indexes（210 行）/ pg_constraint（FK+UQ 52 条）+ numeric 精度/vector 维度，逐表核对 docs/10 全部「实现补登」段，另以 ORM `Base.metadata` 在 SQLite `create_all` 比对模型⇄迁移。结论：**58 表表名/列名模型⇄迁移零漂移、零单边表**，各迁移新增表数对账闭合（0001=4/0002=8/0003=3/0004=5/0005=6/0006=3/0007=3/0008=7/0009=2/0010=2/0012=2/0014=5/0019·0020·0023·0025·0028·0034·0035·0036 各 1＝58），新三表（effect_records 现 14 列/effect_claims 5/export_jobs 11）、content_products partial unique index `WHERE status<>'discarded'`、numeric（ai_models 12,4 / skill_runs 成本 12,6）、vector(1536) 两列、FK/UQ 约束名全部与登记一致；**仅订正两处旧稿笔误**：content_products.status String24→**String32**（迁移 0025/模型本就 32）、tenants.detail JSON→**JSONB（SQLite 变体 JSON）**。复核结论写入 docs/10 头部 + §4，销 handoff「10 待 DBA 复核」挂账；§3 未落地复合索引仍按性能实测后置，不判缺陷。

- **Q136 客户效果批量 CSV 回填（纯前端加法，零后端改动/零迁移；前端 feat 2915bdd（docs cc314d4）、**已 push**；后端 656 测试/eval 101·golden 21/Alembic 头 0036/物理表 58 全不变，前端 tsc+八 checker〔check-content 扩守卫、token 仍 86、CSS 零新增〕、next build 成功）**：负责人 2026-09-20「好的，开搞A到E」点工 D 项，接缝按推荐（甲）拍板记 Q136（02 C1.80），销 Q131「批量表格/CSV 随 V2」挂账。**甲案=纯前端形态，不建后端上传端点、无 PG 迁移、无新 npm 依赖**——浏览器内 CSV 解析+预览+行级校验，一次调 Q128 `POST /api/effects/backfill` 的 `records[]` 整批提交（后端早已 min_length=1·无上限·整批 all-or-nothing·422 detail 带 index·绝不产生孤儿），与单条岛共用同一客户通道/审计/租户隔离/422 回显。① 新增 `(shell)/content/backfill-batch-island.tsx`（"use client"）：固定 9 列表头 platform_post_id,captured_at,plays,likes,comments,shares,inquiries,conversions,read_rate 按列名映射（缺列即表头错误），最小 CSV 行解析（双引号包裹/`""` 转义，不支持字段内换行）；行级校验同单条岛（post 必填、captured_at 支持 ISO/`YYYY-MM-DD HH:MM`/datetime-local、纯日期按本地 00:00、统一 toISOString 带 Z、六计数非负整数、read_rate 0..1、空指标即缺席不补 0），错误按物理行号列出、任一行错禁用提交；前端软上限 MAX_ROWS=500（后端无上限）、预览前 10 行、FileReader/readAsText 文件载入 + textarea 粘贴双入口；成功清空并 router.refresh 显整批 matched 数，失败复用单条岛 failureText；② content/actions.ts 加 batchBackfillEffectsAction + BatchBackfillRowInput（rows.map 拼 content_id 整批走 customerBackfillEffects，空 rows 422、unconfigured 同单条）；③ 详情页非 discarded 片段内单条岛后挂批量岛，zh-CN +13 个 backfillBatch* 键，CSS 零新增；④ check-content.mjs 扩守卫（13 消息键+占位符、必需文件、挂 BatchBackfillIsland、action token、岛纪律循环、FileReader/readAsText/toISOString/all-or-nothing token、禁直连）。**挂 V2**：服务端文件上传端点（multipart/大文件流式/大小闸）、Excel(.xlsx)、列名映射/多模板/编码探测、服务端行数硬上限与异步导入任务（同 Q132 queued/running 形态）、完整 RFC4180 字段内换行、导入结果逐行回执（当前整批 all-or-nothing）。

- **Q133–Q135 多副本 V2 基建三件（纯代码零迁移；C-1 feat c48a003/test 08951f4、C-2 feat f255856/test 80da0c2、C-3 feat 032bab6/test 038b376 原子提交（docs 4a11d60）、**已 push**；Alembic 头仍 0036/业务物理表 58，后端 633→656 测试〔+23 集成〕/ eval 101·golden 21 不变，ruff 净，前端零变化〔tsc+八 checker、token 86〕）**：负责人 2026-09-20「好的，开搞A到E」点工 C 项，销 Q89–Q97 与 docs/07 §7.5 长期挂账「fencing token / Redis Streams 细粒度认领 / 配置缓存多副本广播」的**原语层**（纪律同 Q88/Q91：纯 Redis 原语、env 默认关、FakeRedis 内存替身测试、只落原语不改变 V1 行为、不建占位端点）。① **Q133 fencing token（02 C1.77）**：locking 新增 `leader_lease()`/`LockLease`——抢锁 SET NX PX 成功后 `INCR loom:fence:{name}` 取跨易主全局单调整数令牌、序列键不设 TTL 防回绕；INCR 失败释放刚取得的锁并 LockBackendError fail-closed；看门狗续约返回 0（易主）或续约期 Redis 故障 → `_mark_lost()`+error，持锁方关键步骤 `raise_if_lost()` 协作中止；`leader_lock` 重构为薄封装保持 Q89 布尔语义（关闭 yield 字面 False、中途易主仍只 log 不打断），两循环两端点零改动；+8 集成（test_fencing_lease.py，test_distributed_locks.py 局部 FakeRedis 补 incr 后原 13 例零回归）。② **Q134 Redis Streams（02 C1.78）**：新横切包 `app/core/queue/`（streams.py）落 `add_event`/`ensure_group`/`read_new`/`ack_event`/`reclaim_pending`/`move_to_dead`/纯函数 `delivery_exhausted`（流 loom:stream:restock、消费组 restock-workers、死信流 :dead 附 _dead_reason/_dead_origin_id、读新 XREADGROUP ">" 即入 PEL、XCLAIM idle 60s 崩溃接管、MAX_DELIVERIES=5、近似 MAXLEN 10000、COUNT 默认 20），StreamBackendError fail-closed、override_stream_client 懒连注入，**不接 worker 主路径、不落 env、无占位端点**；+8 集成（test_redis_streams.py，FakeStreamsRedis 六命令）。③ **Q135 配置缓存广播（02 C1.79）**：新模块 config_center/broadcast.py——配置事务 after_commit 在本进程 config_cache.apply 后 `spawn_invalidation(key)`（同步钩子取 running loop 起 fire-and-forget PUBLISH loom:config:invalidate，模块级 _bg_tasks 保强引用防 GC，门控关直接 return 不触 Redis），广播 **best-effort**（区别于锁 fail-closed：事务已提交，PUBLISH 失败只 error 不上抛）；ConfigBroadcastSubscriber 后台任务（start/stop/running 同 SweepScheduler）pubsub 订阅、get_message 轮询 1s、收消息用独立会话 config_cache.reload() 全量重载，Redis 故障丢 pubsub 下轮懒重连、reload 异常只告警不自毁、CancelledError 透传保证 stop；env `LOOM_CONFIG_CACHE_BROADCAST_ENABLED` 默认 false，main lifespan 开启时启停（启动失败 warning 不阻断应用）；+7 集成（test_config_broadcast.py），既有 config 测试零回归。三件均无 PG 迁移（头仍 0036/物理表 58）、无新配置键（Q135 仅 env 开关，按 Q89–Q91 先例不入 backend/.env.example）、前端零改动。**挂 V2**：fence 下游 PG 行级条件更新与两循环 tick 改 leader_lease 协作中止、Streams 的 XADD 生产接线与消费组 worker 循环（水平并行/并发度，含 Q132 queued/running 真后台导出 worker、与 Q90 退避游标/Q133 fencing 协同）、广播单 key 增量失效与断线退避/版本号防陈旧、真 Redis 多副本集成验证。




- **09-20 进度归档（2026-09-20 滚出 Q128 customer-backfill、Q129 批量认领/解绑、Q130 管理端认领台、Q131 客户回填岛四条；续写滚出 Q132 中台导出 JSON/异步任务一条，原文见该档）** → [docs/handoff-archive-2026-09-20.md](docs/handoff-archive-2026-09-20.md)。
- **09-18 ~ 09-20 进度归档（2026-09-19 滚出 Q118–Q123 共七条；2026-09-20 续写滚出 Q124 作废回池/Q125 发布回填/Q126 effect-callback 入站/Q127 孤儿认领四条，原文均见 09-19 档）** → [docs/handoff-archive-2026-09-19.md](docs/handoff-archive-2026-09-19.md)。
- **09-13 ~ 09-17 进度归档（2026-09-18 按归档惯例滚出；Q113 及更早：M0–M12 各切片 / WF 替换 / 五 LLM 站点 / 工程基建 / 前端 / 契约回填等约 40 条，原文见归档文件）** → [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md)；权威逐条台账：docs/02（Q73 起 C1.17）、docs/08 各 Q 完成情况、docs/16 测试台账、CHANGELOG [Unreleased]。
## 待办 / 下一步（按依赖顺序）

1. **M0 规格仲裁（08 §2.1）— ✅ 2026-09-13 完成**：Q2/Q22/Q60(a/b/c) 转定稿；Q22a/Q22b 评分分项规格新裁决并同步 01/06/§C2；素材采纳经负责人确认（02 C1.16 注记）；段 1-6 缺口实体 ProductSpace / g2FieldCandidates 已补字段（04 §2.2/§2.6，同步 10）。挂账不阻塞 M1 的 4 个子项：PS.lifecycle 迁移触发条件、~~18 通用字段值归属（M1 前定）~~ ✅ Q74 已裁决、候选实体多租户可见性、候选状态完整枚举。KUP-PROPOSE 随第二阶段段 13 补。
2. **技术选型下游同步 — ✅ 2026-09-13 完成**：14 六选型拍板（模块化单体/自研编排器/PG+pgvector+Redis/DB+广播热更新/进程内合规模块/质量两件套）；技术栈同日确认（Python 3.12 + FastAPI / React + Next.js + TS / YAML 声明式编排）；已刷新 15（目录+技术栈）、17（中间件/部署）、08（新增 M10-Q 质量两件套）、10（§1.3 存储拓扑 + 配置中心热更新 + 辅助系统 MVP 范围）、06（§4.2 编排器选型）。
3. **排期填表（08 §2）— 🟡 骨架已搭、实质待填（2026-09-17）**：路线图已合并（Q73），V1 任务包为 M1–M8、M10、M10-Q、M11（段7/8 静态底表）、M12（平台厚度）；08 §2.2 已新增「V1 落地状态总览/排期骨架」表，逐包落地状态/关键 Q·迁移/剩余挂账已据 handoff 与完成情况注记填好。**各包工期/人员/计划起止原文未给，仍待负责人提供资源信息后填写（禁止倒推编造）**。注：代码已按 15 大规模落地（M1–M12 多切片，见"当前状态"），"排定后才搭脚手架"的旧表述不再适用，排期表当前作用为进度对账与后续 V2/V3 排期基线。
4. **待核验缺口**：
   - 两个基准 HTML 文件**经核验均不在本机**（2026-09-13，仓库目录 + Spotlight 全盘 + git 全历史）：
     ① `Loom_SaaS后台全景图.html`——docs/README 第 3/38 行称"保留在上级目录/`../`（仓库根）"，实际不存在，且从未进入 git；
     ② `Docs/Loom_后台_V6.0-需求说明（不是原型).html`——AGENTS.md 行号溯源（`line NNNN`）基准文件，`Docs/` 目录本身不存在。
     被 8 个文档引用（AGENTS / README / MIGRATION_CONVENTION / handoff / docs 01·09·12·README）。**✅ Q115 已收口（2026-09-17）**：负责人确认 V6.0 HTML 彻底丢失无法找回——line 溯源降级为历史痕迹、12 PT 名单定为最终名单（7+35）、触发方永久标【待补】、G2 fid 改判工程定稿（迁移 0024 落 12 字段）；不擅自修改或移除这些历史引用。
   - ~~12 的 PT 占位候选名单待与基准 HTML 核对~~ ✅ Q115 已收口（2026-09-17）：7 定稿 + 35 推断候选定为最终名单，35 候选保留【推断】标注；
   - ~~11/13 的接口与迁移行待 M0 裁决后补齐~~ ✅ Q112 已补（2026-09-17）：13 号 productIntake15 七处迁移行据 M1 实现补登（G1 审核流随 V3、PWC 复核/待入库维持【待补】）；11 号白名单消费/CSV/Key 治理按已落地端点回填（effect-callback 本体随段13 P3、中台 API/SDK 随 V2/V3、DB 浏览/调试台/配额、JSON/异步导出仍【待补】）；
   - ~~10 的建表要素待 DBA 复核~~ ✅ **首轮 DBA 复核已完成（2026-09-20，E 项）**：一次性 PG16 容器 upgrade head 实测 + ORM 比对，58 表零漂移，订正 content_products.status String32、tenants.detail JSONB 两处笔误，结论见 docs/10 §4。
5. **仓库基建 — ✅ 主体完成（2026-09-18；详细原文见 [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md)）**：git（dev/main，GitHub origin，提交规范见 git-commit-message.md）；脚手架按 15 落地；venv=backend/.venv（uv + Python 3.12）；pytest 699 绿（**必须在 backend/ 目录执行**——根目录跑会因 pythonpath/testpaths 失效误报）、ruff 净、迁移头 0037_restock_claims（0032–0037 经 pg16 容器实测，Q141/Q142 零迁移、Q143 迁移 0037 restock_claims 业务表 58→59）、eval 101/101、前端 tsc+八 checker（Q122 起含 check-content，Q130 起 sidebar 8 项）、next build 通过、token 86；浏览器人工验收通道 chrome-devtools MCP 已注册本仓（约定见 15 §4）。

6. **~~2026-09-17 文档⇄代码一致性审计发现的挂起项~~ — ✅ 2026-09-18 Q118 全部收口（02 C1.62；详细原文见 [docs/handoff-archive-2026-09-18.md](docs/handoff-archive-2026-09-18.md)）**：① 4 个裸 GET 补 query actor 闸（content-goals / cp-law-domains / publish-slots / slot-type-defaults）；② publish_slots.gate 回填 V1 仅 Q42 人工直编口径；③ product_spaces 两 FK（迁移 0027）；④ CI 六→七 checker；另修 Alembic 版本表列长。

7. **private beta 上线准备 / 段12·V2 余量 — ⬜ 待点工（2026-09-21 Q144 后，首发形态已裁＝受控 private beta）**：上线前三件（Checklist 与工时见 08 §2.2 Q144）——① ✅ **真 LLM 接线已落地（Q148，2026-09-21，裁决 B 完成）**：agnes 网关 + agnes-2.5-flash + USD + 50 USD/天，8 chat 场景真模型、Fernet Key、真实调用冒烟通过（单价待供应商给、ATOM-AFFINITY 维持 synthetic）；② 首批平台主数据：publish_slots 零种子——**业务方按 docs/19 模板回填（至少 1 平台 1 发布位 + PCP + 三包）——为 #6 唯一剩余阻塞，禁工程臆造**；（#3 阈值/G2 已确认 ✅ Q149：72h/24h/48h、0.85、0.9、12 字段基线，付费档额度划掉）；③ ✅ **最小部署制品已落地（Q145，2026-09-21）**：前后端 Dockerfile、compose 含 app（backend 内网不暴露、frontend 唯一入口）、entrypoint 启动前自动迁移、健康检查；**备份监控最小集（#5）✅ 已落地（Q146，2026-09-21）**：每日 pg_dump+7 天保留、日志轮转、watchdog 探活+webhook 告警；异机备份/恢复演练/指标监控随 V2。**#6 beta 验收：2026-09-21 已完成 staging 彩排（Q147）——部署/迁移/备份/监控/SSR/段1 冒烟全通过；验收仍阻塞，彩排库 publish_slots=0，① B 已就绪（Q148）、#3 阈值已确认（Q149），**仅须待 ② 主数据回填后跑真实 final_id****。~~公网自助上线 P0-① 身份层~~ 已裁**压到 V2 第一项**（登录/会话或签名 Token + actor↔tenant 绑定）。续片候选：⑤ 效果反哺 KUP 与 Q54/Q57/Q61/Q65 校准算法（**口径原文未给【待补】，禁止臆造公式**）；⑥ ~~回填批量表格/CSV 导入~~ ✅ **Q136 已落**（02 C1.80，2026-09-20：纯前端批量岛 CSV 解析+行级校验+预览，整批走 Q128 records[]，无后端端点/迁移/依赖；服务端上传端点/Excel/异步导入任务/逐行回执随 V2）；⑦ Agent 抓取/发布自动化明确不做（Q62 本系统不抓取，发布由 Q125 运营回填）。其他待点工：段12 视频支线 video-studio、复检余两项（施工指令核对/国家规则核对，规则原文未给）、语义四 code 与 QC 阈值 0.85 的业务/法审校准、DIM-SOURCE 七路连接器、~~CSV JSON 形态/异步导出~~ ✅ **Q132 + Q137 已落**（02 C1.76/C1.81；JSON 形态 + 导出任务记录，queued/running 真后台 worker 经 Redis Streams 消费组落地〔env 默认关〕+ `GET /jobs` 列表口；~~大结果集分页/行数硬上限~~ ✅ **Q142 已落**（02 C1.86，limit/offset 分页 + env `LOOM_EXPORT_MAX_ROWS` 默认 10 万硬上限、JSON envelope 加 total/limit/offset/has_more、CSV 走 X-Export-* 头；6 层原料包全量 JSON/多 ExportWorker 并发度随 V2）、~~V2 基建（Redis Streams/配置缓存广播/fencing token）~~ ✅ **Q133–Q135 原语 + Q137–Q140 生产接线已落**（02 C1.77–C1.84，2026-09-20；导出消费组 worker/restock 生产侧 XADD/两循环 lease 协作中止/缓存单 key 增量+退避齐、~~配置版本号/TTL 防陈旧~~ ✅ Q141 已落（02 C1.85）、~~fence 下游 PG 条件更新（restock 花钱路径）~~ ✅ Q143 已落（02 C1.87，迁移 0037）；restock 消费组水平并行〔花钱隔离〕/SLA sweep 行级 fence/真 Redis 多副本验证仍随 V2）、付费档额度·计费订阅·团队账号。均须负责人点工，不自动开工。

8. **A2A 封臣接入（Zeus 联邦）— ✅ 第一阶段 plan 模式已落地（Q150，2026-09-21）**：负责人裁决四项全按草案（接入不阻塞 #6 / skills＝generate-content·compliance-check·effect-backfill / 复用 Q88 Agent Key / 租户维度 _platform）。`backend/app/core/a2a/`（card·skills·rpc·router）+ 12 项单测全绿，详见 02 C1.94 与 [docs/design-a2a-vassal.md](docs/design-a2a-vassal.md)。边界：任务进程内存存储、不触链不花 token 不越 Gate、未真机联调（待部署）。

## 关键约束（接手者不得违反）

- **唯一事实源** = docs/；不得以摘要/记忆/二手来源替代文档原文。
- **不擅自裁决**：原文挂起的三项（两套路线图合并、业务方素材采纳、技术选型）均已由负责人拍板（2026-09-13，Q73 / 02 C1.16 / 14）；今后新出现的挂起事项仍须由用户/负责人拍板，禁止静默替换或默认值，并在 02 追加 Q 记录。
- **事实纪律**：文档空缺处标注【原文未给出，待补】，禁止编造业务事实；数字/比例/状态词改动需溯源（line 行号 / Q 编号）。
- **两套口径**：13 段链（权威）与展示口径（03-A~E）分置 01/09，不混用。
- **AI 治理红线**（02 Q66 等）：AI 只产候选，人工 Gate 裁决；红线操作（如 PWS 冻结）需指定角色 + 人工 Gate。

## Conventions（handoff 归档惯例，carry over from archive）

- **commit 消息**：英文、Conventional Commits `<type>(<scope>): <subject>` 格式；不加 `Co-Authored-By: ...`；不 `push`（除非用户明确说）
- **commit 颗粒度**：原子提交；一次 commit 解决一件事（bug 修复 / 单一 feature / 单一迁移 / 单一文档主题）
- **主文件容量上限**：本文件只保留项目当前状态 + 活跃待办 + 最近 5 条进度；超过 5 条的进度条目滚到 `docs/handoff-archive-YYYY-MM-DD.md`，本文件只留一行结论 + 链接
- **完成项归档动作**：编号待办标 ✅ 后，详细过程记录滚到 `docs/handoff-archive-YYYY-MM-DD.md`，本文件只留一行结论 + commit hash / Q 编号，避免无限膨胀
- **活跃项保留详情**：正在推进 / 还在跑的待办（排期填表、待业务方输入项、待点工切片）详情保留在主文件不动
- **命名格式**：`docs/handoff-archive-YYYY-MM-DD.md`（首次 2026-09-18；同日多次可续写同一档，历史档不覆盖、不删除）

## 交接完成定义

接手者在开始编码前应能明确回答：

- Loom 的 13 段链是哪 13 段？`final_id` 由哪 6 层快照相加、唯一出口是什么？
- 为什么"白名单"是相撞产物而不是手写词表？frozen PWS 在链条中的作用是什么？
- S 级红旗 4 条分别是什么？M0 为什么必须在写代码前完成？
- 两套路线图（A7 vs D9）原来为什么并存？合并裁决（Q73）的结论是什么——V1 为什么不含段 12、段 7/8 在 V1 的最小切片是什么？
- 技术选型 6 项建议分别是什么？当前是"建议"还是"决策"？
- Q1–Q149 里哪些是硬约束（配置化/审计/AI 治理/多租户/成本/不静默生效）？
- 当前文档体系里哪些是"权威"、哪些是"待补/待拍板"？修改一份规格文档的正确流程是什么？
