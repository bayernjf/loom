# Q185：Grafana 数据源自动置备（指向 compose 内的 Prometheus）。
apiVersion: 1

datasources:
  - name: LoomPrometheus
    uid: loom-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
