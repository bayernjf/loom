skill_id: TYPE-MATCH
layer: modeling
version: 1.0.0
input_output_schema:
  input: 已识别类目 + G1 类目模板
  output: 整 C7 解析请求（required_fids + L1 未命中时的 l4_proposals 新字段提案，Q81）
knowledge_deps:
  - G1CategoryTemplate
  - C7 四层兜底（Q6/Q68）
model_tier: ""
thresholds:
  l3_coverage_floor_config: c7.layer3_coverage_floor   # Q6 拍板 0.6
failure_policy: fallback_to_c7                         # 无匹配走 C7 Layer2/3/4
review_rule: skill7 候选 Gate → operations（Q81-3）；新字段转正另走 Q13 dictionary_admin
cost_limit: null
